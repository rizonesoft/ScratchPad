<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="IntelligentNotepad.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <!-- Shell regions in Notepad layout. Owners fill them: MenuBar items are D01 T02 §1,
         the TabView is D01 T01 §3, the editor is D02 T01, the status segments are D01 T02 §4. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>

        <ContentControl x:Name="TabRegion" Grid.Row="0" AutomationProperties.AutomationId="TabRegion" AutomationProperties.Name="Tab region" Background="Transparent" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <MenuBar x:Name="MenuRegion" Grid.Column="0" AutomationProperties.AutomationId="MenuRegion" VerticalAlignment="Center" />
            <Button x:Name="WhatsNewButton" Grid.Column="1" AutomationProperties.AutomationId="WhatsNewButton" AutomationProperties.Name="What's new" Click="WhatsNewButton_Click" VerticalAlignment="Center" Margin="0,0,8,0" Background="Transparent" BorderThickness="0">
                <FontIcon Glyph="&#xE789;" FontSize="16" />
            </Button>
        </Grid>

        <ContentControl x:Name="EditorRegion" Grid.Row="2" AutomationProperties.AutomationId="EditorRegion" AutomationProperties.Name="Editor region" Background="Transparent" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />

        <ContentControl x:Name="StatusRegion" Grid.Row="3" AutomationProperties.AutomationId="StatusRegion" AutomationProperties.Name="Status region" Background="Transparent" />
    </Grid>
</Window>
