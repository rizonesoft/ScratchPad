# Phase run: Phase 1 -- Notepad parity (window, tabs, files, menus, editor)

Opened 2026-09-14. Prior phase file read: `2026-09-13-phase-0.md` closed at 12/12 with no unresolved items; its Lessons (read the shell around the tests, keep the mutation-probe habit, keep stamps quotable) carry into this run.

## Phase repairs

- `validate`: 0 fatal, 0 warnings, 19 adjacency advisories (all pre-existing kinds).
- `plan --check` failed on a stale `build/todo-operator.json` (derived, gitignored); `plan --sync` regenerated it, `--check` green, 0 boxes changed, plan 12/106.
- Resolve table for all 36 Phase-1 rows: `D01 T01 §1` exit 0 (no deps); all others exit 4 with UNMET sets inside Phase 1 (T01 chain §1→§2→§3/§4→…, T02 on T01 §1 plus intra-file, D02 T01 on T01 §1, T02/T03/T04 fanning from D02 T01 §2 with two edges to D01 T02 §2). Leftovers, not stalls; no exit 1/2, no broken refs, no external blockers: the phase can run to 100% without parking.

## Shipped-row verification

- No open Phase-1 row formally depends on a shipped row (all 36 resolve deps are in-phase), so no spine re-verification is owed. Practically every section leans on the D00 backbone (suites, goldens, loopback, soak); each section's gates re-run those suites, which is the continuous re-verification.

## Gap audit

- Read the phase as the user would use it end to end (window, tabs, files, menus, settings, status, print, editor, find, spell, format) against the coverage table: every D01/D02 coverage row has its TODO section, and every section sits in the phase table. No gaps filed.
- Watch items for section runs (not repairs): (a) ten UI items name `src/Notepad/*.xaml`, but the app project is `src/IntelligentNotepad/` (settled by D00 T01 §2); each section run corrects its own paths. (b) Fidelity lines name per-surface dirs (`main-window/`, `tab-bar/`, …) but the §3 store is flat (`stock/`, `app/`) with 4 captures; each UI section captures its own surfaces first via the refresh procedure, with §1 establishing the pattern. (c) T01 §1's theme item names the D01 T02 §2 store before it exists: §1 drives theme-following with a seam the store later binds to, no dep cycle. (d) Keep table order (all of T01 before T02): T02 §1's dep edge understates its real need for T01 §§2-9 handlers.

## Sections

- `D01 T01 §1`: surface accounting against `stock/notepad-main` (every control resolved): megaphone plus What's New dialog working here; tab strip with tab/close/new-tab to D01 T01 §3 (model §2); File/Edit/View to D01 T02 §1; formatting toolbar (H1, lists, B/I/S, link, table, clear) to D02 T04 §2; writing-tools dropdown to D05 T02 §6 (AI entries); avatar to D04 T02 (account); gear to D01 T02 §3; editor area to D02 T01 §3; status segments to D01 T02 §4. No control left unowned.
- `D01 T01 §1`: recon evidence. Live titles: `Untitled - Notepad`, `*Untitled - Notepad`, `*Implementation.txt - Notepad`, `*hello - Notepad` (dirty `*` prefixes the name); clean-saved `name - Notepad` is the symmetric inference. What's New is an in-window pop-up titled "New in Notepad" with 4 recorded blurbs plus "Start exploring". Our dark body samples RGB(27,34,34) vs stock (39,39,39): Mica both. Probe hygiene: the first probe's blind SendKeys may have typed "hello" into a live window (possible `*hello` window) and may have left a stray Untitled window; all later probes used HWND-diff scoping and closed their own window. Operator: check Untitled windows for stray "hello" text.
- `D01 T01 §1`: UI launch flake now has a signature. Run 34815770936 (a commit touching no UI code) failed `FreshCaptureMatchesGolden` ("app showed no main window" after 15s) and `FirstRunShowsWhatsNewAndMegaphoneReopensIt` (UIA COM timeout in GetMainWindow): both are cold-start app-launch timeouts under parallel-project CI load, with later launches in the same run green. Local: 1 unidentified UI failure in 8 runs, same shape. Not quarantined yet (no §2-scope fix without scope creep): the §3 run hardens UI launch waits (longer GetMainWindow timeouts) or quarantines with this signature, since §3 drives launches heavily.
- `D01 T01 §1`: shipped and stamped. Candidates `8d7c266 93bf74f 79f6786`, review round 1, all lenses approve. Corrections: seed paths (IntelligentNotepad, *Tests, flat store) plus seam persistence. Evidence: UI 9/9, Unit 7/7 locally and in CI both jobs (run 34813967613) after the artifact-proven first-run red (run 34812982991). Watch item for §9: off-screen geometry clamp. Plan 13/106.
- `D01 T01 §2`: recon evidence. Auto-naming proven live: first line only, trimmed, 35 chars, no ellipsis ("spaced out" from padded input; 34/35/36/50-char boundary probed); empty and cleared content show "Untitled"; whitespace-only to "Untitled" inferred. Dirty-dot replaces the tab X (no "Close Tab" button on dirty tabs until hover); single-tab strip shows the X child. Prompt recorded: title "Notepad", body "Do you want to save changes to {auto-name}.txt?" (.txt appended for untitled), buttons Save / "Don't save" / Cancel. Reopen: 11 empty closes and 11 Don't-save closes each yield 0 Ctrl+Shift+T reopens; the model stacks only content-or-path closes that were not discarded, unbounded (no measurable cap; matches §6/§7 session semantics). Watch items: T02 §1's scottsekinger shortcuts link is 404; §3 owns the tab-close prompt dialog and the dirty-dot-vs-X visual.
- `D01 T01 §2`: shipped and stamped. Candidates `f16aeae a0dbe54`, review round 1, all lenses approve. Correction: singular test name twice. Evidence: TabModel 24/24 locally, Unit 31/31 in CI both jobs (run 34816231797); mutation probe red then green. Plan 14/106.
- `D01 T01 §4` watch: when file IO lands, UI-drive saved tab round-trips (open dirty, close with Save answer writing the file, reopen restoring by path). §3 covers untitled UI flows only; saved flows stay model-level until then.
 - `D01 T01 §3`: recon evidence. Ctrl+Tab/Shift+Tab cycle sequentially (not MRU); Ctrl+1..8 positional, Ctrl+9 last tab; right-click menu holds New tab / Close tab / Close other tabs / Close tabs to the right; middle-click closes; prompt is title "Notepad", "Do you want to save changes to {name}? (.txt appended for untitled)", Save / "Don't save" / Cancel; Don't-save close yields no Ctrl+Shift+T reopen (re-verified); tabs do NOT drag-reorder (three negative probes, section corrected); overflow shrinks with no scroll UI (narrow 25-tab: widths 3..128, selected widest); dirty dot replaces the X until hover (per §2 log). UIA names carry ". Modified." / ". Unmodified." suffixes ours does not reproduce (noted gap).
- `D01 T01 §3`: shipped and stamped. Candidate `fea3412`, review round 1 (5 approve, 1 advisory). Evidence: UI 18/18 (TabBar 9/9 across three consecutive local runs), Unit 31/31, Protocol 8/8, Smoke 1/1; CI both jobs green (run 34828749529) with identical counts including the real-mouse drives. Plan 15/106.
- `D01 T01 §3`: implementation evidence. WinUI's TabView strips its own containers on middle-click without raising TabCloseRequested and without touching the model (proven by instrumented runs: no event, no model change, UIA count 0); XAML PointerPressed never fires for middle and a 6-HWND window-proc subclass sees no middle message, so the close is intercepted with a low-level mouse hook that swallows only downs over cached tab bounds plus the paired up. Test-side: testhost is DPI-unaware so clickable points must be queried inside the PerMonitorV2 context with the cursor move (an unaware point at 150% missed into VS Code), and the three real-mouse drives pin the window topmost (repo PinTopmost pattern) so overlapping windows cannot steal the input. Watch items: (a) two transient UI flakes absorbed locally, both unreproduced on rerun (one unknown 8/9 mid-run, one empty TabItems query mid-churn now covered by a polling TabItemAt helper); MainWindowTests keeps direct indexing and may want the same helper. (b) The middle-click miss closed an unknown number of the operator's VS Code tabs before the coordinate fix; operator may want Ctrl+Shift+T there. (c) ContextMenuMatchesNotepad red twice in CI (same dialog assert, run 34829833626 both attempts) while local stays green: suspected SelectTab-to-box-swap race under load; content sets now wait for tab names through the full chain and all six `?.Invoke()` sites assert non-null. Test-only hardening, no product change; the next green CI run re-proves §3's counts.

## Critical events

## Lessons

---

# Run 2 (continued 2026-09-14, via process-plan)

Prior run shipped D01 T01 §§1-3 (plan 15/106 then) and stopped with no closeout or park record. Carry-in: §1 UI-launch flake signature (hardening vs quarantine still open); §2 T02 §1 404 link watch; §3 MainWindowTests polling-helper watch plus ContextMenuMatchesNotepad race (re-proof now local, CI retired); gap-audit watches (a) `src/Notepad/*` vs `src/IntelligentNotepad/`, (b) flat capture store, (c) §1 theme seam, (d) table order.

## Phase repairs (run 2)

- `validate`: 0 fatal, 0 warnings; `plan --check` green at 15/152 before repair.
- Resolve table for all 67 open Phase-1 rows: exit 0 on D01 T01 §4, §9, §11, §17, §24 only; all others exit 4; no exit 1/2, no broken refs.
- **Cycle found and repaired:** T01 §22 (Smart untitled titles, filed today) deped on D05 T02 §6, while D05 T01 gates T01-whole and §6 gates D05 T01-whole: T01-whole → §22 → §6 → T01-whole, which parked the whole of T02/D02 behind Phase 3 and made Phase 1 uncloseable. Split per the groom-plan rule: §22 is now parity-only (First-line titles, deps §2, 3 items, exit 0); the agent half is new D05 T02 §9 (Agent title suggestions, deps §6 + D01 T01 §22, Phase 3 row, 4 items). T02 §8 (palette, deps D05 T02 §6) stays a legitimate Phase-1 leftover: nothing gates on T02-whole. Plan re-synced at 15/153, `--check` green.
- Staleness sweep of the 7 Phase-1 TODO files: T01 Current state still claimed the stub window only, corrected with a dated note (§§1-3 shipped). All other Current-state lines still true. No moot sections, no dead callouts.

## Shipped-row verification (run 2)

- Spine (shipped rows open Phase-1 rows dep on): D01 T01 §§1-3, all resolve exit 3 with `Verified:` stamps naming the section.
- Toolchain repair: global.json pins SDK 10.0.401 with rollForward disabled, but Windows had 10.0.400 and WSL 10.0.112, so nothing built anywhere. Ran `tools/provision.ps1`: SDK 10.0.401 ready in `.tools/dotnet-win-x64`, all dotnet runs use it with `DOTNET_ROOT` plus `DOTNET_MULTILEVEL_LOOKUP=0`.
- Neutral re-proof green: Unit 31/31, Smoke 1/1, Protocol 8/8 (matches §§2-3 stamps). UI 18/18 re-proof for §§1/3 OWED at bedtime: the UI suite drives the real binary on the interactive session and 17:56 EDT is inside operator daytime use.
- Full-regime re-proof 2026-09-14T23:30Z: Unit 82/82, Protocol 35/35, UI 18/18, all on the live machine. No audit-stance review needed: the one red found (UI 12/18 with null tab names) was the uncommitted §28 half's SetName rewire breaking shipped §3 tests, reverted same-turn with a code comment; §28 re-applies at ship with its stock probe.

## Gap audit (run 2)

- Read the phase as the user would use it end to end (launch, tabs, files, dirty, menus, settings, status, print, editor, find, spell, format, grammar). Two gaps filed with evidence: §27 file drag-and-drop onto the window (no section owned shell file-drop; §8 is association plus command-line only); §28 UIA tab names (run-1 recon: stock UIA names carry ". Modified." / ". Unmodified." suffixes ours does not reproduce).
- Checked and covered, not filed: tab tear-off (§9 item 5), paste-as-plain (§3 item 5: every paste strips to CF_TEXT, which composes with §14 since links and fences are plain text), print preview stays a section-runtime concern for §5 (capture comparison decides), overwrite/Insert mode stays a watch item (no stock evidence yet).
- **Corrected 2026-09-14:** the §27 gap filing above was wrong: §8 item 6 already owns Explorer file-drop. §27 (row, body, plan row) removed same-day; nothing shipped under it.
- Full-regime re-pass 2026-09-14T23:30Z: no new gaps. Validate carries zero advisories after the groom; the D02 T01 §16-19 and D01 T02 §7-11 rows filed since sit in this phase table with owners and deps.

## Sections (run 2)

- `D01 T01 §4` (in progress, neutral half tonight): Started 2026-09-14T22:04:41Z. Validation corrections: `tests/Data/` to `tests/Fixtures/` (items 1, 5), open-path threshold owns item 4 (§7 owns the global budget), §4 records the .LOG/F5 format first (§5 matches later), `FileIO.cs` renamed `FileOpen.cs` (CA1724), added already-open-dedup item (focus-existing default). Built: `src/Notepad.Core/FileOpen.cs` (detection, async chunked open, size gate, .LOG, failure map), `FileWatcher.cs`, `TabModel.OpenTab`, 19 fixtures, `tests/Unit/FileOpenTests.cs` (30 tests). Unit 61/61 green, warnings/analysis clean. Ticked items 1, 4(new), 6, 9. OWED at bedtime (operator active, 2s idle): stock probes (failure wording, .LOG format, size threshold plus redirect wording, mixed-EOL rule, UTF-16-no-BOM check, ANSI codepage, focus-vs-duplicate) plus Open-dialog/redirect captures, UI drives for items 2, 3, 5, 7, 8, then review plus stamp plus commit. Recorded defaults awaiting probes: detection order, 1252 ANSI, EOL plurality, case-sensitive .LOG, focus-existing, 1 MiB progress threshold.
- `D01 T01 §9` (in progress, neutral half tonight): Started 2026-09-14T22:14:18Z. Added `openin.mode` to the ShellSettings seam ("new-tab" default matches the stock settings capture; fresh-install default unconfirmed). `MultiWindowTests` 2/2: default plus per-window tab/dirty/closed isolation. No ticks: items 1, 2, 5 need window UI (bedtime, and `resources/baseline/windows/` does not exist yet so the capture ships first); item 3's undo half waits on D02 T01 §4; item 4's restore waits on §6. The stock capture also confirms the "Opening files" label verbatim.
- `D01 T01 §11` (in progress, neutral half tonight): Started 2026-09-14T22:14:51Z. `ApplicationIcon` plus `notepad.ico` content in the csproj; `AppWindow.SetIcon` in OnFirstLoaded (Loaded, not ctor or first-activation: HWND invalid in the former, background launches skip the latter). Build 0 warnings. Item 1 ticked on byte proof: extracted exe icon matches the asset 0/1024 pixels. OWED at bedtime: window plus taskbar captures for items 2-3, then review plus stamp plus commit.
- `D01 T01 §17` (in progress, neutral half tonight): Started 2026-09-14T22:16:45Z. Sufficiency corrections: built-ins named (Blank, Meeting, Journal), `{date}` locale-short plus `{title}` from picker (empty means Untitled, unknown braces literal), customs as `.txt` under `%LocalAppData%/IntelligentNotepad/templates/`. Built `Templates` plus `TemplateStore` (atomic save, name sanitize); `TemplateTests` 5/5, Unit 68/68. Items 2-3 ticked. Night correction: sanitize now uses the explicit Windows invalid set (`Path.GetInvalidFileNameChars` omits `:`/`?` on Linux, which reddened the suite there); Unit back to green. OWED at bedtime: picker dialog UI for item 1, then review plus stamp plus commit.
- `D01 T01 §14` (in progress, neutral half tonight): Started 2026-09-14T22:56:00Z. Sufficiency decisions recorded (top 10 with Ordinal tie-break, word/sentence/repetition rules, short 1-10 / medium 11-25 / long 26+, English stopwords, naive sentence split as recorded limitation) plus count-based benchmark instead of BenchmarkDotNet. Built `TextStats` engine, `ITextProvider` seam, `StatsController` (compute on open, refresh-only after) over `tests/Fixtures/stats/sample.txt`; `TextStatsTests` 9/9, Unit 82/82. Item 4 ticked. OWED at bedtime: panel UI plus drives for items 1-3, then review plus stamp plus commit.
- `D01 T01 §22` (in progress, neutral half tonight): Started 2026-09-14T22:18:27Z. Item 1 ticked: the live first-line default is proven by §2's shipped `UntitledShowsFirstLineOnly`. OWED at bedtime: stock capture confirmation for item 2 plus surface re-confirm, then review plus stamp plus commit.
- `D01 T01 §24` (in progress, neutral half tonight): Started 2026-09-14T22:19:07Z. Validation split: registration needs package identity (unbuildable unpackaged), so item 1 moved to new D07 T01 §7 (deps §1 plus D01 T01 §24, Phase 4 row); the unverified "stock receives nothing" claim corrected. Built `ShareReceiver` plus 3 tests, Unit 71/71. Items 1-2 (receive, decline) ticked. OWED at bedtime: activation wiring drive plus sheet presence via §7's packaged install, then review plus stamp plus commit.
- `D01 T01 §28` (in progress, neutral half tonight): Started 2026-09-14T22:20:48Z. `TabAccessibilityName.For` in core plus both TabBar `SetName` sites rewired to it; 2/2 tests, Unit 73/73, app builds 0 warnings. Punctuation (`. Modified.` / `. Unmodified.`) is a recorded default from run-1 recon. No ticks: both items need host drive. OWED at bedtime: stock punctuation probe, live dirty-tracking hookup, UI drives, then review plus stamp plus commit.

## Critical events (run 2)

- Regime change 2026-09-14T23:15Z (operator): no day/night split, single continuous run, full testing on the live machine in plan order. The 8 neutral halves complete here; D03 T01 §1 parked mid-write (JsonRpc.cs done, JsonRpcTests half done, no csproj ref yet) for phase 2. Stock probe run 00:45Z mostly NO-WINDOW on file opens (titles plus Open-dialog capture worked); §4 owns the harness fix.
- Paused 2026-09-14T23:45Z (operator): icon update, run resumes on their word. Operator replaced `resources/notepad.ico` (131902 bytes, 8 images, valid header) at 01:44Z. §11 item 1's byte proof is stale until re-proven against the new asset at ship.
- Cleanup 2026-09-15T00:10Z: run 1's new-window-only probing had abandoned ~28 tabs (all probe-named: GUID temp files, probe-size-*, encoding fixtures, "My first line" titles debris) plus 2 stuck OK-only error notices across 3 stock Notepad windows, and the leftovers were interfering (size-64m reported a lock error from a still-loading sibling). Closed 25 tabs by interlocked select-plus-Ctrl+W with scroll-into-view (offscreen tabs need it); the 2 wedged notices ignored Enter and Click, so after a full tab inventory proved zero operator content, killed the single owning Notepad process (helpers exited with it, 0 remain). Rerunning probes on the clean slate as run 4. Probe harness reworked tab-aware (OpenProbe/CloseProbe/DumpAllDialogs) and run 2 started: titles reconfirmed plus fail-missing captured ("The system cannot find the path specified.", OK-only dialog) before the pause kill. Rerun from scratch on resume; possible leftover probe tab or dialog in stock Notepad.

## Lessons (run 2)
