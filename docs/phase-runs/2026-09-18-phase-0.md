# Phase run: Phase 0 -- Workspace spine (run 3, 2026-09-18)

Opened 2026-09-18 ~10:05Z via process-plan (user: "commit and push all then process plan"). Carry-in: run2 (`2026-09-17-phase-0-run2.md`, no Closeout, no PARKED) shipped `D00 T01 §§12-13` and died at 03:23 SAST when its own guard heartbeat fired into the live turn (`inbox_delivery_anomaly deferred_token_held`, turn plus background suite run cancelled; session seq 12350-12372). `D00 T02 §8` is in-flight (funnel/no-activate/monitor-2 checkpointed in `6d1a8be`, `OpenInNewWindowModeOpensSecondWindow` failing backgrounded); `D00 T02 §9` is blocked on §8; `D00 T01 §14` (plan reviews) was filed this morning.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings (22 todos, 180 sections).
2. Plan currency: `plan --check` current, 52/175 sections complete (30%).
3. Ready rows: 12 now, 1 elsewhere. First runnable-now per phase: Phase 0 `D00 T01 §14`; Phase 1 `D01 T02 §5`; Phase 2 none; Phase 3 `D08 T01 §1`; Phase 4 `D07 T01 §1`. Elsewhere: `D01 T02 §15` (needs display-session).
4. Blocked rows: 110. Key blockers: `test-backbone §8` gates §9; `permission-apply-ux §6` gates `D01 T02 §8`; `editing-surface §1` gates the editor chain (§§2-13); `menus-settings-status §9` gates §11.
5. Phase started: Phase 0, because it is the first phase with a ready row.

Run guard: a3a92a2b (`*/10 * * * *`, recurring, skips while a run is active). Created fresh; `cron_list` was empty.

**Decided 2026-09-18 (run guard delivery):** the guard is created skip-if-active, against the process-plan canonical text ("fires during active runs"). Reason: the run2 guard fired into a live turn at 03:23 SAST and the runtime cancelled the turn plus its background tasks (measured, seq 12350-12372). A guard that can cancel a live run violates the operator's only-you-stop-a-run rule. Cost of changing back: re-creating the measured killer. Permanent skill fix is owned by the `D00 T02 §9` guard item filed this morning (`97d0267`).

## Phase repairs

- `validate`: 0 fatal, 0 warnings. `plan --check`: current (52/175, 30%). No sync needed.
- Resolve table: `D00 T01 §14` exit 0 (0/6 items, dep §9 shipped); `D00 T02 §8` exit 0 (2/14 items, dep §2 shipped, `needs windows-host`); `D00 T02 §9` exit 4 (unmet §8, same phase; `needs windows-host`) -- leftover, not a stall. No exit 1/2, no broken refs.
- Re-read `TODO-01-repo-and-toolchain.md` and `TODO-02-test-backbone.md` top to bottom: no phase-level staleness. Current-state blocks agree (T01 open §§8-moved/14, T02 open §§8-9); §14's XREF resolves (§9 shipped); §8's Started/item ticks match the tree (`6d1a8be` checkpoint, audit table plus docs gate ticked); §9's XREFs resolve (§8 open, §5 shipped) and carries the morning's guard item.
- Items-count staleness: standing 09-13 Run 1 decision, cosmetic, `--sync` recomputes boxes only. No fix.
- Guard check: plan-owned guard a3a92a2b verified live at phase start (skips while active, per the Decided line above), adopted, not duplicated. Prior run file `2026-09-17-phase-0-run2.md` read: shipped §§12-13, died guard-killed at 03:23 SAST with §8 in-flight; nothing else unresolved.
- No plan edits made in repair.

## Shipped-row verification

- Spine for §14 is T01 §9: `resolve` exits 3, `Verified:` stamp covers it. Checkpoint names no single command (rule fires on a fresh violator, silent on the live tree); re-proved via current state (run2 precedent): self-test 446/446, live `validate` 0 fatal. No downgrade.
- Spine for §8 is T02 §2: `resolve` exits 3, `Verified:` stamp covers it. Checkpoint's CI leg stays retired by the operator's CI-narrowing decision (run2 precedent, world moved not stamp defect). No downgrade, no separate §2 audit.
- §9's dep (§8) is open. No other spine rows.

## Gap audit

- Re-read Phase 0 end to end (pin, scaffold, runners, gates, `dotnet test`, bootstrap, graph checks, panel enforcement plus hardening, hooks, env gates, plan reviews, seven harnesses, focus-free fence, nightly run): the chain is complete. New since run2 is owned where it landed (§14 owns plan reviews plus the fallback matrix; the §9 guard item owns skip-if-active). The §8 backgrounded failure is owned by §8's own items. No gaps filed.

## Sections

- `D00 T01 §14`: shipped and stamped. Skill gains the post-stamp plan-review step (GPT-high round over section plus neighbors, `add-todo` synthesis, fallback chain onto Opus high with record-and-continue) and the panel mirror rung (Opus unreachable runs the lenses via GPT medium; both fail stops with no stamp); validator accepts the `GPT panel` fallback record (4 verdicts plus `Opus outage` note, last of either family governs); README row extended; self-test 454/454 (+8 §§29-36). Contract corrected before build (Why gains the mirror rung, checkpoint rewritten to gate item 4) plus one review correction (Opus-governs to last-wins). Candidates `5eaef41` (ship) + `2bf7f1f` (R1 fix) + `bb801bf` (R2 fix) + `3295cd0` (stamp); review rounds 1-3, final all approve; findings `docs/reviews/00-workspace/D00-T01-s14.md`. Evidence: GPT-high/Opus-high/GPT-medium `runner-ok` probes, bogus-model 400 reaching the Opus rung, §36 mutation-proven, live drives A-D reverted clean. First live plan review run post-stamp (15 findings, 10 filed as §15, 5 rejected with reasons).

- `D00 T01 §15`: shipped and stamped. Rule 17 enforces the `Plan review:` marker (post-2026-09-18 stamps, cutoff shared via the graph module); skill gains after-panel-close timing (plan review renumbered to step 7, stamp to 8, audit to 9), reverse-dependents scope, 600/900 timeouts, same-family label, evidence citation, clean-record shape, safety exception, manifest plus ledger rules; `query plan-health` reports five dimensions plus unreadable-files; fence stripper extracted verbatim from rule 16 into the shared module; self-test 469/469 (+15). Contract corrected before build (marker timing, verifiable Done-whens, ledger shape, mechanical dimensions). Candidates `a643242` (ship) + `6d0957c` (self-fix) + `7179c90` (R1) + `b323364` (R2) + `4e8ee55` (R3) + `0c174cd` (R4) + stamp; review rounds 1-5 (FINAL), all approve; findings `docs/reviews/00-workspace/D00-T01-s15.md` (rounds reordered chronological for last-wins after a scrambled insert). Evidence: TODO-07 marker plus query fixtures, §36-style mutation discipline, rule-17 live drive reverted, reverse-dep grep plus 124-hang quoted. Stamp carries the first voluntary marker. Second live plan review run post-panel-close (21 findings, 15 filed as §16 across 14 items, 5 rejected, ledger live).

## Critical events

## Lessons

## §19 shipped 2026-09-18T13:57Z

D00 T01 §19 stamped (`2ac8396`), plan 58/184 (32%). 22 work items plus commit: run identity, scope bound, ledger history (rule 22), marker lineage, partial outage, escalation, owner/due, fix-commit clearance with touch leg, recursive reopen, ledger blocks (LIKE retired), checked fencing (`fence` subcommand, both templates rewired), provenance mandate, schema plan-health/2 (total keys, typed), gate mode, migration note, lifecycle policy, §17 checkpoint reconciliation, battery citations, count cross-check plus grammar, dispatch coverage, scored record 21-23. Self-test 539 to 619 (80 new). Panel: 5 Opus rounds (R1 output off-shape, prompt fixed; R2-R5 findings all fixed in-loop; R5 closed by touch-up, no carry-forward). Plan review (gpt-5.6-sol high, first fenced plan pipeline): 23 findings, 16 filed across new §§20-23 (four-way split per PR21), 7 rejected. Runner note: installed `codex exec` rejects `--reasoning`; used `-c model_reasoning_effort='"high"'` (filed in §23).
