# Phase run: Phase 0 -- Workspace spine (run 4, 2026-09-18)

Opened 2026-09-18 ~23:15Z via process-plan (user: "process plan"). Carry-in: run3 (`2026-09-18-phase-0.md`, no Closeout, no PARKED) shipped through `D00 T01 §§14-23` plus `§§35/40` and died or stalled with `D00 T01 §24` in flight (Started 18:38Z, validator lineage flags uncommitted). This run committed that in-flight work as `e7b1b5a` per operator order and adopts §24.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings (24 todos, 215 sections).
2. Plan currency: `plan --check` current, 64/210 sections complete (30%).
3. Ready rows: runnable-now across phases; first per phase: Phase 0 `D00 T01 §24`; Phase 1 `D01 T02 §5`; Phase 2 none listed; Phase 3 none listed; Phase 4 `D07 T01 §1`; plus `D08 T01 §1` voice. Elsewhere: `D00 T03 §1` (needs display-session).
4. Blocked rows: `test-backbone §9` on §8; `readme-and-github §§2-3` chained on §1; `permission-apply-ux §6` gates `D01 T02 §8`; `editing-surface §1` gates its chain; `menus-settings-status §9` gates §11.
5. Phase started: Phase 0, first phase with a ready row.

Run guard: 7baa40f5 (`*/10 * * * *`, recurring, skip-if-active per run-3 decision). Heartbeat 23:22Z confirmed live (run file plus session log fresh); no guard action, run continues. Single writer confirmed: no repo writes in 30 min except this run's filing, no repo processes, peer session `flint-albiorix` unreachable (message target_unavailable) with its log idle ~24h.

## Phase repairs

- Refs: all 21 open Phase 0 rows resolve (16 T01 exit 0; T02 §8 exit 0 with `needs windows-host`, §9 exit 4 on §8 same-phase; T03 §1 exit 0 runnable-elsewhere on display-session, §§2-3 exit 4 chained). No exit 1/2, nothing to repair.
- Staleness scan §§24-41 plus T02 §§8-9: all items reference shipped machinery; nothing moot, no dead blockers. Per-section validation re-runs inside each `process-todo-section`.
- Leftovers parked in place: T02 §9 (waits §8), T03 §§1-3 (display host chain).

## Shipped-row verification

- Spine stamps all exit 3: T01 §§9,12-23,35,40 plus T02 §2 (covers every open row's stamped deps).
- Checkpoint layer re-run: `self-test` 746/746 green, `validate` 0 fatal, `plan --check` current. No regressions; no audits opened.

## Shipped-row verification

## Gap audit

## Sections

## Critical events

- 2026-09-18 ~23:28Z: reported mid-run workspace move from `/mnt/r/conclave/projects/ScratchPad` to `/mnt/r/GitHub/ScratchPad` was wrong (the tree never moved and the new path never existed), so the guard recreated for the new root pointed at a phantom path. Corrected 2026-09-19: tree verified intact at the original path (commits through `51e97f3`, validate 0 fatal), guard deleted, run session stopped by operator order.

## Lessons
