# Phase run: Phase 0 -- Workspace spine (run 2, 2026-09-17)

Opened 2026-09-17 ~23:05Z via process-plan (user: "process plan"). Carry-in: Phase 0 closed 13/13 earlier today (`2026-09-17-phase-0.md`); four new rows landed since (`D00 T01 §§12-13`, `D00 T02 §§8-9`), plus two uncommitted-state commits already on the tree (`dc2dbc7` focus-free UI conversion = §8 work, `3e59883` print engine = Phase 1 §5 work) with no stamps. Run file takes the `-run2` suffix because the canonical `2026-09-17-phase-0.md` name already holds a closed run, and the guard's stall test keys on the absence of a Closeout header.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings (22 todos, 179 sections).
2. Plan currency: `plan --check` current, 50/174 sections complete (29%).
3. Ready rows: 14. First per phase: Phase 0 `D00 T01 §12`; Phase 1 `D01 T02 §15` (run 5 deferred it host-blocked despite exit 0, re-check at Phase 1 time); Phase 2 none; Phase 3 `D08 T01 §1`; Phase 4 `D07 T01 §1`.
4. Blocked rows: 110. Key blockers: `repo-and-toolchain` whole-TODO (needs §§12-13) gates all of D03; `D02 T01 §1` gates the editor chain; `test-backbone` whole-TODO (needs §§8-9) gates D06 T01.
5. Phase started: Phase 0, because it is the first phase with a ready row.

Run guard: 604d309b (`*/10 * * * *`, recurring, fires during active runs). Created fresh; `cron_list` showed no prior guard (earlier guards 06a5e3ff/46f48c1d are gone with their sessions).

## Resume audit (process-plan, 2026-09-17 ~23:10Z, user: "process plan")

1. Graph: 0 fatal, 0 warnings (22 todos, 179 sections).
2. Plan currency: `plan --check` current, 50/174 sections complete (29%).
3. Ready rows: 14. First per phase: Phase 0 `D00 T01 §12`; Phase 1 `D01 T02 §15`; Phase 2 none; Phase 3 `D08 T01 §1`; Phase 4 `D07 T01 §1`.
4. Blocked rows: 110. Key blockers unchanged from open: `repo-and-toolchain` whole-TODO (needs §§12-13) gates all of D03; `D02 T01 §1` gates the editor chain; `test-backbone` whole-TODO (needs §§8-9) gates D06 T01.
5. Phase started: Phase 0, first phase with a ready row. Adopting this file (no Closeout, no PARKED) as the resumed run's findings file.

Run guard: a06cdf62 (`*/10 * * * *`, recurring, fires during active runs). Created fresh; `cron_list` showed no prior guard (604d309b was deleted at the operator stop, per the stop rule).

## Phase repairs

- `validate`: 0 fatal, 0 warnings. `plan --check`: current (50/174, 29%). No sync needed.
- Resolve table: `D00 T01 §12` exit 0 (0/3 items, no deps); `D00 T01 §13` exit 0 (0/6 items, dep §7 shipped); `D00 T02 §8` exit 0 (2/7 items, dep §2 shipped, `needs windows-host`); `D00 T02 §9` exit 4 (unmet §8, same phase; `needs windows-host`). No exit 1/2, no broken refs.
- Re-read `TODO-01-repo-and-toolchain.md` and `TODO-02-test-backbone.md` top to bottom: no phase-level staleness. Current-state blocks agree (T01 open §§12-13, T02 open §§8-9 in-progress); §13's XREFs resolve (§7 shipped, D01 T02 §15 open); §8's Started/item ticks match the tree (`dc2dbc7` on main); §9's XREFs resolve (§8 open, §5 shipped).
- Items-count staleness (plan table vs checklists: T01 §1/§13, T02 §6/§8/§9): standing 09-13 Run 1 decision, cosmetic, operator call, `--sync` recomputes boxes only. No fix, same as the stopped pass.
- Guard check: plan-owned guard a06cdf62 verified live at phase start, adopted, not duplicated. Prior run file 2026-09-17-phase-0.md read: closed 13/13, nothing unresolved.
- No plan edits made in repair.

## Shipped-row verification

- Spine for §13 is T01 §7: `resolve` exits 3, `Verified:` stamp covers it. Checkpoint names probe pushes, not a command; re-proved via current state instead (09-17 precedent): self-test 422/422, live `validate` 0 fatal, `plan --check` current, CI plan-gates success on the two latest main commits (`c274f79`, `e070cac`). Gates hold; no downgrade.
- Spine for §8 is T02 §2: `resolve` exits 3, `Verified:` stamp covers it. Checkpoint's CI leg was retired by the operator's CI-narrowing decision (world moved, not a stamp defect); the suite was green on the Windows host at §6 time (09-17) and the current tree breakage is §8's own in-flight commit (see Critical events), so live re-proof rides §8's own checkpoint. No downgrade, no separate §2 audit.
- §12 has no deps; §9's dep (§8) is open. No other spine rows.

## Gap audit

- Re-read Phase 0 end to end (pin, scaffold, runners, gates, `dotnet test`, bootstrap, graph checks, seven harnesses, hooks, env gates, focus-free fence, nightly run): the chain is complete and each new row is the filed gap for its own hole. The red HEAD build is §8's open work, not a gap. No gaps filed.

## Sections

- `D00 T01 §12`: shipped and stamped. Hook validates the staged tree in a temp index checkout (LF-pinned, fall-through python probe, git-dir temp, advisory drift warning); provisioners wire hooks up front with warn-and-continue; plan.yml asserts hook mode, blob LF, and eol pin; bootstrap carries the one-time step. Contract corrected before build (SOURCE interval 4 minutes to 22 seconds) plus two review Decided lines (staged scope with temp-index mechanism, warn path). Candidates `01fb330` (ship) + `d4e1f9c` + `2fa3f30` + `5a8a2a2` + `43f32e9` (stamp); review rounds 1-3, final adversarial/consistency/integration approve, record advisory (candidate list completed in the stamp); findings `docs/reviews/00-workspace/D00-T01-s12.md`. Evidence: T1 refused, T2 allowed with warning, T3 0s silent, T4 refused via the measured temp index, W1/W2 green on real Windows git, assert step positive plus negatives, plan-gates success on the ship push; plan-gates success on the stamp push (new assert step green in CI).

- `D00 T01 §13`: shipped and stamped. `**Requires:**` marker (closed `display-session` vocabulary, required reason) with parser, two FATAL rules, SESSIONNAME detector (session 0 excluded), `query ready` now/elsewhere split with `--context`, `resolve` verdict, and snapshot `first_ready` filter; §15 marked as the proving instance; runnable-now rules in process-plan/process-phase/process-todo-section plus authoring prompts in add-todo/create-todo. Candidates `21c53b2` (ship) + `ceae321` + `5c29b83` + `3b0653b` + `2a697a4` + `3f922ce` + `9a20476` + `8edd375` (findings) + `a8ceaed` (stamp); review rounds 1-5, final adversarial approve, consistency/integration advisory (both fixed post-panel in the tidy), record approve; findings `docs/reviews/00-workspace/D00-T01-s13.md`. Evidence: self-test 446/446 (+24 cases), validate 0 fatal 0 warnings, live split 12 now plus §15 elsewhere named, `--context display-session` lists 13, Phase 1 first_ready moved §15 to §5 (quoted before/after), plan current 52/174.

## Critical events

- 2026-09-17 ~23:25Z: operator said "stop now" during Step 1 repair. Guard 604d309b deleted first; run halted before any section work. Tree: only the untracked run file is new; no section commits made. Resume point: Phase 0 repair (5 stale Items counts identified, fix not applied) then ship §12, §13; §8/§9 host-blocked (interop denied).
- 2026-09-17 ~23:10Z: resumed via process-plan (user: "process plan"). Guard a06cdf62 created before any other step.
- 2026-09-17 ~23:11Z: interop is LIVE in this session (`cmd.exe /c "echo interop-ok"` via the default socket), so the stopped pass's "interop denied" verdict does not carry over; §8's `needs windows-host` is satisfiable until proven otherwise.
- 2026-09-17 ~23:12Z: HEAD (`dc2dbc7`, §8's in-flight commit) is red on CI build-windows (run 35278760497, build-linux success): `tests/UI/HookFactAttribute.cs(9,34)` CS0509 (cannot derive from sealed `InteractiveFactAttribute`) plus `tests/UI/TabBarTests.cs(430,6)` CS0616. Named cause, owned by open §8; the fix lands there as fix-forward. Ship order stays table order (§12, §13, then §8): §12/§13 gates do not depend on build-windows green, and each commit body will name the pre-existing red.

## Lessons
