# Phase run: Phase 0, 2026-09-24

Runner: Claude Code (claude-opus-5-5), the only writer (AGENTS.md). Entered through process-plan with no phase argument, so the plan chains to the next ready phase after this one closes or parks.

Plan-level audit (process-plan step 1):

1. Graph: 0 fatal, 1 warning (known owner-login mapping).
2. Plan currency: `plan --check` current, 111/290 sections complete (38%).
3. Ready rows: 49 runnable now, 0 runnable elsewhere. Per phase: Phase 0 first `D00 T02 §19` (34 rows); Phase 1 first `D01 T01 §34` (10); Phase 2 first `D03 T01 §1` (1); Phase 3 first `D08 T01 §1` (1); Phase 4 first `D07 T01 §9` (3).
4. Blocked rows: 130, all on unshipped Depends (the six cross-phase waits are noted in docs/plans/2026-09-23-groom.md).
5. Starting Phase 0 first: it is the first phase with a ready row and holds 34 of 49. Provision verify 9/9 green.

Run guard: heartbeat job `8943e823` (`3-59/5 * * * *`, session-only, 7-day expiry); guard file `build/claude-campaign-guard.json` written for this session. The prior Phase 0 run (`docs/phase-runs/2026-09-22-phase-0.md`) parked when its writer was retired; its unresolved input is this run's first input.

## Phase repairs

- `validate`: 0 fatal, 1 warning (known owner-login mapping). `plan --check`: current.
- Resolved all 39 open Phase 0 rows in one `classify` pass: 34 exit 0; 5 exit 4, all leftovers or in-phase unlocks: `D00 T02 §16` (waits on `D01 T01 §34` plus `D01 T02 §16`, phase 1), `D00 T03 §2`-`§4` (unlock in order after `D00 T03 §1`), `D00 T03 §5` (waits on `D05 T01 §3`, phase 3). No exit 1 or 2 broken refs.
- Phase-level staleness read: covered by the whole-tree groom of 2026-09-23 (`docs/plans/2026-09-23-groom.md`, commits c5850ac, f60f02a, c4f3487), finished hours before this run; no new staleness found.

## Shipped-row verification

- Machine checkpoints the open rows build on, re-run on HEAD: self-test 1599 cases (one run read 1 failed, the immediate rerun 0 failed: an intermittent Windows flake, filed as `D00 T04 §27`), probe self-test 9/9, probe live ok, validate 0 fatal. Behavior checkpoints (builds, nightly runs, captures) stand on their stamps; nothing read contradicts them.

## Gap audit

- Covered by the 2026-09-23 groom (five domain sweeps, 8 new sections plus the README pass).
- Found this run: the self-test gate flakes intermittently on Windows (locked-recording race `PermissionError`, the no-git child lane); filed `D00 T04 §27` (Self-test determinism on Windows), row pulled into Phase 0.

## Sections

- `D00 T02 §19` (Night-debt due dates and escalation): implemented and gated (self-test 1603/0; live night-debt shows S8-N1 and S12-N1 OVERDUE with escalation), committed unstamped; review owed on resume.

## Critical events

- 2026-09-24T00:21:03Z run opened (process-plan chain). Heartbeat `8943e823` created and guard file written. Caveat: the Stop hook was wired into `.claude/settings.json` after this session started, and hooks load at session start, so it may not guard this session; the heartbeat covers idle turns either way.

- 2026-09-24T00:40:51Z operator stop ("stop cleanly and safely, then commit and push all"): guard file and state file deleted, heartbeat 8943e823 cancelled, in-flight §19 committed unstamped, tree pushed. Resume recreates the guard before any other step, then reviews §19.

## Lessons
