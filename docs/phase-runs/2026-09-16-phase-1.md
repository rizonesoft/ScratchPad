# Phase run: Phase 1 -- Notepad parity (run 4, 2026-09-16)

Opened 2026-09-16 ~18:41 SAST via process-plan. Carry-in from `2026-09-15-phase-1.md` run 3: plan 41/160 with `D01 T02 §3` shipped and stamped at the tip (`78b5b0b`); run-3 lessons stand (COMException catches, file-redirected gates, UIA Invoke preference, Retry lastValueOnTimeout, ref-free stamps, content-match before stock input, ToggleState re-fetch, golden transient analysis). Uncommitted trailing state is documented prior-run work, not another writer (only this session's log live in the last 15 min): Sept 14-15 leftovers (AGENTS domains, testing.md, Protocol ref, D03 T01 §1 Started plus untracked JsonRpc halves, 09-14 run file) plus run-guard skill enforcement, committed here as `c616891` before the first phase so the run starts on its own rules.

## Plan audit (process-plan §1)

1. Graph: 0 fatal, 0 warnings (22 todos, 165 sections).
2. Plan currency: `--check` green, 41/160 complete (26%).
3. Ready rows: 9 total. First ref per phase: Phase 1 `D01 T02 §4`, Phase 2 `D03 T01 §1`, Phase 3 `D06 T01 §1`, Phase 4 `D07 T01 §1`.
4. Blocked rows: 110. Shape: T02 §§6/7/9/11 wait on §4 (status bar spine); §8 waits on Phase-3 `D05 T02 §6` (known cross-phase leftover); D02 T01 §§2+ fan behind §1; the rest fan out behind those spines.
5. Phase started: Phase 1, first because Phase 0 is complete and Phase 1 holds the first ready row in table order (`D01 T02 §4`) plus 5 of the 9 ready rows.

Run guard: armed at run start per the enforced skill rule (job id below in Critical events).

## Phase repairs

- `validate`: 0 fatal, 0 warnings. `plan --check` green, no sync needed (41/160).
- Resolve table for all 76 Phase-1 rows: exit 0 on 5 rows (`D01 T02` §§4, 5, 10, 12; `D02 T01 §1`); exit 3 on all 29 shipped rows; exit 4 on the rest; exit 5 on the 4 moved rows (expected). No exit 1/2, no broken refs. `D01 T02 §8` UNMET is `D05 T02 §6` (the known legitimate cross-phase leftover); every other UNMET set is in-phase (§6 on §4; §7 on §4; §9 on §4; §11 on §4 plus §9; D02 T01 §§2+ behind §1).
- Shippable spine in table order: §4, §5, §10, §12, `D02 T01 §1`, then §6/§7/§9 flip as §4 stamps, §11 as §9 stamps, D02 T01 §2 as §1 stamps.
- Staleness sweep: `TODO-02` header Current state re-corrected (§§1-3 shipped, open work §§4-12; committed as its own `todo:` commit so section commits stay pure). `D02 T01-T05` Current states still true (touched only by §1/§3 enablement lines since run 3, no D02 section shipped). No moot sections, no dead callouts. Watch items for section runs (not repairs): §4 Fidelity dir `resources/baseline/status-bar/` does not exist (capture ships first per the no-baseline-no-build rule); §4 item-1 `src/Notepad/` seed path (same seed error as §§1/3); §4 Chrome shared-status-styles existence unverified; §4 item-7 Markdown switch pending-owner pattern for `D02 T04 §3`; §4 must record the typing-latency bar §9 item 4 measures against; §5 Fidelity dir `resources/baseline/print/` does not exist (same capture-first rule); §6 items 2-3 plus the file Verification CI legs are stale (CI retired 2026-09-14; owners: the §6 run and file closeout).
- `validate` after repairs: 0 fatal, 0 warnings.

## Shipped-row verification

- Spine (shipped rows open Phase-1 rows dep on): `D01 T01` 26 rows plus `D01 T02` §§1-3, all resolve exit 3 with `Verified:` stamps naming their sections (26 plus 3 counted in-file).
- Last full gate green 2026-09-16 ~15:12 SAST per the run-3 file (Smoke 1/1, Unit 285/285, Protocol 35/35, UI 156 plus 1 pre-existing quarantine of 157); no src/tests code changes since except the untracked Phase-2 JsonRpc halves plus the Protocol csproj ref.
- Checkpoint re-prove: Windows cheap gate green just now via `build/gate-cheap-win.ps1` (Smoke 1/1, Unit 285/285, Protocol 35/35, seconds, no desktop use). UI spine stands on the 15:12 full sweep per the 5-section cadence (0 sections shipped since the rule started today; sweep owed by the 5th). No audit-stance review needed: nothing regressed, nothing looks wrong against today's source.
- Linux neutral run red 16/285, all Windows-semantics path tests (JumpListFeed 2, ProtocolAssociation 5, LaunchArgs 9; e.g. `D:\docs\a.txt` rooted on Windows, relative on Linux): platform-expected, not regressions. Filed as a gap (owner: the test-strategy section) since it contradicts the documented Linux suite claim.

## Gap audit

- Re-read the phase end to end as the user against the run-3 pass (menus, settings, status, print, palette, counts, themes, goals, editor, find, spell, format, grammar): the run-3 pass stands, and the §§1-3 seams it predicted are all owned (§4 status bar with the View-toggle enablement line, §5 print with the File-item enablement lines, §12 Recent Files card XREF bidirectional with §3, §10 gallery consuming §3 theme defaults, `D02 T04 §3` Markdown switch target present in the table).
- Filed this run: Linux Unit red on Windows-semantics tests (see Shipped-row verification; routed to `D06 T01 §1` as the suite-platform rule owner).
- Checked and covered, not filed: §4 item-7 Markdown switch (pending-owner pattern, `D02 T04 §3` exists); §9 latency bar (watch item on the §4 run to record the bar); §5 item-5 command-line print (`D01 T01 §8` shipped); file Verification CI legs (stale, owner file closeout).

## Sections

## Critical events

- 2026-09-16 ~18:45 SAST: run guard armed (job 1bbad1af, `*/10`, enforced skill rule; adopted-none, created fresh).
- 2026-09-16 ~19:00 SAST (§4 probe build): 6th and 7th direct-csproj build-hang occurrences (both idle pre-MSBuild at <2 CPU-s, both killed; DLL left stale, never execed). Decision: stop feeding the hang class; the §4 recon runs as a pwsh 7 one-shot loading the already-restored FlaUI assemblies (the §29 pattern), porting `ProbeStatusRecon` with the DPI-proof shot fix.
- PAUSED by operator ~19:35 SAST. Guard job 1bbad1af deleted first per the enforced rule; resume recreates it before any other step. Resume state: §4 capture-first nearly done, nothing lost (all evidence on disk: `build/probe-status.ps1` with all fixes, `build/probes/probe-status-run.log` with 113 PROBE lines, interim captures in `build/probes/probe-out-s4b/`). On resume: (1) apply the one pending edit (sel1/home2 count steps; prior edit failed on an ambiguous anchor, disambiguate with count-block context); (2) run ONE final recon pass (restore-plus-pin, corrected Zoom/Line hints, MD switch result, singular selection); (3) file committable strips under `resources/baseline/stock/`; (4) implement §4 per the corrected plan. Desktop left clean: own empty Untitled window closed (verified single Unmodified tab, lossless), windowless launchers already exited, no operator windows touched. Banked UIA evidence: 6 segments with exact names, count rule (breaks count one; singular/plural/of-formats), `Macintosh (CR)`, `Formatted` Button on .md.

## Lessons

- FlaUI from pwsh 7: `AsWindow` is an extension (static-call it), `$pid` is reserved (use another name), inline arithmetic inside `New-Object Type(...)` needs its own parens, and `CaptureToFile` captures the screen composition (pin topmost first) rather than the isolated window.
- Stock status bar, first recon (UIA names quoted): 6 segments left to right (`Line N,\nColumn M` with a newline in the name, count, `Plain text` mode, `Zoom`, ` Windows (CRLF)`, ` UTF-8`); count rule is non-break chars plus one per line break (`0/1 character` singular, `N characters`, `N of M characters` on selection); CR file reads ` Macintosh (CR)`; .md files show a `Formatted` Button where plain text shows `Plain text`; EOL/encoding/count single-clicks showed no popup in-frame (re-probe foreground-pinned before concluding).
