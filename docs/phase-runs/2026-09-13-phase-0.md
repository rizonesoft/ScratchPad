# Phase run: Phase 0 -- Workspace spine (2026-09-13)

First run of the plan. Linux session, no Windows host reachable. Ended parked: the only graph-ready row needs Windows, everything else waits on deps.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings, 19 adjacency advisories (all pre-existing kinds).
2. Plan currency: `plan --check` current, 0 of 106 sections complete.
3. Ready rows: 1 (`D00 T01 §1`); Phase 0 started the same turn as the audit.
4. Blocked rows: 105, all on unmet section or file deps.
5. Phase started: Phase 0, because it holds the only ready row.

## Phase repairs

- `validate`: 0 fatal, nothing to fix. `plan --check`: current, no sync needed.
- Resolve table for all 12 Phase-0 rows: `D00 T01 §1` exit 0; `D00 T01 §2` through `§7` and `D00 T02 §1` through `§5` exit 4 (unmet deps inside or ahead of the phase; leftovers, not stalls).
- Read `TODO-01-repo-and-toolchain.md` and `TODO-02-test-backbone.md` top to bottom: no phase-level staleness. T01 Current state verified true (`src`, `tests`, `tools`, `.github/workflows` all absent, proven by `ls`); T02 forward refs to `D00 T01 §5` and `D06` sections exist as open sections, which is normal.
- No plan edits made in repair.

## Shipped-row verification

- Spine empty: 0 `[x]` rows, nothing to re-check.

## Gap audit

- Read Phase 0 as the developer and CI would use it: layout and pin, scaffold, CI runner, warning gates, CTest, bootstrap, graph checks, then the five test-backbone harnesses. No gaps filed.
- Considered and skipped: `.editorconfig` (dev hygiene enforced nowhere in the plan, below the row bar).
- Decision record, no row (add-todo outcome needs-a-decision): plan Items counts go stale when checklists grow (e.g. `D01 T02 §1` shows 5, carries 11) because `plan --sync` recomputes boxes, not counts. Operator call: teach `--sync` to recompute, or drop the column. Cosmetic, unenforced, zero functional impact.

## Sections

- `D00 T01 §1`: resolve exit 0, full contract read, seven-check validation passed (legitimacy, currency, consistency, correctness, sufficiency, verifiability, accuracy all green against the repo; 0 deferrals open or resolved; the Microsoft Learn URL returns 200; absence claims proven by `ls`). HALTED before build: the checkpoint needs a Windows host (pinned versions install-confirm, provisioner run) and none is reachable from this Linux session, so nothing committable could be produced. No `Started:` written, no files touched. This is the first work for the Windows session.
- All other Phase-0 rows: exit 4, untouched.

## Critical events

- Parked 2026-09-13: every Phase-0 row is exit-4-blocked except `D00 T01 §1`, which is host-blocked. Resume on a Windows 11 host (or a Windows CI loop) with `process-phase` on Phase 0; first row `D00 T01 §1`.
- process-plan chain check after the park: `query ready` still shows only `D00 T01 §1`, host-parked and proven above, so no progressable phase remains. The plan run ends here, parked, not complete. Re-entering Phase 0 on this host would re-park identically.

## Lessons

- `Needs:` convention gap: the format spec wants a `Needs:` line for Windows-bound work, but zero exist tree-wide (grep 2026-09-13), so `process-todo-section`'s early host-stop never triggers and sections rediscover the boundary at gate time. Park records (like this one) carry the boundary durably instead. Revisit if wrong-host starts waste real sessions.
- Items counts in the plan go stale when checklists grow; `--sync` only maintains boxes. See the decision record above.
- Resolving a full phase fits one loop over refs with exit codes; keep the pattern for the next run.

---

# Run 2 -- 2026-09-13 (same host class, fresh evidence)

Second entry in this file: same day, same phase, new session. Run 1's conclusions were re-proven, not assumed; the one durable change is the `Needs:` repair below.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings, 19 adjacency advisories (unchanged, all pre-existing kinds).
2. Plan currency: `plan --check` reported stale operator JSON on entry; `plan --sync` fixed it (0 boxes changed), re-check current, 0 of 106 complete.
3. Ready rows: 1 (`D00 T01 §1`); Phase 0 started the same turn as the audit.
4. Blocked rows: 105, all on unmet section or file deps.
5. Phase started: Phase 0, because it holds the only ready row.

## Phase repairs

- `validate`: 0 fatal (before and after the repair). `self-test`: 391 cases, 0 failed. `plan --check`: current.
- Resolve table for all 12 Phase-0 rows re-run: `D00 T01 §1` exit 0; all others exit 4 with the same UNMET sets as Run 1.
- Repair: added `**Needs:** Windows host (build/test)` to the 10 host-bound Phase-0 sections (`D00 T01 §1, §2, §4, §5, §6`; `D00 T02 §1`-`§5`), so `resolve` prints `needs` and the next runner stops at step 0 instead of rediscovering the boundary at gate time. `D00 T01 §3` and `§7` deliberately unmarked: their work executes on CI runners, so a session without a Windows host can still ship them once their deps land.
- Post-repair `resolve 'D00 T01 §1'` prints `needs windows-host`; ready/blocked counts unchanged (1/105).
- Re-read `TODO-01-repo-and-toolchain.md` and `TODO-02-test-backbone.md` top to bottom: no phase-level staleness. T01 Current state re-proven (`src`, `tests`, `tools`, `.github` all absent). T02 Current state names `D00 T01 §5` output as existing; read in the TODO's start frame (nothing in T02 can start before §5 ships) it is true, so no correction.
- Note: `resolve` hints at a `plan-gate.py` host-probe; no such script exists in `scripts/` yet, so the host decision stays with the skill rule plus live probes.
- Note: the Microsoft Learn URL in T01 Inputs returned no response from this sandbox (`curl` 000, no network); re-confirm reachability on the Windows session rather than treating Run 1's 200 as current.

## Shipped-row verification

- Spine still empty: 0 `[x]` rows, nothing to re-check.

## Gap audit

- Re-read Phase 0 as the developer and CI would use it: pin, scaffold, runner, gates, CTest, bootstrap, graph checks, then the five harnesses. No gaps filed.
- Considered and skipped: `.editorconfig` (below the row bar; `.gitattributes` in §1 covers line endings); plan Items-count staleness (Run 1 decision record stands: cosmetic, operator call).
- Tree-wide `Needs:` remainder (Phases 1-4 are all Windows-bound, zero lines): out of phase scope for repair; noted here and in Lessons for `groom-plan` or the operator.

## Sections

- `D00 T01 §1`: `resolve` exit 0 with `needs windows-host`. Host check failed: Linux WSL2, `powershell.exe` on PATH but interop fails (`WSL UtilBindVsockAnyPort: socket failed 1`, twice, including a trivial echo). No `Started:`, no section files touched, per the step-0 host stop. The checkpoint's second-machine review clause is independently unobtainable here.
- Leftovers (all exit 4): §2 on §1; §3 on §2; §4 on §2; §5 on §2; §6 on §1; §7 on §3; T02 §1 on T01 §5; T02 §2 on T01 §5; T02 §3 on T02 §2; T02 §4 on T02 §1; T02 §5 on T02 §1, §2. All blockers live in Phase 0 ahead of §1.

## Critical events

- Parked 2026-09-13 (run 2): §1 host-blocked, all other Phase-0 rows dep-blocked. Resume on a Windows 11 host with `process-phase` on Phase 0; first row `D00 T01 §1`.
- process-plan chain check after the park: `query ready` still shows only `D00 T01 §1`, host-parked and proven above, so no progressable phase remains. The plan run ends here, parked, not complete.

## Lessons

- Interop presence is not interop function: `command -v powershell.exe` succeeding while every invocation fails on a vsock error is the precise shape of this boundary. Future host checks must execute, not just locate.
- Run 1's `Needs:` revisit trigger ("revisit if wrong-host starts waste real sessions") is met by this run, and the Phase-0 repair above closes it for this phase. The tree-wide sweep is still open.
- `plan --sync` on entry fixed a stale operator JSON with zero box changes: cheap, keep doing it.

## Run 2 addendum -- interop failure diagnosed (2026-09-13)

- Operator correction verified: this session runs in WSL on Windows 11 and the repo sits on the R: drive (`R:\` on `/mnt/r`, 9p).
- The WSL interop door is still shut from inside this session: `cmd.exe` and `powershell.exe` fail identically (5 attempts total) with `UtilBindVsockAnyPort: socket failed 1`.
- `WSL_INTEROP` points at a dead socket (`/run/WSL/3109585_interop`, absent), but retrying against both live sockets fails the same way, so the failure is client-side vsock creation, not a dead server. The shell runs as pid 2 (namespaced), consistent with the sandbox blocking vsock.
- Unsandboxed retry refused by the session profile ("escalated execution requires an unrestricted permission profile"), so no sanctioned path around it exists mid-session.
- Unblock: restart the session with the sandbox bypass (`--disable-sandbox` or `--yolo`) and re-run; interop should then work and Phase 0 can proceed. A restarted session also refreshes the stale `WSL_INTEROP`.

---

# Run 3 -- 2026-09-13 (unsandboxed session, interop live)

Third entry: same day, same phase, new session launched with the sandbox bypass Run 2 prescribed. Run 2's park conditions were re-proven, not assumed; interop now executes (`powershell.exe` returns `interop-ok`, exit 0), network is live (nuget/learn 200), and the replatform commit below explains the Needs count.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings, 19 adjacency advisories (unchanged, all pre-existing kinds).
2. Plan currency: `plan --check` current on entry, 0 of 106 sections complete. No sync needed.
3. Ready rows: 1 (`D00 T01 §1`); Phase 0 started the same turn as the audit.
4. Blocked rows: 105, all on unmet section or file deps.
5. Phase started: Phase 0, because it holds the only ready row.

## Phase repairs

- `validate`: 0 fatal. `plan --check`: current.
- Resolve table for all 12 Phase-0 rows re-run: `D00 T01 §1` exit 0 (no deps, no needs line); all others exit 4 with UNMET sets inside Phase 0 ahead of §1 (T01 §2 on §1; §3, §4, §5 on §2; §6 on §1; §7 on §3; T02 §1, §2 on T01 §5; T02 §3 on T02 §2; T02 §4 on T02 §1; T02 §5 on T02 §1, §2). Leftovers, not stalls; no exit 1/2, no broken refs.
- Needs count reconciled, not drift: Run 2's commit `9110803` added 10 Needs lines, then the replatform `498ef28` deliberately kept 4 ("Needs: kept on T01 S2/S6, T02 S2/S3") because neutral .NET work now runs on Linux. Current tree matches the replatform record.
- Re-read `TODO-01-repo-and-toolchain.md` and `TODO-02-test-backbone.md` top to bottom: no phase-level staleness. T01 Current state re-proven by `ls` (no `src/`, `tests/`, `tools/`, `.github/`; root holds only docs, `todo/`, `scripts/`, `docs/`, `build/`, `.claude/`). T02 Current state start-frame reading from Run 2 stands.
- `resolve` still hints at a `plan-gate.py` host-probe that does not exist in `scripts/`; host decision stays with the skill rule plus live probes. Cosmetic hint text, below the row bar, noted for `groom-plan`.
- No plan edits made in repair.

## Shipped-row verification

- Spine still empty: 0 `[x]` rows, nothing to re-check.

## Gap audit

- Re-read Phase 0 as the developer and CI would use it: pin (§1), scaffold (§2), runners (§3), warnings (§4) plus `dotnet test` (§5), bootstrap (§6), graph checks (§7), then the five T02 harnesses. Chain is complete; no gaps filed.
- Considered and skipped: `.editorconfig` (owned by §4's committed ruleset, not a gap); NuGet pins (owned by §1 lockfiles); `plan-gate.py` hint (above, cosmetic); plan Items-count staleness (Run 1 decision record stands).

## Sections

- `D00 T01 §1`: shipped and stamped. Candidate `a5daf42` plus fix `52a4218` (exec bit: `core.fileMode=false` on 9p swallowed `chmod +x`, found in review, re-verified `FRESH2-IS-X`). Review rounds 2, all lenses approve; findings `docs/reviews/00-workspace/D00-T01-s1.md`. Evidence: wipe+provision reproduces SDK 10.0.401/runtime 10.0.12 on Linux (twice) and Windows via interop; pinned test trio restores+builds; fresh clones clean both OSes; `validate` 0 fatal; `self-test` 391/391. Stamp commit `eef8f0a`; SHIP+STAMP pushed; plan 1/106.
- `D00 T01 §2`: shipped and stamped. Candidate `46319af`, review round 1, all lenses approve with 2 advisories (ARM64 lockfile regen, title-stamp fallback). Contract corrected to per-OS scopes after the spike proved the XAML compiler cannot complete on Linux (1.8: CS5001/CS0103; 2.4.0: WMC1006). Evidence: slnf green Linux 0w/0e, slnx green Windows 0w/0e, stub launched with versioned title + HWND, ProductVersion 0.0.0+sha from the binary, fresh clones clean both OSes. Decisions: unpackaged, framework-dependent, 0.0.0+sha, RID-suffixed provision dirs. Flag recorded for §3/§6 runs (per-OS build scopes). Stamp commit `d8e3b8f`; SHIP+STAMP pushed; plan 2/106.
- `D00 T01 §3`: shipped and stamped. Candidates `ef12eb3` (workflow) + `6fb4909` (red probe) + `e858133` (revert); review round 1, all lenses approve. Contract corrected to builds-only (test/UI suites don't exist; §5/T02 §2 extend). Evidence: run 34785141981 green both jobs with stub artifact downloaded; run 34785638088 red both jobs with MSB4025 naming file+line; run 34786154222 green after revert. Pins: ubuntu-24.04, windows-2025, action SHAs, SDK via global.json. Note: `ef12eb3` body has a recoverable drafting typo, left per no-history-surgery. Stamp commit `4245a6d`; plan 3/106.
- `D00 T01 §4`: shipped and stamped. Candidates `f5482ca` (gates) + `f554b05` (red probe) + `2e8a38f` (revert); review round 1, all lenses approve. No corrections; decisions: root placement, `AnalysisMode All` (CA rules silent otherwise, probe-proven), CA1515 path-scoped to `*.xaml.cs` (XAML mandates public, CS0262 proven). Evidence: gates green locally both OSes 0 warnings; run 34787039263 green; probe run 34787733488 red both jobs with CS0169+CA1822+CA1823 named, reproduced locally; run 34788413405 green after revert. Stamp commit `6513443`; plan 4/106.
- `D00 T01 §5`: shipped and stamped. Candidates `a7f6fce` (wiring) + `805ce64` (red probe) + `c317b8a` (revert); review round 1, all lenses approve. Reversed the §1 v3 default to the v2 VSTest line (v3+MTP discovers zero tests on SDK 10.0.401, proven on our project and xunit's own template across ~20 probes; VSTest passes first try); T02 §1 flagged to re-evaluate. Evidence: suite 1/1 green both OSes locally and in CI (run 34790029058), `--filter Smoke` passes, blanked version red, probe run 34790728911 red with test named, run 34791418524 green after revert. Stamp commit `5eeb4c9`; plan 5/106.
- `D00 T01 §6`: shipped and stamped. Candidate `ef2e014`, review round 1, all lenses approve with 1 advisory (cold machines had step-1 prereqs preinstalled; bare metal follows the documented vendor installers). Evidence: bootstrap doc cold-followed green on Linux (0 warnings, Passed 1/1) and Windows (0 warnings, Passed 1/1, stub launch title observed); env-failure entries reproduced verbatim both OSes; run 34792965135 green both jobs. Plan 6/106.
- `D00 T01 §7`: shipped and stamped. Candidates `ba504d2 d51bbf5 7adbd7c d18932b bbaf5c6 f18b59d 7c2b739 294ec5e`, review round 1, all lenses approve. Evidence: plan-gates green (run 34793534551, 391 cases 0 failed); self-test probe red 34793587854, validate probe red 34793710261 with FATALs named, projection probe red 34793805213, each reverted to green; docs-only push ran no plan-gates job. T01 file closed (`status: done`, Verification ticked). Plan 7/106.
- `D00 T02 §1`: shipped and stamped. Candidates `f0b847c 1468835 7629678`, review round 1, all lenses approve. Re-evaluated the §5 flag: v3/MTP reproduces zero-test discovery on the vendor template, v2 VSTest line stands. Evidence: Unit 2/2 green locally both OSes and in CI (run 34794828910); inverted assertion red locally; probe run 34795487610 red both jobs with the test named; run 34796097790 green after revert. Plan 8/106.
- `D00 T02 §2`: shipped and stamped. Candidates `749fe22 4eb476f 40d3373`, review round 1, all lenses approve. Evidence: FlaUI (506 ms attach, island traversal works) beats WinAppDriver (3755 ms with admin/client costs) on measured drives of the stub; UISmoke green locally and in CI (run 34798097715, UI.dll 1/1) after the crash-dialog red run 34797312338; gaps routed to D06 T01 §1/§3, T02 §3/§5. Plan 9/106.
- `D00 T02 §3`: shipped and stamped. Candidates `bf39eb8 9501c57 4d459fd e9414bf 8d866b7 dbcd869`, review round 1, all lenses approve. Evidence: 4 stock captures (Notepad 11.2607.14.0) plus app golden with README procedure; comparison green in CI (run 34804426231, UI 3/3) with 10px shift failing (736px vs 410px budget, PIL cross-checked); refresh shakedown reviewed and green (run 34805175346). Plan 10/106.
- `D00 T02 §4`: shipped and stamped. Candidates `6fdfda3 b505081`, review round 1, all lenses approve. In-review hardening: non-object input exited 2 cleanly instead of crashing, script-field errors exit 3, plus a new test. Evidence: loopback fixture plus Protocol suite (turn, 3 faults, 4 validation tests) green locally (8/8) and in CI both jobs (run 34807778662, 8/8); mutation probe red then green on revert; ownership recorded with D03 T01/T02 back-refs. Plan 11/106.
- `D00 T02 §5`: shipped and stamped. Candidates `549ff76 77aff94`, review round 1, all lenses approve. Self-review caught the Linux repeat loop swallowing red (shell-proven, fixed with `|| exit 1`). Evidence: soak workflow plus procedure doc; soak green twice by dispatch (runs 34808621885, 34809456063; 22 passed, 0 failed each); probe lifecycle proven locally (pass/fail/fail/fail/fail/pass same binary, quarantined suite green twice, removed with decision row). T02 file closed (`status: done`, Verification ticked). Plan 12/106.

## Critical events

- Run 3 continued past the §5 log gap: sections T01 §6 through T02 §5 shipped without Sections entries and were backfilled from their stamps at closeout; no evidence was reconstructed, only copied.

## Lessons

- A review that skips the shell around the tests misses real defects: the soak repeat loop and the §1 exec bit both hid outside any assertion. Read the workflow and the file modes, not just the code.
- The mutation probe (break the fixture, watch the test go red) is the cheapest proof a protocol test exercises the wire. D03 and D06 should keep the habit.
- Stamps are the durable record: the phase log went stale for 7 sections and the closeout recovered fully from `Verified:` lines. Keep stamps quotable.

## Closeout -- Phase 0 complete, 12/12

What shipped: pinned toolchain with repo-local provisioners, per-OS solution scopes with one-command build, CI on both runners with warning/analysis gates and red-probe proof, smoke plus unit harnesses on the proven v2 VSTest line, a cold-followed bootstrap doc, TODO graph checks in CI, a measured UI driver decision (FlaUI) with a passing stub drive, a golden capture store with comparison and refresh procedure, a scripted ACP loopback fixture with turn/fault/validation suites, and a nightly soak plus quarantine procedure with a demonstrated probe lifecycle. Both TODO files closed with Verification ticked; `validate` 0 fatal; `self-test` 391/391; full suite green in CI on both jobs (build run 34810042091) plus two green soak runs. No deferrals carried forward; no gaps filed beyond the D06-routed spike gaps. Next: Phase 1, first ready row `D01 T01 §1`.
