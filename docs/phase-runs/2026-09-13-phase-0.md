# Phase run: Phase 0 -- Workspace spine (2026-09-13)

First run of the plan. Linux session, no Windows host reachable. Ended parked: the only graph-ready row needs Windows, everything else waits on deps.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings, 19 adjacency advisories (all pre-existing kinds).
2. Plan currency: `plan --check` current, 0 of 106 sections complete.
3. Ready rows: 1 (`D00 T01 §1`); Phase 0 started the same turn as the audit.
4. Blocked rows: 105, all on unmet section or file deps.
5. Phase started: Phase 0, because it holds the only ready row.

## Phase repairs

- `validate`: 0 fatal, nothing to fix. `plan --check`: current, no sync needed.
- Resolve table for all 12 Phase-0 rows: `D00 T01 §1` exit 0; `D00 T01 §2` through `§7` and `D00 T02 §1` through `§5` exit 4 (unmet deps inside or ahead of the phase; leftovers, not stalls).
- Read `TODO-01-repo-and-toolchain.md` and `TODO-02-test-backbone.md` top to bottom: no phase-level staleness. T01 Current state verified true (`src`, `tests`, `tools`, `.github/workflows` all absent, proven by `ls`); T02 forward refs to `D00 T01 §5` and `D06` sections exist as open sections, which is normal.
- No plan edits made in repair.

## Shipped-row verification

- Spine empty: 0 `[x]` rows, nothing to re-check.

## Gap audit

- Read Phase 0 as the developer and CI would use it: layout and pin, scaffold, CI runner, warning gates, CTest, bootstrap, graph checks, then the five test-backbone harnesses. No gaps filed.
- Considered and skipped: `.editorconfig` (dev hygiene enforced nowhere in the plan, below the row bar).
- Decision record, no row (add-todo outcome needs-a-decision): plan Items counts go stale when checklists grow (e.g. `D01 T02 §1` shows 5, carries 11) because `plan --sync` recomputes boxes, not counts. Operator call: teach `--sync` to recompute, or drop the column. Cosmetic, unenforced, zero functional impact.

## Sections

- `D00 T01 §1`: resolve exit 0, full contract read, seven-check validation passed (legitimacy, currency, consistency, correctness, sufficiency, verifiability, accuracy all green against the repo; 0 deferrals open or resolved; the Microsoft Learn URL returns 200; absence claims proven by `ls`). HALTED before build: the checkpoint needs a Windows host (pinned versions install-confirm, provisioner run) and none is reachable from this Linux session, so nothing committable could be produced. No `Started:` written, no files touched. This is the first work for the Windows session.
- All other Phase-0 rows: exit 4, untouched.

## Critical events

- Parked 2026-09-13: every Phase-0 row is exit-4-blocked except `D00 T01 §1`, which is host-blocked. Resume on a Windows 11 host (or a Windows CI loop) with `process-phase` on Phase 0; first row `D00 T01 §1`.
- process-plan chain check after the park: `query ready` still shows only `D00 T01 §1`, host-parked and proven above, so no progressable phase remains. The plan run ends here, parked, not complete. Re-entering Phase 0 on this host would re-park identically.

## Lessons

- `Needs:` convention gap: the format spec wants a `Needs:` line for Windows-bound work, but zero exist tree-wide (grep 2026-09-13), so `process-todo-section`'s early host-stop never triggers and sections rediscover the boundary at gate time. Park records (like this one) carry the boundary durably instead. Revisit if wrong-host starts waste real sessions.
- Items counts in the plan go stale when checklists grow; `--sync` only maintains boxes. See the decision record above.
- Resolving a full phase fits one loop over refs with exit codes; keep the pattern for the next run.

---

# Run 2 -- 2026-09-13 (same host class, fresh evidence)

Second entry in this file: same day, same phase, new session. Run 1's conclusions were re-proven, not assumed; the one durable change is the `Needs:` repair below.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings, 19 adjacency advisories (unchanged, all pre-existing kinds).
2. Plan currency: `plan --check` reported stale operator JSON on entry; `plan --sync` fixed it (0 boxes changed), re-check current, 0 of 106 complete.
3. Ready rows: 1 (`D00 T01 §1`); Phase 0 started the same turn as the audit.
4. Blocked rows: 105, all on unmet section or file deps.
5. Phase started: Phase 0, because it holds the only ready row.

## Phase repairs

- `validate`: 0 fatal (before and after the repair). `self-test`: 391 cases, 0 failed. `plan --check`: current.
- Resolve table for all 12 Phase-0 rows re-run: `D00 T01 §1` exit 0; all others exit 4 with the same UNMET sets as Run 1.
- Repair: added `**Needs:** Windows host (build/test)` to the 10 host-bound Phase-0 sections (`D00 T01 §1, §2, §4, §5, §6`; `D00 T02 §1`-`§5`), so `resolve` prints `needs` and the next runner stops at step 0 instead of rediscovering the boundary at gate time. `D00 T01 §3` and `§7` deliberately unmarked: their work executes on CI runners, so a session without a Windows host can still ship them once their deps land.
- Post-repair `resolve 'D00 T01 §1'` prints `needs windows-host`; ready/blocked counts unchanged (1/105).
- Re-read `TODO-01-repo-and-toolchain.md` and `TODO-02-test-backbone.md` top to bottom: no phase-level staleness. T01 Current state re-proven (`src`, `tests`, `tools`, `.github` all absent). T02 Current state names `D00 T01 §5` output as existing; read in the TODO's start frame (nothing in T02 can start before §5 ships) it is true, so no correction.
- Note: `resolve` hints at a `plan-gate.py` host-probe; no such script exists in `scripts/` yet, so the host decision stays with the skill rule plus live probes.
- Note: the Microsoft Learn URL in T01 Inputs returned no response from this sandbox (`curl` 000, no network); re-confirm reachability on the Windows session rather than treating Run 1's 200 as current.

## Shipped-row verification

- Spine still empty: 0 `[x]` rows, nothing to re-check.

## Gap audit

- Re-read Phase 0 as the developer and CI would use it: pin, scaffold, runner, gates, CTest, bootstrap, graph checks, then the five harnesses. No gaps filed.
- Considered and skipped: `.editorconfig` (below the row bar; `.gitattributes` in §1 covers line endings); plan Items-count staleness (Run 1 decision record stands: cosmetic, operator call).
- Tree-wide `Needs:` remainder (Phases 1-4 are all Windows-bound, zero lines): out of phase scope for repair; noted here and in Lessons for `groom-plan` or the operator.

## Sections

- `D00 T01 §1`: `resolve` exit 0 with `needs windows-host`. Host check failed: Linux WSL2, `powershell.exe` on PATH but interop fails (`WSL UtilBindVsockAnyPort: socket failed 1`, twice, including a trivial echo). No `Started:`, no section files touched, per the step-0 host stop. The checkpoint's second-machine review clause is independently unobtainable here.
- Leftovers (all exit 4): §2 on §1; §3 on §2; §4 on §2; §5 on §2; §6 on §1; §7 on §3; T02 §1 on T01 §5; T02 §2 on T01 §5; T02 §3 on T02 §2; T02 §4 on T02 §1; T02 §5 on T02 §1, §2. All blockers live in Phase 0 ahead of §1.

## Critical events

- Parked 2026-09-13 (run 2): §1 host-blocked, all other Phase-0 rows dep-blocked. Resume on a Windows 11 host with `process-phase` on Phase 0; first row `D00 T01 §1`.
- process-plan chain check after the park: `query ready` still shows only `D00 T01 §1`, host-parked and proven above, so no progressable phase remains. The plan run ends here, parked, not complete.

## Lessons

- Interop presence is not interop function: `command -v powershell.exe` succeeding while every invocation fails on a vsock error is the precise shape of this boundary. Future host checks must execute, not just locate.
- Run 1's `Needs:` revisit trigger ("revisit if wrong-host starts waste real sessions") is met by this run, and the Phase-0 repair above closes it for this phase. The tree-wide sweep is still open.
- `plan --sync` on entry fixed a stale operator JSON with zero box changes: cheap, keep doing it.
