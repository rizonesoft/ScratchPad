# Phase run: Phase 1 -- Notepad parity (run 5, 2026-09-17)

Opened 2026-09-17 via process-plan chain after the Phase 0 closeout (13/13, plan 43/164). Carry-in from run 4 (`2026-09-16-phase-1.md`): §4 shipped and stamped at the tip; run-3/run-4 lessons stand (COMException catches, file-redirected gates, UIA Invoke preference, Retry lastValueOnTimeout, ref-free stamps, content-match before stock input, ToggleState re-fetch, golden transient analysis, pwsh FlaUI patterns, stock-menu Invoke-only probes). New since run 4: D00 T02 §6 shipped (CI green both jobs after 36h red; note its Core/test/CI touchpoints in the spine check), three new Phase-1 rows (§§13-15), and D06 T01 §1 newly ready (Phase 3 row, not this phase).

Run guard: plan-owned 46f48c1d (`*/10 * * * *`), verified present, adopted (re-pointed from the Phase 0 guard 06a5e3ff at chain time).

## Phase repairs

- `validate`: 0 fatal, 0 warnings. `plan --check`: current (43/164, 26%). No sync needed.
- Resolve table for all 49 open Phase-1 rows: exit 0 on 8 (`D01 T02` §§13, 5, 6, 7, 9, 10, 12; `D02 T01 §1`); exit 4 on the rest. Blockers: §14/§15 on §13 (in-phase ahead, flip when §13 stamps); §8 on `D05 T02 §6` (the known legitimate cross-phase leftover, per run 4); §11 on §9 (in-phase ahead); all D02 rows behind `D02 T01 §1` or deeper in-phase deps. No exit 1/2, no broken refs. Shippable spine in table order: §13, §5, §6, §7, §9, §10, §12, `D02 T01 §1`, then §14/§15/§11/D02-chain as stamps land.
- Read `TODO-02` and `TODO-01` top to bottom; delta-checked D02 T02-T05 headers against run 4's sweep (no premise touched since except §6's harness change, which breaks no editor premise: capture checkpoints still hold under the blur, shifts ≥5px still caught). No plan edits in repair. Watch items for section runs (not repairs, per run-4 precedent): §13's Why premise is stale (the rename commit `0dbdaf8` already landed; verification items 1-4 remain; the §13 run re-derives from the landed state); §5 item 1 and §8 item 1 carry the `src/Notepad/` seed error (app is `src/ScratchPad/`); §3 item 1 the same; §5 Fidelity dir `resources/baseline/print/` still absent (capture-first rule); §18/D01-T01-§18 overlap already resolved in-text (clean export/copy-as split with dependency, no duplication).
- Run-4 staleness correction: run 4's repair notes claim twice that CI was retired 2026-09-14 (flagging §6 items 2-3 and file Verification CI legs as stale). That premise is itself stale: CI gates every push (runs 35208253143 and 35209653181 green both jobs today), and the 2026-09-17 testing-doc edit it likely read retired CI *telemetry*, not CI (word restored by §6). No TODO edits follow from the run-4 claim; §6 items stand for their run's own validation.

## Shipped-row verification

- Spine: open Phase-1 rows depend on shipped D01 T01 rows, D01 T02 §§1-4, and (new) D00 T02 §6. All carry `Verified:` stamps (run 4 counted 26+3 exit-3; §6 stamped today).
- No per-row checkpoint re-runs: the full suite ran green on the final tree minutes before this run opened (Windows `dotnet test src/ScratchPad.slnx`: Smoke 1/1, Unit 336/336, Protocol 35/35, UI 166 + 1 pre-existing skip, 0 warnings; Linux neutral green; CI 35209653181 success both jobs), which subsumes every spine checkpoint. §6's touchpoints in shipped D01 code (JumpListFeed/LaunchArgs/ProtocolAssociation) are covered by that green, including the D01 UI drives.
- No audit-stance triggers: nothing regressed, nothing looks wrong against today's source.

## Gap audit

- Run 4's end-to-end pass (menus through grammar) stands; delta since is §§13-15 (operator-filed, in-table, sequenced, XREFs bidirectional: §10↔§15 accent interplay, §12→§3 card, §14→T01-§11 asset) plus the §6 landing (harness-only, no Phase-1 surface gap). Re-read the delta as the user: rename verification, icon placement, color finetune each owned with no handoff missing.
- Run 4's filed gap (Linux Unit red, routed to `D06 T01 §1` as the suite-platform rule owner) is fixed in code by §6; the strategy doc (Phase 3, open) should still cite the §6 rule when written. Noted here, no action in this phase.
- No gaps filed.

## Sections

- `D01 T02 §13`: shipped and stamped. Contract re-derived from the landed state (mechanical `0dbdaf8` predated verification; keep-list extended; scheme item added, 5 to 6 items; commit message and checkpoint corrected). Scheme `intelligent-notepad` to `scratchpad` (const + 15 test literals; unclaimed in HKCU/HKCR); §26 decision lines corrected with re-run evidence; skill prompt line fixed. Evidence: Linux link units 38/38, Windows ProtocolHandlerTests 5/5, both-OS builds 0 warnings, neutral + golden subset green both OSes, live migration proof with byte-verified restore, CI 35212112337 success both jobs. Candidates `f9e38b2` (ship) + `4ccd61a` (stamp) + `dcf8d3c` (projection); Opus panel round 1, 4x advisory, all applied; findings `docs/reviews/01-notepad-core/D01-T02-s13.md`. §§14-15 unblocked by the stamp.
- INTERRUPT (operator): validator rule for the Opus panel (`stamp-no-opus-panel` + self-test cases) routed and built next, out of phase order, per direct instruction; Phase 1 resumes at §14 after.

## Critical events

## Lessons
