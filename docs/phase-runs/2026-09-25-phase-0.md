# Phase run: Phase 0, 2026-09-25

Runner: Claude Code (claude-opus-5-5), the only writer (AGENTS.md). Entered through process-plan with no phase argument, so the plan chains to the next ready phase after this one closes or parks. This run resumes the Phase 0 chain the 2026-09-24 run left on an operator stop (`docs/phase-runs/2026-09-24-phase-0.md`), in a new session after the workspace moved from `R:\conclave\projects\ScratchPad` to `R:\GitHub\ScratchPad`.

Plan-level audit (process-plan step 1):

1. Graph: 0 fatal, 1 warning (known owner-login mapping).
2. Plan currency: `plan --check` current, 114/301 sections complete (38%).
3. Ready rows: 51 runnable now, 0 runnable elsewhere. Phase 0 first `D00 T02 §22` (the first open ready row in plan order; `D00 T02 §16` waits on `D01 T01 §34` plus `D01 T02 §16`); Phase 1 first `D01 T01 §34`; Phase 2 first `D02 T01 §1`; Phase 3 first `D08 T01 §1`; Phase 4 first `D07 T01 §9`.
4. Blocked rows: 136, all on unshipped Depends.
5. Starting Phase 0 first: it is the first phase with a ready row and holds most of the 51. Provision verify 9/9 green.

Run guard: heartbeat job `7b4291ba` (`3-59/5 * * * *`, session-only, 7-day expiry); guard file `build/claude-campaign-guard.json` written for session `765b680e-761d-4cff-b59f-3f0c8d325934`; no stale state file.

Move check before the run: build 0 warnings 0 errors on the new path; default suite Smoke 1/1, Unit 361/361, Protocol 35/35, UI 250 passed 14 skipped 1 failed (`LargeFileOpensResponsively` UIA attach timeout, green 3/3 isolated, filed on `D01 T01 §35` in da049c8).

## Phase repairs

- Guard check (process-phase step 0): `CronList` shows `7b4291ba` and `build/claude-campaign-guard.json` names this session and this run file; the plan owns it, no second guard.
- `validate`: 0 fatal, 1 warning (known owner-login mapping). `plan --check`: current.
- Resolved all 47 open Phase 0 rows: 36 exit 0; 11 exit 4, all in-phase unlocks or leftovers: `D00 T02 §16` (waits on `D01 T01 §34` plus `D01 T02 §16`, phase 1), `D00 T03 §2`-`§4` (unlock in order after `D00 T03 §1`), `D00 T03 §5` (waits on `D05 T01 §3`, phase 3, plus `D00 T03 §4`), `D00 T09 §3`-`§8` (unlock after `D00 T09 §1`-`§2`). No exit 1 or 2 broken refs.
- Phase-level staleness: covered by the 2026-09-23 whole-tree groom and the 2026-09-24 run's repair pass; the only change since is the directory move, checked clean before this run (one stale live path fixed in b5115e7).

## Shipped-row verification

- Machine checkpoints re-run on HEAD 5144d27/da049c8 this session: self-test 1608 cases 0 failed (the sibling stamp-gate clamp probe skips by design: `section_commit_gate.py` is not in this repo), validate 0 fatal, plan current, provision verify 9/9, build 0/0, default suite green except the filed UIA attach flake. Behavior checkpoints stand on their stamps; nothing read contradicts them.

## Gap audit

- Covered by the 2026-09-23 groom plus the 2026-09-24 gap audit (`D00 T04 §27` filed).
- Found this run: `UI.LaunchTests.LargeFileOpensResponsively` UIA attach flake in the default suite, filed on `D01 T01 §35` (da049c8); no Phase 0 row pulled in (the owner sits in Phase 1).
- Found this run: the 2026-09-25 02:30 governed nightly ran no legs (`Population: DRIFT: population drift: interactive-cases: fingerprinted 37 vs discovered 42`): d21ae60 (`D00 T02 §21` R5) recorded 37 interactive cases against a 6-case Theory. Stamped-row regression, fixed forward in b9f1bee (Step 1b); the nightly's own comparer reads `population OK run-a=212/265 run-b=4/4 interactive=37/42` after, the exact drift line before. The gap (the fingerprint is only checked inside the night) filed as `D00 T02 §29` (Population fingerprint gate before the night), row pulled into Phase 0 after `D00 T02 §28`.

## Sections

- `D00 T02 §22` (Nightly evidence hardening follow-ups): STAMPED 2026-09-25. Candidates 9f9d8b5..78de9db. Full panel on gpt-6-astra, 4 rounds: self-review fixed a one-pass incident close and shell-title disclosure before round 1; R1 five findings (name-based title ownership, quoted secrets, cross-id streak reset, backfill soak filter, ledger validation), R2 two (scan failure fails open, occurrence validation), R3 sign-off one safety finding in the R2-F1 family fixed at the retain enforcement point, R4 depth all approve. Plan review 21 findings: 10 filed as `D00 T02 §30` (Nightly evidence residuals), 4 on `D00 T09 §1`, 4 on `D00 T09 §8`, 3 rejected. Proof runs on 9f9d8b5 and 78de9db: Population OK, Tree clean at start and end, Catalog current.
- `D00 T02 §23` (Nightly acknowledgement hardening): STAMPED 2026-09-25. Candidates 098add8..1676a26. Full panel on gpt-6-astra, 3 rounds: self-review verified commit findings and caught a Windows PowerShell 5.1 native-stderr crash before round 1; R1 four findings (older copy keeps a changed result acked, unverified section and INC findings, name-selected format, unchecked git status), R2 three (stale incidents void a batch, incident union across copies, history lost on rename), R3 sign-off one below-bar finding in the R2-F1 family fixed at its root and re-gated. Plan review 17 findings: 14 filed as `D00 T02 §31` (Acknowledgement residuals), 3 rejected. Live: the gate demanded 25 RED runs; the drift night and the three section proof runs are acknowledged in a120e4b by the runner (21 remain for the operator).
- `D00 T02 §24` (Notify follow-ups): ESCALATED, no stamp. Candidates 1c17978..ab5fee0. Full panel on gpt-6-astra, 5 rounds (hard cap): R1 six findings, R2 five, R3 sign-off three (R3-F1 a lost alert, data integrity), R4 depth two (R4-F2 a destroyed record, data integrity; its live proof with a corrupted ledger held and exposed an Environment separator defect, fixed in 359fa77), R5 depth one (R5-F1, R4-F2 family: the publication guard was set after the journal write). R5-F1 is fixed in ab5fee0 but unreviewed, and a data-integrity finding open at the hard cap stops the review with no stamp, so the operator decides (confirming sixth round, direct acceptance, or redesign); see `docs/reviews/00-workspace/D00-T02-s24.md` Escalation. The Nightly Morning reconciler task is registered through provision (verify 10/10); its first real run is 07:05 today.
- `D00 T02 §25` (Trend and telemetry follow-ups): STAMPED 2026-09-25. Candidates 611acdf..5fee709. Full panel on gpt-6-astra, 3 rounds: R1 six findings (path redaction at whitespace, basis secret scan, unproven alert rates, pruned backfill markers, trend-alert key, artifact-named provenance), R2 three (missing os semantics, metrics revisions, a fixture on ignored history), R3 sign-off two below-bar (zero baseline, window order) fixed and re-gated. The live proof caught Windows PowerShell's ISO-to-DateTime round-trip (ddda0a5). The plan review showed item 6's change-point alerts missing; 5fee709 adds a sustained-shift detector, re-gated by fixtures and a plant but not panel-reviewed, and the record says so. Plan review 22 findings: 15 filed as `D00 T02 §32` (Trend and telemetry residuals), 4 as `D00 T02 §33` (Notification residuals), 1 filed on §25 and fixed, 2 rejected.
- `D00 T02 §26` (Sibling sweep narrowing): STAMPED 2026-09-25. Candidates 95fdf99..dfddd7e. The first test shape (a third window born from the second window's menu) could not keep the first window's flyout alive; a redirected launch births without touching either window's UI, and a before/after rect read the pre-fix sweep as harmless because the popup host re-anchors, so a WinEvent location recorder proves it: red `moved to (3160,0)` pre-fix, green on the fix. Full Run A under the foreground gate green with zero primary births. Full panel on gpt-6-astra, 3 rounds: R1 record (Run A evidence) answered, R2 and R3 sign-off all approve. Plan review 8 findings: 6 filed as `D00 T02 §34` (Sibling sweep residuals), 2 rejected.
- `D00 T02 §27` (Night-debt escalation lifecycle): STAMPED 2026-09-25. Candidates 2269257..b343dbf. Found while building: the §19 query change had broken the collector's text scrape (0 of 4 debts parsed, collector blind since §19); fixed forward by a JSON contract and a verbatim report block. Also corrected the §22 and §23 docs' claim that the nightly writes no tracked file (it appends Night-collected lines). Full panel on gpt-6-astra, 3 rounds: R1 six findings, R2 two (the strict-date family), R3 sign-off all approve. Plan review 14 findings: 12 filed as `D00 T02 §35` (Night-debt lifecycle residuals), 2 rejected.
- `D00 T02 §28` (Binding guard and funnel hardening): STAMPED 2026-09-25. Candidates ce9501f..b36a3d7, Full tier, three rounds; sign-off below-bar R3-F2 fixed and re-gated at the stamp, R3-F1 (third patch of the static assertion rule) filed D00 T02 §36 instead of re-patching. Found while building: FlaUI `Keyboard.Press` is key-down only, so the pre-§28 funnel never released the non-modifier key; fixed by the key-down/key-up split. Found while regenerating the fingerprint: discovery lists a fenced Theory as one skipped case outside the quiet window, so a daytime regen wrote interactive=39/39 where the night discovers 39/44 (filed on D00 T02 §29; this run regenerates with SCRATCHPAD_INTERACTIVE_FORCE=1). Plan review filed D00 T02 §36 and three items on D02 T01 §5 (including the NumPad-only zoom binding displayed as Ctrl++). Night-owed D00-T02-S28-N1 (44 Interactive).

## Critical events

- 2026-09-25T01:44:31Z run opened (process-plan chain, resume after the 2026-09-24 operator stop). Heartbeat `7b4291ba` created and guard file written.

## Lessons

- A fingerprint regenerated outside the quiet window undercounts fenced Theory cases; regenerate with SCRATCHPAD_INTERACTIVE_FORCE=1 until D00 T02 §29 makes discovery time-independent.
- When a sign-off finding is the third patch of one unit, file the execution-based replacement instead of a fourth syntactic patch.

- 2026-09-25: grep for writers before claiming a script writes no tracked file. The §22 and §23 docs said the unattended nightly writes none, but `Add-CollectedLine` (tools/NightDebt.ps1) appends §10's green-collection `Night-collected:` lines to TODO files; corrected forward in the docs and in §22's item 7 note while building §27.

- 2026-09-25: under Windows PowerShell 5.1 with `$ErrorActionPreference = 'Stop'`, a native command writing to stderr throws even with `2>$null`; scope the preference to `Continue` around git probes that may fail and read `$LASTEXITCODE`. Also keep `§` out of code strings in BOM-less `.ps1` files (5.1 reads them as ANSI): use `§` in regex and `[char]0xA7` in strings.

- 2026-09-25: the Bash tool mangles backslash escapes inside quoted heredocs (`\b` arrived as a backspace byte in a regex); write multi-line patches to a scratch `.py` file with the Write tool and run it, and grep for control bytes (`grep -c $''`) after any shell-driven edit of a regex.

- 2026-09-25: regenerate `tests/UI/TestPopulation.fingerprint` only after a fresh build of the UI tests; the regen reads the built binaries, and a stale build records the wrong case counts (a Theory's rows count as cases). Filed as `D00 T02 §29` item 4.
