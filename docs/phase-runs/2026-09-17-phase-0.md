# Phase run: Phase 0 -- Workspace spine (2026-09-17)

Follow-up run: Phase 0 closed 12/12 on 2026-09-13; a new row (`D00 T02 §6`, golden comparison determinism) landed since. This run ships the one new row.

## Plan audit (process-plan)

1. Graph: 0 fatal, 0 warnings (22 todos, 169 sections).
2. Plan currency: `plan --check` current, 42/164 sections complete (26%).
3. Ready rows: 12. First per phase: Phase 0 `D00 T02 §6`; Phase 1 `D01 T02 §13`; Phase 2 `D03 T01 §1`; Phase 3 `D08 T01 §1`; Phase 4 `D07 T01 §1`.
4. Blocked rows: 110, all on unmet section or file deps.
5. Phase started: Phase 0, because it is the first phase with a ready row.

Run guard: 06a5e3ff (`*/10 * * * *`, recurring, fires during active runs). Adopted by process-phase; verified, not duplicated.

## Phase repairs

- `validate`: 0 fatal, 0 warnings. `plan --check`: current (42/164, 26%). No sync needed.
- Resolve table: Phase 0 has one open row, `D00 T02 §6` exit 0 (dep §3 shipped; `needs windows-host`, interop live in this session). All other Phase-0 rows `[x]`. No exit 1/2, no broken refs.
- Re-read `TODO-02-test-backbone.md` top to bottom: no phase-level staleness. Current-state line is the file's start frame (09-13 Run 2 reading stands); Implementation Order shows §1-§5 shipped. §6's rename assumption verified (`src/ScratchPad.slnx` exists). §6 item 1's "operator step" is executable in-session (`gh` authed as rizonesoft), so no operator wait.
- Guard check: plan-owned guard 06a5e3ff verified, adopted, not duplicated. Prior run file 2026-09-13-phase-0.md read: closed 12/12, nothing unresolved (Needs convention closed for Phase 0; Items-count staleness decision stands; plan-gate.py hint still cosmetic).
- No plan edits made in repair.

## Shipped-row verification

- Spine: §6 depends only on §3. `resolve 'D00 T02 §3'` exits 3, `Verified:` stamp covers it (2026-09-14, UI 3/3 green in CI run 34804426231).
- §3's checkpoint has no single command (behavioral: shift fails, refresh passes). Its CI-green leg is currently red on `build-windows`, which is exactly §6's scoped work: fix-forward, not a stamp defect (the stamp's quoted CI run was green at stamp time; the runner world moved). No downgrade, no separate §3 audit: §6 carries the regression through its own process plus review.
- Other `[x]` rows are not on §6's spine; not re-checked.

## Gap audit

- Re-read Phase 0 end to end (pin, scaffold, runners, gates, `dotnet test`, bootstrap, graph checks, five harnesses, §6 determinism): the chain is complete and §6 itself was the filed gap for CI determinism.
- The `b5ec3ac` non-golden signature is owned (§6 item 6). Unset `origin/HEAD` (found today) is cosmetic, below the row bar. No gaps filed.

## Sections

- `D00 T02 §6`: shipped and stamped. Contract corrected before build (red window starts 35028328028; scope covers all three HEAD signatures per the fold-in rule, +2 items; checkpoint strengthened to both CI jobs; 7 to 9 items). Track A: new `WindowsPath` (Windows BCL path semantics) wired into 3 Core call sites, 7 test expectations to Windows-spelling literals. Track B: 3x3 blur in `GoldenComparer` (noise to 18/21px, shift still 1069px vs 500px threshold) plus capture logging and a wobble pin. Track C: `HideFileExt` 0 CI step (all 6 reproduced locally by flipping that bit). Evidence: Windows slnx green (Smoke 1/1, Unit 336/336, Protocol 35/35, UI 166 + 1 skip, 0 warnings), Linux neutral green, CI 35208253143 success both jobs zero golden artifacts. Candidates `7e0283d` (ship) + `ef29903` (stamp) + `1a3c151` (projection); review round 1, all lenses approve; findings `docs/reviews/00-workspace/D00-T02-s6.md`. Sidebar: interop relay died mid-session (`UtilAcceptVsock` timeout on the default socket); `/run/WSL/1654881_interop` works, prefix Windows commands with it when the default fails.

## Critical events

## Lessons

- Interop relay sockets die individually: when the default `WSL_INTEROP` socket times out with `UtilAcceptVsock`, probe the other sockets in `/run/WSL/` before declaring the host unreachable (the 09-13 "retrying fails" conclusion does not hold unsandboxed).
- A section whose premise cites CI failures must re-derive them with auth before building: the §6 author diagnosed goldens without log access and missed two of three signatures. `gh run view --log-failed` is the first tool, not the artifact download.
- PIL replicas of test comparers decide renderer debates in minutes: exact-count replication first, then hypothesis probes (shift search, fringing, half-res collapse), each killing one theory.
- The Common Item Dialog honors Explorer extension-hiding: any machine-level default that differs between runner and dev box is a precondition, and `build.yml` already sets the precedent (theme keys).

## Closeout -- Phase 0 complete, 13/13

What shipped: the one new row, `D00 T02 §6`, turning 36 hours of red CI (3 signatures, 25 failures at HEAD) green at named roots: OS-independent Windows-path handling in neutral Core, cross-DPI normalization in the golden comparer, and extension-visibility as a CI precondition. What was repaired: the §6 contract itself (red window, three-signature scope, strengthened checkpoint) before building; one doc typo beside the work. What was learned is above. Evidence: local Windows slnx green (Smoke 1/1, Unit 336/336, Protocol 35/35, UI 166 + 1 pre-existing skip, 0 warnings), local Linux neutral green, ship CI 35208253143 and stamp CI 35209653181 success both jobs with zero golden-failure artifacts, `validate` 0 fatal, plan current 43/164. No deferrals, no gaps filed, no leftovers. Next: Phase 1, first ready row `D01 T02 §13`.
