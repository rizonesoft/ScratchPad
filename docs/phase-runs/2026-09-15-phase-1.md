# Phase run: Phase 1 -- Notepad parity (run 3, 2026-09-15)

Opened 2026-09-15 ~22:45 SAST via process-plan. Carry-in from `2026-09-14-phase-1.md` run 2: 8 neutral halves complete here (D01 T01 §§4, 9, 11, 14, 17, 22, 24, 28, all OWED-UI noted there); run 2 paused 2026-09-14T23:45Z and resumes on the operator's word, which is this run; §11 item 1 byte proof is stale (operator replaced `resources/notepad.ico` at 01:44Z, re-prove at ship); D03 T01 §1 parked mid-write for Phase 2. Since run 2: §§4-7, 9, 27 shipped and stamped (plan 21/158); §8 implemented in `511c99a` but unstamped, so it ships first in table order; uncommitted trailing state (run-2 notes, CI-retirement doc edits, csproj content refs, morning neutral-half sources) is documented prior-run work, not another writer: no test/app processes running, tree quiet since the 22:13 commit.

## Plan audit (process-plan §1)

1. Graph: 0 fatal, 0 warnings (22 todos, 163 sections).
2. Plan currency: `--check` failed only on stale `build/todo-operator.json` (0 stale boxes); `plan --sync` regenerated it (158 rows, 0 boxes changed), re-check green at 21/158 complete (13%).
3. Ready rows: 17 total. First ref per phase: Phase 1 `D01 T01 §8`, Phase 2 `D03 T01 §1`, Phase 3 `D06 T01 §1`, Phase 4 none.
4. Blocked rows: 120. Shape: T01-whole gate holds all of T02/D02 behind the 16 open T01 rows; §25 waits on §13, §26 on §8; the rest fan out behind those spines.
5. Phase started: Phase 1, first because Phase 0 is complete (12/12 plus 1 moved) and Phase 1 holds the first ready row in plan order plus 14 of the 17 ready rows.

## Phase repairs

- `validate`: 0 fatal, 0 warnings. `plan --check` green after the sync above.
- Resolve table for all 65 open Phase-1 rows: exit 0 on 14 rows (`D01 T01` §§8, 11, 13, 14, 16, 17, 18, 19, 20, 21, 22, 24, 28, 29); exit 4 on 51 rows, all UNMET sets in-phase except `D01 T02 §8` (also `D05 T02 §6`, the known legitimate Phase-3 leftover from run 2); no exit 1/2, no broken refs.
- Shippable spine: the 14 exit-0 rows in table order, then §25/§26 flip as §13/§8 stamp, then the T01-whole gate lifts T02/D02.

- Staleness sweep: `D01 T01` Current state re-corrected (§§4-7, 9, 27 shipped, §8 unstamped, open/moved lists current); `D01 T02` Current state corrected the same way. `D02 T01-T05` Current states still true. No moot sections, no dead callouts. Watch items for section runs (not repairs): (a) `D01 T02 §8` still seeds `src/Notepad/CommandRegistry.cs`, the run-1 watch item stands (section run corrects its own paths); (b) stamped `D01 T01 §1` box 4 still says "in CI", CI retired, but stamped checklists are not rewritten; (c) `D01 T01` §§14/17/24 boxes ticked from neutral halves whose sources are still untracked, each section run re-proves before stamping.
- `validate` after repairs: 0 fatal, 0 warnings (re-check below with the gate).

## Shipped-row verification

- Spine (shipped rows open Phase-1 rows dep on through the T01-whole gate): `D01 T01` §§1-7, 9, 27, all resolve exit 3 with `Verified:` stamps naming their sections (read in-file).
- Toolchain repair: the gate failed out with SDK-not-found (global.json pins 10.0.400 exact, `.tools/` held 10.0.401 on both RIDs since the 3723ee7 retarget moved the pin without re-provisioning). Fix: re-running the sanctioned `tools/provision.ps1`, which reads the pin and installs exactly it. The run-2 `DOTNET_ROOT=.tools` prescription stands once provision aligns; the stale half was the SDK bits, not the procedure.
- Checkpoint re-run: full gate green after provision. Smoke 1/1, Unit 174/174, Protocol 35/35, UI 54 passed plus 1 pre-existing quarantine of 55 (6m40s), build 0 warnings, log `build/gate-last.log`. Counts include the unstamped §8 implementation and the untracked neutral halves, all green. No lingering `IntelligentNotepad` processes after the run. No audit-stance review needed: nothing regressed, nothing looks wrong against today's source.
- Linux SDK provisioned to match the pin too (`tools/provision.sh`, 10.0.400), so neutral suites can re-prove on both OSes.

## Gap audit

- Re-read the phase end to end as the user (launch, window, tabs, pins, stats, templates, snapshots, export, encrypt, backup, reload, titles, share, jump list, protocol, names, encoding, menus, settings, status, print, palette, counts, themes, goals, editor, find, spell, format, grammar). No new gaps filed.
- Checked and covered, not filed: launch variants (§8) with protocol links (§26 deps §8); jump recents (§8 item 5) vs jump tasks (§25); T01 render triggers (T02 §1 item, recorded 2026-09-15); §8 print seam to T02 §5 item 5 (recorded both sides); T01 §18 converter to D02 T01 §18 (recorded both sides); provider seams to D02 T01 §1 (§§14/16/18 Treatments); §24 registration to D07 T01 §7 (verified the section exists). Owed-but-owned items stay with their owners (§8 Sandbox re-drive and drop end-to-end, §5 stock warning wording, §4/§6 rendered surfaces at T02 §1).

## Sections

- `D01 T01 §28`: shipped and stamped. Candidate `1475bbb`, review round 1, all lenses approve. Section work this run: neutral `TabAccessibilityName.For` (`{display}. Modified/Unmodified.`), wired into both `SetName` sites (`MainWindow.Tabs`, `MainWindow.Launch`) with refresh through the §2 dirty path, punctuation live-proven against stock (`PROBE22. Modified.`, `Untitled. Unmodified.`), suite-wide asserts migrated to settled suffixed names; validation decision (WinUI nullable confirms versus QA nil renders, neither is the bar, stock punctuation is). Evidence: UI TabAccessibilityTests 2/2 (CreateTab edit drive against the live `MainWindow` handle; Ctrl+S no-op replaced the SaveFile helper with a save-dialog-driven close-plus-save), Unit TabAccessibilityNameTests 2/2, full gate Smoke 1/1 Unit 259/259 Protocol 35/35 UI 115 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393, green first try. Stamp commit TBD, pushed. Plan 36/159.
- `D01 T01 §26`: shipped and stamped. Candidate `11816b6`, review round 1, all lenses approve. Section work this run: scheme `intelligent-notepad` with HKCU plans mirroring §8 (backup, claim, restore) plus register/unregister verbs; links map to files in `LaunchArgs` with malformed ignored into the bare path; validation decisions (scheme, link form, manual parse, bare decline, missing-path reuse, non-interference, verb mirror, secondary-parses-first). Evidence: UI ProtocolHandlerTests 5/5, Unit association plus args suites green, full gate Smoke 1/1 Unit 259/259 Protocol 35/35 UI 113 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393, green first try. Found while driving: the shell appends a trailing slash to the substituted URL (proven via command-line read-back; trimmed in the parser with a pinned test). Stamp commit `bae2935`, pushed. Plan 35/159.
- `D01 T01 §25`: shipped and stamped. Candidate `fcdd3b4`, review round 1, all lenses approve. Section work this run: `/new-note` flag through `LaunchArgs`, `LaunchDrops` (flagged record, `DrainAll`, pre-flag JSON back-compat), and both launch paths into `EnsureFreshNote` (first clean untitled tab selected, else opened; files-first ordering; redirected targets first window); static task datum on the feed committed ahead of it by the service (`Tasks` group); validation decisions (flag grammar and combining, spare-or-open rule, first-window target, service-side layering, group name). Evidence: UI JumpListTaskTests 5/5, Unit launch/feed 29/29, full gate Smoke 1/1 Unit 236/236 Protocol 35/35 UI 108 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393, green first try. Blemishes: SHIP body carries a stray non-English word (immutable, message only); CRLF working copies need `file`-first edits (mixed-ending repair on `MainWindow.Launch.cs`). Stamp commit `3359138`, pushed. Plan 34/159.
- `D01 T01 §24`: shipped and stamped. Candidate `5dff99f`, review round 1, all lenses approve. Section work this run: verified the run-2 neutral half (`ShareReceiver` plus 3 tests, sat uncommitted) true as scoped, added drive notes; no product change. Evidence: Unit ShareReceiverTests 3/3, full gate Smoke 1/1 Unit 226/226 Protocol 35/35 UI 103 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393, green first try. Stamp commit `c1ca898`, pushed. Plan 33/159.
- `D01 T01 §22`: shipped and stamped. Candidate `47b571d` plus `28e355f` (round 1: retried snapshot prompt-button lookup), review round 1, all lenses approve. Section work this run: no product change (the §2 rule flows through `RefreshHeader` untouched); `UntitledTitleTests` surface drives (live rename, back-to-Untitled, trim, 35-cap) plus a one-shot stock drive proving parity live (tab `PROBE22` from `PROBE22 `, 40-char line titled at exactly 35, UIA `Modified` punctuation corroborates the §28 recon) with two crops filed under `resources/baseline/stock/`; validation corrections (Started, item-1 un-tick/re-tick for the half-true §2 tick under the retired bedtime regime, Chrome shared-styles correction). Evidence: UI 2/2, full gate Smoke 1/1 Unit 226/226 Protocol 35/35 UI 103 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. First gate failed 1/104 on the lone unretried `SnapshotTests` button lookup, fixed in the round, re-gate green. Incidents: direct-csproj `dotnet build` hangs pre-MSBuild (third build-hang occurrence; probe ran prebuilt, `build/run-probe.ps1 --prebuilt` records it); a crashed scratch run left a pristine stock window (restored debris tabs, verified untouched); FlaUI `Keyboard.Type` plus `Press(RETURN)` interleaved mid-string (single-Type sidestep). Stamp commit `ef090da`, pushed. Plan 32/159.
- `D01 T01 §21`: shipped and stamped. Candidate `e71fa04` plus `5c2c1e6` (round 1: dirty-dot Keep postcondition, path-filtered static-event tests, Cancel drive), review round 1, all lenses approve. Section work this run: per-tab `FileWatcher` plus `FileSave.WroteFile` baselines (own commits never prompt), buffer-convergent auto-resolve, active-or-next-activation prompt gate with asked-hash straggler dedup, pure reload-or-keep `ReloadDialog` (dirty text names the discard, Cancel keeps), spec-applying reload, missing-file and locked-file branches, no trigger; validation decisions (watcher lifecycle, baseline semantics, auto-resolve, prompt gating, dialog shape, both branches, overflow declared with hook-plus-rescan cost) plus Chrome shared-styles and Current-state corrections; interplay fix disclosed (unlock applies the detected spec, detour reports success). Evidence: UI ReloadTests 8/8, Unit WroteFile contract 2/2, full gate Smoke 1/1 Unit 226/226 Protocol 35/35 UI 101 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. First gate failed 2/327 (static-event cross-talk under parallel Unit collections; vacuous post-Keep box-text wait racing Ctrl+W under load), both root-caused and fixed in the round, re-gate green. Stamp commit `6dd36d4`, pushed. Plan 31/159.
- `D01 T01 §20`: shipped and stamped. Candidate `f32b447`, review round 1, all lenses approve. Section work this run: backup engine in `FileSave` (pre-save bytes to `{file}.{UTC ms}.bak` with collision suffix, atomic sibling writes, cap 5, creation-time rotation of own-pattern names, backup-then-commit-then-rotate order, redirect-mapped backup failures) covering `SaveFile` and `SaveBytes`; validation decisions (naming, rotation, cap, coverage, order, failure maps) plus Current-state correction; the §5 leftover test now expects the sibling (mandated contract change, disclosed). Evidence: UI BackupTests 3/3, Unit FileSaveBackupTests 5/5, full gate Smoke 1/1 Unit 224/224 Protocol 35/35 UI 93 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. Post-stamp `c8f26a5` clears an artifact-delivery advisory wording trip. Stamp commit `52165e5`, pushed. Plan 30/159.
- `D01 T01 §19`: shipped and stamped. Candidate `f0937c8`, review round 1, all lenses approve. Section work this run: `NoteCrypto` (AES-256-GCM, PBKDF2-SHA256-600k, header AAD, stated layout, pinned vector cross-checked in Python) plus `FileSave.SaveBytes` twin plus `Tab.IsLocked`, Ctrl+Shift+L `LockDialog` (password plus confirm), unlock detour on open with in-dialog retry, re-lock on close-save, snapshot-restore Save branch fails safe, restore ghosts locked files, trigger contract recorded both sides with T02 §1 item 5; validation decisions (final algorithm, layout, dialog shapes, no-silent-decrypt rule, ghost restore, memory-only) plus Current-state and Chrome corrections. Evidence: UI EncryptedNotesTests 6/6, Unit NoteCryptoTests 16/16, full gate Smoke 1/1 Unit 219/219 Protocol 35/35 UI 90 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. First gate failed 1/91 on the §16 reshow wait (transient, second occurrence, watch item; focused green, unchanged re-gate green). Stamp commit `af0f2cf`, pushed. Plan 29/158. Filed `D01 T01 §30` (locked-tab residue hardening, from review advisory (a); deps shipped, ships in table order). Plan 29/159.
- `D01 T01 §18`: shipped and stamped. Candidate `29b9f2f`, review round 1, all lenses approve. Section work this run: shared UI-free `FormatConverter` (Markdown identity, HTML fragment plus document shell, plain strip; pinned subset with literal fallbacks) plus Ctrl+Shift+X `ExportDialog` (one button per format, editable base name defaulting to `{basename}-export`, UTF-8 no-BOM CRLF through the §5 writer, untitled save-first state), trigger contract recorded both sides with T02 §1 item 5; validation decisions (converter semantics and subset, fragment/document split, beside-source destination, export spec) plus item-1 native-apps, Chrome, and Current-state corrections; post-stamp `5a089e1` owns the artifact-delivery adjacency step the word "destination" tripped. Evidence: UI ExportTests 4/4, Unit FormatConverterTests 13/13, full gate Smoke 1/1 Unit 203/203 Protocol 35/35 UI 84 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. Stamp commit `b2c9570`, pushed. Plan 28/158.
- `D01 T01 §17`: shipped and stamped. Candidate `2ff23cb` plus `d7cc0d1` (round 1: Daily-journal open drive, panel containment drive), review round 1, all lenses approve. Section work this run: adopted the neutral half (`Templates` plus `TemplateStore` plus 5 unit tests), bound it to a Ctrl+Shift+E `TemplatesDialog` (three built-ins as use-buttons, title prompt, save-current-as-template customs at the settings seam, untitled tab opens with natural dirty tracking), trigger contract recorded both sides with T02 §1 item 5; validation decisions (picker is a separate entry, Ctrl+N stays blank; title box doubles as custom name) plus Current-state, Chrome, and adjacency corrections. Evidence: UI TemplateTests 6/6, Unit TemplateTests 5/5, full gate Smoke 1/1 Unit 190/190 Protocol 35/35 UI 80 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. Stamp commit `78a38c4`, pushed. Plan 27/158.
- `D01 T01 §16`: shipped and stamped. Candidate `44404da` plus `089922a` (round 1: read-only save-failure drive, containment drive) plus `a628db3` (round 2: fresh-dialog reshow for the 1-in-4 hidden-instance reappear flake), review rounds 3, all lenses approve. Section work this run: `SnapshotStore` (`<file>.snapshots/` with atomic manifest, heal scan, cap 10) plus Ctrl+Shift+H `SnapshotsDialog` (named takes byte-identical via `FileSave`, one-click restore buttons, sequential §7 prompt, save-first state for untitled), trigger contract recorded both sides with T02 §1 item 5; validation corrections (store/decisions, shared-styles correction) plus review-round-3 Chrome correction (restore buttons, not ListView). Evidence: SnapshotTests 8/8, SnapshotStoreTests 8/8, full gate Smoke 1/1 Unit 190/190 Protocol 35/35 UI 74 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. Stamp commit `a88e799`, pushed. Plan 26/158.
- `D01 T01 §14`: shipped and stamped. Candidate `b247cff` plus `ab51d4f` (round 1: dialog reentrancy guard, single-dialog drive, truncation drive), review rounds 2, all lenses approve. Section work this run: re-proved the neutral-half engine 9/9, bound it to a Ctrl+Shift+G `StatsDialog` (right-aligned label/value grids per operator direction, eyeballed on capture; a Star-column blowout caught and fixed), Refresh control, trigger contract recorded both sides with T02 §1 item 5; validation corrections (Current-state staleness, trigger decision with goldens rationale, Chrome shared-styles correction, Ordinal tie-break reading). Evidence: StatsPanel 4/4, TextStats 9/9, full gate Smoke 1/1 Unit 182/182 Protocol 35/35 UI 66 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. Stamp commit `6008ce6`, pushed with the plan projection. Plan 25/158.
- `D01 T01 §13`: shipped and stamped. Candidate `a14f5d8` plus `6a5d36d` (round 0: UI-harness cascade repair, tests only) plus `7799a73` (round 1: feed release on close), review rounds 2, all lenses approve. Section work this run: pin model plus session round-trip, double-click toggle with live-tab guard, pin glyph, bulk-close skips, jump feed with release-on-close; round 0 repaired the gate cascade (first-test linger poisoned 58 launches into ~60 redirected windows, operator-closed; `UiApp.Attach` funnel, `AppIconTests` wait-then-kill, icon-read poll, gate stray sweeps); round 1 fixed the orphan feed entry on close with covering drives. Evidence: PinnedTabs 5/5, full gate Smoke 1/1 Unit 182/182 Protocol 35/35 UI 62 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. Stamp commit `0755add`, pushed. Plan 24/158. Unlocks: §25 now exit 0.
- `D01 T01 §11`: shipped and stamped. Candidate `22f6b4c` plus the `SetIcon` lines in `9f43493`, review round 1, all lenses approve. Section work this run: re-proof against the replacement asset, `AppIconTests` (exe plus `WM_GETICON` 0-diff pins), taskbar and chrome captures filed and eyeballed, stale-proof correction, extended-chrome correction. Evidence: UI 2/2, build 0 warnings, validate 0 fatal, self-test 393/393. Stamp commit `8633931`, pushed. Plan 23/158.
- `D01 T01 §8`: shipped and stamped. Candidates `511c99a` (prior-run implementation) plus `973d50e` (review fix), review rounds 2, all lenses approve. Section work this run: stock offer capture (new `--offer` probe mode; orphaned-modal incident resolved, see Critical events), box ticks with drive mapping, item-2/item-6 alignment clauses, Enter-default drive (`MissingFileOfferEnterAcceptsAsYes`). Evidence: Unit 26/26, UI 20/20 (filter), full gate Smoke 1/1 Unit 174/174 Protocol 35/35 UI 54 plus 1 pre-existing quarantine, build 0 warnings, validate 0 fatal, self-test 393/393. Stamp commit `68aba7e`, pushed. Plan 22/158. Unlocks: §26 now exit 0.

## Critical events

- 2026-09-15 ~23:42 SAST (§8 evidence probe): new `ProbeNotepad --offer` mode captured the stock create-offer dialog (wording triple-confirmed verbatim) then died after its screenshot, orphaning the Yes/No modal in the operator's stock window for several minutes. Cause: native fault on the live UIA tree past the dump (post-`shot=` lines never printed, no managed exception); masked by a `$LASTEXITCODE` pipeline bug in the runner (`& exe ... | Out-File` never sets it from the exe, so the crash reported 0). Fixed: probe answers No first with every step guarded, plus a scoped `--dismiss-offer` (No only on dialogs naming our `newoffer.txt`); both runner scripts now capture native output to a variable before writing the log. The gate's green stands on its quoted `Passed!` lines, not its exit code.
- Same night: two `dotnet build` hangs (30 min, 12 min) under the fresh provisioned SDK, both before/without compile output; cleared by killing a stale `VBCSCompiler` (gate leftover) and rebuilding with `/nodeReuse:false /p:UseSharedCompilation=false`. Prime suspect is the carried-over compiler server; the bypass stays in the probe script.
- Observed, not touched: the operator's stock window holds `PROBE-w7-crash-9d23fd1d.txt` (modified, §7 debris), `Process todo section D07 T06 §12 Pr.txt` (modified, unknown provenance, possibly an earlier mis-targeted SendKeys à la run-1 "hello"), and `The End Of Oak Street (2026).srt` (unmodified, operator content). The offer capture also caught these on screen, so the committed crop must be dialog-only.
- 2026-09-15 ~00:20 SAST (§13 stock probe): my double-click probe's HWND-diff matched the WRONG window (operator's, not the fresh launch) and its cleanup `CloseMainWindow` closed the operator's 3-tab stock window (silent close per the §7 finding; stock session restore brings it back on relaunch under the continue default; nothing deleted, the T: file was already missing before). Double-click itself changed nothing (tabs identical before/after). Cause: handle-timing match with no content verification, plus an unverified close. Fix owed in `build/probe-dblclick.ps1`: match the own window by content (Untitled-only, no foreign tabs) and close only the verified handle. Rule from here: no stock-window input or close without a content match. Operator asked whether to relaunch now or leave it.

- 2026-09-16 ~01:10 SAST (§13 gate cascade): the full gate's UI phase failed 58/63 in 27s and left ~50-60 live Intelligent Notepad windows in ONE process on the operator's desktop; the operator closed them all (Windows-side stray count now 0, verified). Mechanism, all confirmed in source: the app is single-instance (`App.Launch.cs`, `SingleInstanceKey`), and a redirected bare launch opens a new window in the primary while the secondary exits. The first UI test (`AppIconTests.WindowChromeIconMatchesAsset`) failed its icon-handle read, and its `finally` only calls `window.Close()` with no exit-wait and no `Kill` fallback (every other UI file uses the `CloseApp` wait-then-kill guarantee; the comment claiming "the dispose below reaps the process" is wrong, FlaUI `Dispose` does not terminate). The primary lingered, so all 58 later launches redirected into it: one new window each, own process exited, `GetMainWindow` threw "process not running" outside `try`, no cleanup possible. Trigger for the first failure looks like a race, not a product regression: the pin commit (`a14f5d8`) never touched the `SetIcon` path (line 513 intact), and the test reads `WM_GETICON` immediately after attach while `SetIcon` runs on first `Loaded`. Fixes owed before the §13 review: (1) `AppIconTests` gets the same wait-then-kill exit guarantee; (2) audit every UI test so a launched process is always reaped even on attach failure; (3) `run-gate.ps1` sweeps stray `IntelligentNotepad` processes before the UI phase and fails loudly (then sweeps) if any survive it; (4) the chrome-icon read retries until `Loaded` sets it instead of a single read. Rule from here: no UI test or probe may leave a window behind on ANY path, pass or fail, and the gate proves it by counting strays afterward.

## Lessons

- Stamps must not carry `DNN TNN §N` references: quote lines feed XREF reciprocity, so a stamp naming another file goes FATAL one-sided (§14 caught it, reworded to "the menu owner"). Contracts live in Treatment/Recorded prose (not collected); stamps stay ref-free.
