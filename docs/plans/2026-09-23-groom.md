# Groom record: whole tree (2026-09-23)

Scope: every open section in `todo/` (170 open of 285 at start). Read-only sweeps ran per domain, and one session applied every edit serially. No row moved phase, no box was ticked, and no campaign started. Every edit carries a `**Groomed 2026-09-23:**` note or a `(Groomed 2026-09-23.)` item suffix.

## Totals

- Sections: 285 to 293 (8 new); plan rows 280 to 288; 110 complete.
- Edits: 84 dated notes and 43 new checklist items across 25 files.
- Gates: `validate` 0 fatal, 1 warning (the known owner-login mapping); `plan --check` current.

## Sequence

- Deadlocks broken (not caught by `validate`, which checks section cycles only): D01 T02, D02 T01, D05 T01, and D07 T01 each held a file-level `depends_on` on the whole app spine (D01 T01). §33-§35 were filed into D01 T01 after the spine shipped, which reopened it. That blocked 53 sections and deadlocked D01 T01 §33 against D07 T01 §11, and §35 against D01 T02 §16. The whole-spine edge is dropped in all four files, and every real prerequisite stays a section-level row. Blocked sections fell from 137 to 125 once this landed; the rest wait on genuine section dependencies.
- Late edges (prerequisite in a later phase), kept with notes as a recorded default: D01 T01 §33 waits on D07 T01 §11 (phase 4), D01 T02 §8 on D05 T02 §6 (phase 3), and D01 T02 §17 on D07 T01 §10 (phase 4). Splitting would duplicate the work, and D01 T01 sits at its 55-section cap. D02 T01 §18 depends on a shipped row, so it needs nothing.
- D00 T02 §16 (phase 0) waits on D01 T01 §34 and D01 T02 §16 (phase 1): recorded here only (see Proposals).

## New sections

- D03 T01 §8 Session list, resume, close, delete (the v1 `sessionCapabilities.*` methods had no wire owner).
- D03 T01 §9 Modes and config options (`session/set_mode`, `session/set_config_option`).
- D03 T02 §6 Elicitation requests (`elicitation/create` answered exactly once).
- D05 T02 §10 Grants and audit viewer.
- D05 T03 §6 AI settings group.
- D07 T01 §13 User-guide backfill (`docs/user-guide/` does not exist).
- D01 T02 §18 Fix-or-remove the MenuBarTests flakes.
- D00 T05 §2 Run-guard proof (`.claude/hooks/campaign-stop.ps1` had no self-test and no owner).

## Drift corrected (highlights)

- ACP shapes against the live spec: permission outcomes (`selected` optionId or `cancelled`), fs methods (exactly read and write text file), terminal (agent cwd, `outputByteLimit`, agent-owned timeouts), v2 removals (`session/load`, SSE MCP, auth renames), auth method types, and session close semantics.
- Kokoro license is Apache-2.0 (not MIT); model bytes provision into app data (only the pin manifest is committed); `AudioTranscode` needs the Windows-TFM project.
- Editor: the placeholder is `TabBar`'s TextBox; `MenuCommands` is the `commands` dictionary behind `AppMenuBar.SetEnabled`; word wrap and zoom wiring; find-option contradictions resolved toward the source; existing disabled settings cards named for spellcheck, autocorrect, formatting, and writing tools.
- Release: clean-VM claims retired (manual clean system), CI builds and launches only, packaging and certificate choices owed, a second version source found, and uninstall data policy.
- Core: print scaffold 3e59883 recorded, interactive leg of the pinned-tab regressions, 8 quarantined tests without an open owner (one overdue since 2026-09-22) now owned by D01 T01 §35.
- Workspace: panel items moot after §23 and §25 and the `.grok/` removal marked with notes (not ticked); the §24 target retargeted to the plan-health print; README drift filed.

## Proposals for TODO-02 (not applied)

`todo/00-workspace/TODO-02-test-backbone.md` carries Muse's uncommitted §18 stamp work, so grooming left the file untouched. To apply once §18 lands:

- Current state: open rows are §16 and §§18-26; §§8-15 and §17 have shipped.
- §16 sequence note: its prerequisites D01 T01 §34 and D01 T02 §16 sit in phase 1 and are runnable; `process-plan` parks §16 until they ship.

## Sources

Five read-only sweeps (workspace; notepad core, quality, manual; editor; ACP, agents, voice; AI surface and release) checked every claim against the repo, the live ACP docs (agentclientprotocol.com), and package registries (npm, GitHub, Hugging Face).
