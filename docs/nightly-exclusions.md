# Nightly exclusions

Results the operator excluded on purpose (D00 T02 §40 item 8). An excluded result is intentional, not unusable: it leaves every trend series (`[native]`, `[rate]`, `[recurrence]`, `[budget]`), its night renders `excluded` with the reason, it never marks the night degraded, and its baseline slot counts as missing. Name the run identity exactly as the result records it; a reason is required.

| Run identity | Reason |
| --- | --- |
