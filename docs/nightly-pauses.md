# Nightly pauses

Nights the governed nightly was deliberately not run (D00 T02 §32 item 7). The trend reads a night listed here as `paused` with its reason instead of `missing`, so an intentional pause never reads as a failed start. Add a row before or on the paused night; remove nothing (the history stays).

| Night | Reason |
| --- | --- |
