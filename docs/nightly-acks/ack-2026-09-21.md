# RED acknowledgement: 2026-09-21

Owner: operator. Signed: 2026-09-21 (D00 T02 §17 item 3 proof: this file's presence is the explicit sign-off `Test-RedAcknowledged` reads).

One run, RED and unproven:

- `fl5f1-red-2026-09-21` (timer fire 02:30): every leg started (transcripts present) but produced no trx. The leg logs carry `MSBUILD : error MSB1009: Project file does not exist. Switch: tests/UI/UI.csproj`, although `tests/UI/UI.csproj` exists at the run's HEAD (`78c336e`, verified) and CWD pinning (`Push-Location $Root`) predates the run (commit `2e40b8d`, 2026-09-20). The worktree state at 02:30 is unrecoverable from retained evidence, so the trigger reads unknown-worktree, not wrong-directory. Class infrastructure.

Compensating control going forward: the §16 tree fingerprint records worktree content at run start plus end, so a recurrence names the mutation instead of leaving MSB1009 unexplained. Re-examination trigger: any future MSB1009 immediately re-opens this diagnosis with the fingerprinted evidence.
