# RED acknowledgement: 2026-09-20

Owner: operator. Signed: 2026-09-21 (D00 T02 §17 item 3 proof: this file's presence is the explicit sign-off `Test-RedAcknowledged` reads).

Two runs, both RED:

- `nightly-rerun-2026-09-20` (manual re-run 03:20, `-SkipSoak`): Run A 545/1/3 across Smoke plus Unit plus Protocol plus UI (the report row reads UI-only 163/1/3 per the §9 S4 generator defect; the stamped count is 545) with `UI.DirtyPromptTests.WindowCloseWithDirtyTabsIsSilentAndRestores` failing on a COM timeout (`0x80131505`), Interactive 24/3/1 with `UI.PinnedTabsTests.SingleCloseStillClosesPinned` plus `UI.PinnedTabsTests.CloseRightSkipsPinned` (both `Assert.NotNull`, value null) plus `UI.SessionRestoreTests.QuitAndRelaunchRestoresTabsContentsAndCarets` (`Assert.Equal`, values differ). Class infrastructure: the §9-era gate cells carry prose, not exit codes, so the gate proof is unrecoverable (noted on the backfilled result).
- `nightly-taskrun-2026-09-20` (demand fire 04:13, full legs plus soak): Run A 540/0/9 with gate exit 1 (census counted one 0,0,1x1 `GDI+ Window` on primary; the current §8 census excludes zero-area windows, so this gate shape no longer reds), Interactive 24/2/2 with the same two PinnedTabs failures. Class gate.

Follow-up owned, not blocking: the PinnedTabs pair failed in both runs (same-night repeat, backfilled incidents `INC-446cffda` plus `INC-e8104189`); if either recurs on a later night it graduates to the quarantine procedure (`docs/soak-and-quarantine.md`). The DirtyPrompt COM timeout plus the SessionRestore diff each owe triage findings on their next occurrence.
