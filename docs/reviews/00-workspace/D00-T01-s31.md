# Review: D00 T01 §31 -- Clearance Causality and Precision

Candidate: `0ad715f` + `9f882d9` + `0a7b498` + `366b5d3` + `f25c064` (SHIP plus three panel fixes plus the filing commit; output `check-panel PASS` on all four headless rounds). Panel tier: Full (the diff touches `scripts/todo-graph.py` clearance core, shared contract surface), Sol rounds 1-2, Opus rounds 3-4 (sign-off plus confirmation).

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. No separate self-review fix commit: the validation pass had already recorded its corrections on items 1-7 before the build, and the diff matched the corrected contract.

Does it make sense: seven items take fix-predates-attestation, section-scoped attribution, range-touch recency, linear ranges, proof binding, strict ancestry, and merge exclusion, each with its helper, leg, fixtures, and mutation lock. Exactly the contract, including the recorded shapes (strict day fallbacks, file-level touch boundary, definition-site plus proof-touch binding, truthful helper with clearance-level exclusion).

Is it logical: legs order fail-fast from cheap to dear (merge, spans, touch, recency, attestation, proof, ancestry); new helpers mirror the existing five (None on unprovable, never raise); canned profiles extend the established maps; the clearing rows keep clearing on described-possible attestations. The panel later tightened three spots self-review missed (merge-None, off-shape strictness, side-branch bases).

Did it break anything else: suite moves 879 to 898 by the new legs plus probes (902 after panel fixes); live validate stays 0 fatal with no newly-listed rows; plan-health --check stays green. Blast radius is the clearance query plus its fixtures, both covered.

## GPT panel (round 1)

- `adversarial` needs-attention: (1) `git_is_merge(tip) == None` passes as non-merge against the fail-closed contract.
- `consistency` needs-attention: (1) `git_is_merge` and `git_range_touch_ts` contradict their off-shape docstrings (unvalidated tokens, skipped malformed lines).
- `integration` needs-attention: (1) the base is never proven on the tip's first-parent chain, so a side-branch base can open a supposedly linear range.
- `record` needs-attention: (1) item 4 records first-parent topology complete while the base check is any-ancestor.

Fix record: merge leg fails closed on None (`is not False`); parent tokens hex-validated; range-touch timestamps reject any malformed line; new `git_on_first_parent_chain` helper plus leg with canned profiles, real-git pins, and the PR81 side-branch-base probe. Fixed in `9f882d9`; gates re-ran green before round 2 (902 cases, validate 0 fatal, plan current).

Raw round-1 output (fenced, not part of the verdict scan; digest `af53aaba06b6eecc0cffb4b3439078cffe2faa7b252121f34bd82fabc9f4788c`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:3151` treats `git_is_merge(tip) == None` as a nonmerge, allowing unprovable merge status to pass despite the fail closed contract.

**consistency: needs-attention** (1)
1. `scripts/todo-graph.py:1797` and `scripts/todo-graph.py:1832` contradict their off-shape-output docstrings: `git_is_merge` accepts arbitrary one-token output as nonmerge, while `git_range_touch_ts` ignores malformed lines when any numeric line exists.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:3169`, `scripts/todo-graph.py:1704`, and `scripts/todo-graph.py:1817` prove only ordinary ancestry and filter returned commits by first parent; they never prove that `base` itself lies on `tip`’s first-parent chain, so a side-branch base merged into the tip can still define a supposedly linear range.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:864` records first-parent topology as complete even though `scripts/todo-graph.py:3169` accepts a base that is merely any ancestor of the tip.
```

## GPT panel (round 2)

Round 2 ran against the R1-fix range with the round-1 verdicts appended as fixed context.

- `adversarial` needs-attention: (1) empty or single-token parent output accepted as proven non-merge.
- `consistency` needs-attention: (1) first-parent membership tested against unstripped lines while validation strips.
- `integration` approve
- `record` approve

Fix record: the consistency finding fixed (strip parity in the membership test). The adversarial finding rejected as a false positive with reasons: exit-0 `%P` output is authoritative (empty means root, one token means an ordinary commit, anything else exits nonzero into None), so no unprovable tip passes; the cited command name is also wrong (`log --pretty=%P`, not `rev-list --parents`). Fixed in `0a7b498`; gates re-ran green before round 3 (902 cases, validate 0 fatal, plan current).

Raw round-2 output (fenced, not part of the verdict scan; digest `2bc1fd6cd5b1fc6db1f33c83a207f04e2e6eebef7ba780656277a308bd1fc739`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1800` and `scripts/todo-graph.py:3179` still treat successful empty output or an arbitrary single hexadecimal token from `git rev-list --parents` as proven nonmerge status, allowing an unprovable tip to pass clearance.

**consistency: needs-attention** (1)
1. `scripts/todo-graph.py:1828` returns `True` when first-parent output contains the resolved base alongside malformed lines, contradicting the helper’s stated off-shape handling and the fail-closed clearance rule.

**integration: approve**

**record: approve**
```

## Opus panel (round 3)

Round 3 signed off over the SHIP-to-R2 range with rounds 1-2 appended.

- `adversarial` approve
- `consistency` needs-attention: (2) the first-parent docstring still says off-shape reads False while the code returns None; membership strips inconsistently with validation.
- `integration` approve
- `record` needs-attention: (1) AGENTS.md self-test count stale (879 vs 902).

Fix record: docstring corrected to the shipped None behavior; strip parity completed. Fixed in `366b5d3`; the record count rides the stamp commit (convention pre-tracked at D00 T01 §42). Gates re-ran green before round 4 (902 cases, validate 0 fatal, plan current).

Raw round-3 output (fenced, not part of the verdict scan; digest `417b9cfb559dfac50b2e47a153e47ddadb5c3809d3b1f9bb62d63155c95ac4b8`):

```
**adversarial: approve**

**consistency: needs-attention** (2)
1. `scripts/todo-graph.py:1809` states off-shape output reads `False`, but `scripts/todo-graph.py:1828` returns `None` for off-shape lines; sibling helpers `git_is_merge` (`scripts/todo-graph.py:1782`) and `git_range_touch_ts` (`scripts/todo-graph.py:1840`) state the shipped behavior ("Off-shape output reads None, never raises"), so only this docstring misdescribes its helper.
2. `scripts/todo-graph.py:1828` validates lines stripped while `scripts/todo-graph.py:1830` tests membership against the unstripped lines, so a line that passes the shape gate with surrounding whitespace is reported as not-on-chain rather than matched.

**integration: approve**

**record: needs-attention** (1)
1. `AGENTS.md:55` records `self-test # 879 cases` while the shipped self-test reports 902 cases (0 failed); 879 was set at `e90de6e` (§30) and this candidate's new fixtures raised the count.
```

## Opus panel (round 4)

Round 4 confirmed over the R3-fix delta with rounds 1-3 appended.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` needs-attention: (1) AGENTS.md self-test count stale (879 vs 902).

Filing record: sign-off confirmation, below the blocking bar, so fixed at stamp instead of re-rounding. The AGENTS.md count syncs 879 to 902 in the stamp commit; the convention stays pre-tracked at D00 T01 §42. No fix commit.

Raw round-4 output (fenced, not part of the verdict scan; digest `2e7380afd67da0965c185c83704875cfd6d8f4f053113860e10add1d1c001892`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)
1. `AGENTS.md:55` records `self-test # 879 cases` while the shipped self-test reports `902 cases, 0 failed`.
```

## Review notes

Clearance-core section with shared-surface touch: seven legs across the query, five new git helpers, canned profiles, fixture sections, and real-git pins. The panel's six corrections (merge-None, off-shape strictness twice, first-parent base chain, strip parity twice) all landed before close; the R2 adversarial rejected as a false positive with reasons on record; the sign-off record fixed at stamp (§42 pre-tracks the convention going forward). The implementation caught two of its own defects by mutation (non-hex fixture shas went vacuous, fixed-span ID search subsumed by the proof loop) before the panel ever saw them. No panel leftovers. Architecture gate skipped silently (no editor state, storage, extensibility, protocol, or consent surface in the diff).

## Plan review

Round: `gpt-5.6-sol` high over §31 plus §22 plus §23, 2026-09-19. Neighbors: §22 and §23 (both Depends filers: §31 filed from their reviews). Dependents: two refs by the mechanical rule (no reverse Depends rows; no first-clause XREF consumers; §22 and §23 ride as the filers per the §26/§27 precedent). The run minted through the `run-id` genesis path (no prior claims, bare base). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (17 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §31, D00 T01 §22, D00 T01 §23]; dependents [D00 T01 §22, D00 T01 §23]; bytes 21943; run 20260919-D00-T01-S31-gpt

Ledger (namespaced IDs per the §16 shape; 12 of 17 filed at §55, 3 duplicates of earlier triage, 2 rejected with reasons on their rows):

Ledger:

- [D00-T01-S31-PR1] [minor] Stamp missing at review time -> rejected plan review runs pre-stamp by design (S28-PR1 precedent); the stamp commit lands the Verified block plus row flip
- [D00-T01-S31-PR2] [minor] Wall-clock precedence unproven -> duplicate D00-T01-S22-PR8
- [D00-T01-S31-PR3] [minor] Reviewed tree unnamed -> filed D00 T01 §55
- [D00-T01-S31-PR4] [minor] Fix off the live branch -> rejected clearance gates the review, which precedes merge; live-HEAD ancestry would forbid pre-merge clearance, and strict ancestry on the reviewed candidate is the designed mechanism
- [D00-T01-S31-PR5] [minor] Regressions never re-open -> filed D00 T01 §55
- [D00-T01-S31-PR6] [minor] Range-candidate join loose -> filed D00 T01 §55
- [D00-T01-S31-PR7] [minor] Touch stays file-level -> filed D00 T01 §55
- [D00-T01-S31-PR8] [minor] Execution unattested -> filed D00 T01 §55
- [D00-T01-S31-PR9] [minor] Test run unproven -> filed D00 T01 §55
- [D00-T01-S31-PR10] [minor] Merge laundering through ranges -> filed D00 T01 §55
- [D00-T01-S31-PR11] [minor] Provenance bytes unverified -> filed D00 T01 §55
- [D00-T01-S31-PR12] [minor] Provenance accepts any object -> filed D00 T01 §55 (the branch-reachability half is rejected with PR4's rationale; the commit peel is the filed remainder)
- [D00-T01-S31-PR13] [minor] Short SHAs ambiguous -> duplicate D00-T01-S30-PR2
- [D00-T01-S31-PR14] [minor] Unprovable reads silent -> filed D00 T01 §55
- [D00-T01-S31-PR15] [minor] Head join unpinned -> filed D00 T01 §55
- [D00-T01-S31-PR16] [minor] Reviewer runtime unbounded -> duplicate D00-T01-S23-PR16
- [D00-T01-S31-PR17] [minor] Runner drift unprobed -> filed D00 T01 §55
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `f62e97bb6b95e7423211c22ee5a82638f77cdf55c034bdeb9a0f38671eb9c252`):

```
- §31 has every item marked complete but no `Verified:` stamp; add the required review, candidate range, gates, CRUD evidence, and duration because completed checkboxes alone cannot close the section.
- Fix precedence relies on author-controlled commit timestamps and editable review durations; strengthen §31 “Fix predates attestation” with repository-order evidence from a recorded attestation commit because wall-clock comparison does not prove causal order.
- The target review is not required to name a candidate containing the fix tip; add a canonical reviewed-tip field and require `fix tip <= reviewed tip` in §31 because a later review can currently attest a different tree.
- Fix tips and ranges are not required to be reachable from the validated live branch; add live-HEAD ancestry to §31 strict ancestry because a detached or abandoned branch could clear a finding while the shipped plan remains defective.
- Clearance is not invalidated when later commits revert or alter the fix or proof; add live-tree preservation or automatic re-review to §31 because tip-only evidence can remain green after regression.
- Range clearance only requires the reviewed candidate to precede the range tip; require the candidate to be an ancestor of the range base and each qualifying touch to descend from it in §31 because the declared range can include pre-finding work.
- Section attribution still accepts a file-level touch outside the target section; bind the qualifying diff hunk to the resolved section span in §31 because an unrelated edit in the same TODO file can satisfy clearance.
- “Proof binds execution” is inconsistent with its delivered definition-site and file-touch checks; restore exact command, exit status, result digest, and reviewed candidate fields in §31 because the stated checkpoint claims execution evidence that the plan explicitly does not collect.
- A named test may be skipped, unreachable, or merely pre-existing while the proof file receives a cosmetic edit; require test discovery plus recorded execution of that exact test in §31 because definition text and file touch do not prove acceptance behavior ran.
- Merge exclusion applies to the tip while a range can still contain the real remediation in a merge followed by a cosmetic non-merge touch; extend §31 merge policy to the evidentiary commits or bind the remediation diff because merge-carried fixes can be laundered through a later tip.
- Provenance paths are checked in the current workspace while their digest is deliberately never verified; require the path from the recorded candidate tree and recompute its digest in §23 because mutable evidence cannot support durable provenance.
- Provenance accepts any Git object through `^{object}`; require a commit peel and branch reachability in §23 because blobs, trees, tags, and dangling commits are not valid reviewed candidates.
- Legal short SHAs conflict with §22’s immutable-candidate goal; persist canonical full object IDs in §22 and §23 because abbreviations can later become ambiguous and change validation outcomes.
- Git-unavailable provenance checks silently skip instead of producing an explicit unverified gate state; add an indeterminate verdict to §23 plan health because a green result from an export can be mistaken for verified evidence.
- Supersession governs which ledger row is current, but the supplied clearance rules do not explicitly require proof and fix tokens to bind to the unsuperseded head; add that join to §23 because an obsolete amendment must not satisfy the active finding.
- Output byte and line caps do not bound reviewer runtime, subprocess memory, or process-tree cleanup; add streaming capture, a wall-clock timeout, and deterministic termination to §23 because a silent or stalled reviewer can still block automation.
- The plan-runner compatibility proof is a one-time manual probe against one installed CLI; add a bounded capability probe in CI under §23 because future CLI drift can break every review run before semantic validation begins.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `f25c064`: `todo-graph self-test: 902 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `f25c064`: `24 todos, 229 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `f25c064`: `implementation plan is current -- 71/224 sections complete (32%)`, exit 0.

Provenance: candidate f25c064; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 19844f550e4fb7baafce0eef2d38ec2284e686e06c668a6f902e8c8243362dfc; path docs/reviews/00-workspace/D00-T01-s31.md; run 20260919-D00-T01-S31-gpt
