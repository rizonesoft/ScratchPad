# D00 T02 S36 review - Binding Guard Residuals

Candidate: d0aa13c0298c63025104ef2f4f0ef47f932bd1f1 (Started plus implementation), 805b31d62a5f331b2f491011e1df4e807c669cdf (round-1 fixes), c455fca68616d4cb669bf67f4dfc6af7080d4f7e (round-2 fixes), d6f6130f28f87014e1516c57efc9a612b34bb7f6 (sign-off R3-F1..F3 fixes, reviewed tip).
Tier: Full panel. The diff changes the shared UI test harness (the binding manifest guard, the input funnel, `UiLaunch`), the app's bound menu handlers and tab accelerator helper (a test-only mutation seam in `Notepad.Core.TestMutation`), and `docs/ui-input-audit.md`; every chord test and the nightly Interactive leg consume them.

## Evidence map

- Item 1 (mutation run): `Notepad.Core.TestMutation` (marker-gated swap and observe modes), the `Mutated` guard on every bound handler (required by `HandlerRouting`), `MutationHandled` in the sanctioned `AddAccel` body; `BindingMutationTests.CoveringTestFailsWithItsCommandSwapped` (baseline pass plus post-press assertion failure under the swap), the executed plant `MutationPlantTests.PlantedConstantLocalAssertion` via `PlantedConstantLocalSurvivesTheMutationRun`, and `ChildRunReadsAPassingPureTest`.
- Item 2 (physical identity): `BindingManifest.Physical`, `PressedIdentities`, every distinct physical declaration covered (R3-F1); `NumPadDeclarationWithMainRowPressFails`.
- Item 3 (layout matrix): the Layout matrix table plus `LayoutMatrixProblems`; the US row switches to and verifies 00000409 (R3-F2); `LayoutMatrixPlantsFail`.
- Item 4 (routing oracle): the Routing oracle table plus `RoutingOracleProblems`; `ChordRoutingTests.EveryLiveChordRoutesPerTheOracle` proves every live row on four surfaces in observe mode (R1-F4), with lost log lines failing the app (R2-F2); `RoutingOraclePlantsFail`.
- Item 5 (exactly once): `BindingManifest.ExactlyOnce` plus `AssertsExactlyOnce` (a count of one, R1-F2); `PlantedDoubleRegistrationFails`.
- Item 6 (partial sends): `UiInput.ChordDown` and `ChordUp`; `PartialSendReleasesExactlyTheInjectedKeys`.
- Item 7 (owner states): the Enablement states table plus `EnablementStateProblems`; `MenuLabelTests.DisabledExemptionsHoldAcrossEnablementStates` builds read-only document and text edited twice; `EnablementStatePlantsFail`.
- Item 8 (Ctrl+wheel): `UiInput.Wheel` over `WheelChecked`; `WheelWithFocusLostScrollsNothing`, `WheelOnTargetScrollsOnceBetweenCtrlDownAndUp`; audit inventory row.

## Self-review

- Does it make sense: eight items from the §28 plan review and sign-off; each rule is a pure check with a planted failure, and the physical halves are fenced and Night-owed D00-T02-S36-N1.
- Is it logical: the mutation seam is test-only by construction (the run marker), a swap proves discrimination, the baseline plus press-line rule proves causation, and observe mode proves routing with no side effects.
- Did it break anything: Run A stays green with a clean foreground log at every candidate; the population gate reads OK at run-a=245/298 run-b=4/4 interactive=43/74.

Arch trigger: none - a test-only seam gated by the test-run marker and test instrumentation; no editor state, storage format, extension API, AI, protocol, or consent/undo surface changes.

## GPT panel Round 1

`adversarial` needs-attention
`consistency` needs-attention
`integration` needs-attention
`record` needs-attention

Telemetry: round 1; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

Run 20260925-D00-T02-S36-codex-c06751119 (bulk) over d0aa13c0298c63025104ef2f4f0ef47f932bd1f1 (output check PASS). R1-F1 (adversarial 1), R1-F2 (adversarial 2), R1-F3 (consistency 1), R1-F4 (integration 1), R1-F5 (record 1) fixed in 805b31d.

```
**adversarial: needs-attention** (2)
1. `tests/UI/BindingMutation.cs:84` accepts any child-test failure as a killed mutation, and `tests/UI/BindingMutationTests.cs:38` discards the exit code without establishing an unmutated passing baseline. Launch, setup, or unrelated assertion failures can therefore certify coverage without demonstrating sensitivity to the mutated command.
2. `tests/UI/BindingManifest.cs:1036` checks only that an exactly-once test presses the chord and asserts afterwards. A test asserting merely that an outcome exists passes even when dispatch occurs twice; the planted double-registration test only demonstrates rejection of a missing registry entry, not duplicate delivery.

**consistency: needs-attention** (1)
1. `tests/UI/BindingMutation.cs:14-19` explicitly substitutes suppression for the contract's required host-member swap. Suppression cannot reject an assertion satisfied by any dispatched command, leaving the wrong-command outcome gap unresolved despite marking the requirement complete.

**integration: needs-attention** (1)
1. `tests/UI/ChordRoutingTests.cs:196-198` hardcodes oracle lookup to `Ctrl+T` and `Tabs.NewTab`. The other commands' surface expectations in `docs/ui-input-audit.md:295-338` receive table validation only, so their editor, tab-strip, menu, and modal routing remains unproved.

**record: needs-attention** (1)
1. `tests/UI/BindingMutationTests.cs:87-106` never executes the planted constant-local test: it parses source and supplies a fabricated passing summary to `BindingMutation.Problem`. Consequently, `todo/00-workspace/TODO-02-test-backbone.md:1167` marks the execution-based acceptance condition complete without its required proof.
```

## GPT panel Round 2

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` approve

Telemetry: round 2; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

Run 20260925-D00-T02-S36-codex-c06751119-r2 (bulk) over 805b31d62a5f331b2f491011e1df4e807c669cdf (output check PASS). R2-F1 (adversarial 1) and R2-F2 (integration 1) fixed in c455fca.

```
**adversarial: needs-attention** (1)
1. `tests/UI/BindingMutationTests.cs:33-34` still runs only the mutated candidate without establishing a passing unmutated baseline. `tests/UI/BindingMutation.cs:142-155` accepts any assertion failure after the press, including an unrelated assertion that also fails without mutation. R1-F1 remains partially unresolved: source-line ordering does not establish that the swap caused the failure.
**consistency: approve**
**integration: needs-attention** (1)
1. `src/ScratchPad/MainWindow.xaml.cs:617` and `src/ScratchPad/MenuBar.xaml.cs:189` discard dispatch-log write failures. `tests/UI/ChordRoutingTests.cs:205-210` interprets an absent entry as suppression, so an incorrectly dispatched command whose log write fails passes a `suppress` expectation. Propagate recording failures through an independently checked channel so the routing proof cannot certify lost evidence.
**record: approve**
```

## GPT panel Round 3

`adversarial` needs-attention
`consistency` needs-attention
`integration` needs-attention
`record` approve

Telemetry: round 3; model gpt-6-astra; effort high; duration unknown; outcome needs-attention; tokens unknown

Run 20260925-D00-T02-S36-codex-c06751119-r3 (signoff) over c455fca68616d4cb669bf67f4dfc6af7080d4f7e (output check PASS). Governing round. R3-F1 (adversarial 1: coverage credits only the first physical declaration), R3-F2 (consistency 1: the US row never activates its layout), and R3-F3 (integration 1: a static field retains a closed window's tab bar in production) sit below the bar (no safety, data-integrity, or stamp-invalidating defect); per the §18/§21 precedent all three were fixed in d6f6130 and re-gated at the stamp (Run A green with a clean foreground log, population OK, manifest and mutation suites green on d6f6130).

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | 805b31d |
| R1-F2 | fixed | 805b31d |
| R1-F3 | fixed | 805b31d |
| R1-F4 | fixed | 805b31d |
| R1-F5 | fixed | 805b31d |
| R2-F1 | fixed | c455fca |
| R2-F2 | fixed | c455fca |
| R3-F1 | fixed | d6f6130 |
| R3-F2 | fixed | d6f6130 |
| R3-F3 | fixed | d6f6130 |

```
**adversarial: needs-attention** (1)
1. `tests/UI/BindingManifest.cs:825,880-881` collapses declarations by canonical chord and command, retaining only the first physical identity. When one item declares both main-row and NumPad variants, coverage of the first variant credits both. Validate coverage against every distinct physical declaration.

**consistency: needs-attention** (1)
1. `tests/UI/ChordRoutingTests.cs:245-254` labels the new case US English but never activates or verifies layout `00000409`. It presses under the inherited layout, so a passing test does not prove the US Shift-dependent-plus row.

**integration: needs-attention** (1)
1. `src/ScratchPad/MainWindow.xaml.cs:578,596` unconditionally stores the latest window’s `TabBar` in a static field, including ordinary production runs, and never clears it. Closing that window while another remains open retains its control graph indefinitely until another window is created. Capture the owning bar in the accelerator callback instead.

**record: approve**
```

## Plan review

Round: `gpt-6-astra` medium (plan-primary slot) over §36 plus its Depends neighbor §28 and the section it shares the zoom rows with, D02 T01 §5, 2026-09-25. Output check-plan PASS (14 findings, one per line). Triage: 5 filed as the new D00 T02 §43, 3 filed as items on D02 T01 §5, 2 duplicate of open D02 T01 §5 items, 4 rejected.

Manifest: sections [D00 T02 §36, D00 T02 §28, D02 T01 §5]; dependents [none]; bytes 18364; run 20260925-D00-T02-S36-codex-c06751119-r4

Ledger:

- [D00-T02-S36-PR1] [minor] a validity rule for mutation failures -> rejected shipped in R1-F1 and R2-F1: an unmutated baseline must pass and the failure must be an assertion in the covering method after the press line, so launch errors and timeouts never count
- [D00-T02-S36-PR2] [minor] mutation activation evidence and isolation -> filed D00 T02 §43
- [D00-T02-S36-PR3] [minor] future covered rows enroll automatically -> rejected shipped: the theory's cases derive from the audit's covered rows at discovery, and physical variants are required per declaration (R3-F1)
- [D00-T02-S36-PR4] [minor] layout rows' evidence -> rejected shipped: every matrix row's fenced case is ChordRoutingTests.LayoutAltGrAndNumpadKeepTheirIdentity, inside the owed D00-T02-S36-N1 filter
- [D00-T02-S36-PR5] [minor] in-app focus change during wheel injection -> filed D00 T02 §43
- [D00-T02-S36-PR6] [minor] release failures during cleanup -> filed D00 T02 §43
- [D00-T02-S36-PR7] [minor] held-key auto-repeat versus duplicate delivery -> filed D00 T02 §43
- [D00-T02-S36-PR8] [minor] enablement transitions and restored usability -> filed D00 T02 §43
- [D00-T02-S36-PR9] [minor] zoom persistence contract -> duplicate of D00-T02-S21-PR22 on D02 T01 §5 (item: "Zoom persistence is one contract")
- [D00-T02-S36-PR10] [minor] word-wrap scope derived from the baseline -> duplicate of D00-T02-S28-PR10 on D02 T01 §5 (item: "Word-wrap persistence scope is explicit")
- [D00-T02-S36-PR11] [minor] Ctrl+wheel gesture semantics -> filed D02 T01 §5
- [D00-T02-S36-PR12] [minor] zoom and wrap preserve the document -> filed D02 T01 §5
- [D00-T02-S36-PR13] [minor] F5 across timezone, DST, and live locale -> filed D02 T01 §5
- [D00-T02-S36-PR14] [minor] owed evidence tied to a resolution gate -> rejected by policy: completion-first ships with Night-owed debt (AGENTS.md), and the collector records each owed id's collection or red against its section

End of ledger

Raw plan output (fenced; fences strip from every scan):

```
- Mutation failure lacks a validity rule; D00 T02 §36 should require a passing unmutated control and an outcome assertion failure under mutation, excluding launch errors, timeouts, and unrelated failures, so broken infrastructure cannot certify coverage.
- Mutation selection and isolation are unspecified; D00 T02 §36 should require a distinguishable wrong outcome, verify the mutation activated, and reset it between cases, so equivalent handlers or leaked switches cannot produce misleading evidence.
- Future commands can escape the current 27-case mutation population; D00 T02 §36 and D02 T01 §5 should require automatic enrollment when rows become covered, including every physical variant, so enabling zoom and F5 preserves the guard’s guarantees.
- Layout coverage is weaker than its stated contract; D00 T02 §36 requires fenced cases for every matrix row but names only the US Shift-plus execution in its debt, so it should account for executed or owed evidence for every named layout and AltGr row.
- Focus-loss rules conflict; D00 T02 §28’s verification aborts only when input leaves the app, while §36 requires wheel focus binding, so §36 should define and test focus changes to another in-app control during injection to prevent input reaching the wrong target.
- Cleanup assumes key-up injection succeeds; D00 T02 §36 should own release failures, window closure, and cancellation during recovery, with bounded recovery and explicit failure reporting, because a failed cleanup can leave modifiers stuck.
- Exactly-once coverage lacks a held-key contract; D00 T02 §36 should distinguish duplicate delivery from legitimate auto-repeat and test press, hold, and release according to captured command behavior, so zoom remains usable without repeating destructive commands.
- Enablement evidence can pass with incomplete states; D00 T02 §36 should require every owner-declared state and relevant transition to execute, checking both suppressed effects and restored usability, because one readable state does not validate the matrix.
- Zoom persistence contradicts itself; D02 T01 §5 initially requires storage-backed persistence but later allows session-only levels, so it should resolve one capture-backed contract covering current tabs, new tabs, windows, and restart before implementation.
- Word-wrap scope is nominally undecided but its acceptance test mandates identical state across windows; D02 T01 §5 should derive propagation and restart expectations from the baseline before fixing the fixture, avoiding an accidental product behavior decision.
- Ctrl+wheel specifies safe injection without defining gesture semantics; D02 T01 §5 should own wheel direction, partial-delta accumulation, multi-detent input, modifier release, and clamping at limits, with §36 supplying delivery fixtures, so high-resolution mice behave consistently.
- Zoom and wrap lack document-preservation checks; D02 T01 §5 should prove changes preserve text, selection, undo history, dirty state, and the baseline’s caret and viewport behavior, preventing visual preferences from disrupting editing.
- F5 formatting lacks timezone and live preference coverage; D02 T01 §5 should extend its deterministic fixture to timezone, daylight-saving boundaries, and locale changes, so inserted timestamps follow the user’s current regional settings.
- Deferred physical evidence is not tied here to an explicit resolution gate; D00 T02 §36 should link each owed case to its candidate, collector result, and failure owner, and distinguish pending execution from proven checkpoint claims, so checked boxes cannot conceal unverified behavior.
```

## Live proof

Provenance: candidate d6f6130f28f87014e1516c57efc9a612b34bb7f6; command dotnet test src/ScratchPad.slnx --no-build --filter "Category!=Interactive&Category!=Primary" -e SCRATCHPAD_BACKGROUND=1 plus tools/Test-TestPopulation.ps1; exit 0; tool .NET SDK 10.0.400; digest 6ee3921df6ed5e39a8a85cc248fc95e3b814b50d9f00b4820aa5d49fc09e5856; path tests/UI/BindingManifest.cs; run 20260925-D00-T02-S36-codex-c06751119-r4

```
population OK run-a=245/298 run-b=4/4 interactive=43/74
Passed!  - Failed:     0, Passed:     1, Skipped:     0, Total:     1 - Smoke.dll (net10.0)
Passed!  - Failed:     0, Passed:   373, Skipped:     0, Total:   373 - Unit.dll (net10.0)
Passed!  - Failed:     0, Passed:    35, Skipped:     0, Total:    35 - Protocol.dll (net10.0)
Passed!  - Failed:     0, Passed:   283, Skipped:    15, Total:   298 - UI.dll (net10.0)
```
