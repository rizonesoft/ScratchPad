# Review: D00 T01 §25 -- Rule-24 Comment Touch-Up

Candidate: `7f24021` + `b9bf393` + `aacaa83` (checked `fence` pipeline, R1-R3 output appended; output `check-panel PASS` on all three rounds). Panel tier: docs-only plus required tests (comment-only behavior diff with the two probes item 2 requires), 3 Opus rounds.

## Self-review

Three questions over the candidate diff plus its blast radius, answered with files and behavior. No self-review fixes; the lenses found the rest.

Does it make sense: both rule-24 descriptions name the review-window fourth leg (record-expiry window, bounds inclusive), and one probe per site locks its comment block's own extent. Exactly the contract, no behavior change outside the two additive checks.

Is it logical: the comment wording matches the rule code (review inside record..expiry, bounds inclusive, in-file dates only); probes anchor on the entry line plus rule header and read fixed windows covering both comments. Failure mode is loud (a moved comment fails the probe, never silently unguards).

Did it break anything else: suite moves 784 to 786 by exactly the 2 new probes; live validate stays 0 fatal; AGENTS.md count synced. Blast radius nil.

## Opus panel (round 1)

- `adversarial` advisory: (1) fixed-window probe brittleness (inserted lines break the grep while the leg stays present).
- `consistency` advisory: (1) severity citation reads as §25 adding the leg, against the validator's accurate provenance.
- `integration` approve
- `record` approve

Fix record: probes scan each comment block's own extent (upward/downward to the first non-`#` line); severity citation matches the validator's provenance (leg from §21 review R4, named here). Fixed in `b9bf393`; gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `a3aa96b9b6649d7cda3fc2a7dcec41028151904547fcfa04c15b22f965e6ed36`):

```
**adversarial: advisory (1)**
1. `scripts/todo-graph.py:8732` and `scripts/todo-graph.py:8739`: both probes scope their grep to a fixed-size window anchored on an adjacent line (4 lines above the `SEVERITY_MAP` entry; 10 lines from the rule header, while the rule-24 comment is 11 lines at `scripts/todo-validate.py:1331-1341`). Inserting two lines above the map entry, or pushing the `record..expiry` clause past the 10th header line, makes both probes report a missing leg that is still present. The failure is loud, not silent, so this is brittleness rather than a coverage hole; anchoring on the comment block's own extent (scan upward/downward to the first non-`#` line) removes it.

**consistency: advisory (1)**
1. `scripts/todo-graph.py:941-942`: the citation `(D00 T01 §21, review-window leg D00 T01 §25)` reads, under the convention set by the sibling entry at `scripts/todo-graph.py:927-929` (`provenance-malformed`: `(D00 T01 §20, checkable shape plus run equality D00 T01 §23)`), as "§25 added the review-window leg". §25 is comment-only; the leg landed in §21 review R4, which is what `scripts/todo-validate.py:1336-1337` states correctly (`§21 review R4, named here D00 T01 §25`). The two sites now describe the same provenance differently.

**integration: approve**

**record: approve**
```

## Opus panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` needs-attention: (1) item-2 Done note describes the superseded fixed windows.

Fix record: Done note rewritten to the shipped extent-scanning probes with a dated correction. Fixed in `aacaa83`; gates re-ran green before round 3.

Raw round-2 output (fenced, not part of the verdict scan; digest `2c992dade40c98290a0812de7f41ac08cec4f1355b06bdf94ce5cd4bcb6493d2`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:693`: the item-2 Done text states "each scoped to its comment window (4 lines above the map entry, 10-line rule header)", which describes the superseded fixed-window probes. The shipped probes at `scripts/todo-graph.py:8730-8735` and `scripts/todo-graph.py:8740-8746` scan each comment block's own extent (upward/downward to the first non-`#` line), with no 4-line or 10-line bound. The record claims an implementation the candidate range no longer contains.
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Fix record: none (all approve). Panel closed.

Raw round-3 output (fenced, not part of the verdict scan; digest `fc52c157b6f4a3154c61d8c9fddcca514347b90416974ddb51fe4de3dee22ecb`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Review notes

Comment-only section with its two required probes: the lenses' three corrections (probe anchoring, citation provenance, Done-note wording) all landed before close. No panel leftovers. Plan-review PR2 plus PR3 corrected §25's own text in place before the stamp (open section, record edits, noted on their ledger rows); PR4 plus PR6 plus PR8 filed as tracked work.

## Plan review

Round: `gpt-5.6-sol` high over §25 plus §21, 2026-09-19. Neighbors: §21 (Depends plus XREF); dependents: none (no Depends beyond §21, no XREF consumers point at §25). The run minted through the `run-id` genesis path over the findings file (no prior claims, bare base). Runner note: the skill's `codex exec` template ran verbatim over stdin; no new CLI drift. Output `check-plan PASS` (9 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §25, D00 T01 §21]; dependents [none]; bytes 10192; run 20260919-D00-T01-S25-gpt

Ledger (namespaced IDs per the §16 shape; 3 of 9 filed at §46 plus two §29 items, 1 duplicate with canonical ID, 5 rejected with reasons on their rows):

Ledger:

- [D00-T01-S25-PR1] [minor] Closure records missing pre-stamp -> rejected satisfied by the stamp step: stamp, findings file, and Live proof land with the stamp commit; re-verify post-stamp
- [D00-T01-S25-PR2] [minor] Checkpoint contradicts its correction -> rejected reworded in place before stamp (open section, record edit): lone expectation is 786
- [D00-T01-S25-PR3] [minor] No-behavior-changes overbroad -> rejected clarified in place before stamp (open section, record edit): production unchanged, suite moves by item 2
- [D00-T01-S25-PR4] [minor] Probes lock one leg only -> filed D00 T01 §46
- [D00-T01-S25-PR5] [minor] Dependents block misses §21 XREF -> rejected wrong direction: §21 files into §25 (upstream), nothing downstream consumes it; the block is correct
- [D00-T01-S25-PR6] [minor] Passed review dates stay live -> filed D00 T01 §29
- [D00-T01-S25-PR7] [minor] Grandfathered acceptance covers nothing -> duplicate D00-T01-S21-PR3
- [D00-T01-S25-PR8] [minor] Inert waivers stay silent -> filed D00 T01 §29
- [D00-T01-S25-PR9] [minor] No declarative rule spec -> rejected validator-architecture redesign disproportionate to a comment-sync residual; the §46 probes hold the line
End of ledger

## Live proof

`python3 scripts/todo-graph.py self-test` on `8718ea1`: `todo-graph self-test: 786 cases, 0 failed`, exit 0.

Provenance: candidate 8718ea1; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest e76d02c5bd90c57a9237c58cc6b769b82bc9cd191e1c24a703827f234d699324; path docs/reviews/00-workspace/D00-T01-s25.md; run 20260919-D00-T01-S25-gpt

`python3 scripts/todo-graph.py validate` on `8718ea1`: `24 todos, 220 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate 8718ea1; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest 9e9e1f2125baf50f45f4187800e435cce156b4a5677245c223f545d80d108dab; path docs/reviews/00-workspace/D00-T01-s25.md; run 20260919-D00-T01-S25-gpt

`python3 scripts/todo-graph.py plan --check` on `8718ea1`: `implementation plan is current -- 65/215 sections complete (30%)`, exit 0.

Provenance: candidate 8718ea1; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest bf96796e2b0f391efb19767d3c87a4d8e6f93e526e60114762387a18649db274; path docs/reviews/00-workspace/D00-T01-s25.md; run 20260919-D00-T01-S25-gpt

`python3 scripts/todo-graph.py query plan-health --check` on `8718ea1`: `gate: ok`, exit 0.

Provenance: candidate 8718ea1; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 0a7685d0d2d1ae4ecb4eb3b11fbca5c7fbc142fad89722730c96080980b1d3b7; path docs/reviews/00-workspace/D00-T01-s25.md; run 20260919-D00-T01-S25-gpt
