# D00 T02 S43 review - Binding Guard Second Residuals

Candidate: af49989 (Started plus implementation), c977b9c (round-1 fixes), 6e0baf5 (round-2 fixes), b0492f9 (sign-off R3-F1 fix, reviewed tip).
Tier: Full panel. The diff changes the app's command dispatch (`src/ScratchPad/MenuBar.xaml.cs`, `src/ScratchPad/MainWindow.xaml.cs`), adds `src/Notepad.Core/HeldChord.cs` and swap evidence in `src/Notepad.Core/TestMutation.cs`, and changes shared UI-harness surface: the input funnel (`tests/UI/UiInput.cs`), the binding guard (`tests/UI/BindingManifest.cs`), the mutation run (`tests/UI/BindingMutation.cs`), and the menu enablement proofs (`tests/UI/MenuLabelTests.cs`); consumers are every keyboard-driven UI test, the mutation and routing runs, and every bound command.

## Evidence map

- Item 1 (mutation activation): `TestMutation.RecordSwap`/`SwapLine`, swap recording in both swap sites, `BindingMutation.Problem(..., activated)`, fresh per-case child environments with their own dispatch log; `VerdictsSeparateKilledSurvivedAndInconclusive` pins inconclusive for a failure and a pass without swap evidence.
- Item 2 (in-app focus moves): documented outcome (bound to the original target; only leaving the process aborts); `CommandMovingFocusInsideTheAppIsNotAnInterruption` (keys) and `InAppFocusMoveBetweenCtrlAndWheelStillScrollsOnce` (wheel).
- Item 3 (cleanup failures): `UiInput.ChordUp` attempts every release, names each stuck key, and is bounded; `ThrowingReleaseNamesTheStuckKeyAndReleasesTheRest`, `CancelledReleaseAndTheBoundReportEveryStuckKey`, `WindowClosingDuringRecoveryStillReleases`.
- Item 4 (held keys): `Notepad.Core.HeldChord` (repeatable zoom and tab cycling, one-shot otherwise; a recorded default), the root observer and the suppression in `AddAccel` and `MenuBar.Mutated`, the guard's sanctioned observer shape and helper body; `HeldChordTests` reads the count per class. The physical held-key proof is Interactive, Night-owed with the §36 mutation run.
- Item 5 (transitions): `MenuLabelTests.LeavingAStateRestoresEveryItemsEnablement` (read-only entry and exit over every bound item; selection made and cleared in one window).

## Self-review

- Does it make sense: five items, each with a fixture; item 4 records its class split as a default because stock held-key behavior is not captured.
- Is it logical: every new rule narrows what can happen (inconclusive instead of killed, suppressed repeats instead of duplicates, reported stuck keys instead of silent ones); the guard's exemption is one exact shape, and a fixture shows any other hook still reads.
- Did it break anything: the AddAccel sanctioned body and its test text moved together; every bound handler still opens with the Mutated guard; Run A at af49989 UI 300/16/0.

Arch trigger: none - UI test harness and command dispatch guards for auto-repeat; no editor state, app storage format, extension API, AI, protocol, or consent/undo surface changes.

## GPT panel Round 1

Candidate af49989, slot bulk, run 20260925-D00-T02-S43-codex-c06751119.

**adversarial: needs-attention** (1)
1. `src/Notepad.Core/HeldChord.cs:31` and `src/ScratchPad/MenuBar.xaml.cs:176`: suppression uses a thread-wide repeat flag without identifying the command or invocation source. After an auto-repeat, clicking a one-shot menu command while the key remains held suppresses that mouse action. Losing focus before key-up can also leave suppression active. Mouse invocation therefore does not always dispatch as documented.

**consistency: approve**

**integration: needs-attention** (1)
1. `tests/UI/MenuLabelTests.cs:94` and `tests/UI/MenuLabelTests.cs:103`: the transition test compares the returned writable state with another observed baseline, but checks only that the read-only observations are nonempty. It passes if enablement never changes at all, including when read-only restrictions are broken. Assert the documented enablement on entry and after exit; the contract also requires coverage of each owner-declared state.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:1390` marks cleanup complete, but `tests/UI/UiInputFunnelTests.cs:362` exercises only a throwing release. Neither cancellation nor window closure during recovery is exercised, and `tests/UI/UiInput.cs:253` supplies no elapsed-time bound around synchronous release callbacks. The checked item claims recovery guarantees that the candidate does not establish.

Telemetry: round 1; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | c977b9c (the repeat flag lives for one input message; `ReleaseEndsTheHoldAndMouseInvocationAlwaysRuns`) |
| R1-F2 | fixed | c977b9c (`LeavingAStateRestoresEveryItemsEnablement` over every bound item, read-only and selection; MenuLabelTests 3/3) |
| R1-F3 | fixed | c977b9c (bounded release pass, cancellation, closing window; `CancelledReleaseAndTheBoundReportEveryStuckKey`, `WindowClosingDuringRecoveryStillReleases`) |

## GPT panel Round 2

Candidate af49989~1..c977b9c over the section's own files, slot bulk, run 20260925-D00-T02-S43-codex-c06751119-r2. Round-1 findings R1-F1 and R1-F2 did not re-report; R1-F3 re-reported in part as R2-F1.

**adversarial: needs-attention** (1)
1. `tests/UI/UiInput.cs:265-274`: the deadline is checked only before calling synchronous `release`. A release that blocks never returns to the deadline check, preventing remaining releases and stuck-key reporting indefinitely. The simulated elapsed-time test does not establish the required bounded recovery; R1-F3 remains partially unresolved.

**consistency: approve**

**integration: needs-attention** (1)
1. `tests/UI/MenuLabelTests.cs:110-111`: accepting `baseline.Count >= all.Count - disabled.Count` permits missing bound items, including enabled commands. Restoration is checked only for items present in that incomplete baseline, so missing commands escape verification. Require every expected bound item to be observed before comparing entry and exit states.

**record: approve**

Telemetry: round 2; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R2-F1 | fixed | 6e0baf5 (each release runs with the time left; `BlockingReleaseIsReportedWithinTheBound`) |
| R2-F2 | fixed | 6e0baf5 (every expected item read in every state before comparing; MenuLabelTests 3/3) |

## GPT panel Round 3

Candidate af49989~1..6e0baf5 over the section's own files, slot signoff, run 20260925-D00-T02-S43-codex-c06751119-r4. Round-1 and round-2 findings did not re-report. R3-F1 sits below the blocking bar (a late key-up from an abandoned release in a test harness; no user data, no product behavior), so it was fixed in b0492f9 and re-gated at the stamp (the §18/§21 precedent).

**adversarial: needs-attention** (1)
1. `tests/UI/UiInput.cs:277,289-295`: a timed-out release task remains runnable after `ChordUp` reports failure and clears its tracking list. A queued or blocked callback can subsequently inject key-up during another user or test chord, releasing that chord’s key. The timeout bounds waiting but leaves keyboard input running beyond cleanup.

**consistency: approve**

**integration: approve**

**record: approve**

Telemetry: round 3; model gpt-6-astra; effort high; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | c977b9c |
| R1-F2 | fixed | c977b9c |
| R1-F3 | fixed | c977b9c |
| R2-F1 | fixed | 6e0baf5 |
| R2-F2 | fixed | 6e0baf5 |
| R3-F1 | fixed | b0492f9 (abandon flag checked before injecting; `AbandonedQueuedReleaseNeverInjectsLater`) |

## Plan review

Round: `gpt-6-astra` medium (plan-primary slot) over §43 plus its Depends neighbor §36 and the input contracts §28 and §21, 2026-09-25. Output check-plan PASS (13 findings, one per line). Triage: 12 filed as the new D00 T02 §51, 1 rejected.

Manifest: sections [D00 T02 §43, D00 T02 §36, D00 T02 §28, D00 T02 §21]; dependents [none]; bytes 31150; run 20260925-D00-T02-S43-codex-c06751119-r3

Ledger:

- [D00-T02-S43-PR1] [minor] correlated execution acknowledgment -> filed D00 T02 §51
- [D00-T02-S43-PR2] [minor] failure attribution -> filed D00 T02 §51
- [D00-T02-S43-PR3] [minor] negative-routing cases -> filed D00 T02 §51
- [D00-T02-S43-PR4] [minor] one focus policy per input path -> rejected: §28's focus precondition and §43's mid-chord rule compose, not conflict: §28 binds the target before the press (no key goes out otherwise), and §43 governs only between key-down and key-up, after the key-down already reached that target (`CommandMovingFocusInsideTheAppIsNotAnInterruption`, `InAppFocusMoveBetweenCtrlAndWheelStillScrollsOnce`)
- [D00-T02-S43-PR5] [minor] key-state reconciliation -> filed D00 T02 §51
- [D00-T02-S43-PR6] [minor] containment after a cleanup failure -> filed D00 T02 §51
- [D00-T02-S43-PR7] [minor] release-and-repress and interrupted holds -> filed D00 T02 §51
- [D00-T02-S43-PR8] [minor] parity capture for repeat classes -> filed D00 T02 §51
- [D00-T02-S43-PR9] [minor] one counting definition -> filed D00 T02 §51
- [D00-T02-S43-PR10] [minor] enumerated transitions -> filed D00 T02 §51
- [D00-T02-S43-PR11] [minor] owned held-key debt -> filed D00 T02 §51
- [D00-T02-S43-PR12] [minor] mutation child isolation -> filed D00 T02 §51
- [D00-T02-S43-PR13] [minor] combined hold, focus, and layout matrices -> filed D00 T02 §51

End of ledger

Raw plan output (fenced; fences strip from every scan):

```
- Mutation activation may be credited by a dispatch log even when the substitute never executes; strengthen D00 T02 §43 to require a correlated execution acknowledgment or observable substitute effect, preventing false mutation kills.
- A failing assertion after a press does not establish that the handler swap caused it; D00 T02 §43 should distinguish expected outcome mismatches from cleanup, focus, timeout, and unrelated assertion failures so infrastructure faults cannot count as coverage.
- Suppressed or disabled chords cannot activate a swapped handler by design; D00 T02 §36/§43 should separate positive-dispatch mutation cases from negative-routing cases and require zero dispatch plus unchanged relevant state for suppression.
- §43 permits binding to the original target after an in-app focus move, while §28 requires the expected focus target before input; D00 T02 §43 should define one policy per input path and prove no keystroke or wheel event reaches the newly focused control.
- Cleanup names stuck keys but does not specify how they are detected after a failed release; D00 T02 §43 should reconcile injected-key ownership with observed key state and distinguish confirmed stuck keys from unknown state.
- Cleanup failure has no explicit containment policy for subsequent tests; D00 T02 §43 should stop further physical input when release cannot be confirmed and preserve both the original failure and cleanup diagnostics, preventing cascading failures or escaped shortcuts.
- Held-command classification lacks release-and-repress and interrupted-hold reset requirements; D00 T02 §43 should prove a second deliberate press executes and focus loss, cancellation, or disablement cannot leave a command permanently suppressed or repeating.
- The blanket one-shot default lacks a named parity-capture obligation and successor coverage for newly enabled commands; D00 T02 §43 and the command owners linked from §21 should own stock Notepad captures and explicit repeat classifications, preserving familiar editing behavior.
- Exactly-once dispatch and intentional auto-repeat use incompatible counting language; D00 T02 §36/§43 should define one dispatch per accepted repeat event for repeatable commands and one per physical hold for one-shot commands, including multiply registered chords.
- Enablement transition coverage has only one concrete acceptance example despite promising every owner-defined state; D00 T02 §43 should enumerate required transitions and test disablement during a held chord, including tab switches and selection, clipboard, and undo-history changes.
- §43 attaches its physical held-key proof to §36’s existing Night-owed run without updating the listed test population or candidate identity; D00 T02 §43 should own an explicit debt entry with discoverable cases and candidate-bound collection evidence.
- Fresh mutation children are required, but run isolation is underspecified; D00 T02 §43 should require unique logs, exact target resolution, verified mutation configuration, and bounded child termination so stale evidence or orphan processes cannot contaminate results.
- Layout and surface coverage remain separate from the new hold and focus-transition contracts; D00 T02 §43 should add targeted combinations such as Shift-dependent plus during a hold and menu or modal opening mid-chord, catching failures that isolated matrices miss.
```

## Stamp re-gate

The sign-off's below-bar R3-F1 fix (b0492f9) re-gated at the stamp with the suite display restored: `dotnet test src/ScratchPad.slnx --no-build --filter "Category!=Interactive&Category!=Primary" -e SCRATCHPAD_BACKGROUND=1` at b0492f9 read UI 304 passed, 16 skipped, 0 failed; Unit 387; Protocol 35; Smoke 1; the foreground gate exited 0 (census 264, primary 0, events 272, event-primary 0, uncovered 4, mismatch 0). An earlier attempt's gate red on the operator's Photos window through pid reuse (D00 T02 §49) and a second attempt was discarded after a mid-run checkout; neither is used as evidence.

## Live proof

Provenance: candidate b0492f97c15388f41e2c1a120e9b1d54bf897473; command dotnet test src/ScratchPad.slnx --no-build --filter "Category!=Interactive&Category!=Primary" -e SCRATCHPAD_BACKGROUND=1 beside Bin/ForegroundLog/Debug/ForegroundLog.exe 1500; exit 0; tool .NET SDK 10.0.400 and ForegroundLog; digest 51b78a1ab64fe941035447f51ea0392e4c7bd3efb7cb277984082f2c48af450f; path src/Notepad.Core/HeldChord.cs; run 20260925-D00-T02-S43-codex-c06751119-r3
