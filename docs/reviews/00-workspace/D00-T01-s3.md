# Review: D00 T01 §3 -- CI on Linux and Windows Runners

Candidate: `ef12eb3` (workflow) plus checkpoint evidence commits `6fb4909` (red probe) and `e858133` (revert). No fixes; one round.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Workflow | `.github/workflows/build.yml`: slnf on `ubuntu-24.04`, slnx on `windows-2025`, every push to `main`; run 34785141981 both jobs success |
| 2. Artifact | `intelligent-notepad-stub-windows-x64` (10.8 MB) downloaded from run 34785141981; contains the exe, dll, and compiled XAML |
| 3. Red with cause | Run 34785638088 conclusion failure, both jobs failed in Build steps with `MSB4025 ... Notepad.Core.csproj(11,1)` naming file, line, and cause; probe reverted immediately |
| 4. Pins | Runner labels exact, actions pinned to SHAs (`checkout` 3d3c42e, `upload-artifact` 043fb46, both v7), SDK governed by `global.json` via the provisioners |
| 5. Commit | `ef12eb3`, subject matches the item plus the skill-mandated ref |

Post-revert run 34786154222 is green on both jobs; `main` is green at HEAD.

## Self-review

1. Does it make sense? Yes. The workflow runs exactly the corrected contract; `fetch-depth: 0` serves SourceLink stamping; test and UI steps are correctly left for §5 and T02 §2.
2. Is it logical? Yes. Every command matches `docs/build.md` and the §1 provision invocations verbatim; the artifact path matches the §2 output layout (proven by the upload); trigger, labels, and SHAs are as decided.
3. Did it break anything else? No. First workflow in the repo; `validate` 0 fatal; `self-test` 391/391; the red probe commit remains in history by design with its revert adjacent.

## Lenses

- `adversarial`: approve. Push-to-`main` trigger only (no fork-PR code execution); SHA pins defeat tag moves; provision downloads stay HTTPS-plus-SHA512; default token permissions sufficed.
- `consistency`: approve. Commands, paths, and env vars match the §1/`docs/build.md` contract; corrections dated with dependency evidence; Markdown one line per item.
- `integration`: approve. §2 outputs consumed correctly; §5/T02 §2 extension points recorded in the item; §7's workflow is a separate file and trigger.
- `record`: approve. Three run IDs with conclusions plus the artifact download and log excerpts are quoted; the `ef12eb3` body contains a stray phrase ("checkout Speicher dir 3d3c42e", a drafting typo, meaning recoverable) and is left untouched per the no-history-surgery rule.
- `source-defect`: not owed (no Notepad behavior or protocol detail).
- `design`: not owed (no UI surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; CI evidence stands (runs completed on the exact pushed commits); `plan --check` green after the stamp-flow sync.
