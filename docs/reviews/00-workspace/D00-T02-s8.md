# D00 T02 §8 findings

Candidate range: `dc2dbc7 6d1a8be 67f2f21 0242f52 14ccfb0 04a5a6a 67b88bc 3d7f658` (non-contiguous: interleaved T01 Bin-output commits `373a7d3 2941c85` touch the same paths and are excluded; filings `83c363c 2d8dd76 de17abf` ride alongside, not in the candidate).

Tier: Full panel (diff touches the shared UI test harness plus the product launch path). Rounds 1-2 Sol, rounds 3-5 Opus; round 5 final at the hard cap.

Panel scope note: rounds reviewed a focused diff (harness core, product funnel, ForegroundLog, docs, audit, §8 record, three special test files, 150-232KB) plus a convert-file manifest; the 25 mechanical convert hunks were excluded for size and covered by the green suite plus author sweeps (every remaining physical-input call sits in a fenced test; every fenced test cites its proof), disclosed in every prompt.

## GPT panel Round 1

Round 1 over `dc2dbc7 6d1a8be 67f2f21 0242f52`, Sol medium. Two needs-attention plus two advisories.

- `adversarial` needs-attention (1)
- `consistency` advisory (1)
- `integration` advisory (1)
- `record` needs-attention (1)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 283s; outcome needs-attention; tokens 100827

1. R1-F1 `tests/UI/UiForeground.cs` Restore yanked focus back to a stale pre-test window when the operator navigated mid-test -> fixed in `14ccfb0` (Restore takes the suite HWND and restores only when the suite itself holds the foreground; probed real: background SetForegroundWindow succeeded on this box).
2. R1-F2 `docs/testing.md:29` named only `[InteractiveFact]` plus the forward guard while `[HookFact]` and the reverse guard existed -> fixed in `14ccfb0` (both gates plus both guards named).
3. R1-F3 `src/ScratchPad/MainWindow.xaml.cs` discarded the SetWindowLong/SetWindowPos results establishing WS_EX_NOACTIVATE -> fixed in `14ccfb0` (style read back after placement, missing bit throws on the test-only path; backgrounded launch green).
4. R1-F4 section checkpoint still asserted the visible fenced pass while the record owed it as night debt -> fixed in `14ccfb0` (completion-first checkpoint revision, D00 T02 §10 hardens).

## GPT panel Round 2

Round 2 over the R1 candidate plus `14ccfb0`, Sol medium. One birth-flash family across all four lenses.

- `adversarial` needs-attention (1)
- `consistency` advisory (1)
- `integration` needs-attention (1)
- `record` needs-attention (1)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 115s; outcome needs-attention; tokens 57335

1. R2-F1 show-then-move exposed each window at its primary birth cascade for 250 ms (settle sleep between show and move); 61 census-caught visible flashes per Run A -> fixed in `04a5a6a` (settle after the suite-display move; hidden-minimized spike proved no pre-show ordering avoids the restore).
2. R2-F2 comments claimed a microsecond flash despite the 250 ms sleep -> fixed in `04a5a6a` (measured numbers stated).
3. R2-F3 the 250 ms dwell aligned with the 250 ms census poll while the gate needs two sightings -> fixed in `04a5a6a` (transients sub-poll by construction; seen-twice documents transient-vs-resting).
4. R2-F4 funnel plus census marked complete despite the dwells -> fixed in `04a5a6a` (record corrected to resting semantics); structural hardening filed D00 T02 §11.

## Opus panel Round 3

Round 3 (sign-off) over the R2 candidate plus `04a5a6a`, Opus medium. Two needs-attention, one advisory, one needs-attention pair.

- `adversarial` needs-attention (1)
- `consistency` advisory (1)
- `integration` needs-attention (1)
- `record` needs-attention (2)

Telemetry: round 3; model opus; effort medium; duration 81s; outcome needs-attention; tokens unknown

1. R3-F1 cascade second window never InPlaced, allegedly passing the rect comparison vacuously -> mechanism disproved by probe (UIA reports restore bounds; a placed-bounds guard passed on the unshown window), bypass fixed anyway in `67b88bc` (InPlace on the Primary second, tripwire kept).
2. R3-F2 two `docs/testing.md` stales (single-monitor fallback wording, unqualified foreground claim) -> fixed in `67b88bc`.
3. R3-F3 two mid-test second windows bypassed both funnel legs; the item-7 query was attach-site scoped -> fixed in `67b88bc` (InPlace plus PlaceForBackground); extended query 20 enumerations, 0 offenders.
4. R3-F4 item-7 Done evidence did not cover the bypassed windows -> fixed in `67b88bc` (note extended with routing plus query numbers).
5. R3-F5 CENSUS lines recorded first sightings (iconic at birth), so resting placement did not read off them -> fixed in `67b88bc` (lines upgrade to the latest non-iconic sighting).

## Opus panel Round 4

Round 4 over the R3 candidate plus `67b88bc`, Opus medium. Two needs-attention, one advisory, one needs-attention pair.

- `adversarial` needs-attention (1)
- `consistency` needs-attention (1)
- `integration` advisory (1)
- `record` needs-attention (2)

Telemetry: round 4; model opus; effort medium; duration 86s; outcome needs-attention; tokens unknown

1. R4-F1 Category=Primary tests were plain Facts needing `-e SCRATCHPAD_BACKGROUND=1` to avoid Activate; an unflagged full run would steal the foreground -> fixed in `3d7f658` (new `[PrimaryFact]` runs only backgrounded or in-window/forced; unflagged daytime 2/2 skip naming the flag; both pairing guards added, QuietHoursTests 22/22).
2. R4-F2 `docs/testing.md:3` read as if both sets skip outside the window while Primary never skipped -> fixed in `3d7f658` (both skip rules stated).
3. R4-F3 MenuBarTests FileNewWindow second window bypassed the funnel (picked and closed, never routed) -> fixed in `3d7f658` (PlaceForBackground); per-pick query 13 picks, 0 offenders.
4. R4-F4 item Done evidence predated the fix commits (re-proof owed, not a content defect) -> fixed in `3d7f658` by the full re-proof with updated quotes (Run A 162/3/0 flagged=0 primary=0, flashes 61 to 2; Run B 2/2 flagged=0 primary=1).
5. R4-F5 audit table orphaned the ClickFoundItem row (blank-split) -> fixed in `3d7f658` (blanks removed; orphan sweep 0 splits, 187 intact).

## Opus panel Round 5

Round 5 (FINAL, hard cap) over the R4 candidate plus `3d7f658`, Opus medium. Approvals plus below-bar findings only; no blocking matter, so the review stamps without re-rounding.

- `adversarial` approve
- `consistency` needs-attention (1)
- `integration` approve
- `record` needs-attention (2)

Telemetry: round 5; model opus; effort medium; duration 135s; outcome needs-attention; tokens unknown

1. R5-F1 the test named `CtrlShiftNOpensSecondWindowAtCascade` no longer presses Ctrl+Shift+N (menu Invoke after conversion) with zero Shift+N coverage suite-wide -> below-bar (honest cascade test, narrower binding coverage, misleading name): filed D00 T02 §12 (accelerator sweep plus fenced restoration plus rename); no candidate change at the cap.
2. R5-F2 audit tally misfiled (first table 87/25, file 88/44/4; stated 88/24 and 89/43/4; totals intact) -> below-bar typo: corrected in the STAMP commit (doc header plus item-1 Done), recount quoted.
3. R5-F3 item 13 Done quoted the retired Run A (746 lines) while items 2 and 11 quote the re-proof (739) -> below-bar typo: corrected in the STAMP commit.

## Dispositions

| ID | Disposition | Evidence |
| R1-F1 | fixed | 14ccfb0 |
| R1-F2 | fixed | 14ccfb0 |
| R1-F3 | fixed | 14ccfb0 |
| R1-F4 | fixed | 14ccfb0 |
| R2-F1 | fixed | 04a5a6a |
| R2-F2 | fixed | 04a5a6a |
| R2-F3 | fixed | 04a5a6a |
| R2-F4 | fixed | 04a5a6a |
| R3-F1 | fixed | 67b88bc |
| R3-F2 | fixed | 67b88bc |
| R3-F3 | fixed | 67b88bc |
| R3-F4 | fixed | 67b88bc |
| R3-F5 | fixed | 67b88bc |
| R4-F1 | fixed | 3d7f658 |
| R4-F2 | fixed | 3d7f658 |
| R4-F3 | fixed | 3d7f658 |
| R4-F4 | fixed | 3d7f658 |
| R4-F5 | fixed | 3d7f658 |
| R5-F1 | filed | D00 T02 §12 |
| R5-F2 | fixed | STAMP commit (doc header plus item-1 Done, recount quoted) |
| R5-F3 | fixed | STAMP commit (item-13 quote aligned to the re-proof) |

## Architecture triggers

Arch trigger: none - test-harness conversion plus the test-only product background-launch path (no-activate show, style, minimize); no editor, storage, extensibility, AI-integration, protocol, or consent/undo surface changes.

## Plan review

Round: `gpt-5.6-sol` high over §8 plus 11 corpus sections, 2026-09-19. Neighbors: D00 T02 §2 (Depends), §5/§9/§10/§11/§12 (XREFs); dependents: D00 T02 §9/§10/§11/§12. Output `check-plan PASS` (11 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T02 §8, D00 T01 §3, D00 T01 §8, D00 T01 §28, D00 T02 §6, D00 T02 §7, D00 T02 §9, D00 T02 §10, D00 T02 §11, D00 T04 §1, D06 T01 §3, D06 T01 §4]; dependents [D00 T02 §9, D00 T02 §10, D00 T02 §11, D00 T02 §12]; bytes 59806; run 20260919-D00-T02-S8-gpt

Ledger (namespaced IDs per the §16 shape; 0 filed, 3 corrected in place before stamp, 8 rejected with reasons on their rows):

Ledger:

- [D00-T02-S8-PR1] [minor] T01 §3 stale CI promise -> rejected promise absent from the section text (verified by reading); the CI narrowing it gestures at landed in c2c2362 before §8 and the current contract lives in docs/testing.md
- [D00-T02-S8-PR2] [minor] T01 §8 input capability -> rejected panel reject stands: retired historical record, no fence or placement reliance
- [D00-T02-S8-PR3] [minor] T01 §28 governance rules -> rejected panel reject stands: review governance independent of §8 mechanics
- [D00-T02-S8-PR4] [minor] T02 §6 golden evidence -> rejected panel reject stands: retired CI evidence historically valid; §8 defects do not touch the golden diagnosis
- [D00-T02-S8-PR5] [minor] T02 §7 evidence capture -> rejected panel reject stands: capture compatible with smoke-only CI, no census reliance
- [D00-T02-S8-PR6] [minor] T02 §9 two-half contract -> rejected corrected in place before stamp (open section): rewritten to three tiers with markers
- [D00-T02-S8-PR7] [minor] T02 §10 completion-first -> rejected panel reject stands: already defines tier split plus async collection
- [D00-T02-S8-PR8] [minor] T02 §11 central launch -> rejected panel reject stands: already the owned birth-flash follow-up
- [D00-T02-S8-PR9] [minor] T04 §1 panel governance -> rejected panel reject stands: disposition governance does not consume UI rules
- [D00-T02-S8-PR10] [minor] D06 T01 §3 CI-run requirement -> rejected corrected in place before stamp (open section): rewritten to §8 tier classification with markers
- [D00-T02-S8-PR11] [minor] D06 T01 §4 CI-run requirement -> rejected corrected in place before stamp (open section): rewritten to §8 tier classification with markers
End of ledger

## Live proof

Digests below are sha256 over the quoted outputs (attested per the §20 shape rule). Proofs ran on the R4 working tree whose code is byte-identical to candidate tip `3d7f658` (only TODO prose quotes were added after); quoted trees resolve there. Box: Venom-PC, 4K 150% primary plus 1080p 100% secondary, never locked.

Run A (`dotnet test tests/UI --filter Category!=Interactive&Category!=Primary -e SCRATCHPAD_BACKGROUND=1`): `Passed!  - Failed:     0, Passed:   162, Skipped:     3, Total:   165, Duration: 9 m 29 s - UI.dll (net10.0) / flagged=0, census=739 primary=0, gate exit=0`.

Provenance: candidate 3d7f658a9b9b7eb652ddfec6eb71bc9311b15e12; command dotnet test tests/UI (Run A); exit 0; tool dotnet SDK 10.0.400 (repo-local); digest 53f44e1d445be3c6847f27cadb630503f59cfd89f4bf2bcb321a2837f318819d; path tests/UI/UI.csproj; run 20260919-D00-T02-S8-gpt

Run B (`--filter Category=Primary`, `--expect-primary`): `Passed!  - Failed:     0, Passed:     2, Skipped:     0, Total:     2, Duration: 5 s - UI.dll (net10.0) / flagged=0, census=11 primary=1, gate exit=0`.

Provenance: candidate 3d7f658a9b9b7eb652ddfec6eb71bc9311b15e12; command dotnet test tests/UI (Run B); exit 0; tool dotnet SDK 10.0.400 (repo-local); digest 4f0a7d027e238e4ea6afc80a80eaf5eca136da7579302ab7a09446ee4a9723df; path tests/UI/UI.csproj; run 20260919-D00-T02-S8-gpt

`py scripts/todo-graph.py validate`: `28 todos, 245 sections -- 0 fatal, 0 warning(s)`.

Provenance: candidate 3d7f658a9b9b7eb652ddfec6eb71bc9311b15e12; command py scripts/todo-graph.py validate; exit 0; tool CPython 3.14.0; digest 8511bdf7910b90aa32a3239955a234ca1ba92885494b78470883298db27cbdec; path scripts/todo-graph.py; run 20260919-D00-T02-S8-gpt

`py scripts/todo-graph.py plan --check`: `implementation plan is current -- 89/240 sections complete (37%)`.

Provenance: candidate 3d7f658a9b9b7eb652ddfec6eb71bc9311b15e12; command py scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.0; digest aa7450effbd83f81b7608d86259569f12a43886a2a21d52f57e41c9495df3be2; path scripts/todo-graph.py; run 20260919-D00-T02-S8-gpt

## Raw panel outputs

```text
--- round 1 (run 20260919-D00-T02-S8-sol) ---
**adversarial: needs-attention** (1)
1. `tests/UI/UiForeground.cs:22-25,115-123` restores a previously captured HWND unconditionally; if the operator changes windows during launch or attachment, `SetForegroundWindow` steals focus back to the stale window, violating the uninterrupted-run contract.

**consistency: advisory** (1)
1. `docs/testing.md:29` says every Interactive test uses `[InteractiveFact]` and names only the forward pairing guard, but `tests/UI/TabBarTests.cs:435-436` uses `[HookFact]`, and `tests/UI/QuietHoursTests.cs:92-120` adds a reverse guard; the documented gate shape is stale.

**integration: advisory** (1)
1. `src/ScratchPad/MainWindow.xaml.cs:83-85,115-122` discards the results of the `SetWindowLong` and `SetWindowPos` calls that establish `WS_EX_NOACTIVATE`, so a platform failure silently removes the product-side focus-safety guarantee instead of failing the gate loudly.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:234,247` records the visible Interactive pass as uncollected night debt while the section checkpoint still asserts that the fenced set passes visibly; the completed record therefore claims an unmet checkpoint.
--- round 2 (run 20260919-D00-T02-S8-sol-r2) ---
**adversarial: needs-attention** (1)
1. `tests/UI/UiForeground.cs:61-80,103-113` calls `Show(window)`, which sleeps 250 ms, before moving the window to the secondary display, visibly exposing each background window at its primary birth position and violating the uninterrupted-primary-screen guarantee.

**consistency: advisory** (1)
1. `tests/UI/UiForeground.cs:67-74,103-113` describes the show and move as back-to-back with a microsecond flash despite the explicit 250 ms delay, while `docs/testing.md:24-26` says windows become visible on the secondary display; both descriptions contradict the implemented ordering.

**integration: needs-attention** (1)
1. `tests/UI/UiForeground.cs:67-80,103-113` leaves the window on its primary birth position for one full 250 ms census interval, while `tools/ForegroundLog/Program.cs:119-136` reports a primary violation only after two sightings, so scheduling alignment can let the real primary-screen interruption pass the gate.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:239-244` marks the display funnel and zero-primary census complete even though `tests/UI/UiForeground.cs:67-80,103-113` deliberately exposes windows at their birth position for 250 ms and `tools/ForegroundLog/Program.cs:119-136` excludes a single observed primary sighting from the asserted count.
--- round 3 (run 20260919-D00-T02-S8-opus) ---
**adversarial: needs-attention** (1)
1. `tests/UI/MultiWindowTests.cs:43` — in the `Category=Primary` placement run the second window is never routed through `InPlace`/`Show` (it is created by `InvokeMenuItem` at line 37 and picked at lines 40-42), so under the documented placement command `-e SCRATCHPAD_BACKGROUND=1` it stays minimized per `src/ScratchPad/App.xaml.cs` `ShowWindow`; a minimized window's `BoundingRectangle` is the minimized/empty rect, so `Assert.NotEqual(first.BoundingRectangle.Location, second.BoundingRectangle.Location)` passes whatever the app does and the cascade premise the test exists to prove is no longer asserted.

**consistency: advisory** (2)
1. `docs/testing.md:25` — "moves each window to the secondary monitor (off-screen when the box has one)" inverts the code: `tests/UI/UiForeground.cs:102-108` returns the secondary working area when a secondary exists and falls back to `(10000, 10000)` only when none does (stated correctly at `tests/UI/UiForeground.cs:13`).
2. `docs/testing.md:3` — "The Windows run includes the fenced interactive UI tests (below), so it owns the foreground while it runs" contradicts `docs/testing.md:29` ("a daytime full-solution run reads green-plus-skipped") and `tests/UI/InteractiveFactAttribute.cs:11-14`, and omits the `Category=Primary` set the same full run now executes.

**integration: needs-attention** (1)
1. `tests/UI/MultiWindowTests.cs:40` and `tests/UI/MultiWindowTests.cs:167` — second windows obtained from `WaitForWindowCount` bypass both funnel legs (`Background`/`InPlace`/`PlaceForBackground`), unlike the same pattern at `tests/UI/MultiWindowTests.cs:73`, `tests/UI/LaunchTests.cs:157`, `tests/UI/LaunchTests.cs:428`, and `tests/UI/SessionRestoreTests.cs:422-423`; the item-7 "launch-without-background query" is attach-site scoped and therefore cannot see these mid-test-created windows, so the funnel guard reports empty while windows escape it.

**record: needs-attention** (2)
1. `todo/00-workspace/TODO-02-test-backbone.md:241` — item 7 records "the method-level attach-vs-`Background` query ... found 2 misses ..., query now 0 offenders" as proof that "every app window the suite creates (first, second, redirect-created) routes through `UiForeground.Background`", but the second windows at `tests/UI/MultiWindowTests.cs:40` and `:167` route through neither leg, so the quoted evidence does not cover the done-when it is filed against.
2. `todo/00-workspace/TODO-02-test-backbone.md:245` and `docs/testing.md:27` — item 11's done-when ("the foreground proof records every app HWND with monitor, rect, and iconic state") is credited to the 746 quoted CENSUS lines, but `tools/ForegroundLog/Program.cs:150` uses `census.TryAdd`, recording each HWND once at first sighting; under `SCRATCHPAD_BACKGROUND=1` every window is born minimized (`src/ScratchPad/App.xaml.cs` `ShowWindow`), so those lines read `iconic iconic iconic=True` and carry no monitor or rect for the resting placement the item claims they record (the primary assertion comes from the separate `sightings`/`primarySeen` counters at `tools/ForegroundLog/Program.cs:142-145`, not from the census lines).
--- round 4 (run 20260919-D00-T02-S8-opus-r2) ---
**adversarial: needs-attention** (1)
1. `tests/UI/MainWindowTests.cs:68-70` and `tests/UI/MultiWindowTests.cs:19-21`: the two `Category=Primary` tests are plain `[Fact]` with no quiet-hours gate, and nothing in test code sets `SCRATCHPAD_BACKGROUND`; in the documented unfiltered Windows run (`dotnet test src/ScratchPad.slnx`, `docs/testing.md:3`, which passes no `-e`) `App.ShowWindow` (`src/ScratchPad/App.xaml.cs:97-109`) falls through to `window.Activate()`, so `GeometryRestoresAcrossLaunches` and `CtrlShiftNOpensSecondWindowAtCascade` (plus its second window) steal the foreground at 14:00 on the operator's box, the exact interruption item 3 and item 6 exist to prevent; both reflection guards (`tests/UI/QuietHoursTests.cs:64-121`) key on `Category=Interactive` only and cannot see this set.

**consistency: needs-attention** (1)
1. `docs/testing.md:3`: the sentence names the `Category=Primary` placement set alongside the fenced set and then states only that "outside the window it skips so the run reads green-plus-skipped", which reads as covering both sets; the Primary set never skips and is focus-free only when the caller adds `-e SCRATCHPAD_BACKGROUND=1`, which the `dotnet test src/ScratchPad.slnx` command on that same line omits.

**integration: advisory** (1)
1. `tests/UI/MenuBarTests.cs:446-455`: `FileNewWindowOpensSecondWindow` (default set) creates a second app window through the File menu and enumerates it at 453-454 without routing it through `UiForeground.Background`/`PlaceForBackground`, so it is an item-7 funnel bypass of the same family as the two fixed in 67b88bc (its `other.Close()` at 455 is the only use, so the window stays minimized, but the item-7 rule is "every app window the suite creates").

**record: needs-attention** (2)
1. `todo/00-workspace/TODO-02-test-backbone.md:236,245,247`: the Done evidence still quotes only Run A/Run B of 2026-09-19 (`build/gate-final2.log` 159/3/0, census 746, primary=0; `build/gate-primary3.log`) as proof, but that run predates 14ccfb0, 04a5a6a, and 67b88bc, which changed the restore guard, the show-then-move ordering (R2: removed 61 census-caught primary flashes), the census line-upgrade rule, and the InPlace/PlaceForBackground routing of two mid-test windows; the R2 disposition itself recorded "full-run re-proof owed before stamp" and no re-run is quoted anywhere in the section.
2. `docs/ui-input-audit.md:147-148`: the `MenuBarTests.cs:1272` `ClickFoundItem` row is separated from the clicks table by a blank line, so it is an orphaned row that renders as literal text rather than a table row, and it is the row the table's own "22 sites: 1 convert, 17 fence, 4 keep" tally depends on.
--- round 5 (run 20260919-D00-T02-S8-opus-r3) ---
**adversarial: approve**

**consistency: needs-attention** (1)
1. `tests/UI/MultiWindowTests.cs:21` — the test named `CtrlShiftNOpensSecondWindowAtCascade` no longer presses Ctrl+Shift+N: line 37 now calls `UiInput.InvokeMenuItem(first, "MenuFile", "MenuFileNewWindow")`. The audit dispositioned `MultiWindowTests.cs:195-209` (the `Press` helper) as "convert", but this call site was the suite's only exercise of the Ctrl+Shift+N accelerator — grep of `UiInput.Press` across `tests/UI` shows KEY_N only with `withControl: true` (`tests/UI/MenuBarTests.cs:297`), never with shift — so the accelerator now has zero coverage while the test name still asserts it. The test is `Category=Primary` and runs with the foreground available in-window, so a fenced `UiInput.Press` was available and was not used.

**integration: approve**

**record: needs-attention** (2)
1. `docs/ui-input-audit.md:3` and `todo/00-workspace/TODO-02-test-backbone.md:235` both state the first table's tally as "88 convert, 24 fence, 0 keep"; the table's 112 rows (lines 6-118) actually hold 87 convert, 25 fence, 0 keep. Whole-file counts confirm the mismatch: 88 convert / 44 fence / 4 keep across all 136 dispositioned rows, against the stated 89 / 43 / 4 (first table 88+24, clicks 1+17+4, raw 2). The 112 total and the 187 inventory are intact; only the convert/fence split is misfiled, in both the doc header and the item-1 Done evidence.
2. `todo/00-workspace/TODO-02-test-backbone.md:247` — item 13's Done still quotes the superseded Run A ("primary=0 across 746 census lines"). The R4 re-proof replaced that run with `build/gate-final3.log` at 739 census lines, and items 2 (line 236) and 11 (line 245) were both updated to 739; item 13 alone still cites the retired `gate-final2.log` figure, so the file's census evidence disagrees with itself.
```
