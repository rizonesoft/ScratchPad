# Review: D00 T01 §51 -- Acceptance Record Follow-Ups

Tier: Full (the diff touches shared contract surface: `scripts/todo-graph.py`, `scripts/todo-validate.py`, the review skill, and `todo/README.md`). Candidates: `2e294ff` (ship), `ae427e8` (fix-loop 1).

## Self-review

Q1 (exact outcome, no widening): item 1 maps to the ancestry leg in `acceptance_hold` (`target_present_at` over `target_digest`, `predated-evidence` cause plus fix text); item 2 maps to `evidence_lineage` plus the validator `risk-acceptance-foreign-evidence` leg, the 40-hex evidence tightening, and the `foreign-evidence` cause; item 3 maps to `target_digest` plus `evidence_state_from_bytes` (LF, trailing-whitespace-stripped, acceptance-lines-excluded); item 4 maps to `REOPENED_BODY_RE` generation, `STAMP_GENERATION_RE`, the per-section generation state, and the `stale-generation` gate; item 5 maps to the `outcome`/`action` grammar, the `accepted_outcome` key (schema plan-health/7), and the §29-style doc sweep; item 6 maps to required `id A<n>` plus ID-keyed supersession, chains, and append-only history; item 7 maps to the deferred-trigger link-bounded strip plus the filed `OVERLONG_REF_RE` strip; item 8 maps to rule 28 `marker-history` over `marker_bodies_by_section`. Item 9 matches the commit subject verbatim. No widening: sibling doc bumps (AGENTS /1 count 1236, README severity rows, skill cause lists, §17/§19/§21 superseded notes) are each forced by the shipped rules.

Q2 (logic): one 14-tuple threads query and validator with field-index migration at every site (no split expressible); `acceptance_hold` leg order matches its documented order (generation, target day, target-before-record, ancestry, liveness, lineage, freshness); unprovable ancestry and lineage defer rather than report, so only proven verdicts fire; ID-keyed chains enforce same-target succession explicitly with cross-target claims read once under their own leg; the live record (D00-T02-s10 A1, generation 0 against section generation 0) still covers and the register stays current. No finding; fixed nothing before the lenses.

Q3 (blast radius): `evidence_state` keeps its name with the new kind/tgt parameters and one shared fetch between the ancestry and freshness legs; supersession re-keying moves validators, query, and fixtures together (zero live records besides the migrated A1); the generation-strip gap needs deliberate hand editing and is recorded in code; the skill-sibling sweep for the old whole-file/whole-date wording returns no site outside the edited review skill. Live gates green (self-test 1236/1236, validate 0 fatal 0 warnings, risk-register current, plan current).

## GPT panel (round 1)

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` needs-attention
- `record` needs-attention

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Fix record: R1-F1 (generation base resets on a fresh parse after the line lifts) fixed by seeding `_gen_state` from persisted stamps (max, never last-wins) with the §111 pin; R1-F2 (prose passes as marker presence) fixed by requiring `STAMP_RE` Plan-review-kind lines in both `target_digest` legs with prose-decoy pins; R1-F3 (tag-object evidence passes the peel) rejected with reason (below); R1-F4 (skill sweep incomplete) fixed by the 7-site skill update (schema /7, record grammar, coverage legs, cause list, chain vocabulary, six-field accepted-by, stamp-replace carve-out, generation Reopened); R1-F5 second half (reviews leg reads TODO bytes) fixed by binding the findings text with the §19 overdue pin, first half (first-candidate rule) pinned as the declared contract with no live multi-candidate file; R1-F6 (checkpoint overclaims against the recorded gap) fixed by scoping the checkpoint plus the item-4 gap note. R1-F3 rejection: no contract text requires evidence to be a commit object (item 2 says 40-hex full ids, the README says peeled-to-commit normalization); tag object ids are immutable and deterministic through every leg (peel, compare, byte fetch); the §33 must-be-the-commit rule is provenance-specific because tree/path/run checks need a commit, which evidence never undergoes. Fixed in the fix-loop 1 commit; gates re-ran green before round 2 (self-test 1243/1243, validate 0 fatal, risk-register current, plan current).

Raw round-1 output (fenced, not part of the verdict scan; digest `1f35fd86d02f76668a74cfbccfe0c24a5b95eb208629dab3570c979c33e0c7a1`):

```
**adversarial: needs-attention** (3)
1. `scripts/todo-graph.py:498-501,543-549,627-632` never seeds `_gen_state` from a persisted `Verified:` generation and checks re-stamps only while a live `Reopened:` line remains. Removing that line permits generation 0 and revives old acceptances; subsequent reopens also incorrectly restart from generation 0.
2. `scripts/todo-graph.py:1417-1434` treats any text line containing `Plan review` plus a run or outage shape as target evidence without requiring `STAMP_RE`. Prose can therefore fabricate same-day target presence and freshness.
3. `scripts/todo-graph.py:1619-1623,2515-2528` and `scripts/todo-validate.py:1757-1760` peel evidence references with `^{commit}` but never require the recorded 40-hex value itself to be that commit ID. A 40-hex annotated-tag object ID passes despite the contract requiring a full commit ID.

**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:186,190,196,200` remains on `plan-health/6`, documents the obsolete acceptance shape and date-based supersession, instructs stamp replacement despite append-only markers, and emits the old generation-less `Reopened:` syntax. Following the skill now creates validator-invalid records.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:1603-1611,4020-4024,4420,4912` and `scripts/todo-validate.py:1747-1760` do not bind lineage to the target’s review record: they select the first provenance candidate in a multi-run findings file, while the reviews loop derives run/outage candidates from TODO bytes and therefore gets `None`. Later-run acceptances can be compared to the wrong candidate, and foreign run/outage evidence can incorrectly enter the reviews dimension.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1305,1312` records generation handling complete and claims reopens never revive, while `scripts/todo-graph.py:86-92` explicitly documents the unclosed post-lift revival gap. The completion record and checkpoint are therefore unsupported.
```

## GPT panel (round 2)

- `adversarial` needs-attention
- `consistency` approve
- `integration` needs-attention
- `record` needs-attention

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Fix record: R1-F1, R1-F2, R1-F4, and the R1-F5 reviews-half fixes confirmed (not re-reported); consistency approve. R1-F3 re-reported (tag-object evidence): held with the fix-loop-1 reason, strengthened — the item-2 contract reads 40-hex full ids with peel-to-commit normalization (never commit-object-only), tag ids are immutable and deterministic through peel, compare, and byte fetch, and forbidding them would cost a validator leg plus a new query cause plus fixtures for a writer-attested non-threat; the Opus sign-off judges. R1-F5 first half re-reported (first-candidate rule): fixed by run-matching — `provenance_candidate` takes the target run and binds the matching Provenance entry's candidate (normalized, -r1 reads through), falling back to first on no match; all four lineage call sites (marker loop, findings loop, reviews leg, validator leg with per-candidate peel cache) pass the target run for run-kind waivers; unit pins for match plus fallback. Behavior-preserving everywhere except true multi-run multi-candidate files (none live), where it is strictly more principled. R1-F6 re-reported (checkpoint vs gap): held — the checkpoint now explicitly scopes to modeled paths (the re-report quotes the ship-version contract chunk, not the fix diff) and the §50 same-day residual precedent establishes recorded-gap-with-cost as a legitimate completion state; exploiting the gap needs deliberate operator self-sabotage, below the blocking bar. Fixed in the fix-loop 2 commit; gates re-ran green before round 3 (self-test 1245/1245, validate 0 fatal, risk-register current, plan current).

Raw round-2 output (fenced, not part of the verdict scan; digest `9a36f5b9eabb9bc77f00cf44938f0a442dbc12fb0e102dafae80180089760e95`):

```
**adversarial: needs-attention** (1)
1. Still live from R1-F3: `scripts/todo-graph.py:1633-1637,2529-2542` and `scripts/todo-validate.py:1757-1760` accept a 40-hex annotated-tag object by peeling it with `^{commit}` instead of requiring the recorded evidence value itself to be the full commit ID required by the contract.

**consistency: approve**

**integration: needs-attention** (1)
1. Still live from R1-F5: `scripts/todo-graph.py:4930,16851-16857` and `scripts/todo-validate.py:1747-1760` bind every acceptance in a multi-run findings file to its first provenance candidate instead of the provenance entry matching the target review run; the new test explicitly preserves that wrong first-candidate behavior.

**record: needs-attention** (1)
1. Still live from R1-F6: `todo/00-workspace/TODO-01-repo-and-toolchain.md:1305,1312` keeps generation handling checked complete while explicitly admitting that stripping the persisted generation revives old acceptances, contrary to the section’s unconditional “reopens never revive” completion contract.
```

## GPT panel (round 3)

Opus outage: the Opus sign-off rung timed out at the 600s wall clock (run 20260920-D00-T01-S51-opus, partial artifact `e975d45b60baa38213529c4270b2c07b36da38860fc5163be9cfe7f32b686edf`); the sign-off ran once on Sol over the same prompt per the outage matrix.

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` needs-attention
- `record` approve

Telemetry: round 3; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Fix record: sign-off round, no re-round (all three findings below the blocking bar; none touches safety, data integrity, or stamp validity on the live tree). R1-F6 hold confirmed by the record approve. R1-F3 (tag-object evidence, third report): held and filed — the contract dispute is genuine but undecidable by fiat, so D00 T04 §6 item 2 carries it with the full rejection record for a fresh implementer. R3-F2 (fence-strip asymmetry): rejected as factually wrong — `scripts/todo-validate.py:1645` reassigns `ftext` to fence-stripped text before the shape, chain, and lineage legs, so all three legs read stripped bytes; the strip predates §51 and rode outside the candidate diff, which is why the panel missed it. No filing: there is nothing to do. R3-F3 (accepted/deferred critical tuple order): real but latent — verified the append at `scripts/todo-graph.py:4548-4553` puts `aout` before the empty `failure_code` while every consumer unpacks `fc, aout`; no live or fixture row exhibits it (all covering acceptances there stand alone with empty outcomes), and the §11 superseding record on D00-T02-S10-PR1 is the first live trigger. Filed as D00 T04 §6 item 1 with the trigger named. Panel closed.

Raw round-3 output (fenced, not part of the verdict scan; digest `42668171eba8ff7f4e635710dbef60d143555c3802dd30c164e356784b4bdf59`):

```
**adversarial: needs-attention** (1)
1. Still live from R1-F3: `scripts/todo-graph.py:1633-1652` and `scripts/todo-validate.py:1747-1768` peel evidence through `git_full_sha` without proving that the recorded 40-hex value is itself a commit ID, so an annotated-tag object ID still satisfies the full-commit contract.

**consistency: needs-attention** (1)
1. `scripts/todo-validate.py:1728-1768` scans raw `ftext` for acceptance-chain and evidence-lineage checks instead of the fence-stripped text used by the shape check. Fenced `Risk accepted:` examples can therefore create duplicate IDs, chain failures, or foreign-evidence findings despite `todo/README.md:420-423` promising that fenced examples are excluded.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:4538-4553,5107-5123` appends `aout` before the empty `failure_code` for accepted or deferred critical rows, but the JSON consumer unpacks those positions as `failure_code, accepted_outcome`. A superseding acceptance on such a critical reports its outcome as a clearance failure and leaves `accepted_outcome` empty.

**record: approve**
```

## Disposition

| ID | Disposition | Evidence |
| R1-F1 | fixed | fix-loop 1, confirmed R2 |
| R1-F2 | fixed | fix-loop 1, confirmed R2 |
| R1-F3 | filed | D00 T04 §6 item 2 (contract dispute, full rejection record attached) |
| R1-F4 | fixed | fix-loop 1, confirmed R2 |
| R1-F5 | fixed | fix-loop 1 (reviews half) plus fix-loop 2 (run-matching) |
| R1-F6 | rejected | scoped checkpoint plus §50 precedent; confirmed by the R3 record approve |
| R3-F2 | rejected | factually wrong; `scripts/todo-validate.py:1645` strips before all rule-24 legs |
| R3-F3 | filed | D00 T04 §6 item 1 (latent tuple swap; §11 superseder is the first live trigger) |

Arch trigger: none - validator/query/fixtures/skill prose only; no editor state, storage, extensibility, AI, protocol, or consent surface in the diff.
