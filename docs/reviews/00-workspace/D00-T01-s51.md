# Review: D00 T01 §51 -- Acceptance Record Follow-Ups

Tier: Full (the diff touches shared contract surface: `scripts/todo-graph.py`, `scripts/todo-validate.py`, the review skill, and `todo/README.md`). Candidates: `2e294ff` (ship).

## Self-review

Q1 (exact outcome, no widening): item 1 maps to the ancestry leg in `acceptance_hold` (`target_present_at` over `target_digest`, `predated-evidence` cause plus fix text); item 2 maps to `evidence_lineage` plus the validator `risk-acceptance-foreign-evidence` leg, the 40-hex evidence tightening, and the `foreign-evidence` cause; item 3 maps to `target_digest` plus `evidence_state_from_bytes` (LF, trailing-whitespace-stripped, acceptance-lines-excluded); item 4 maps to `REOPENED_BODY_RE` generation, `STAMP_GENERATION_RE`, the per-section generation state, and the `stale-generation` gate; item 5 maps to the `outcome`/`action` grammar, the `accepted_outcome` key (schema plan-health/7), and the §29-style doc sweep; item 6 maps to required `id A<n>` plus ID-keyed supersession, chains, and append-only history; item 7 maps to the deferred-trigger link-bounded strip plus the filed `OVERLONG_REF_RE` strip; item 8 maps to rule 28 `marker-history` over `marker_bodies_by_section`. Item 9 matches the commit subject verbatim. No widening: sibling doc bumps (AGENTS /1 count 1236, README severity rows, skill cause lists, §17/§19/§21 superseded notes) are each forced by the shipped rules.

Q2 (logic): one 14-tuple threads query and validator with field-index migration at every site (no split expressible); `acceptance_hold` leg order matches its documented order (generation, target day, target-before-record, ancestry, liveness, lineage, freshness); unprovable ancestry and lineage defer rather than report, so only proven verdicts fire; ID-keyed chains enforce same-target succession explicitly with cross-target claims read once under their own leg; the live record (D00-T02-s10 A1, generation 0 against section generation 0) still covers and the register stays current. No finding; fixed nothing before the lenses.

Q3 (blast radius): `evidence_state` keeps its name with the new kind/tgt parameters and one shared fetch between the ancestry and freshness legs; supersession re-keying moves validators, query, and fixtures together (zero live records besides the migrated A1); the generation-strip gap needs deliberate hand editing and is recorded in code; the skill-sibling sweep for the old whole-file/whole-date wording returns no site outside the edited review skill. Live gates green (self-test 1236/1236, validate 0 fatal 0 warnings, risk-register current, plan current).
