# Review: D00 T01 §30 -- Clearance Fixture Residuals

Candidate: `8b2f9a1` + `653aee3` + `f9e14fe` + `8ac8d7a` (SHIP plus the R1 tick fix plus two filing commits; output `check-panel PASS` on all three headless rounds). Panel tier: Full (the diff touches `scripts/todo-graph.py`, shared contract surface), Sol rounds 1-2, Opus round-3 sign-off.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. No separate self-review fix commit: the validation pass had already recorded its corrections on items 1-3 before the build, and the diff matched the corrected contract.

Does it make sense: three items take the proof/row ID negative, the fix-less negative, and the real-git helper fixtures, with the canned profile, marker extension, ledger rows, assertions, and the temp-repo block each owes. Exactly the contract, including the corrected probe identities (PR71/PR72, §§76-77) and the REPO-patch shape.

Is it logical: PR71 stays only for the ID mismatch (every other leg passes on `f000001`); PR72 stays only for the missing fix (the row plus back-link legs pass); the real-git block restores REPO and removes the temp dir in `finally` pairs. The merge pins document live truth with the §31 finding owning the exclusion.

Did it break anything else: suite moves 856 to 879 by exactly the 23 new probes (2 clearance negatives plus 21 real-git checks); live validate stays 0 fatal; plan-health --check stays green. Blast radius is the self-test block, which runs nowhere else.

## GPT panel (round 1)

- `adversarial` approve
- `consistency` needs-attention: (1) item 3 unticked despite Done plus shipped checks.
- `integration` approve
- `record` needs-attention: (1) canonical section state disagrees with the implementation evidence.

Fix record: item 3 ticked. Fixed in `653aee3`; gates re-ran green before round 2 (validate 0 fatal, plan current).

Raw round-1 output (fenced, not part of the verdict scan; digest `82b0343f38ec88b3f28674bb93a3815aa2130e403ca7f2724a0b270c9efb4886`):

```
**adversarial: approve**
**consistency: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:845` leaves the completed real-git clearance-fixtures checklist item unchecked despite stating “Done” and shipping all 21 required checks.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:845` records completed work as incomplete, so the canonical section state disagrees with the candidate implementation and completion evidence.
```

## GPT panel (round 2)

Round 2 ran against the tick-fix candidate with the round-1 verdicts appended as fixed context.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

No fix commit: the round-1 finding stays fixed and nothing live re-reported.

Raw round-2 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

Round 3 signed off over the range diff (`f9e14fe..653aee3`) with rounds 1-2 appended.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` needs-attention: (1) AGENTS.md self-test count stale (856 vs 879).

Filing record: sign-off round, below the blocking bar, so fixed at stamp instead of re-rounding. The AGENTS.md count syncs 856 to 879 in the stamp commit; the convention stays pre-tracked at D00 T01 §42. No fix commit.

Raw round-3 output (fenced, not part of the verdict scan; digest `b2179c55beb02bfd89f279b25c2cde55f99313bf091dcd32a85db9c26e5680c0`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: needs-attention** (1)
1. `AGENTS.md:55` still reads `self-test      # 856 cases, must stay green`, but this candidate adds 23 cases (2 clearance negatives at `scripts/todo-graph.py:10314-10321`, 21 real-git checks at `scripts/todo-graph.py:11915-12018`) and the shipped tree reports `todo-graph self-test: 879 cases, 0 failed` (base `8b2f9a1^` reports 856), so the canonical count documented for the gate is stale by exactly this range.
```

## Review notes

Fixtures-only section with shared-surface touch: three items across the self-test block (canned profiles, fixture sections, temp-repo probes). The panel's one correction (the item-3 tick) landed before close; the sign-off record fixed at stamp (§42 pre-tracks the convention going forward). The merge-visibility probe corrected itself by replication mid-implementation (botched first probe, clean characterization, narrowed §31 filing). No panel leftovers. Architecture gate skipped silently (no editor state, storage, extensibility, protocol, or consent surface in the diff).

## Plan review

Round: `gpt-5.6-sol` high over §30 plus §22, 2026-09-19. Neighbors: §22 (Depends plus XREF neighbor: §30 filed from its review). Dependents: one ref by the mechanical rule (no reverse Depends rows; no first-clause XREF consumers; §22 rides as the filer per the §26/§27 precedent). The run minted through the `run-id` genesis path (no prior claims, bare base). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (15 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §30, D00 T01 §22]; dependents [D00 T01 §22]; bytes 10206; run 20260919-D00-T01-S30-gpt

Ledger (namespaced IDs per the §16 shape; 8 of 15 filed at §54, 4 duplicates of earlier clearance triage, 3 rejected with reasons on their rows):

Ledger:

- [D00-T01-S30-PR1] [minor] Touch recency untied to finding -> duplicate D00-T01-S22-PR3
- [D00-T01-S30-PR2] [minor] Short SHAs uncanonicalized -> filed D00 T01 §54
- [D00-T01-S30-PR3] [minor] Timestamps forgeable -> duplicate D00-T01-S22-PR8
- [D00-T01-S30-PR4] [minor] Proof binds bytes not execution -> duplicate D00-T01-S22-PR9
- [D00-T01-S30-PR5] [minor] Token ambiguity unruled -> filed D00 T01 §54
- [D00-T01-S30-PR6] [minor] Same-ID proof ambiguity -> filed D00 T01 §54
- [D00-T01-S30-PR7] [minor] Fix-less lock leans on a crash -> filed D00 T01 §54
- [D00-T01-S30-PR8] [minor] No query-level end-to-end proof -> filed D00 T01 §54
- [D00-T01-S30-PR9] [minor] Git absence skips silently -> filed D00 T01 §54
- [D00-T01-S30-PR10] [minor] Undeclared §31 dependency -> rejected no build-time dependency exists (§30 fixtures pass standalone; §31 flips one pin later, which is follow-up flow, not a prerequisite)
- [D00-T01-S30-PR11] [minor] Checkpoint omits item 3 -> rejected fix window closed at sign-off and shipped checklists are immutable (S28-PR10 precedent); the item-3 Done carries the evidence
- [D00-T01-S30-PR12] [minor] Cap prose contradicts correction -> rejected corrections govern over item text (S28-PR12 shipped-text precedent); the correction scopes the cap explicitly, so the section stays executable
- [D00-T01-S30-PR13] [minor] Fixture IDs collide by convention -> filed D00 T01 §54
- [D00-T01-S30-PR14] [minor] Per-leg diagnostics unspecified -> duplicate D00-T01-S22-PR11
- [D00-T01-S30-PR15] [minor] Git fixtures Linux-only -> filed D00 T01 §54
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `625ed10acfaa45f717b4f091b391e7bb34e876ed7c8dde5c36fa9212569707be`):

```
- The fix-range chronology can clear stale work; D00 T01 §22 must require the commit that touches the target, not merely the range tip, to postdate the finding review, because an old touching commit followed by a new no-op tip currently satisfies both independent legs.
- Short commit IDs contradict immutable candidate identity; D00 T01 §22 should require and persist full object IDs or canonicalize abbreviations immediately, because a once-valid short SHA can later become ambiguous and make clearance unreproducible.
- Committer timestamps are deterministic but not trustworthy chronology; D00 T01 §22 should bind recency to recorded review ancestry or another controlled clock, because Git timestamps can be forged or reordered without changing the implemented evidence.
- Proof binding establishes only that a path exists and a test name appears in its bytes; D00 T01 §22 should require evidence that the named test was discovered and passed against the exact fix tip, because comments, dead code, or an unrelated textual match can satisfy the present rule.
- Duplicate and conflicting `proof` or `fix` tokens have no declared resolution policy; D00 T01 §22 should reject ambiguity or specify tested precedence, because reordered metadata could silently change which candidate or evidence clears a finding.
- The proof-mismatch fixture protects ID equality but not duplicate-ID ambiguity; D00 T01 §30 should add cases with multiple proof lines for the same finding that disagree on path or test, because accepting one convenient pointer can conceal stale or fraudulent evidence.
- The fix-less mutation crashes with `AttributeError` instead of demonstrating the promised fail-closed listing behavior; D00 T01 §30 should require a normal diagnostic result and a mutation that incorrectly clears the row, because any unrelated exception can turn the suite red without protecting the control.
- The real-Git item promises end-to-end clearance but its completion text describes helper-level checks with a patched repository global; D00 T01 §30 should exercise the public clearance query over a complete temporary repository, because composition, parsing, and reporting can regress while every helper remains green.
- Git absence is allowed to skip the permanent regression fixture; D00 T01 §30 should designate at least one required CI lane where missing Git is fatal, because the critical clearance contract otherwise has no guaranteed end-to-end execution.
- Merge-only fixes remain split across an undeclared dependency; D00 T01 §30 relies on a §31 fix for conflict-resolution merges while the supplied dependency listing names only §22, so §31 must be an explicit prerequisite or the fixture must not be considered complete.
- The test checkpoint covers only “both guards” and omits the third real-Git deliverable; D00 T01 §30 should require all three acceptance groups explicitly, because a section-level checkpoint should detect loss of any owned behavior.
- The original fixture guidance says the 55-section cap bars new marker sections while its correction says fixture sections are uncapped; D00 T01 §30 and the fixture-format documentation should state one canonical rule, because layered contradictory prose is not executable with zero conversation context.
- Fixed fixture addresses such as PR71, PR72, §76, and §77 are asserted available but no collision check is owned; D00 T01 §30 should generate or validate unique fixture identities, because later fixture growth can make tests exercise the wrong row or section.
- Clearance failures have no specified per-leg diagnostic contract; D00 T01 §22 should return stable reasons for proof, chronology, ancestry, touch, and token failures, because actionable explanations are a small implementation cost and a large premium-quality win for operators.
- Cross-platform Git behavior is unowned; D00 T01 §30 should run the real-repository fixtures on Windows as well as a neutral CI host, covering path case, separators, CRLF, and file locking, because this Windows-first product cannot rely solely on Linux Git semantics.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `8ac8d7a`: `todo-graph self-test: 879 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `8ac8d7a`: `24 todos, 228 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `8ac8d7a`: `implementation plan is current -- 70/223 sections complete (31%)`, exit 0.

Provenance: candidate 8ac8d7a; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 5991ba22c53a6fa341b9b209557ade2fd4524c7076cba04a3c8c72a334cf134a; path docs/reviews/00-workspace/D00-T01-s30.md; run 20260919-D00-T01-S30-gpt
