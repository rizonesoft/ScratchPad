# Review: D00 T01 §44 -- Range-Fallback Lineage Guards

Candidate: `2de3c7f` (SHIP). Full panel (shared contract surface: validator plus graph): Sol rounds 1-2, Opus round-3 sign-off.

Arch trigger: none - validator guard plus fixtures; no editor, storage, extensibility, AI, protocol, or consent surface changes.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. No fixes; asked clean.

Does it make sense: each shipped item maps to its code (fallback skip on both checks, `span_marker_bodies` extraction, fixtures §§94-97, count sync to 1102). Exactly the contract; the Why's option-b design choice is recorded in the item Done note.

Is it logical: `_fallback` is true exactly for nonempty chains over marker-less spans (range members); OSError and empty-body paths yield empty chains and skip via the existing gate; members carrying their own markers keep full checking; fixtures mirror the §§9/26/64 proven shapes and the provenance auto-migration derives their runs by construction; §58 still fires exactly once.

Did it break anything else: self-test 1102/1102 with all pre-existing cases green; live validate silent; `section_markers` return values unchanged for all 6 call sites (query run included); plan current with no box changes.

## GPT panel (round 1)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 44s; outcome approve; tokens unknown

No findings; nothing to disposition.

Raw round-1 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`; run `20260919-D00-T01-S44-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 110s; outcome approve; tokens unknown

No findings; nothing to disposition.

Raw round-2 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`; run `20260919-D00-T01-S44-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` advisory: (R3-F1) fallback probe bypasses the marker-helper convention.
- `integration` approve
- `record` approve

Telemetry: round 3; model opus; effort medium; duration 123s; outcome advisory; tokens unknown

Round-3 sign-off disposition: R3-F1 is an advisory (three-line convention shape, no behavior risk), so it files at D00 T04 §1 instead of re-rounding. No live findings remain; this round is FINAL.

Final disposition table:

| ID | Disposition | Evidence |
| R3-F1 | filed | D00 T04 §1 |

Architecture gate: not triggered (`Arch trigger: none`, see header).

Raw round-3 output (fenced, not part of the verdict scan; digest `cc894b5afca183860708d3009829ba88dc9b81ff4e334b4f0816c5487e0380d8`; run `20260919-D00-T01-S44-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**

**consistency: advisory (1)**
1. scripts/todo-validate.py:834 calls `graph.span_marker_bodies(todo_lines, t, num)` directly, bypassing the file's marker-helper convention: every other marker read in this module goes through the local wrapper `section_markers(todo, num)` defined at scripts/todo-validate.py:635-636 (used at 766, 829, 1052, 1415), which exists to keep `todo_lines` out of the rule bodies. No sibling wrapper (e.g. `span_markers(todo, num)`) was added.

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §44 plus §24 plus §20 plus D00 T04 §1, 2026-09-19. Neighbors: §24 (Depends, filed-from), §20 (XREF, follows-check home), D00 T04 §1 (XREF, filing sink). Dependents: none by the mechanical rule (no Depends row names §44; citers are upstream filers). Runner note: run via the skill template; output `check-plan PASS` (12 findings, one per line; re-verified at transcription against the ledgered artifact). Run note: plan run `20260919-D00-T01-S44-gpt-r3`, digest `sha256:921e6de533b794e0278fe172f7f8735f38e92c2505e2ea6bb01608c0d415a9cc`, receipt ledgered under ignored `build/review-runs/`; bytes 27272 counted live over the prompt via `canonical_prompt_bytes`. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §44, D00 T01 §24, D00 T01 §20, D00 T04 §1]; dependents [none]; bytes 27272; run 20260919-D00-T01-S44-gpt-r3

Ledger (namespaced IDs per the §16 shape; 3 of 12 filed at D00 T04 §1, 1 accepted in the §44 stamp run, 1 duplicate, 7 rejected with reasons on their rows):

Triage: 3 filed findings ride 2 items (PR4 plus PR12 share the observability item at D00 T04 §1).

Ledger:
- [D00-T01-S44-PR1] [minor] Skips suppress instead of correcting -> rejected the range head validates the full chain in its own span, so malformed lineage cannot escape; the skip only stops false fires on partial member views, and the option-b decision is recorded in the item Done note
- [D00-T01-S44-PR2] [minor] Positive follows-fire coverage missing -> rejected already satisfied: §§28/31/32 fire probes with exact-count assertions prove dangling follows-outage still fires; suite green
- [D00-T01-S44-PR3] [minor] Range matrix too narrow -> filed D00 T04 §1
- [D00-T01-S44-PR4] [minor] query run hides partial lineage -> filed D00 T04 §1 (shared observability item)
- [D00-T01-S44-PR5] [minor] Helper naming splits ownership -> accepted in the §44 stamp run: the R3-F1 item now reconciles span_markers with span_marker_bodies (one API, no parallel)
- [D00-T01-S44-PR6] [minor] Fallback marker undocumented -> rejected fixtures are the refactor guard (they fail if the skip is lost) and the code comment documents the two-line derivation; no propagation exists to specify
- [D00-T01-S44-PR7] [minor] Checkpoint should assert classification -> rejected the checkpoint correctly asserts the shipped contract (silence); the classification is new work and rides the filed observability item
- [D00-T01-S44-PR8] [minor] Full-chain parsing needs an owner -> rejected no consumer exists: section_markers' contract is unchanged by §44 and every marker line is fully checked in its head span; the recorded decision stands until a member-level-chain need lands
- [D00-T01-S44-PR9] [minor] Wrapper residual overstates closure -> rejected already satisfied: the bidirectional XREF pair links §44 to the T04 item and the stamp Review line names the filed follow-up
- [D00-T01-S44-PR10] [minor] T04 §1 needs splitting -> rejected T04 §1 holds 24 boxes, under the 30-item sizing trigger; the split rule fires at 30, not at topic count
- [D00-T01-S44-PR11] [minor] T04 checkpoint omits joiners -> duplicate D00-T01-S37-PR21
- [D00-T01-S44-PR12] [minor] Lineage observability premium win -> filed D00 T04 §1 (shared observability item)
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `sha256:921e6de533b794e0278fe172f7f8735f38e92c2505e2ea6bb01608c0d415a9cc`; run `20260919-D00-T01-S44-gpt-r3`):

```
- Range-member validation is suppressed rather than made correct; D00 T01 §44 should replace the blanket `_fallback` skips with full-chain retrieval or an explicit tri-state result, because malformed genesis and outage lineage inside range stamps can now escape validation.
- Positive coverage for the guarded follows-outage rule is missing; D00 T01 §44 should prove that a genuinely dangling `follows-outage` still fires when complete lineage is available, because silence-only fixtures can pass even if the rule has been disabled universally.
- Coverage is too narrow for a general range-lineage claim; D00 T01 §44 should add three-plus-marker, middle-member, final-member, mixed outage/rerun, and `-r1` synonym cases, because one two-marker range cannot establish correct span selection across realistic histories.
- Partial lineage is invisible to operators; D00 T01 §24’s `query run` output should label fallback-derived lineage as partial or return the complete span, because a one-marker display can falsely present a range member as genesis even when validation quietly skips that conclusion.
- The helper design has conflicting ownership and naming; D00 T01 §44 introduces `span_marker_bodies`, while D00 T04 §1 later requires `span_markers` and removal of direct `todo_lines` use, so D00 T04 §1 should define the final public helper contract and migration target to avoid parallel APIs and another refactor.
- The fallback marker is an undocumented correctness boundary; D00 T01 §44 should specify how `_fallback` is produced, propagated, and prevented from being lost during refactors, because both guards depend on private metadata remaining intact.
- The checkpoint does not distinguish “valid” from “not evaluated”; D00 T01 §44 should assert an observable partial-chain classification in addition to diagnostic silence, because no warning currently tells maintainers that lineage guarantees were withheld.
- The selected option leaves the root truncation defect in place without an explicit follow-up; D00 T01 §44 or D00 T04 §1 should own eventual full-chain parsing with acceptance criteria, because permanent suppression accumulates blind spots as more lineage rules reuse `section_markers`.
- The section claims completion before its known review residual is resolved; D00 T01 §44 should either include the local-wrapper work or explicitly record it as non-blocking debt with dependency linkage to D00 T04 §1, because the current checked state overstates architectural closure.
- The T04 overflow section combines unrelated panel, telemetry, operator, social-preview, ledger, and marker work under one commit; D00 T04 §1 should split the span-marker follow-up into a focused section dependent on §44, because a large mixed candidate weakens atomic review and makes regression ownership unclear.
- D00 T04 §1’s checkpoint omits most joined items, including telemetry, operator enforcement, proof design, ledger reconciliation, and the §44 wrapper; each item or split section should have an explicit checkpoint, because generic “suite green” cannot prove the promised behavior.
- Lineage observability is a premium-product opportunity; D00 T01 §§24/44 should expose complete, partial, and skipped-check status in `query run` and telemetry, because operators need trustworthy explanations rather than silent validation gaps during incident and audit work.
```
