# Review: D00 T02 §2 -- UI Automation Driver Spike

Candidate: `749fe22` (spike plus UISmoke) plus in-candidate fixes `4eb476f` (CI framework install) and `40d3373` (crash-dialog signature doc). Two fix cycles before the lenses (CA1307 plus apphost resolution locally, WindowsAppRuntime CI step from run evidence); lenses clean.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Spike doc | FlaUI measured (launch+attach 506 ms, find 35 ms, screenshot 198 ms/106 KB verified by eye, click 177 ms, island traversal works) and WinAppDriver measured (session+launch 3755 ms, find 262 ms, screenshot 282 ms/177 KB, click 329 ms) with install, admin, and client-pinning costs; third contender considered and ruled out with rationale |
| 2. Decision | FlaUI named winner with reversal cost (one file, six API calls; reversal adds a server lifecycle plus a legacy client pin) |
| 3. tests/UI | `UISmoke` green locally (642 ms) and run `34798097715` green with UI.dll 1/1 on the Windows runner; run `34797312338` red first on the missing-framework crash dialog, fixed in-candidate |
| 4. Gaps | No Invoke control until D01 (D06 T01 §3, D01 AutomationIds noted); AutomationId/Name selector convention (D06 T01 §1); golden comparison procedure (T02 §3 plus D06 T01 §3); flakes (T02 §5, D06 T01 §6) |
| 5. Commit | `749fe22`, subject matches the item exactly |

Runs: `34797312338` red on Windows (crash dialog, missing framework), `34798097715` green both jobs with UI.dll 1/1 on the runner. `main` is green at HEAD.

## Self-review

1. Does it make sense? Yes. Every item has its change; WinAppDriver got a full measured drive rather than dismissal by reputation; the CI framework gap was found by the checkpoint and fixed in-candidate with a pinned URL matching SDK 2.4.0.
2. Is it logical? Yes. Pins cohere (FlaUI 5.0.0, framework 2.4.0, SDK 2.4.0); the Windows-only TFM stays out of the neutral filter with the Linux suite proven unchanged; `apppath.txt` plus the asserted dll-to-exe swap is configuration-proof; the failure screenshot and 15 s timeout serve CI.
3. Did it break anything else? No. `build.yml` gained one Windows-only step with Linux proven green; the app project untouched (the spike added no controls to the stub); `validate` 0 fatal; `self-test` 391/391; full suites green.

## Lenses

- `adversarial`: approve. The server-based alternative lost, shrinking the ops surface; failure screenshots stay on local temp with a printed path; the framework installer is pinned by URL and version.
- `consistency`: approve. Test trio pins match Smoke/Unit; lockfile committed; `UISmoke` naming; Markdown one line per item; no em dashes.
- `integration`: approve. Both solution consumers green; gap routings name live open sections (D06 T01 §1/§3, T02 §3/§5); `docs/testing.md` updated for the new suite.
- `record`: approve. Run IDs plus local measurements quoted; the red-then-green arc recorded with its cause; zero deferrals.
- `source-defect`: not owed (no Notepad behavior or protocol detail; captures are §3).
- `design`: not owed (test code only, no app surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; full suites green on both OSes locally and in CI; `plan --check` green after the stamp-flow sync.
