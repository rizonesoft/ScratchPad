# Review: D00 T01 §5 -- Test Wiring and First Smoke Test

Candidate: `a7f6fce` (wiring) plus checkpoint evidence commits `805ce64` (red probe) and `c317b8a` (revert). No fixes; one round.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Test command | `dotnet test` on the slnf (Linux) and slnx (Windows) exits 0 with 1/1 passing on both OSes locally and in CI |
| 2. Smoke test | `Smoke.SmokeTests.CoreConstructsAndReportsVersion` asserts construct + `NotEmpty(Version)`; `--filter Smoke` passes; blanked version fails locally (Failed: 1) |
| 3. CI tests | Test steps after build in both jobs; probe run red on both with the failing test named; reverted immediately |
| 4. Output layout | `TestResults/` ignore proven by dummy probe at project level; `git status` clean after full runs on both OSes |
| 5. Commit | `a7f6fce`, subject matches the item plus the skill-mandated ref |

Runs: `34790029058` green both jobs with `Passed! 1/1` in both Test steps, `34790728911` red both jobs with `Failed Smoke.SmokeTests.CoreConstructsAndReportsVersion`, `34791418524` green after revert. `main` is green at HEAD.

## Self-review

1. Does it make sense? Yes. Every item has its change; the v2 reversal is evidence-backed (v3+MTP zero-test discovery proven on our project and xunit's own template across ~20 config probes); `Version` centralization and the `README` pins update are recorded with the §1 body untouched.
2. Is it logical? Yes. The VSTest trio is coherent and pinned with a lockfile; `NotepadCore.Version` fails loud on a missing stamp; the app binary still stamps `0.0.0+HEAD` after centralization (re-verified from the exe); CI mirrors the local commands.
3. Did it break anything else? No. Builds stay warning-free under the gates (test code included); `validate` 0 fatal; `self-test` 391/391; the `global.json` test-runner experiment left no diff; T02 §1 is flagged to re-evaluate v3/MTP with no file overlap.

## Lenses

- `adversarial`: approve. Red-by-design probe reverted immediately; no secrets; the version string is the only new observable and carries no sensitive data.
- `consistency`: approve. Naming (`Smoke`, `SmokeTests`, `NotepadCore`) matches the plan; T01 marks dated; Markdown one line per item; no em dashes.
- `integration`: approve. §2 outputs, §3 CI, and §4 gates consumed and extended in place; T02 §1 owns `tests/Unit` separately with the v3/MTP flag recorded in the commit.
- `record`: approve. Three run IDs plus local green/red/filter/blank outputs quoted; the reversal evidence trail (template failure included) recorded; zero deferrals.
- `source-defect`: not owed (no Notepad behavior or protocol detail).
- `design`: not owed (no UI surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; local suite green on both OSes; CI evidence stands on the exact pushed commits; `plan --check` green after the stamp-flow sync.
