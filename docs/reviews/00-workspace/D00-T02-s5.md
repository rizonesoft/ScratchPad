# Review: D00 T02 §5 -- Soak and Quarantine Procedure

Candidate: `549ff76` (soak workflow plus procedure doc) plus in-review fix `77aff94` (red-repeat propagation). One fix cycle from self-review evidence; lenses clean.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Nightly soak | `.github/workflows/soak.yml` (03:17 UTC schedule plus dispatch; full suite both OSes; UI x5 and Protocol x5 repeats; trx artifacts; 45/60 min timeouts) and `docs/soak-and-quarantine.md` (what runs, how long, where results go); soak ran twice by dispatch (`34808621885`, `34809456063`), both success with 22 `Passed!` lines and zero failures |
| 2. Quarantine | Doc procedure (prove the flake with two quoted runs, xUnit `Skip` with a `QUARANTINED <date> <owner> <signature-id>` stamp, list row) plus the list table with Test, Failure signature, First seen, Owner, Quarantined, Due; empty after the probe lifecycle |
| 3. Window | 7-day fix-or-remove window per test, day-3 status note, day-7 overdue-removal by the maintainer with the owner named |
| 4. Deletion rule | "Deleting a test without a recorded decision fails review" plus the Removal decisions log; the probe's removal is the first row |
| 5. Commit | `549ff76`, subject matches the item exactly |

Checkpoint lifecycle (local, same `Unit.dll` binary): probe added, six runs went pass, fail, fail, fail, fail, pass; quarantined with the documented `Skip` stamp, suite green twice at 2 passed plus 1 skipped; probe removed with its removal row recorded. No retry anywhere: the suite goes red on flakes, quarantine is the only silencer, and it stays visible and owned.

## Self-review

1. Does it make sense? Yes. Every item has its change; the workflow mirrors `build.yml` pins and provision steps exactly. Self-review found one real defect (the Linux repeat loop swallowed a red middle repeat, exiting 0); fixed in `77aff94` with `|| exit 1`, proven by a shell-construct probe (bare loop exit 0, guarded loop exit 1), and re-proven by soak run `34809456063`.
2. Is it logical? Yes. The soak repeats fail the job on the first red on both OSes now; trx filenames are unique per repeat; artifacts upload `always()` so red runs keep their logs; the quarantine stamp format is greppable; the window and escalation name actors and dates.
3. Did it break anything else? No. The workflow has no push trigger, so per-commit CI is unchanged; push builds on both candidate commits green; `validate` 0 fatal; `self-test` 391/391.

Timing note: the soak-log run link lands in the stamp commit, not the candidate, because the run URL cannot exist before the workflow is on `main`. The §5 stamp commit therefore touches the doc as well as the findings, TODO, and plan files.

## Lenses

- `adversarial`: approve. A flake cannot hide: repeats propagate red, `Skip` without a list row fails review by the written rule, and trx artifacts persist on failure. The `Skip` stamp is free text, so a malformed stamp still skips; review is the backstop, as written.
- `consistency`: approve. Workflow pins and provision steps identical to `build.yml`; doc follows one-line Markdown; no em dashes in new files.
- `integration`: approve. No callers change; future quarantiners consume the doc; the review skill does not yet cross-check test deletions against the removal log (advisory: the doc rule binds reviewers now, automation later if ever).
- `record`: approve. Two soak run IDs plus the six-run probe sequence quoted; the stamp-commit doc touch recorded with its reason; zero deferrals.
- `source-defect`: not owed (no Notepad behavior or protocol detail at stake).
- `design`: not owed (procedure doc and workflow only, no app surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; full neutral solution green locally (Smoke 1/1, Unit 2/2, Protocol 8/8, probe gone); push builds green on both candidate commits; soak green twice by dispatch; `plan --check` green after the stamp-flow sync.
