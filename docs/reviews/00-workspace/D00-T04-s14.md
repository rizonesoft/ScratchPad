# Review: D00 T04 §14

Candidate range `af565e3..b338d6c`.
Reviewed tip `b338d6c` (fix-loop round 4 record: R4 verdicts plus R4-F1 filing; code tip 3663ec8; scripts untouched since).
Full panel: the diff touches `scripts/` (shared plan tooling) plus skills. Rounds 1 and 2 are `gpt-5.6-terra` high on the pre-rewire panel (ledger runs `20260922-D00-T04-S14-codex-c7001fd6f` and `-r2`, both PASS). Round 3 up runs the rewired panel per operator direction 2026-09-22 (§15 stamped first): round 3 is the `signoff` slot (`claude-opus-5` xhigh) sign-off, ledger run `20260922-D00-T04-S14-claude-c7001fd6f` (PASS); blocking findings extend to round 4 on the `depth` slot.

Arch trigger: none - the section changes validator legs, query displays, fixtures, and review-skill prose, not editor state, storage, extensibility, AI integration, protocol, or consent/undo.

## GPT panel (round 1)

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` needs-attention

Telemetry: round 1; model gpt-5.6-terra; effort high; duration 47s; outcome needs-attention; tokens unknown

```text
**adversarial: needs-attention** (1)
1. `scripts/todo-validate.py:572` ignores a post-cutover `Opus panel` when a later valid `GPT panel` fallback exists, allowing retired panel wording to validate despite the contract requiring wrong-shape-for-date records to fire.

**consistency: needs-attention** (1)
1. `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:286` claims both review skills were updated, but the candidate contains no `.grok` review-skill change, leaving that contract requirement unmet.

**integration: approve**

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:286` records the `.grok` skill requirement as done without the required tracked change, making the section’s completion record inaccurate.
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` needs-attention
- `integration` needs-attention
- `record` needs-attention

Telemetry: round 2; model gpt-5.6-terra; effort high; duration unknown; outcome needs-attention; tokens unknown

```text
**adversarial: approve**
**consistency: needs-attention** (1)
1. `.grok/skills/review-todo-section/SKILL.md:123`: The Light panel declares a soft cap of 2 but requires rounds 1 through 3, creating an extra round and contradicting its own cap.
**integration: needs-attention** (1)
1. `scripts/todo-graph.py:2714`: `TELEMETRY_OPUS_RE` is case-sensitive while validator headings and GPT-outage parsing are case-insensitive, so a valid lowercase `claude outage` record is omitted from telemetry output.
**record: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:79`; `.grok/skills/review-todo-section/SKILL.md:85`: Both skills still direct the current workflow to an unqualified “Opus sign-off,” contradicting the post-cutover `claude-sonnet-5` sign-off and retired-label requirement.
```

## Opus panel (round 3)

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` needs-attention
- `record` needs-attention

Telemetry: round 3; model claude-opus-5; effort xhigh; duration unknown; outcome needs-attention; tokens unknown

```text
**adversarial: needs-attention (1)**

1. `scripts/todo-validate.py:575,593,639` ? the R1-F1 guard is one-directional. Post-cutover, a stray legacy section fires (`:593`), but pre-cutover there is no mirror for a new-word section: `signoff_re` at `:575` collects only `Opus panel` heads when `new_era` is false, so a findings file holding `## Opus panel (round 1)` (four approves) followed by the governing `## Claude panel (round 2)` (needs-attention) under a stamp dated `2026-09-22` or earlier validates the superseded round-1 slice at `:639` and stays silent, breaking the rule's own last-wins invariant and the checkpoint clause "any silent wrong-shape record". Fixture ?57 (`scripts/todo-graph.py:10598`) pins only the pure Claude-only pre-cutover case, not the mixed one; the post-cutover mirror is pinned at ?56.

**consistency: needs-attention (2)**

1. `scripts/todo-graph.py:3991` and `scripts/todo-graph.py:3997` ? the two outage display labels added by this change use opposite case policies. `:3991` normalizes (`slab = "Sol" if sm.group(1).lower() == "sol" else "GPT"`), while `:3997` echoes the raw match (`om.group(1)`). Both feed the same per-record line at `scripts/todo-graph.py:4725` and `:4728`, so `query telemetry` prints a normalized `Sol outage:`/`GPT outage:` beside a raw-cased `claude outage:`/`OPUS outage:` from the same file. The self-test pins the divergence rather than catching it: `scripts/todo-graph.py:24502` expects `"GPT"` and `:24561` expects lowercase `"claude"`.
2. `scripts/todo-graph.py:1012`, `scripts/todo-graph.py:1016`, `scripts/todo-graph.py:4535` ? retired-word comments left un-era-qualified while the same pass era-qualified every neighbouring comment (`:1215-1221`, `:1563-1567`, `:1575-1579`, `:3889-3893`, `scripts/todo-validate.py:445-452`, `:733`, `:748`). `:1012-1014` still describes `stamp-no-opus-panel` as "the Opus-panel rule"; `:1016-1019` still describes `panel-sol-outage-missing` as "an Opus-only review ... with no Sol-outage accountability ... or the Sol run"; `:4535` still heads the telemetry block "Sol-outage coverage" after the display moved to "GPT-outage coverage" at `:4652`. These are the in-code definitions of the two classes the section renamed, so they now document only the pre-cutover era.

**integration: needs-attention (1)**

1. `.grok/rules/campaign-runner.md:40` ? "Panel headings stay `Opus panel` for the Claude family and `GPT panel` for the Codex family, because the validator matches those words." The candidate's new `.grok/skills/review-todo-section/SKILL.md:8` routes the Grok runner to that file as its host rules, and the skill's own panel section mandates `Claude panel` after 2026-09-22. A post-cutover review following the host rules writes `Opus panel` and fires `stamp-no-opus-panel` twice (`scripts/todo-validate.py:584` missing-section and `:593` legacy-section), so the rename's `.grok` leg is contradicted by the rules file the same skill defers to.

**record: needs-attention (1)**

1. `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:289` ? item 2's Done note reads "Done: ??46-55 plus telemetry and plan-health pins", but the candidate ships twelve fixture sections, ?46 through ?57: the Implementation Order rows at `scripts/todo-graph.py:9907-9918` and the bodies at `scripts/todo-graph.py:10416-10606` include ?56 (mixed legacy-plus-new) and ?57 (pre-cutover Claude-worded), both added in the R1-F1 fix round and both carrying their own self-test pins at `scripts/todo-graph.py:24505` and `:24513`. The note names the pre-fix range, so the evidence line understates what the row will be stamped on.
```

R3-F1 disposition: fixed by 3663ec8 (pre-cutover mirror leg plus §58 fixture reusing the mixed file; README row extended). Blocking: silent wrong-shape records falsify the checkpoint, so round 4 re-runs on the `depth` slot. R3-F2 disposition: filed §17 (below-bar display case split needing a designed echo-vs-normalize decision). R3-F3 disposition: fixed by 3663ec8 (three comments era-qualified). R3-F4 disposition: filed §17 (below-bar; the contradicting rules file sits outside the candidate untracked, so the fix needs owner direction). R3-F5 disposition: fixed by 3663ec8 (Done note corrected to §§46-58, 1537 to 1571).

## Dispositions

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | 207a6ee |
| R1-F2 | fixed | 207a6ee |
| R1-F3 | fixed | 207a6ee |
| R2-F1 | fixed | c66111d |
| R2-F2 | fixed | c66111d |
| R2-F3 | fixed | c66111d |
| R3-F1 | fixed | 3663ec858d5b548872f2b69e5807f39f2a8f39eb |
| R3-F2 | filed | D00 T04 §17 |
| R3-F3 | fixed | 3663ec858d5b548872f2b69e5807f39f2a8f39eb |
| R3-F4 | filed | D00 T04 §17 |
| R3-F5 | fixed | 3663ec858d5b548872f2b69e5807f39f2a8f39eb |
| R4-F1 | filed | D00 T04 §17 |
| R4-F2 | fixed | (R4 record commit: Live proof plus provenance re-quoted at c57eaad) |
| R5-F1 | fixed | (stamp commit: item-1 Done note re-quoted to 1571 plus mirror leg named; s50 stamp-fixed precedent) |

## Opus panel (round 5)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` needs-attention

Telemetry: round 5; model claude-opus-5; effort high; duration unknown; outcome needs-attention; tokens unknown

```text
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)

1. `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:290` -- item 1's Done note still quotes gate evidence from a superseded candidate: `self-test 1537/1537, live validate 0 fatal`, while items 2 and 3 on the same checklist (`:291`, `:292`) quote `1571` and the findings-file Live proof (`docs/reviews/00-workspace/D00-T04-s14.md:129-135`) quotes `1571 cases, 0 failed` at the reviewed tip `c57eaad` (digest `db698737...` verified as the sha256 of `scripts/todo-graph.py` at `c57eaad`, identical at `3663ec8`; `git diff --stat c57eaad b338d6c -- scripts/` is empty, so 1571 is the tip's total). Item 1 owns the validator legs (`Done when: the legs plus the cutover constant ship in scripts/todo-validate.py and scripts/todo-graph.py`), and the R3-F1 pre-cutover mirror leg at `scripts/todo-validate.py:649-660` shipped at `3663ec8` under that item, after the quoted 1537 run: the note therefore names neither the leg it shipped nor a run that exists at the reviewed tip. Failure scenario: the stamp is written asserting `self-test 1571/1571` while the checklist row it flips carries 1537 as item 1's proof; an auditor re-running at the tip reconciles items 2-3 and the Live proof but not item 1, and the row rests on evidence from a changed candidate, which `AGENTS.md` forbids transferring. This is the R4-F2 family (stale gate evidence) at an unswept site -- the R4-F2 fix note names only "item-3 Done note re-quoted to 1571/1571", and the skill's same-family circuit breaker (`.claude/skills/review-todo-section/SKILL.md:133`) requires sweeping the family across the candidate with the sweep quoted. Fix: re-quote item 1 to the reviewed tip's total and name the mirror leg among its legs.
```

R5-F1 disposition: fixed in the stamp commit (s50 stamp-fixed precedent for record-level findings at the cap: no round 6 exists, and filing a one-line evidence re-quote as tracked work would stamp a known-stale note). Same-family sweep quoted: `1537|1557|1560|1570|1571` across TODO-04 hits item 1 (stale, fixed here), item 2 (1571 current), item 3 (1571 current), §15's stamp plus Done (1570 point-in-time, correct as stamped), and nothing else; Live proof quotes 1571 at c57eaad. Panel closed at the hard cap: R5 adversarial, consistency, and integration approve; the single record finding is stamp-fixed with no live code findings.

## Opus panel (round 4)

- `adversarial` needs-attention
- `consistency` approve
- `integration` approve
- `record` needs-attention

Telemetry: round 4; model claude-opus-5; effort high; duration unknown; outcome needs-attention; tokens unknown

```text
**adversarial: needs-attention** (1)

1. `scripts/todo-validate.py:593` (with `:534`) keys the label era to the stamp date while findings files are append-only records, so every pre-cutover record becomes FATAL the moment its section re-stamps. `new_era = s.stamped_on > LABEL_CUTOVER` and the R1-F1 leg fires on *any* `Opus panel` heading anywhere in the file, with no record-date or grandfather escape (the only grandfather, `PANEL_CUTOFF`, stops at 2026-09-17). Failure scenario: any of the 62 live findings files carrying `^## Opus panel` (`grep -rlE '^#{2,6}[[:space:]]+Opus panel' docs/reviews` = 62) is re-verified after 2026-09-22 -- audit stance re-stamp, reopen-and-re-stamp, or any fix-loop that moves `Verified:` past the cutover -- and the skill mandates appending the new rounds to the same file (`.claude/skills/review-todo-section/SKILL.md:110` "re-run the panel against the NEW candidate diff with the prior verdicts appended"), so the file holds both eras: the new `Claude panel` round satisfies `heads`, and `:593` fires on the superseded legacy round. The only remedy is editing committed review records, which `AGENTS.md` forbids ("Do not rewrite a stamped checklist or transfer evidence across changed candidates"), and the section checkpoint names this exact failure ("Falsifiable by ... any old-word grandfather firing"). The same trap is imminent for this candidate's own record: `docs/reviews/00-workspace/D00-T04-s14.md:50` is `## Opus panel (round 3)`, committed in c57eaad, so if this run's stamp lands on 2026-09-23 UTC rather than today, §14's stamp fires `stamp-no-opus-panel` on its own findings file. Fixtures §56 and §58 pin the mixed-file case as FAIL in both directions, so the behavior is deliberate; what is missing is the record-era or re-stamp escape that keeps 62 committed records re-verifiable.

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)

1. `docs/reviews/00-workspace/D00-T04-s14.md:100,104,107` -- the record's only quoted gate predates the last two fix commits, so the stamp would rest on unquoted evidence. Live proof quotes `self-test` **on `c66111d`** at **1560 cases**, and `Provenance:` digests `scripts/todo-graph.py` at candidate `c66111da63a...`; but the reviewed tip is `3663ec8` (line 4), and `git show --stat 3663ec8` shows it modified `scripts/todo-graph.py` (+41/-12) and `scripts/todo-validate.py` (+13), so the recorded digest `4f69135825...` is of a blob that no longer exists at the reviewed tip and 1560 is not the tip's total. Meanwhile `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:291` (R3-F5's fix) now asserts "1537 to **1571** with 0 failed" and `:292` asserts "1557/1557" -- three different totals, none of them the one quoted in the findings file. Failure scenario: the stamp is written with `self-test 1571/1571` as its `Verified:` evidence while the committed artifact behind it says 1560 at a superseded commit; a later auditor re-running at the tip cannot reconcile the claim against the record, and the skill's own rule ("After the last fix, re-run the section's Test checkpoint and the owed gates ... Quote the outputs. A fix verified by reasoning is not verified.", `.claude/skills/review-todo-section/SKILL.md:170`) is unmet for `3663ec8`. Fix: re-run the gates at the reviewed tip and replace the Live proof quote, provenance candidate, and digest with the tip's.
```

R4-F1 disposition: filed §17 (below bar: the stamp-date era keying is the shipped contract design, and a record-era or re-stamp escape is a contract redesign, not a fix-loop fix; the 62-file count verified by grep; the imminent sub-point is controlled by stamping 2026-09-22, which keeps this record in the old era where its `Opus panel` round 3 stays silent). R4-F2 disposition: fixed by this commit (Live proof plus provenance re-quoted at the reviewed tip c57eaad with digest db698737fcde120cbb02db26a6536344ece7cfaa5ee324987547fb23b7a40eed; item-3 Done note re-quoted to 1571/1571).

Self-review sweep in c66111d (not a panel finding): the `.grok` outage matrix ran per-round retry against the `.claude` no-flap rule, so it now mirrors the sticky-failover sentences; the Light tier fix is R2-F1 proper.

## Live proof

`python3 scripts/todo-graph.py self-test` on `c57eaad` (exit 0):

```text
todo-graph self-test: SKIP sibling stamp-gate clamp probe (no section_commit_gate.py)
todo-graph self-test: 1571 cases, 0 failed
```

`python3 scripts/todo-graph.py validate` on the same tree: 0 fatal, 1 warning (known owner-login mapping). `python3 scripts/probe_runner.py --self-test`: 6 cases, 0 failed.

Provenance: candidate c57eaad8926cce53c75a33e52ec0e263c3fd6464; command python3 scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.4; digest db698737fcde120cbb02db26a6536344ece7cfaa5ee324987547fb23b7a40eed; path scripts/todo-graph.py; run 20260922-D00-T04-S14-codex-c7001fd6f-r4

The provenance run finalized to the plan marker's run at the stamp (`-r4`: the provisional `-r3` prediction was consumed by its own claim in this file, so the plan run minted one past it). (R4-F2: re-quoted at the reviewed tip; prior quote at c66111d/1560 superseded by the 3663ec8 fix.)

## Plan review

Round: `gpt-5.6-sol` medium over §14 plus §35 plus §37 plus §1 plus §15, 2026-09-22. Neighbors: §35, §37, §1, §15 (XREF both directions with §15: the rewire stamps before this review's round 3); dependents: §15 (XREF-only consumer; the transitive Depends hop past it adds nothing: §15 carries no Depends). §17 filed after the run from this review's own sign-off rounds; it carries no independent plan content (this review's own filings), so no rerun. Output `check-plan PASS` (8 findings, one per line). Two filed (PR4, PR6 as §18 items 1-2, this commit); four rejected with reasons below; two premium wins noted. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T04 §14, D00 T01 §35, D00 T01 §37, D00 T04 §1, D00 T04 §15]; dependents [D00 T04 §15]; bytes 41852; run 20260922-D00-T04-S14-codex-c7001fd6f-r4

Ledger:
- [D00-T04-S14-PR1] [major] Ambiguous cutover boundary for the round-3 header switch -> rejected conflates the model rewire with the label cutover: the header follows the stamp-date era (opus producer under the era-correct Opus heading on this 2026-09-22 stamp), and the switch rides telemetry plus header prose
- [D00-T04-S14-PR2] [major] Missing fixture for the mixed-era review shape -> rejected no new validator shape exists: the review carries era-clean old words throughout (producer identity is telemetry, not validation), and mixed-word shapes pin at sections 56 and 58
- [D00-T04-S14-PR3] [major] Review sequencing unenforced by the plan graph -> rejected the graph encodes implementation need and the order was honored in fact (section 15 stamped before round 3 ran), recorded in both Whys plus bidirectional XREFs; no edge vocabulary exists for runner sequencing
- [D00-T04-S14-PR4] [major] Claude outage note lacks the failure payload shape -> filed §18 (define plus enforce the Claude-outage failure payload matching the shaped GPT-outage note)
- [D00-T04-S14-PR5] [major] Section 1 items should retitle before implementation -> rejected re-argues section 14 item 6's recorded reconcile-at-implementation decision; the coordination note reads at section 1 as contracted
- [D00-T04-S14-PR6] [major] Section 15 stamped with its Commit item unticked -> filed §18 (dated shipped-state annotation on the stamped checklist per the section-35 precedent)
- [D00-T04-S14-PR7] [minor] Compatibility win in date-scoped labels with stable enums -> accepted win affirmed in the stamp run; no filing needed (premium win, no work)
- [D00-T04-S14-PR8] [minor] Maintainability win in the centralized slot registry -> accepted win affirmed in the stamp run; no filing needed (premium win, no work)
End of ledger
