# Review: D00 T01 §21

Candidate: `d5d9385` (ship) + `9ba77f9` (self-review) + `de721b4` (probe isolation) + `9e6e087` (count) + `d6ad768` (R1 fix) + `081d0a6` (R2 fix) + `569c503` (R3 fix) + `196007a` (R4 fix). Full panel (scripts plus skill plus validator: shared contract surface).

## Self-review

Sense: all 5 work items land as specified plus the commit. Each Done-when maps to code or prose: the partial coherence rule in skill plus README with validator plus query legs (item 1), the acceptance record shape with rule 24 plus surfacing (item 2), the four batches with criteria plus the deadline gate (item 3), the operator summary digest (item 4), the 57-file provenance inventory (item 5).

Logic: the validator, query, skill, and README agree on every new shape (partial rung vocab, acceptance record, coverage window, lenient versus strict gating, /3 keys); fixture counts preserved where behavior kept (legacy pair intact, §44 rule-24 probes extended by taxonomy, clean-tree summary exits intact).

Blast radius: no behavior change outside the review loop (all edits are review-loop code, its fixtures, its docs, and dated supersession notes); live tree silent (validate 0 fatal, grandfathered count steady at 53, `--check` and summary exit 0).

Fix: one self-review finding, fixed in `9e6e087` before the panel (costs no round): the AGENTS self-test count read 692 against a tree emitting 694. Two suspected defects disproved against code (outage-rung regex already spans spaces; summary next-line shapes match their report dicts).

## Live proof

Gates on `c254f85` (code-identical to candidate tip `196007a`; the three later commits touch TODO and plan filings only). Digests are sha256 over the quoted bytes, each line LF-terminated.

`python3 scripts/todo-graph.py self-test`:

```
todo-graph self-test: SKIP sibling stamp-gate clamp probe (no section_commit_gate.py)
todo-graph self-test: 699 cases, 0 failed
```

Provenance: candidate c254f85; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest d4ad995c94f2475cd8fa9d886659cec6b6b46396f962a890dd1174ce1856e852; path docs/reviews/00-workspace/D00-T01-s21.md; run 20260918-D00-T01-S21-gpt

`python3 scripts/todo-graph.py validate`: `22 todos, 195 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate c254f85; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest bd5f41b38074b6044cfe42337be713b02627dec1393e1a249accc7bf0c43a471; path docs/reviews/00-workspace/D00-T01-s21.md; run 20260918-D00-T01-S21-gpt

`python3 scripts/todo-graph.py plan --check`: `implementation plan is current -- 59/190 sections complete (31%)`, exit 0.

Provenance: candidate c254f85; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest ab2873ac907eec8622a704adf03a3951f41e90d94a7e87b83053f90d517880dc; path docs/reviews/00-workspace/D00-T01-s21.md; run 20260918-D00-T01-S21-gpt

`python3 scripts/todo-graph.py query summary`:

```
incomplete runs     0
blocked clearances  0
overdue owners      0
next: nothing actionable
gate: ok
```

Provenance: candidate c254f85; command `python3 scripts/todo-graph.py query summary`; exit 0; tool CPython 3.12.3; digest a9f5ee96d76b39d16392c3a6ce78384c961c3bac00f2a3e613ef86bc2cdee342; path docs/reviews/00-workspace/D00-T01-s21.md; run 20260918-D00-T01-S21-gpt

`python3 scripts/todo-graph.py query plan-health --check`: `gate: ok`, exit 0.

Provenance: candidate c254f85; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 83e9a060459d5921fedae881c1e079a0169941dd7fcff74fabc57c3188d0943b; path docs/reviews/00-workspace/D00-T01-s21.md; run 20260918-D00-T01-S21-gpt

## Opus panel (round 1)

Candidate: `d5d9385` + `9ba77f9` + `de721b4` + `9e6e087` (checked `fence` pipeline; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` needs-attention

Findings (all verified true, all fixed in `d6ad768`): adversarial-1 exact case-sensitive finding-ID compare while the target pattern admits lowercase (cover now folds case like the duplicate-ID rule, lowercase PR50 fixture); adversarial-2 rule 24 skips pre-cutoff files while the query consulted their acceptances (query now consults only validated files, old.md waiver probe); consistency schema comment still enumerating /2 (now names the /3 keys); record CRUD count 692 against 694 (synced, then 695 with the new probe). Fix: `d6ad768`.

Raw round-1 output (fenced, not part of the verdict scan; digest `1568217ca06f6edf5e2fd09a17438e5de322491a1ba534bcea5beff2294471bd`):

```
**adversarial: needs-attention (2)**
1. `scripts/todo-graph.py:1889` — acceptance-to-row matching is an exact, case-sensitive string compare (`tgt == lr.group(1)`), while every other ledger-ID comparison normalizes case (`scripts/todo-validate.py:1071`, `scripts/todo-validate.py:1199` both key on `.lower()`); an acceptance written `Risk accepted: d00-t01-s15-pr4; ...` (or with a padded variant, which `RISK_TARGET_RE` at `scripts/todo-graph.py:957` explicitly admits as a finding target) passes rule 24 as well-formed yet never covers the row, so the operator sees a validated waiver and a still-escalating OVERDUE with no diagnostic naming the mismatch.
2. `scripts/todo-validate.py:1275` vs `scripts/todo-graph.py:1886` — rule 24 skips findings files whose stamp is on or before `PLAN_REVIEW_CUTOFF`, but the query's ledger scan that consumes acceptances (`scripts/todo-graph.py:1793-1800`, unscoped by date) and `file_acceptances` read those same pre-cutoff files; an acceptance placed in a grandfathered findings file clears OVERDUE, escalation, and the `--check` gate vote for open criticals and majors in that file while never being checked for inverted dates (`date 2030-01-01; expires 2029-01-01` is accepted and live), so an unvalidated waiver can silence a gate.

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:929-941` — the machine-contract comment above `PLAN_HEALTH_SCHEMA` still enumerates only the /2 additions ("New keys since /1: stale entries carry `run`, degraded entries carry `escalation`, majors carry `owner`, `due`, and `escalation` ... criticals carry ..."), while the constant now reads `plan-health/3` and the report emits `accepted_by`/`accepted_expires` on degraded, criticals, and majors plus `overdue` on grandfathered (`scripts/todo-graph.py:1738-1739`, `2103-2113`); the one place that documents the schema contract does not name the keys the bump was for.

**integration: approve**

**record: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:591` — the `CRUD:` stamp cites "self-test 692/692" while the candidate range's own final commit `9e6e087` corrected the count to 694 (`AGENTS.md:55`, and `python3 scripts/todo-graph.py self-test` reports `694 cases, 0 failed`); the section's evidence line names a count the tree no longer produces.
```

## Opus panel (round 2)

Candidate: `d5d9385` + `9ba77f9` + `de721b4` + `9e6e087` + `d6ad768` (checked `fence` pipeline, R1 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings (all verified true, all fixed in `081d0a6`): adversarial review date plus rationale parsed for shape then dropped (both now ride the text tag and the JSON record, still /3 pre-stamp); consistency `--check` help still claiming non-emptiness (now states the lenient rule). A loop-var shadow (`rev` over the reverse-dependency map) crashed the self-test mid-fix and was renamed before commit. Fix: `081d0a6`.

Raw round-2 output (fenced, not part of the verdict scan; digest `48be799f6f4f8f822f1a04d79d8867fbf0f4db5bfbcf7d9b0bf58a3820b43a1a`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:955` (`RISK_ACCEPTED_RE` groups 5 and 6) with `scripts/todo-graph.py:997` and `scripts/todo-validate.py:1300` — the acceptance record's review date and rationale are parsed for shape only and then dropped: `acceptance_lines` returns `(target, approver, expires, kind)`, the validator's only date check is `am.group(4) < am.group(3)` (expiry vs record date), and the surfacing sites print `accepted by <approver> expires <date>` (`scripts/todo-graph.py:2292`, `2322`, `2335`) with no review date or rationale. Contract item 2 requires the review date and rationale "surfaced where the escalation lands". Concretely: `Risk accepted: D00-T01-S19-PR8; approver bob; date 2026-09-18; expires 2099-01-01; review 2020-01-01; rationale x` validates (rule 24 silent), clears OVERDUE, escalation, and the `--check`/summary gate vote for the life of the waiver, and its already-passed review date produces no signal in `query plan-health`, `query summary`, or the validator, so a waiver that was supposed to be revisited is indistinguishable from a fresh one.

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:9329` — the `--check` help string still reads "plan-health: exit 1 when any actionable dimension is non-empty", which this candidate made false: `dim_failing` (`scripts/todo-graph.py:983-1006`) gates `--check` leniently, so a non-empty `degraded` holding only a bare `partial:` and a non-empty `criticals`/`majors` holding only covered rows exit 0 (self-test `plan-health --check passes a bare partial`, `scripts/todo-graph.py:8087`). The skill (`.claude/skills/review-todo-section/SKILL.md:149`) already carries the strict-versus-lenient split; the CLI help, the one place an operator reads the flag's contract from the tool, does not. `--fail-on`'s help (`scripts/todo-graph.py:9330`) is accurate and needs no change.

**integration: approve**

**record: approve**
```

## Opus panel (round 3)

Candidate: prior five plus `081d0a6` (checked `fence` pipeline, R1-R2 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings (all verified true, all fixed in `569c503`): adversarial post-dated acceptance covered today (new `acceptance_live` predicate requires recorded on or before today, PR53 tree probe plus unit bounds); consistency-1 provenance gate undocumented (skill plus README now state it); consistency-2 degraded tag continuation misindented (reindented). Validator deliberately stays wall-clock-free: a post-dated record validates but covers nothing, failing loud as a persisting escalation. Fix: `569c503`.

Raw round-3 output (fenced, not part of the verdict scan; digest `797befc405858ea2b66f3a2ee8857e34d5ef92c699a7e4f94ff4519860ea18f5`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:997` with `scripts/todo-graph.py:1746` and `scripts/todo-graph.py:1924` — `acceptance_lines` drops the record date (`am.group(3)`) from its tuple, so coverage is decided by expiry alone (`if exp < today: continue`); the validator's only use of the record date is the inversion check at `scripts/todo-validate.py:1300`. `Risk accepted: 20260920-D90-T07-S42-gpt; approver bob; date 2099-01-01; expires 2099-12-31; review 2099-06-01; rationale typo'd year` passes rule 24 and covers today: it clears OVERDUE, the escalation, and the `--check`/summary gate vote for a waiver whose own record date has not arrived, and no surface or check can flag it since the field never leaves the regex.

**consistency: needs-attention (2)**
1. `.claude/skills/review-todo-section/SKILL.md:153` and `todo/README.md:405` — the acceptance rule is stated as "one line in the findings file carrying the escalation" with expiry as the only covering precondition, but `scripts/todo-graph.py:1666-1667` (`file_acceptances` returning `[]` for any path outside `validated_files`) makes an acceptance in a findings file owned only by a pre-cutoff stamp cover nothing, silently and with no diagnostic; the precondition exists only in the code comment at `scripts/todo-graph.py:1641-1647`, so an operator following the skill to waive an overdue row in a grandfathered file gets a persisting OVERDUE and no explanation.
2. `scripts/todo-graph.py:2306-2309` — the acceptance-tag continuation lines and the closing paren are indented to the `if` body's outer level instead of inside the parenthesized expression, unlike the two identical acceptance tags at `scripts/todo-graph.py:2339` and `scripts/todo-graph.py:2352`; the block parses but reads as if the strings and `)` were separate statements.

**integration: approve**

**record: approve**
```

## Opus panel (round 4)

Candidate: prior six plus `569c503` (checked `fence` pipeline, R1-R3 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings (all verified true, all fixed in `196007a`): adversarial review date unordered against record and expiry (rule 24 gains the review-window leg, bounds inclusive, in-file dates only, §44 third probe); consistency degraded sort key covering four of ten fields (now the full tuple; order unchanged since `ref` is unique). Fix: `196007a`.

Raw round-4 output (fenced, not part of the verdict scan; digest `ee26a4dad52bafe4b0a32878a3fbbc690ac3bc9d542d2edbea0ca3fdc306a498`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-validate.py:1298-1303` with `scripts/todo-graph.py:955-957` and `scripts/todo-graph.py:997` — rule 24's ordering leg compares only expiry against the record date (`elif am.group(4) < am.group(3)`); the review date (`am.group(5)`) is parsed, carried as `accepted_review`, and printed at `scripts/todo-graph.py:2323`, `2338` and `2352`, but never ordered against the record date or the expiry, and no query or gate consults it (`grep accepted_review` reaches only print and type assertions). `Risk accepted: D90-T07-S4-PR51; approver bob; date 2026-09-18; expires 2099-01-01; review 2020-01-01; rationale x` validates with 0 fatal, clears OVERDUE, the escalation and the `--check`/summary gate vote for the waiver's full life, while its review date fell due six years before the waiver was written; the same holds for a review date past the expiry (the fixture at `scripts/todo-graph.py:6759` carries `expires 2026-01-01; review 2026-10-01` and is flagged only for the expiry inversion). The one field that makes an acceptance revisitable is the only field in the record with no check of any kind.

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:2112-2116` — the comment directly above reads "Total sort keys (D00 T01 §19 item 13): the tuple of every scalar field, so no two entries tie", and this candidate extended the criticals and majors keys to the four new acceptance fields (`scripts/todo-graph.py:2117-2118`, 11 and 10 elements), but `degraded_sorted` still keys on `(ref, state, owner, due)` while the record now carries ten scalar fields (`overdue`, `escalation`, `accepted_by`, `accepted_expires`, `accepted_review`, `accepted_rationale` unkeyed, `scripts/todo-graph.py:1781-1790`); the self-test case named "plan-health --json sort keys are total" asserts the full tuple for criticals and majors but the four-field key for degraded (`scripts/todo-graph.py:7972-7975`), so it no longer proves totality for that list, and the §17 PR16 row's `/3` supersede note at `todo/00-workspace/TODO-01-repo-and-toolchain.md:526` claims the cases lock the acceptance keys under "every list keyed by the tuple of its scalar fields".

**integration: approve**

**record: approve**
```

## Opus panel (round 5)

Candidate: prior seven plus `196007a` (checked `fence` pipeline, R1-R4 output appended; output `check-panel PASS`).

- `adversarial` approve
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings: consistency two in-code rule-24 descriptions under-enumerate the new fourth leg (verified true against both sites). The cap sends it out instead of a sixth round: filed D00 T01 §25, named in the stamp's `Review:` line. No fix commit.

Raw round-5 output (fenced, not part of the verdict scan; digest `f365fd17ca530467e6a8c7d48ca97c4181b4b9a5e0a614323ed470548083e1c3`):

```
**adversarial: approve**

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:903-905` and `scripts/todo-validate.py:1261-1265` — both in-code descriptions of the `risk-acceptance-malformed` class still enumerate only three legs ("outside the record shape, with an uncoverable target, or expiring before it is recorded" / "carries target, approver, record date, expiry, review date, and rationale; the target names a finding ID, a run ID, or `outage <rung>`; and expiry never predates the record"), but rule 24 now has a fourth fatal leg, the review date outside `record..expiry` (`scripts/todo-validate.py:1298-1307`), which `todo/README.md:405` does name; the two places a reader learns the class's contract from the code under-report what it fires on.

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §21 plus §19 plus §20 plus §25 plus transitive §17, 2026-09-18. Neighbors: §19 (the Depends plus XREF neighbor), §20 (XREF neighbor), §25 (review dependent plus XREF consumer), §17 (transitive hop past §19); dependents §19, §20, §25 (XREF consumers; §25 also the reverse dependent). Runner note: the skill's `codex exec` template ran verbatim (`-c model_reasoning_effort='"high"'`); exit 0. Output `check-plan PASS` (25 findings, one per line).

Manifest: sections [D00 T01 §21, D00 T01 §19, D00 T01 §20, D00 T01 §25, D00 T01 §17]; dependents [D00 T01 §19, D00 T01 §20, D00 T01 §25]; bytes 46803; run 20260918-D00-T01-S21-gpt

Ledger (22 of 25 filed across §§26-29 plus a §25 item, 3 rejected with reasons on their rows; merges state both numbers in the target sections):

Ledger:

- [D00-T01-S21-PR1] [major] Grandfathered baseline reads 53 stamps and 57 files unexplained -> filed D00 T01 §26
- [D00-T01-S21-PR2] [major] Batches planned but not executable -> filed D00 T01 §26
- [D00-T01-S21-PR3] [major] Batch acceptance has no machine effect -> filed D00 T01 §26
- [D00-T01-S21-PR4] [minor] Migration unit undefined across stamps and files -> filed D00 T01 §26
- [D00-T01-S21-PR5] [minor] Current state unaddressable (no heading) -> filed D00 T01 §26
- [D00-T01-S21-PR6] [major] Outage target not bound to a run -> filed D00 T01 §27
- [D00-T01-S21-PR7] [minor] Run target waives many findings -> rejected wrong premise: run targets cover markers only, rows need per-finding targets
- [D00-T01-S21-PR8] [major] Acceptance may predate its target -> filed D00 T01 §27
- [D00-T01-S21-PR9] [major] Acceptance outlives its evidence -> filed D00 T01 §27
- [D00-T01-S21-PR10] [major] Acceptance edits bypass history -> filed D00 T01 §27
- [D00-T01-S21-PR11] [minor] No action owner beside approver -> filed D00 T01 §27
- [D00-T01-S21-PR12] [minor] Review date schedules nothing -> filed D00 T01 §27
- [D00-T01-S21-PR13] [minor] No authorization model recorded -> filed D00 T01 §28
- [D00-T01-S21-PR14] [minor] Accepted risk leaves no register -> filed D00 T01 §29
- [D00-T01-S21-PR15] [minor] Minimum review quorum unrecorded -> filed D00 T01 §28
- [D00-T01-S21-PR16] [minor] Bare partial names no next action -> rejected complete by design: a bare partial resolves under the item-1 rule with nothing owed, plan-health still lists it
- [D00-T01-S21-PR17] [minor] Partial records lack failure detail -> filed D00 T01 §28
- [D00-T01-S21-PR18] [minor] Legacy owner exception implicit -> filed D00 T01 §28
- [D00-T01-S21-PR19] [minor] Deferred date borrows review language -> filed D00 T01 §28
- [D00-T01-S21-PR20] [minor] Next-action precedence unspecified -> filed D00 T01 §28
- [D00-T01-S21-PR21] [minor] Summary needs a JSON form -> rejected plan-health --json is the machine contract over the same data; the summary is the human digest by design
- [D00-T01-S21-PR22] [major] Dates fire only when run -> filed D00 T01 §29
- [D00-T01-S21-PR23] [minor] Owner strings unvalidated -> filed D00 T01 §28
- [D00-T01-S21-PR24] [minor] No risk dashboard -> filed D00 T01 §29
- [D00-T01-S21-PR25] [minor] Section 25 assertions too coarse -> filed D00 T01 §25
End of ledger

Raw plan-review output (fenced, not part of the verdict scan; digest `6b9cc5b95b544625e1cf7a5335709319e84419309af1807fd9f843f0b5a31b69`):

```
- The grandfathered baseline is inconsistent between §19 and §21: §19 and §21 item 3 use 53 stamps/files in batches 13/7/27/6, while §21 item 5 claims 57 files with 17/7/27/6; reconcile the population and counting unit because migration completion and deadline gating cannot be audited against two totals.
- The “Grandfathered migration executes” item in §21 is checked after only planning batches and a future gate; create executable batch sections with dependencies, owners, per-batch verification, and completion stamps because governance machinery does not migrate the outstanding records.
- The grandfathered inventory in §21 claims a risk acceptance, but the new acceptance grammar only targets a finding ID, run ID, or outage and explicitly says grandfathered-only files cover nothing; add a batch/stamp target or replace the claim with an enforceable record because the stated acceptance currently has no machine-valid effect.
- Grandfathered work is counted by files in §21 item 5 but described as unmarked stamps in §19; define the canonical migration unit and handle multiple stamps per file because file-level completion can leave individual records unproven.
- The migration details are delegated to an unnamed “Current state” rather than stated or precisely referenced in §21; add an exact section reference containing batch membership and completion criteria because each TODO section is required to be executable without conversational context.
- `Risk accepted: outage <rung>` in §21 is not bound to a run ID, so repeated outages of the same rung can be covered ambiguously or accidentally; make outage targets compound run-and-rung identities because acceptance must attach to one known event.
- Allowing a whole run ID as a risk-acceptance target in §21 can waive several unrelated findings with one rationale; require an enumerated finding set and severity/scope limits because blanket run waivers obscure materially different risks.
- Risk acceptance coverage in §21 checks that the record is not post-dated but does not require the acceptance to follow the target finding, run, or outage; require target-created-at-or-before-record-date because a prewritten waiver cannot demonstrate informed acceptance.
- Risk acceptances in §21 are not bound to the reviewed candidate, finding revision, or risk version; add a candidate hash or immutable risk digest and invalidate acceptance on reopen or material change because an old decision must not silently cover changed evidence.
- Ledger history in §19 protects finding transitions but no connected section protects edits to an acceptance’s approver, rationale, dates, or scope; add append-only amendment and supersession validation because silently extending or rewriting a waiver destroys its audit value.
- The §21 acceptance shape names an approver but no owner responsible for review, renewal, or remediation; add an accountable action owner and escalation recipient because an expiring acceptance otherwise creates another ownerless obligation.
- The `review date` in §21 is validated only as lying between record date and expiry; surface review-due and review-overdue states with a recorded review outcome because the field currently schedules no enforceable action.
- §21 does not define who is authorized to accept which severity of risk or whether self-approval is allowed; add role-based approval thresholds and separation-of-duties rules because a syntactically valid identity is not governance approval.
- A live acceptance clears overdue status, escalation, and the gate in §21; keep accepted risks in a persistent, separately gatable register with residual severity and upcoming review/expiry because acceptance changes disposition rather than removing risk.
- §21 says a partial run with only the primary survivor owes no retry or accountability fields, while its purpose and checkpoint promise accountable recovery; define the minimum review quorum and why fallback loss is acceptable, or require a retry/acceptance, because reduced independent coverage is otherwise normalized silently.
- Bare partials are listed but deliberately do not fail `--check`, and a complete review alone receives no next action in §21; give every unresolved partial a recovery action or explicit accepted disposition because the operator summary can currently report incompleteness while recommending nothing.
- Partial markers in §21 identify only the failed rung and omit failure class, evidence, attempt count, and retry linkage; extend the record or link an immutable outage artifact because operators cannot diagnose recurring failures or prove that the debt was discharged.
- §19 says every open critical or major carries an owner and due date, yet grandfathered bare rows remain validator-legal and merely surface as UNACCOUNTABLE; state and time-bound the legacy exception explicitly because the normative rule and implemented behavior currently disagree.
- §19 reuses a deferred finding’s review date as its due date, while §21 gives review dates a separate acceptance meaning; introduce distinct `due` and `review_on` fields because conflating remediation deadlines with governance reviews produces misleading overdue reports.
- `query summary` in §21 promises “the exact failing gate” and one next action without defining precedence when several dimensions fail; specify a total priority order and list all blocking gates because a single arbitrary recommendation can hide a more severe blocker.
- The §21 summary is text-only even though plan-health has a versioned JSON contract; add a versioned summary JSON form because CI, dashboards, notifications, and premium operator tooling should not scrape prose.
- Deadline, expiry, and overdue enforcement occurs only when an operator runs the queries; add a scheduled CI check and owner notification path near §21’s summary work because unattended obligations can pass their dates without anyone seeing the failure.
- Owner and approver values appear to be unrestricted strings across §§19 and 21; validate them against a canonical owner registry with contact and backup escalation data because “operator” or a stale name is not reliably actionable.
- Add a premium health dashboard over §21’s existing structured data showing active accepted risks, partial runs, migration progress, upcoming reviews, expiries, and overdue owners because the current CLI summary is optimized for recovery but not proactive risk management.
- §25 verifies only unchanged aggregate self-test count and live validation after correcting two descriptions; add exact assertions for both rule-24 descriptions because count stability cannot prove that the documented four-leg contract remains synchronized.
```
