# D00 T02 §13 review: Backgrounding Leak on the Default Leg

Candidate: `83dff21c586d59a1171298f6fcddffc9a696901`.
Tier: Full panel (the diff changes the governed-run filter: the scheduled task, triage, and Run A evidence all consume it).

## Self-review

Three questions over `git diff 88b7365..83dff21` (3 files, 19 insertions, 17 deletions):

1. Makes sense: yes. Item 1 proves the 09-20 `primary=1` is the Primary set resting by design under the inclusive filter (plus pre-§11 funnel races), not a product leak; item 3 narrows the Run A filter to the proven population; items 2 and 9 strike honestly into §18 item 7; items 4/7/8 own three greens plus the manifest.
2. Logical: yes. `Category=Primary` post-dates the 09-19 proof (`3d7f658`, 2026-09-20); the census carries no timestamps so the trx-correlation method was corrected to geometry-plus-filter analysis; soak legs stay inclusive because they run ungated.
3. Breakage: none found. Syntax check 0 errors; validate 0 fatal; the only remaining old-filter quote is the deliberate soak line; morning-report counts read trx dynamically. No self-finds.

## GPT panel Round 1

Round 1 over candidate `83dff21` (Sol medium, run 20260920-D00-T02-S13-gpt, output check PASS).

- `adversarial` needs-attention (R1-F1: attempt 2 is no full green; its suite leg failed)
- `consistency` needs-attention (R1-F2: attempts 2-3 quoted as shorthand, not full gate lines)
- `integration` needs-attention (R1-F3: evidence runs used the mirror script, not `nightly.ps1`)
- `record` needs-attention (R1-F4: manifests missing for attempts 2-3)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

All four real; the fix is a nightly-driven attempt 5 (full green, full echo, true integration) plus full verdict quotes and reconstructed manifests for attempts 2-3. Disposition:

| ID | Disposition | Evidence |
| R1-F1 | fix pending | attempt 5 via `tools/nightly.ps1`; three full greens become 3-4-5 |
| R1-F2 | fix pending | item 7 quotes full verdicts from the three `gate.out` files |
| R1-F3 | fix pending | attempt 5 drives `Invoke-GatedLeg` through the fixed filter line |
| R1-F4 | fix pending | item 8 reconstructs attempt-2/3 manifests from build plus box facts |

Raw round-1 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:418` counts attempt 2 among three consecutive greens despite recording one failed test; `docs/testing.md:50` defines Run A green as zero test failures plus a clean gate.
**consistency: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:418` marks the evidence-row requirement complete, but attempts 2 and 3 provide only `primary=0` shorthand rather than the required quoted gate lines containing the complete verdict and census.
**integration: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:419` states that the candidate `tools/nightly.ps1` change was uncommitted and the runs used a mirror script, so the three claimed governed runs did not exercise the actual integration being reviewed.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:419` marks "Each governed run captures a manifest" complete but records a manifest only for attempt 4; attempts 2 and 3 have no quoted build, settings, topology, DPI, session, or harness manifests.
```

Fix loop 1 (`8464a52`): attempt 2 retired as non-green, items 7/8 rewritten around attempts 3-4-5 with full verdicts plus all three manifests, attempt 5 driven through the committed `tools/nightly.ps1`, run file noted.

## Aside A: subagent re-review (non-panel; the scripted panel below governs)

Round 2 over range `83dff21 8464a52`, every quote checked against file bodies (gate.out files read as Unicode, trx counters, transcript, morning report, `eca14cb..fa67055` diff verified docs-only, session plus SDK corroborated live).

- `adversarial` approve: attempts 3-4-5 each satisfy the Run A green definition (trx `total=165 executed=151 passed=151 failed=0`, gate exit 0); attempt-2 retirement honest (its trx reads `failed=1`, quarantine claim survives in §11); runs 3-4-5 consecutive (no intervening Run A dirs). R1-F1 holds.
- `consistency` approve: attempt-3/4 quotes byte-match gate.out; attempt-5 quote matches the transcript gate line including `overrun: False test-seconds: 487`; items 4/6/7/8 agree on the 3-4-5 roster. R1-F2 holds. Advisories: C1, `docs/testing.md:50` is off by one (definition at line 49); C2, item 4 quotes verdict cores without the constant `changes logged;` prefix (full lines in item 7).
- `integration` approve: attempt 5 transcript shows `tools/nightly.ps1` with the committed leg-label string, head `83dff21`, `Invoke-GatedLeg` line-188 format, trx `total=165` confirming Primary exclusion; `nightly: GREEN` follows deterministically. R1-F3 holds.
- `record` approve: item 8 build ids corroborate (attempt 3 from `eca14cb` code-identical to `fa67055`, attempt 4 from `31b0906`, attempt 5 from `83dff21`); session plus SDK plus DPI corroborated. R1-F4 holds. Advisories: R1, tag topology pixels as 150%-virtualized against the §10 4K box fact; R2, capture console tail for future nightly-driven proofs since `nightly: GREEN` is console-only.

No needs-attention findings; all four R1 dispositions verify against primary evidence. Advisories C1/C2/R1/R2 are non-blocking and folded below where cheap (C1 cite fix); R1/R2 are process notes for future runs, not §13 defects.

## Aside B: subagent sign-off attempt (non-panel; the scripted panel below governs)

Round 3 over range `83dff21 8464a52` (fix-loop-2 record corrections in `1edd8ff` land after sign-off as signed-off polish), verified first-hand against artifacts. SIGN-OFF with 6 advisories, none blocking:

- Filter change principled: old Run A asserted `primary=0` over a population containing tests whose contract is resting on primary (`MainWindowTests.cs:68`, `MultiWindowTests.cs:19`, both `InPlace`, never moved); exclusion is the only coherent resolution and cannot hide a product leak (gate still enumerates every HWND geometrically; only the 2 pairing-guarded Primary tests absent). Nondeterminism mechanism confirmed in code: `InPlace` 250 ms show vs 250 ms poll with the seen-twice rule, i.e. phase luck, explaining the 0,0,1,1 pattern. Run B byte-unchanged with same-day proof (`build/s11-runb-20260920-172225`, primary=1, 2/2).
- Attempts 3-4 legitimately count: population proof (taskrun trx `total=167` with 3 Primary-name hits; attempts 3/4/5 trx each `total=165` with 0 hits), honest mirror-vs-nightly labeling, adjacency holds, cross-tree variance strengthens the nondeterminism claim.
- Strikes land with a gap (§18 item 7 subsumes item 2's core but only partly item 9's per-path semantics): fixed by recording the narrowing as explicitly accepted (A5).
- Record misses fixed in `1edd8ff`: A1 trait-flip hash `67f2f21` (not `3d7f658`, which added only the gate); A2 100,100 line is the cascade first window `MenuNewWindowOpensSecondWindowAtCascade` (seed `MultiWindowTests.cs:27`); A3 checkpoint closure credits the §11 funnel plus the filter fix; A4 exact hwnd unidentifiable (close rests on re-verification) plus phase-race mechanism cited, sliver gloss dropped. Inherited R2-C2 (item-4 verdict prefixes) and R2-R2 (nightly GREEN as console tail with report corroboration) also folded there.

A6 (Run B count guard pinning the Primary set) filed in §15 (`520d6eb`), not §13 scope. Per the §12 R3 precedent, polish filed, not blocking: the fix is design-required, the three greens are artifact-solid with the committed line proven, and every Done-when is literally satisfied.

Fix loop 2 (`1edd8ff`): the seven record corrections above, self-verified by re-read (each cite checked against its file body before commit).

## GPT panel Round 2

Round 2 over candidate `88b7365..1edd8ff` scoped to `tools/nightly.ps1`, `docs/testing.md`, `todo/00-workspace/TODO-02-test-backbone.md` (Sol medium, run 20260920-D00-T02-S13-gpt-r2, output check PASS), with the round-1 verdicts appended.

- `adversarial` needs-attention (R2-F1: attempts 3-4 UI-only; only attempt 5 ran the full solution)
- `consistency` approve
- `integration` needs-attention (R2-F2: shared `run-a.trx` filename overwrites per-project results)
- `record` needs-attention (R2-F3: attempts 3-4 manifests reconstructed, not captured live)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

All three real-and-answered; none reopens the gate proof (the checkpoint requires the gate verdict, which all three runs satisfy over the full window-producing population). Disposition:

| ID | Disposition | Evidence |
| R2-F1 | fixed | fix commit states per-run scope in item 7 (3-4 UI-under-governed-gate, 5 full-solution with transcript/report quotes) plus the window-producing-population proof (Smoke/Unit spawn nothing, Protocol spawns only `AcpLoopback`, census ScratchPad-only per `Program.cs:111`) |
| R2-F2 | filed | script fix filed at §15 (per-project trx names, `D00-T02-S13-R2-F2`); fix commit states in item 7 that `run-a.trx` retains the UI leg |
| R2-F3 | fixed | fix commit ties each reconstructed field in item 8 to its checkable source (run-dir timestamps vs `git log`, code refs, live corroboration); runtime capture going forward is §15 PR9 |

Raw round-2 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:417` claims three consecutive governed Run A greens, but attempts 3 and 4 were UI-only mirror-script runs; only attempt 5 exercised the full-solution governed procedure defined at `docs/testing.md:49`.
**consistency: approve**
**integration: needs-attention** (1)
1. `tools/nightly.ps1:439` gives every project in `src/ScratchPad.slnx` the same `run-a.trx` filename, causing successive projects to overwrite that file; the resulting integration artifact retains only the final project's results and cannot substantiate the full-solution run cited at `todo/00-workspace/TODO-02-test-backbone.md:417`.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:418` marks per-run manifest capture complete, but explicitly reconstructs attempts 3 and 4 afterward from shared box facts rather than recording manifests captured alongside those runs.
```
