# Review: D00 T01 §11 -- Quote-End Lookahead Removal

## Evidence map

Round-1 candidate `3375f2f` carried all 3 items: lookahead deletion with §§25-26 (item 1), lazy-comment correction plus skill condition removal (item 2), the commit itself (item 3), with count 420. Round-2 candidate `f847d09` answered round 1 (§27 locking the forward window, substitute-clause qualification, label-dependence removal, count 421). Round-3 candidate `0aaab16` answered round 2 (blank-line rule with §28, §27 rename, fourth checklist item, count 422). Round-4 candidate `77016ec` answered round 3 (substitute-clause correction, blank clause in both docs). Round-5 candidate `ba9be18` answered round 4 (record-only: stale Done claims corrected, round-3 addendum, forward-window wording). Evidence for each fix is quoted under its panel record.

## Self-review

Sense: the deletion is the fix the round-5 §10 panel prescribed (laziness never applies to fenced content), and the two probe shapes from that round are now fixtures. Logic: one bare depth comparison replaces the whole-remainder scan; blanks end quoted fences via the same comparison while unquoted fences keep blank content. Breakage: none beyond the intended strictness (quoted fences must now be blank-free and close at the same depth; both live paneled findings files carry no quoted fences and re-probe passing). No self-review fixes; the panel found one real one (the blank rule) plus record corrections below.

## Opus panel (round 1, candidate `3375f2f`)

**adversarial: advisory**: a 2-line lookahead bound still passes the suite, so only large windows are locked; a blank line may end a quoted fence per CommonMark 5.1 (filed as out-of-scope, worth filing). First half fixed with §27; second half rebutted with a live probe, then overturned in round 2.

**consistency: approve**: comment, skill, and README state the same unconditional rule; stamped §10 text correctly left as record.

**integration: approve**: §§25-26 wiring complete; gates green; count updated; rows correctly open pre-stamp.

**record: needs-attention**: substitute claim false for small windows; NEW-13/14/15 labels unresolvable in the s10 file; re-asserted battery lines not re-run. Fixed: claim qualified, labels replaced with resolvable verdict refs, full battery re-measured.

## Opus panel (round 2, candidate `f847d09`)

**adversarial: needs-attention**: rebuttal overturned with CommonMark 0.31.2 section 5.1 plus example 228 and a reference-parser run: the blank line ends the quote and its fence, so quoted verdicts after a re-closed quoted fence are code content that satisfied the rule. Fixed: bare depth comparison with §28.

**consistency: needs-attention**: docs stated the unconditional rule while code exempted blanks, falsifying the item-2 Done claim. Fixed by the same deletion; claim re-verified.

**integration: approve**: §27 wiring complete; gates green; rows correctly open.

**record: needs-attention**: item-2 Done claim false; §27 title over-claimed; fail-open unfiled. Fixed: claim re-verified, case renamed, fourth checklist item added inside §11.

## Opus panel (round 3, candidate `0aaab16`)

**adversarial: approve**: discriminators re-measured (2-line bound, full lookahead, dropped early-close); reference parser agrees with §28; over-closing probes all fail-closed.

**consistency: advisory**: blank-ends-quote clause missing from both docs. Fixed in both.

**integration: approve**: §28 wiring complete; §27 rename applied everywhere; gates green.

**record: needs-attention**: substitute claim falsified again by §28 (2-line bound now fails two cases). Fixed with the measured pair.

## Opus panel (round 4, candidate `77016ec`)

**adversarial: approve**: code unchanged, discriminators re-measured and holding; no new attack shape.

**consistency: approve**: blank clause matches code comment and depth comparison; no stale wording remains.

**integration: approve**: no wiring touched; fixtures and gates unchanged and green.

**record: needs-attention**: item-1 Done carried two stale claims from before §28; "window-1" wording survived the rename in two lines; round 3 lacked a Done addendum. Fixed: claims corrected to the measured sets, wording unified, addendum added.

## Opus panel (round 5, candidate `ba9be18`, FINAL)

**adversarial: approve**: gates and all discriminators re-measured on the final candidate; working tree byte-clean after every probe; no new attack shape.

**consistency: approve**: docs, comment, and code agree; renamed case is the only wording in the tree; no em dashes in added lines.

**integration: approve**: fixtures, cases, rows, and gates all complete and green; no findings file yet, matching the §10 precedent.

**record: approve**: all four round-4 findings fixed and each corrected claim verified by independent measurement; no new or still-live issues.

## Gates re-run

`self-test` 422/422, live `validate` 0 fatal 0 warnings, `plan --check` current. Panel rounds used: 5 of 5 (all four lenses approve at close, zero needs-attention, zero advisories). No leftovers: nothing to file.
