# Review: D00 T01 §28 -- Partial Records and Governance Docs

Candidate: `5b43b08` + `f45c71d` + `915cdd6` + `e114b4a` + `565255f` + `2876201` (SHIP plus two panel fixes plus three filing commits; output `check-panel PASS` on all three headless rounds). Panel tier: Full (the diff touches `scripts/todo-graph.py`, `scripts/todo-validate.py`, plus the review skill, shared contract surface), Sol rounds 1-2, Opus round-3 sign-off.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. One self-review fix landed in the SHIP commit before pushing.

Does it make sense: six items take partial-record detail plus governance prose (quorum, marker failure detail, owner exception, deferred rename, next precedence, trust model) with the validator, query, fixture, skill, and README updates each owes. Exactly the contract, including the two validation corrections recorded on the items before building.

Is it logical: detail rides every degraded marker line chain-wide (history, not claims); the deferred triple unifies on `due`; the digest precedence matches the gate's own order; the trust default is stated with its cost. Bare partials carry detail but no accountability fields.

Did it break anything else: suite moves 812 to 822 by exactly the 10 new probes (4 helper units plus 6 marker-field cases); live validate stays 0 fatal; plan-health --check stays green. Blast radius is the marker grammar plus deferred-row paths, both covered.

Session fix: case-sensitivity alignment on the new field regexes (siblings `OWNER_RE`/`DUE_RE` are case-sensitive; the first draft used `IGNORECASE`), landed in SHIP before commit, no separate hash.

## GPT panel (round 1)

- `adversarial` needs-attention: (2) `attempts` prefix match accepts `2x`/`1.5`; deferred owner/trigger legs are bare substrings.
- `consistency` needs-attention: (1) bare partials exempt from `class`/`attempts` although a rung failed.
- `integration` advisory: (1) grandfathered `date` fallback reads the first date anywhere, including trigger text.
- `record` needs-attention: (1) three checklist boxes unchecked despite Done claims plus the commit message.

Fix record: whole-token `attempts` match; shaped deferred owner/trigger legs plus PR25 hollow-trigger probe; partial leg in the fields rule with §17/§37/§39/§40 carrying fields plus §74 probe; the three boxes ticked. The integration advisory stays noted (pre-sign-off advisory: the fallback preserves pre-cutoff parse behavior byte for byte, and changing grandfathered semantics is outside the contract). Fixed in `f45c71d`; gates re-ran green before round 2 (self-test 827/827, validate 0 fatal, plan current).

Raw round-1 output (fenced, not part of the verdict scan; digest `cf43f49b3598b17f4a6105bcaa9a0417a632fa8120007f646fc543bf08e6f1b8`):

```
**adversarial: needs-attention** (2)
1. `scripts/todo-validate.py:731` accepts numeric prefixes such as `attempts 2x` or `attempts 1.5`, so malformed attempt counts satisfy the positive-integer rule.
2. `scripts/todo-validate.py:1009` checks only for the substrings `owner` and `trigger`; a deferred row with empty or prose-only fields, such as `deferred owner due 2026-10-01 trigger`, passes without the required `owner <name>` and `trigger <t>` values.

**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:149` and `scripts/todo-validate.py:724` exempt primary-survivor `partial:` markers from `class` and `attempts`, leaving a recorded rung failure without the failure class and attempt count required by the partial-record contract.

**integration: advisory** (1)
1. `scripts/todo-graph.py:1329` treats the first date anywhere in a grandfathered deferred row as its due date instead of requiring the legacy `date <YYYY-MM-DD>` field, so dates embedded in trigger text can be reported as accountability dates.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:795`, `todo/00-workspace/TODO-01-repo-and-toolchain.md:797`, and `todo/00-workspace/TODO-01-repo-and-toolchain.md:800` remain unchecked despite carrying completed implementation claims and despite candidate commit `5b43b083e7f4ac48fe55e939a1e33aa922e579bb` having the required commit message.
```

## GPT panel (round 2)

- `adversarial` needs-attention: (1) `isdigit` accepts `²`, which `int()` rejects: crash, not flag.
- `consistency` needs-attention: (1) README scope sentence still limits the fields rule to outage/retry-owed lines.
- `integration` approve
- `record` approve

Fix record: ASCII-digits `fullmatch` plus §75 non-ASCII probe; README scope sentence fixed to include partials (skill plus rule-table prose swept to match). Sibling `isdigit`-then-`int()` sites (`review_prompt.py` run-id minting, `todo-graph.py` frontmatter parsing) filed in D00 T01 §34, outside this contract. Fixed in `915cdd6`; gates re-ran green before round 3 (self-test 829/829, validate 0 fatal, plan current).

Raw round-2 output (fenced, not part of the verdict scan; digest `6c6568d1ac8e4b604ff5f8623a3cfcbaff1366ed97abb7098556ef4bd788d6cc`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-validate.py:738` calls `int()` after `str.isdigit()`, but values such as `²` satisfy `isdigit()` and cannot be parsed by `int()`, so a malformed `attempts ²` marker crashes validation instead of producing the required FATAL finding.
**consistency: needs-attention** (1)
1. `todo/README.md:160` first says every partial carries `class` and `attempts`, then limits the normative requirement to “Every outage or retry-owed line,” contradicting the validator and `.claude/skills/review-todo-section/SKILL.md:148`, which require those fields on partial lines too.
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` advisory: (1) 4300+ digit `attempts` tokens crash `int()` past CPython's string limit.
- `consistency` approve
- `integration` approve
- `record` needs-attention: (1) AGENTS.md self-test count stale (812 vs 829).

Filing record: sign-off round, both below the blocking bar, so filed instead of re-rounding. The advisory filed as a D00 T01 §34 item (attempts length cap); the record finding is already tracked (D00 T01 §42 carries the AGENTS.md sync item), so nothing new filed. No fix commit.

Raw round-3 output (fenced, not part of the verdict scan; digest `f807add46499e24f869311f45dd4a47eedaa04d2a0694fb588e6ea43f9db8c4d`):

```
**adversarial: advisory** (1)
1. `scripts/todo-validate.py:741` still calls `int()` on the matched token after the ASCII-digit `fullmatch`; CPython's 4300-digit integer-string limit makes `int()` raise `ValueError` for a marker such as `attempts <4301 digits>`, so validation crashes instead of emitting the FATAL (verified: `int('1'*5000)` raises `ValueError: Exceeds the limit (4300 digits)`). Comparing the token's length or matching `[0-9]{1,9}` avoids the unguarded conversion.

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)
1. `AGENTS.md:55` still documents `self-test      # 812 cases, must stay green`; the candidate range raised the suite to 829 cases (`python3 scripts/todo-graph.py self-test` reports `829 cases, 0 failed`, and the §28 item text says `Suite 829/829`), so the documented gate count is stale by 17.
```

## Review notes

Governance section with shared-surface touch: six items across marker grammar, validator, fixtures, skill, and README. The panel's six corrections (whole-token attempts, shaped triple legs, partial detail, ticked boxes, ASCII digits, README scope) all landed before close; the sign-off advisory filed as tracked work (§34 attempts cap) and the sign-off record finding confirmed pre-tracked (§42 AGENTS.md sync). The round-1 integration advisory stays noted per the pre-sign-off rule. No panel leftovers. Architecture gate skipped silently (no editor state, storage, extensibility, protocol, or consent surface in the diff).

## Plan review

Round: `gpt-5.6-sol` high over §28 plus §21, 2026-09-19. Neighbors: §21 (Depends plus XREF neighbor: §28 filed from its plan review); dependents: none (no reverse Depends rows, no XREF consumers outside the filer; the D01 T01 §28 mentions are bare refs in another file). The run minted through the `run-id` genesis path (no prior claims, bare base). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (14 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §28, D00 T01 §21]; dependents [none]; bytes 14105; run 20260919-D00-T01-S28-gpt

Ledger (namespaced IDs per the §16 shape; 5 of 14 filed at §52 plus a §48 joiner, 8 rejected with reasons on their rows):

Ledger:

- [D00-T01-S28-PR1] [minor] Unstamped section at review time -> rejected plan review runs pre-stamp by design; the stamp commit lands the Verified block plus row flip
- [D00-T01-S28-PR2] [minor] Outage-note link undefined -> filed D00 T01 §52
- [D00-T01-S28-PR3] [minor] Attempt counting undefined -> filed D00 T01 §52
- [D00-T01-S28-PR4] [minor] Class vocabulary unbounded -> rejected open vocabulary is the recorded default: new failure modes must not break the grammar, and the record is grep-able with no aggregation contract; bounding serves automation §28 deferred
- [D00-T01-S28-PR5] [minor] Quorum depends on failed rung -> filed D00 T01 §52
- [D00-T01-S28-PR6] [minor] Bare partials accumulate silently -> filed D00 T01 §52
- [D00-T01-S28-PR7] [minor] Identity registry missing -> rejected single-operator default recorded with its cost of changing (registry plus thresholds); the registry is the stated alternative, not owed work
- [D00-T01-S28-PR8] [minor] New exception rows unblocked -> filed D00 T01 §48
- [D00-T01-S28-PR9] [minor] Date fallback never retires -> filed D00 T01 §52
- [D00-T01-S28-PR10] [minor] Bare section refs -> rejected fix window closed at sign-off (below-bar, no re-round) and shipped checklists are immutable; refs resolve via the adjacent suite citations
- [D00-T01-S28-PR11] [minor] Empty review dependents -> rejected dependents follow the mechanical rule (none found by Depends plus XREF lookup); compatibility is proven by the suite (829/829, exact-count locks on shared fixtures)
- [D00-T01-S28-PR12] [minor] Obsolete shape beside current -> rejected shipped text is history; the Superseded note is the canonical pointer and §27 plus skill plus README carry /4
- [D00-T01-S28-PR13] [minor] Checkpoint states no baseline -> rejected Live proof is the consolidated gate record by design; item counts are at-writing evidence read against the stamp's quoted 829/829
- [D00-T01-S28-PR14] [minor] One-rung survival looks complete -> rejected complete by design (PR16 precedent); the marker names the failed rung and plan-health lists bare partials, so survival is visible
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `93b3235f838445b88ff0cbb749d42aeb1c281f865bd0774039d30e6c1902221f`):

```
- Section 28 is fully checked but has no `Verified:`, review candidate, CRUD record, or duration; add its closeout record to §28 because the plan cannot distinguish implemented work from an approved section.
- Partial-record evidence is stored in a findings-file outage note without a stable link from the marker; define the link and integrity rule in §28 because `class` and `attempts` alone cannot identify the evidence supporting a failure.
- `attempts <n>` lacks counting semantics such as per rung versus per run, inclusion of the initial attempt, and reset behavior; define these in §28 because identical records can otherwise describe different retry histories.
- `class <class>` has no documented vocabulary or extension policy; add a bounded taxonomy to §28 because free-form classes prevent reliable grouping, reporting, and automation of recurring failures.
- A primary-survivor partial is treated as complete and carries no retry accountability, while a fallback-survivor partial owes retry; reconcile this rule between §§21 and 28 because minimum quorum and degraded-review status currently depend on which rung failed rather than the missing independent review.
- Bare partials may accumulate indefinitely without owner, due date, or escalation and never fail the summary gate; add a recurrence or age threshold to §21 because persistent loss of one review rung can become normal without corrective action.
- Owners and approvers are free-form, unvalidated labels under a single-operator trust model; add an identity registry or authenticated attribution milestone to §28 because arbitrary text cannot provide strong auditability or prevent self-approval.
- The grandfathered owner exception has a deadline but no explicit rule forbidding newly created exception rows; add creation-time enforcement to §28 because the migration population can otherwise grow until the cutoff.
- The deferred-row parser retains the legacy `date` spelling indefinitely despite zero live rows; add a removal date and compatibility test retirement to §28 because a permanent dual schema preserves ambiguity and maintenance cost.
- References such as `§4`, `§7`, and `§§70-75` do not identify their owning TODO or artifact; qualify them in §28 because the section is not executable from zero conversation context.
- Section 28 changes shared marker grammar, validation, query behavior, and governance prose, yet `REVIEW DEPENDENTS` is empty; add §§21 and 27 compatibility review coverage because previously verified accountability and acceptance behavior can regress under the new schema.
- Section 21 presents the obsolete plan-health/3 risk-acceptance shape alongside a superseding plan-health/4 note; move the current contract into one canonical statement and retain the old shape as clearly historical evidence because operators can implement or audit against the wrong schema.
- The final checkpoint in §28 states an outcome but names no consolidated commands or final suite count, while item-level results report both 827 and 829 cases; add one final integrated gate record because the reviewed candidate needs a single reproducible verification baseline.
- Calling one surviving advisory rung a complete quorum weakens independent review for high-impact governance changes; add a degraded status and escalation path in §28 because a premium workflow should visibly distinguish one-reviewer survival from full panel coverage.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `2876201`: `todo-graph self-test: 829 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `2876201`: `24 todos, 226 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `2876201`: `implementation plan is current -- 68/221 sections complete (31%)`, exit 0.

Provenance: candidate 2876201; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest ff92c356824b071a82f9d537133e41f5672cb6316e0779c99c91acf1bc8cbaf2; path docs/reviews/00-workspace/D00-T01-s28.md; run 20260919-D00-T01-S28-gpt
