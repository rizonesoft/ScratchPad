# Review: D00 T01 §1 -- Repo Layout and Toolchain Pin

Candidate: `a5daf42` (section commit) plus `52a4218` (review fix, mode-only). Final tree reviewed at `52a4218`.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. README pins | `README.md` Toolchain pins table: SDK 10.0.401, WinAppSDK 2.4.0, xunit.v3 4.0.1, runner 4.0.0, test SDK 18.10.0, all exact numbers verified 2026-09-13 |
| 2. Layout dirs | `src/.gitkeep`, `tests/.gitkeep`; matches the Layout table (app/suites land in §2/T02) |
| 3. gitignore | `bin/`, `obj/`, `dist/` added; `TestResults/`, `.tools/`, `.vs/`, `*.user`, `*.suo` already present |
| 4. gitattributes | LF for sh/ps1/md, CRLF for cs/xaml/csproj/slnx; fresh clones clean on Linux and Windows |
| 5. Provisioners | `tools/provision.sh` + `tools/provision.ps1` install SDK 10.0.401 into `.tools/dotnet` (SHA512-verified); both executed green |
| 6. Exact versions | `global.json` pins 10.0.401 (`rollForward: disable`); wipe plus re-provision reproduces it twice on Linux; lockfile timing clarified in the item |
| 7. Commit | `a5daf42`, subject matches the item plus the skill-mandated ref |

## Self-review

1. Does it make sense? Yes. Every item has its change, no widening: the xunit v3 default is recorded with its swap cost in `README.md`, the lockfile clarification is dated in the item, and the commit body quotes the checkpoint.
2. Is it logical? Yes, after the fix below. `global.json`, `README.md`, both scripts, and the installed SDKs agree on 10.0.401; `rollForward: disable` coheres with script-first bootstrap; both scripts verify before clobbering so a failed provision never destroys a working SDK.
3. Did it break anything else? No. `self-test` 391 cases 0 failed, `validate` 0 fatal, no consumers exist yet, and no tracked file matches the new ignore rules.

Finding S1 (blocking, fixed): `a5daf42` stored `tools/provision.sh` as 644 because `core.fileMode=false` on this 9p checkout swallowed `chmod +x`; a fresh clone could not run the documented `./tools/provision.sh` (proven `FRESH-NOT-X`). Fixed in `52a4218` via `update-index --chmod=+x`, blob `156528a` unchanged, fresh clone proven `FRESH2-IS-X`.

## Lenses

Round 1 on `a5daf42`:

- `adversarial`: approve. Failure exits are loud (`set -euo pipefail`, `ErrorActionPreference Stop`); hash mismatch aborts before the destination is touched; no secrets, no consent flow, no concurrency surface beyond implausible parallel provisions. Accepted: tarball and sidecar share one HTTPS server.
- `consistency`: needs-attention (1). F1 is the exec bit (same as S1). Markdown keeps one line per paragraph and item, no em dashes, naming follows the plan.
- `integration`: approve. The contracts §2, §3, §5, §6 need (pin location, provision invocation, exit codes, env vars) are documented in `README.md`; the CI path depends on the same exec-bit fix.
- `record`: approve. Every evidence claim quotes a real run; the commit subject matches item 7 plus the mandated ref; zero deferrals open or owed.
- `source-defect`: not owed (no Notepad behavior or protocol detail at stake).
- `design`: not owed (no UI surface).

Round 2 on `52a4218`: `consistency` re-run approve (fresh clone executable, script bytes identical); `record` re-verified. No open findings. Untested by choice, noted: the hash-mismatch exit path (two-line compare, not demanded by the checkpoint).

## Gates re-run after the last fix

`self-test` 391 cases 0 failed; `validate` 0 fatal; checkpoint evidence stands (script bytes unchanged since the green runs); `plan --check` green after the stamp-flow sync. The stale progress JSON seen mid-review was the expected derived-artifact lag from ticking items, resolved by that sync.
