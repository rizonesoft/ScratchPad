# Review: D00 T01 §22

Candidate: `1ce9aa1` (ship) + `527154c` (R1 fix) + `acf8888` (R2 fix) + `6da4e37` (R3 fix) + `99d8cde` (R4 fix). Full panel (scripts plus skill plus validator-adjacent parser: shared contract surface).

## Self-review

Sense: all 3 work items land as specified plus the commit. Each Done-when maps to code: the proof pointer with tip-tree resolution (item 1), deterministic chronology (item 2), and range tokens with ancestry plus range touch (item 3).

Logic: the validator, query, skill, and README agree on every new shape (proof token, fix ranges, ordering rule); fixture counts preserved where behavior kept (PR1 family still clears, PR17/PR24 stay for their documented legs, PR4/PR7/PR9 absence probes intact via PR60 numbering).

Blast radius: no live behavior change (zero filed criticals in the tree, so the new legs never consult live records); the Duration parser addition only fills a previously-None field. No findings; straight to panel.

## Live proof

Gates on `155653c` (code-identical to candidate tip `99d8cde`; the two later commits file §§30-32 plus joiners in TODO and plan only). Digests are sha256 over the quoted bytes, each line LF-terminated.

`python3 scripts/todo-graph.py self-test`:

```
todo-graph self-test: SKIP sibling stamp-gate clamp probe (no section_commit_gate.py)
todo-graph self-test: 715 cases, 0 failed
```

Provenance: candidate 155653c; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest 2c96056ee77898e9fd646458d786808c4aae1917a0ec0da85dbc3c5a8cdd0011; path docs/reviews/00-workspace/D00-T01-s22.md; run 20260918-D00-T01-S22-gpt

`python3 scripts/todo-graph.py validate`: `22 todos, 198 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate 155653c; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest c811776da96c26f7e0139c99e48f693cbfe4283ac87211bf20b8c743db24fbe5; path docs/reviews/00-workspace/D00-T01-s22.md; run 20260918-D00-T01-S22-gpt

`python3 scripts/todo-graph.py plan --check`: `implementation plan is current -- 60/193 sections complete (31%)`, exit 0.

Provenance: candidate 155653c; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 835a92ab8cf988f22e36897313c40f546dd214c3538dc3e0ff45c4f1f9fa684c; path docs/reviews/00-workspace/D00-T01-s22.md; run 20260918-D00-T01-S22-gpt

`python3 scripts/todo-graph.py query summary`:

```
incomplete runs     0
blocked clearances  0
overdue owners      0
next: nothing actionable
gate: ok
```

Provenance: candidate 155653c; command `python3 scripts/todo-graph.py query summary`; exit 0; tool CPython 3.12.3; digest a9f5ee96d76b39d16392c3a6ce78384c961c3bac00f2a3e613ef86bc2cdee342; path docs/reviews/00-workspace/D00-T01-s22.md; run 20260918-D00-T01-S22-gpt

`python3 scripts/todo-graph.py query plan-health --check`: `gate: ok`, exit 0.

Provenance: candidate 155653c; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 83e9a060459d5921fedae881c1e079a0169941dd7fcff74fabc57c3188d0943b; path docs/reviews/00-workspace/D00-T01-s22.md; run 20260918-D00-T01-S22-gpt

## Opus panel (round 1)

Candidate: `1ce9aa1` (checked `fence` pipeline; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` needs-attention
- `record` approve

Findings (all verified true, all fixed in `527154c`): adversarial the shipped day-only rule kept day granularity against the item's letter (Duration-end instant ordering with day fallback; PR68 clears same-day, PR69 locks the dateless fallback); consistency-1 stale PR40 comment from the renumber; consistency-2 `%cs` renders in the commit's offset, not UTC (unix epoch via `%ct`); integration base-excluded range semantics undocumented and uncovered (skill plus comment plus base-touched canary on §52). Fix: `527154c`.

Raw round-1 output (fenced, not part of the verdict scan; digest `4d2667d804a146e942704aa856c86e395d4af0a60271f9dde005b8c04997d8f3`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:2192` — `fix_day <= reviewer_day` compares two calendar days, so a critical filed by a review stamped day D and remediated the same day D can never clear: the fix commit is necessarily after the review yet reads equal, `provable` goes False, and the row stays an unresolved critical that votes the gate. This is the repo's normal loop shape (§21 was stamped and its filings worked the same day, 2026-09-18), and the only exits are an extra later-day commit named as the tip or a risk acceptance. Item 2's stated goal was to remove day granularity from clearance ordering; day granularity is still the comparison, now strictly rejecting rather than ambiguously accepting.

**consistency: needs-attention (2)**
1. `scripts/todo-graph.py:7093-7099` — the fixture profile comment names `PR40-42` (proof leg), `PR43-44` (recency), `PR45`, `PR46`, `PR47`, but the rows those profiles drive are `D90-T07-S4-PR60/61/62`, `PR63/64`, `PR65`, `PR66`, `PR67` (ledger `:6751-6758`, sections §§48-53 at `:6613-6702`). No `D90-T07-S4-PR40`..`PR47` row exists anywhere in the fixture, so the comment misdirects any later reader tracing a failing case.
2. `scripts/todo-graph.py:1176` and `todo/00-workspace/TODO-01-repo-and-toolchain.md:609` both claim the committer day is UTC ("Committer day of a commit (UTC, `%Y-%m-%d`)"; "reads the fix committer day (UTC)"), but `--pretty=%cs` at `scripts/todo-graph.py:1187` renders the committer date in the commit's own recorded offset. Verified against git: a commit with committer date `2026-09-18T01:30:00+05:00` prints `2026-09-18` while its UTC day is `2026-09-17`. The value is deterministic and immutable, but it is not UTC, so a fix committed just after local midnight east of UTC compares one day later than the record promises.

**integration: needs-attention (1)**
1. `.claude/skills/review-todo-section/SKILL.md:153` and `todo/00-workspace/TODO-01-repo-and-toolchain.md:610` specify `fix <base>..<tip>` without stating that `base` is excluded, and the same SKILL.md sentence uses `record..expiry (bounds inclusive)` for the acceptance window, so the one paragraph gives `..` both meanings. An operator who names the first fix commit as `base` (the natural reading of "the range tip and the range" and of this repo's candidate-range prose in `AGENTS.md`) loses that commit from `git_range_touches` (`scripts/todo-graph.py:2183`); when only that first commit touched the target file the clearance fails closed with no diagnostic distinguishing it from a genuinely untouched range. No fixture covers a base-only touch (§§51-53 cover touched, untouched, and forked only).

**record: approve**
```

## Opus panel (round 2)

Candidate: `1ce9aa1` + `527154c` (checked `fence` pipeline, R1 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` needs-attention
- `record` approve

Findings (all verified true, all fixed in `acf8888`): adversarial three new legs survive mutation (no tie, unprovable-ts, or fallback-fail fixture; ordering plus recency extracted as unit-tested helpers); consistency same-day-stay naming contradicts the same-day-clear (renamed pre-completion across fixture, row, and probe); integration Duration range plus proof tokens missing from README and the skill stamp list (both documented). Fix: `acf8888`.

Raw round-2 output (fenced, not part of the verdict scan; digest `7184b1d75a590949c29b3afd86afd6bb50b03a8ae917b8d503be78b52dace277`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:2175`, `:2226`, `:2237` — three new fail-closed legs have no fixture. Replacing `if tend <= rend:` (2175) with `if False:`, `if fix_ts is None:` (2226) with `if False:`, and `if fday <= reviewer_day:` (2237) with `if False:` each still yields `self-test: 707 cases, 0 failed`. No fixture carries a target whose `Duration` end is at or before its reviewer's (§54 at `:6760` is the only both-ends pair and it orders correctly), no fixture yields an unprovable committer timestamp, and every fixture reviewer (§2 `:6166`, §4 `:6190`) now carries a `Duration` end so `rts` is never None and the day fallback at 2237 never executes. Item 2's acceptance ("same-day fixes evaluate deterministically with fixtures") therefore has no negative fixture for the instant rule itself: a later change that drops the tie/inversion rejection, the unprovable-clock rejection, or the dateless fallback clears criticals silently with the suite green. By contrast `fix_ts <= rts` (2231), `git_is_ancestor` (2220), `git_range_touches` (2223), the stamp-day fallback (2178) and the proof leg (2253) all fail the suite under the same mutation.

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:7952`, `:6841`, `:6130`, `:6685` versus `todo/00-workspace/TODO-01-repo-and-toolchain.md:609` — the PR63 probe carries two contradictory names. The self-test case is `"clearance fails a same-day or pre-day fix"` (7952), the ledger row is `Same-day fix stays` (6841), the fixture row and heading are `Same-day fix target` (6130, 6685), but the section's own Done text calls it "pre-completion" (609), and what §49 actually exercises is a fix committed at `d4 11:00` against a review completing `d4 12:00` (`canned_ts` `:7218`, §4 `Duration` `:6190`): a fix that predates the review completion instant. As written, case 7952 asserts same-day fixes fail while case 7973 asserts same-day fixes clear, which reads as a direct contradiction to anyone tracing the shipped rule from the suite.

**integration: needs-attention (1)**
1. `todo/README.md:165` and `.claude/skills/review-todo-section/SKILL.md:153`, `:157` — the format spec was not updated for two now-load-bearing section tokens. README:165 still defines `Duration:` as "optional integer minutes from `Started:` to stamp", but the query's same-day ordering (`scripts/todo-graph.py:2175`) only fires on the `<start> to <end>` Zulu range shape parsed by `DURATION_END_RE` (`:109`), which README documents nowhere; SKILL.md:157 likewise lists only "plus `Duration:`" in the stamp block and SKILL.md:153 says "Ordering reads Duration ends" without ever giving the shape that produces one. README also documents no `proof <finding-id> <path>[::<test>]` token, though `PROOF_RE` (`:1105`) now fails a clearance closed when it is absent (`:2253`). An operator writing a target section from the canonical format spec writes minutes and no proof line, and their filed critical stays listed with no diagnostic naming the missing token.

**record: approve**
```

## Opus panel (round 3)

Candidate: prior two plus `acf8888` (checked `fence` pipeline, R1-R2 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` needs-attention
- `record` approve

Findings (all verified true, all fixed in `6da4e37`): adversarial calendar-invalid Duration ends crash the query at strptime (ends validate at parse, fail soft to None, day fallback applies); consistency stacked Duration fixture violates last-governs (parser resolves last-wins, fixture split to single-line sections); integration `_duration_by_ref` blind to ranges (ranges compute their minutes at parse, both forms project identically). Fix: `6da4e37`.

Raw round-3 output (fenced, not part of the verdict scan; digest `d00f6f009fcf6316d1cef670e4b60c9b6fe9ff3bed84f91ca07b5a8f15a71e05`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:2179-2186` — `rts = int(datetime.strptime(rend, "%Y-%m-%dT%H:%M:%SZ")...)` runs unguarded on any `Duration:` end that `DURATION_END_RE` (`:109`) accepted, and that regex only checks digit counts, so a calendar-invalid instant (`2026-09-31T13:57:32Z`, `...T25:00:00Z`) reaches `strptime` and raises. Nothing validates Duration bodies (no rule references `duration_minutes`/`duration_end` outside parse and query), so the bad value survives `validate`. Reproduced on a copy of this tree: setting §19's stamp to `> **Duration:** 2026-09-18T12:32:20Z to 2026-09-31T13:57:32Z` and adding one filed critical row to `docs/reviews/00-workspace/D00-T01-s19.md` makes `query plan-health` exit 1 with `ValueError: day is out of range for month` instead of failing that clearance closed; `query summary` and the `--check`/`--fail-on` gate ride the same path, so a single typo in a stamp takes down the governance gate rather than degrading it.

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:3867-3868` and `todo/README.md:149`, `:165` — the parse fixture §1 stacks two `Duration:` markers (`> **Duration:** 7` then `> **Duration:** 2026-01-01T10:00:00Z to 2026-01-01T10:07:00Z`), but README:149 states markers are "one per stamp (the last marker line governs, so these never stack)" and README:165 defines Duration as "either integer minutes ... or `<start> to <end>`". The parser at `:492-499` writes `duration_minutes` and `duration_end` from separate lines, so stacked markers silently merge instead of last-wins, and the only coverage for end-parsing beside minutes (`:4249-4254`) depends on a shape the format spec forbids.

**integration: needs-attention (1)**
1. `scripts/todo-graph.py:3113-3119` versus `todo/README.md:165` and `.claude/skills/review-todo-section/SKILL.md:157` — README and the skill now present the `<start> to <end>` range as a sanctioned Duration form (and clearance ordering requires it), but `_duration_by_ref` still reads `s.duration_minutes` only, which `DURATION_BODY_RE` never populates from a range. Verified live: `progress --json` reports `D00 T01 §15 29` (minutes form) and `None` for §16, §19, §20, §21, all of which record full Zulu ranges. Sections that adopt the form the spec now steers them to drop out of the duration projection with no diagnostic, even though the range determines the minutes exactly.

**record: approve**
```

## Opus panel (round 4)

Candidate: prior three plus `6da4e37` (checked `fence` pipeline, R1-R3 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings (all verified true, all fixed in `99d8cde`): adversarial inverted-range and range-then-unshaped guards untested (both locked by fixture, fail soft to None); consistency `build_progress` docstring still claiming minutes-only (corrected for computed minutes; live §16/§19/§20/§21 project from ranges). Fix: `99d8cde`.

Raw round-4 output (fenced, not part of the verdict scan; digest `25f89d023d002c6cda030f2424fe2c98b2c4feb4eabb16eff500c70e794329d9`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:519` and `:501` — two new fail-soft parse guards have no fixture; both survive mutation with the suite green (verified: `if end <= start:` → `if False:` and `target.duration_minutes = None` → `pass` each still yield `713 cases, 0 failed`). No fixture carries an inverted range, so a typo'd stamp (`> **Duration:** 2026-09-18T16:35:19Z to 2026-09-18T14:54:33Z`) would, after any regression at `:519`, store an end that predates its start plus a negative `duration_minutes` at `:522` that flows into `_duration_by_ref` (`:3136`) and the clearance ordering at `:2221`; and no fixture stacks a shaped range followed by an unshaped body (`> **Duration:** unclear`), the only case the minutes reset at `:501` decides, so last-wins for `duration_minutes` is unlocked. The TODO Done text claims both behaviors as R3 fixes (`todo/00-workspace/TODO-01-repo-and-toolchain.md:609`, "calendar-invalid or inverted ranges fail soft to None ... stacked Duration lines resolve last-wins"); the calendar-invalid half is locked by `:4348`, the inverted and minutes-reset halves are not.

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:3186` — `build_progress`'s docstring still states "Duration is present only when a stamp recorded integer minutes", contradicting the candidate's own change at `:521-522` (a range computes its minutes) and its self-test claim "range and minutes forms project identically" (`:4283-4290`). Verified live: `D00 T01 §16/§19/§20/§21` now project `36/85/51/100` from `<start> to <end>` stamps that record no integer minutes, so a reader trusting the docstring reads those four values as absent.

**integration: approve**

**record: approve**
```

## Opus panel (round 5)

Candidate: prior four plus `99d8cde` (checked `fence` pipeline, R1-R4 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` approve
- `integration` approve
- `record` approve

Findings: adversarial the proof-to-row ID comparison plus the missing-fix-token rejection have no negative fixtures (verified true by inspection against §48's unresolving siblings and the token-carrying targets; both survive mutation). The cap sends it out instead of a sixth round: filed D00 T01 §30, named in the stamp's `Review:` line. No fix commit.

Raw round-5 output (fenced, not part of the verdict scan; digest `b6e762d3fcc95d47a9840af6a105640bf0d6a35309152440434ff0705b2e38f1`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:2278` and `:2252` — two clearance legs have no negative fixture; both survive mutation with the suite green (verified: `if pm.group(1).lower() != lr.group(1).lower():` → `if False:` yields `715 cases, 0 failed`, and `if fm is None:` → `if fm is None and False:` also yields `715 cases, 0 failed`, without even raising `AttributeError` on `fm.group(1)` at `:2255`, proving no fixture reaches it). The proof-to-row binding is therefore unlocked: §48 (`:6803`) is the only fixture pairing a proof-less row with sibling proof lines, and both siblings are deliberately unresolvable (`tests/nope.py` absent from `fff0001`, `test_missing_name` absent from `_proof_bare` at `:7315`), so PR60 stays for lack of any resolving proof rather than for the ID mismatch. A regression dropping the ID comparison would let one resolving `proof` line in a target clear every filed critical filed to that target (e.g. §2 at `:6294` carries three resolving proof lines; a fourth critical with no proof of its own would clear), which is exactly the binding item 1 claims to enforce (`todo/00-workspace/TODO-01-repo-and-toolchain.md:609`, "the query fails closed on missing or unresolvable proof"). Likewise a regression letting a `fix`-less target clear passes the suite.

**consistency: approve**

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §22 plus §19 plus §30 plus transitive §17, 2026-09-18. Neighbors: §19 (the Depends plus XREF neighbor), §30 (review dependent plus XREF consumer), §17 (transitive hop past §19); dependents §19, §30 (§19 an XREF consumer, §30 the reverse dependent). Runner note: the skill's `codex exec` template ran verbatim (`-c model_reasoning_effort='"high"'`); exit 0. Output `check-plan PASS` (12 findings, one per line).

Manifest: sections [D00 T01 §22, D00 T01 §19, D00 T01 §30, D00 T01 §17]; dependents [D00 T01 §19, D00 T01 §30]; bytes 70182; run 20260918-D00-T01-S22-gpt

Ledger (all 12 filed across §§31-32 plus §23 and §30 joiners, none rejected):

Ledger:

- [D00-T01-S22-PR1] [major] No fix-before-attestation check -> filed D00 T01 §31
- [D00-T01-S22-PR2] [major] Attribution file-scoped, not section-scoped -> filed D00 T01 §31
- [D00-T01-S22-PR3] [major] Range touch undated against the finding -> filed D00 T01 §31
- [D00-T01-S22-PR4] [minor] Ranges admit merged side branches -> filed D00 T01 §31
- [D00-T01-S22-PR5] [minor] Duration instants untied to the stamp -> filed D00 T01 §32
- [D00-T01-S22-PR6] [minor] Malformed Durations fall back silently -> filed D00 T01 §32
- [D00-T01-S22-PR7] [major] Same-day clearance needs absent ranges -> filed D00 T01 §32
- [D00-T01-S22-PR8] [major] Chronology self-asserted, no causal bind -> filed D00 T01 §23
- [D00-T01-S22-PR9] [major] Proof is occurrence-only -> filed D00 T01 §31
- [D00-T01-S22-PR10] [minor] Git helpers never run for real -> filed D00 T01 §30
- [D00-T01-S22-PR11] [minor] Clearance failures name no leg -> filed D00 T01 §32
- [D00-T01-S22-PR12] [minor] Sub-minute ranges project zero -> filed D00 T01 §32
End of ledger

Raw plan-review output (fenced, not part of the verdict scan; digest `06db34858720d5e44017a21471c890b9bf359e6770f395547dea4150f5a93c93`):

```
- Clearance chronology lacks a fix-before-target-review check; §22 item 2 should require the fix timestamp to precede the target review end, with a day fallback, because a review can currently attest to a candidate that did not yet exist.
- Fix attribution searches the entire TODO file at the tip rather than the resolved target section; §22 item 3 should parse that historical section and require its backlink there, because an ID and unrelated touch in another section can satisfy the immutable-candidate claim.
- Range recency is checked only at the tip; §22 items 2 and 3 should require a qualifying target-file touch after the finding review, because an old pre-finding touch plus an unrelated later tip can currently clear the finding.
- `base..tip` is described as a linear fix loop but allows merged side-branch commits; §22 item 3 should enforce the intended ancestry or first-parent topology, because an unrelated branch touch can satisfy `git_range_touches`.
- Duration ranges are not tied to `Started:` or `Verified:`; §22 item 2 and the validator should require matching start and stamp dates, because arbitrary but well-shaped instants can manufacture clearance ordering.
- Invalid, inverted, or unshaped Duration values silently fall back to day ordering; §22 item 2 and `validate` should emit a blocking diagnostic for new records, because a typo can weaken the promised deterministic clock without being visible.
- Duration ranges remain optional even when same-day critical clearance depends on them; the review skill and format spec should require ranges for finding reviews and clearing target reviews, because otherwise the normal same-day fix loop remains impossible despite valid evidence.
- Committer timestamps and editable Duration markers provide deterministic but self-asserted chronology; §22 item 2 should bind the finding review’s recorded candidate commit to the fix by ancestry, using §23 provenance, because wall-clock fields alone do not prove causal ordering.
- Proof binding establishes only that a path and optional name occur in tip bytes; §22 item 1 should bind the pointer to the exact live-proof command, result, digest, and candidate, because stale, commented-out, or unexecuted tests can currently support clearance.
- The new Git range, timestamp, and proof helpers are covered through canned replacements rather than a real temporary repository; §22’s fixture contract should add end-to-end commits, branches, merges, timestamps, and proof files, because command-level regressions can escape the self-test.
- Clearance failures collapse into an unresolved row without naming the failed leg; §22’s text and JSON queries should report proof, chronology, ancestry, touch, or resolution failure codes, because premium operator workflows need actionable remediation rather than forensic reconstruction.
- Positive sub-minute Duration ranges project as zero minutes through floor division; the Duration parser and progress contract should preserve seconds or round explicitly, because premium reporting should not represent completed work as zero-duration.
```
