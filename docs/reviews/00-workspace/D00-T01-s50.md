# Review: D00 T01 §50 -- Outage Instance Dating

Tier: Full (the diff touches shared contract surface: `scripts/todo-graph.py`, `scripts/todo-validate.py`, the review skill, and `todo/README.md`). Candidates: `ff3cd86` (ship), `bbbf1e4` (fix-loop 1), `66c7e8e` (fix-loop 2), `7b88f43` (fix-loop 3), `9234c7a` (plan-review filing).

## Self-review

Q1 (exact outcome, no widening): item 1 maps to the validator event leg (`scripts/todo-validate.py`, chain-wide outage-line check reusing `graph.EVENT_RE`) plus the skill and README shape sentences plus fixtures §§100-103; item 2 maps to `marker_event_day` plus the three re-keyed consumption sites (marker loop match and target-before-record, reviews-leg match and target day, register residual join) plus clean-tree probes §§12-14; item 3 matches the commit subject verbatim (`workspace: date outage instances per §27 panel`, `ff3cd86`). Count updates (register 16 to 19, notify 10 to 13 and 8 to 11, dashboard, digest tally, reviews list) are forced by the new probes, not widening.

Q2 (logic): validator and query read the event token through one compiled pattern (`graph.EVENT_RE`, start-or-whitespace boundary), so no validator/query split is expressible; an undated marker keys `(rung, "")`, which no shaped waiver target matches, and target-before-record reports `missing-target` on the empty day, so every failure direction stays loud. First-match, case-sensitive, whole-token reading matches the `owner`/`due`/`attempts` idiom; chain-wide dating follows the §28 detail-is-record precedent. No finding; fixed nothing before the lenses.

Q3 (blast radius): `_join_debt` is nested with two call sites, both updated; `stamp_day` and `_pday` stay live for the finding-kind fallback; the live tree carries no outage markers and no outage waivers (verified pre-change), so the re-key moves fixtures only. A scratch end-to-end probe outside the fixtures (two-outage chain plus old-event waiver) validates with no marker fire and reports `no-waiver` with an `unmatched` residual. Skill-sibling sweep for the old stamp-key wording returns no site outside the edited review skill.

## GPT panel (round 1)

- `adversarial` needs-attention
- `consistency` advisory
- `integration` approve
- `record` approve

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 62s; outcome needs-attention; tokens 32894

Fix record: R1-F1 (hyphen-boundary `no-event` satisfies the leg) fixed by the strict left boundary plus the §103 pin; R1-F2 (skill/README overclaim same-day keying) fixed by scoping both sentences to different-day pairs with the residual named. Fixed in `bbbf1e4`; gates re-ran green before round 2 (self-test 1197/1197, validate 0 fatal).

Raw round-1 output (fenced, not part of the verdict scan; digest `34f838d330e098f1902a96fa884359d7acf3a7f88ce561901247b1d39580f96c`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1266` and `scripts/todo-validate.py:802` use `\bevent\s+`, which accepts non-fields such as `no-event 2026-09-19`; an outage can therefore pass validation and bind a waiver key without the required `event <YYYY-MM-DD>` token.

**consistency: advisory** (1)
1. `.claude/skills/review-todo-section/SKILL.md:166` and `todo/README.md:163` claim that two same-rung outages under one stamp always key differently, but `(rung, event day)` still collides for two outages on the same day, contradicting the residual explicitly recorded in `todo/00-workspace/TODO-01-repo-and-toolchain.md:1286`.

**integration: approve**

**record: approve**
```

## GPT panel (round 2)

- `adversarial` needs-attention
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 36s; outcome needs-attention; tokens 17455

Fix record: R2-F1 (slash-joined `no/event` still satisfies `(?<![\w-])`) is an R1-F1 variant: fixed by the start-or-whitespace boundary `(?<!\S)` on both readers with §103 extended to both joined forms. Circuit-breaker sweep quoted: exactly two event-token readers (`scripts/todo-graph.py:1271`, `scripts/todo-validate.py:806`), both strict; the pre-existing `\b` owner/due/class/attempts idiom stays out of scope with validator/query agreement noted. Fixed in `66c7e8e`; gates re-ran green before round 3 (self-test 1197/1197, validate 0 fatal); boundary matrix old `[F,T,T,T]` new `[F,F,T,T]` over no-event, no/event, spaced, and start-of-string probes.

Raw round-2 output (fenced, not part of the verdict scan; digest `4d7e7ca3270838c01e81593afb63b35ee530a3e593fb0caec7b656892ad38854`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1269` and `scripts/todo-validate.py:805` still accept punctuation-joined prose such as `no/event 2026-09-19` as the required field because `(?<![\w-])` permits `/`; require start-or-whitespace before the exact `event <YYYY-MM-DD>` token.
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` advisory
- `integration` approve
- `record` approve

Telemetry: round 3; model opus; effort medium; duration 72s; outcome advisory; tokens unknown

Fix record: sign-off round, no re-round. R3-F1 (validator comment still unscoped) and R3-F2 (validator inlines the pattern instead of reusing `graph.EVENT_RE`) are sign-off advisories fixed without re-rounding under the §29 stamp-fixed precedent: comment scoping plus pattern reuse are behavior-identical (suite re-run green), so no lens verdict rested on them. Fixed in `7b88f43`. Panel closed.

Raw round-3 output (fenced, not part of the verdict scan; digest `a7f1f68343ebfefbd3d095c08a0ba3698e34c351c9f87fd6de593fbd338d5796`):

```
**adversarial: approve**

**consistency: advisory** (2)
1. `scripts/todo-validate.py:794` still carries the unscoped invariant "two outages of one rung under one stamp key differently", the exact claim scoped to different-day pairs at `.claude/skills/review-todo-section/SKILL.md:166` and `todo/README.md:163` (R1-F2); it is the only remaining site of that family (`scripts/todo-graph.py:1224-1225`, `1258`, `3753`, `3833` and `todo/00-workspace/TODO-01-repo-and-toolchain.md:1284,1288` all read event-day-scoped).
2. `scripts/todo-validate.py:806` inlines the event-token pattern as a string literal instead of reusing `graph.EVENT_RE` (`scripts/todo-graph.py:1271`), while the same leg reaches into the graph module for its other shared parts (`graph.datetime` at `scripts/todo-validate.py:811`, `graph.is_outage_marker` at `scripts/todo-validate.py:642`); the duplicated literal is what forced the identical two-site edit in both fix loops and leaves the two readers free to drift.

**integration: approve**

**record: approve**
```

## Disposition

| ID | Disposition | Evidence |
| R1-F1 | fixed | bbbf1e4 |
| R1-F2 | fixed | bbbf1e4 |
| R2-F1 | fixed | 66c7e8e |
| R3-F1 | fixed | 7b88f43 |
| R3-F2 | fixed | 7b88f43 |

Arch trigger: none - validator/query/fixtures/skill prose only; no editor state, storage, extensibility, AI, protocol, or consent surface in the diff.

## Plan review

Round: `gpt-5.6-sol` high over §50 plus §27, 2026-09-20. Neighbors: §27 (Depends plus XREF both directions: §50 filed from its panel round 6 and re-keys its instance binding); dependents: none (no reverse dependents and no XREF-only consumers; the transitive hop adds nothing). Output `check-plan PASS` (13 findings, one per line). One filed (PR6, marker append-only joiner on §51, committed as `9234c7a`); twelve rejected with reasons below. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §50, D00 T01 §27]; dependents [none]; bytes 13410; run 20260920-D00-T01-S50-gpt

Ledger:
- [D00-T01-S50-PR1] [minor] Same-day collision wants incident ID -> rejected recorded §50 residual stands; same-day same-rung rerun stays pathological and reopening costs a shape migration for no reachable scenario
- [D00-T01-S50-PR2] [minor] §27 text still says stamp date -> rejected stamped sections are shipment records not live spec; skill README code agree on event day with the §50 trail
- [D00-T01-S50-PR3] [minor] Event-date onset-vs-detection undefined -> rejected day the outage happened plus one-round-one-event determines the value; no reachable mis-assignment shown
- [D00-T01-S50-PR4] [minor] No timezone rule -> rejected event day never meets the UTC clock; equality plus same-author ordering are timezone-free
- [D00-T01-S50-PR5] [major] Backdated or future event dates -> rejected single-operator trust model with writer-attested dates; future events self-void via the record-date gate and backdating is unprovable without an independent event clock the run-less marker cannot carry
- [D00-T01-S50-PR6] [major] Markers lack append-only protection -> filed §51
- [D00-T01-S50-PR7] [minor] Duplicate same-key markers unspecified -> rejected semantics defined as last-governs in-chain with per-section consultation and set-union across sections; same-chain same-key is the recorded same-day pathological residual
- [D00-T01-S50-PR8] [minor] No shared identity constructor -> rejected factually wrong: marker_event_day is the single named constructor at all three sites, pinned by §§12-14 plus §§100-103
- [D00-T01-S50-PR9] [minor] Register silently substitutes empty day -> rejected validator FATALs are the loud signal and queries fail closed by architecture; unmatched plus persisting escalation plus FATAL is the designed triple
- [D00-T01-S50-PR10] [minor] No migration checks for persisted consumers -> rejected no persisted outage instruments exist and no schema shape changed; all surfaces compute live
- [D00-T01-S50-PR11] [minor] Section checked off without stamp -> rejected plan review precedes the stamp by procedure; stamp Review Duration land in the stamp commit
- [D00-T01-S50-PR12] [minor] Checkpoint should test more paths -> rejected each sub-case already covered or recorded-out or gate-covered or owned by the PR6 filing
- [D00-T01-S50-PR13] [minor] Incident-lifecycle premium section -> rejected no concrete contract proposed; shipped fields already carry timeline basics and re-filing waits on a naming consumer
End of ledger

Rejection detail: PR1 re-argues the recorded residual without new evidence. PR2 mistakes the §27 shipment record for the live contract. PR3 asks to elaborate an already-determined value. PR4 wants a clock rule for a value that never meets the clock. PR5 needs a multi-party trust model the tree does not have. PR7 asks to specify already-defined join semantics. PR8 describes code that shipped (single constructor plus pins). PR9 mistakes designed fail-closed layering for silence. PR10 wants compat checks for nonexistent persisted data. PR11 mistakes review-in-progress state for final state. PR12 bundles covered, recorded-out, and PR6-owned paths. PR13 proposes invention without a consumer.

Raw plan-review output (fenced, not part of any scan; digest `8531aafa1bdaa614c1179992e39a6d71caf928acaa660559b2a6ad4bb2a64c5f`):

```
- The event key is still not unique for same-day, same-rung outages; D00 T01 §50 items 1–2 should use an immutable incident ID or timestamp because an earlier waiver can still cover a distinct later outage.
- D00 T01 §27 still specifies matching and ordering by the outage marker’s stamp date, while §50 changes both to event date; §50 should add an explicit superseding correction to §27 because the canonical rules currently disagree.
- “Event date” is undefined as onset, detection, failed attempt, or record time; define it in §50’s grammar and README work because operators can otherwise assign different identities to the same incident.
- The event calendar has no timezone rule; §50 should require UTC, consistent with §27’s review calendar, because outages near midnight can acquire different keys across environments.
- A merely valid calendar date can be backdated or future-dated; §50 should validate event chronology and provenance because a marker added later could make a prewritten acceptance appear temporally valid.
- Outage marker identity is not protected as append-only or immutable; §50 or §27’s acceptance-integrity rules should detect changed or deleted event keys because edits can silently retarget coverage and corrupt audit history.
- Duplicate outage markers with the same rung and event key have no specified outcome; §50 should reject duplicates or define aggregation because coverage, residual joins, and counts may otherwise disagree.
- The three consumers lack one named shared identity constructor; §50 should centralize and test key construction for covering, reviews, and risk-register joins because independent parsing invites future drift.
- The risk-register join silently substitutes an empty event day for undated markers; §50 should surface a fatal diagnostic instead because silent “unmatched” results conceal malformed governance data.
- Migration testing covers existing fixtures but not persisted JSON, dashboards, exports, or automation consumers of the old stamp-day meaning; §50 should own compatibility and schema-version checks because downstream reports may silently reinterpret historical targets.
- §50 is fully checked off but has no `Verified:` stamp, review result, candidate hash, or duration; its closeout should supply those records because the plan cannot prove the implementation was independently accepted.
- The checkpoint proves only different-day separation and earlier-waiver rejection; §50 should also test same-day collisions, UTC boundaries, future and backdated events, duplicates, edited markers, and all-consumer parity because these are the remaining failure paths.
- Outages have only a rung and date, with no onset, resolution, severity, source, or linked run; a follow-up incident-lifecycle section would enable premium audit timelines, actionable dashboards, and meaningful reliability analytics.
```

## Live proof

`py scripts/todo-graph.py self-test` on `7b88f43`: `todo-graph self-test: 1197 cases, 0 failed`, exit 0.

`py scripts/todo-graph.py validate` on `7b88f43`: `28 todos, 252 sections -- 0 fatal, 0 warning(s)`, exit 0.

`py scripts/todo-graph.py plan --check` on `7b88f43`: `implementation plan is current -- 91/247 sections complete (37%)`, exit 0.

Digests cover each quoted line's UTF-8 bytes without trailing newline.

Provenance: candidate 7b88f43d379470f9f91b5dbda32e835a0941c0a0; command py scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.3; digest 9e50a420c0129e3b9c9f21f6336a3a20d935d2155247381a4642b3bcb06dc45a; path scripts/todo-graph.py; run 20260920-D00-T01-S50-gpt

Provenance: candidate 7b88f43d379470f9f91b5dbda32e835a0941c0a0; command py scripts/todo-graph.py validate; exit 0; tool CPython 3.14.3; digest 70ad83f5d9c2932d840d9d2f7baee79c98e1861a8bdf36b724b24d99fa4ea70a; path scripts/todo-graph.py; run 20260920-D00-T01-S50-gpt

Provenance: candidate 7b88f43d379470f9f91b5dbda32e835a0941c0a0; command py scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.3; digest b1e0c90b1c8210e23c6ee8a64b252767311716ff7bd3f70d56b1253a6ba38536; path scripts/todo-graph.py; run 20260920-D00-T01-S50-gpt

## Review notes

Full panel over five commits (ship plus three fix loops plus the plan-review filing): Sol rounds 1-2 found the boundary family (fixed, pinned, swept), Opus round 3 signed off with two advisories (stamp-fixed, behavior-identical). Plan review filed one joiner (§51 marker append-only) and rejected twelve with recorded reasons. No panel leftovers. Architecture gate untriggered with the `none` line recorded.
