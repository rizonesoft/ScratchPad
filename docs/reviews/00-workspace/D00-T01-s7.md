# Review: D00 T01 §7 -- TODO Graph Checks in CI

Candidate: `ba504d2` (gate) plus checkpoint evidence commits `d51bbf5`/`7adbd7c` (self-test probe/revert), `d18932b`/`bbaf5c6` (validate probe/revert), `f18b59d`/`7c2b739` (projection probe/revert), `294ec5e` (docs-only skip proof). No fixes; one round.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Self-test in CI | `.github/workflows/plan.yml` runs it on pushes touching `scripts/`, `todo/`, or itself; run `34793534551` green with `391 cases, 0 failed` in the log; probe run `34793587854` red at the Self-test step, reverted, green `34793663339` |
| 2. Validate in CI | Probe run `34793710261` red at the Validate step with both FATALs (orphaned-items, missing-stamp) named in the log; reverted immediately; green `34793744245` |
| 3. Projection check | `plan --sync` plus clean-tree check per the item's primary text; probe run `34793805213` red at the Check-plan-projection step (`D00 T02 §1: plan says [x], graph says [ ]`, `stale plan projection:`); reverted; green `34793846789` |
| 4. Paths filter | Docs-only push `294ec5e` produced no plan-gates run (empty run list for the SHA) while build `34793893849` ran |
| 5. Commit | `ba504d2`, subject matches the item exactly |

Runs: `34793534551` green, `34793587854` red (self-test), `34793663339` green, `34793710261` red (validate), `34793744245` green, `34793805213` red (projection), `34793846789` green. Every probe reproduced red locally before pushing. `main` is green at HEAD.

## Self-review

1. Does it make sense? Yes. Every item has its change; a separate workflow is the literal reading of item 4 (path filters are per-workflow); the workflow's own path is in the filter so gate edits gate themselves; ubuntu-only is justified (stdlib-only tool, OS-neutral, dual-OS would double cost for zero signal).
2. Is it logical? Yes. Step order self-test, validate, sync-check gives clean probe attribution: each probe failed exactly its step with later steps skipped; local rehearsal matched CI for all three probes; sync plus clean-tree check implements the item's primary text.
3. Did it break anything else? No. New workflow only, `build.yml` untouched; `validate` 0 fatal; `self-test` 391/391; the docs-only build run stayed green; nothing else reads the new file.

## Lenses

- `adversarial`: approve. All probes reverted immediately; the stale-projection failure prints the diff stat (plan files carry no secrets); the paths filter cannot be dodged since any `scripts/` or `todo/` change triggers.
- `consistency`: approve. The workflow mirrors `build.yml` (pinned checkout SHA, ubuntu-24.04, branches main); Markdown one line per item; no em dashes.
- `integration`: approve. Consumes the `todo-graph.py` subcommands as documented in `AGENTS.md`; the `docs/build.md` CI note is accurate (proven by the skip itself).
- `record`: approve. Seven run IDs quoted with step-level attribution; local reproductions quoted; all probes reverted; zero deferrals.
- `source-defect`: not owed (no Notepad behavior or protocol detail).
- `design`: not owed (no UI surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; checkpoint probes red-then-green in CI on the exact pushed commits; `plan --check` green after the stamp-flow sync.
