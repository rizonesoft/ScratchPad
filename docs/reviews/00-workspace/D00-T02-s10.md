# D00 T02 §10 findings

Full panel (scripts plus skills surface; soft cap 3, hard cap 5). Candidate: `41d0567` plus self-review fix `7d0c501`.

## Self-review

Three questions over the diff (`41d0567`, 14 files) plus blast radius. Four defects fixed in `7d0c501` (CollectDebt fail-safe plus else-guard, red-entry staging text, repair wrap, redundant fetch); two accepted with notes (Night-collected skip count reuses the report cell §15 hardens; task re-register proven by probe roundtrip, never fired live). T02 Current-state staleness (says open §§8-12) is adjacent pre-existing, noted not fixed.

Evidence (quoted): self-test `1184 cases, 0 failed`; `query night-debt` lists N1 open only (`count 28 filter Interactive age 1n`); dry run quotes filter plus log path exit 0, unknown id exit 2; green append plus skip paths plus staged stubs on a temp T02 copy; provision `-Verify` 9/9 green, fresh-clone repair to 9/9, miswired plus missing hook repairs green with byte-identical restore; PSParser 0 errors on all three scripts; validate 0 fatal, plan current 90/247.

## Evidence (quoted)

TBD at stamp (Live proof with Provenance).

## GPT panel (round 1)

Sol rung, 135s, session `01a0bd42-c935-7e21-b219-aca5446bdd01` (check-panel PASS).

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

| ID | Disposition | Evidence |
|----|-------------|----------|
| R1-F1 (provision restores user-deleted hook) | defended, superseded by R2-F1 | plan authorization plus loud report plus reversibility argued; R2 re-filed, fixed by split repair |
| R1-F2 (unlocked read-modify-replace) | fixed | `f5dc41e` (`Add-CollectedLine` re-reads before replacing: one retry, then `skip: file changed under write, triage appends`; append re-proof green) |
| R1-F3 (XML BOM invalid) | defended + hygiene | invalid as stated: XML allows BOM, `[xml]` parse plus `Register-ScheduledTask` proven by probe roundtrip (True/True); BOMs stripped anyway, timers re-verified OK in `f5dc41e` |
| R1-F4 (narrower debts never close) | fixed | `f5dc41e` (`Test-DebtCoverage`: exact closes, `&`-only Interactive subset on a full-green run stages for triage close, `\|`/`!` exact-only; docs; 7/7 truth table) |
| R1-F5 (no census check) | fixed | `f5dc41e` (`Test-DebtCensus`: totals equal owed, else `census mismatch` reds; 4/4 truth table) |
| R1-F6 (commit row unchecked) | fixed | `f5dc41e` (item-12 tick rides the fix-loop commit) |

## GPT panel (round 2)

Sol rung, 215s (check-panel PASS). Extraction note: the codex output echoes the prompt plus a duplicate verdict block; the checkable answer is the first `**adversarial:` after the last `^codex$` line, stdin via cmd `<` redirect (PowerShell pipes corrupt the bytes).

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

| ID | Disposition | Evidence |
|----|-------------|----------|
| R2-F1 (`git checkout --` survives) | fixed | split repair: re-wire only when unset or pointing at nothing, existing-other-dir left alone, missing file reports the exact restore; bogus rewires to 9/9, `.git` left alone at 8/9, missing file un-restored with the command quoted |
| R2-F2 (residual TOCTOU; collected-on-skip) | defended + fixed | TOCTOU defended: retry closed the human-scale race, the microsecond check-act window is uncloseable in user space (repo precedent: plan `--sync`), single-writer plus run mutex bound it, readback guards the copy count; collected-on-skip fixed by `Format-DebtGreenEntry` (appended/already-closed/skip-red branches, 3/3 proven) |
| R2-F3 (UTF-16 decl vs UTF-8 bytes) | fixed | both XMLs rewritten UTF-16LE with BOM to match the declaration; timers re-verified OK, probe register plus remove roundtrip True/True |
| R2-F4 (no subset counts) | fixed | `Get-TrxSubsetCounts`: FQN/Name-attributable subsets close with their own counts plus census; red subsets stage, mismatch reds, non-attributable stages; 5/5 subset truth rows on crafted trx |
| R2-F5 (substring Category match) | fixed | clause-aware `&` split with exact `Category=Interactive` clause match; trap filter uncovered, paren case superset |
| R2-F6 (record IDs/dispositions) | fixed | findings tables renamed to round-scoped `R1-Fn`/`R2-Fn` with fix commits cited, per the §9 precedent; `defended` kept (same precedent) |

## Opus panel (round 3, sign-off)

Sign-off, 21s (check-panel PASS). Panel closed.

`adversarial` approve
`consistency` approve
`integration` approve
`record` approve

## Plan review

Round: `gpt-5.6-sol` high over §10 plus 4 corpus sections, 2026-09-20. Neighbors: D00 T02 §8 (Depends), §5/§9 plus D00 T05 §1 (XREFs and boundary); dependents: none (no section Depends on §10 yet). Output check-plan PASS (4 findings, one per line). An earlier 9-finding attempt went VOID on a wrapped finding line (shape failure, verdicts unrecorded); the full-context re-prompt delivered the 4 below. One critical, corrected in-loop before stamp (open section), so no open critical reaches the stamp.

Manifest: sections [D00 T02 §5, D00 T02 §8, D00 T02 §9, D00 T02 §10, D00 T05 §1]; dependents []; bytes 40559; run 20260920-D00-T02-S10-gpt

Ledger (namespaced IDs per the §16 shape; 3 filed, 1 rejected with rationale):

Ledger:

- [D00-T02-S10-PR1] [critical] blocker list omits red focus-free plus Primary proofs -> filed D00 T02 §10
- [D00-T02-S10-PR2] [major] N1 never closed on the real line -> rejected closing now would fabricate evidence: last collection red 24/2/2, close fires on first green run with census (clarified in item 5)
- [D00-T02-S10-PR3] [major] cause enum contradicts the narrowed-out example -> filed D00 T02 §10
- [D00-T02-S10-PR4] [major] closure-safe skips undefined -> filed D00 T02 §10
End of ledger
Risk accepted: D00-T02-S10-PR1; id A1; approver DerickPayne; owner DerickPayne; date 2026-09-20; expires 2026-10-20; review 2026-09-27; evidence f713089f61ef77cc4bdb0663e37580152bcb468a; rationale Self-filed critical cannot clear by ordering (target == reviewer §10); proofs ride §11's pipeline, re-review at its landing

## Live proof

Digests below are sha256 over the quoted outputs. Quotes ran on the stamp working tree whose code is byte-identical to candidate tip `87796e9` (the stamp commit adds this file's Live proof plus the §10 stamp lines and row flip only). Box: Venom-PC. Collector and provision proofs ran as quoted scratch plus live legs in the R1/R2 dispositions above, not re-run here.

`py scripts/todo-graph.py validate`: `28 todos, 252 sections -- 0 fatal, 0 warning(s)`.

Provenance: candidate 87796e9a43c29cf7083db4e9555df9c4a1678c63; command py scripts/todo-graph.py validate; exit 0; tool CPython 3.14.3; digest 70ad83f5d9c2932d840d9d2f7baee79c98e1861a8bdf36b724b24d99fa4ea70a; path scripts/todo-graph.py; run 20260920-D00-T02-S10-gpt

`py scripts/todo-graph.py plan --check`: `implementation plan is current -- 90/247 sections complete (36%)`.

Provenance: candidate 87796e9a43c29cf7083db4e9555df9c4a1678c63; command py scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.3; digest 0d05af9eade340502843223941f1786e851a8a5aca6e34b161ae3a1780c92d5a; path scripts/todo-graph.py; run 20260920-D00-T02-S10-gpt

`py scripts/todo-graph.py self-test` (tally line): `todo-graph self-test: 1184 cases, 0 failed`.

Provenance: candidate 87796e9a43c29cf7083db4e9555df9c4a1678c63; command py scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.3; digest b00ecf23ad5cfdd11bd4caec37c1f9b68e53c2feec706026c8901520c8e52b57; path scripts/todo-graph.py; run 20260920-D00-T02-S10-gpt

## Arch gate

arch: none (tooling plus skills plus docs; no editor, storage, extensibility, AI, protocol, or consent surface).
