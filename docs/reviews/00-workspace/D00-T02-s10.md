# D00 T02 §10 findings

Full panel (scripts plus skills surface; soft cap 3, hard cap 5). Candidate: `41d0567` plus self-review fix `7d0c501`.

## Self-review

Three questions over the diff (`41d0567`, 14 files) plus blast radius. Four defects fixed in `7d0c501` (CollectDebt fail-safe plus else-guard, red-entry staging text, repair wrap, redundant fetch); two accepted with notes (Night-collected skip count reuses the report cell §15 hardens; task re-register proven by probe roundtrip, never fired live). T02 Current-state staleness (says open §§8-12) is adjacent pre-existing, noted not fixed.

Evidence (quoted): self-test `1184 cases, 0 failed`; `query night-debt` lists N1 open only (`count 28 filter Interactive age 1n`); dry run quotes filter plus log path exit 0, unknown id exit 2; green append plus skip paths plus staged stubs on a temp T02 copy; provision `-Verify` 9/9 green, fresh-clone repair to 9/9, miswired plus missing hook repairs green with byte-identical restore; PSParser 0 errors on all three scripts; validate 0 fatal, plan current 90/247.

## Evidence (quoted)

TBD at stamp (Live proof with Provenance).

## Sol panel (round 1)

Verdicts (check-panel PASS): adversarial needs-attention (2), consistency approve, integration needs-attention (3), record needs-attention (1). 135s, session `01a0bd42-c935-7e21-b219-aca5446bdd01`.

| ID | Disposition | Evidence |
|----|-------------|----------|
| R1-F1 (provision restores user-deleted hook) | defended, superseded by R2-F1 | plan authorization plus loud report plus reversibility argued; R2 re-filed, fixed by split repair |
| R1-F2 (unlocked read-modify-replace) | fixed | `f5dc41e` (`Add-CollectedLine` re-reads before replacing: one retry, then `skip: file changed under write, triage appends`; append re-proof green) |
| R1-F3 (XML BOM invalid) | defended + hygiene | invalid as stated: XML allows BOM, `[xml]` parse plus `Register-ScheduledTask` proven by probe roundtrip (True/True); BOMs stripped anyway, timers re-verified OK in `f5dc41e` |
| R1-F4 (narrower debts never close) | fixed | `f5dc41e` (`Test-DebtCoverage`: exact closes, `&`-only Interactive subset on a full-green run stages for triage close, `\|`/`!` exact-only; docs; 7/7 truth table) |
| R1-F5 (no census check) | fixed | `f5dc41e` (`Test-DebtCensus`: totals equal owed, else `census mismatch` reds; 4/4 truth table) |
| R1-F6 (commit row unchecked) | fixed | `f5dc41e` (item-12 tick rides the fix-loop commit) |

## Sol panel (round 2)

Verdicts (check-panel PASS): adversarial needs-attention (2), consistency approve, integration needs-attention (3), record needs-attention (1). 215s. Extraction note: the codex output echoes the prompt plus a duplicate verdict block; the checkable answer is the first `**adversarial:` after the last `^codex$` line, stdin via cmd `<` redirect (PowerShell pipes corrupt the bytes).

| ID | Disposition | Evidence |
|----|-------------|----------|
| R2-F1 (`git checkout --` survives) | fixed | split repair: re-wire only when unset or pointing at nothing, existing-other-dir left alone, missing file reports the exact restore; bogus rewires to 9/9, `.git` left alone at 8/9, missing file un-restored with the command quoted |
| R2-F2 (residual TOCTOU; collected-on-skip) | defended + fixed | TOCTOU defended: retry closed the human-scale race, the microsecond check-act window is uncloseable in user space (repo precedent: plan `--sync`), single-writer plus run mutex bound it, readback guards the copy count; collected-on-skip fixed by `Format-DebtGreenEntry` (appended/already-closed/skip-red branches, 3/3 proven) |
| R2-F3 (UTF-16 decl vs UTF-8 bytes) | fixed | both XMLs rewritten UTF-16LE with BOM to match the declaration; timers re-verified OK, probe register plus remove roundtrip True/True |
| R2-F4 (no subset counts) | fixed | `Get-TrxSubsetCounts`: FQN/Name-attributable subsets close with their own counts plus census; red subsets stage, mismatch reds, non-attributable stages; 5/5 subset truth rows on crafted trx |
| R2-F5 (substring Category match) | fixed | clause-aware `&` split with exact `Category=Interactive` clause match; trap filter uncovered, paren case superset |
| R2-F6 (record IDs/dispositions) | fixed | findings tables renamed to round-scoped `R1-Fn`/`R2-Fn` with fix commits cited, per the §9 precedent; `defended` kept (same precedent) |
