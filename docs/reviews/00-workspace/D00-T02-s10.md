# D00 T02 §10 findings

Full panel (scripts plus skills surface; soft cap 3, hard cap 5). Candidate: `41d0567` plus self-review fix `7d0c501`.

## Self-review

Three questions over the diff (`41d0567`, 14 files) plus blast radius. Four defects fixed in `7d0c501` (CollectDebt fail-safe plus else-guard, red-entry staging text, repair wrap, redundant fetch); two accepted with notes (Night-collected skip count reuses the report cell §15 hardens; task re-register proven by probe roundtrip, never fired live). T02 Current-state staleness (says open §§8-12) is adjacent pre-existing, noted not fixed.

Evidence (quoted): self-test `1184 cases, 0 failed`; `query night-debt` lists N1 open only (`count 28 filter Interactive age 1n`); dry run quotes filter plus log path exit 0, unknown id exit 2; green append plus skip paths plus staged stubs on a temp T02 copy; provision `-Verify` 9/9 green, fresh-clone repair to 9/9, miswired plus missing hook repairs green with byte-identical restore; PSParser 0 errors on all three scripts; validate 0 fatal, plan current 90/247.

## Evidence (quoted)

TBD at stamp (Live proof with Provenance).

## Sol panel (round 1)

Verdicts (check-panel PASS): adversarial needs-attention (2), consistency approve, integration needs-attention (3), record needs-attention (1). 135s, session `01a0bd42-c935-7e21-b219-aca5446bdd01`.

| ID | Disposition | Evidence |
|----|-------------|----------|
| R1-A1 (provision restores user-deleted hook) | defended | §10 item 10 (approved plan) explicitly requires broken-hook repair with a deletion-class proof; hooks are owned automation, not work product; repair runs only on explicit invocation (audit runs read-only `-Verify`); the restore is quoted (`pre-commit restored from HEAD`) and one `rm` reverses it |
| R1-A2 (unlocked read-modify-replace) | fixed | `Add-CollectedLine` re-reads before replacing: one retry from current content, then `skip: file changed under write, triage appends`; append re-proof green |
| R1-I1 (XML BOM invalid) | defended + hygiene | invalid as stated: XML allows BOM, `[xml]` parse plus `Register-ScheduledTask` proven by probe roundtrip (True/True); BOMs stripped anyway to kill the confusion class, timers re-verified OK |
| R1-I2 (narrower debts never close) | fixed | `Test-DebtCoverage`: exact closes, `&`-only Interactive subset on a full-green run stages for triage close with subset counts, `\|`/`!` exact-only; testing.md plus README document the superset rule; 7/7 truth table |
| R1-I3 (no census check) | fixed | `Test-DebtCensus`: collected totals must equal owed, else `census mismatch` reds as collector bug; 4/4 truth table |
| R1-R1 (commit row unchecked) | fixed | item-12 tick rides the fix-loop commit |
