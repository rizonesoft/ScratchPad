# D00 T01 §53 review

Candidate: `b4b1b32..16ff038` (base `0b8acec` plus self-review fix plus fix-loops 1-3; fix loops append, sign-off names the tip).
Tier: Full panel (the diff changes the validator plus the graph plus the workflow plus two new tools: every stamp, query, gate, and notification consumes them).

## Self-review

Self verdict: FIXED (four self-review findings committed pre-panel in `92652c2`; panel rounds to confirm).

- Narrow as a cross-target superseder violates the validator's same-target succession rule (chain-broken FATAL): the query tolerates it but no real tree could carry it. Redesigned to a same-target `closed` successor plus a fresh narrowed waiver, narrowed the item-14 linkage to same-target successors ("?" otherwise), and corrected the item-11/item-14 Done notes.
- Rule-33 non-object mappings and unmapped marker owners rode unpinned branches (fixtures covered only unparseable JSON, bad logins, ledger, and acceptance owners). Two fixtures added: non-object fatal plus unmapped-marker warn with mapped-others silence.
- Heartbeat `--now` without an offset parsed naive and would crash aware-minus-naive arithmetic. Naive now reads UTC plus fixture.
- Item-11, item-14, and item-17 Done notes miscounted or misworded their fixtures (escalation pin updated in place, not new; cross-target pin replaced; 21 legs, not 20). Corrected.
- Surveyed clean: retry bound exactness (1 plus --retries on every path including adoption), count-check ordering (shape errors before count mismatch), list-based lookup rationale (search indexes with delay), schema /9 sites (constant, help probe, version pin, AGENTS, README), `_r0` freeze coverage (21 legs; malformed-flag error paths never read the clock).

Candidate diff: 10 files, +1982/-134 (`scripts/todo-graph.py` +1390 mostly fixtures, `tools/notify_poster.py` +369 new poster, `tools/check_heartbeat.py` +114 new detector, `scripts/todo-validate.py` +112 rules 24/33, workflow rewiring, mapping plus baseline, TODO-01 item ticks).

## GPT panel Round 1

Round 1 over candidate `b4b1b32..92652c2` (Sol medium, run 20260921-D00-T01-S53-gpt, output check PASS, duration 287s).

- `adversarial` needs-attention (R1-F1: blind create retry double-mints on lost responses)
- `consistency` needs-attention (R1-F2: header count discarded, truncated payloads post a subset)
- `integration` needs-attention (R1-F3: push filter omits heartbeat plus mapping)
- `record` needs-attention (R1-F4: no-duplicate plus no-silent-expiry ticks overstate their residuals)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 287s; outcome needs-attention; tokens unknown

All four real, all fixed. Disposition:

| ID | Disposition | Evidence |
| R1-F1 | fixed | `8555505`: creates adopt on lost responses (exact-title re-check per attempt inside the 1-plus-retries bound; adopted issues run the update path); lookups list with client-side match since search indexes with delay; `FAKEGH_LOST_CREATE` injection plus adoption fixture (one issue, one create, no comment) |
| R1-F2 | fixed | `8555505`: header count binds in `parse_payload` (truncated/padded fail closed); 2 fixtures |
| R1-F3 | fixed | `8555505`: push paths cover `tools/check_heartbeat.py` plus `.github/owner-logins.json`; filter pin |
| R1-F4 | fixed | `8555505`: adoption removes the double-mint residual (contract comment plus item-6 Done rewritten); item 7 narrowed to delayed-or-failing with the disable exclusion recorded (in-repo step cannot detect its own schedule disabled) |

Raw round-1 output (fenced, stripped from scans):

```
**adversarial: needs-attention**

`tools/notify_poster.py:217-235` blindly retries every GitHub operation, including `issue create` at `tools/notify_poster.py:280-285`. If GitHub creates the issue but its response is lost, the retry creates another issue. The module itself acknowledges that the duplicate remains (`tools/notify_poster.py:46-49`). This violates the one-issue-per-obligation requirement and can notify owners multiple times.

**consistency: needs-attention**

The payload header count is parsed at `tools/notify_poster.py:105-122` but discarded as `_count` at `tools/notify_poster.py:182-185`. A payload claiming two obligations but containing one valid line is accepted and posts only one. That contradicts the documented fail-closed contract and permits a truncated notification file to silently lose obligations.

**integration: needs-attention**

The push path filter at `.github/workflows/plan.yml:9-15` includes `tools/notify_poster.py` but omits both `tools/check_heartbeat.py` and `.github/owner-logins.json`. A standalone change to heartbeat behavior or owner assignment therefore does not run this workflow's self-test, validator, or unattended gates, despite both files being operational inputs introduced by this section.

**record: needs-attention**

The completion record claims obligation-keyed behavior prevents duplicate issues (`todo/00-workspace/TODO-01-repo-and-toolchain.md:1346`), while the implementation explicitly records that create retries can double-mint and leave the duplicate visible (`tools/notify_poster.py:46-49`). It also claims disabled schedules cannot expire silently before recording deliberate disable as an undetected residual in the same item (`todo/00-workspace/TODO-01-repo-and-toolchain.md:1351`). Those unresolved exceptions make the corresponding ticks overstate what was proven.
```

run 20260921-D00-T01-S53-gpt

## GPT panel Round 2

Round 2 over candidate `b4b1b32..8555505` with the round-1 verdicts appended (Sol medium, run 20260921-D00-T01-S53-gpt-r2, output check PASS, duration 328s). No round-1 finding re-reported.

- `adversarial` needs-attention (R2-F1: success-gated heartbeat latches red permanently)
- `consistency` needs-attention (R2-F2: bootstrap passes forever when cron never fires)
- `integration` needs-attention (R2-F3: fabricated evidence closes reviews, validator silent)
- `record` needs-attention (R2-F4: item-7 plus item-11 ticks overstate)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 328s; outcome needs-attention; tokens unknown

All four real, all fixed. Disposition:

| ID | Disposition | Evidence |
| R2-F1 | fixed | `e9a5b7c`: heartbeat redesigned to completion-gated liveness (conclusions ignored; any completion heals, failing schedules surface through their own red jobs); item-7 text plus Done rewritten, workflow comment rewritten |
| R2-F2 | fixed | `e9a5b7c`: empty schedule history with other-event completions fails (cron never fired); true bootstrap still passes; 2 fixtures (never-ran, stale-despite-pushes) |
| R2-F3 | fixed | `e9a5b7c`: evidence resolving to no commit while git answers fires risk-acceptance-malformed (git_resolves False; None keeps the gitless degrade); marker A6 plus count 5 |
| R2-F4 | fixed | `e9a5b7c`: item-7 Done rewritten to completion semantics with 10 fixtures; item-11 Done records the fabrication leg with 5 fixtures |

Raw round-2 output (fenced, stripped from scans):

```
**adversarial: needs-attention**

The heartbeat becomes permanently red after its first stale interval. It queries only completed historical schedule runs (`tools/check_heartbeat.py:65-68`) and fails when the newest success exceeds 26 hours (`tools/check_heartbeat.py:100-105`). Because that failure makes the current workflow fail (`.github/workflows/plan.yml:130-132`), the current run never becomes a new successful historical run. Every subsequent cron therefore observes the same increasingly stale success and fails again, requiring manual intervention to recover.

**consistency: needs-attention**

The stated delayed-schedule guarantee conflicts with unconditional bootstrap success. The workflow says a delayed schedule will surface (`.github/workflows/plan.yml:69-73`), and the completion record repeats that claim (`todo/00-workspace/TODO-01-repo-and-toolchain.md:1351`), but an empty schedule history always returns success (`tools/check_heartbeat.py:81-83`). If the new cron never produces its first completed run, every push reports bootstrap indefinitely, so the exact delayed-schedule case remains silent.

**integration: needs-attention**

Disposition evidence is not enforced when closing a review. The review query excludes a predecessor solely because its ID appears in the superseded set (`scripts/todo-graph.py:5233-5248`), while validation explicitly accepts unresolved evidence objects by continuing when `_efull` is absent (`scripts/todo-validate.py:1841-1845`). Consequently, a successor containing any syntactically valid but nonexistent 40-character hash can close the predecessor’s review obligation. This contradicts the claimed re-examination-evidence contract (`scripts/todo-graph.py:5216-5220`) and the section’s evidence requirement.

**record: needs-attention**

The completion record overstates both affected items. It marks heartbeat monitoring complete despite the unrecoverable stale-state latch and indefinite empty-history pass (`todo/00-workspace/TODO-01-repo-and-toolchain.md:1351`). It also says disposition evidence requirements landed (`todo/00-workspace/TODO-01-repo-and-toolchain.md:1355`), while the implementation admits that unchanged-tree reuse is merely a reviewer expectation that is not enforced (`scripts/todo-graph.py:5216-5220`). Those ticks need implementation and negative fixtures before the record is supportable.
```

run 20260921-D00-T01-S53-gpt-r2

## Opus panel Round 3

Round 3 over candidate `b4b1b32..e9a5b7c` with the rounds 1-2 verdicts appended (Opus medium, run 20260921-D00-T01-S53-opus, output check PASS, duration 305s). No round-1 or round-2 finding re-reported; the round confirms the fabrication leg, the header count, and the adoption path hold.

**Corrected 2026-09-21 (run-8 R4 transcription):** this round first transcribed as GPT/Sol under run `20260921-D00-T01-S53-gpt-r3`, which never ran (no ledger line, no artifact); the raw output below matches artifact `3f06c4ff1d93cfd6e85628e88db9cebf6d7ba333deaf98fef0a6e655e6342ca5` of ledger run `20260921-D00-T01-S53-opus` modulo the transcriber's em-dash normalization, so attribution follows the ledger. The artifact's closing `Provenance: {model: gpt-5.6-sol, effort: medium, duration: 305s, tokens: ?}` line is model-written text inside the output, not the runner record.

- `adversarial` needs-attention (R3-F1: unfiltered 20-row window false-reds a live cron on push-heavy days)
- `consistency` needs-attention (R3-F2: skill still on plan-health/8, README malformed row misses two FATALs, rule 33 skips the fence strip)
- `integration` needs-attention (R3-F3: false red reddens the whole job, disposition workflow lives only in a comment)
- `record` needs-attention (R3-F4: item-7 tick overclaims an unproven filter, item-14 scope omits the skill, execution unavailable)

Telemetry: round 3; model opus; effort medium; duration 305s; outcome needs-attention; tokens unknown

All four real, all fixed. Disposition:

| ID | Disposition | Evidence |
| R3-F1 | fixed | `16ff038`: schedule listing filters server-side (`--event schedule`); a second unfiltered listing runs only for the never-ran split; the fake branches on argv and a call log pins the two-call shape |
| R3-F2 | fixed | `16ff038`: review skill `/8` to `/9`; README malformed row enumerates the lead-time plus fabrication FATAls; rule 33 fence-strips (quoted supersedes excludes nothing) with a phantom-owner fixture |
| R3-F3 | fixed | `16ff038`: the same filter fix ends the job-reddening; the narrow shape documents in the README outcome matrix plus the review skill (fresh waiver plus same-target closed superseder; cross-target is chain-broken) |
| R3-F4 | fixed | `16ff038`: item-7 Done records the filter plus the proven filter; item-14 Done adds the skill sweep site; the execution limit answered here (self-test 1392/1392, validate 0 fatal, plan current) |

Raw round-3 output (fenced, stripped from scans):

```
**adversarial: needs-attention**

`tools/check_heartbeat.py:80-83` asks GitHub for `run list --workflow plan.yml --status completed --limit 20` with no `--event` filter, then classifies the 20 rows it gets back into schedule versus other. In this repository the 20-row window does not reach back one cron interval. `git reflog show origin/main` records 24 pushes to `main` between the 2026-09-20 04:23 UTC fire and the 2026-09-21 04:23 UTC fire (2026-09-20 07:17 CEST through 2026-09-21 02:41 CEST), and `git rev-list --count` over the same window on the workflow's own trigger paths (`scripts todo tools/githooks .github/workflows/plan.yml .gitattributes`) returns 68 commits, so effectively every one of those pushes starts a `plan.yml` run. The newest 20 completed runs are therefore all push runs, `sched` comes back empty while `other` is full, and control falls to `tools/check_heartbeat.py:110-114`, which exits 1 with "schedule never produced a completed run of plan.yml" -- on a cron that fired hours earlier. The consequence is the inverse of item 7: on any active day the unattended job goes red on every push and on the scheduled run itself, the message misdiagnoses a live cron as one that never fired, and the one signal meant to prove the schedule is alive becomes the noisiest step in the job. An `--event schedule` filter (or a `--created` window instead of a row count) is what makes the query mean what the file's docstring says.

**consistency: needs-attention**

The `plan-health/8` to `/9` sweep stops short of a consumer the repo treats as contract. `scripts/todo-graph.py:1281` now pins `PLAN_HEALTH_SCHEMA = "plan-health/9"` and the diff updates `AGENTS.md:62`, `todo/README.md:348`, and both help strings, but `.claude/skills/review-todo-section/SKILL.md:186` still tells the runner "`query plan-health --json` is the machine contract (schema `plan-health/8`, total sort keys, singly typed fields)". The precedent for this sweep is stated in the same TODO file: §29 item 7 records "New record keys bump the machine contract to schema plan-health/5 (doc sweep: skill, AGENTS.md, README, contract comment)" (`todo/00-workspace/TODO-01-repo-and-toolchain.md:831`), so the skill is a named sweep site, and the review skill is exactly the document a runner reads while stamping.

Second site: `todo/README.md:429` is the canonical spec for `risk-acceptance-malformed` and enumerates each firing condition, ending "or carrying a review date outside its record-expiry window". Two new FATAL conditions land in this candidate without reaching that row: the lead-time strictness (`scripts/todo-validate.py:1749`, review at expiry now fires "needs a day of lead time") and fabricated evidence (`scripts/todo-validate.py:1845-1858`, evidence resolving to no commit while git answers). An author working from the README writes `review == expires` and meets an undocumented FATAL. Related, smaller: new validator rule 33 scans findings files line by line (`scripts/todo-validate.py:2206-2226`) without the fence strip every sibling acceptance scan applies ("Date-scoped and fence-stripped like rule 23", `scripts/todo-validate.py:1687`), so a documented example row inside a fence would warn on a fictional owner. Nothing live trips it today -- `docs/reviews` holds exactly two line-start `Risk accepted:` records, both `owner DerickPayne` -- so this one is latent, not broken.

**integration: needs-attention**

The heartbeat's false red does not stay contained. Every gate step now runs `always()` and `Verdict on all gates` (`.github/workflows/plan.yml:118-133`) exits 1 on any `failure`, so a spurious heartbeat failure reddens the whole unattended job every time it fires. Item 9's verdict step is what makes that visible rather than fatal to the other gates, but the operational result is a job that is red for the wrong reason on ordinary work days, which is how real plan-health and register drift stops being read.

Second integration gap: item 11 ships a review-disposition workflow whose only home is a Python comment. `scripts/todo-graph.py:5202-5221` defines RENEW, NARROW (fresh waiver on the narrowed target plus a same-target `closed` superseder), and REVOKE plus the re-examination-evidence expectation, and the only operator-visible surface is the escalation string at `scripts/todo-graph.py:5299` ("record the review disposition (renew, narrow, revoke) with evidence in a superseding acceptance"). Neither `todo/README.md` nor `.claude/skills/review-todo-section/SKILL.md` describes the narrow shape, so the reviewer who follows the escalation has no documented way to learn that a cross-target superseder is chain-broken -- the exact trap the candidate's own self-review records hitting ("narrow first shipped as a cross-target superseder, which the validator forbids", `todo/00-workspace/TODO-01-repo-and-toolchain.md:1355`).

**record: needs-attention**

Item 7's tick reads "`tools/check_heartbeat.py` fails when the daily cron has no completion in `--max-age-hours` (26), fails when other-event completions exist with zero from schedule (cron never fired) ... 10 fixtures" (`todo/00-workspace/TODO-01-repo-and-toolchain.md:1351`). The fixtures are real but they cannot reach the defect: the fake gh at `scripts/todo-graph.py:21622-21625` is `print(os.environ.get('FAKEGH_RUNS', '[]'))`, which ignores argv entirely, so `--limit 20` and the missing `--event` filter are unproven by construction and the "cron never fired" branch is proven only against a hand-authored run list, never against the listing this repo would actually return. The tick therefore claims a delay detector that, on this repo's measured cadence, reports a healthy cron as never having fired.

Item 14's Done line is honest about its own scope ("AGENTS.md, todo/README.md, help probe, version pin", `todo/00-workspace/TODO-01-repo-and-toolchain.md:1358`) but leaves `.claude/skills/review-todo-section/SKILL.md:186` on `/8`, so the record and the tree disagree about what the machine contract says. Prior-round fixes I did check hold up on the paper trail: the fabricated-evidence leg is real and target-kind-agnostic (`scripts/todo-validate.py:1834-1858`), the header count now binds (`tools/notify_poster.py` `parse_payload`), and the adoption path is present (`create_adopting`). One limit on this report: python execution is unavailable in this session, so the counted suite results (1381/1381) and "live validate 0 fatal" are unverified here; static checks are consistent with them -- no live acceptance carries `review == expires` (so the new lead-time FATAL does not bite the tree) and `operator` is the only unmapped owner rule 33 can reach, matching the single line in `todo/.warning-baseline`.

Provenance: {model: gpt-5.6-sol, effort: medium, duration: 305s, tokens: ?}
```

run 20260921-D00-T01-S53-opus

## Opus panel Round 4

Round 4 over candidate `b4b1b32..16ff038` with the rounds 1-3 verdicts appended (Opus medium, run 20260921-D00-T01-S53-opus-r2, output check PASS, duration unknown). No round-1, round-2, or round-3 finding re-reported. (Transcribed 2026-09-21 by the run-8 session; the round ran 09:41 local, before the prior session stalled.)

- `adversarial` needs-attention (R4-F1: degraded keys drop the section address)
- `consistency` needs-attention (R4-F2: rule 33 reads the whole marker chain; R4-F3: README "rats" typo)
- `integration` needs-attention (R4-F4: poster fixtures never meet real notify output)
- `record` needs-attention (R4-F5: item-2 tick overclaims the per-obligation key)

Telemetry: round 4; model opus; effort medium; duration unknown; outcome needs-attention; tokens unknown

All five real, all fixed. Disposition:

| ID | Disposition | Evidence |
| R4-F1 | fixed | `691854c`: degraded keys keep path plus section (state dropped only); same-file sections key apart plus stateless refs key whole (2 fixtures); hand fixtures move to real three-token shapes |
| R4-F2 | fixed | `691854c`: rule 33 reads the governing last marker only; superseded-owner silence fixture |
| R4-F3 | fixed | `691854c`: "rats" to "waits" in the class dictionary |
| R4-F4 | fixed | `691854c`: real notify output parses in the poster plus per-section key pins (2 fixtures) |
| R4-F5 | fixed | `691854c`: item-2 Done plus poster docstring state path-plus-section; the checkpoint holds now that keys do |
| R4-F6 (self-found in the loop) | fixed | `691854c`: poster plus fake speak UTF-8 stdio (the § byte crashed locale pipes, found when the real-shape fixtures first ran) |

Raw round-4 output (fenced, stripped from scans):

```
**adversarial: needs-attention (1)**

1. `tools/notify_poster.py:93` — `re.fullmatch(r"degraded (\S+)( .*)?", ref)` keys a degraded obligation on the first whitespace token only, but `scripts/todo-graph.py:5904` builds the ref as `degraded {d['ref']} {d['state']}` where `d['ref']` is `f"{t.path} §{num}"` (`scripts/todo-graph.py:4518`), i.e. two tokens. Failure scenario: two verified sections of one TODO file carry unaccepted degraded markers with dues inside the horizon (e.g. `todo/00-workspace/TODO-01-repo-and-toolchain.md §29 outage` and `... §44 retry-owed`). Both payload lines reduce to key `degraded todo/00-workspace/TODO-01-repo-and-toolchain.md`, so one issue titled `Risk watch: degraded todo/00-workspace/TODO-01-repo-and-toolchain.md` carries both obligations; when §29 clears, the body diff fires an "update" comment while the title still names neither section, and the per-obligation create/close lifecycle can never distinguish or close them independently. The intended drop is the evolving state only; the section address is dropped with it.

**consistency: needs-attention (2)**

1. `scripts/todo-validate.py:2198-2202` — rule 33 collects marker owners from `section_markers(t, num)`, the whole `Plan review:` chain, while the notify population reads only the governing last marker line (`scripts/todo-graph.py:4359`, `body = (s.plan_review_body or "").strip()`). The rule's own comment at `scripts/todo-validate.py:2162-2164` claims the set is "the notify population, so rejected-row reasons and superseded records never warn". Failure scenario: a section whose degraded marker named `owner ann` is superseded by a rerun marker naming `owner operator`; `ann` can never appear in a payload, yet rule 33 warns `.github/owner-logins.json: owner ann has no GitHub login mapping`, a NEW warning that reds `validate` and can only be cleared by inventing a login for a retired name or baselining it.
2. `todo/README.md:416` — the `owner-login-unmapped` dictionary row reads "so the warning rats until mapped"; `rats` is not a word the sentence supports (no other site in the tree uses it: `grep -rn "\brats\b"` returns this line alone). The canonical class dictionary states the rule's behavior in garbled prose.

**integration: needs-attention (1)**

1. `scripts/todo-graph.py:21319` — the poster fixtures are driven entirely by hand-written payload lines (`_l1 = "    bob | 2026-09-22 | degraded D00-T02-S15 retry-owed"`), whose degraded ref is one token, a shape `query notify` never emits. The suite does capture real notify output at `scripts/todo-graph.py:18198` and asserts on it at `:18318`, but that text is never handed to `tools/notify_poster.py`. Failure scenario: the producer/consumer contract between `query notify` and the poster is unpinned end to end, which is exactly how the `degraded` key defect above ships green at 1381/1381; any future change to `d['ref']`, the state vocabulary, or the escalation suffix likewise passes the suite while breaking the live post.

**record: needs-attention (1)**

1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1346` — item 2's Done note states the key as "reviews normalize due/overdue, degraded drops state", and the item's contract is "a stable obligation key ... so a new failure after a clear reopens rather than minting a duplicate issue". The shipped key also drops the `§N` section address (`tools/notify_poster.py:93` against `scripts/todo-graph.py:5904`/`:4518`), so the tick claims a per-obligation key the code does not provide; the same overstatement rides `tools/notify_poster.py:15` ("degraded lines drop the evolving state") and the section's test checkpoint claim "issues track obligations".
```

run 20260921-D00-T01-S53-opus-r2
