# Review: D00 T01 §6 -- Developer Bootstrap Doc

Candidate: `ef2e014` (doc plus item ticks). No fixes; one round.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Prereqs and commands | `docs/bootstrap.md` steps 1-5 pin SDK 10.0.401, install order, and the per-OS build/test commands; cold readers reached green on both OSes without improvising |
| 2. OS boundary | Per-OS table: Linux builds/tests `src/Notepad.Neutral.slnf`, Windows builds/tests/launches `src/IntelligentNotepad.slnx`; carries the §2 XAML-compiler correction |
| 3. Troubleshooting | Two entries, both reproduced live: missing-env-vars failure captured verbatim on both OSes during the cold follows, stale-WSL-interop from Run 2 with the Run 3 fix; each fix verified by the subsequent green step |
| 4. Script-first | Cold follows ran `tools/provision.sh` / `tools/provision.ps1` then the doc steps verbatim: Linux `0 Warning(s)`, `Passed! 1/1`; Windows `0 Warning(s)`, `Passed! 1/1`, stub launch title `Intelligent Notepad (stub 0.0.0+5eeb4c9..., .NET 10.0.12)` |
| 5. Commit | `ef2e014`, subject matches the item exactly |

Runs: `34792965135` green both jobs on the doc-only push. `main` is green at HEAD.

## Self-review

1. Does it make sense? Yes. The doc delivers exactly the four content items in 43 lines with no extra scope; every tick has its change; the committed doc is byte-identical to the twice-cold-followed text (written once, never edited after).
2. Is it logical? Yes. SDK 10.0.401 matches `global.json` and the provisioners; the slnf/slnx split matches the §2 correction and CI; the env-failure messages are quoted verbatim from live reproductions; the stale-interop entry matches the Run 2 record and the Run 3 fix; Markdown one line per paragraph; no em dashes (`grep` 0).
3. Did it break anything else? No. Doc plus TODO ticks only; `validate` 0 fatal; `self-test` 391/391; CI green on the push; nothing else reads the new file yet.

## Lenses

- `adversarial`: advisory. Both cold follows ran on machines with the step-1 prereqs (git, and VS 2026 on Windows) preinstalled; a bare-metal reader follows the documented vendor installers in step 1, a standard path the doc never claims the script replaces. Noted, not blocking.
- `consistency`: approve. SDK string, script names, solution names, and the OS table match the repo, CI, and the §2 correction; commands are copy-pasteable per OS.
- `integration`: approve. Consumers are future cold readers plus the referenced provision scripts; all paths valid; no code callers.
- `record`: approve. Cold-follow outputs quoted from this session's transcripts; the prereq-preinstalled boundary stated above; zero deferrals.
- `source-defect`: not owed (no Notepad behavior or protocol detail).
- `design`: not owed (no UI surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; checkpoint cold-followed green on both OSes after the doc's final write; CI evidence stands on the exact pushed commit; `plan --check` green after the stamp-flow sync.
