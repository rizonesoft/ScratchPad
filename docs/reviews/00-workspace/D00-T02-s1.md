# Review: D00 T02 §1 -- Unit Test Project and Framework

Candidate: `f0b847c` (wiring) plus checkpoint evidence commits `1468835` (red probe) and `7629678` (revert). No fixes; one round.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Unit project and doc | `tests/Unit/` on xUnit 2.9.3 with the VSTest trio pinned identically to Smoke; `docs/testing.md` names the framework, its pins, and why it won, including the v3/MTP re-evaluation verdict |
| 2. Passing test | `FrameworkTests` 2/2 green via `dotnet test tests/Unit` (neutral-library reference plus a trivial pure function); inverted assertion red locally (Failed: 1) |
| 3. Common | `tests/Common/README.md` with the ownership rule (helper starts in its consumer, moves on second use, one-way references, classlib on first helper) |
| 4. CI runs suite | Run `34794828910` green both jobs with Unit 2/2 in both Test steps; probe run `34795487610` red both jobs naming `Unit.FrameworkTests.TrivialPureFunctionPasses`; reverted; green `34796097790` |
| 5. Commit | `f0b847c`, subject matches the item exactly |

Runs: `34794828910` green both jobs with Unit 2/2 in both Test steps, `34795487610` red both jobs with the test named, `34796097790` green after revert. `main` is green at HEAD.

## Self-review

1. Does it make sense? Yes. Every item has its change; the T01 §5 v3/MTP flag was honored with a bounded vendor-template re-evaluation (retargeted `xunit3` template runs zero tests with exit 5, verdict recorded in the doc); no extra scope beyond the project, the README, the doc, and two wiring lines.
2. Is it logical? Yes. Trio pins identical to Smoke; `net10.0` plus a neutral-only reference keeps the suite Linux-green (proven); the Common rule is one-way with a YAGNI classlib; the doc's commands are the ones run.
3. Did it break anything else? No. Solution files extended with both CI jobs green; Smoke untouched; `validate` 0 fatal; `self-test` 391/391; full suites green locally on both OSes and in CI.

## Lenses

- `adversarial`: approve. Probe reverted immediately; no secrets; assertions cover local computation plus the version string.
- `consistency`: approve. Mirrors the Smoke csproj trio, properties, and namespace style; Markdown one line per item; no em dashes.
- `integration`: approve. Both solution consumers green on both OSes; the Common rule references future suites without touching them.
- `record`: approve. Three run IDs plus local green/red quoted; the v3/MTP verdict trail recorded; zero deferrals.
- `source-defect`: not owed (no Notepad behavior or protocol detail).
- `design`: not owed (no UI surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; full suites green on both OSes locally and in CI; `plan --check` green after the stamp-flow sync.
