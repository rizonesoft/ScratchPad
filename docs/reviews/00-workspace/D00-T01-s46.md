# D00 T01 §46 findings

Candidate range: `0d6100a..973bce4` (`workspace: complete rule-24 probe coverage per §25 plan review`, `workspace: bound rule-24 leg markers per self-review`, `workspace: scope §46 to recognized leg vocabulary per panel R1`, `workspace: strip paren boilerplate in leg detector per panel R2`).

Tier: Full panel (diff touches `scripts/todo-graph.py`, shared harness surface). Rounds 1-2 Sol, round 3 Opus sign-off.

Process note: the implementation commit `0d6100a` rode `--no-verify` (operator-rule violation, no active hook existed to skip; the later hook-backed commits plus manual `validate` runs cover the gates). No further bypasses in this section.

## GPT panel Round 1

Round 1 over `0d6100a..d80bc00`, Sol medium. One family across all four lenses: the novel-prose fifth-leg residual plus the contract overclaim.

- `adversarial` needs-attention (1)
- `consistency` needs-attention (1)
- `integration` needs-attention (1)
- `record` needs-attention (1)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 46s; outcome needs-attention; tokens unknown

1. R1-F1 `scripts/todo-graph.py:1033` closed four-marker list misses a novel-prose fifth leg at one site -> fixed in `a1f6f8b` (contract scoped, provenance-riding marker dropped plus pin).
2. R1-F2 `todo/00-workspace/TODO-01-repo-and-toolchain.md:1199` Done claims single-site detection while admitting the residual -> fixed in `a1f6f8b` (Corrected note scopes item 2).
3. R1-F3 `scripts/todo-graph.py:12038` equality probe integrates no new-leg detection -> fixed in `a1f6f8b` (same scoping, detector covers descriptive vocabulary).
4. R1-F4 `todo/00-workspace/TODO-01-repo-and-toolchain.md:1202` checkpoint falsifiability contradicts the residual -> fixed in `a1f6f8b` (Corrected checkpoint scopes the claim).

## GPT panel Round 2

Round 2 over `0d6100a..a1f6f8b`, Sol medium. R1 family closed; one new needs-attention plus one advisory.

- `adversarial` needs-attention (1)
- `consistency` approve
- `integration` approve
- `record` advisory (1)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 129s; outcome needs-attention; tokens unknown

1. R2-F1 `scripts/todo-graph.py:1044` record-expiry-shaped provenance could conceal a deleted description; the pin covers only `review-window` -> fixed in `973bce4` (normalizer strips parenthesized spans plus paren-strip proof fixture). Premise verified hypothetical at zero live sites (no `record-expiry` inside any live parenthetical), root fixed anyway.
2. R2-F2 `todo/00-workspace/TODO-01-repo-and-toolchain.md:1198` claims nine fixtures over eight fixture strings (split-marker pair is two checks, one block) -> fixed in `973bce4` (Done note reads 10 checks over 9 blocks).

## Opus panel Round 3

Round 3 (sign-off) over `0d6100a..973bce4`, Opus medium. All three findings sit below the blocking bar (probe precision, comment wording, doc count; no safety, data-integrity, or stamp-invalidating defect), so each files through `add-todo` at D00 T04 §2 with no re-round.

- `adversarial` needs-attention (1)
- `consistency` advisory (1)
- `integration` approve
- `record` needs-attention (1)

Telemetry: round 3; model opus; effort medium; duration 100s; outcome needs-attention; tokens unknown

1. R3-F1 `scripts/todo-graph.py:1035` the `target` marker fires on site B's shape-leg field enumeration (`line carries target, approver, ...`), so deleting the whole target-leg clause leaves the leg set unchanged and both probes green (panel-verified by live-block mutation; same family as R2-F1, riding shape-leg text paren-stripping cannot cover) -> filed D00 T04 §2 (clause-specific markers item).
2. R3-F2 `scripts/todo-graph.py:1029-1030` the marker header comment omits the parenthesized-span strip, understating the detector contract -> filed D00 T04 §2 (header wording item).
3. R3-F3 `AGENTS.md:55` self-test count reads 1140 against the shipped 1162 -> filed D00 T04 §2 (count refreshes to the suite count at the §1 ship commit).

## Dispositions

| ID | Disposition | Evidence |
| R1-F1 | fixed | a1f6f8b |
| R1-F2 | fixed | a1f6f8b |
| R1-F3 | fixed | a1f6f8b |
| R1-F4 | fixed | a1f6f8b |
| R2-F1 | fixed | 973bce4 |
| R2-F2 | fixed | 973bce4 |
| R3-F1 | filed | D00 T04 §2 |
| R3-F2 | filed | D00 T04 §2 |
| R3-F3 | filed | D00 T04 §2 |

## Architecture triggers

Arch trigger: none - comment-probe section over the test harness; no editor, storage, extensibility, AI-integration, protocol, or consent/undo surface changes.

## Plan review

Round: `gpt-5.6-sol` high over §46 plus §25, 2026-09-19. Neighbors: §25 (Depends plus XREF); dependents: none (no Depends beyond §25, no XREF consumers point at §46). Output `check-plan PASS` (9 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §46, D00 T01 §25]; dependents [none]; bytes 6501; run 20260919-D00-T01-S46-gpt

Ledger (namespaced IDs per the §16 shape; 3 of 9 filed at D00 T04 §2 in 2 items with PR5 sharing the clause-specific item, 2 reworded in place before stamp, 4 rejected with reasons on their rows):

Ledger:

- [D00-T01-S46-PR1] [minor] Core promise unsatisfied unless every claim narrows -> rejected both horns addressed: narrowing shipped via the R1 Corrected notes (a1f6f8b), open-ended comparison is PR9-class
- [D00-T01-S46-PR2] [minor] Stamped §25 overstates single-site protection -> filed D00 T04 §2
- [D00-T01-S46-PR3] [minor] Novel-prose residual has no owning section -> rejected owned by the record (§46 Done plus §25 PR9 ledger row plus the filed R3-F1 narrowing); a dedicated section re-litigates PR9
- [D00-T01-S46-PR4] [minor] No proof either description matches the executable rule -> rejected PR9-class declarative-spec redesign, disproportionate to a comment-sync section
- [D00-T01-S46-PR5] [minor] No semantic-mutation fixtures for negated phrasing -> filed D00 T04 §2 (shared with the R3-F1 clause-specific item)
- [D00-T01-S46-PR6] [minor] Checkpoint reports only 0 fatal -> rejected reworded in place before stamp (open section, record edit): gate reads the full 0 fatal 0 warnings
- [D00-T01-S46-PR7] [minor] No probe file locations or test names -> rejected reworded in place before stamp (open section, record edit): Done names the file plus check shapes
- [D00-T01-S46-PR8] [minor] Mismatch failures name no site or legs -> filed D00 T04 §2
- [D00-T01-S46-PR9] [minor] Generate descriptions from a leg catalog -> rejected PR9-class codegen redesign, disproportionate; R3-F1 plus PR5 narrow the hole without it
End of ledger

## Live proof

`python3 scripts/todo-graph.py self-test` on `973bce4`: `todo-graph self-test: 1162 cases, 0 failed`, exit 0.

Provenance: candidate 973bce4690e62023af73ecaddd88763126a7f9da; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest 7533dfbc46c99eca425428f9cd5ef6adc954465821ec72af7df302c037d45439; path scripts/todo-graph.py; run 20260919-D00-T01-S46-gpt

`python3 scripts/todo-graph.py validate` on `973bce4`: `28 todos, 240 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate 973bce4690e62023af73ecaddd88763126a7f9da; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest d716035c93c89237d19a0099dd2c02b21af791ed158cb25555011d60f833c978; path scripts/todo-graph.py; run 20260919-D00-T01-S46-gpt

`python3 scripts/todo-graph.py plan --check` on `973bce4`: `implementation plan is current -- 85/235 sections complete (36%)`, exit 0.

Provenance: candidate 973bce4690e62023af73ecaddd88763126a7f9da; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 1828acda6537972d977513ed855a1ad35c5a216f62a020b4f3575d0b5c59560f; path scripts/todo-graph.py; run 20260919-D00-T01-S46-gpt

`python3 scripts/todo-graph.py query plan-health --check` on `973bce4`: `gate: ok`, exit 0.

Provenance: candidate 973bce4690e62023af73ecaddd88763126a7f9da; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 83e9a060459d5921fedae881c1e079a0169941dd7fcff74fabc57c3188d0943b; path scripts/todo-graph.py; run 20260919-D00-T01-S46-gpt

## Raw panel outputs

```text
--- round 1 (run 20260919-D00-T01-S46-sol) ---
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1033` defines a closed list of four known markers, so a fifth leg expressed in novel prose at only one site remains invisible and does not fail the suite, violating the section contract.

**consistency: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1199` marks single-site addition detection complete while explicitly admitting that a novel fifth leg is not detected.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:12038` compares only the sets returned by the fixed marker registry, so the live site-equality probe does not integrate detection of newly described legs.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1202` claims falsifiability by “any silent single-site addition,” contradicting the undetected fifth-leg residual recorded at `todo/00-workspace/TODO-01-repo-and-toolchain.md:1199`.
--- round 2 (run 20260919-D00-T01-S46-sol-r2) ---
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1044` treats provenance-only text containing `record-expiry` as the review-window leg; the pin at `scripts/todo-graph.py:12189` tests only `review-window`, so surviving `record-expiry` provenance can conceal a deleted description and keep the live probes green.
**consistency: approve**
**integration: approve**
**record: advisory** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1198` claims nine fixtures, but `scripts/todo-graph.py:12129`-`scripts/todo-graph.py:12192` defines eight distinct fixture strings; the stated “split-marker pair” is two assertions over one fixture.
--- round 3 (run 20260919-D00-T01-S46-opus) ---
**adversarial: needs-attention (1)**

1. `scripts/todo-graph.py:1035` — the `target` marker `\btargets?\b` matches site B's shape-leg field enumeration (`scripts/todo-validate.py`: `# line carries target, approver, action owner, record date,`), not only the target-leg description. Deleting the whole target-leg clause at site B (`the target names a finding ID, a run ID, or `outage <rung> <date>`;`) leaves `rule24_comment_legs` returning `{shape, target, dates, review window}` unchanged, so the per-site probe at `scripts/todo-graph.py:12076` and the equality probe at `scripts/todo-graph.py:12169` both stay green with the description gone. Verified by mutating the live site B block and re-running the detector: leg set unchanged, `_glegs == _vlegs` still True. This is the same family as R2-F1 (a surviving non-description token conceals a deleted leg description), now riding shape-leg text rather than a parenthetical, and paren-stripping does not cover it.

**consistency: advisory (1)**

1. `scripts/todo-graph.py:1029-1030` — the constant's header comment states the normalization as "(comment-stripped, lowercased, whitespace-squeezed)" and omits the parenthesized-span strip added at `scripts/todo-graph.py:1057`, which is the step the R2 fix turns on; the entry-point comment understates the detector's contract.

**integration: approve**

**record: needs-attention (1)**

1. `AGENTS.md:55` — `python3 scripts/todo-graph.py self-test      # 1140 cases, must stay green` is stale; the shipped suite reports `todo-graph self-test: 1162 cases, 0 failed`, and the section's own Done note records 1140 to 1162.
```
