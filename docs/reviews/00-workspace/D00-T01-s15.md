# Review: D00 T01 §15 -- First Plan-Review Residuals

Candidate: `a643242` (ship) + `6d0957c` (review self-fix). Tier: full panel (diff touches shared contract surface: `scripts/`, skills). Lenses owed: adversarial, consistency, integration, record (source-defect/design not owed: no Notepad behavior or surface at stake).

## Self-review

Worked the three questions across the diff and its blast radius. One fix: unresolved-criticals counted every non-filed disposition, which cries wolf on rejected/duplicate rows; narrowed to accepted-or-deferred with the PR4 absence check (in `6d0957c`).

- Sense: items 1-7 and 10 land as specified; items 8-9 verify at this review's own plan review (manifest carried, ledger used). README Tooling and AGENTS.md query lines keep the docs true for the new query; the 36 panel-fixture markers isolate rule 16 under the new rule 17.
- Logic: marker rule mirrors rule-16 grandfathering with the cutoff shared via the graph module (no copied constants); the query's five dimensions are mechanical per the corrected spec; ledger regex backtracks across `->` in finding text.
- Blast radius: parser change is additive (new stamp kind plus body); 463 pre-existing self-test cases green; live validate silent; live plan-health quoted (0/0/0/0 files/1 outage-discussion file/0 criticals).

## Plan review

Round: `gpt-5.6-sol` high via codex read-only, after panel-close, second live run of the procedure. Input manifest: D00 T01 §15 plus neighbor §14 (Depends + XREF) plus reverse dependents (none; qualified grep for `D00 T01 §15` outside its own file returns nothing); prompt 13,939 bytes. Output: 21 one-line findings. No criticals (nothing safety/data/stamp-invalidating), so the safety exception does not trigger and the stamp proceeds.

Ledger (first live use of the §15 shape; IDs are review-local `PRn` pending the namespace fix filed below):

- [PR1] [major] §14 timing contradicts panel-close rule -> filed D00 T01 §16
- [PR2] [major] double-outage has no honest marker -> filed D00 T01 §16
- [PR3] [minor] same-family fallback permanently satisfies -> filed D00 T01 §16
- [PR4] [major] filed criticals invisible while target open -> filed D00 T01 §16
- [PR5] [minor] manifest never compared, scope goes stale -> filed D00 T01 §16
- [PR6] [minor] manifest lacks reproduction precision -> rejected proportionate-record (advisory review; full reproducibility belongs to build review, not plan review)
- [PR7] [minor] oversized input has no ceiling -> rejected hypothetical (no evidenced truncation vector; revisit on the first oversized failure)
- [PR8] [major] zero-exit malformed output accepted -> filed D00 T01 §16
- [PR9] [major] ledger IDs lack namespace -> filed D00 T01 §16
- [PR10] [minor] disposition transitions undefined -> filed D00 T01 §16 (merged with PR11 as one lifecycle item)
- [PR11] [minor] deferred rows lack owner/date -> filed D00 T01 §16 (merged with PR10)
- [PR12] [minor] severity lacks criteria -> filed D00 T01 §16
- [PR13] [minor] filing links not bidirectional -> filed D00 T01 §16
- [PR14] [minor] §14 findings not backfilled to ledger -> filed D00 T01 §16
- [PR15] [major] manifest/ledger lack validator gates -> filed D00 T01 §16
- [PR16] [major] rule 17 does not verify named filings -> filed D00 T01 §16
- [PR17] [minor] date-grandfathering trusts stamp text -> rejected forgery-boundary (§9 precedent: forgetfulness, not forgery; git history is the backstop)
- [PR18] [minor] commit ticked with items open -> rejected designed-lifecycle (review-time verification; the stamp, not the commit, is the completion claim)
- [PR19] [minor] checkpoint overclaims mid-build -> rejected universal-lifecycle (checkpoints state stamp-time gates)
- [PR20] [minor] plan-health needs machine-readable mode -> filed D00 T01 §16
- [PR21] [major] TODO text has no untrusted-content boundary -> filed D00 T01 §16

## Review notes

On round-1 record advisory 1: the precedent reading is rebutted, the cost statement accepted. Rule 16's cutoff is `<= PANEL_CUTOFF` (landing day included: §6 stamped 2026-09-17 stays silent), so rule 17's `<= 2026-09-18` mirrors it exactly rather than departing; landing-day exemption is the shared shape. The advisory's actionable half (state the enforcement cost) is fixed in the README row and the item-1 correction text: mechanical enforcement begins 2026-09-19, §15's stamp carries the marker voluntarily, and the skill still mandates today's rounds.

## Opus panel (round 1)

- `adversarial` needs-attention: (1) uncovered-dependents counts unbuilt sections (no status filter), printing 53 rows on this tree, the same cry-wolf removed from criticals; restrict to stamped dependents. (2) Ledger scan does not strip fenced code blocks, so a fenced worked example counts as a live critical; no fixture locks it.
- `consistency` needs-attention: step 7's stamp enumeration omits `Plan review:`, so following it produces a rule-17 FATAL on every post-cutoff stamp; sole omission in the family (README example plus dictionary both carry it).
- `integration` needs-attention: the unnumbered plan-review block sits after step 7 (stamp and commit) while mandating the marker ride the stamp commit; document order contradicts the rule and the only way out is a forbidden amend. Renumber as a step before stamping, or halt step 7 for it.
- `record` advisory: (1) landing-day cutoff exempts §15's own stamp, so rule 17 has no live proof beyond the scratch drive, and the cost is unstated. (2) Item-10 Done says "README" for the Tooling line but only `todo/README.md` carries it; root README and the domain INDEX have query blocks too.

## Opus panel (round 2)

- `adversarial` needs-attention: the query discards the unbalanced-opener signal, so on an unbalanced fence all three findings scans silently see a truncated file with no diagnostic; grandfathered files have no rule-16 FATAL to compensate. (Verdict header clipped in transit by `tail -12`; the finding text is complete and was fixed as blocking regardless. Panel output is teed to a file from round 3 on.)
- `consistency` advisory: uncovered applies no cutoff while reviewed exempts pre-cutoff stamps, so a grandfathered stamp is simultaneously exempt and reported, a state no work can clear.
- `integration` approve
- `record` needs-attention: item-10 Done points its live quote at a commit body that says the opposite (1 outage vs 0 after the strip fix); the claim cannot be checked where it points.

## Opus panel (round 3)

- `adversarial` advisory: the §5 unbalanced fixture is post-cutoff, so rule 16 already FATALs it; the motivating case (grandfathered stamp, query as sole signal) has no lock.
- `consistency` advisory: the review-owed predicate is written twice verbatim (unmarked plus uncoverable); the README Tooling comment enumerates four of six blocks while the other two labels stay generic.
- `integration` approve
- `record` advisory: item-10 Done says "domain INDEX" but the third site is the tree-wide `todo/TODO-00-INDEX.md`.

## Opus panel (round 4)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` advisory: item-10 evidence trail stops at R2; the R3 changes (§5 re-date to the grandfathered side, validate-silence lock, shared `_owed`) are unrecorded.

## Opus panel (round 5)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Raw round-1 output (fenced, not part of the verdict scan):

```
**adversarial: needs-attention (2)**

1. `scripts/todo-graph.py:1216-1220` (printed at `:1264-1266`) -- the uncovered-dependents dimension treats *every* reverse dependent that is not marked as uncovered, including sections that have never been implemented. `rev` is built at `:1210-1215` from `t.sections.items()` with no status filter, so an unbuilt future section counts as a review gap. Measured on this tree: with 53 verified sections, marking them all produces 53 uncovered rows (e.g. `packaging-and-update §1 waits on marked repo-and-toolchain §2`, `editing-surface §18 waits on winui-app-spine §18`), printed unbounded with no cap. This is the same cry-wolf failure the `6d0957c` self-fix removed from the criticals dimension, reintroduced on a bigger scale: the dimension becomes permanently non-zero and grows with the unbuilt plan. The dependent set must be restricted to sections that are themselves stamped (`t.verified_sections`), since an unstamped section cannot have had a plan review at all.

2. `scripts/todo-graph.py:1249-1254` -- the `Plan review` ledger scan does not strip fenced code blocks before matching `ledger_re`, unlike the panel scan it sits beside (rule 16 strips fences, `todo/README.md:383`). A findings file that quotes the ledger shape inside a fence (for instance a worked example of `- [PR1] [critical] ... -> accepted ...`) is counted as a live unresolved critical, and the same file cannot demonstrate the shape without corrupting the count. The self-test fixture at `scripts/todo-graph.py:4806-4814` only uses unfenced rows, so the gap is unlocked.

**consistency: needs-attention (1)**

`.claude/skills/review-todo-section/SKILL.md:123` -- step 7 ("Write the stamp and flip the row") enumerates the stamp block as `Verified:`, `Review:`, `CRUD:`, plus `Duration:` and `Deferred:`, and does not list `Plan review:`. That enumeration is the instruction an executor follows when writing the stamp, and following it now produces a FATAL `stamp-no-plan-review` for every post-cutoff stamp. The line is mandated only in the prose of the unnumbered `### Plan review` block at `:129`, which is a different step. Swept: `todo/README.md:144` (example block) and `:152` (line dictionary) both carry the new line, so `SKILL.md:123` is the sole omission in the family.

**integration: needs-attention (1)**

`.claude/skills/review-todo-section/SKILL.md:127-129` -- the plan-review block is unnumbered and placed *after* `### 7. Write the stamp and flip the row` (`:121-125`) and before `### 8. Audit stance` (`:143`), while its own first sentence says the round runs "After the panel closes (before the stamp commit)". The document order therefore instructs the opposite of the rule: an executor reading steps 1-8 in sequence stamps and commits at step 7, then discovers at the unnumbered block that the marker had to ride that commit, and the only way out is amending a recorded candidate, which `AGENTS.md` forbids. The block needs to become a numbered step between step 6 and the stamp step, or step 7 must explicitly halt for it.

**record: advisory (2)**

1. `scripts/todo-validate.py:654` with `scripts/todo-graph.py:779` -- `PLAN_REVIEW_CUTOFF = "2026-09-18"` compared with `<=` exempts every stamp dated on the landing day, which includes §15's own stamp and every other section stamped during today's Phase 0 run. The section's Test checkpoint claims "§15's own plan review exercises the new rules", but rule 17 provably cannot fire on §15's stamp; the only live proof of the rule is the reverted scratch drive. This also departs from the precedent it cites: rule 16's `PANEL_CUTOFF = "2026-09-17"` (`scripts/todo-validate.py:47`) grandfathered only strictly-earlier days, so §14's own 2026-09-18 stamp was enforced by the rule it landed. The choice is defensible (§14's stamp predates the marker) but the cost is unstated in both `todo/README.md:386` and the item-1 correction text: enforcement begins tomorrow, and today's remaining stamps skip the required round silently.

2. `todo/00-workspace/TODO-01-repo-and-toolchain.md:389` -- the item-10 Done text claims "Tooling lines in README and AGENTS.md name it". `AGENTS.md:59` and `todo/README.md:327` carry the line; the root `README.md:57-62` query block does not, and neither does `todo/TODO-00-INDEX.md:85-88`. If "README" meant `todo/README.md` the claim holds, but the bare word reads as the repository README that the same file references elsewhere.
```
