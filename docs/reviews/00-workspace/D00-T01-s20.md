# Review: D00 T01 §20

Candidate: `6ab8eae` (ship) + `f02b812` (self-review) + `41a7141` (R1 fix) + `d109a92` (R2 fix) + `d6142aa` (R3 fix) + `81dcc2d` (R4 fix). Full panel (scripts plus skill: shared contract surface).

## Self-review

Sense: all 5 work items land as specified plus the commit. Each Done-when maps to code or prose: the §17 supersession note plus the skill's single rule plus the `run-id` generator (item 1), rule 23 with the skill mandate (item 2), genesis pinned in skill plus README with the singleton-silence probe (item 3), `follows-outage` vocab with validator plus fixtures (item 4), the dated §17 checkpoint reconciliation (item 5).

Logic: the validator, generator, skill, and README agree on every new shape (numbering, genesis, chaining, provenance); fixture counts preserved where behavior kept (panel silence ×12 restored with provenance, fence lines shifted by one, §§26-30 marker probes intact).

Blast radius: one behavior change outside the review loop (none: all edits are review-loop code, its fixtures, its docs, and dated annotations); live tree silent (validate 0 fatal, grandfathered stamps untouched, `--check` exits 0).

Fix: three self-review findings, all fixed in `f02b812` before the panel (costs no round): the dangling check sat inside the multi-marker gate so a singleton carrying `follows-outage` stayed silent (now fires on any chain length, §31 case); the generator minted off-shape IDs for non-positive or non-int sections (floor added with rejection probes); the `provenance-malformed` dictionary row claimed semicolon enforcement the checker does not do (row restated to presence plus run, shape-tightening noted as §23's). AGENTS count carried at each step (640 at ship+self-review, 648 after R1, 653 after R3).

## Live proof

`python3 scripts/todo-graph.py self-test` on `81dcc2d`: `todo-graph self-test: 653 cases, 0 failed`, exit 0.

Provenance: candidate 81dcc2d; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest f53251412a2beb93bf9fe5797817aca21ebcb1ad68902e4ce687bf1ceeaf34d5; path docs/reviews/00-workspace/D00-T01-s20.md; run 20260918-D00-T01-S20-gpt

`python3 scripts/todo-graph.py validate` on `81dcc2d`: `22 todos, 189 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate 81dcc2d; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest fae160e271d4beb714460eef7497cd4e152ac40c0b1b8d2d2901cc7e50807db6; path docs/reviews/00-workspace/D00-T01-s20.md; run 20260918-D00-T01-S20-gpt

`python3 scripts/todo-graph.py plan --check` on `81dcc2d`: `implementation plan is current -- 58/184 sections complete (32%)`, exit 0.

Provenance: candidate 81dcc2d; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 841b6a6719ee450732ad207d13c343a7529dac2df8ed251149f932d5d88dbd15; path docs/reviews/00-workspace/D00-T01-s20.md; run 20260918-D00-T01-S20-gpt

`python3 scripts/todo-graph.py query plan-health --check` on `81dcc2d`: `gate: ok`, exit 0.

Provenance: candidate 81dcc2d; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 52c27a015e0f85ba6e34646fe387053b44ac7aab8d62cc611c1268b5c86addc1; path docs/reviews/00-workspace/D00-T01-s20.md; run 20260918-D00-T01-S20-gpt

## Opus panel (round 1)

Candidate: `6ab8eae` + `f02b812` (checked `fence` pipeline; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` needs-attention
- `record` needs-attention

Findings (all verified true, all fixed in `41a7141`): adversarial-1 anywhere-upchain outage test (now the immediately preceding marker, §32 case); adversarial-2 `-r0`/`-r1` with no normalization (shape tightened to `-r[1-9][0-9]*`, `normalize_run_id` in all three lineage comparisons, §§33-34 cases); consistency AGENTS count plus the §§26-28 comment (both synced); integration generator unwired (skill carries the invocation with the findings file as scan input, phantom-template comment rewritten); record CRUD count (648). Fix: `41a7141`.

Raw round-1 output (fenced, not part of the verdict scan; digest `e32fce79d9dd336e8805265ef49223a8ee1740a643ac758b56155dd1bd92d40c`):

```
**adversarial: needs-attention** (2)

1. `scripts/todo-validate.py:736` -- `prior_outage = any("outage:" in body.lower() for body in chain[:-1])` scans the whole prior chain, not the immediately preceding marker, so once any outage marker exists in a section every later rerun can carry `follows-outage` in place of `supersedes` and stay silent at `:750`. Chain `[outage] -> [run A, follows-outage] -> [run B, follows-outage] -> [run C, follows-outage]`: B and C name no predecessor, yet the rule the diff ships (`.claude/skills/review-todo-section/SKILL.md:129` "with `follows-outage` when it follows an outage marker") is satisfied only for A. Lineage after the first outage becomes unreconstructable, which is the failure §20 item 4 exists to close.
2. `scripts/todo-graph.py:949` (`RUN_ID_SHAPE_RE` accepts `-r\d+`, including `-r0` and `-r1`) with no normalization anywhere: `SKILL.md:129` newly declares `-r1` "run 1's synonym", but `scripts/todo-validate.py:743-746` (duplicate-run check) and `:773-786` (manifest-run match) compare raw strings. Chain `[... filed §2 (run 20260918-D00-T01-S20-gpt)]` then `[... filed §3 (run 20260918-D00-T01-S20-gpt-r1, supersedes 20260918-D00-T01-S20-gpt)]` passes every check while the second marker is the same run superseding itself; conversely a marker written `-r1` whose manifest carries the bare base fires a false `matches no manifest run` FATAL at `:786`. `-r0` likewise passes the shape while the stated numbering (`bare base is run 1, -rN is run N for N >= 2`) has no meaning for it.

**consistency: needs-attention** (2)

1. `AGENTS.md:55` still reads `# 619 cases, must stay green`; the candidate's self-test emits `todo-graph self-test: 640 cases, 0 failed`. Every prior section that added cases synced this line (613 -> 614 -> 615 -> 617 -> 619 in `git log -p -- AGENTS.md`); this one did not.
2. `scripts/todo-graph.py:6059` comment reads "§§26-28 share the file with one shared run" while the R1 fix added `§31` (`scripts/todo-graph.py:5528`, fixture at `:5880`) sharing the same `90-health-outage.md` record and the same `20260920-D90-T07-S26-gpt-r2` run. The comment now understates the fixture set it documents.

**integration: needs-attention** (1)

1. The generator ships but is not wired into the procedure it governs. `.claude/skills/review-todo-section/SKILL.md:129` and `todo/README.md:161` both assert every marker run is "minted by the `run-id` subcommand", yet SKILL.md carries no invocation for it, unlike every other `review_prompt.py` subcommand (`SKILL.md:88`, `:99`, `:143`, `:146`), and never states which files to pass as `<scan-file>...` -- so the operator cannot know the run-claim scan is complete. `scripts/review_prompt.py:251` compounds it: "the template fills it from `date -u +%Y%m%d`" names a template that does not exist (`grep -rn "date -u" .claude/skills/review-todo-section/SKILL.md` returns nothing, `grep -rn "run-id"` finds no caller outside the self-test). The "one canonical generator" of §20 item 1 is unreachable from the documented workflow.

**record: needs-attention** (1)

1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:563` -- the `CRUD:` line claims "read back via self-test 638/638"; the candidate at `f02b812` emits `todo-graph self-test: 640 cases, 0 failed`. The R1 fix commit added the `§31` dangling case and the generator section-floor cases after the count was written, so the stamped evidence undercounts by two.
```

## Opus panel (round 2)

Candidate: `6ab8eae` + `f02b812` + `41a7141` (checked `fence` pipeline, R1 output appended; output `check-panel PASS`).

- `adversarial` approve
- `consistency` advisory
- `integration` advisory
- `record` needs-attention

Findings: record lineage dictionary row stale against the shipped checker (now names the outage exemption, the dangling condition, and the `-r1` read-through), fixed in `d109a92`; consistency blank-lines advisory (two blank lines restored, same commit). Integration missing-scan-file advisory first kept as fail-closed (a genesis run passes zero scan files, locked by the bare-base probe); the round-3 repeat upgraded it to warn-and-mint, see round 3. Fix: `d109a92`.

Raw round-2 output (fenced, not part of the verdict scan; digest `76e575a9c9cb283ce5f71c7270eba8fb2c65dd8d6f62407690a7cdf2210302c1`):

```
**adversarial: approve**

**consistency: advisory** (1)

1. `scripts/todo-graph.py:966-967` -- `normalize_run_id` ends at `return run` with zero blank lines before the next top-level statement `RUN_ID_RE = re.compile(...)`. Every other top-level definition in the three scripts separates with two blank lines (swept: `scripts/todo-graph.py`, `scripts/review_prompt.py`, `scripts/todo-validate.py`; the only other deviation, `todo-graph.py:730`->`:788` with one blank, is pre-existing and untouched by this candidate).

**integration: advisory** (1)

1. `scripts/review_prompt.py:263-267` -- the documented mint invocation (`.claude/skills/review-todo-section/SKILL.md:129`) passes the section's findings file as `<scan-file>`, but a missing path is an `OSError` -> exit 2, so the generator refuses in exactly the case whose answer is unambiguous (no claims -> bare base, the genesis run of §20 item 3). Verified live: `python3 scripts/review_prompt.py run-id todo/00-workspace/TODO-01-repo-and-toolchain.md 20 gpt 20260918 docs/reviews/00-workspace/D00-T01-s20.md` -> `run-id: cannot read ...: [Errno 2] No such file or directory`, exit 2. The skill's step-3 ordering makes the file present in the normal flow, so this is a robustness gap, not a broken workflow; `fence` at `:238` legitimately hard-fails because its inputs are always pre-written, and the self-test covers no missing-scan-file case.

**record: needs-attention** (1)

1. `todo/README.md:401` -- the `plan-review-no-lineage` dictionary row is stale against the checker this candidate ships. It still reads "appends a rerun marker without `supersedes <prior-run>`" as a fire condition, but `scripts/todo-validate.py:750` now exempts a rerun whose immediately preceding marker is an outage and which carries `follows-outage`, and `:768-774` adds a fire condition the row does not name at all (`marker follows no outage (dangling follows-outage)`, any chain length). The sibling prose bullet at `todo/README.md:161` and `.claude/skills/review-todo-section/SKILL.md:129` were both updated for `follows-outage`; the error-class table, which is the canonical dictionary for FATAL classes (the candidate itself amends it at `:403` for `provenance-malformed`), was not.
```

## Opus panel (round 3)

Candidate: `6ab8eae` + `f02b812` + `41a7141` + `d109a92` (checked `fence` pipeline, R1+R2 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` advisory
- `integration` advisory
- `record` approve

Findings (all verified true, all fixed in `d6142aa`): adversarial prose-outage predecessor (the outage test now parses the predecessor through the shared grammar predicate, §35 case); consistency day-scope wording stated section-wide but computed per date base (restated at all four sites with a locking probe); integration missing-scan-file repeat (now warns and reads as no claims with CLI probes for the warning and the still-fatal directory case, skill notes the genesis form). Fix: `d6142aa`.

Raw round-3 output (fenced, not part of the verdict scan; digest `71fbedee9bc84a575362f172b9fc63a43235cf032342c37093c6ffd587e396b9`):

```
**adversarial: needs-attention** (1)

1. `scripts/todo-validate.py:736` -- `prior_outage = len(chain) > 1 and "outage:" in chain[-2].lower()` tests a bare substring over the whole predecessor marker body, not that the predecessor is an outage marker. Only the last marker is grammar-checked (`:624` takes `s.plan_review_body`, and the purity rules at `:658-686` run on that body alone, per the comment at `:641`), so a non-last marker may carry `outage:` beside filings with nothing firing. Chain `[> **Plan review:** GPT high, filed §5 (run 20260920-D90-T07-S5-gpt) after a runner outage: codex 429]` then `[> **Plan review:** GPT high, filed §6 (run 20260920-D90-T07-S5-gpt-r2, follows-outage)]`: the predecessor is a normal review marker carrying a nameable run, yet `prior_outage` is true, so the `supersedes` requirement at `:750` is skipped and the dangling check at `:768` stays silent. The rerun names no predecessor although one exists, which is the lineage break §20 item 4 exists to close. No fixture covers a predecessor whose `outage:` token is prose (§26/§32 both use real outage markers).

**consistency: advisory** (1)

1. The ordinal semantics are stated as section-wide but are computed per date base, so "run 1" is not unique within a section. `scripts/review_prompt.py:172` builds `base = f"{date}-D{dom}-T{num}-S{section}-{family}"` and walks `-rN` only past claims sharing that dated base, so a rerun of the same section on a later day mints a bare base again. Sites asserting the unqualified reading: `.claude/skills/review-todo-section/SKILL.md:129` ("the bare base is run 1 and `-rN` is run N for N >= 2"), `todo/README.md:161` (same sentence), `scripts/todo-graph.py:949-951`, `scripts/review_prompt.py:163-166`. Identity stays unique (the date differs) and lineage stays explicit via `supersedes`, so the defect is the wording, not the shape: neither the skill nor the README says the numbering is scoped to the day.

**integration: advisory** (1)

1. `scripts/review_prompt.py:259-265` -- unchanged since the prior round: the documented mint invocation (`.claude/skills/review-todo-section/SKILL.md:129`) passes the section's findings file, and a not-yet-created findings file is an `OSError` -> exit 2, so the generator refuses in exactly the genesis case whose answer is unambiguous (no claims -> bare base, §20 item 3). Verified live: `python3 scripts/review_prompt.py run-id todo/00-workspace/TODO-01-repo-and-toolchain.md 20 gpt 20260918 docs/reviews/00-workspace/D00-T01-s20.md` -> `run-id: cannot read ...: [Errno 2] No such file or directory`, exit 2 (that file does not exist in `docs/reviews/00-workspace/`). The self-test covers no missing-scan-file case.

**record: approve**
```

## Opus panel (round 4)

Candidate: `6ab8eae` + `f02b812` + `41a7141` + `d109a92` + `d6142aa` (checked `fence` pipeline, R1-R3 output appended; output `check-panel PASS`).

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` advisory

Findings: record unbalanced parenthetical on the skill's generator sentence (my R3 edit dropped the close; one character restored plus a paren-balance sweep across the skill, README, AGENTS, and the TODO file, all balanced), fixed in `81dcc2d`. Fix: `81dcc2d`.

Raw round-4 output (fenced, not part of the verdict scan; digest `fb6fe6b244cbb83b3b9cc2acf10513e522025f27307a7cd293424648aed6ca6d`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: advisory** (1)

1. `.claude/skills/review-todo-section/SKILL.md:129` -- the parenthetical opened by "minted by the `run-id` subcommand (one generator: base from the TODO path plus section plus family plus date, ..." is never closed: the line ends at paren depth 1 (verified by scanning every line of `SKILL.md`, `todo/README.md`, `AGENTS.md`, and `todo/00-workspace/TODO-01-repo-and-toolchain.md`; this is the only unbalanced line in the four). The inner `(a genesis run passes no scan file; a missing scan file warns and reads as no claims)` and the two trailing groups `(numbering restarts per day, ...)` and `` (`-r1` accepted as run 1's synonym, ...) `` all balance, so the `)` after "`-r0` is outside the shape" reads as closing the generator aside while the following sentences ("Genesis is a singleton marker ...", "A rerun opens a NEW `Plan review` record ...") sit inside a parenthetical that syntactically never terminates. The canonical statement of the genesis, supersedes, and `follows-outage` rules is therefore nested inside the generator description rather than standing as the marker contract. `todo/README.md:161` states the same rules with balanced punctuation.
```

## Opus panel (round 5)

Candidate: `6ab8eae` + `f02b812` + `41a7141` + `d109a92` + `d6142aa` + `81dcc2d` (checked `fence` pipeline, R1-R4 output appended; output `check-panel PASS`). Final round of five; panel closed.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Findings: none. All prior findings verified fixed and not re-reported.

Raw round-5 output (fenced, not part of the verdict scan; digest `fc52c157b6f4a3154c61d8c9fddcca514347b90416974ddb51fe4de3dee22ecb`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §20 plus §19 plus §17, 2026-09-18. Neighbors: §19 (the Depends plus XREF neighbor) and §17 (carries the superseded rule and the annotated checkpoint); dependents none (no reverse Depends or XREF consumers). The run minted through the new `run-id` genesis path (missing findings file warned and read as no claims, first live use). Runner note: the skill's `codex exec` template ran verbatim (`-c model_reasoning_effort='"high"'`, the §23-noted form); no new CLI drift. Output `check-plan PASS` (18 findings, one per line).

Manifest: sections [D00 T01 §20, D00 T01 §19, D00 T01 §17]; dependents [none]; bytes 38129; run 20260918-D00-T01-S20-gpt

Ledger (namespaced IDs per the §16 shape; 6 of 18 filed across §§21, 23, 24, 10 rejected with reasons on their rows, 2 duplicate with canonical IDs):

Ledger:

- [D00-T01-S20-PR1] [minor] -r1 synonym undermines canonical identity -> rejected recorded design: the synonym preserves §19-era markers, comparisons normalize, the generator never mints
- [D00-T01-S20-PR2] [minor] Singleton supersedes stays silent -> filed D00 T01 §24
- [D00-T01-S20-PR3] [minor] Immediate predecessor conflicts with position-free precedence -> rejected outage markers carry no run or timestamp to derive from; §19's rule targets governing precedence
- [D00-T01-S20-PR4] [minor] No cross-component integrity proof -> duplicate D00-T01-S20-PR7 (provenance-equality instance; rows carry no runs by design, marker-manifest match enforced)
- [D00-T01-S20-PR5] [minor] Manifest run optional on new records -> filed D00 T01 §24
- [D00-T01-S20-PR6] [minor] Outage and probe exemptions contradict the contract -> rejected second affirmation: run IDs for unruns rejected as false attribution; probes are not records
- [D00-T01-S20-PR7] [major] Provenance run need not equal the marker run -> filed D00 T01 §23
- [D00-T01-S20-PR8] [minor] Provenance fields unvalidated -> duplicate D00-T01-S19-PR15 (PR15n item covers fields, candidate, path; digest re-verification stays rejected)
- [D00-T01-S20-PR9] [minor] Grandfathered run-less provenance has no inventory -> filed D00 T01 §21
- [D00-T01-S20-PR10] [minor] Missing scan file is fail-open -> rejected single writer plus whole-tree duplicate check at every validate; collisions fail loud pre-stamp
- [D00-T01-S20-PR11] [minor] No atomic run allocation -> duplicate D00-T01-S19-PR2 (rejected: the single-writer rule precludes concurrent runs)
- [D00-T01-S20-PR12] [minor] Date reset and midnight boundary undefined -> rejected day scope specified and probed; chains ride date-agnostic supersedes; one generator
- [D00-T01-S20-PR13] [minor] Date clock and rollover undeclared -> rejected UTC pinned by date -u; the date prefix is the recorded timestamp; mint-before-run fixes start-date semantics
- [D00-T01-S20-PR14] [minor] Suffixed PR IDs lack a grammar -> rejected prose shorthand only; zero suffixed bracketed IDs repo-wide; rows and backlinks use clean IDs
- [D00-T01-S20-PR15] [minor] Lineage cycles and forks unchecked -> filed D00 T01 §24 (fork-check half; cycles impossible by backward-only edges)
- [D00-T01-S20-PR16] [minor] Crashed-before-marker runs have no record -> rejected unrecorded runs leave no trace; the first marker is the existence proof
- [D00-T01-S20-PR17] [minor] Checkpoint asserts then retracts the exception -> rejected stamped checkpoints are historical record; Reconciled notes are the mechanism
- [D00-T01-S20-PR18] [minor] No run inspection facility -> filed D00 T01 §24
End of ledger

Raw plan-review output (fenced, not part of the verdict scan; digest `6cfe178633f558f79d0a1b6d4c3f6b572970ed35d2391685debc268c28e88be8`):

```
- `-r1` is accepted as a synonym for the bare base despite the “one canonical identity” goal; §20 item 1 should reject it at input boundaries or normalize it before storage, because two serialized IDs for one run undermine equality, search, and audit joins.
- Genesis is stated as “run with no supersedes,” yet the validator deliberately leaves singleton lineage silent; §20 item 3 needs a negative fixture and failure for a singleton carrying `supersedes`, because deleted or fabricated ancestry can otherwise masquerade as genesis.
- Requiring `follows-outage` to reference the immediately preceding marker conflicts with §19’s rule that precedence never rests on line position; §20 item 4 should derive succession from explicit run IDs and timestamps, because unrelated intervening markers or reordered records must not corrupt lineage.
- No item proves referential integrity across the ledger record, manifest, marker, provenance, and artifact even though the checkpoint claims it; §20 needs one cross-component validation item, because individually valid IDs can still name different runs.
- Manifests retain an optional run field while §20 claims one run ID exists everywhere; §20 item 1 should require the field for every post-cutoff review record, because omission leaves the reviewed scope unjoinable to its execution.
- Outage markers and record-less probes are exempt from marker-manifest matching, which contradicts the universal run-identity contract; §20 item 4 should define a minimal outage record and a separate non-run namespace for probes, because failed executions still need attributable scope and provenance.
- Provenance validation checks that a run-shaped value exists but does not state that it must equal the enclosing review record and marker run; §20 item 2 should enforce that equality, because a well-formed wrong run silently misattributes evidence.
- The broader provenance contract names candidate hash, command, exit status, tool version, digest, and immutable path, but the described validator only rejects malformed or run-less lines; §20 item 2 should validate required fields, digest readback, candidate association, and artifact existence, because syntactic provenance is not evidentiary provenance.
- Run-less pre-cutoff provenance remains grandfathered without an inventory, health dimension, migration owner, or deadline; §20 item 2 should add these controls, because “one run everywhere” otherwise excludes an unknown legacy evidence set.
- Treating a missing run-ID scan file as “no claims” is fail-open; §20 item 1 should require an explicit initialization mode and otherwise stop, because a typo or missing checkout can mint a duplicate identity before later validation sees both records.
- The generator scans one findings file rather than reserving identities atomically in an authoritative registry; §20 item 1 should add allocation/reservation semantics or explicitly prove serialized ownership, because concurrent or interrupted runs can receive the same ID.
- “Rows number past the file max” and “numbering is day-scoped” do not define whether a new date resets the sequence; §20 item 1 should specify the exact `(date, section, family)` allocation scope and test a midnight boundary, because reruns spanning UTC dates can number differently across tools.
- The date component lacks a declared clock and rollover rule; §20 item 1 should derive it from one UTC start timestamp recorded with the run, because local-time and UTC callers can mint different identities for the same execution.
- Partial identifiers such as `PR3a`, `PR5n`, `PR11n`, and `PR15n` depart from the canonical numbered finding vocabulary without a defined grammar; §20 and the §19 cross-references should retain the original PR ID plus an explicit accepted/rejected sub-disposition, because ad hoc suffixes weaken collision checks and backlinks.
- Lineage rules do not explicitly prohibit cycles, forks, or two successors claiming the same predecessor; §20 item 4 should validate the lineage as a well-formed directed chain per review family, because unique node IDs alone do not guarantee coherent history.
- A minted run that crashes before writing its first marker has no defined terminal record and may later be reused; §20 item 1 or 4 should persist an allocated/abandoned state, because premium auditability includes attempts that fail before normal artifact creation.
- §17’s active checkpoint still asserts a known double-count exception and then retracts it in later prose; §19’s reconciliation item should move stale claims into an explicit historical note outside the canonical checkpoint, because readers and automation should not have to resolve contradictory status text.
- The section lacks a user-facing run inspection facility; a premium enhancement belongs in §20 as an `explain-run` query that resolves one ID to candidate, scope, findings, marker lineage, outage state, and verified artifacts, because operators need a single trustworthy audit view rather than manual joins.
```
