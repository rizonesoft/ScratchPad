# Review: D00 T01 §41 -- Bin Output Residuals

Candidate: `2941c85` plus `12d33a9` (SHIP plus the self-review fix). Full panel (shared contract surface: `Directory.Build.props`, CI workflows, `tools/` guards): Sol rounds 1-2, Opus round-3 sign-off. Item 3 (induced soak) runs as CI run `35451085849` beside the panel; its tick lands before the stamp.

Arch trigger: none - build layout plus CI guards plus docs; no editor, storage, extensibility, AI, protocol, or consent surface changes (the `Protocol.csproj` touch is a test-harness copy path, not a wire shape).

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. Two fixes, committed in `12d33a9`, re-asked clean.

Does it make sense: each shipped item maps to its code (pointers on §§1-2, stems plus conformance plus clean plus launcher in `tools/` with fixtures under `tests/Fixtures/`, manifest plus guard steps in `build.yml`, TFM legs dropped in props plus csproj plus CI plus docs). Exactly the contract; item 3's soak run is dispatched, not yet quoted.

Is it logical: the first pass found two gaps, fixed in `12d33a9`: the conformance path compare was case-sensitive, which false-fires on Windows drive-letter case, and the evaluation-failure diagnostic printed a list instead of the line. Considered and kept: file-stem equality for the project leg (an `MSBuildProjectName` override breaks the `Bin/<stem>` contract, so the probe bans it rather than tracking it), the denylist shape of the manifest foreign-output leg (47 projection dlls make an allowlist endless), and three documented fixture exclusions (each proven by green CI with the fixtures present). Re-asked clean.

Did it break anything else: the TFM drop is tree-wide by mechanism (props) with every named consumer moved (CI, docs, fixture copy; old-shape grep clean outside stamped history); Debug plus Release neutral builds green with zero TFM legs and Protocol 35/35; the `.gitignore` exception is path-scoped with root `Bin/` still ignored; SHIP CI run `35450767652` green on both jobs including all six new steps.

## GPT panel (round 1)

- `adversarial` needs-attention: (R1-F1) case-sensitive stem compare.
- `consistency` approve
- `integration` needs-attention: (R1-F2) legs uncompared to Configuration/RID; (R1-F3) manifest not recursive.
- `record` needs-attention: (R1-F4) manifest tick cited pre-proof, not branch-green.

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 72s; outcome needs-attention; tokens unknown

Capture note: the wrapped `run` swallows producer stderr (see the §39 record), so tokens read `unknown`; duration is the run-command wall clock.

Disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 673c305 |
| R1-F2 | fixed | 673c305 |
| R1-F3 | fixed | 673c305 |
| R1-F4 | fixed | 673c305 |

Fix record: stems compare casefolded with a case-variant fixture stub proving it; conformance legs must equal the evaluated Configuration plus RuntimeIdentifier exactly; the manifest enumerates recursively; the Done notes cite the branch-green build run. Fixed in `673c305` (stems green plus case fire, conformance green plus bad-output fire, yaml OK, validate 0 fatal); gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `0f240821f478a7c59fda06e6570d1373fccbb7065516f0bd8f4f4c21f34fcd08`; run `20260919-D00-T01-S41-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (1)
1. `tools/check-bin-layout.py:57-64` compares project stems case-sensitively, so names such as `Foo.csproj` and `foo.csproj` pass the guard but collide in `Bin/<Project>/` on Windows.

**consistency: approve**

**integration: needs-attention** (2)
1. `tools/check-bin-layout.py:98-108` accepts any one or two path segments after `Bin/<Project>/` without comparing them to the evaluated `Configuration` and `RuntimeIdentifier`; invalid layouts such as `Bin/Foo/net10.0/` or `Bin/Foo/Debug/net10.0/` therefore pass the claimed `Bin/<Project>/<Configuration>[/<RID>]` conformance guard.
2. `.github/workflows/build.yml:125-135` checks only top-level DLL, PDB, and foreign-project files because every `Get-ChildItem` call lacks `-Recurse`, while the uploaded directory includes descendants; nested foreign outputs can enter the artifact without failing the manifest check.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1101` marks the manifest item complete even though its contract requires the check green on this branch, while the recorded evidence only says its legs were pre-proven against a previous artifact rather than that the newly added branch check passed.
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 33s; outcome approve; tokens unknown

Disposition table (R1-F1 through R1-F4 stay fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 673c305 |
| R1-F2 | fixed | 673c305 |
| R1-F3 | fixed | 673c305 |
| R1-F4 | fixed | 673c305 |

Clean round: no new findings; the R1 fixes hold.

Raw round-2 output (fenced, not part of the verdict scan; digest `fc52c157b6f4a3154c61d8c9fddcca514347b90416974ddb51fe4de3dee22ecb`; run `20260919-D00-T01-S41-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Opus panel (round 3)

- `adversarial` advisory: (R3-F1) hardcoded foreign-stem list rots.
- `consistency` approve
- `integration` needs-attention: (R3-F2) launcher RID/name wrong off Windows.
- `record` needs-attention: (R3-F3) Done citations predate the fixed code; (R3-F4) trigger still in tree at review time.

Telemetry: round 3; model opus; effort medium; duration 204s; outcome needs-attention; tokens unknown

Disposition table (R1-F1 through R1-F4 stay fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 673c305 |
| R1-F2 | fixed | 673c305 |
| R1-F3 | fixed | 673c305 |
| R1-F4 | fixed | 673c305 |
| R3-F1 | fixed | 74c07cb |
| R3-F2 | fixed | 74c07cb |
| R3-F3 | fixed | 9771e99 |
| R3-F4 | fixed | 3ec7d6c |

Fix record: the manifest denylist derives from tracked stems minus the app's `ProjectReference` closure, so new projects join by existing (R3-F1, an advisory fixed in-loop since round 4 runs regardless for the needs-attention findings); the launcher defaults to win-`<arch>` plus `.exe` on every OS, and the Linux CI leg asserts the resolved path against the evaluated `OutputPath` instead of a hardcoded shape (R3-F2, proven consistent locally); the Done notes cite the covering build run `35451971220`, green on both jobs with all six new steps (R3-F3); the trigger reverted byte-exact (R3-F4). Fixed in `74c07cb` plus `3ec7d6c` plus `9771e99` (launcher consistent, yaml OK, validate 0 fatal, CI green); gates re-ran green before round 4.

Raw round-3 output (fenced, not part of the verdict scan; digest `0be46877f286b984b329a4e0a51a4ad945c687f0f208e893ef3cdee251d03a89`; run `20260919-D00-T01-S41-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: advisory** (1)
1. `.github/workflows/build.yml:128` pins the "no foreign project outputs" leg to a hardcoded stem list (`UI, Unit, Smoke, Protocol, AcpLoopback, CaptureBaseline, ForegroundLog, Notepad.Acp, Notepad.Agents`) rather than deriving it from the tracked csproj stems minus `ScratchPad`'s reference closure, so any project added after this commit leaks its output into `Bin/ScratchPad/Debug/win-x64/` and the uploaded artifact without the manifest check firing.

**consistency: approve**

**integration: needs-attention** (1)
1. `tools/launch.py:23,27-31` returns no RID leg and the extensionless name `ScratchPad` off Windows, but `src/ScratchPad/ScratchPad.csproj:8` sets `RuntimeIdentifier` to `win-<arch>` unconditionally, so the evaluated output on Linux is `Bin/ScratchPad/Debug/win-x64/` (verified: `dotnet msbuild src/ScratchPad/ScratchPad.csproj -getProperty:OutputPath` from Linux prints `.../Bin/ScratchPad/Debug/win-x64/`). The Linux assertion at `.github/workflows/build.yml:66` therefore pins `$PWD/Bin/ScratchPad/Debug/ScratchPad`, a path no build can ever produce, so the launcher's non-Windows resolution is wrong and the CI leg claiming to prove it passes vacuously.

**record: needs-attention** (2)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1096,1099,1100,1101` cite "branch-green on build run 35450767652" as the CI proof, but that run's `headSha` is `2941c85`, the first commit of the candidate range; the guards were then changed twice (`12d33a9`, `673c305`: casefolded stems, evaluated Configuration/RID leg comparison, `-Recurse` manifest scan) and `gh run list` shows no build run for either commit, so the cited evidence predates the code it is offered for and no CI run covers the shipped guards.
2. `resources/baseline/tolerance.json:7-8` still carries the deliberate golden-failure trigger from `d601618` ("revert next": `perPixelDelta` 1, `maxDifferentFraction` 0) at HEAD under this candidate, so the tree ships a knowingly red golden gate while item 3 ("the trigger reverted") remains open.
```

## Opus panel (round 4)

- `adversarial` approve
- `consistency` approve
- `integration` needs-attention: (R4-F1) Windows docs say `python3`, prerequisite provides `py`.
- `record` approve

Telemetry: round 4; model opus; effort medium; duration 128s; outcome needs-attention; tokens unknown

Disposition table (R1-F1 through R3-F4 stay fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 673c305 |
| R1-F2 | fixed | 673c305 |
| R1-F3 | fixed | 673c305 |
| R1-F4 | fixed | 673c305 |
| R3-F1 | fixed | 74c07cb |
| R3-F2 | fixed | 74c07cb |
| R3-F3 | fixed | 9771e99 |
| R3-F4 | fixed | 3ec7d6c |
| R4-F1 | fixed | 3301a9d |

Fix record: Windows doc commands use the `py` launcher the prerequisite provides (run line plus bootstrap step 6 outright, clean paragraph split by OS); every remaining `python3` in the touched docs sits beside Linux. Fixed in `3301a9d` (sweep clean); gates re-ran green before round 5.

Raw round-4 output (fenced, not part of the verdict scan; digest `5d72b3f2f3cf1eaf80380a8ae890b9c00cb44c9b3173909c9060da3c69fbdb61`; run `20260919-D00-T01-S41-opus-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**

**consistency: approve**

**integration: needs-attention** (1)
1. `docs/bootstrap.md:25` and `docs/build.md:31` (also `docs/build.md:19`) instruct Windows operators to run the new launcher and clean script as `python3 tools/launch.py` / `python3 tools/clean-bin.py`, but `docs/bootstrap.md:12` states the Windows Python prerequisite as `py --version` (python.org or `winget install Python.Python.3`), which installs `python.exe`/`py.exe` and no `python3.exe`; on such a box `python3` hits the Windows Store app-execution alias instead of the interpreter. The repo's own hook at `tools/githooks/pre-commit:9` proves the point by probing `python3 python py` in order. The previous step 6 launched the exe directly, so the clean-clone Windows launch path (item 7's "works from a clean clone on Windows") is now documented with a command the documented prerequisite does not provide.

**record: approve**
```

## Opus panel (round 5)

- `adversarial` approve
- `consistency` advisory: (R5-F1) prerequisite row understates Python's role.
- `integration` approve
- `record` approve

Telemetry: round 5; model opus; effort medium; duration 134s; outcome advisory; tokens unknown

Round-5 bar disposition: R5-F1 is below bar (doc-wording advisory on a prerequisite row), so it files at D00 T06 §1 and the stamp proceeds. No blocking leftovers; the run continues.

Final disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 673c305 |
| R1-F2 | fixed | 673c305 |
| R1-F3 | fixed | 673c305 |
| R1-F4 | fixed | 673c305 |
| R3-F1 | fixed | 74c07cb |
| R3-F2 | fixed | 74c07cb |
| R3-F3 | fixed | 9771e99 |
| R3-F4 | fixed | 3ec7d6c |
| R4-F1 | fixed | 3301a9d |
| R5-F1 | filed | D00 T06 §1 |

Architecture gate: not triggered (`Arch trigger: none`, see header).

Raw round-5 output (fenced, not part of the verdict scan; digest `1ca1c9911b67c214ba0223fe84452f7480633cf63ec1ab952584b11b8d87e6be`; run `20260919-D00-T01-S41-opus-r3` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**

**consistency: advisory** (1)
1. `docs/bootstrap.md:12` still scopes the Windows Python prerequisite as "for the pre-commit TODO gate", but after this candidate step 6 at `docs/bootstrap.md:25` (and `docs/build.md:19,31`) makes Python the only documented way to launch the stub and prune `Bin/`, so the prerequisite row understates what a clean Windows clone needs Python for.

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §41 plus §40 plus D00 T06 §1, 2026-09-19. Neighbors: §40 (Depends plus XREF). Dependents: D00 T06 §1 by the mechanical rule (Depends plus XREF consumer; recorded in the run's dependents file). Runner note: `run plan` with the codex high-effort producer reading the prompt on stdin; no new CLI drift. Output `check-plan PASS` (24 findings, one per line). Run note: the plan run shares its base with the panel runs (`20260919-D00-T01-S41-gpt-r3`, minter-walked past the panel runs); kind distinguishes them and the minter is authoritative. Review-scope note: the prompt carried section bodies only, not Implementation Order rows, so the T06 Depends edge was invisible to the reviewer (PR23's false premise). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §41, D00 T01 §40, D00 T06 §1]; dependents [D00 T06 §1]; bytes 11595; run 20260919-D00-T01-S41-gpt-r3

Ledger (namespaced IDs per the §16 shape; 11 of 24 filed at D00 T06 §1 plus D00 T07 §1, 2 accepted in the stamp run, 1 duplicate, 10 rejected with reasons on their rows):

Triage: 11 filed findings ride 10 items (PR1 plus PR2 share the pointer item at D00 T06 §1).

Ledger:
- [D00-T01-S41-PR1] [minor] §40 lacks a pointer to §41 -> filed D00 T06 §1
- [D00-T01-S41-PR2] [minor] Pointer item should cover §40 -> filed D00 T06 §1
- [D00-T01-S41-PR3] [minor] Multi-TFM future collides silently -> filed D00 T07 §1
- [D00-T01-S41-PR4] [minor] Stems must compare case-insensitively -> rejected already satisfied: R1-F1 casefolded the compare with the FOO.csproj case-variant fixture
- [D00-T01-S41-PR5] [minor] Guard should check evaluated names -> rejected already satisfied: an MSBuildProjectName override fires the conformance probe as a shape violation, recorded design
- [D00-T01-S41-PR6] [minor] Probe belongs on Windows CI too -> filed D00 T07 §1
- [D00-T01-S41-PR7] [minor] Matrix should cover configs and RIDs -> duplicate D00-T01-S40-PR5
- [D00-T01-S41-PR8] [minor] No-bin check misses case variants -> rejected already satisfied: the probe matches `bin` case-insensitively
- [D00-T01-S41-PR9] [minor] Clean leaves stale TFM legs -> filed D00 T07 §1
- [D00-T01-S41-PR10] [minor] Clean lacks safety cases -> filed D00 T07 §1
- [D00-T01-S41-PR11] [minor] Launcher selection nondeterministic -> rejected design is computed-path: fixed defaults with no probing, so nothing to disambiguate
- [D00-T01-S41-PR12] [minor] Launcher may run obsolete binary -> rejected no recursive resolver exists; the path is computed, so old layouts are never candidates
- [D00-T01-S41-PR13] [minor] Clean-clone-no-exe undefined -> filed D00 T07 §1
- [D00-T01-S41-PR14] [minor] Launcher must propagate process contract -> rejected exec design satisfies args, cwd, exit code, and signals by construction
- [D00-T01-S41-PR15] [minor] Soak proof needs maintained fixture -> filed D00 T07 §1
- [D00-T01-S41-PR16] [minor] Manifest must pin exact commit -> rejected already satisfied: exact `0.0.0+$env:GITHUB_SHA` equality in build.yml
- [D00-T01-S41-PR17] [minor] Artifact contract too vague -> rejected denylist plus launch smoke jointly cover omission and contamination; an exact allowlist over 47 projections is brittle, recorded design
- [D00-T01-S41-PR18] [minor] Commit item lacks candidate range -> accepted range recorded on the item in the stamp run; no TODO work
- [D00-T01-S41-PR19] [minor] Layout manifest wanted -> rejected premature: the PR21 drift-check filing guards the duplication without a manifest rewrite
- [D00-T01-S41-PR20] [minor] Consumer listings will rot -> accepted stable-anchor criteria in the stamp run; no TODO work
- [D00-T01-S41-PR21] [minor] Drift check wanted -> filed D00 T06 §1
- [D00-T01-S41-PR22] [minor] Version, invocation, plus Windows proof wanted -> filed D00 T07 §1
- [D00-T01-S41-PR23] [minor] T06 dependency not in graph -> rejected already satisfied: the Depends edge is in T06's order table; the prompt carried bodies only
- [D00-T01-S41-PR24] [minor] Unified operator command wanted -> filed D00 T07 §1
End of ledger

Raw plan-review output (fenced, not part of any scan):

```
- §40 needs a dated supersession pointer to §41 because its verified layout still promises a `<tfm>` directory; without the reverse pointer, readers following the stamped section receive obsolete instructions.
- §41’s pointer item belongs in §40 as well as §§1–2 because the item currently says §40 governs the earlier sections but never records that §41 subsequently changes §40’s own output shape.
- §41’s conformance guard must reject multi-targeted projects or restore TFM folders automatically because `AppendTargetFrameworkToOutputPath=false` turns any future `TargetFrameworks` addition into silent output collisions.
- §41’s project-name uniqueness guard must compare names case-insensitively because stems such as `Editor` and `editor` collide on the Windows filesystem even if a Linux CI check considers them unique.
- §41’s uniqueness guard should validate evaluated `MSBuildProjectName` values rather than only csproj stems because `BaseOutputPath` is formed from the evaluated property and that is the actual collision boundary.
- §41’s conformance probe belongs on Windows CI as well as Linux because evaluating Windows projects from Linux cannot expose OS-conditioned MSBuild properties or Windows path-normalization collisions.
- §41’s conformance matrix should explicitly cover Debug, Release, RID and non-RID builds because checking one default evaluation does not prove the promised `Bin/<Project>/<Configuration>[/<RID>]` shape.
- §41’s no-stray-output check should detect case variants such as `bin`, `BIN`, and `Bin` beneath project directories because the product targets a case-insensitive filesystem while the stated Linux check may miss them.
- §41’s clean script must remove obsolete `<Configuration>/<tfm>/` directories inside still-valid project folders because pruning only defunct project directories leaves the exact residuals created by this migration.
- §41’s clean-script tests need symlink, junction, path-traversal, repository-root, and dry-run safety cases because a cleanup tool that recursively removes derived output must prove it cannot escape `Bin/` or delete user data.
- §41’s launcher needs deterministic configuration and RID selection rules plus an ambiguity error because simultaneous Debug, Release, RID, and stale pre-migration executables can otherwise make “current exe” nondeterministic.
- §41’s launcher tests should include coexisting old `<tfm>` and new layouts because the migration’s most likely residue could cause a recursive resolver to launch an obsolete binary while still reporting success.
- §41 must define what “works from a clean clone” means when no executable exists, preferably build-and-run or an actionable build command, because a resolver cannot launch an artifact that has not yet been built.
- §41’s launcher should propagate arguments, working directory, child exit code, and Ctrl+C correctly because a one-command entry point becomes a real operator interface rather than merely a path lookup.
- §41’s soak proof needs a maintained failure-path fixture in CI because one historical induced failure only proves the upload glob once and future workflow edits can silently break diagnostic collection again.
- §41’s smoke manifest check should compare embedded provenance with the exact candidate commit, not merely require a full-SHA-shaped value, because a stale binary can satisfy the format while misrepresenting the reviewed source.
- §41’s artifact contract should name the exact required application files and permitted project-produced assemblies because “exe, dlls, symbols, and no foreign outputs” is too vague to detect omissions or distinguish dependencies from contamination.
- §41’s completed commit item needs the implementation commit or candidate range recorded beside its CI run because the current evidence cannot establish that run `35451971220` tested the exact bytes presented for review.
- §41 should introduce one checked, machine-readable output-layout manifest consumed by CI, the launcher, cleanup, and documentation because duplicating path logic across YAML, Python, fixtures, and prose invites another drift round.
- D00 T06 §1 should replace brittle file-and-line consumer listings with stable file anchors or command identifiers because ordinary documentation edits will otherwise make an accurate prerequisite audit immediately stale.
- D00 T06 §1 needs an automated drift check mapping live Python consumers to the prerequisite table because a one-time prose audit does not prevent the launcher or cleaner from becoming undocumented again.
- D00 T06 §1 should specify the supported Python version and Windows invocation (`py -3` versus `python`) and verify both scripts in a clean Windows job because merely adding “Python” to a table does not make the documented launch and cleanup commands usable.
- D00 T06 §1’s dependency on D00 T01 §41 should be asserted in the canonical Implementation Order graph, not only through review prose and XREFs, because execution ordering must remain machine-enforced.
- A premium follow-up belongs after §41 to provide a single `build`, `run`, and `clean` operator command with diagnostics showing the selected configuration, RID, executable, and provenance because users should not need to understand the internal `Bin/` topology.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `b99c34c`: `todo-graph self-test: 1088 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `b99c34c`: `28 todos, 239 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --sync` on `b99c34c`: `synced 234 row(s) -- 79 complete (34%), 0 box(es) changed`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `b99c34c`: `implementation plan is current -- 79/234 sections complete (34%)`, exit 0.

Provenance: candidate b99c34ca288493f0c4647e7352a7d5a1512ecadb; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --sync && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 968e29a0de3abb5c8f7df98ac576661c7ffcd88661d85e5edbfc5bcd6721c1c0; path .claude/skills/review-todo-section/SKILL.md; run 20260919-D00-T01-S41-gpt-r3

(Digest over the self-test line, the validate line, the sync line, and the plan line, joined by newlines with a trailing newline. Observed in a clean worktree at the candidate; the sync writes ignored projections only and the tracked tree verified clean.)

