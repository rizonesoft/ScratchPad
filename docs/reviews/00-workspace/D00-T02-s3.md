# Review: D00 T02 §3 -- Golden Capture Store and Refresh

Candidate: `bf39eb8` (store) plus in-candidate fixes `9501c57` (serialize UI tests), `4d459fd` (failure artifacts), `e9414bf` (CI theme and fit), `8d866b7` (delta 96), `dbcd869` (refresh shakedown). Five fix cycles, all from CI or measurement evidence; lenses clean.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Baseline store | 4 stock captures (Notepad 11.2607.14.0 on 25H2, versions in filenames and README), all eyeballed; `tools/CaptureBaseline` with DPI-canonical output, verified placement, topmost pin, and thread-awareness handling |
| 2. Comparison | `GoldenComparer` plus `tolerance.json` (delta 96, 0.1%) plus 2 tests; 10px shift fails locally (0.34%, C# matches an independent PIL implementation to the pixel) and the suite passes in CI (run `34804426231`, UI 3/3) |
| 3. Refresh | Procedure in the README; shakedown 800x600 to 900x650 with reviewed diff (size-only, slivers within crop), local green, and CI green (run `34805175346`) |
| 4. Versioned | Everything under `resources/baseline/` (`stock/`, `app/`, README, `tolerance.json`); all scratch captures outside the repo deleted |
| 5. Commit | `bf39eb8`, subject matches the item exactly |

Runs: `34801269448` red (parallel interference), `34802083920` red (light theme plus off-screen window), `34802802025` red with artifacts (diagnosis run), `34803666690` red at 0.164% (rasterization noise over budget), `34804426231` green both jobs with UI 3/3, `34805175346` green on the refresh. `main` is green at HEAD.

## Self-review

1. Does it make sense? Yes. Every item has its change; ImageSharp 4.x was rejected on license evidence (commercial-key warning) for `System.Drawing.Common` 10.0.12; the DPI, occlusion, parallelism, theme, screen-size, and rasterization issues were each found by evidence and fixed in-candidate.
2. Is it logical? Yes. The canonical-logical pipeline (registry DPI scale, verified SetWindowPos placement, bicubic downscale) is identical in tool and test; the crop drops the versioned title bar plus measured invisible borders; delta 96 sits between fringe and content physics per the CI-pixel histogram; the 0.1% threshold clears noise 3x and catches the shift near 2x.
3. Did it break anything else? No. `build.yml` gained two Windows-only steps with Linux green throughout; `UISmoke` took one collection attribute (justified: §3's new tests interfered with it); `validate` 0 fatal; `self-test` 391/391.

## Lenses

- `adversarial`: approve. Captures never touch user windows (HWND-diff for Notepad, owned processes only, the running user Notepad verified alive throughout); no secrets in captures; failure artifacts are test outputs uploaded on failure only.
- `consistency`: approve. Pins and lockfiles throughout; naming matches the suites; Markdown one line per item; no em dashes.
- `integration`: approve. Both solution consumers green; stock captures routed to D06 T01 §3 by the spike doc; the refresh procedure names its users; the failure-artifact upload serves all future UI work.
- `record`: approve. Six run IDs plus local measurements quoted; the PIL cross-check and the delta histogram recorded; zero deferrals.
- `source-defect`: approve. The four stock captures were eyeballed against live Notepad 11.2607.14.0 with source builds named; no behavior claims beyond the pixels.
- `design`: not owed (test infrastructure only, no app surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; full suites green on both OSes locally and in CI; `plan --check` green after the stamp-flow sync.
