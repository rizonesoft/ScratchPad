# Review: D00 T01 §9 -- Opus Panel Enforcement in the Validator

## Evidence map

Round-1 candidate `6de1d9a` carried all 5 items: SEVERITY_MAP class plus README row (item 1), rule 16 in `todo-validate.py` (item 2), 7 self-test cases with fixtures (item 3), suite green at 400 with AGENTS.md count and live-tree silence (item 4), the commit itself (item 5). Round-2 candidate `f405c7a` answered round 1 (decoy move, cutoff placement, fail-closed undated, record format in skill/README, item-2 box, live drives). Round-3 candidate `8ea92aa` answered round 2 (findings rewritten in the mandated shape, heading regex narrowed to level-2+ starting words, cutoff comment, undated-branch note). Round-4 candidate `b922ded` answered round 3 (last panel section wins, two multi-round fixtures, skill/README state last-wins). Round-5 candidate `2305e87` answered round 4 (fence strip, level 2-6 headings, level 1-6 terminator, three mutation-proven fixtures, count 405). Evidence for each fix is quoted under its panel record.

## Self-review

Sense: the rule enforces exactly what the skill now requires (panel verdicts on record for post-cutoff stamps) with a date grandfather that keeps the live tree silent; no behavior beyond the check. Logic: findings path from the `Review:` line, existence, heading, in-section per-line verdicts; cutoff beside the other cutoffs with fail-closed undated handling. Breakage: none (additive FATAL class; live tree silent; suite green). No fixes in self-review; the panel found four real ones below.

## Opus panel (round 1, candidate `6de1d9a`)

**adversarial: needs-attention**: the panel-section truncation was untested (deleting it kept the suite green) and the decoy sat in the clean fixture where it changed nothing. Fixed: decoy moved into the partial fixture (missing `record` in-panel, present outside); mutation re-run goes 1 failed with truncation removed, 400/400 with it restored.

**consistency: needs-attention**: cutoff declared inline instead of beside the other cutoffs, and the undated-stamp branch failed open against the file's own convention. Fixed: `PANEL_CUTOFF` beside `FIDELITY/FILTER_ACK_CUTOFF`; undated stamps now evaluate (fail closed). `pre_convention()` itself not reused: it conjoins row-status `[x]` while the stamp is the claim here; the divergence is documented at the rule.

**integration: needs-attention**: the skill and README never stated the record format the rule demands (`Raw findings:` path, per-line lens verdicts). Fixed: both state it; existing §13 findings already match.

**record: needs-attention**: item 2 ticked-box missing under its Done note, and "live silent" vacuous (no post-cutoff stamp existed). Fixed: box ticked; two live drives run and reverted clean (post-cutoff paneled stamp silent, unpointed stamp 1 fatal).

## Opus panel (round 2, candidate `f405c7a`)

**adversarial: approve**: round-1 fixes verified by re-running the mutation and the live drives. No new issues.

**consistency: advisory**: (1) `PANEL_CUTOFF` sits inside the ack-mechanism comment while gating a FATAL: fixed with a disambiguating clause. (2) The undated branch is unreachable under today's parser: kept as documented defense (parser invariant cited), no case covers it by construction.

**integration: approve**: skill and README state the format; the documented shape is a strict subset of what the regex accepts, so compliant authors cannot trip it.

**record: needs-attention**: the §9 findings file itself failed its own rule (prose panel summary instead of per-line verdicts). Fixed: panel section rewritten in the mandated shape. The fix exposed a second, deeper rule bug the panel's measurement caught: the heading regex matched the `# Review:` title (which mentions the Opus panel) and sliced the verdicts away. Fixed: heading must be level 2+ starting with the words, with a panel-naming fixture title locking it. Self-application probe (temp post-cutoff stamp at the §9 findings) now silent except the probe's own INDEX scaffolding FATAL; probe removed.

## Opus panel (round 3, candidate `8ea92aa`)

**adversarial: needs-attention**: rule read the FIRST panel section, so multi-round files validated the superseded round. Fixed: LAST section wins, with two fixture directions (stale-clean fires, stale-gap silent); suite 402/402.

**consistency: approve**: cutoff comment and undated-branch note confirmed.

**integration: needs-attention**: skill/README never stated the level-2+-starting-words heading contract. Fixed: both state it plus last-wins; skill also notes the latest round is the record.

**record: needs-attention**: round-2 dash fix uncommitted (committed herewith) and Evidence map stale (updated to cover all candidates).

## Opus panel (round 4, candidate `b922ded`)

**adversarial: needs-attention**: heading scan fence-blind (a fenced `## Opus panel (round 2)` quote displaced the real panel under last-wins) and the `^#{1,4}` terminator let a `##### Leftover notes` tail supply lens verdicts. Fixed: fenced blocks (backtick and tilde) stripped before the scan, heading levels widened to 2-6, terminator to any level 1-6; three fixtures (fenced quote passes, level-5 tail fires naming both lenses, level-5 heading passes), each mutation-proven (strip/terminator/regex reverted singly, exactly its case fails); suite 405/405.

**consistency: approve**: round-2 advisories still fixed, count claims line up. One panel citation corrected in transcription: the verdict named `AGENTS.md:234`, but the file has 72 lines; the second 402 claim is `TODO-01:234`, now 405 with `AGENTS.md:54`.

**integration: needs-attention**: docs said level-2 (or deeper) but the regex capped at level 4, so a `##### Opus panel` section false-fired. Fixed: regex accepts levels 2-6, matching the documented contract; level-5 heading fixture locks it.

**record: advisory**: Evidence map stops one candidate short of the one reviewed (accepted: a commit cannot cite its own hash). Round-5 candidate (this commit) answers round 4: fence strip, level 2-6 headings, level 1-6 terminator, three fixtures, count 405 in suite/AGENTS/TODO, skill and README note the fence strip.

## Opus panel (round 5, candidate `2305e87`, FINAL)

**adversarial: advisory**: all three round-4 fixes mutation-proven on the live tree (strip/terminator/regex reverted singly, exactly each case fails); 405/405, validate 0 fatal 0 warnings. Residual, pre-existing, not a regression: unheaded prose after an incomplete panel still supplies verdicts (including via a fence-hidden heading), and an unbalanced fence misreports as a missing panel. Filed at D00 T01 §10 per the stop-and-re-think clause, not patched a fourth time.

**consistency: advisory**: counts reconcile (405 in suite, AGENTS.md:54, TODO-01:233-234 with the 1+6+5 decomposition exact). Gap: the fenced-quote fixture only exercises the displaces direction, never fence-only satisfies (probed correct today, coverage only). Filed at D00 T01 §10.

**integration: approve**: levels 2-6 match skill ("level-2 (or deeper)") and README ("level-2+"); §11 locks it; fence strip stated in both docs in matching terms.

**record: advisory**: Evidence map ended at `8ea92aa`; now names `b922ded` and `2305e87` (map closed, this transcription is the last entry). No em dashes in added text; round-4 citation self-corrected.

## Gates re-run

`self-test` 405/405, live `validate` 0 fatal 0 warnings, `plan --check` current. Panel rounds used: 5 of 5 (adversarial/consistency/record advisory, integration approve, zero needs-attention at close). Leftovers filed at D00 T01 §10; stamp carries them in its `Review:` line.
