# Review: D00 T01 §9 -- Opus Panel Enforcement in the Validator

## Evidence map

Round-1 candidate `6de1d9a` carried all 5 items: SEVERITY_MAP class plus README row (item 1), rule 16 in `todo-validate.py` (item 2), 7 self-test cases with fixtures (item 3), suite green at 400 with AGENTS.md count and live-tree silence (item 4), the commit itself (item 5). Round-2 candidate (fix commit) applies the panel's four advisories-turned-findings; evidence for each fix is quoted under the panel record.

## Self-review

Sense: the rule enforces exactly what the skill now requires (panel verdicts on record for post-cutoff stamps) with a date grandfather that keeps the live tree silent; no behavior beyond the check. Logic: findings path from the `Review:` line, existence, heading, in-section per-line verdicts; cutoff beside the other cutoffs with fail-closed undated handling. Breakage: none (additive FATAL class; live tree silent; suite green). No fixes in self-review; the panel found four real ones below.

## Opus panel (round 1, candidate `6de1d9a`)

All four lenses `needs-attention`, each verified against the code before fixing (none blind-applied):

- `adversarial`: the panel-section truncation was untested (deleting it kept the suite green) and the decoy line sat in the clean fixture where it changed nothing. Fixed: decoy moved into the partial fixture (missing `record` in-panel, present outside); mutation re-run goes 1 failed with truncation removed, 400/400 with it restored.
- `consistency`: cutoff declared inline instead of beside the other cutoffs, and the undated-stamp branch failed open against the file's own convention. Fixed: `PANEL_CUTOFF` beside `FIDELITY/FILTER_ACK_CUTOFF`; undated stamps now evaluate (fail closed). `pre_convention()` itself not reused: it conjoins row-status `[x]` while the stamp is the claim here; the divergence is documented at the rule.
- `integration`: the skill and README never stated the record format the rule demands (`Raw findings:` path, per-line lens verdicts). Fixed: both state it; existing §13 findings already match.
- `record`: item 2 ticked-box missing under its Done note, and "live silent" vacuous (no post-cutoff stamp existed). Fixed: box ticked; two live drives run and reverted clean (post-cutoff paneled stamp silent, unpointed stamp 1 fatal).

## Gates re-run

To be quoted at stamp time: `self-test` 400/400 after the fixes, live `validate` 0 fatal, plus panel round 2 on the fix candidate.
