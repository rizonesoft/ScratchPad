# Review: D00 T01 §23

Candidate: `b8f0bb8` (ship) + `218e4f9` (self-review) + `119ba85` (R1 fix) + `17b5b41` (R2 fix) + `3c7b0fb` (R3 fix). Full panel (scripts plus skill plus validator: shared contract surface).

## Live proof

Gates on `3c7b0fb`. Digests are sha256 over the quoted bytes, each line LF-terminated.

`python3 scripts/todo-graph.py self-test`:

```
todo-graph self-test: SKIP sibling stamp-gate clamp probe (no section_commit_gate.py)
todo-graph self-test: 737 cases, 0 failed
```

Provenance: candidate 3c7b0fb; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest b0dd478962c0ac6817e3fc00dc45a77c6d144f1b054b5b877f10b308b76db71c; path docs/reviews/00-workspace/D00-T01-s23.md; run 20260918-D00-T01-S23-gpt

`python3 scripts/todo-graph.py validate`: `22 todos, 200 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate 3c7b0fb; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest 24c05b86641d769579ebaa2ba429f3ff9d4ac7ca1d0042679770eaf6febbfecf; path docs/reviews/00-workspace/D00-T01-s23.md; run 20260918-D00-T01-S23-gpt

`python3 scripts/todo-graph.py plan --check`: `implementation plan is current -- 62/195 sections complete (32%)`, exit 0.

Provenance: candidate 3c7b0fb; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest ced9c3367465ed4f81085b302d4fa010cd15ad45c90e0bc05a54e384bc6f4204; path docs/reviews/00-workspace/D00-T01-s23.md; run 20260918-D00-T01-S23-gpt

`python3 scripts/todo-graph.py query plan-health --check`: `gate: ok`, exit 0.

Provenance: candidate 3c7b0fb; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 83e9a060459d5921fedae881c1e079a0169941dd7fcff74fabc57c3188d0943b; path docs/reviews/00-workspace/D00-T01-s23.md; run 20260918-D00-T01-S23-gpt

## Opus panel (round 1)

Candidate: `b8f0bb8` + `218e4f9` (checked `fence` pipeline; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` needs-attention
- `record` approve

Findings (all verified true, all fixed in `119ba85`): adversarial-1 unbounded stdin read plus full-copy byte measure defeat the caps at the CLI entry (bounded read plus chunked early-exit measure); adversarial-2 `git cat-file -e` exits 128 for absent shorts, collapsing the None/False distinction (`rev-parse --verify --quiet` distinguishes: 0/1/128, probed); consistency the skill's count instruction lacks the 4-digit cap and the over-long failure names neither count nor cap (instruction plus specific error); integration None-candidate FATALs on gitless exports against the AGENTS anywhere-with-Python-3 rule (unprovable skips, rule-22 precedent). Fix: `119ba85`.

Raw round-1 output (fenced, not part of the verdict scan; digest `2f69381b5e99ac963ecc72ebf0065ea4a35b53f49c3ab45ed784d10a25d988d3`):

```
**adversarial: needs-attention (2)**
1. `scripts/review_prompt.py:315` -- `sys.stdin.read()` materializes the entire reviewer output before `_output_within_bounds` (`:111`) can reject it, so the new caps bound nothing at the entry point the section's checkpoint names ("hostile output exhausts nothing"). A 10 GiB `check-panel` stdin exhausts memory before the 1 MiB check runs; `scripts/review_prompt.py:112` then doubles peak memory with a full `text.encode("utf-8")` copy of whatever did get read. Read at most `OUTPUT_MAX_BYTES + 1` bytes, and measure bytes without a second full copy.
2. `scripts/todo-graph.py:1400-1410` -- `git cat-file -e <short-sha>` exits **128**, not 1, for a well-formed-but-absent short sha (`git cat-file -e deadbee; echo $?` -> 128 on git 2.43.0); only a full 40-hex absent object exits 1. Since every real candidate in this repo is a 7-hex short (`candidate fea3412`), `git_resolves` returns `None` for a bogus candidate, so the `resolves to nothing` branch at `scripts/todo-validate.py:1275-1279` is unreachable in practice and every bad candidate reports as "unprovable (git cannot resolve it)". The fixture at `scripts/todo-graph.py:7599` cans `"deadbee": False`, a value real git never returns for that input, so the check at `scripts/todo-graph.py:8319` locks a path the code cannot take.

**consistency: needs-attention (1)**
1. `.claude/skills/review-todo-section/SKILL.md:92` -- the reviewer-facing count grammar still reads "ASCII digits with no sign, space, or leading zeros" with no digit cap, while `scripts/review_prompt.py:36-37` now caps counts at 4 digits. A reviewer obeying the instruction verbatim with a 5-digit count has its verdict line silently demoted to a detail line, and the self-test at `scripts/todo-graph.py:9467-9476` pins the resulting message as `line 1 precedes the first verdict: **adversarial: needs-attention (99999999999999999999)**`, which names neither the count nor the cap. The instruction and the failure text both need the bound.

**integration: needs-attention (1)**
1. `scripts/todo-validate.py:1254-1284` -- rule 23 now makes `todo-graph.py validate` depend on a working `git` that can resolve the recorded candidate: `git_resolves` returns `None` when git is absent, when the exec fails, or when the object is missing from a shallow/partial clone (`scripts/todo-graph.py:1396-1410`), and `None` flags FATAL at `:1281-1284`. Rule 21 sets the opposite precedent for the same unprovability (`scripts/todo-validate.py:1159`, "Uncommitted findings skip: without history nothing is provable"), and `AGENTS.md` states the TODO tooling runs anywhere with Python 3. The break is latent only because every current record is pre-cutoff; the first stamp dated after 2026-09-18 (this section) makes `validate` on a no-`.git` export or shallow clone emit a FATAL per provenance line, with no `--no-git` degradation path.

**record: approve**
```

## Opus panel (round 2)

Candidate: prior two plus `119ba85` (checked `fence` pipeline, R1 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings (all verified true, all fixed in `17b5b41`): adversarial `rev-parse --verify` exits 0 for an absent full-length hex (no existence check), silently passing the most precise candidate form (`^{object}` peel: absent short/full exit 1, present exit 0, no-git 128, all probed); consistency the §19 count-grammar row still records arbitrary length plus fails-by-tally (dated supersession note appended, family swept). Fix: `17b5b41`.

Raw round-2 output (fenced, not part of the verdict scan; digest `ef382413d3cb3e57e60014efb3276f8acd06f43675d73e43845706456159aa93`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:1404` -- `git rev-parse --verify --quiet <sha>` exits **0** for a full 40-hex string naming no object in the repo (probed on git 2.43.0: `git rev-parse --verify --quiet 0123456789012345678901234567890123456789` prints the sha, exit 0, while `git cat-file -e` on the same input exits 1). `PROVENANCE_RE` (`scripts/todo-graph.py:1175`) accepts `candidate \S+`, so a full-sha candidate is a legal record shape, and R1 traded the short-sha misreport for a silent pass on the most precise candidate form: `git_resolves` returns `True`, and the `resolves to nothing` branch at `scripts/todo-validate.py:1280-1284` never fires for a mistyped or foreign full sha. Peeling the rev fixes both states at once (`<sha>^{object}` exits 1 for the absent full hex and the absent short, 0 for `119ba85` and its full form).

**consistency: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:541` -- the shipped PR5 count-grammar row still records the accepted grammar as "ASCII digits only, no sign, no whitespace, no leading zeros ... zero allowed, **arbitrary length** (the cross-check bounds it semantically)", which this candidate falsifies: `_COUNT_MAX_DIGITS` caps the grammar at 4 digits (`scripts/review_prompt.py:36-37`) and `_PANEL_LONG_COUNT_RE` (`:55-59`) now rejects a 5-digit count with a bound-naming error rather than letting it reach the tally. The §23 row at `:631` and `.claude/skills/review-todo-section/SKILL.md:92` carry the cap; the §? row that states the grammar does not, so the file reads both ways. Amend the row (not a rewrite) the way §23's own rows were amended.

**integration: approve**

**record: approve**
```

## Opus panel (round 3)

Candidate: prior three plus `17b5b41` (checked `fence` pipeline, R1-R2 output appended; output `check-panel PASS`).

- `adversarial` approve
- `consistency` needs-attention
- `integration` needs-attention
- `record` approve

Findings (all verified true, all fixed in `3c7b0fb`): consistency the §23 provenance row records bare rev-parse plus the three-state rationale with no R2 peel note (command plus note updated); integration the :541 edit left the plan projection stale (`plan --check` red; re-synced, current). Fix: `3c7b0fb`.

Raw round-3 output (fenced, not part of the verdict scan; digest `e615a77d4b67ba6e05344b8b35d48c2398cbe1e22e2b489d15083021cf8837e2`):

```
**adversarial: approve**

**consistency: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:631` -- the §23 provenance-shape row still records the check as "a git-resolving candidate (`rev-parse --verify --quiet`, shorts legal like elsewhere)" and carries an R1 correction note asserting "rev-parse carries the three states", which this candidate falsifies: bare `rev-parse --verify --quiet` exits 0 for an absent full 40-hex (probed, git 2.43.0), which is why `scripts/todo-graph.py:1404` now peels with `^{object}`. Every other claim changed by a review round in this file carries a dated note (`:631` two `**Corrected 2026-09-18 (review R1):**` notes, `:632` one, `:541` the `**Superseded 2026-09-18 (D00 T01 §23):**` note added by this very commit); the R2 peel is the only fix of the family with no note, so the row's recorded command and its stated three-state rationale both read as the pre-R2 behavior.

**integration: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:541` -- the row edit shipped without re-deriving the plan projection, so `python3 scripts/todo-graph.py plan --check` now exits 1 (`::error::build/todo-operator.json is stale -- run python3 scripts/todo-graph.py plan --sync`) and `python3 scripts/todo-graph.py validate` ends its summary with "operator JSON stale"; `build/todo-operator.json` (mtime 20:08) predates this commit (20:22) and contains neither the new supersession note nor the amended row text. `AGENTS.md` lists `plan --check` as a gate and the section lifecycle ends "then sync the plan", so the candidate leaves a repo gate red.

**record: approve**
```

## Opus panel (round 4)

Candidate: prior four plus `3c7b0fb` (checked `fence` pipeline, R1-R3 output appended; output `check-panel PASS`).

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Final round, all approve, no findings.

Raw round-4 output (fenced, not part of the verdict scan; digest `fc52c157b6f4a3154c61d8c9fddcca514347b90416974ddb51fe4de3dee22ecb`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Plan review

Manifest: sections [D00 T01 §23, D00 T01 §19, D00 T01 §20, D00 T01 §22, D00 T01 §17]; dependents [D00 T01 §19, D00 T01 §20, D00 T01 §22]; bytes 51820; run 20260918-D00-T01-S23-gpt

Ledger (namespaced IDs per the §16 shape; 14 of 22 filed across §§33-34 plus §24 and §31 joiners, 7 rejected, 1 duplicate, with reasons on their rows):

Ledger:

- [D00-T01-S23-PR1] [minor] Silent skip needs a degraded state -> filed D00 T01 §33
- [D00-T01-S23-PR2] [minor] Candidate type unpinned -> filed D00 T01 §33
- [D00-T01-S23-PR3] [minor] Short IDs never canonicalize -> filed D00 T01 §33
- [D00-T01-S23-PR4] [minor] Path unbound to candidate tree -> filed D00 T01 §33 (path-at-candidate half; blob-ID half rejected as PR16 affirmation: git history stays the immutability mechanism)
- [D00-T01-S23-PR5] [minor] Path adversarial fixtures missing -> filed D00 T01 §33
- [D00-T01-S23-PR6] [minor] Digest never re-verified -> rejected second affirmation of the PR15 design: the file keeps no bytes to verify against, the digest stays attested
- [D00-T01-S23-PR7] [minor] Run equality is section-wide -> filed D00 T01 §33
- [D00-T01-S23-PR8] [minor] Pre-mandate skip uncross-referenced -> filed D00 T01 §33
- [D00-T01-S23-PR9] [major] Self-ancestry clears -> filed D00 T01 §31
- [D00-T01-S23-PR10] [major] Timestamps still order causally -> rejected unworkable as stated: the review record commits at stamp, after fixes land, so no review commit can topologically precede the fix; timestamps plus candidate ancestry are the workable pair
- [D00-T01-S23-PR11] [major] Proof is occurrence-only -> duplicate D00-T01-S22-PR9 (execution binding already filed D00 T01 §31)
- [D00-T01-S23-PR12] [minor] Row forks ungoverned -> filed D00 T01 §24 (fork prohibition; temporal-ordering half rejected: rows carry no timestamps and precedence never rests on line position)
- [D00-T01-S23-PR13] [minor] Amendment identity is namespace-only -> filed D00 T01 §34
- [D00-T01-S23-PR14] [minor] Amendment transition contract undefined -> filed D00 T01 §34
- [D00-T01-S23-PR15] [minor] Count-less output bypasses the cap -> rejected byte and line caps bound count-less output, so no exhaustion path; undeclared details passing is the specified §19 item-19 design
- [D00-T01-S23-PR16] [minor] Runner boundary unbounded -> filed D00 T01 §34
- [D00-T01-S23-PR17] [minor] Malformed bytes unspecified -> filed D00 T01 §34
- [D00-T01-S23-PR18] [minor] Trivial runner proof -> rejected this review ran the full template verbatim (51820-byte prompt through fence, codex, and output check); version preflight without an observed drift failure is speculative
- [D00-T01-S23-PR19] [minor] Pre-commit ledger unaudited -> rejected second affirmation of the PR13 design: pre-commit states are not governed, the review attests triage not the byte diff
- [D00-T01-S23-PR20] [minor] Checkpoint omits gates -> rejected checkpoints summarize by convention; items plus fixtures enumerate and enforce the gates
- [D00-T01-S23-PR21] [minor] Stale sixth-round labels -> rejected titles stay stable by design; the SOURCE and XREF trail annotates joiners and is the audit mechanism
- [D00-T01-S23-PR22] [minor] No atomic review-run command -> filed D00 T01 §34
End of ledger

Raw plan-review output (fenced, not part of the verdict scan; digest `0f32413fdb22321191e5e94e7bbd6750d894605a3f0aeba2562532f67f52da17`):

```
- Provenance validation contradicts itself by requiring the candidate to resolve while silently skipping unresolvable candidates; §23 should fail closed in CI and surface an explicit degraded state for gitless exports, because “validate 0 fatal” currently can include unverifiable evidence.
- Candidate verification accepts any Git object through `^{object}` rather than requiring a commit; §23 should peel to and verify a commit object, because blobs, trees, and tags cannot provide the promised candidate or ancestry semantics.
- Abbreviated commit hashes are treated as immutable identities; §§22-23 should record canonical full object IDs after resolution, because short hashes can become ambiguous as repository history grows.
- Provenance checks only that the artifact path exists in the current checkout; §23 should bind the artifact to a review-record commit or blob ID, because a mutable working-tree path does not prove which bytes were reviewed.
- Repo-relative provenance paths lack explicit normalization, containment, regular-file, and symlink rules; §23 should reject traversal and escaping symlinks, because “not absolute” is insufficient to keep evidence inside the repository.
- The output digest is shape-checked but deliberately never recomputed; §23 should define the hash algorithm and canonical bytes and verify the digest against the artifact, because a digest that cannot detect artifact mutation is not provenance.
- Provenance-run equality accepts a run matching any marker in the section rather than the exact enclosing review record; §§20 and 23 should bind each provenance line to one specific marker and manifest, because an old run can otherwise be reused within a multi-run section.
- Pre-mandate ancestry records silently skip the causal leg without an exact cutoff or local migration reference; §23 should name and cross-reference §21’s grandfathered inventory, owner, and deadline, because permanent exemptions otherwise become invisible trust.
- Ancestry permits the reviewed candidate to equal the fix tip; §§22-23 should require strict ancestry for filed defects, because the same immutable commit cannot both contain the reviewed defect and a later remediation.
- Candidate-to-fix ancestry still relies on forgeable commit timestamps to establish that the fix followed the review; §22 should record the commit containing the review ledger and require topological ordering from reviewed candidate through review record to fix, because timestamps are not causal proof.
- A proof test is accepted when its name merely appears word-bounded in arbitrary file bytes; §22 should bind the proof to an executed command, exit status, result artifact, and exact test identifier, because comments or stale test names can clear findings without exercising them.
- Supersession enforces existence and acyclicity but not linearity; §23 should prohibit multiple successors for one row and require the successor to be later than its target, because forks create multiple “current” heads and ambiguous disposition.
- “Matches the finding” is implemented only as namespace equivalence; §23 should preserve a stable finding identity or machine-readable subject and acceptance-condition fingerprint across amendments, because unrelated findings in the same review namespace can be linked as supersessions.
- Superseding rows do not define which lifecycle, severity, owner, due date, proof obligations, and rationale must be inherited or restated; §§19 and 23 should specify that transition contract, because amendment can otherwise erase accountability while remaining structurally valid.
- Count validation is bypassed when a verdict omits its declared count; §§19 and 23 should require a count for every accepted output or reject count-less findings, because the new four-digit cap provides no protection on the optional path.
- The checker bounds stdin only after the model process has produced output; §23 should impose streaming byte, line, token, and wall-clock limits at the runner boundary, because an unbounded producer can exhaust resources before the checker sees the data.
- Malformed byte streams, invalid UTF-8, NULs, and decoder failures have no specified outcome; §23 should define them as bounded validation failures with stable diagnostics, because hostile or corrupted output must not crash the runner or emit a traceback.
- The plan-runner compatibility proof uses one trivial invocation rather than the full prompt, fencing, streaming, validation, and provenance pipeline; §23 should add a verbatim end-to-end smoke fixture and capability/version preflight, because a working model flag alone does not prove the template works.
- Ledger history skips uncommitted findings, leaving the interval between run creation and first commit unaudited; §19 should freeze or digest the initial ledger and manifest atomically, because a finding can otherwise be rewritten or removed before history enforcement begins.
- §23’s test checkpoint mentions only parsing and linking, omitting run equality, digest binding, current-head uniqueness, strict causal ancestry, and runner compatibility; §23 should enumerate those gates, because the checkpoint currently permits the section’s most important guarantees to regress.
- §23’s title and commit message still describe the sixth review even though the section now contains seventh-review PR7 and later §22 PR8 amendments; §23 should rename or annotate the candidate scope and commit range, because stale provenance labels make later additions difficult to audit.
- A premium workflow should replace the manual glue with one atomic `review-run` command in §§20-23 that mints the run ID, executes with hard limits, validates output, stores a content-addressed artifact, records full provenance, and updates the ledger, because this removes the highest-risk handoff points while improving operator experience.
```
