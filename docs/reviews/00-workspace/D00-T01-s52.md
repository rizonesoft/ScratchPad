# D00 T01 §52 review

Candidate: `69ec265..8d753ee` (base `42d7f68` plus fix-loops 1-2; fix loops append, sign-off names the tip).
Tier: Full panel (the diff changes the validator plus the graph plus the review skill: every stamp, query, and review consumes them).

## Self-review

Self verdict: SHIP (advisory round to confirm).

- Rule 30 mirrors rule 18's findings iteration (ref resolve, fence strip, per-section loop); cutoff 2026-09-20 exempts every 2026-09-20 fixture plus all shipped records (zero live outage markers surveyed).
- `dim_failing` restructure preserves the owed branch verbatim (early-return True) and adds only the persistence disjunct; schema /8 migration covers the sort key, the pinned help/JSON probes, and the AGENTS/README/skill pins (one historical /7 mention in D00-T01-s51.md left as history).
- Rule 31 scopes to the complement of rule 18 (stamps on or before 2026-09-18), so no `date` row can double-fire; zero live `date` rows surveyed.
- `ledger_row_due` keeps its `(disp, rest)` signature with `disp` now unused (stable API for the caller plus unit pins; no lint gate on scripts).
- Risk watched: degraded-entry consumers beyond JSON (text renders) — the new `rung` key is additive and the suite pins the renders green.

Candidate diff: 6 files, +689/-29 (`scripts/todo-graph.py` +582 mostly fixtures, `scripts/todo-validate.py` +109 rules 30/31, skill plus AGENTS plus README prose, TODO-01 item ticks).

## GPT panel Round 1

Round 1 over candidate `69ec265..42d7f68` (Sol medium, run 20260921-D00-T01-S52-gpt, output check PASS, duration 146s).

- `adversarial` needs-attention (R1-F1: key-only outage note links; R1-F2: dual date-plus-due spelling passes)
- `consistency` needs-attention (R1-F3: same-family GPT sign-off undetected; R1-F4: composite counts unspecified)
- `integration` needs-attention (R1-F5: shared-file notes false-orphan; R1-F6: gate counts what acceptance cannot clear)
- `record` approve

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 146s; outcome needs-attention; tokens unknown

All six real, all fixed. Disposition:

| ID | Disposition | Evidence |
| R1-F1 | fixed | `28d93bb`: rule 30 fires a note key with no following content (blank run, heading, next note, or EOF); §11 key-only fixture plus 2 asserts |
| R1-F2 | fixed | `28d93bb`: rule 31 fires a deferred row carrying both `due` and the retired `date` spelling on every stamp (rule 18's `due` leg is satisfied there, so only this leg sees it) |
| R1-F3 | fixed | `28d93bb`: rule 32 `quorum-same-family` fires a clean or bare-partial review whose last panel section is the same family (post-2026-09-20); skill plus README family-flip prose; §4 pin 5 to 6 |
| R1-F4 | fixed | `28d93bb`: skill plus README specify one `attempts` count per outcome in outcome order with the class line-level; the detail leg fires a composite line without exactly two counts; §§114/115 probes; §36/§38 markers to two counts; `!= 999` mutation reds 3 pins, restored |
| R1-F5 | fixed | `28d93bb`: rule 30 pools marker keys file-wide across sections sharing one findings file (malformed, duplicate, and orphan legs report under the first reporter); §§9/10 shared-file fixture plus assert |
| R1-F6 | fixed | `28d93bb`: `persistent_rungs` is the one shared read for the builder escalation, the lenient gate, and the summary next action; the escalation names the true recovery (a clean rerun supersedes; rung-level risk acceptance is not yet a register target); the summary next action names the tripped partials |

Raw round-1 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (2)
1. `scripts/todo-validate.py:2262` treats a key-only `Outage note: <rung> <date>` line as linked evidence without requiring any failure detail, so an empty placeholder satisfies the claimed integrity rule.
2. `scripts/todo-validate.py:2309` and `scripts/todo-validate.py:2331` let post-cutoff deferred rows containing both `due <date>` and the retired `date <date>` spelling pass, so the dual schema has not actually retired.

**consistency: needs-attention** (2)
1. `.claude/skills/review-todo-section/SKILL.md:166` and `todo/README.md:163` assume the sign-off panel is always Opus, although `todo/README.md:411` permits a final GPT fallback panel; a GPT plan review under that GPT sign-off is same-family and still owes an independent pass, but no validator rule detects it.
2. `.claude/skills/review-todo-section/SKILL.md:166` and `todo/README.md:163` define one attempt count per marker line while composite `retry-owed` plus `partial` markers carry two distinct outcomes; they never specify that each outcome needs its own ordered `class`/`attempts` pair, so identical composite records can encode different retry histories.

**integration: needs-attention** (2)
1. `scripts/todo-validate.py:2246` and `scripts/todo-validate.py:2286` validate outage notes independently per section while scanning the entire referenced findings file; when multiple sections share that file, each section falsely reports the other sections’ notes as orphans.
2. `scripts/todo-graph.py:1995` counts accepted bare partials toward the persistence gate while `scripts/todo-graph.py:5235` tells the operator that recording a risk acceptance clears the escalation; no rung-level acceptance target exists, so the advertised recovery cannot satisfy the gate.

**record: approve**
```

## GPT panel Round 2

Round 2 over candidate `69ec265..28d93bb` with the round-1 verdicts appended (Sol medium, run 20260921-D00-T01-S52-gpt-r2, output check PASS, duration 73s). No round-1 finding re-reported.

- `adversarial` needs-attention (R2-F1: composite leg passes malformed extras, order unenforced; R2-F2: structural lines read as note evidence)
- `consistency` needs-attention (R2-F3: quorum grammar inconsistent under the GPT-sign-off flip)
- `integration` needs-attention (R2-F4: grandfathered sharer false-orphans)
- `record` needs-attention (R2-F5: item-3 Done misstates the shipped validator)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 73s; outcome needs-attention; tokens unknown

All five real, all fixed. Disposition:

| ID | Disposition | Evidence |
| R2-F1 | fixed | `8d753ee`: the composite leg requires exactly two `attempts` tokens both well-formed (a third malformed count fires) and in outcome order (retry-owed count before `partial:`, partial count after); §§116/117 probes; the class rides line-level or per outcome (skill plus README) |
| R2-F2 | fixed | `8d753ee`: `Ledger:`, `End of ledger`, `Manifest:`, and `Provenance:` terminate an outage note like a heading, so structural lines never read as evidence; §13 probe |
| R2-F3 | fixed | `8d753ee`: rule 21 is family-relative (the survivor owes `retry-owed` exactly when same-family as the sign-off panel, read through one shared `signoff_family` helper) and rule 32 narrows to clean markers (partials stay out, so the two rules never contradict); skill plus README prose family-relative throughout; §§118-121 flipped 2x2 matrix |
| R2-F4 | fixed | `8d753ee`: rule 30 pools every reporter's keys but only post-cutoff reporters fire (a file with no live reporter stays fully silent); §12 grandfathered-sharer probe |
| R2-F5 | fixed | `8d753ee`: §52 items 1-5 Done notes rewritten to the shipped mechanisms (rule 32, family-relative rule 21, composite shape enforcement, shared persistence read, dual-spelling leg) |

Raw round-2 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (2)
1. `scripts/todo-validate.py:795-811` validates composite attempt counts by filtering for valid tokens and checking only that two remain; a marker with two valid counts plus an extra malformed count passes, and the code never enforces the documented outcome order or associates each count with its outcome.
2. `scripts/todo-validate.py:2303-2318` treats any next nonblank, non-heading line as outage evidence, so unrelated content such as `Ledger:` or provenance text lets a key-only outage note satisfy the integrity rule without attributable failure evidence.

**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:166` and `todo/README.md:163` simultaneously require a same-family GPT primary review under a GPT sign-off to carry `retry-owed` and declare that primary survivors cannot carry `retry-owed`, while also saying every fallback survivor owes retry even when an Opus fallback is the independent family under that GPT sign-off; the documented quorum grammar has no consistent family-relative rule.

**integration: needs-attention** (1)
1. `scripts/todo-validate.py:2264-2278` removes pre-cutoff sections from the file-wide reporter pool, but `scripts/todo-validate.py:2340-2347` checks every note in the shared findings file for a marker in that reduced pool; therefore a grandfathered section’s legitimate note is falsely reported as orphaned whenever its findings file is shared with a post-cutoff section.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1326` records the quorum item as “behavior unchanged, validator untouched,” but `scripts/todo-graph.py:1016-1023` adds the `quorum-same-family` fatal class and `scripts/todo-validate.py:2380-2439` adds validator rule 32, so the completion record materially misstates what shipped.
```

## Opus panel Round 3

Round 3 over candidate `69ec265..8d753ee` with the rounds-1-and-2 verdicts appended (Opus medium, run 20260921-D00-T01-S52-opus, output check PASS, duration 129s). No round-1 or round-2 finding re-reported. All five findings sit below the blocking bar (validator diagnostic quality, strictness gaps, and doc drift with no live victim: zero live outage notes, zero live composite lines), so each files through `add-todo` and the section stamps without re-rounding.

- `adversarial` needs-attention (R3-F1: rule-30 locations shift under fences; R3-F2: malformed extras pass on single-outcome lines)
- `consistency` needs-attention (R3-F3: README row states the retired absolute rule; R3-F4: key-only leg undocumented)
- `integration` needs-attention (R3-F5: note-side legs over-fire on grandfathered sharers)
- `record` approve

Telemetry: round 3; model opus; effort medium; duration 129s; outcome needs-attention; tokens unknown

Disposition:

| ID | Disposition | Evidence |
| R3-F1 | filed | D00 T04 §10 |
| R3-F2 | filed | D00 T04 §10 |
| R3-F3 | filed | D00 T04 §10 |
| R3-F4 | filed | D00 T04 §10 |
| R3-F5 | filed | D00 T04 §10 |

Raw round-3 output (fenced, stripped from scans):

```
**adversarial: needs-attention (2)**

1. `scripts/todo-validate.py:2339` feeds `outage_note_keys` the fence-stripped text (`graph.strip_fenced_code` deletes fenced lines rather than blanking them, `scripts/todo-graph.py:879-906`), but the four diagnostics at `scripts/todo-validate.py:2344`, `:2352`, `:2371`, and `:2389` print `{fpath}:{ln}` as a file location. Any findings file that quotes raw runner output in a fence before its `Outage note:` line reports a line number short by the number of fenced lines, pointing the operator at unrelated text; rule 16 by contrast reports raw-file numbering, and no fixture in `on30` places a fence ahead of a note, so the suite cannot catch it.
2. `scripts/todo-validate.py:813-818` still inspects only the first `attempts` token (`re.search`), so the malformed-extra hardening added at `:819-849` covers composite (retry-owed plus partial) lines only. A single-outcome degraded line such as `outage: both rungs (owner ann, due 2099-01-01) class timeout attempts 4 attempts 2x event 2026-09-20` passes every leg, while `todo/README.md:417` states that `2x` fires; the same malformed-extra shape the composite leg was hardened against rides along silently on outage, bare retry-owed, and bare partial lines.

**consistency: needs-attention (2)**

1. `todo/README.md:417` (`stamp-no-plan-review` row, edited in this candidate for the composite-count sentence) still states the retired absolute rule: "a fallback survivor without `retry-owed`, a primary survivor with `retry-owed` or accountability fields ... are FATAL". That contradicts the family-relative rule shipped at `scripts/todo-validate.py:760-777`, the updated bullet at `todo/README.md:163`, the `quorum-same-family` row at `todo/README.md:414`, and `.claude/skills/review-todo-section/SKILL.md:166`. Under a GPT sign-off panel the row's two legs invert against the validator.
2. The key-only-note FATAL leg (`scripts/todo-validate.py:2357-2373`: a note whose key is followed by EOF, a heading, another note, or a `Ledger`/`End of ledger`/`Manifest`/`Provenance` line "carries no evidence") is documented nowhere. Sites that enumerate the rule and omit it: `todo/README.md:413` (`outage-note-unlinked` row lists only dangling, orphan, duplicate, malformed), `todo/README.md:163` (marker-vocabulary sentence, same four legs), and `.claude/skills/review-todo-section/SKILL.md:186` (the authoring instruction, same four legs). An author following the skill writes a bare key and hits an undocumented FATAL, and the structural-terminator boundary is unstated everywhere.

**integration: needs-attention (1)**

1. `scripts/todo-validate.py:2341-2391`: the note-side legs (malformed at `:2341`, duplicate at `:2348`, key-only at `:2357`, orphan at `:2385`) scan the whole shared findings file and fire as soon as any post-cutoff sharer exists, attributing every hit to `live[0]`. A pre-cutoff section whose note is malformed, duplicated, key-only, or unkeyed therefore fails FATAL through a post-cutoff sibling, contradicting `todo/README.md:413` ("pre-cutoff sections pool their keys but never fire") and the rule comment at `scripts/todo-validate.py:2305-2310`. Only the marker-side leg at `:2374` is correctly restricted to `live`; the `on30` fixture covers the grandfathered sharer only with a well-formed note (§12), so the asymmetry is untested.

**record: approve**
```

Arch trigger: none - validator rules plus query legs plus skill/README prose; no editor state, storage, extensibility, AI integration, protocol, or consent/undo surface changes.

## Plan review

Round: `gpt-5.6-sol` high over §52 plus §28 plus §29 plus D00 T04 §10, 2026-09-21. Neighbors: §28 (Depends), §29 (XREF neighbor: §52 carries its R3 advisory joiner), D00 T04 §10 (XREF neighbor plus review dependent: §52's sign-off residuals, direct reverse Depends); dependents: D00 T04 §10 (no other reverse Depends rows or XREF consumers; §28's and §29's §52 mentions are the filers' own stamps). The run minted `-r3` past the panel's `gpt` and `gpt-r2` claims. Output `check-plan PASS` (24 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §52, D00 T01 §28, D00 T01 §29, D00 T04 §10]; dependents [D00 T04 §10]; bytes 28547; run 20260921-D00-T01-S52-gpt-r3

Ledger (namespaced IDs per the §16 shape; 3 of 24 filed at D00 T04 §11, 1 reworded in place, 3 duplicates, 17 rejected with reasons on their rows):

Ledger:

- [D00-T01-S52-PR1] [minor] Sign-off residuals belong in the fix loop -> rejected contradicts the skill's file-and-stamp for below-bar sign-off findings
- [D00-T01-S52-PR2] [minor] §52 unstamped while §10 cites reviewed candidate -> rejected timing artifact; T04 §10 Depends on §52 orders the lifecycle and §52 stamps this run
- [D00-T01-S52-PR3] [minor] §52 needs blocking dependency on §10 -> rejected would create a dependency cycle; file-and-stamp is the designed flow
- [D00-T01-S52-PR4] [minor] Done claims overstate vs §10 findings -> rejected Dones describe the candidate accurately; §10 items are hardening, not corrections
- [D00-T01-S52-PR5] [major] Retry-owed counts stay family-absolute under the flip -> filed D00 T04 §11
- [D00-T01-S52-PR6] [minor] §28 partial prose predates the flip -> filed D00 T04 §11
- [D00-T01-S52-PR7] [minor] Quorum blurs acceptance with remediation -> rejected skill already separates standings (filed) from debt (retry-owed)
- [D00-T01-S52-PR8] [minor] Outage key collides same-rung same-day -> rejected documented limitation; outage markers carry no run by design
- [D00-T01-S52-PR9] [minor] Notes lack tamper binding -> rejected single-operator trust model; cost exceeds benefit
- [D00-T01-S52-PR10] [minor] Rule 30 resolution scope unstated -> rejected scope explicit: per-file pooling, cross-file keys never couple
- [D00-T01-S52-PR11] [minor] Recurrence gate lacks distinct-evidence definition -> rejected cumulative-by-design per item 4; one line per run, no triple-count
- [D00-T01-S52-PR12] [minor] Persistent partials lack owner routing -> rejected escalation names operator per the single-operator default
- [D00-T01-S52-PR13] [minor] Rung debt has no deferment path -> rejected no-deferment is the designed stance; the gate clears only through a clean rerun
- [D00-T01-S52-PR14] [minor] Re-probe failure has no terminal state -> rejected re-probe failure re-marks and re-escalates; permanent red is terminal-by-design
- [D00-T01-S52-PR15] [minor] Counts need receipt cross-check plus rung labels -> rejected receipts are the recorded cost-of-changing; outcome order disambiguates
- [D00-T01-S52-PR16] [minor] Class and rung need canonical vocabularies -> rejected rung already closed with unknown-rung FATAL; class open by design
- [D00-T01-S52-PR17] [minor] Composite residual severity weighting undefined -> duplicate D00-T01-S29-PR15
- [D00-T01-S52-PR18] [minor] Dual rows lack an exactly-once pin -> filed D00 T04 §11
- [D00-T01-S52-PR19] [minor] Cutoff dates need timezone plus boundary rule -> rejected dateless string comparisons with stated inclusive bounds; no midnight boundary exists
- [D00-T01-S52-PR20] [minor] Checkpoint spelling phrase ambiguous -> rejected reworded in place before stamp (open section, checkpoint names `due`)
- [D00-T01-S52-PR21] [minor] Poster path lacks integration proof -> duplicate D00-T01-S29-PR2
- [D00-T01-S52-PR22] [minor] Issue update/closure/concurrency undefined -> duplicate D00-T01-S29-PR3
- [D00-T01-S52-PR23] [minor] Pooling scans lack a perf fixture -> rejected scans linear by construction (dict grouping plus set membership)
- [D00-T01-S52-PR24] [minor] Dashboard incident timeline missing -> rejected unscoped premium win with no failing incident
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `d1db75ed00b82ad65b459d466cc0ca7fa01c602458d711c39273c4b5138b0900`):

```
- The five below-bar §52 sign-off findings belong in the D00 T01 §52 fix loop, not a new D00 T04 §10 section, because deferring known defects violates completion-first review and breaks the exact candidate-range evidence.
- D00 T01 §52 still has an unchecked commit and no Verified stamp while D00 T04 §10 calls its rules shipped and cites reviewed candidate `8d753ee`; reconcile the lifecycle state in §52 because reviewers cannot tell what candidate is authoritative.
- If D00 T04 §10 remains separate, D00 T01 §52 needs an explicit blocking dependency on its completion and re-review, because a mere review-dependent reference can let §52 close with five acknowledged below-bar faults.
- The checked Done claims in §52 conflict with §10 findings about rule-30 scoping, extra counts, family-relative documentation, and key-only documentation; correct the §52 item status or append fix-loop corrections because checked claims currently overstate the shipped behavior.
- Attempt semantics in §52 say `retry-owed` counts the fallback, while its quorum rule allows the primary to become retry-owed under a GPT sign-off; make the count follow the owed rung and add a flipped-family fixture because the present rules cannot describe that case consistently.
- §28 still says primary-survivor loss needs no retry, while §52 replaces that with a family-relative rule; add an explicit supersession correction to §28 and the governing documentation because historical Done prose now directs readers to the wrong behavior.
- Define separately when quorum is minimally satisfied and when a clean record requires no retry in §52, because “one survivor meets quorum” and “same-family survivor owes retry” currently blur acceptance with remediation.
- The outage key `Outage note: <rung> <date>` cannot distinguish two genuine outages for the same rung on one day; replace or extend it with a run ID or stable unique anchor in §52 because collision rejection would otherwise prevent valid incident records.
- The outage-note “integrity” rule proves only linkage, not immutability; bind the note to a Git object, content hash, or immutable run artifact in §52 because later edits can silently change evidence behind a valid marker.
- Rule 30 needs explicit resolution scope from marker to findings file, not only chain-wide pooling, in §52 because identical keys in multiple review files can become ambiguous or accidentally coupled.
- The recurrence gate needs a definition of distinct evidence, target identity, ordering, supersession, and consecutive versus cumulative failures in §52 because three duplicated or ancient partial lines could otherwise trigger the same escalation as three independent failed runs.
- Persistent bare partials still have no owner or due date, while §29 notifications are owner-oriented; add accountable routing in §52 or §29 because unattended CI can fail without telling anyone who must recover it.
- §52 explicitly says rung-level risk acceptance is not a register target, leaving persistent outages with no governed deferment path; extend §29’s risk register to cover rung-level partial debt because operators otherwise must either leave the gate permanently red or erase the evidence.
- The plan owns the three-partial re-probe trigger but not what happens when that re-probe also fails; add bounded retries, severity escalation, and a terminal owner decision in §52 because recurring infrastructure loss can remain indefinitely unresolved.
- Attempt counts remain author-asserted and positional; add a machine cross-check against run receipts and label counts by rung in §52 because copied, reordered, or family-flipped outcomes can produce plausible but false histories.
- Failure `class` and `rung` values need canonical vocabularies and unknown-value handling in §28 or §52 because free-form labels prevent dependable aggregation, alert routing, and trend analysis.
- Composite residual fixtures pin only the sorted text `partial+retry-owed`, not how its residual severity is calculated; define max-versus-combined weighting in §29 because the risk register can display the right label while prioritizing it incorrectly.
- Rule 18 and rule 31 need an exactly-once diagnostic fixture for dual-spelled legacy rows in §52 because overlapping retirement rules can produce duplicate or contradictory failures.
- The cutoff and removal dates need an explicit timezone and inclusive-boundary rule in §52 because a 2026-09-21 retirement executed near midnight can classify the same record differently locally and in scheduled CI.
- Replace §52’s checkpoint phrase “one deferred spelling remains” with the exact surviving spelling `due`, because the current wording is ambiguous beside the claimed retirement of `date`.
- §29’s scheduled issue path needs an integration or dry-run test covering workflow permissions, absent `gh` authentication, API failure, and retry behavior because query fixtures alone do not prove unattended notification actually reaches GitHub.
- Define update, closure, and concurrency behavior for §29’s deduplicated GitHub issue because overlapping push and cron runs can create spam or leave resolved incidents visibly open.
- Add a growth or performance fixture for rule-30 file-wide pooling and persistent-partial scans in §52 because an audit system that becomes quadratic as review history grows will degrade the premium workflow.
- Add a dashboard incident timeline linking each partial, outage note, retry, clean supersession, and current owner across §§29 and 52 because it turns the new grammar into a premium operator experience instead of requiring manual reconstruction from markers.
```

## Live proof

Digests below are sha256 over the quoted outputs (UTF-8, no trailing newline). Quotes ran on the stamp working tree whose scripts, skill, and README are byte-identical to candidate tip `8d753ee` (later commits add the T04 §10/§11 filings, the §52 stamp lines, and row flips only).

`py scripts/todo-graph.py self-test`: `todo-graph self-test: 1321 cases, 0 failed`.

Provenance: candidate 8d753eee0fa8032eda2f262036bd415cb55eda33; command py scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.3; digest f28e4506949e2806b53d6df54a83712e3d1ed638b5e11597e8cff4f395f62b65; path scripts/todo-graph.py; run 20260921-D00-T01-S52-gpt-r3

`py scripts/todo-graph.py validate`: `28 todos, 264 sections -- 0 fatal, 0 warning(s)`.

Provenance: candidate 8d753eee0fa8032eda2f262036bd415cb55eda33; command py scripts/todo-graph.py validate; exit 0; tool CPython 3.14.3; digest af902fc5edf9a81717d3db40e5828e1a66e962bbb7909f5dc47c3ff8923a8a4c; path scripts/todo-graph.py; run 20260921-D00-T01-S52-gpt-r3

`py scripts/todo-graph.py plan --check`: `implementation plan is current -- 99/259 sections complete (38%)`.

Provenance: candidate 8d753eee0fa8032eda2f262036bd415cb55eda33; command py scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.3; digest 603582cbe6a0912dd07b6204bb20e8205619b972dc9baaa11815cfa83c3ab16a; path scripts/todo-graph.py; run 20260921-D00-T01-S52-gpt-r3

## Sign-off

Candidate range `69ec265..8d753ee` (base `42d7f68` plus 2 fix loops). All rounds closed: GPT R1 (6 fixed), GPT R2 (5 fixed), Opus R3 sign-off (5 filed D00 T04 §10), plan review (3 filed D00 T04 §11, 1 reworded in place, 3 duplicates, 17 rejected). Stamped 2026-09-21.
