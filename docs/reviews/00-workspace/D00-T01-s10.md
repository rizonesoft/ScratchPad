# Review: D00 T01 §10 -- Opus Panel Rule Hardening Follow-ups

## Evidence map

Round-1 candidate `f8740c2` carried all 4 items: line-anchored verdict regex with required marker (item 1), unbalanced-fence FATAL naming the opener line (item 2), fence-only fixture plus §9 check rename (item 3), the commit itself (item 4), with skill/README marker clauses and count 408. Round-2 candidate `7f7fcfa` answered round 1 (quoted-headings decision with §§15-16, char-plus-length fence tracking with §17, `_`/`~` narrowing, unbalanced-FATAL docs, count 411). Round-3 candidate `9c4dc55` answered round 2 (info-string close bar with §18, indented-code bar with §19, marker-narrowing lock with §20, count 414). Round-4 candidate `fe5e6cc` answered round 3 (blockquote-aware fences with §21, backtick-info paragraph rule with §22, count 416). Round-5 candidate `b6c858b` answered round 4 (quote-depth-exact closes with §23, quote-end lookahead with §24, shared `_fence_shape` helper, count 418). Evidence for each fix is quoted under its panel record.

## Self-review

Sense: the three filed residuals are closed (prose can no longer supply verdicts, unbalanced fences name their opener, the fence-only direction is fixtured), and four review rounds hardened the fence tracker to a documented CommonMark approximation. Logic: verdicts are per-lens line-anchored regexes with a required same-line marker run; fences track char, run length, opener line, and quote depth, with closes requiring same depth, same char, run at least the opener's, and no info string. Breakage: the verdict rule is stricter (bare, indented-4+, table-cell, `_`/`~`-led, and `+`-bulleted verdict mentions no longer count; no live findings use those shapes; both live paneled findings files re-probed passing). Process note: a mutation-battery script truncated `todo-validate.py` to zero bytes mid-run (write before compute on a failed assert); recovered exactly from git plus the two round-4 edits, verified byte-identical in behavior at 418/418 with a clean validate before proceeding. No self-review fixes; the panel found the real ones below.

## Opus panel (round 1, candidate `f8740c2`)

**adversarial: needs-attention**: fenced-heading escape half open (marker-shaped verdicts after a fenced heading counted); naive parity toggle mishandled the nesting idiom; cost note omitted indented and table-cell shapes. Fixed: quoted headings declared not structure with §15 firing and §16 silent; char-plus-length tracking with §17; full cost recorded.

**consistency: advisory**: `s = ln.strip()` shadowed the section object. Fixed: renamed to `stripped` in the fence loop rewrite.

**integration: needs-attention**: unbalanced-fence FATAL undocumented; code accepted markers beyond the documented four. Fixed: both docs state the FATAL; class narrowed to the documented set.

**record: needs-attention**: item-1 Done-when required a suite count the note lacked; checkpoint over-claimed exactness for §14. Fixed: count quoted; checkpoint names the shared strip.

## Opus panel (round 2, candidate `7f7fcfa`)

**adversarial: needs-attention**: info-string close leaked quoted verdicts; indented markers toggled; marker narrowing unlocked. Fixed: closes require no info string (§18); 4-space-indented markers are content (§19); `_`/`~` rejection fixtured (§20).

**consistency: approve**: no new issues; prior advisory fix confirmed.

**integration: advisory**: fenced-heading decision and marker details lived only in the TODO. Fixed: skill and README state quoted-headings, the marker list, and the 3-space allowance.

**record: needs-attention**: checkpoint still miscounted shared mutations; cost note omitted the narrowing. Fixed: checkpoint regenerated from measurement; narrowing and `+` recorded.

## Opus panel (round 3, candidate `9c4dc55`)

**adversarial: needs-attention**: blockquoted fences untracked so quoted verdicts satisfied; backtick-in-info openers false-fired. Fixed: quote-aware tracking (§21); backtick info makes a paragraph (§22).

**consistency: advisory**: shared-mutation counts needed care (fed NEW-9, fixed in the same diff by measurement).

**integration: advisory**: no new issues beyond the round-2 doc items, confirmed landed.

**record: needs-attention**: checkpoint undercounted the anchored-regex and strip mutations. Fixed: full 9-mutation battery measured and recorded.

## Opus panel (round 4, candidate `fe5e6cc`)

**adversarial: needs-attention**: quoted closes shut unquoted fences; quoted fences never ended with their quote. Fixed: depth-exact closes (§23); quote-end lookahead with lazy kept as content (§24); `_fence_shape` helper.

**consistency: advisory**: no new issues; helper shape confirmed shared by scan and lookahead.

**integration: advisory**: depth rule needed stating. Fixed: skill and README carry the depth and quote-end clauses.

**record: needs-attention**: shared-machinery counts wrong under the panel's mutation readings. Fixed: checkpoint rewritten with exact mutation definitions and a full 11-mutation measured battery.

## Opus panel (round 5, candidate `b6c858b`, FINAL)

**adversarial: needs-attention**: the lookahead scans the whole remainder, so a later quoted block retroactively swallows content (false FATAL and fail-open probes, both unfixtured beyond §24). Filed at D00 T01 §11 per the max-5 rule, not patched as round 6.

**consistency: advisory**: laziness never applies to fenced-code content, so the lazy branch models a rule CommonMark lacks. Filed with §11 as the fix direction (delete the branch).

**integration: advisory**: README states the quote-end rule unconditionally while skill and code condition it. Filed with §11 (both state the unconditional rule truthfully after deletion).

**record: approve**: all checkpoint claims verified by measurement on the live tree; counts reconcile at 418.

## Gates re-run

`self-test` 418/418, live `validate` 0 fatal 0 warnings, `plan --check` current. Panel rounds used: 5 of 5 (adversarial needs-attention, consistency/integration advisory, record approve at close). Leftovers filed at D00 T01 §11; stamp carries them in its `Review:` line.
