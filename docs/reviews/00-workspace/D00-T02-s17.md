# D00 T02 §17 review: Nightly Notify Plus Trend Surface

Candidate: `f52a2e2..813c0f2` (§17 implementation `7bd5228`, self-review fix loop `2a2487f`, panel fix loops `9c1cbef`, `1020f3d`, `cdcd651`, `63d2ee3`, `813c0f2`; signed off 2026-09-21).
Tier: Full panel (the diff changes the governed-run scripts plus the result/trend/notify contract surface: the night run, morning triage, retention verify, and the toast consumer all read it).

## Self-review

Three questions over `git diff f52a2e2..7bd5228` (12 files, 823 insertions, 30 deletions), read whole plus the live proofs re-run at review time:

1. Makes sense: yes. Every item's Done names its mechanism plus its proof: toast with trigger (item 1, fired plus history-read), trend five series over two nights (item 2, 2 nights from 3 results), ack sign-offs (item 3, 2 files plus `Test-RedAcknowledged` green), budget telemetry (item 4, RunA/B seconds plus reserve plus median), overdue alert (item 5, fixture quote), v1 result plus backfill (item 6, 3/3 validate), loser result (item 7, live collision), seven routed classes (item 8, all quoted), env dims (item 9, ledger over 3 results). No scope beyond the contract; the fl5f1 checksumming plus manifest is required retention closure, not extra scope.
2. Logical: yes. Transcript assembly sums reproduce the stamped §9 counts (545/1/3, never the UI-only row); enforcement replays the live rule on the retained trx; the classifier honors its documented precedence on all three backfills (infrastructure/gate/infrastructure, each for its evidenced reason); JSON inputs are stripped raw (the decoration blowup is measured plus pinned); the emitter fails closed on its own invalid file; ack files live beside the evidence dir so verify never reads sign-offs as manifests.
3. Breakage: none found. The scheduler-preflight refactor into `Get-SchedulerState` preserves every reading and adds one honest line (unreadable defs read unknown, never none); `Test-QuarantineWindows` gains `OpenRows` additively; the toast cap 4 to 6 carries its fixture; retention verify reads green on all three runs (12/22/7 files); the parser suite reads 200 green; `validate` 0 fatal plus `plan --check` current. Self-finds fixed pre-panel in `2a2487f`: transcript counts override the UI-only row (stamp consistency), enforcement replay replaces the hardcoded false, soak failures wire through, scheduler-enabled plus tree-stable read null on unknowns, dead `$repText` plus the wrong legA note removed, stale ack-path comment corrected, rawline JSON assert guarded so a strip regression fails instead of hanging the suite.

## Setup note (void rounds, not verdicts)

Three runs (`20260921-D00-T02-S17-gpt-r2`, `-gpt-r3`, `-opus`) executed against a prompt whose chunks were empty: a same-file truncate-before-read one-liner emptied both fence inputs, so Sol approved nothing twice and Opus honestly reported empty input. Those runs stay in the run ledger as setup error (plus the earlier `codex`-without-extension spawn failure); they carry no panel sections here because they reviewed nothing. The rounds below rerun the full sequence against the real 83KB prompt; round numbers and run-ids below are the record.

## GPT panel Round 1

Round 1 over candidate `f52a2e2..2a2487f` (Sol medium, run 20260921-D00-T02-S17-gpt-r4, output check PASS).

- `adversarial` needs-attention (3)
- `consistency` needs-attention (1)
- `integration` needs-attention (3)
- `record` needs-attention (2)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Disposition (all fixed in `9c1cbef`, re-round 2 below):

| # | Lens | Finding | Disposition |
|---|------|---------|-------------|
| 1 | adversarial | `Test-ResultFile` accepts empty legs/soak/env/timings blocks | Fixed: green/red now require legs run-a/run-b/interactive each with ran plus a known soak verdict; fixtures `result-emptylegs`, `result-badsoak` pin both, `result-good` carries the full shape |
| 2 | adversarial | Empty ack file suppresses a RED (presence-only check) | Fixed: new `Test-AckFile` requires owner plus day plus 200-char substance; the collector filters through it; fixtures `ack-good/empty/noday/noowner/short`; both real acks validate True |
| 3 | adversarial | Failure-free legacy run with null gates backfills green | Fixed: a ran gated leg with a null gate reds (unproven foreground proof); no verdict change on the three retained runs (all already red) |
| 4 | consistency | Trend Soak cell reads `passed/failed/unproven` ints but v1 carries failed/killed/cut arrays | Fixed: the cell renders verdict plus failed names plus killed/cut counts from the arrays (legacy ints degrade to `failed=N`); t1/t2 fixtures carry the v1 array shape; `trend-soakcell` pins it; the known-gap paragraph is gone. Same-family sweep: `@($null).Count` reads 1, so the quarantine and killed/cut counts null-filter |
| 5 | integration | Loser results undiscoverable by trend; supervisor collision console-only | Fixed: trend discovers `loser-*.result.json` (both live losers render as mark rows) and the supervisor stand-down writes the loser md plus result.json mirroring the nightly block; live supervisor collision quoted (exit 0, kind supervisor) |
| 6 | integration | Overdue toast line drops due plus owner | Fixed: the line builds from overdue objects as `Test (due D, Owner)` joined; live toast history quotes `Overdue quarantine: UI.FlakyWidget (due 2026-09-17, DerickPayne)` |
| 7 | integration | Toast never invokes `Classify-NightlyOutcome`; routes live only in trend | Fixed: the title carries the class (`Nightly <day> : <GREEN\|RED> (<class>)`); live history quotes `RED (infrastructure)` |
| 8 | record | Trend shows pass counts not rates, quarantine counts not age | Fixed: Pass cells carry the rate (`571/4/4 (98.6%)`, fixture `trend-rate`); the quarantine line names the oldest overdue age (`trend-oldest` pins `oldest 4d`); historical per-night dues stay unrecoverable from v1 by construction |
| 9 | record | Budget telemetry lacks consumed, all phases, per-night percentiles | Fixed: the emitter records `consumed` elapsed beside `reserve` (`consumed` null on backfills); a `## Budget` ledger renders all phase timings plus used/left halves plus RunA rank (`trend-budget`, `trend-budget-partial`) |

Raw round-1 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (3)
1. `tools/NightlyParse.ps1:1233-1253` and `tools/NightlyParse.Tests.ps1:620-622` accept empty `legs`, `soak`, `env`, and `timings` objects as schema-valid, so a structurally unusable v1 result satisfies the contract's version-validation gate.
2. `tools/NightlyParse.ps1:1441-1448`, `tools/nightly.ps1:1327-1329`, and `tools/NightlyParse.Tests.ps1:687-689` treat any correctly named acknowledgement file as valid without checking owner, signature, failures, or cause; an empty `ack-YYYY-MM-DD.md` can therefore suppress an unattended RED permanently.
3. `tools/NightlyBackfill.ps1:67-79` records unrecoverable legacy gates as null, but `tools/NightlyBackfill.ps1:211-219` only reds non-null, nonzero gates; a failure-free legacy run with unproven gates is incorrectly backfilled as green.

**consistency: needs-attention** (1)
1. `tools/nightly.ps1:1303` and `tools/NightlyBackfill.ps1:238` define `soak.failed` as an array of failed-iteration names, while `tools/NightlyParse.ps1:1389-1390` formats it as a numeric failed count; `tools/NightlyParse.Tests.ps1:675-676` masks the mismatch with integer fixtures, and `docs/testing.md:90` explicitly records the resulting empty live counts.

**integration: needs-attention** (3)
1. `tools/nightly.ps1:391-396` writes mutex stand-down results as `loser-*.result.json` and exits before trend rendering, while `tools/NightlyTrend.ps1:18-19` discovers only `morning-*.result.json` and retained `result.json`; `tools/NightlySupervisor.ps1:41-43` still exits on supervisor collision with console text only, so neither stand-down path satisfies scheduler-health and telemetry surfacing.
2. `tools/nightly.ps1:1294-1304` reduces overdue quarantines to test names, and `tools/nightly.ps1:1342` sends only those names in the morning notification; due dates and owners required for actionable overdue alerts are discarded.
3. `tools/nightly.ps1:1336-1346` sends only GREEN/RED plus counts and never invokes `Classify-NightlyOutcome`; the distinct test, gate, enforcement, infrastructure, degraded-soak, recovery, and scheduler-no-start routes implemented at `tools/NightlyParse.ps1:1294-1351` appear only in the trend, not in alert delivery.

**record: needs-attention** (2)
1. `tools/NightlyParse.ps1:1363-1402` renders raw passed/failed/skipped counts rather than pass rates and overdue/due-soon counts rather than quarantine age, so the five-series trend required by `todo/00-workspace/TODO-02-test-backbone.md:551` is not recorded.
2. `tools/nightly.ps1:1309` records phase timings and remaining reserve, but `tools/NightlyParse.ps1:1363-1425` exposes only Run A/B seconds, remaining reserve, and one aggregate Run A median; reserve consumed, all phase durations, and historical percentiles per night required by `todo/00-workspace/TODO-02-test-backbone.md:553` are absent.
```

## GPT panel Round 2

Round 2 over candidate `f52a2e2..9c1cbef` (Sol medium, run 20260921-D00-T02-S17-gpt-r5, output check PASS).

- `adversarial` needs-attention (2)
- `consistency` approve
- `integration` needs-attention (2)
- `record` needs-attention (3)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Disposition (fixed in `1020f3d` unless rebutted with evidence; re-round 3 below):

| # | Lens | Finding | Disposition |
|---|------|---------|-------------|
| 1 | adversarial | Empty env/timings still validate; no verdict/exit consistency | Fixed in part: verdict/exit contradiction now fails (`result-contra`, `result-contra2`) and green/red require a proven env os (`result-noenv`). Timings-emptiness rebutted: the fl5f1 backfill legitimately carries `timings {}` (no leg transcript parsed), so presence is the correct gate and the rule is documented in `docs/testing.md` |
| 2 | adversarial | Ack gate accepts filler (`Owner: TBD`, long fluff) | Fixed in part: placeholder owners (TBD/TODO/TBS/XXX/none/n/a/unknown) now fail (`ack-tbd`). Full semantic filler-detection rebutted as beyond mechanical gating: ack files are git-tracked, so authorship plus review backstop a determined liar; documented in `docs/testing.md` |
| 3 | integration | Cancelled runs get no ack demand, toast, or trend refresh | Fixed: the red-day collector counts red plus cancelled, the trap toasts `CANCELLED` with its reason best-effort, and the trap refreshes the trend best-effort; trap-scope probe quotes toast sent plus trend written plus `CANCELLED-COLLECTED:2026-09-21` |
| 4 | integration | Loser paths render no trend at stand-down time | Fixed: both loser paths (nightly plus supervisor) refresh the trend best-effort right after writing the mark; atomic writes keep holder/loser renders safe either order |
| 5 | record | Quarantine age not tracked across nights | Fixed: the emitter snapshots `overdueDetail` (test, due, owner) per result and each trend row renders its oldest age-at-night (`trend-qage` pins `1/0 (oldest 2d)`); backfills carry the empty detail with the windows-unrecoverable note |
| 6 | record | No historical percentile values per night | Fixed by construction: budget lines now read `RunA <s>s rank <r>/<n> pct <p>` (percentage of rendered nights beaten, `n/a` for a single night; `trend-budget`, `trend-budget-partial` pin both ends) |
| 7 | record | Backfilled env stamps live-box values onto old nights | Fixed: every backfilled env dimension reads `unknown` with the unrecoverable basis; all three results re-backfilled, retention verify green, trend env cells read `unknown unknown` |

Raw round-2 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (2)
1. `tools/NightlyParse.ps1:1233-1266` and `tools/NightlyParse.Tests.ps1:616-617` still accept empty `env` and `timings` objects and do not validate verdict/exit consistency, so a structurally incomplete or contradictory v1 result passes validation.
2. `tools/NightlyParse.ps1:1528-1540` treats any 200-character file containing `Owner:` and the date as acknowledgement; filler text, `Owner: TBD`, or prose without a signature, failures, or cause can suppress a RED.

**consistency: approve**

**integration: needs-attention** (2)
1. `tools/nightly.ps1:405-409` records an interrupted governed run as `verdict = 'cancelled'` and exits through the trap, while `tools/nightly.ps1:1330-1335` collects only `verdict = 'red'`; cancelled partial-run REDs therefore receive no acknowledgement demand, toast, or trend refresh.
2. `tools/nightly.ps1:389-399` and `tools/NightlySupervisor.ps1:39-52` still exit immediately after writing stand-down results; although `tools/NightlyTrend.ps1:17-22` can discover those files later, the collided run does not update the trend surface when the stand-down occurs.

**record: needs-attention** (3)
1. `tools/NightlyParse.ps1:1399-1456` records per-night quarantine counts and only one current-snapshot oldest age, so quarantine age is not tracked across nights as required by `todo/00-workspace/TODO-02-test-backbone.md:551`.
2. `tools/NightlyParse.ps1:1458-1505` emits one aggregate Run A median/max and per-run ordinal ranks, not historical percentile values per night as required by `todo/00-workspace/TODO-02-test-backbone.md:553`.
3. `tools/NightlyBackfill.ps1:226-228` populates historical results with the current machine environment, including current settings, topology, DPI, and adapter data; this creates false cross-night comparability instead of recording unrecoverable historical dimensions as unknown.
```

## Opus panel Round 3

Round 3 over candidate `f52a2e2..1020f3d` (Opus medium, run 20260921-D00-T02-S17-opus-r3, output check PASS; a first attempt timed out at 600s and the verbatim retry passed, so no outage).

- `adversarial` advisory (2)
- `consistency` needs-attention (2)
- `integration` needs-attention (1)
- `record` needs-attention (3)

Telemetry: round 3; model opus; effort medium; duration unknown; outcome needs-attention; tokens unknown

Disposition (needs-attention fixed in `cdcd651`; advisories file as D00 T02 §23 post-stamp; re-round 4 below):

| # | Lens | Finding | Disposition |
|---|------|---------|-------------|
| 1 | adversarial | Acks key on day, so one ack suppresses every same-day RED | Advisory, filed: per-run acknowledgement granularity is a design change beyond item 3's night granularity; files as D00 T02 §23 post-stamp |
| 2 | adversarial | Substance gate gameable by 200-char fluff | Advisory, filed: placeholder rejection plus tracked authorship is the mechanical bar; structured ack frontmatter with machine-checked failure refs files as D00 T02 §23 post-stamp |
| 3 | consistency | Ack-2026-09-20 says rerun Run A 163/1/3 vs result 545/1/3 | Fixed: the ack now quotes 545/1/3 with the S4-defect note and still validates True |
| 4 | consistency | Docs claim a proven env os; the gate allows `unknown` | Fixed: the paragraph now says a present os with `unknown` the honest backfill value |
| 5 | integration | Supervisor tombstone is Markdown-only (no result, toast, trend) | Fixed: the tombstone lands a red `result.json` (unproven legs, live env, validates True, classifies infrastructure), toasts, and refreshes the trend; proven live with a hanging dummy child (exit 1, trend row plus toast quoted, artifacts cleaned) |
| 6 | record | Percentile inverted (fastest reads pct 100) | Fixed: standard percentile rank (`100*(pos-1)/(n-1)`); budget fixtures pin pct 0 fastest plus pct 100 slowest |
| 7 | record | Null gates render as empty halves | Fixed: ran legs with null gates render `null`, missing legs `-` (`trend-gatesnull` pins `null/-`; live rows read `null/null` plus `1/0`) |
| 8 | record | `Flakes` header mislabels the soak-failure count | Fixed: header renamed `SoakFail` (no fixture referenced the old header) |

Raw round-3 output (fenced, stripped from scans):

```
**adversarial: advisory** (2)
1. `tools/NightlyParse.ps1:1541-1547` keys acknowledgement on the day string only, and `tools/nightly.ps1:1328-1332` collects RED days the same way: `docs/nightly-acks/ack-2026-09-20.md` written after the 03:20 rerun silently suppresses the unacked demand for the later 04:13 RED of the same day, and for any future RED that lands on a day already signed; two distinct RED runs cannot be distinguished by the check.
2. `tools/NightlyParse.ps1:1562` still gates substance on `$text.Length -lt 200` alone, so any 200-character prose carrying a non-placeholder `Owner:` and the date permanently suppresses an unattended RED; the placeholder list at `tools/NightlyParse.ps1:1560` catches only the fixed `TBD|TODO|TBS|XXX|none|n/a|unknown` set.

**consistency: needs-attention** (2)
1. `docs/nightly-acks/ack-2026-09-20.md:7` records `nightly-rerun-2026-09-20` Run A as `163/1/3`, the report-row count that `tools/NightlyBackfill.ps1:97-118` deliberately overrides with the transcript assembly sum (`545/1/3`, recorded as a count note); the tracked sign-off therefore contradicts the `result.json` it acknowledges, and the suite's own fixture at `tools/NightlyParse.Tests.ps1:704` uses `545/1/3`.
2. `docs/testing.md:90` states `Test-ResultFile` validates "a proven environment os", but `tools/NightlyParse.ps1:1266` only checks that `env.os` is a non-empty string, and `tools/NightlyBackfill.ps1:226-228` writes `os = 'unknown'` and validates green; the gate cannot distinguish a proven environment from an explicitly unrecoverable one.

**integration: needs-attention** (1)
1. `tools/NightlySupervisor.ps1:92` publishes the hang/no-publish tombstone as Markdown only: no `morning-<stamp>.result.json`, no toast, no trend invocation. `tools/nightly.ps1:1328-1330` builds `redDays` exclusively from result files and `tools/NightlyTrend.ps1:17-20` discovers only `morning-*.result.json`, `loser-*.result.json`, and retained `result.json`, so a supervised deadline RED (the unattended-deadline case named by item 3) produces no acknowledgement demand, no alert, and no trend row, while the mutex and cancellation paths now do.

**record: needs-attention** (3)
1. `tools/NightlyParse.ps1:1518-1524` inverts the per-night percentile: `$pos` ranks ascending by seconds, so `pct = 100*(n-pos)/(n-1)` gives the fastest night `pct 100` and the slowest `pct 0`; the slowest Run A on record renders "RunA 700s rank 2/2 pct 0", and `tools/NightlyParse.Tests.ps1:665-666` pins the inverted values.
2. `tools/NightlyParse.ps1:1418` renders the gate series as "$($r.legs.'run-a'.gate)/$($r.legs.'run-b'.gate)", so a null (unproven or legacy-prose) gate prints as an empty half (`0/`, `/`) indistinguishable from a real exit 0 or a missing leg; exactly the backfilled nights whose gate verdicts are unrecoverable record no gate verdict in the cross-night series.
3. `tools/NightlyParse.ps1:1378` labels the column `Flakes` while `tools/NightlyParse.ps1:1449-1450` fills it with the soak-failure count, and `docs/testing.md:90` documents it as "soak-failure count"; flake recurrence is a separate line, so the rendered table mislabels the series a reader uses for recurrence review.
```

## Opus panel Round 4

Round 4 over candidate `f52a2e2..cdcd651` (Opus medium, run 20260921-D00-T02-S17-opus-r4, output check PASS).

- `adversarial` approve
- `consistency` advisory (1)
- `integration` approve
- `record` advisory (1)

Telemetry: round 4; model opus; effort medium; duration unknown; outcome advisory; tokens unknown

Disposition (both advisories fixed in `63d2ee3` as cheap and correct; re-round 5 below):

| # | Lens | Finding | Disposition |
|---|------|---------|-------------|
| 1 | consistency | Skipped legs (`ran:false`) render `null` like unproven gates | Fixed: ran-false legs render `skip` (`null` stays ran-but-unproven, `-` stays missing; `trend-gatesskip` pins `skip/-`; docs reworded) |
| 2 | record | Fail-closed RED reason console-only (not report, note, toast) | Fixed: the emitter seeds `note` in the result literal (backfill parity; avoids the missing-property throw), appends the reason to the result note plus the report, and carries it as the seventh toast line (cap 6 to 7 with fixture) |

Raw round-4 output (fenced, stripped from scans):

```
**adversarial: approve**

**consistency: advisory** (1)
1. `tools/NightlyParse.ps1:1419-1422` renders a leg that exists but never ran (`ran:false`, `gate:null` — the `-SkipDefault`/`-SkipPrimary` shape emitted at `tools/nightly.ps1:1252` and `tools/nightly.ps1:1263`) as `null`, the same cell as a leg that ran with an unrecoverable gate, while `docs/testing.md:90` documents the series as "`null` for unproven and `-` for missing legs"; a deliberately skipped leg therefore records as an unproven gate in the cross-night series.

**integration: approve**

**record: advisory** (1)
1. `tools/nightly.ps1:1320-1325` flips the run RED when its own `result.json` fails validation but writes the reason only to console: it is appended to neither `$report` (published at `tools/nightly.ps1:1334`, which then carries `- Exit: 1` with no cause line), nor `$result.note`, nor the toast body built at `tools/nightly.ps1:1338-1352`, so the morning surface shows an unexplained RED for exactly the failure mode the fail-closed path exists to record.
```

## Opus panel Round 5

Round 5 over candidate `f52a2e2..63d2ee3` (Opus medium, run 20260921-D00-T02-S17-opus-r5, output check PASS).

- `adversarial` approve
- `consistency` advisory (1)
- `integration` approve
- `record` advisory (1)

Telemetry: round 5; model opus; effort medium; duration unknown; outcome advisory; tokens unknown

Disposition (both advisories fixed doc-only in `813c0f2`; re-round 6 below):

| # | Lens | Finding | Disposition |
|---|------|---------|-------------|
| 1 | consistency | `skip` conflates deliberately-skipped with never-started legs (tombstone, dead-early backfill) | Fixed: docs define `skip` as legs that never ran regardless of cause (deliberate or never started); the cause lives in verdict/class/notes, not the gates cell |
| 2 | record | Docs toast enumeration omits the fail-closed line | Fixed: the paragraph names the fail-closed reason line |

Raw round-5 output (fenced, stripped from scans):

```
**adversarial: approve**

**consistency: advisory** (1)
1. `tools/NightlyParse.ps1:1422-1423` renders every leg with `ran:false` as gate `skip`, but `ran:false` is also the shape of legs that never started: `tools/NightlySupervisor.ps1:98-100` writes all three tombstone legs as `ran = $false; gate = $null` (hang/no-publish), and `tools/NightlyBackfill.ps1:157-160` writes `ran = $false` for any leg with no transcript, so a supervised deadline RED and a dead-early backfill both record `skip/skip` in the cross-night gate series, which `docs/testing.md:90` defines as "`skip` for deliberately skipped legs".

**integration: approve**

**record: advisory** (1)
1. `docs/testing.md:90` enumerates the toast body as "counts, trigger, top incident, overdue quarantine with due plus owner, unacked REDs, report path" and omits the fail-closed `Result invalid: ...` line now appended at `tools/nightly.ps1:1357`, so the documented notification content no longer matches the emitted one for exactly the self-invalid-result case.
```

## Opus panel Round 6

Round 6 over candidate `f52a2e2..813c0f2` (Opus medium, run 20260921-D00-T02-S17-opus-r6, output check PASS).

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 6; model opus; effort medium; duration unknown; outcome approve; tokens unknown

No findings; no disposition table owed. Raw round-6 output (fenced, stripped from scans):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Arch checklist (storage family)

Touched surfaces: a new file format (`morning-<stamp>.result.json`, schema v1, plus `loser-*.result.json` and retained `result.json`), a migration path (`NightlyBackfill.ps1` deriving v1 for pre-emitter runs), and a new operator notification (morning toast). Scope note per surface, no separate arch round owed (routine surface change, all consumers in-tree):

- Storage/format: v1 is additive-by-design (new readers tolerate missing keys via try/catch; `Test-ResultFile` pins version plus identity plus verdict/exit consistency plus shaped legs plus soak verdict plus env os). No migration of live files (the emitter writes v1 from birth); backfilled files are new artifacts beside retired evidence, checksummed into SHA256SUMS plus tracked manifests. Retention verify covers the new files (12/22/7 green).
- Storage/migration: the backfill never re-runs tests and never guesses (transcript sums beat UI-only rows per the stamped §9 record; unrecoverable dims read null/unknown/empty with notes). Backfilled ids (incidents) use the live formatter, so recurrence continues across the live/backfill boundary.
- Notify surface: the toast is best-effort by contract (delivery failure returns false, never reds), capped at seven lines, and quoted live with history readback. No new seams beyond the toast id plus the trend script entry points, both owned by this section.
- Contracts: trend, retention verify, ack collection, and the classifier consume v1 through `Test-ResultFile`/`Read-ResultFile`; malformed inputs skip with notes or fail closed, never throw into the run.

## Plan review

Round: `gpt-5.6-sol` high over §17 plus 4 corpus sections, 2026-09-21. Neighbors: D00 T02 §9 (Depends plus XREF), §14 plus §15 (filed here), §18 (successor boundary); dependents: none. Output check-plan PASS (37 findings, one per line). A first attempt went FAIL on producer start (WinError 2: bare codex unresolvable without a shell; r6 minted, no findings); the cmd /c retry (r7) delivered the 37 below. No criticals; 5 majors filed, 32 minors filed, 0 rejected, 0 duplicate.

Manifest: sections [D00 T02 §9, D00 T02 §14, D00 T02 §15, D00 T02 §17, D00 T02 §18]; dependents [none]; bytes 58212; run 20260921-D00-T02-S17-gpt-r7

Ledger:

- [D00-T02-S17-PR1] [minor] section edges to 14/15/22 -> filed D00 T02 §24
- [D00-T02-S17-PR2] [major] canonical run selection -> filed D00 T02 §24
- [D00-T02-S17-PR3] [minor] ack binds run plus incident ids -> filed D00 T02 §23
- [D00-T02-S17-PR4] [minor] structured ack disposition -> filed D00 T02 §23
- [D00-T02-S17-PR5] [major] ack deadline plus escalation -> filed D00 T02 §23
- [D00-T02-S17-PR6] [minor] toast fallback plus delivery health -> filed D00 T02 §24
- [D00-T02-S17-PR7] [major] no-start heartbeat -> filed D00 T02 §24
- [D00-T02-S17-PR8] [major] final-only publication -> filed D00 T02 §24
- [D00-T02-S17-PR9] [minor] notify plus ack idempotency -> filed D00 T02 §23, D00 T02 §24
- [D00-T02-S17-PR10] [minor] session delivery matrix -> filed D00 T02 §24
- [D00-T02-S17-PR11] [minor] morning digest plus urgent -> filed D00 T02 §24
- [D00-T02-S17-PR12] [minor] multi-label outcomes -> filed D00 T02 §24
- [D00-T02-S17-PR13] [minor] class routing maps -> filed D00 T02 §24
- [D00-T02-S17-PR14] [minor] cap priority plus truncation -> filed D00 T02 §24
- [D00-T02-S17-PR15] [minor] pass-rate denominators -> filed D00 T02 §25
- [D00-T02-S17-PR16] [minor] series inclusion rules -> filed D00 T02 §25
- [D00-T02-S17-PR17] [major] run identity plus timezone grouping -> filed D00 T02 §25
- [D00-T02-S17-PR18] [minor] trend edge fixtures -> filed D00 T02 §25
- [D00-T02-S17-PR19] [minor] median p50 duplication -> filed D00 T02 §25
- [D00-T02-S17-PR20] [minor] regression thresholds -> filed D00 T02 §25
- [D00-T02-S17-PR21] [minor] long-term metrics store -> filed D00 T02 §25
- [D00-T02-S17-PR22] [minor] consumed env validation -> filed D00 T02 §25
- [D00-T02-S17-PR23] [minor] single typed result source -> filed D00 T02 §24
- [D00-T02-S17-PR24] [minor] backfill field provenance -> filed D00 T02 §25
- [D00-T02-S17-PR25] [minor] env schema allowlist -> filed D00 T02 §25
- [D00-T02-S17-PR26] [minor] incident identity contract -> filed D00 T02 §22
- [D00-T02-S17-PR27] [minor] incident lifecycle -> filed D00 T02 §22
- [D00-T02-S17-PR28] [minor] recovery notifications -> filed D00 T02 §24
- [D00-T02-S17-PR29] [minor] ack integrity record -> filed D00 T02 §23
- [D00-T02-S17-PR30] [minor] tracked manifest mechanics -> filed D00 T02 §22
- [D00-T02-S17-PR31] [minor] reconciliation pin -> filed D00 T02 §18
- [D00-T02-S17-PR32] [minor] schema matrix split -> filed D00 T02 §18
- [D00-T02-S17-PR33] [minor] deterministic launch proof -> filed D00 T02 §18
- [D00-T02-S17-PR34] [minor] off-screen rigor -> filed D00 T02 §18
- [D00-T02-S17-PR35] [minor] analyzer guard -> filed D00 T02 §18
- [D00-T02-S17-PR36] [minor] diagnostics privacy rules -> filed D00 T02 §18
- [D00-T02-S17-PR37] [minor] incident bundle links -> filed D00 T02 §24
End of ledger

## Live proof

Digests below are sha256 over the quoted outputs (UTF-8, no trailing newline). Quotes ran on the stamp working tree whose code is byte-identical to candidate tip `813c0f2` (the stamp commit adds this file's review sections plus the §17 stamp lines, §§18/22 corrections, §§23/24/25 filings, plan rows, and row flips only).

`py scripts/todo-graph.py validate`: `28 todos, 267 sections -- 0 fatal, 1 warning(s)`.

Provenance: candidate 813c0f2b62a5d42eb8f4b4248dbf2a078eaaf1b3; command py scripts/todo-graph.py validate; exit 0; tool CPython 3.14.3; digest 3b1ad37eb0e13db18f13640ff9a7f5f900088b28d237fc3259eef518033d4a65; path scripts/todo-graph.py; run 20260921-D00-T02-S17-gpt-r7

`py scripts/todo-graph.py plan --check`: `implementation plan is current -- 101/262 sections complete (39%)`.

Provenance: candidate 813c0f2b62a5d42eb8f4b4248dbf2a078eaaf1b3; command py scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.3; digest 9471d47fa7ac5efcc6783021c3b6733eeb76bb5f961158243eaa6fef01a557ea; path scripts/todo-graph.py; run 20260921-D00-T02-S17-gpt-r7

`powershell -NoProfile -ExecutionPolicy Bypass -File tools/NightlyParse.Tests.ps1`: `NightlyParse.Tests: all green` (222 PASS lines, 0 FAIL).

Provenance: candidate 813c0f2b62a5d42eb8f4b4248dbf2a078eaaf1b3; command powershell -NoProfile -ExecutionPolicy Bypass -File tools/NightlyParse.Tests.ps1; exit 0; tool Windows PowerShell 5.1; digest 305aa922f9b07dcb0445d62fe62550994c13d937c6247edba265eaeb23c206ab; path tools/NightlyParse.Tests.ps1; run 20260921-D00-T02-S17-gpt-r7

`powershell -NoProfile -ExecutionPolicy Bypass -File tools/NightlyRetention.ps1 -Verify`: `verify: all retained runs clean (3 runs)`.

Provenance: candidate 813c0f2b62a5d42eb8f4b4248dbf2a078eaaf1b3; command powershell -NoProfile -ExecutionPolicy Bypass -File tools/NightlyRetention.ps1 -Verify; exit 0; tool Windows PowerShell 5.1; digest b05732a39e97eeab2b092cd2df6749577918bedbda71cad19823beb187f4010f; path tools/NightlyRetention.ps1; run 20260921-D00-T02-S17-gpt-r7

## Sign-off

Candidate range `f52a2e2..813c0f2` (implementation plus self-review plus 5 panel fix loops). All rounds closed: GPT R1, GPT R2, Opus R3 (advisories filed §23), Opus R4, Opus R5, Opus R6 all-approve, GPT plan review r7 (37 filed). Stamped 2026-09-21.
