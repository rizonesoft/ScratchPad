# Review: D00 T01 §40 -- Centralized Build Output in Bin

Candidate: `373a7d3` plus `d9e8d17` plus `916dfdc` (implement, CI-proof tick, round-3 fixes; filing `6815631` holds the section skeleton per convention, unreviewed). Tier: Full panel (the diff touches shared contract surface: `Directory.Build.props`, CI workflows, `.gitignore`). Regime: mixed panel (Sol rounds 1-2, Opus sign-off round 3, soft-3/hard-5 caps).

## Live proof

Gates on `9f8d6aa`. Digests are sha256 over the quoted bytes, each line LF-terminated. Tool for the graph gates is CPython 3.12.3; the build runs the repo-local .NET SDK 10.0.400.

`python3 scripts/todo-graph.py self-test`:

```
todo-graph self-test: SKIP sibling stamp-gate clamp probe (no section_commit_gate.py)
todo-graph self-test: 746 cases, 0 failed
```

Provenance: candidate 9f8d6aa; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest 391a36b78c1b7cf3eb3c06716267052d0fd03f29325fe06306e3d4ee6f12fbd2; path docs/reviews/00-workspace/D00-T01-s40.md; run 20260918-D00-T01-S40-gpt

`python3 scripts/todo-graph.py validate`: `22 todos, 208 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate 9f8d6aa; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest 6db96ffebee1c42dff38ecc87d5fddebd3ec19bb4ed2edce4ee8865f09332680; path docs/reviews/00-workspace/D00-T01-s40.md; run 20260918-D00-T01-S40-gpt

`python3 scripts/todo-graph.py plan --check`: `implementation plan is current -- 63/203 sections complete (31%)`, exit 0.

Provenance: candidate 9f8d6aa; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest edb6d778cb08193e102e0f1892b560ca51c89f677e77c283b388681a3d2b5381; path docs/reviews/00-workspace/D00-T01-s40.md; run 20260918-D00-T01-S40-gpt

`python3 scripts/todo-graph.py query plan-health --check`: `gate: ok`, exit 0.

Provenance: candidate 9f8d6aa; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 83e9a060459d5921fedae881c1e079a0169941dd7fcff74fabc57c3188d0943b; path docs/reviews/00-workspace/D00-T01-s40.md; run 20260918-D00-T01-S40-gpt

`.tools/dotnet-linux-x64/dotnet build src/Notepad.Neutral.slnf --nologo`, exit 0:

```
Build succeeded.
    0 Warning(s)
    0 Error(s)
```

Provenance: candidate 9f8d6aa; command `.tools/dotnet-linux-x64/dotnet build src/Notepad.Neutral.slnf --nologo`; exit 0; tool .NET SDK 10.0.400; digest ebd5c0266f10d9927a4d52d864e320c10f8fce88336e5718559c30a6ecabded2; path docs/reviews/00-workspace/D00-T01-s40.md; run 20260918-D00-T01-S40-gpt

CI build run 35399013132 success both jobs (build-linux, build-windows) on `373a7d3`; plan-gates run 35399013081 success (external runs, quoted by id).

## Self-review

Three questions, answered against the candidate diff, not memory. (1) Sense: the diff delivers the five contract items (props redirect, gitignore, CI moves, doc moves, stale cleanup) with no widening; the `Protocol.csproj` fixture re-point was required to keep the build green, not scope creep. (2) Logic: per-project `Bin/` subfolders match `MSBuildProjectName` stems (verified against csproj filenames); the `win-x64` RID leg survives because the app csproj declares it; intermediates stay per-project `obj/` as the contract states. (3) Blast radius: every consumer swept (CI workflows, docs, csproj/props/targets, tracked files, TODO) and moved; `dotnet test` proven from `Bin/` (343 passed); publish (`dist/`) and `TestResults/` untouched as specified.

Failure-mode sweep: config-only change (no code paths); no trusted values, no consent, no protocol, no frozen behavior, no UI surface. Known limit recorded honestly: the `soak.yml` golden glob follows the same verified pattern but its first live proof waits for a soak run (UI tests are Windows-only; the section checkpoint requires build plus smoke, both green).

Verdict: clean, no self-review fixes.

## Architecture gate

Not triggered. Build output layout is none of the trigger surfaces (editor state, storage, extensibility, AI integration, protocol, consent/undo). Absence of an `Architecture review` record follows the gate's own skip rule.

## GPT panel (round 1)

Candidates `373a7d3` plus `d9e8d17`, Sol medium, exit 0, `check-panel PASS`.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

## GPT panel (round 2)

Same candidates, Sol medium, exit 0, `check-panel PASS`, R1 output appended.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

## Opus panel (round 3)

Same candidates, Opus medium, exit 0, `check-panel PASS`, R1-R2 outputs appended. Sign-off round.

- `adversarial` approve
- `consistency` needs-attention (1): `docs/build.md:15` prose still asserted per-project `bin/` while line 27 named `Bin/`. Accepted genuine; fixed in the R3-fix commit.
- `integration` approve
- `record` needs-attention (1): item-4 Done overclaimed (path-shaped grep cannot see prose). Accepted genuine; prose sweep rerun clean, Done note corrected in the R3-fix commit.

R4 justification (soft-cap rule): the record finding implicates the stamp's own honesty (evidence proving less than claimed), which clears the stamp-invalidating bar for a post-sign-off round; the consistency fix rides the same commit.

```text
Raw Opus panel round 3 output (check-panel PASS four lenses, one verdict each):
**adversarial: approve**

**consistency: needs-attention (1)**
1. `docs/build.md:15` still states "outputs land under per-project `bin/` and `obj/`, publish output under `dist/`, all gitignored", the exact layout this section reverses, while line 27 of the same file now names `Bin\ScratchPad\Debug\...`. The file contradicts itself and `Directory.Build.props:8`.

**integration: approve**

**record: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:879` — item 4's Done claims "all three docs plus both workflows moved; old-shape grep clean across CI, docs, build files", but `docs/build.md:15` still asserts per-project `bin/` as the output home. The `src/*/bin/Debug` grep shape in the Done-when cannot see that sentence, so the evidence proves less than the claim.
```

## Opus panel (round 4)

Candidates `373a7d3` plus `d9e8d17` plus `916dfdc`, Opus medium, exit 0, `check-panel PASS`, R1-R3 outputs appended. Post-sign-off confirmation (justified: R3 record finding cleared the stamp-invalidating bar).

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

## Plan review

Manifest: sections [D00 T01 §40, D00 T01 §1, D00 T01 §2]; dependents [none]; bytes 11134; run 20260918-D00-T01-S40-gpt

Reviewer `gpt-5.6-sol` high, exit 0, `check-plan PASS 14 findings`. Triage: 11 accepted (9 filed at §41, 2 fixed in the stamp run), 3 rejected with reasons; no criticals. Counts in accepted findings versus implementation items: 9 findings into 7 §41 items (PR1+PR2 merged as one pointer item, PR7+PR8 merged as one probe item); PR13 ticked the Commit item and PR14 completed the checkpoint sentence in this run.

Ledger:
- [D00-T01-S40-PR1] [minor] §2 keeps contradicting §40 without a pointer -> filed D00 T01 §41
- [D00-T01-S40-PR2] [minor] §1 gitignore change wants its own pointer -> filed D00 T01 §41
- [D00-T01-S40-PR3] [minor] Duplicate project names could collide silently -> filed D00 T01 §41
- [D00-T01-S40-PR4] [minor] Soak glob lacks execution proof -> filed D00 T01 §41
- [D00-T01-S40-PR5] [minor] Release and RID builds unproven -> rejected Release build green in this record and the CI smoke path proves the win-x64 leg; the mechanism is configuration-agnostic
- [D00-T01-S40-PR6] [minor] dist-untouched claim untested -> rejected no publish flow exists (grep clean) and default publish nests under Bin/ by construction; D07 owns publish design
- [D00-T01-S40-PR7] [minor] No evaluated-property inventory over projects -> filed D00 T01 §41
- [D00-T01-S40-PR8] [minor] git status cannot see ignored-dir contents -> filed D00 T01 §41
- [D00-T01-S40-PR9] [minor] Cleanup lacked an allowlist procedure -> rejected a `bin` dir under a project root is definitionally build output and all targets were gitignored; the Done note is history, not a reusable procedure
- [D00-T01-S40-PR10] [minor] Artifact contents unasserted -> filed D00 T01 §41
- [D00-T01-S40-PR11] [minor] No stable launcher over the deep tree -> filed D00 T01 §41
- [D00-T01-S40-PR12] [minor] Stale binaries risk under the shared root -> filed D00 T01 §41
- [D00-T01-S40-PR13] [minor] Commit item unticked with evidence landed -> accepted Commit item ticked in the stamp run; no TODO work
- [D00-T01-S40-PR14] [minor] Checkpoint omits graph gates -> accepted checkpoint sentence completed in the stamp run; no TODO work
End of ledger

```text
Raw plan review output (check-plan PASS 14 findings, one per line):
- The effective output contract remains contradictory between §2 and §40; add a non-checklist “Superseded by D00 T01 §40” annotation and reciprocal cross-reference in §2, because readers and automation should not have to infer which stamped statement governs.
- The `.gitignore` change also modifies §1’s repository-hygiene contract but §40 references only §2; add a reciprocal §1 cross-reference or supersession note, because the plan requires bidirectional connected-section traceability.
- `Bin/$(MSBuildProjectName)/` does not actually guarantee collision safety when two projects share an MSBuild project name; add a uniqueness validator or use a repository-relative project path in §40, because a future duplicate can silently overwrite assemblies and symbols.
- The `soak.yml` path change has no matching execution evidence; add a deliberately induced golden-failure soak run that proves files are discovered and uploaded from `Bin/`, because a successful ordinary build cannot validate a failure-only glob.
- The acceptance checks prove only Debug outputs even though the contract explicitly promises Configuration, TargetFramework, and RID partitioning; add Release and representative RID builds in §40, because configuration or RID collisions often remain invisible in the default build.
- The claim that `dist/` is untouched is untested; add a publish checkpoint asserting publish output remains under `dist/` and not `Bin/`, because MSBuild output properties can affect derived publish paths.
- “Every project emits under root `Bin/`” is verified through selected builds rather than an evaluated-property inventory; add a test that enumerates every tracked project and asserts its final `BaseOutputPath`/`OutputPath`, because project-level overrides can bypass the central property silently.
- The clean-worktree check cannot detect unwanted files inside ignored directories; add explicit filesystem assertions for allowed `Bin/<Project>/<Configuration>/<TFM>[/<RID>]` shapes and forbidden source-tree `bin/` paths, because `git status` alone provides weak output-placement evidence.
- The cleanup item removes stale trees without first distinguishing generated directories from potentially intentional fixtures; add an exact allowlisted discovery and deletion procedure in §40, because broad future reuse of this instruction could destroy non-generated content.
- The CI artifact check records that an artifact uploaded but not what it contained; add manifest assertions for the expected executable, DLLs, symbols, provenance version, and absence of unrelated project outputs, because a premium build pipeline should make artifact composition deterministic and auditable.
- Root `Bin/` improves discoverability but still leaves operators navigating project/configuration/framework layers; add a stable launcher or `build/latest` pointer owned by §40 or a follow-up workspace section, because one-command launch materially improves the premium developer experience without flattening collision-safe outputs.
- Centralized outputs create a new stale-binary risk across renamed or removed projects; add a documented clean command and CI clean-build gate in §40, because obsolete binaries under an ignored shared root can be mistaken for current products.
- The section has completed implementation and CI evidence while its sole commit row remains unchecked; reconcile the commit identity and candidate range before review, because plan state currently cannot establish which exact bytes the evidence covers.
- The test checkpoint omits `todo-graph.py validate`, `plan --check`, and relevant self-tests after changing stamped-plan relationships; add those gates to §40, because implementation success does not prove the canonical dependency graph and plan projection remain valid.
```

## Rejection evidence (PR5, PR6)

Release: `dotnet build src/Notepad.Neutral.slnf -c Release` exits 0 warnings 0 errors with `Bin/Unit/Release/net10.0/` populated; RID leg proven by CI build run 35399013132 (smoke launched `Bin\ScratchPad\Debug\net10.0-windows10.0.19041.0\win-x64\ScratchPad.exe`). Publish: grep for publish flows across `.github/`, `tools/`, `docs/build.md`, `docs/bootstrap.md` is clean (none exist); a default `dotnet publish tests/Unit/Unit.csproj -c Release` nests at `Bin/Unit/Release/net10.0/publish/` by construction. D07 owns publish design.

## Observation (not filed)

§35 shipped with its Commit item unticked. The validator exempts `commit:` lines from `orphaned-items-shipped` by design, and §35's Review line already names every candidate, so no evidence is lost; ticking a stamped checklist post-hoc is riskier than the cosmetic gap. Recorded here instead of filed; tick opportunistically on the next §35-touching run if desired.
