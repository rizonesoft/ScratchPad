# Review: D00 T01 §34 -- Amendment and Runner Residuals

Candidate: `885414f` + `ed3d76a` + `de9787a` (SHIP plus self-review plus the R1 fix; output `check-panel PASS` on round 1). Panel tier: Full (the diff touches `scripts/review_prompt.py`, `scripts/todo-graph.py`, `scripts/todo-validate.py`, the review skill, README, AGENTS.md, and the TODO contract), Sol rounds 1-2, Opus round 3 up (sign-off plus confirmations).

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. One self-review fix commit (`ed3d76a`): item 9 rewrote the risk-register header sentence, so the committed register read stale under `--check`; re-synced before the panel.

Does it make sense: seven items take the amendment identity token plus the restate-everything contract, the four runner bounds in `collect_producer`, strict UTF-8 plus NUL rejection on all three input paths, the ASCII run-suffix gate plus the frontmatter string-form gate, the `{1,9}` family sweep over every `int()` feeder, and the six-verb `review-run` subcommand. Exactly the contract, including the recorded shapes (12-hex fingerprint over the target triple, restate-everything with free re-verdict, token as one whitespace-delimited run, bare-hex artifact names under ignored `build/review-runs/`).

Is it logical: the identity token recomputes from the target row so unrelated same-namespace links cannot validate; the rationale rule rides the head so superseded rows stay silent; the collector kills mid-stream with the failing reason naming the offset; the minter walks past scan claims; the ledger appends after the store so a stored artifact is never unledgered. The panel later tightened nine spots self-review missed (char tokens, stderr drain, finite timeouts, the XREF boundary, mandated-only rationale, run uniqueness, logical lines, failing-chunk evidence, ending-preserving State blanking).

Did it break anything else: suite moves 968 to 1011 by the new fixtures; live validate stays 0 fatal with the register re-synced; plan-health --check stays green. Blast radius is the ledger-amendment rules, the runner, the run-id minter, the digit grammars, and their fixtures, all covered.

## GPT panel (round 1)

- `adversarial` needs-attention: (1) tokens measured as UTF-8 bytes under an ASCII regex; (2) stderr drain stops at the capture cap; (3) infinite timeouts overflow the queue wait.
- `consistency` needs-attention: (1) XREF_RE truncates 10-digit runs to 9 digits; (2) mandated owner/due/trigger/filed-target text passes as amendment rationale.
- `integration` needs-attention: (1) repeated runs re-record the same run ID; (2) newline bytes counted instead of logical lines.
- `record` needs-attention: (1) the stored artifact omits the failing chunk; (2) State blanking swallows newline drift.

Fix record: tokens count characters under Unicode whitespace with carry in chars; stderr drains to EOF retaining the cap; timeouts refuse past a one-day ceiling in CLI plus library; XREF carries a trailing digit boundary with an over-long companion diagnostic on marker plus ledger sides (filed-only-over-long rows no longer misreport no target); rationale strips disposition-mandated restatements; the ledger joins mint claims with an append-time re-mint guard and PID-suffixed artifact temps; lines count splitlines incrementally over `read1` streaming (the `read()` full-buffer block was caught by the new mid-stream probe); raw keeps the failing chunk; the blanker preserves endings with the committed side read untranslated. Fixed in `de9787a` (suite 1011 to 1031, every fix red-verified against pre-fix code); gates re-ran green before round 2 (validate 0 fatal, plan current, register current).

Raw round-1 output (fenced, not part of the verdict scan; digest `179e43600bcf3d9d430e39e378b84c00eaecfcdacae1839ef5f7ec5a6c5193d2`):

```
**adversarial: needs-attention** (3)
1. `scripts/review_prompt.py:375-403` measures tokens as UTF-8 bytes with an ASCII byte regex, so a valid 4096-character non-ASCII token can exceed the cap and Unicode whitespace does not delimit tokens as contracted.
2. `scripts/review_prompt.py:346-356` stops draining stderr after the capture cap; a producer that continues writing stderr can fill the pipe, block, and incorrectly fail only when the wall-clock timeout expires.
3. `scripts/review_prompt.py:566-575` accepts infinite timeout values such as `--timeout 1e309`; the resulting `queue.get(timeout=inf)` can raise `OverflowError` instead of returning a bounded diagnostic.

**consistency: needs-attention** (2)
1. `scripts/todo-graph.py:62` gives `XREF_RE` no trailing digit boundary, so a section reference containing ten or more digits is silently truncated to its first nine digits instead of reaching an existing structural diagnostic.
2. `scripts/todo-validate.py:1739-1748` treats mandatory owner, due, trigger, or filed-target text as amendment rationale after removing only the supersedes and identity tokens, so accountability-bearing heads with no actual rationale pass the new non-empty-rationale rule.

**integration: needs-attention** (2)
1. `scripts/review_prompt.py:591-650` mints from caller-supplied scan files without checking the run ledger before appending; repeated or concurrent atomic runs can record the same run ID multiple times, violating run uniqueness.
2. `scripts/review_prompt.py:389-391` counts newline bytes instead of logical lines, allowing a final unterminated 100001st line through the runner boundary and leaving the checker to reject it only after the producer completes.

**record: needs-attention** (2)
1. `scripts/review_prompt.py:377-411` appends a stdout chunk to `raw_parts` only after validating it, so the stored artifact and digest omit the chunk that triggered a byte, token, NUL, or UTF-8 failure and do not preserve the collected failing evidence.
2. `scripts/todo-graph.py:1057-1071` rebuilds normalized text with `splitlines()` and `"\n".join()`, causing `risk-register --check` to ignore missing terminal newlines and line-ending drift in addition to the intended State-cell differences.
```

## GPT panel (round 2)

- `adversarial` needs-attention: (1) unbounded chunk queue between the stdout reader and validator; (2) scan-file reads crash on malformed UTF-8 in both `run-id` and `run`; (3) malformed UTF-8 offsets ignore decoder-held bytes from the prior chunk.
- `consistency` needs-attention: (1) identity tokens accepted before the supersedes link against the documented grammar.
- `integration` needs-attention: (1) startup-failure metadata omits the `raw` field the run branch indexes unconditionally; (2) register sync writes through newline-translating `write_text` while `--check` reads untranslated.
- `record` needs-attention: (1) ledger guard read plus append are separate unlocked operations across processes.

Fix record: the queue carries `maxsize=COLLECT_QUEUE_CHUNKS` (32) with a post-reap drain freeing a backpressured reader; scan reads catch `UnicodeDecodeError` into a naming exit-2 diagnostic on both subcommands; the mid-stream offset subtracts decoder-held bytes (flush stays end-relative, commented); identity matches only past the supersedes link end; startup failure returns `"raw": b""` and ledgers an empty artifact; sync writes with `newline=""` so sync-then-check round-trips on every OS; the guard read plus append ride `_record_run` under a sidecar file lock (fcntl/msvcrt, fail-closed with no primitive). Fixed in `3b12ec7` (suite 1031 to 1038, each fix read against its finding with its fixture: queue spy, split-sequence offset, malformed-scan pair, misplaced-identity PR12, unstartable-producer ledgering, byte-exact sync, eight-thread locked recording); gates re-ran green before round 3 (validate 0 fatal, plan current, register current). The commit also carries the four Rizonesoft brand logo files under `resources/` (user-ordered single commit; inert assets for unshipped D07 work, outside the reviewed candidate paths) plus a test-only placement fix moving the byte-exact sync under the fixture redirect (as first written the sync targeted the real tree and the assertion could never pass).

Raw round-2 output (fenced, not part of the verdict scan; digest `11b36fd05c9efc5d9b9beab9a3b2a6d868d47282269e83576987cf2fb2cb7028`; run `20260919-D00-T01-S34-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (3)
1. `scripts/review_prompt.py:358-375` uses an unbounded queue between the stdout reader and validator, so a fast hostile producer can enqueue arbitrary output before the byte cap is processed and exhaust memory.
2. `scripts/review_prompt.py:310-318` does not catch `UnicodeDecodeError` while reading scan files, so malformed UTF-8 crashes both `run-id` and `run` instead of producing the required stable diagnostic.
3. `scripts/review_prompt.py:464-480` calculates malformed UTF-8 offsets from the current chunk boundary even though the incremental decoder may prepend buffered bytes from the prior chunk, producing an incorrect offset when an invalid sequence crosses chunks.

**consistency: needs-attention** (1)
1. `scripts/todo-graph.py:1582` and `scripts/todo-graph.py:1682-1687` accept `identity <12hex>` anywhere on the disposition tail, including before `supersedes <id>`, while the documented amendment grammar requires the identity token after the supersedes link.

**integration: needs-attention** (2)
1. `scripts/review_prompt.py:346-348` returns startup-failure metadata without the documented `raw` field, then `scripts/review_prompt.py:711-713` indexes that field unconditionally, so a missing or unstartable producer crashes the atomic command with `KeyError` instead of storing and ledgering a failed run.
2. `scripts/todo-graph.py:4131-4144` writes the synchronized register through `Path.write_text()` with platform newline translation but reads it back with translation disabled; on Windows, `--sync` can create CRLF bytes that an immediate `--check` rejects as drift.

**record: needs-attention** (1)
1. `scripts/review_prompt.py:749-774` checks run uniqueness and appends the ledger in separate unlocked operations, so concurrent processes can both observe the ID as absent and append duplicate run receipts, violating the atomic run and unique lineage contract.
```

## Opus panel (round 3)

- `adversarial` needs-attention: (1) head-rationale strip leaves multi-word trigger residue and over-long filed targets as passing rationale.
- `consistency` approve
- `integration` approve
- `record` needs-attention: (1) AGENTS.md self-test count stale at 1031 against the live 1038.

Sign-off disposition: both findings sit below the blocking bar (validator edge gaps reachable only through malformed heads, plus a doc ratchet), so per the Full sign-off rule each files through add-todo and the panel closes without re-rounding: adversarial 1 joins D00 T01 §51, record 1 joins D00 T01 §55 (commit `b87bbec`). Verified genuine against source before filing: the strip at `scripts/todo-validate.py:1774` removes `trigger` plus one token while the triple leg requires only one, and the filed leg at `:1776` strips `XREF_RE` but not `OVERLONG_REF_RE`; the AGENTS line reads 1031 while the suite reports 1038.

Architecture gate: not triggered (tooling-only candidate: runner, validator, ledger; none of the six surfaces).

Raw round-3 output (fenced, not part of the verdict scan; digest `d199e9e18d792c8f183625bcd096573d3d0aab58a9cc2bff0785fe46c7a82a49`; run `20260919-D00-T01-S34-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-validate.py:1774` — the head-rationale strip removes only one trigger token (`re.sub(r"\btrigger\s+\S+", "")`), but `trigger <t>` is unbounded prose in the shipped grammar (`scripts/todo-validate.py:1043` requires only one token, and `todo/README.md` writes `trigger <t>`): a bare deferred head `-> deferred owner ann due 2099-05-05 trigger next release supersedes <id> identity <hex>` leaves `next release` as residue, so `_rrest.strip(",; \t")` is non-empty and the no-rationale flag never fires — exactly the R1 consistency 2 defect this strip was added to close. Same family, second site: `scripts/todo-validate.py:1776` strips `graph.XREF_RE` for filed heads but not `graph.OVERLONG_REF_RE`, so a bare filed head naming `§9999999999` (a shape this same candidate teaches the validator to accept as a target at `scripts/todo-validate.py:1056`) leaves the target text as its rationale and passes unflagged.

**consistency: approve**

**integration: approve**

**record: needs-attention (1)**
1. `AGENTS.md:55` — the self-test case count is stale at the candidate tip: the line reads `1031 cases, must stay green` while `python3 scripts/todo-graph.py self-test` at HEAD (`6b96b3f`, clean tree) reports `1038 cases, 0 failed`, so the only documented ratchet for the suite undercounts by 7 and a silently dropped block of cases would read as green.
```

## Plan review

Round: `gpt-5.6-sol` high over §34 plus §20 plus §23 plus §29 plus §51 plus §55, 2026-09-19. Neighbors: §20 (Depends), §23 (Depends), §29 (XREF), §51 plus §55 (XREF). Dependents: D00 T01 §51, D00 T01 §55 by the mechanical rule (XREF-only consumers; no reverse Depends rows). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (21 findings, one per line). Run note: the plan run shares its base with the round-2 panel run (`20260919-D00-T01-S34-gpt`); kind distinguishes them and the minter is authoritative. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §34, D00 T01 §20, D00 T01 §23, D00 T01 §29, D00 T01 §51, D00 T01 §55]; dependents [D00 T01 §51, D00 T01 §55]; bytes 52440; run 20260919-D00-T01-S34-gpt

Ledger (namespaced IDs per the §16 shape; 6 of 21 filed at §55, 0 duplicates, 15 rejected with reasons on their rows):

Ledger:

- [D00-T01-S34-PR1] [minor] Triple hash is not a stable finding identity -> rejected finding IDs are already stable (DNN-TNN-SN-PRN); the token is per-link content proof recomputed over the named target, and identical triples cannot survive duplicate triage
- [D00-T01-S34-PR2] [minor] Free re-verdict needs transitions plus authority -> rejected reversals are chain-visible never silent; the transitions half disputes the recorded free re-verdict (§34 item 2); the authority half is answered by the S21-PR13 trust-model triage
- [D00-T01-S34-PR3] [minor] Day-scoped run IDs collide across clones -> filed D00 T01 §55
- [D00-T01-S34-PR4] [minor] Append-time re-mint changes a used ID -> rejected the pre-record ID is consumed nowhere (the prompt is caller bytes, the checker is shape-only); first consumption is the locked append
- [D00-T01-S34-PR5] [minor] Artifact plus ledger need a journaled transaction -> rejected disproportionate for a local single-writer ledger that fails loud; torn-line detection rides the PR21 lifecycle item
- [D00-T01-S34-PR6] [minor] Ignored artifacts leave bytes unrecoverable -> rejected all three transcriptions are byte-exact (fenced bytes re-hash to the recorded digests); the tracked record carries the bytes
- [D00-T01-S34-PR7] [minor] Receipt omits prompt model effort argv dirty-state -> filed D00 T01 §55
- [D00-T01-S34-PR8] [minor] Unpinned HEAD records the wrong tree -> rejected the candidate resolves pre-producer (pinned starting tree); PR7's dirty-state capture covers drift detection
- [D00-T01-S34-PR9] [major] Reap orphans descendant processes -> filed D00 T01 §55
- [D00-T01-S34-PR10] [minor] Streams need a combined cap plus framing -> rejected both streams are already capped (byte cap plus 64KB stderr); no consumer needs cross-stream order
- [D00-T01-S34-PR11] [minor] Raw capture needs secret redaction -> rejected no secrets in tree; transcription is operator-eyed; local artifacts inherit umask
- [D00-T01-S34-PR12] [major] Nine-digit cap still overflows date arithmetic -> filed D00 T01 §55
- [D00-T01-S34-PR13] [minor] Repeated bounded-int regexes will regress -> filed D00 T01 §55
- [D00-T01-S34-PR14] [minor] State blanking hides corruption plus staleness -> rejected the recorded gate-stability trade (§34 item 9); the register header forbids manual edits
- [D00-T01-S34-PR15] [minor] Clock needs timezone instant today-parity -> rejected already true: UTC single capture per invocation, --today spans all five clock queries
- [D00-T01-S34-PR16] [minor] Checkpoint omits joiners plus concurrency -> rejected the checkpoint executes and fails via fixtures; joiners are owned-by-design in §34
- [D00-T01-S34-PR17] [minor] Strip fix belongs with §34 not §51 -> rejected the joiner mechanism by design; table order sequences §34 before §51
- [D00-T01-S34-PR18] [minor] §55 unprovable item open despite no residual -> rejected disposition-at-build is the mechanism; §33's claim awaits §55's verification
- [D00-T01-S34-PR19] [minor] §55 catch-all needs cap lift or new file -> rejected the new-file path already exists (validator comment plus create-todo); rehoming breaks stable addresses; per-item fixtures bound the review
- [D00-T01-S34-PR20] [minor] §51 needs splitting for review safety -> rejected no split mechanism under stable addresses; same disposition as PR19
- [D00-T01-S34-PR21] [minor] Artifact retention plus GC unowned -> filed D00 T01 §55
End of ledger

Raw plan-review output (fenced, not part of any scan):

```
- The 12-hex hash of mutable subject, severity, and disposition is an edge checksum, not a stable finding identity, and identical triples or collisions can link unrelated rows; D00 T01 §34 should assign an immutable finding ID and use the full digest for integrity.
- Free re-verdicting lets any amendment silently reverse any disposition with only free-form rationale; D00 T01 §34 should define allowed transitions and require machine-checkable authority or evidence so accountability survives reversals.
- Day-scoped IDs minted from a findings file plus an ignored local ledger can collide across clones, branches, or concurrent machines, contradicting D00 T01 §20’s one-run-everywhere contract; D00 T01 §§20/34 need globally unique IDs or a durable shared claim namespace.
- An append-time re-mint can change the run ID after the producer and checker have already used the original ID; D00 T01 §34 should reserve the ID atomically before execution and record abandoned reservations explicitly.
- Artifact creation and JSONL append are not one atomic transaction, and no lock, fsync, partial-line recovery, or idempotent retry contract is owned; D00 T01 §34 should add a journaled state machine so crashes and concurrent runs cannot orphan artifacts or corrupt receipts.
- Review artifacts and their ledger live under ignored `build/`, while D00 T01 §55 expects provenance bytes to resolve from a recorded candidate tree; D00 T01 §§34/55 must define promotion to durable tracked evidence or a durable external manifest.
- The receipt binds HEAD, run, and output digest but not the exact prompt, model, effort, producer argv, checker version, repository dirty state, or starting tree; D00 T01 §34 should capture these safely so a run can be reproduced and audited.
- Capturing HEAD without pinning it before launch allows a long review to inspect one tree and record another after HEAD or the worktree changes; D00 T01 §34 should snapshot the candidate and prompt inputs at reservation time and fail if mutable inputs drift.
- Killing only the immediate subprocess can leave descendants alive and pipes open after timeout or cap failure; D00 T01 §34 should own process-group or Windows Job Object termination and verify descendant cleanup.
- The runner contract does not clearly cap combined stdout and stderr or preserve their framing and ordering; D00 T01 §34 should bound both streams and store them separately or in a framed format so stderr floods cannot evade resource limits or destroy forensic meaning.
- Raw producer output is persisted without a secret-detection, redaction, access-permission, or safe-retention contract; D00 T01 §34 should add credential-safe capture because content-addressed storage otherwise makes accidental secret leakage durable.
- A nine-digit syntax cap prevents CPython’s integer conversion crash but still permits values that overflow date arithmetic or trigger pathological work, especially `--within-days`; D00 T01 §34 should add field-specific semantic maxima and boundary fixtures.
- Repeating bounded-integer regexes across many parsers leaves future `int()` sites free to regress; D00 T01 §34 should centralize ASCII bounded parsing or add a structural regression check rather than relying on a one-time sibling sweep.
- Blanking the risk-register `State` column during comparison also ignores manual corruption and leaves the committed register visibly stale after a date crossing; D00 T01 §§29/34 should compare stable source fields while rendering live state from a declared as-of date or refreshing a published CI artifact.
- Date-derived behavior lacks an explicit timezone and single captured clock instant; D00 T01 §§29/34 should define UTC or repository-local calendar semantics and expose `--today` consistently so midnight crossings cannot make gates flaky.
- The D00 T01 §34 test checkpoint and commit scope omit the two open waiver and risk-register joiners, as well as concurrency and crash recovery; the section should extend its checkpoint and subject or move those items to a separately owned section.
- The head-rationale residue fix is placed in D00 T01 §51 even though it changes D00 T01 §34’s amendment grammar and is absent from §51’s checkpoint; it should remain with the amendment validator or gain an explicit dependency and checkpoint assertion.
- D00 T01 §55 leaves “Unprovable reads indeterminate” unchecked while its reconciliation says there is no residual, and other rows mix shipped halves with open halves; split residual work into new rows and close delivered rows so readiness and completion are truthful.
- D00 T01 §55 has become a heterogeneous catch-all because of the 55-section cap, combining clearance semantics, provenance, CI, diagnostics, inventories, and documentation in one commit; lift the artificial cap or open another TODO file so each independently reviewable behavior has a focused owner and gate.
- D00 T01 §51 similarly combines ordering, lineage, digest scope, reopen generations, outcomes, stable IDs, and amendment parsing into one candidate; split the schema migration from query semantics and amendment validation to reduce review risk and enable incremental rollback.
- No section owns artifact retention, quota, garbage collection, or protection of artifacts still referenced by receipts; D00 T01 §34 should add reference-aware lifecycle rules so premium audit history does not either fill disks indefinitely or disappear unexpectedly.
```
