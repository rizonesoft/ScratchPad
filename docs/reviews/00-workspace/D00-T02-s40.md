# D00 T02 S40 review - Trend and Telemetry Second Residuals

Candidate: 5106710 (Started plus implementation), 999db65 (round-1 fixes), e39b6f5 (round-2 fixes), ef5fb20 (sign-off R3-F1..F3 fixes), e90b0dd (round-4 fixes), 8d8bbbf43d21595b5dcf0eb785b439ec0279e8ec (round-5 below-bar fixes, reviewed tip).
Tier: Full panel. The diff changes shared nightly tooling: the trend model in `tools/NightlyParse.ps1` (night slots, cohorts, alerts, attribution, metrics store, disclosure), `tools/NightlyTrend.ps1`, `tools/NightlyMorning.ps1`, `tools/NightlyNotify.ps1`, `tools/NightlyRetention.ps1`, the nightly result writer in `tools/nightly.ps1`, and `docs/testing.md`; consumers include the morning digest, recovery notices, retention prune, and the ack gate's night keys.

## Evidence map

- Item 1 (host slots): `Get-NightSlotKey`, `Get-ResultHostKey`, `Get-HostKey`; `nightly.ps1` writes `hostKey`; `Select-CanonicalRuns`, `Get-RecoveryNotices`, and `Get-TrendAlerts` key or partition by host; fixtures `s40-two-hosts-key-apart`, `s40-other-host-never-joins-the-baseline`, `s40-recovery-is-host-scoped`. Plan correction: key is `night|host` (Corrected 2026-09-25 in the item).
- Item 2 (repeated trigger): the `repeated timer trigger` label in `Select-CanonicalRuns`; fixture `s40-repeated-trigger-reads-one-night`.
- Item 3 (schedule history and grace): `Read-ScheduleHistory`, `Get-ScheduleEntry`, `Test-NightScheduled`, the pending row in `Format-TrendTable`, `docs/nightly-schedule-history.md`; fixtures `s40-schedule-edit-keeps-history`, `s40-running-night-reads-pending`.
- Item 4 (rebaseline): `Select-SameCohort` and the `- Rebaseline:` line in `Get-TrendAlerts`; fixtures `s40-cross-cohort-rebaselines`, `harness-change-rebaselines`.
- Item 5 (unknowns): `Get-CohortChanges` treats unknown as a change, `Get-RunCohort` drops the count fallback; fixture `s40-unknown-population-is-not-equivalent`.
- Item 6 (boundaries): fixtures `s40-boundary-exactly-125-percent`, `s40-boundary-sixty-second-floor`, `s40-boundary-exactly-five-samples`, `s40-boundary-exactly-two-points`, `s40-boundary-exactly-120-percent-shift`; the detector table in `docs/testing.md`.
- Item 7 (coverage): unique-identity cap and `populationState` (written by `nightly.ps1`); fixture `s40-coverage-counts-unique-tests`.
- Item 8 (exclusions): `Read-NightlyExclusions`, `Set-ResultExclusions`, the excluded row and `- Excluded:` line, budget and env skips, `docs/nightly-exclusions.md`; fixture `s40-exclusion-never-degrades`.
- Item 9 (prune lock): `Remove-ArchivedStamp`, used by `NightlyRetention.ps1 -Prune -Execute`; fixture `s40-archive-and-prune-share-the-lock`.
- Item 10 (store failures): injectable appender and `MetricsWriteError`, abandoned-mutex takeover, `Restore-MetricsStore` and `NightlyTrend.ps1 -Restore`; fixtures `s40-disk-full-keeps-the-store`, `s40-stale-lock-is-taken-over`, `s40-interrupted-compaction-restores`.
- Item 11 (native over backfill): the `mergedFrom` merge in `Sync-MetricsStore`; fixture `s40-partial-native-keeps-backfill-evidence`.
- Item 12 (mixed corpus): fixture `s40-mixed-corpus-structured-equivalence` compares structured rows and alert lists.
- Item 13 (pruned values): row fields `source`, `hostKey`, `derivation`; `Format-PrunedEvidence`; fixture `s40-pruned-value-explains-itself`.
- Item 14 (disclosure): `Protect-DisclosedObject` over rows and the `.bak`; fixture `s40-legacy-row-redacted-on-compaction`.
- Item 15 (alert lifecycle): `Get-AlertIdentity`, `Update-AlertLedger`, the trend's `- Alert lifecycle:` line, NightlyMorning step 1b notifies only new alerts; fixtures `s40-alert-raises-once-and-closes`, `s40-alert-corrected-and-host-scoped`. Default recorded: no alert-level acknowledgement.
- Item 16 (attribution): `$script:AncestryProbe`, `Get-CommitRangeShape`, `revisions` and `correlation, not cause` in `Format-AlertContext`; fixture `s40-attribution-reads-rollback-as-correlation`.
- Item 17 (storage policy): `$script:MetricsMaxBytes` refusal; fixture `s40-capacity-refuses-by-policy`; policy paragraph in `docs/testing.md`.

## Self-review

- Does it make sense: seventeen items from the §32 plan review, each a trend rule with a fixture; the two contract adjustments (the key shape and the alert ack default) are recorded in the items.
- Is it logical: unknowns never establish equivalence, other hosts never join a window, a prune re-verifies under the lock it shares with the store, and a failed or over-capacity append leaves the store's bytes as they were.
- Did it break anything: the canonical map's keys changed to slots, so every consumer was swept (`Format-TrendTable`, `Get-RecoveryNotices`, the ack gate uses `Get-ResultNight` directly); existing cross-cohort fixtures now expect a rebaseline; NightlyTrend, NightlyNotify, NightlyParse, NightlyAck, NightlyRetention, and NightDebt suites all green; a live render over 43 real results succeeds.

Arch trigger: none - nightly trend tooling in ignored scratch; no editor state, app storage format, extension API, AI, protocol, or consent/undo surface changes.

## GPT panel Round 1

Candidate 5106710, slot bulk, run 20260925-D00-T02-S40-codex-c06751119.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:3017-3023`: `AppendAllText` can write partially before throwing; the catch neither truncates nor restores the file. A disk-full failure can therefore change store bytes despite the claimed unchanged-store guarantee.

**consistency: needs-attention** (2)
1. `tools/NightlyParse.ps1:3830-3836`: Capping execution counts at the discovered population does not count unique test identities. Executing one test twice while another remains unexecuted still increases coverage, violating the duplicate-execution contract.
2. `tools/NightlyParse.ps1:3154-3156`: Same-night correction requires a different evaluation identity. Revising an existing result preserves its identity, so an alert that disappears after that correction remains open instead of closing as `corrected`.

**integration: needs-attention** (3)
1. `tools/NightlyParse.ps1:3378-3383`: Alert evaluation selects only the last row's host and discards every other known host. In a multi-host corpus, regressions on other hosts are never evaluated and their alert lifecycles cannot advance.
2. `tools/NightlyParse.ps1:2843-2850,3233,3832-3835`: Coverage reads `populationState`, but metrics serialization and reconstruction omit it. After pruning, failed discovery loses its unknown state and can render numeric coverage instead.
3. `tools/NightlyParse.ps1:3151-3162`, `tools/NightlyTrend.ps1:99-101`, `tools/NightlyMorning.ps1:69-72`: Every trend render replaces `lastRun.new` and `lastRun.closed` without tracking notification delivery. Rendering again before the morning sender consumes those transitions removes them, silently losing opening or recovery notifications.

**record: needs-attention** (2)
1. `tools/NightlyTrend.Tests.ps1:627-644`: The mixed-corpus fixture neither supplies a schedule change nor compares rendered output for that corpus. Its structured round-trip and alert-string comparisons do not prove the checked contract's mixed-corpus equivalence.
2. `tools/NightlyTrend.Tests.ps1:605-611`: The interrupted-compaction fixture completes compaction successfully, then corrupts the store and creates an unused temporary file. It tests backup restoration, but never interrupts compaction or proves recovery from its actual failure points.

Telemetry: round 1; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | 999db65 (partial append truncated to the prior length; `s40-partial-write-is-cut-back`) |
| R1-F2 | fixed | 999db65 (`executedUnique` from trx names; `s40-coverage-counts-unique-tests`) |
| R1-F3 | fixed | 999db65 (evaluation identity `#r<revision>`; `s40-evaluation-identity-carries-revision`) |
| R1-F4 | fixed | 999db65 (per-host groups; `s40-every-host-is-evaluated`) |
| R1-F5 | fixed | 999db65 (`s40-coverage-state-survives-metrics`) |
| R1-F6 | fixed | 999db65 (per-entry delivery flags; `s40-alert-delivery-survives-rerender`, `s40-morning-sends-pending-and-dry-run-keeps-it`) |
| R1-F7 | fixed | 999db65 (schedule change plus rendered comparison in `s40-mixed-corpus-structured-equivalence`) |
| R1-F8 | fixed | 999db65 (compaction fault points; `s40-interrupted-compaction-keeps-the-store`) |

## GPT panel Round 2

Candidate 74f6bc2..999db65, slot bulk, run 20260925-D00-T02-S40-codex-c06751119-r2. Round-1 findings R1-F1..F8 did not re-report.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:3135-3185`: `Update-AlertLedger` silently replaces an unreadable ledger with an empty ledger and overwrites it. Corruption therefore destroys pending notifications and delivery history, allowing previously delivered alerts to open again.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:3924-3933`: Completion grace compares today's time against today's trigger and otherwise marks yesterday due. For a 23:00 trigger, yesterday's run remains within grace until 03:30 but renders missing after midnight. Calculate completion deadlines using each scheduled night's full date and time.

**integration: needs-attention** (1)
1. `tools/NightlyParse.ps1:3287-3290,3439-3441`: `ConvertFrom-MetricsRow` drops `revision`, although alert evaluation now requires it to distinguish corrections. Metrics-only evaluations consequently use the same `identity#r` across revisions, so a same-night correction can leave a disappeared alert open. The prior correction fix does not survive metrics reconstruction.

**record: approve**

Telemetry: round 2; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R2-F1 | fixed | e39b6f5 (`Read-AlertLedger` fails closed; `s40-corrupt-ledger-fails-closed`) |
| R2-F2 | fixed | e39b6f5 (per-night deadlines; `s40-late-trigger-grace-crosses-midnight`) |
| R2-F3 | fixed | e39b6f5 (revision in the metrics row; `s40-revision-survives-metrics`) |

## GPT panel Round 3

Candidate 74f6bc2..e39b6f5, slot signoff, run 20260925-D00-T02-S40-codex-c06751119-r4. Round-1 and round-2 findings did not re-report. R3-F2 (a reopened alert's delivery confirmed unsent) and R3-F3 (two hosts' rows colliding in the store) are data-integrity findings, so round 4 runs on the depth slot.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:3019-3021`: Capacity enforcement adds `$payload.Length` (UTF-16 characters) to the existing byte length, but writes UTF-8. Non-ASCII content can therefore exceed `MetricsMaxBytes` without refusal. Measure the encoded payload’s byte count.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:3166,3208-3210`, `tools/NightlyMorning.ps1:80-83`: Notification keys identify an opening by alert ID and first night only. An alert opened, corrected closed, and reopened on the same night reuses its original opening key. The morning sender treats the reopening as a duplicate and confirms it without delivering it. Include a distinct lifecycle occurrence identifier.

**integration: needs-attention** (1)
1. `tools/nightly.ps1:1463`, `tools/NightlyParse.ps1:2845,3043-3047`: Run identities remain `stamp-pid`, and metrics rows are indexed by that identity without the host. Two hosts producing the same timestamp and PID collide in persistence despite having separate canonical slots; after pruning, one host’s evidence disappears. Scope persisted identities and their references by host.

**record: approve**

Telemetry: round 3; model gpt-6-astra; effort high; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R3-F1 | fixed | ef5fb20 (UTF-8 byte count; `s40-capacity-counts-utf8-bytes`) |
| R3-F2 | fixed | ef5fb20 (per-opening `occurrence`; `s40-reopening-is-its-own-occurrence`) |
| R3-F3 | fixed | ef5fb20 (`Get-MetricsKey` identity@host; `s40-hosts-sharing-an-identity-keep-their-rows`) |

## GPT panel Round 4

Candidate 74f6bc2..ef5fb20, slot depth, run 20260925-D00-T02-S40-codex-c06751119-r5. Round-1 to round-3 findings did not re-report. R4-F1 (an unrestorable backup), R4-F2 (a stale revision overwriting a correction), R4-F3 (raw and metrics renders disagreeing), and R4-F4 (another host's run answering a recovery) are data-integrity findings, fixed and re-run at round 5.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:3091`: Compaction replaces malformed backup lines with `rejected/1` markers, which `Read-MetricsStore` rejects as malformed. `Restore-MetricsStore` consequently refuses that backup, preventing restoration after compacting a store containing malformed lines.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:2985-2991`: Synchronization unconditionally replaces an existing row when its JSON differs, without comparing revisions. A stale native result can overwrite a newer correction under the same metrics key, violating revision-based authority.

**integration: needs-attention** (2)
1. `tools/NightlyTrend.ps1:72-78`: Merged native metrics rows are discarded when their raw results remain present, while superseded backfills are removed. The renderer therefore loses the backfill fields that `Sync-MetricsStore` preserved; those fields appear only after pruning the native result, breaking raw-versus-metrics equivalence.
2. `tools/NightlyNotify.ps1:545`: The previous canonical result is resolved by bare identity or stamp without checking its host. When two hosts share that identity, the first matching result can supply another host’s verdict and incorrectly create or suppress a recovery notice.

**record: approve**

Telemetry: round 4; model gpt-6-astra; effort high; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R4-F1 | fixed | e90b0dd (`rejected/1` counted, not malformed; `s40-backup-with-rejected-marker-restores`) |
| R4-F2 | fixed | e90b0dd (revision authority in `Sync-MetricsStore`; `s40-lower-revision-never-replaces`) |
| R4-F3 | fixed | e90b0dd (`Add-MergedEvidence`; `s40-live-render-takes-merged-evidence`) |
| R4-F4 | fixed | e90b0dd (host-scoped lookup; `s40-shared-identity-resolves-to-this-host`) |

## GPT panel Round 5

Candidate 74f6bc2..e90b0dd, slot depth, run 20260925-D00-T02-S40-codex-c06751119-r6. Round-1 to round-4 findings did not re-report. At the hard cap each leftover is dispositioned against the bar: R5-F1 is below it (supersession records hold only generated run identities, night dates, and timestamps, so no secret-shaped value can reach them in practice); R5-F2 is below it (the store stays correct under R4-F2; only a result file reverted on disk to a lower revision, which the writer never produces, could render stale). Both were fixed in 8d8bbbf and re-gated at the stamp (the §18/§21 precedent).

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:3111-3114`: Compaction sanitizes metrics rows but serializes retained `supersession/1` records unchanged. Sensitive strings in those records survive in the main store even though the backup is sanitized, violating the disclosure contract.

**consistency: approve**

**integration: needs-attention** (1)
1. `tools/NightlyTrend.ps1:72-76`: A live result always suppresses its stored metrics row, even when synchronization rejected that result as an older revision. With revision 2 stored and revision 1 present on disk, rendering and alert evaluation still use revision 1. Select the authoritative revision before excluding stored rows; `Add-MergedEvidence` only copies backfill fields.

**record: approve**

Telemetry: round 5; model gpt-6-astra; effort high; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | 999db65 |
| R1-F2 | fixed | 999db65 |
| R1-F3 | fixed | 999db65 |
| R1-F4 | fixed | 999db65 |
| R1-F5 | fixed | 999db65 |
| R1-F6 | fixed | 999db65 |
| R1-F7 | fixed | 999db65 |
| R1-F8 | fixed | 999db65 |
| R2-F1 | fixed | e39b6f5 |
| R2-F2 | fixed | e39b6f5 |
| R2-F3 | fixed | e39b6f5 |
| R3-F1 | fixed | ef5fb20 |
| R3-F2 | fixed | ef5fb20 |
| R3-F3 | fixed | ef5fb20 |
| R4-F1 | fixed | e90b0dd |
| R4-F2 | fixed | e90b0dd |
| R4-F3 | fixed | e90b0dd |
| R4-F4 | fixed | e90b0dd |
| R5-F1 | fixed | 8d8bbbf (`s40-supersession-records-sanitized-on-compaction`) |
| R5-F2 | fixed | 8d8bbbf (`Select-AuthoritativeResults`; `s40-stale-live-result-renders-the-stored-revision`) |

## Plan review

Round: `gpt-6-astra` medium (plan-primary slot) over §40 plus its Depends neighbor §32 and the trend contract §25, 2026-09-25. Output check-plan PASS (16 findings, one per line). Triage: 14 filed as the new D00 T02 §47, 2 rejected.

Manifest: sections [D00 T02 §40, D00 T02 §32, D00 T02 §25]; dependents [none]; bytes 25897; run 20260925-D00-T02-S40-codex-c06751119-r3

Ledger:

- [D00-T02-S40-PR1] [minor] schedule uniqueness per host, or a schedule part in the key -> filed D00 T02 §47
- [D00-T02-S40-PR2] [minor] ambiguity policy for the legacy host migration -> filed D00 T02 §47
- [D00-T02-S40-PR3] [minor] host identity stability across renames, reinstalls, clones -> filed D00 T02 §47
- [D00-T02-S40-PR4] [minor] acknowledgement for alerts on otherwise passing runs -> filed D00 T02 §47
- [D00-T02-S40-PR5] [minor] per-alert delivery, crash recovery, recurrence after recovery -> rejected: shipped in this candidate (per-entry delivery flags R1-F6, per-opening occurrence ids R3-F2, a closed id reopens as a new occurrence)
- [D00-T02-S40-PR6] [minor] returning cohorts, baseline expiry, prolonged insufficiency -> filed D00 T02 §47
- [D00-T02-S40-PR7] [minor] merge field dependencies and tombstones -> filed D00 T02 §47
- [D00-T02-S40-PR8] [minor] exclusion precedence against unproven and zero-execution runs -> filed D00 T02 §47
- [D00-T02-S40-PR9] [minor] sharded discovery and partial manifests in the denominator -> filed D00 T02 §47
- [D00-T02-S40-PR10] [minor] calendar states for cancellation, disabled schedules, overlaps, overruns -> filed D00 T02 §47
- [D00-T02-S40-PR11] [minor] restore preserves acknowledged writes and reports loss -> filed D00 T02 §47
- [D00-T02-S40-PR12] [minor] disclosure migration for existing backups and restores -> filed D00 T02 §47
- [D00-T02-S40-PR13] [minor] low-space warning and capacity against pruning -> filed D00 T02 §47
- [D00-T02-S40-PR14] [minor] historical queries across derivation versions -> filed D00 T02 §47
- [D00-T02-S40-PR15] [minor] complete the review record and stamp -> rejected: this review writes the stamp; the items are proven by the fixtures in the evidence map
- [D00-T02-S40-PR16] [minor] alert drill-through explains its calculation -> filed D00 T02 §47

End of ledger

Raw plan output (fenced; fences strip from every scan):

```
- §40 removes schedule identity based on an unenforced one-schedule-per-host-per-night assumption; own schedule uniqueness or retain schedule identity in §40, because overlapping trigger edits and catch-up runs can collide.
- §40 migrates all pre-host results to host `legacy` without an ambiguity policy; own collision detection and unresolved identity handling in §40, because historical runs from different hosts can still merge.
- Host identity has no stability contract; define identity across renames, reinstalls, cloned machines, and redaction in §40, because identity changes can split history or combine unrelated hosts.
- §40 delegates alert acknowledgement to the RED run without requiring every regression to produce one; own acknowledgement for alerts on otherwise passing runs in §40 and its §39 integration, because performance shifts and recurring flakes can remain unacknowledgeable.
- §25 routes regressions once per day while §40 requires stable alert lifecycles; specify per-alert delivery, crash recovery, and recurrence after recovery in §40, because daily deduplication can suppress distinct incidents or reopened regressions.
- Cohort rebaselining lacks a rule for returning cohorts and frequently changing harness versions; own baseline reuse, expiry, and visible prolonged insufficiency in §40, because repeated cold starts can indefinitely disable regression detection.
- §40 permits merging partial native rows with backfill fields without defining field dependencies or explicit deletion; own compatible revision rules and tombstones in §40, because merged counts, populations, and timings can describe no actual execution.
- §40 requires exclusions never to degrade a night, but does not bound that rule against §25’s unproven budget-cut and killed runs; clarify exclusion precedence and zero-execution status in §40, because an entirely filtered run must not appear healthy.
- Coverage identity is defined without specifying how shard discovery manifests form the expected population; own partial-discovery and missing-shard denominator rules in §40, because deduplicating available results alone can overstate coverage.
- Schedule history and grace periods lack explicit treatment of cancellation, disabled scheduling, overlapping jobs, and runs exceeding grace; extend §40’s calendar state transitions, because missing-run alerts otherwise depend on ambiguous job states.
- §40’s store recovery criterion requires only readability; require preservation of acknowledged writes and explicit reporting of unrecoverable loss in §40, because an empty or stale restored store can satisfy the stated checkpoint.
- §40 sanitizes legacy rows on compaction without specifying when existing backups and rejected-row archives are scrubbed; own disclosure migration and safe restore behavior in §40, because retained copies can preserve or reintroduce sensitive values.
- Capacity refusal has no defined interaction with archival prerequisites and raw retention; own low-space warning, backpressure, and operator recovery in §40, because refusing metrics writes also blocks pruning and can exhaust the disk.
- Equivalence after raw pruning is required, but equivalence after metrics retention or derivation-version changes is not bounded; define supported historical queries and explicit unavailable or incomparable states in §40, because old trends can silently change meaning.
- §40 lists all implementation items as complete without a verification stamp or candidate-bound evidence in the supplied section; complete its review record and map checkpoints to proof, because checked boxes alone do not establish that the residuals shipped.
- Alert drill-through provides attribution but no concise explanation of the triggering calculation; extend §40 with baseline values, included sample counts, exclusion reasons, and recovery criteria, because operators should be able to understand and act on an alert without reconstructing the detector.
```

## Live proof

Provenance: candidate 8d8bbbf43d21595b5dcf0eb785b439ec0279e8ec; command powershell -File tools/NightlyTrend.Tests.ps1 plus NightlyParse, NightlyNotify, NightlyRetention, NightlyAck, and NightDebt suites; exit 0; tool Windows PowerShell 5.1; digest 34c6381194630cf9e033c381ebeeda75a2b98327caa52c3276455e0265f1944b; path tools/NightlyParse.ps1; run 20260925-D00-T02-S40-codex-c06751119-r3

```
NightlyTrend.Tests: all green
NightlyNotify.Tests: all green
NightlyParse.Tests: all green
NightlyRetention.Tests: all green
NightlyAck.Tests: all green
NightDebt.Tests: all green
trend: 6 nights from 43 results (0 skipped)
- Alert lifecycle: 0 new, 0 persisting, 0 closed
```
