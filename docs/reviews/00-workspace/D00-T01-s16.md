# Review: D00 T01 §16 -- Second Plan-Review Residuals

Candidate: `d9eca6f` (ship). Tier: full panel (diff touches shared contract surface: `scripts/`, skills). Lenses owed: adversarial, consistency, integration, record (source-defect/design not owed: no Notepad behavior or surface at stake).

## Self-review

Worked the three questions across the diff and its blast radius; no fixes.

- Sense: all 15 items land as specified plus the commit. The --json guard edit is required by item 14 (the flag was fenced to adjacency); the §14 findings backfill and item pointer are items 11 and 1. No doc updates owed for --json (Tooling lines never listed flags).
- Logic: rule-17 cross-checks compare resolved keys (bare and full refs unify); outage skip is a documented substring; rule 18 date-matches rules 16-17; staleness flags growth only; filed rows fail closed on unresolvable, unverified, or unnamed targets. Backfill verified outside the gates (manifest plus 15/15 rows parse; severities and dispositions as recorded), since date-scoped rule 18 cannot see the pre-cutoff §14 record.
- Blast radius: query plus rules additive (no parser or STAMP changes this round); 469 pre-existing cases green; live validate silent; live plan-health quoted (1 marked, stale names §15, rest zero). No em dashes in the diff.

## Opus panel (round 1)

- `adversarial` needs-attention: (1) rule-18 ledger trigger matches any `- [` list item, so citation links, checkboxes, and bracket labels inside a `Plan review` section FATAL; only the intended PR-shape case is fixtured. (2) Rules 17b and 18 re-read findings per section with no `seen` dedup, so a shared file multi-fires once per covering section.
- `consistency` needs-attention: (1) `retry-owed` missing from all three marker-vocab enumerations (README dict, README severity row, skill marker sentence). (2) ID spec says zero-padded `SNN`/`PRNN` but nothing pads sections and the regex accepts both; the spec is a shape nothing produces. (3) TODO-07 Implementation Order column reads 1,2,3,4,6,7,8,5 while bodies run §1-§8.
- `integration` advisory: the plan-review prompt has no template block (boundary unenforceable); only the panel prompt does.
- `record` needs-attention: (1) SOURCE key still reads "15 filed, 5 rejected" (= 20) after the merge-count correction. (2) "not verified in marker" misstates a naming-membership check (resolution tests section existence, not stamps).

Fix: `c20f8dc` (trigger narrowed to PR-openers with prose decoys; rules 17b/18 first-reporter-wins; `retry-owed` swept; ID spec unpadded with backfill renumbered; TODO-07 table reordered; prompt template block; SOURCE count fixed; message renamed, bare ref quoted).

## Opus panel (round 2)

- `adversarial` needs-attention: (1) rule-17b dedup keys on the findings path while the check is per-marker, so only the first section over a shared file is ever checked and the §9 fixture locks a real omission silent. (2) LEDGER_LIKE_RE needs a well-formed namespace or bare-PR opener, so a mistyped namespace (`D00-T1x-S15-PR4`, `D00-T01-§15-PR1`) escapes to prose silently, the exact class rule 18 exists to catch.
- `consistency` needs-attention: SKILL.md:142 back-link example still carries zero-padded `PR04` against the unpadded canonical shape; the one surviving site of the swept family.
- `integration` approve
- `record` needs-attention: §16 items 12-13 still carry pre-R1 evidence with no dated note although R1 changed what those gates catch, and item 13's Done overstates dedup-limited coverage.

Fix: `d6eaaf7` (17b dedup dropped for per-marker checks, §9 fixture marker completed; LIKE gains case-sensitive-PR second alternative with a mangled-namespace probe; last padded ID fixed; items 12-13 dated R1-R2 notes).

## Opus panel (round 3)

- `adversarial` needs-attention: the contains-PR second alternative breaks both ways, verified by compiling: prose labels with PR numbers (`see PR12 upstream`, `Fixed in PR12`) false-FATAL, while a lowercase mangled namespace escapes because ROW is case-insensitive; the README does not state the asymmetry.
- `consistency` needs-attention: the fixture order-table §9 row still reads `Shared findings reported once` against the renamed heading; the only divergence in the table.
- `integration` approve
- `record` approve

Fix: `97768b2` (trigger reworked to single-token-plus-ID-tail in either case, 13/13 independent battery, 3 firing rows plus 6 decoys locked; fixture row renamed; README trigger stated exactly).

## Opus panel (round 4)

- `adversarial` needs-attention: the first alternative still has no ID-tail guard under IGNORECASE, so any bracket label starting with `pr` (`proposal`, `prior art`, `prose`, `Prompt`, `preview`) fires FATAL; only the second alternative got the R3 guard, and the docs pin the false positive.
- `consistency` approve
- `integration` approve
- `record` approve

Fix: `c2b866e` (trigger gains the ledger-structure half: adjacent second bracket or `->` after a PR-ish opener; PRX probe restructured with severity; 3 PR-starting prose decoys; 24/24 battery; README restated).

## Opus panel (round 5)

- `adversarial` needs-attention: (1) false positives survive: a pr-starting single-token label plus any `->` or adjacent second bracket fires (`prose` plus arrow, `process-todo-section` plus arrow, `proposal` plus `[major]`), and `->` is ordinary prose here. (2) False negative introduced: a namespaced opener that lost both severity and arrow (`[D00-T01-S15-PR4] oops lost the shape`) is now silent, reopening the R2 class; a full namespace opener is unambiguous and should not need the structure half.
- `consistency` needs-attention: README:387 and the code comment describe a stricter trigger (ID tail, structure-keeps-prose-silent) than LEDGER_LIKE_RE enforces; `[Prompt] [boundary]` and `[prose]` plus arrow both fire.
- `integration` approve
- `record` approve

## Review notes

Round 5 is the bound (max 5 reached), and the trigger unit was patched in 4 consecutive rounds, so the stop-and-re-think clause routes the leftovers here instead of a fifth patch: the round-5 adversarial pair plus the consistency overclaim are filed as D00 T01 §17 (item 20), tracked work, not dropped work. The shipped gate holds its mainline contract (valid rows parse, PRX-with-structure plus mangled-namespaced rows fire, nine prose decoys silent, live tree 0 FATAL); the residual is pr-token-plus-structure false positives and namespaced-without-structure false negatives.

## Plan review

Round: `gpt-5.6-sol` high via codex read-only, after panel-close, third live run of the procedure. Input manifest: D00 T01 §16 plus neighbor §15 (Depends plus XREF) plus reverse dependents (none; qualified grep for `D00 T01 §16` outside its own file returns nothing); prompt 17,103 bytes. Output: 25 one-line findings. No criticals (nothing safety/data/stamp-invalidating), so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §16, D00 T01 §15]; dependents [none]; bytes 17103

Ledger (namespaced IDs per the §16 shape; 24 of 25 filed, PR6 rejected):

**Migrated 2026-09-18 (§19):** `Ledger:`/`End of ledger` block markers added; rows untouched.

Ledger:

- [D00-T01-S16-PR1] [major] review IDs omit run identity, reruns collide -> filed D00 T01 §17
- [D00-T01-S16-PR2] [major] superseding-marker precedence undefined -> filed D00 T01 §17
- [D00-T01-S16-PR3] [major] marker states not mutually exclusive -> filed D00 T01 §17
- [D00-T01-S16-PR4] [minor] outage and retry lack owner, deadline, escalation -> filed D00 T01 §17
- [D00-T01-S16-PR5] [major] stale scope growth-only, misses removals -> filed D00 T01 §17
- [D00-T01-S16-PR6] [minor] manifest lacks content identity -> rejected proportionate-record (§15 PR6 precedent: bytes plus refs are the proportionate record for advisory review)
- [D00-T01-S16-PR7] [minor] byte count lacks encoding and EOL canonicalization -> filed D00 T01 §17
- [D00-T01-S16-PR8] [minor] scope misses XREF-only and transitive consumers -> filed D00 T01 §17
- [D00-T01-S16-PR9] [minor] grandfathered stamps vanish from uncovered reporting -> filed D00 T01 §17
- [D00-T01-S16-PR10] [major] filed critical clears against pre-finding verification -> filed D00 T01 §17
- [D00-T01-S16-PR11] [major] clearance needs no back-link or fix in target candidate -> filed D00 T01 §17
- [D00-T01-S16-PR12] [major] SOURCE back-links policy-only, unenforced -> filed D00 T01 §17
- [D00-T01-S16-PR13] [major] lifecycle transitions documented but unvalidated -> filed D00 T01 §17
- [D00-T01-S16-PR14] [major] deferred owner, date, trigger lack checks -> filed D00 T01 §17
- [D00-T01-S16-PR15] [minor] plan-health ignores overdue majors -> filed D00 T01 §17
- [D00-T01-S16-PR16] [major] reopen semantics undefined for stamp and dependents -> filed D00 T01 §17
- [D00-T01-S16-PR17] [minor] filed-versus-item count terminology inconsistent -> filed D00 T01 §17
- [D00-T01-S16-PR18] [major] fixed delimiters injectable from TODO text -> filed D00 T01 §17
- [D00-T01-S16-PR19] [major] malformed-output rule needs whole-output validation -> filed D00 T01 §17
- [D00-T01-S16-PR20] [major] outage markers can still carry filing claims -> filed D00 T01 §17
- [D00-T01-S16-PR21] [minor] JSON lacks version, ordering, exit contract -> filed D00 T01 §17
- [D00-T01-S16-PR22] [minor] live proofs live in commit bodies, not artifacts -> filed D00 T01 §17
- [D00-T01-S16-PR23] [minor] legacy malformed records outside enforcement -> filed D00 T01 §17
- [D00-T01-S16-PR24] [major] no ID collision or uniqueness check -> filed D00 T01 §17
- [D00-T01-S16-PR25] [major] multi-target findings lack completion semantics -> filed D00 T01 §17
End of ledger
