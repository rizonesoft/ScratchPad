# Review: D00 T01 §42 -- Requires Operator Vocabulary

Candidate: `9961740` (SHIP). Full panel (shared contract surface: `todo-graph.py` queries plus validator): Sol rounds 1-2, Opus round-3 sign-off.

Arch trigger: none - runner-plumbing vocabulary plus detector docs plus fixtures; no editor, storage, extensibility, AI, protocol, or consent surface changes.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. No fixes; asked clean.

Does it make sense: each shipped item maps to its code (tuple plus docstring plus 12 cases, four D99 marks, README row, count sync). Exactly the contract; the blocked-discovery corrections (Why plus item 2 Done plus checkpoint) are in the candidate.

Is it logical: the tuple drives `--context` choices, parser, validator, and split with no other code change, so one insertion wires the value end to end; the detector gains no branch (undetectable by construction) with the docstring stating it; gamma §§7-9 mirror §§1-6 shapes and the 12 checks mirror the §13 spread (2 parser, 3 validator, 4 detector, 3 split). Considered and kept: multi-value `--context` composes (exact-set semantics, no special case), and the `§7`/`§8`/`§9` substring matchers carry the same collision profile as the §13 originals they mirror. Asked clean.

Did it break anything else: self-test 1100/1100 with all pre-existing cases green; live validate silent; live `query ready` unchanged (D99 rows park by dependency today, by mark at unblock); `resolve` gains additive verdict lines only; README and count changes are additive text.

## GPT panel (round 1)

- `adversarial` approve
- `consistency` needs-attention: (R1-F1) vocabulary comment still counts one value.
- `integration` approve
- `record` approve

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 98s; outcome needs-attention; tokens unknown

Disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | f9a3760 |

Fix record: the `REQUIRES_ALLOWED` comment counts two values and states the declared-only exception. Fixed in `f9a3760` (sweep clean, self-test 1100 green); gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `6781bdd5b4bb57a83eac7455caafe5eee5b9eccef64108af314d09203a273456`; run `20260919-D00-T01-S42-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:221-223` still says the vocabulary has one value and that a second requires a detector branch, contradicting the newly added second value whose contract intentionally forbids such a branch.
**integration: approve**
**record: approve**
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 14s; outcome approve; tokens unknown

Disposition table (R1-F1 stays fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | f9a3760 |

Clean round: no new findings; the R1 fix holds.

Raw round-2 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`; run `20260919-D00-T01-S42-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` advisory: (R3-F1) operator detector cases duplicate display-session inputs.
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 3; model opus; effort medium; duration 79s; outcome advisory; tokens unknown

Round-3 sign-off disposition: R3-F1 is an advisory (test-code coverage shape, no behavior risk), so it files at D00 T04 §1 instead of re-rounding. No live findings remain; this round is FINAL.

Final disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | f9a3760 |
| R3-F1 | filed | D00 T04 §1 |

Architecture gate: not triggered (`Arch trigger: none`, see header).

Raw round-3 output (fenced, not part of the verdict scan; digest `bc1a8d794b8a2f5c9211be65949f2f977cb87d61054e47964f497c7804fd00d9`; run `20260919-D00-T01-S42-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: advisory (1)**
1. `scripts/todo-graph.py:6461-6472`: the four new `operator never self-reports ...` detector cases pass `detect_context` the exact same `(platform, environ)` pairs and assert the exact same expected sets as the four pre-existing `display-session ...` cases at `scripts/todo-graph.py:6449-6460`, so they are byte-equivalent duplicates that add count without adding coverage: any regression they would catch (an `operator` branch appearing in `detect_context`) already fails the older four.

**consistency: approve**

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §42 plus §13 plus D99 T01 §1 plus D00 T04 §1, 2026-09-19. Neighbors: §13 (filed-from vocabulary), D99 T01 §1 (gated rows), D00 T04 §1 (XREF). Dependents: none by the mechanical rule (no Depends row names §42). Runner note: run via the skill template; output `check-plan PASS` (17 findings, one per line; re-verified at transcription against the ledgered artifact). Run note: plan run `20260919-D00-T01-S42-gpt-r3`, digest `sha256:1e3c971dc6449f94894305e5336058b9c0f7491bc683153026cfc53c34543838`, receipt ledgered under ignored `build/review-runs/`; the run's prompt framing was lost with `/tmp`, so bytes 19009 is re-derived with the repo `fence_chunks` shape over the four section bodies at `4ab4159` (the run-time tree) with a standard-length tag, and the figures below are re-derived from the live tree. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §42, D00 T01 §13, D99 T01 §1, D00 T04 §1]; dependents [none]; bytes 19009; run 20260919-D00-T01-S42-gpt-r3

Ledger (namespaced IDs per the §16 shape; 4 of 17 filed at D00 T04 §1, 5 accepted in the §42 stamp run, 2 duplicates, 6 rejected with reasons on their rows):

Triage: 4 filed findings ride 2 items (PR7 plus PR8 plus PR13 share the proof-design item at D00 T04 §1).

Ledger:
- [D00-T01-S42-PR1] [minor] `--context operator` defeats the gate -> rejected declared `--context operator` is the designed attended path (the operator declares their own keyboard); the gate never claimed agent-proofness against a flag-passing agent, and no unattended runner dispatches Requires-marked rows it lacks
- [D00-T01-S42-PR2] [minor] Operator conflates capability with authority -> rejected no defect: `Requires:` is a holds-here predicate family, not an environment-sensor registry; `operator` (holds in no agent context) fits the family and the split enforces it without a sensor
- [D00-T01-S42-PR3] [minor] Future manual rows unprotected -> filed D00 T04 §1
- [D00-T01-S42-PR4] [minor] Verification gated behind operator section -> rejected handoff is defined: the operator commits the work, the agent verifies plus ticks plus notes in a second commit (Commit items, disambiguated per PR5); the gate blocks selection, not the second commit
- [D00-T01-S42-PR5] [minor] Operator ticks before agent verification -> accepted in the §42 stamp run: all four D99 Commit items now scope the operator tick to every item except agent verification
- [D00-T01-S42-PR6] [minor] Multi-commit workflow vs one-section rule -> rejected the sequence is explicit: operator commit plus agent verification commit form the candidate range the stamp names; one logical change spans the range, not one hash
- [D00-T01-S42-PR7] [minor] Social preview lacks durable artifact -> filed D00 T04 §1 (shared proof-design item)
- [D00-T01-S42-PR8] [minor] Settings checkpoint impossible for agent -> filed D00 T04 §1 (shared proof-design item)
- [D00-T01-S42-PR9] [minor] Hero prerequisite is a self-reference -> accepted in part in the §42 stamp run (Why now names the D00 T03 §1 hero shot; the D99 §1 to T03 §2 to T03 §1 chain already enforces it); commit-pin half rejected: TODO dependencies are section-addressed, never commit-pinned
- [D00-T01-S42-PR10] [minor] Website blank overwrites metadata -> accepted in the §42 stamp run: Website now stays unchanged
- [D00-T01-S42-PR11] [minor] Proof screenshot lacks redaction rule -> accepted in the §42 stamp run: proof capture now crops to the About bar with avatars, menus, and chrome excluded
- [D00-T01-S42-PR12] [minor] One-liner copy can drift -> accepted in the §42 stamp run: the one-liner is now copied live from the top of README.md, never a quoted copy
- [D00-T01-S42-PR13] [minor] Social preview lacks quality bar -> filed D00 T04 §1 (shared proof-design item)
- [D00-T01-S42-PR14] [minor] T04 commit title hides §42 scope -> duplicate D00-T01-S39-PR17
- [D00-T01-S42-PR15] [minor] T04 checkpoint omits detector matrix -> duplicate D00-T01-S37-PR21
- [D00-T01-S42-PR16] [minor] T04 §1 is an omnibus -> rejected T04 §1 holds 21 boxes, under the 30-item sizing trigger; the split rule fires at 30, not at topic count
- [D00-T01-S42-PR17] [minor] No end-to-end runner refusal fixtures -> rejected no runner fixture harness exists (query classification is the shipped enforcement); end-to-end refusal fixtures would pin a harness that does not exist
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `sha256:1e3c971dc6449f94894305e5336058b9c0f7491bc683153026cfc53c34543838`; run `20260919-D00-T01-S42-gpt-r3`):

```
- The `--context operator` override defeats the claimed agent-proof gate; D00 T01 §42 should require an attended confirmation mechanism that unattended runners cannot assert, because any agent can currently pass the flag and select operator-only work.
- `operator` is modeled as an environment capability even though it represents human authority; D00 T01 §13/§42 should separate detectable capabilities from authorization gates, because conflating them makes future runner safety brittle.
- No invariant protects future manual rows; D00 T01 §42 should validate that every `**Manual:** operator-only` section carries `**Requires:** operator` and reject incompatible uses, because marking only the four current rows allows the original gap to recur.
- D99 T01 §1 mixes operator mutation with agent-only verification under one operator-gated section; split verification into a dependent agent-run section or define an explicit ownership handoff, because the gate prevents the agent from ever selecting its own verification work.
- D99 T01 §1 tells the operator to tick every item before the agent verification occurs; revise the completion sequence so each actor marks only completed work, because the current instructions require a false checklist state.
- D99 T01 §1 requires separate proof-upload, checklist, agent-verification, and review commits despite the repository’s one-section logical-change rule; define the intentional candidate range and commit sequence explicitly, because the current Commit item understates the planned multi-commit workflow.
- The social-preview change has no durable public verification artifact; D99 T01 §1 should capture the finished Settings preview or verify a real link unfurl, because the About screenshot proves only the description and topics.
- The agent cannot perform the stated Settings-page social-preview checkpoint without owner credentials; D99 T01 §1 should assign that check to the operator and give the agent a public independent check, because the present acceptance path is impossible under its own credential boundary.
- The claimed prerequisite that the hero image is committed “first” is a self-reference with no explicit dependency; D99 T01 §1 should name the section and verified commit that owns `docs/assets/readme-hero.png`, because execution can otherwise reach an absent or stale asset.
- “Leave Website blank” can overwrite existing repository metadata without a preflight; D99 T01 §1 should preserve the current value unless an owned requirement explicitly removes it, because manual setup must not silently destroy unrelated configuration.
- The proof screenshot has no cropping or secret-redaction rule; D99 T01 §1 should require a repository-content-only capture with account and browser data excluded, because logged-in screenshots can leak personal information into `main`.
- The duplicated README one-liner can drift after the plan is authored; D99 T01 §1 should identify a pinned source or add an exact parity check against the current README, because GitHub metadata otherwise becomes stale immediately.
- The social preview lacks quality acceptance beyond “renders”; D99 T01 §1 should check GitHub’s recommended dimensions, crop behavior, readability at card size, and light/dark surroundings, because polished unfurls are a high-visibility premium-product win.
- D00 T04 §1’s planned commit title covers only §§37–39 while the section also owns the §42 operator-detector follow-up; retitle it to the full joined scope or split the §42 work, because candidate identity and review provenance would otherwise be misleading.
- D00 T04 §1’s Test checkpoint omits the operator-detector matrix it owns; add the distinct fake-environment cases and green-suite evidence there, because a checked item currently has no closeout checkpoint.
- D00 T04 §1 combines panel governance, telemetry, outage recovery, documentation, and unrelated operator-detector test cleanup in one section; split coherent deliverables into separately reviewable sections, because one large candidate increases regression risk and obscures which acceptance evidence covers which behavior.
- D00 T01 §42 proves selection under declared operator context but not that `process-plan` and `process-phase` require attended execution before dispatch; add end-to-end runner fixtures for refusal and explicit handoff, because query classification alone does not secure the execution path.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `ccc2660`: `todo-graph self-test: 1100 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `ccc2660`: `28 todos, 239 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --sync` on `ccc2660`: `synced 234 row(s) -- 80 complete (34%), 0 box(es) changed`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `ccc2660`: `implementation plan is current -- 80/234 sections complete (34%)`, exit 0.

Provenance: candidate ccc266090036311d03a57727c7ecc930f89cf37e; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --sync && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 938a82dfc2236cc750ccfcc9fdbbce2d948dd43202ffadd7d3c7ae609d0ccb12; path scripts/todo-graph.py; run 20260919-D00-T01-S42-gpt-r3

(Digest over the self-test line, the validate line, the sync line, and the plan line, joined by newlines with a trailing newline. Observed in a clean worktree at the candidate; the sync reported 0 boxes changed.)

