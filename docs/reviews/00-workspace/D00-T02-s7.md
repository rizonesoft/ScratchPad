# Review: D00 T02 §7 -- CI Evidence Capture Pipeline

## Evidence map

Round-1 candidate `bb2d879` carried the pipeline (`build.yml` capture plus upload steps, Started line). Round-1 candidate `bcb6bb4` carried the first use (README evidence procedure, `titlebar-icon-evidence.png` crop, item ticks, §14 deferral resolved). Evidence for each fix is quoted under its panel record. Two filings ride alongside, not in the candidate: `cf8b146` (D01 T01 §31, fresh-tab phantom content) and `5d6c695` (D01 T02 §16, MenuBarTests flake quarantine).

## Self-review

Sense: green builds capture a canonical frame and upload it; the §14 crop comes from that artifact through a written procedure. Logic: default `success()` condition makes capture green-only while red builds keep their golden-failure upload; the exe path matches the Upload-stub path; `CaptureBaseline stub` launches, waits, shoots 900x650, and kills. Breakage: the workflow change is additive (two steps before Upload stub); README and crop additive; TODO ticks plus one Resolved marker. Cost: a few CI minutes per green build for the tool build plus capture; the tool's `packages.lock.json` is covered by the NuGet cache key. No self-review fixes; the panel found the real ones below.

## Opus panel (round 1, candidates `bb2d879` + `bcb6bb4`)

**adversarial: needs-attention**
- `.github/workflows/build.yml:99-106` + `resources/baseline/README.md:15` -- the documented "fresh profile" premise is false. `Capture evidence frame` runs after `Test solution` in the same job, the profile is `%LOCALAPPDATA%\ScratchPad` with no override, and the suite drives it, so the frame restores the suite's leftover session instead of a fresh launch. Verified in-session (AppDataDir fixed path, `WhenStarts` default Continue, `App.xaml.cs` restores non-trivial sessions, `SessionRestoreTests`/`TemplateTests`/`EncryptedNotesTests`/`LockedResidueTests` all touch the real profile; the panel's "no file references AppDataDir" overstates, the references strengthen the finding). Fixed: the step resets the profile before capture and seeds `{"WhatsNewSeen":true}` so no first-run dialog covers the frame (partial seed safe per `SettingsStore.LoadFrom`, which defaults missing members and migrates v0); theme default `system` keeps the frame dark under the CI theme keys. The crop is re-taken from a fresh frame, and D01 T01 §31 stands or falls on the fresh tab state.
- `.github/workflows/build.yml:99-106` -- no black-frame guard: `Shoot` writes and `CaptureStub` returns 0 regardless of pixel content, so a display-less runner uploads a black artifact on a green build. Fixed: the step samples the PNG in PowerShell (every 30th pixel, lit above channel 16) and throws when zero samples are lit.

**consistency: needs-attention**
- `resources/baseline/README.md:7` -- the `## Sources` paragraph catalogues every raw evidence crop but `titlebar-icon-evidence.png` was added without an entry. Fixed in the re-capture commit (entry cites the fresh run id).
- `resources/baseline/README.md:17-21` vs item 2 -- item 3 claimed verbatim procedure-following while item 2 recorded an 8px inset the procedure did not contain. Fixed: step 3 carries the translucent-border inset caveat and step 4 checks for border-bleed pixels.

**integration: approve**

**record: needs-attention**
- `todo/01-notepad-core/TODO-02-menus-settings-status.md:423` -- the `Resolved:` marker omits the `| closed by \`<sha>\`` trailer required by `todo/README.md:176`. Fixed: trailer added (`bcb6bb4`).
- `todo/00-workspace/TODO-02-test-backbone.md:191` -- item 2 asserted "foreground, unoccluded" while recording foreign background-window pixels in the same frame. Fixed: wording distinguishes the unoccluded window from the border bleed the inset handles, with a `Corrected` marker for the false fresh-profile premise.
- D01 T01 §31 vs finding 1 -- the §31 filing rests on a "fresh-profile launch" that was not fresh; restored suite residue is the simpler explanation for the 4-character dirty tab. Verdict deferred to the fresh frame: §31 stands on a fresh-frame dirty tab, else it is unfiled with the reason recorded here.
- File-level Verification staleness (advisory, pre-existing): the ticked block quotes `19 todos, 106 sections` and run 34810042091 against today's `22 todos, 175 sections`. Not this section's to fix; `process-todo-file` re-proves at closeout.

Fix record: all needs-attention findings fixed in `c07b2c6` except the re-capture, which landed in `91e6d46`: fresh crop from run 35234746568 (profile reset, WhatsNewSeen seed, black-frame assert all exercised on the green rerun), Sources entry, corrected first-use run, items 1-2 evidence rewritten. §31 verdict: UNFILED the same day (`48e6734`, address left vacant) -- the fresh frame shows a clean "0 characters" tab, so the 4-char dirty tab was suite residue as the panel suspected. The round-2 run red-flagged three more same-family flakes, all confirmed red-then-green on the rerun: `FileMenuLiveAcceleratorsWork` joined D01 T02 §16 (same file), `WindowChromeIconMatchesAsset` plus `MissingFileOfferYesBindsTabAndSaveCreates` filed as D01 T01 §32. Record note: the `48e6734` message quotes "49/171" but the tree is 48/170; the tree is correct, the message count is wrong, left as history.

## Opus panel (round 2, candidates `c07b2c6` + `91e6d46`)

Process note: the first attempt piped through `head -50` and truncated mid-verdict, so it was discarded and the panel re-ran with full capture; the re-run below is the round-2 record. (The truncated head showed `adversarial: approve` with the same substance as below plus a cut-off consistency line; nothing from it survives as a verdict.)

**adversarial: advisory**
- Round-1 fixes confirmed against source: the reset kills session restore (profile path plus `session.json` under the root match), the seed loads as v0 (not corrupt), the black assert runs before the upload and clears the dark frame while failing flat black.
- Advisory, fixed: the CI guard asserted non-black only while item 1's Done-when is "a non-black 900x650 PNG". The step now throws on any size but 900x650.
- Advisory, fixed: `Remove-Item` had no failure tolerance against an orphaned `ScratchPad.exe` holding the profile. The step now stops orphaned app processes before the reset.

**consistency: advisory**
- Round-1 findings confirmed fixed (Sources entry, inset caveat plus checklist, names/ids/box agree across README, items, and Sources).
- Advisory, fixed: `$profile` shadowed PowerShell's automatic `$PROFILE`. Renamed to `$appProfile`.

**integration: approve**
- §31 unfiled cleanly (no dangling references, vacancy stated, XREFs bidirectional with §16 and §32); `validate` 0 fatal, `plan --check` current, self-test green; row §7 correctly still `[ ]`.

**record: advisory**
- Round-1 findings confirmed fixed (trailer shape, honest eyeball wording, §31 retracted with a `Corrected` marker; the re-capture verified real: dirty dot gone, clean tab, no bleed).
- Advisory, fixed: the `Resolved:` trailer named `bcb6bb4`, whose crop was superseded. The marker keeps `bcb6bb4` as the factual closer and now carries the re-capture trace (`91e6d46`).
- Advisory, noted not fixed: `48e6734`'s "49/171" message count (tree is 48/170) is immutable pushed history, already recorded above.
- Advisory, mitigated in record: the §31 retraction rests on the fresh frame's "0 characters" counter, which sits outside the committed crop and lives in the expiring run-35234746568 artifact. Mitigation: the full-frame reading is quoted here with its re-derivation command: `gh run download 35234746568 -n evidence-capture -D /tmp/evidence && python3 -c "from PIL import Image; im=Image.open('/tmp/evidence/stub-window.png'); print(im.size, im.convert('L').getextrema())"` printed `(900, 650) (12, 255)`; the status bar in that frame reads "0 characters" beside a clean Untitled tab. The committed crop corroborates (dirty dot gone) but does not show the counter; after artifact expiry the run id plus this quote is the trace.

Fix record: all three fixable advisories fixed in the round-3 candidate (size assert, orphan kill, `$appProfile` rename, trailer trace). Provenance: run 35239792170 green on `98140ed` with both asserts exercised. Panel round 3 ran against that candidate.

## Opus panel (round 3, candidate `98140ed`, FINAL unless round 4 re-opens)

**adversarial: advisory**
- Round-2 advisories confirmed fixed and holding (size assert matches the Done-when and the tool defaults; orphan kill precedes the reset; black assert, reset, and seed intact).
- Advisory, fixed: the orphan kill raced the reset (`Stop-Process` async, no wait). The step now pipes `-PassThru` into `Wait-Process -Timeout 10`, guarded by an `if ($orphans)` so the empty pipeline never reaches `Wait-Process`.
- Advisory, fixed: the unqualified kill's blast radius on self-hosted runners. The comment now records the ephemeral-runner assumption.

**consistency: advisory**
- Round-1/2 fixes hold (rename, Sources entry, caveat, checklist, names/ids/box agree).
- Advisory, fixed: the step comment lagged the step (size assert and orphan kill undocumented). The comment now documents both guards and their rationale.

**integration: approve**
- Tree holds (`validate` 0 fatal, `plan --check` 48/170, §31 vacancy clean, Resolved shape kept with the re-capture trace, row still `[ ]`).

**record: advisory**
- Round-2 trailer advisory confirmed fixed; round-1 record findings hold.
- Advisory, fixed: run 35239792170 appeared nowhere in the repo. Item 1 now cites it with both asserts exercised.
- Advisory, fixed: item 1's guard enumeration omitted the size assert and orphan kill. It now names the full guard set per round.

Fix record: all round-3 advisories fixed in the round-4 candidate. Panel round 4 runs against that candidate.

## CI incident note (review window, not the candidate)

Two mass-failure events, both proven environmental by same-binary green reruns, both filed as the first verdict under D06 T01 §6 (`cde6ce8`): run 35238610077 (build on the TODO-only filing commit `48e6734`) failed 9 UI tests at once (2 BackupTests, 2 print-flag LaunchTests, 1 ProtocolHandler, 1 SessionRestore, 1 SettingsPage, 1 Snapshot, 1 TabAccessibility); run 35241743948 (build on `821f233`, whose src/tests tree is byte-identical to the green `98140ed`) failed 34 (33 in a 20-second window, mass "process not running" at attach) then 9 on the next attempt (3 BackupTests, 1 DirtyPrompt, 3 LaunchTests, 2 MenuBarTests), then went green on attempt 3. Different sets on identical binaries, same runner image (`windows-2025-vs2026`) on green and red, greens adjacent both sides: sick runners, not code (and the capture step, which runs after Test, never failed in any event). No test-quarantine filings for these: mass events are incidents, not N flaky tests. The five filed flakes (§16 trio, §32 pair) stand on their own 1-3-failure runs.

## Opus panel (round 4, candidate `821f233`, FINAL)

**adversarial: approve**
- Round-3 race advisory verified fixed at source (empty-set guard, `-PassThru` into `Wait-Process -Timeout 10`); residual paths fail loud (step failure on surviving locks, capture-tool exit 1 on redirect-and-exit launches); no path uploads a contaminated or black frame on green; asserts stay ordered before the upload.

**consistency: advisory**
- Advisory, fixed in the stamp commit: the comment inverted the mechanism ("dead handle" holds nothing; the killer is a live orphan's open handle) and overstated the wait (best-effort under `SilentlyContinue`). Reworded to both truths.

**integration: approve**
- Tree holds (`validate` 0 fatal, `plan --check` 48/170, self-test green); XREFs bidirectional; §31 vacancy holds; row correctly still `[ ]`.

**record: advisory**
- Advisory, fixed in the stamp commit: item 1's round-4 clause carried no run id. It now quotes run 35241743948 attempt 3 green with the attempts 1-2 provenance.
- Advisory, fixed in the stamp commit: the incident note above named only the first event and predated the filing. It now names both events and the D06 T01 §6 verdict.
- Advisory, fixed in the stamp commit: the §7 XREF block omitted D06 T01 §6. The bidirectional pair is now in place.

Fix record: all round-4 advisories fixed in the stamp commit (comment mechanism wording, run-35241743948 citation, incident-note refresh, D06 T01 §6 XREF pair). Panel rounds used: 4 of 5 (round 1 needs-attention all fixed and re-proven; rounds 2-4 advisory-only; zero needs-attention at close).

## Gates re-run

Ship CI run 35241743948 attempt 3 on `821f233`: success both jobs with the capture step green (size plus black asserts exercised; `evidence-capture` 25 KB plus stub artifacts present). The stamp commit's one behavior line (`Wait-Process` close) is parser-verified (`pwsh` PSParser clean), its parameters confirmed present (`Wait-Process -Timeout`, `Stop-Process -PassThru`), and its empty guard executed (`$null` skips); stamp-push CI exercises it for real. `validate` 0 fatal 0 warnings (re-run, quoted at stamp). Leftover: none filed (mass events went to D06 T01 §6 as an incident verdict, carried in the stamp; the §5 flakes went to §16/§32 during the review).
