# Review: D00 T01 §24 -- Lineage Residuals and Run Inspection

Candidate: `e7b1b5a` + `8c4d577` + `880aab9` + `e27109d` + `f713b53` + `8a8b676` + `71588db` + `68deeed` (checked `fence` pipeline, R1-R5 output appended; output `check-panel PASS` on all five rounds). Panel tier: Full (the diff touches shared contract surface: validator, graph, query, fixtures). Rounds 1-2 ran on Sol, rounds 3-5 on Opus; round 5 is the hard cap with one leftover filed, not patched.

Excluded from the candidate diff by path (committed in range, not §24): the run-3 Phase 0 record inside `e7b1b5a` (`docs/phase-runs/2026-09-18-phase-0.md`), the run-4 record fix `5cdf4e8`, and the todo filings `a04bcc1` (§43) plus the §44 filing (panel leftover, see Review notes). The TODO-01 tick hunks ride the section contract inline.

## Self-review

Three questions over the candidate diff plus its blast radius, answered with files and behavior. Two fixes landed in `e27109d` before the lenses ran; no lens re-reported them.

Does it make sense: items 1, 3, and 5 add five validator fires (genesis supersedes, marker re-supersedes, forward edge, run-less manifest, ledger-row fork) with §§58-62 fixtures proving each fire plus its exact-once count; item 4 adds `query run` with six legs and 28 probes; item 2's run-less-chain exemption is the only contract refinement, grounded in the PR6 false-attribution rejection and named in the rule comment plus the README dictionary row. No scope beyond the items: the outage exemption, the as-carried fork target, and the query error exits are the items' own edge conditions.

Is it logical: the lineage fires compose (singleton genesis, per-chain second-claim, non-last backward edges, last-marker unknown-run kept); rule 18 keeps shape checks unexempted while the run leg alone skips run-less chains; rule 25 keeps existence/namespace/cycle legs ahead of the fork leg; the query reads through the `-r1` synonym on markers, manifests, and provenance alike. Failure paths: missing target exits 2, unknown run exits 1, stray target on other queries exits 2, multi-record candidate attribution declines with a note.

Did it break anything else: the query's new positional binds nothing on other queries (rejection probed); the promoted helpers keep validator behavior identical (delegation, full suite green); fixtures use tree-unique runs and back-linked row IDs; live `validate` stays 0 fatal and the plan projection current. Self-review fixes: AGENTS.md self-test count synced to the landed total, and the fork-claim tracker narrowed from dict to set (the stored index was never read).

## GPT panel (round 1)

- `adversarial` needs-attention: (1) query legs truncate findings, lineage, outage, command, and tool text, hiding structural data.
- `consistency` approve
- `integration` needs-attention: (1) candidate lines attribute file-wide across multi-record files; (2) the `target` positional is accepted and ignored by non-run queries.
- `record` approve

Fix record: legs print structural fields whole (rows ID/severity/disposition-first, lineage run-first with edges, artifacts gain exit plus full digest); the candidate leg is labeled file-level by design; non-run queries reject a stray target with exit 2. Fixed in `f713b53` with 5 new probes; gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `1fd5b9f4e3eaa3fb4c10cbe3b768f95e10af86e3974f0a0e65f8db34be01ff32`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1814-1815,1888,1928,1939,1944` silently truncates findings, marker-lineage, outage, command, and tool text, so `query run` can hide supersession IDs, finding dispositions, accountability fields, or artifact details instead of resolving the six promised legs completely.
**consistency: approve**
**integration: needs-attention** (2)
1. `scripts/todo-graph.py:1889-1896` associates every `Candidate:` line in a findings file with the requested run whenever any manifest in that file matches, so files containing multiple review sections can report candidates belonging to different runs.
2. `scripts/todo-graph.py:11000` adds `target` to the shared `query` parser, causing every non-`run` query to accept and silently ignore an unexpected positional argument instead of rejecting malformed invocations.
**record: approve**
```

## GPT panel (round 2)

- `adversarial` needs-attention: (1) re-report of the round-1 truncation family against the fixed legs.
- `consistency` approve
- `integration` needs-attention: (1) re-report of the round-1 candidate-attribution family.
- `record` approve

Fix record: the candidate leg now declines multi-record attribution with an explicit note instead of misattributing (§63 fixture locks validator silence plus the decline); the truncation re-report tripped the same-family circuit breaker (sweep quoted below) and round 3 ran as confirmation. Fixed in `8a8b676` with 3 new probes; gates re-ran green before round 3.

Circuit-breaker sweep (every structural token prints whole; only free prose truncates):

```
awk '/if what == "run":/,/if what == "frozen":/' scripts/todo-graph.py | grep -n "_one_line\|run=\|digest \|exit \|-> {"
15:        def _one_line(text: str, width: int) -> str:
90:                    _text = _one_line(_rm.group("text"), 80) if _rm else ""
91:                    _row = f"- [{_lr.group(1)}] [{_lr.group(2)}] -> {_lr.group(3)}"
93:                        _row += f" {_one_line(_rest, 140)}"
155:                print(f"    {_path} §{_num} [{_i}/{len(_bodies)}] run={_run2}{_edge} :: {_one_line(_b, 120)}")
166:            print(f"    {_path} §{_num} {_one_line(_b, 160)}")
172:                f"    {_path} candidate {_cand} exit {_exit} digest {_digest} "
173:                f"{_one_line(_cmd, 80)} ({_one_line(_tool, 40)}) {_ppath}"
```

Raw round-2 output (fenced, not part of the verdict scan; digest `10cf1ea75aefc6fe7c2b9b6a0099f52dcf7642bf1302cdfce73fcb9c5000d6a2`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1824-1825,1899,1902,1959,1970,1977` silently truncates finding prose, row suffixes, marker lineage, outage details, commands, and tool names, so `query run` can conceal supersession IDs, dispositions, accountability fields, or artifact details instead of fully resolving the promised inspection legs.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/todo-graph.py:1906-1919` associates every `Candidate:` line in a findings file with the requested run whenever any manifest in that file matches, so a file containing multiple review sections can report candidates belonging to other runs.
**record: approve**
```

## Opus panel (round 3)

- `adversarial` needs-attention: (1) row-text variable rebinds the findings-file text, dropping later records in multi-record files (reproduced by the panel).
- `consistency` approve
- `integration` approve
- `record` needs-attention: (1) the no-lineage plus supersession dictionary rows miss the new fires; (2) the AGENTS.md plus README query lists miss `query run`.

Fix record: the row-text variable renamed so the file text survives the loop, with second-record probes locking the fix; both dictionary rows name the new fires; both query lists gain the new command. Fixed in `71588db` with 2 new probes; gates re-ran green before round 4.

Raw round-3 output (fenced, not part of the verdict scan; digest `8a86c3736ef9481bfb2cd2247188821a7c9b170a500ff01cc1d08e547b3e847f`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1898` rebinds the loop variable `_text` (the findings-file text bound at line 1880) to the truncated row prose, so every later `Plan review` record in that same file is sliced out of a <=80-char string at line 1882: once a matching record has ledger rows, subsequent records in the file resolve to an empty section and their `Manifest:`/ledger rows are silently dropped. Reproduced: a file whose first record carries `run 20260920-D90-T07-S1-gpt` and whose second carries `run 20260920-D90-T07-S1-gpt-r1` (same run through the `-r1` synonym, line 1886) reports only `D90 T07 §1` in the scope leg, dropping `D90 T07 §9`.

**consistency: approve**

**integration: approve**

**record: needs-attention** (2)
1. `todo/README.md:403` and `todo/README.md:406` leave the flag dictionary stale for the three new FATAL fires this diff adds: the `plan-review-no-lineage` row documents no genesis-marker-carries-supersedes fire (`scripts/todo-validate.py:797-805`), no re-supersedes fork fire (`scripts/todo-validate.py:806-822`), and no non-last-edge `outside its past` fire (`scripts/todo-validate.py:824-841`), and the `ledger-supersession-broken` row documents no one-target-one-successor fire (`scripts/todo-validate.py:1480-1498`), while the same diff did update the `plan-review-malformed` row (`todo/README.md:399`) for its new fire.
2. `AGENTS.md:61` and `todo/README.md:339` end the operator query list without `query run <id>`, the section's headline operator surface (`scripts/todo-graph.py:1809`, parser choice at `scripts/todo-graph.py:11107`), even though this diff edits `AGENTS.md:55` in the same block.
```

## Opus panel (round 4)

- `adversarial` approve
- `consistency` needs-attention: (1) the query duplicates the outage predicate plus the chain slice instead of sharing them.
- `integration` needs-attention: (1) the query slice drops the parsed-body fallback, hiding range-stamped non-first sections (reproduced by the panel).
- `record` approve

Fix record: `marker_states`, `is_outage_marker`, and `section_markers` promoted to the graph module with the validator delegating; the query shares the slice including the fallback. The §§64-65 range-pair fixture locks non-first-section resolution. Fixed in `68deeed` with 3 new probes; gates re-ran green before round 5.

Raw round-4 output (fenced, not part of the verdict scan; digest `1f9619127b53f90c04ebfe1f232d6f1f0073b7ddce8e51eafee85e9235fccfc1`):

```
**adversarial: approve**

**consistency: needs-attention** (1)
1. `scripts/todo-graph.py:1825-1833` and `scripts/todo-graph.py:1836-1852` re-implement two validator helpers instead of promoting them into the shared graph module: `_is_outage` duplicates the predicate set of `marker_states`/`is_outage_marker` (`scripts/todo-validate.py:617-639`, whose own comment states one function must serve every site so `outage marker` reads the same way everywhere), and the chain re-slice duplicates `section_markers` (`scripts/todo-validate.py:593-615`). Both copies live in the module the validator already imports (`graph.MANIFEST_RE`, `graph.normalize_run_id`), so nothing forced the fork, and the second copy has already drifted (see the integration finding).

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:1836-1852` drops the parsed-body fallback that `scripts/todo-validate.py:604-614` keeps, so a section covered by a range stamp (supported shape, `scripts/todo-graph.py:4807-4822`, `4849-4869`) contributes no marker to `query run`. Reproduced on a two-section fixture whose `> **Verified:** 2026-09-20 | §1-§2` stamp carries `> **Plan review:** GPT high, filed §2 (run 20260920-D90-T07-S1-gpt)`: both sections parse the marker body (`plan_review_body` set for §1 and §2), the validator's `section_markers` returns it for both, but `query run 20260920-D90-T07-S1-gpt` prints `marker lineage` with `§1 [1/1]` only, and §2 is likewise absent from the outage leg; a run recorded solely on range-stamped non-first sections would additionally fall through to `unknown run` (exit 1) when no manifest or provenance line names it.

**record: approve**
```

## Opus panel (round 5)

- `adversarial` needs-attention: (1) the genesis flag misfires on range-stamped rerun fallback chains (filed as D00 T01 §44, see Review notes).
- `consistency` approve
- `integration` approve
- `record` approve

Fix record: none (hard cap). Round 5 is the bound, so the leftover files through `add-todo` instead of patching: D00 T01 §44 carries the genesis range guard plus the same-shape follows-outage verification.

Raw round-5 output (fenced, not part of the verdict scan; digest `08141d1a42daca9243347c54a4af163a7c7a7c6a97c279b749c65c3ef798b256`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-validate.py:757-765` fires `plan-review-no-lineage` on any range-stamped non-first section whose range carries a rerun chain: `graph.section_markers` (`scripts/todo-graph.py:1185-1211`) returns the parsed last-marker body as a one-element chain for such sections, so `len(chain) == 1 and SUPERSEDES_RE.search(chain[0])` treats a legitimate rerun marker as a genesis marker. Reproduced by adding a second marker line `> **Plan review:** GPT high, filed §2 (run 20260920-D90-T07-S64-gpt-r2, supersedes 20260920-D90-T07-S64-gpt)` to the §64-§65 range fixture (`scripts/todo-graph.py:7488`) and pointing the range manifest at `-r2` (`scripts/todo-graph.py:7924`): the self-test's "§§64-65 fire exactly zero" probe goes to 1 with `FATAL TODO-07-marker.md:844: §65 genesis marker carries supersedes (a first run has no ancestry to name)` on a valid chain. The §64-§65 probe only covers a single-marker range, so the fixture set does not exercise the shape that misfires.

**consistency: approve**

**integration: approve**

**record: approve**
```

## Review notes

Round 5 is the bound (max 5 reached), so the adversarial leftover routes to D00 T01 §44 instead of a sixth patch: the genesis flag's range-fallback misfire plus the same-shape follows-outage verification ride there as tracked work, not dropped work. The shipped gate holds its mainline contract (singleton, fork, forward-edge, manifest-run, and row-fork fires all prove with exact-once counts; the query resolves all six legs with 28 probes; live tree 0 FATAL); the residual is range-stamped rerun chains only, a shape no live section carries (no `§A-§B` Verified range with a rerun chain exists in the tree).

## Plan review

Round: `gpt-5.6-sol` high over §24 plus §20 plus §23 plus §43, 2026-09-19. Neighbors: §20 and §23 (Depends plus XREF); dependent §43 (XREF-only consumer; its transitive hop §20 already a neighbor). The run minted through the `run-id` genesis path over the findings file (no prior claims, bare base). Runner note: the skill's `codex exec` template ran verbatim; no new CLI drift. Output `check-plan PASS` (14 findings, one per line). No criticals (nothing safety/data/stamp-invalidating), so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §24, D00 T01 §20, D00 T01 §23, D00 T01 §43]; dependents [D00 T01 §43]; bytes 20751; run 20260919-D00-T01-S24-gpt

Ledger (namespaced IDs per the §16 shape; 3 of 14 filed at §45 plus a §43 item, 5 duplicate with canonical IDs, 6 rejected with reasons on their rows):

Ledger:

- [D00-T01-S24-PR1] [minor] Cutoff reads chain-shape not date -> rejected premise false: run-less non-outage markers fire the lineage no-run-ID flag, so nothing evades; the outage exemption is PR6 design
- [D00-T01-S24-PR2] [minor] Provenance binds any section run -> duplicate D00-T01-S23-PR7
- [D00-T01-S24-PR3] [minor] -r1 stays writable -> duplicate D00-T01-S20-PR1
- [D00-T01-S24-PR4] [minor] Provenance accepts any object -> duplicate D00-T01-S23-PR2
- [D00-T01-S24-PR5] [minor] Paths check current tree -> duplicate D00-T01-S23-PR4
- [D00-T01-S24-PR6] [minor] Digest never recomputed -> rejected recorded design: digest attested never re-verified (§23 item, S20-PR8 note)
- [D00-T01-S24-PR7] [minor] Gitless skips stay silent -> duplicate D00-T01-S23-PR1
- [D00-T01-S24-PR8] [minor] Multi-record candidates decline -> rejected recorded R2 design: candidates name rounds and carry no run; zero multi-record files live; the decline is explicit
- [D00-T01-S24-PR9] [minor] Run view omits trust state -> filed D00 T01 §45
- [D00-T01-S24-PR10] [minor] Fork double-fire undefined -> rejected two true defects earn two fires (outside-past plus second-claim); multi-fire per section is normal (§6 precedent); exact-once holds per defect
- [D00-T01-S24-PR11] [minor] Rationale copies keep wrong tally -> filed D00 T01 §43
- [D00-T01-S24-PR12] [minor] No headcount regression rule -> rejected headers are unshaped prose by design; a headcount-comparison rule costs more than the one-off miscount; rows stay validator-enforced
- [D00-T01-S24-PR13] [minor] Closure records missing pre-stamp -> rejected satisfied by the stamp step: stamp, findings file, and Live proof land with the stamp commit; re-verify post-stamp
- [D00-T01-S24-PR14] [minor] No query run JSON -> filed D00 T01 §45
End of ledger

## Live proof

`python3 scripts/todo-graph.py self-test` on `811a602`: `todo-graph self-test: 784 cases, 0 failed`, exit 0.

Provenance: candidate 811a602; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest eef65d9a42a849eb2dc8af3461e2d0729d96839b7c22b66f741e8ba2395e1fdc; path docs/reviews/00-workspace/D00-T01-s24.md; run 20260919-D00-T01-S24-gpt

`python3 scripts/todo-graph.py validate` on `811a602`: `24 todos, 219 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate 811a602; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest 27f4da00cea27b288bc61de50b9bd9da967c30dd83b08f12b1e67f3aced97d6c; path docs/reviews/00-workspace/D00-T01-s24.md; run 20260919-D00-T01-S24-gpt

`python3 scripts/todo-graph.py plan --check` on `811a602`: `implementation plan is current -- 64/214 sections complete (30%)`, exit 0.

Provenance: candidate 811a602; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest be1ea80190bd556f04bab3ce1cc5361dd550af38870ce5f9401e6822ffd156db; path docs/reviews/00-workspace/D00-T01-s24.md; run 20260919-D00-T01-S24-gpt

`python3 scripts/todo-graph.py query plan-health --check` on `811a602`: `gate: ok`, exit 0.

Provenance: candidate 811a602; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest d87e5312dca9acfa0eea572863f99df34bfbe8cc4a7b93fa265995837e46b187; path docs/reviews/00-workspace/D00-T01-s24.md; run 20260919-D00-T01-S24-gpt
