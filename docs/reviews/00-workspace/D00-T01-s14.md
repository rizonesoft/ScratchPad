# Review: D00 T01 §14 -- Plan Reviews with a Second-Family Reviewer

Candidate: `5eaef41` (ship commit, 6 files, +205/-13). Tier: full panel (diff touches shared contract surface: `scripts/`, skills). Lenses owed: adversarial, consistency, integration, record (source-defect/design not owed: no Notepad behavior or surface at stake).

## Self-review

Worked the three questions across the diff and its blast radius; no fixes.

- Sense: all six items land as specified (skill plan-review step, GPT-high reviewer pin, plan-review fallback chain, panel mirror rung, three echo probes plus forced-failure drive, validator fallback path with 5 cases, AGENTS.md 451). The §33 fenced-GPT case stretches "reject shapes" by one, consistent with the suite's fence-shape-lock culture (§§15-28); flagged for the panel, not fixed.
- Logic: GPT path entered only when no Opus heading exists (Opus governs otherwise); same verdict regexes, same last-wins slicing, outage note as substring per the documented rationale; neither-message kept byte-identical so zero locked cases churn (judgment call, panel should press it).
- Blast radius: self-test 451/451 with all pre-existing panel cases green; live validate silent; README pairs test unaffected (text-only row edit); no C# touched, no other skill references the replaced stop-and-say-so sentence.

## Opus panel (round 1)

- `adversarial` needs-attention: `scripts/todo-validate.py` gates the GPT-panel check behind `if not heads:`, so any findings file with an `Opus panel` heading has its GPT fallback record validated by nothing; the ordinary fix-loop shape (Opus rounds 1-2, GPT round 3 authorizing the stamp) reads the superseded Opus section and never checks the GPT section for lenses or the outage note. Same family: the rule-16 comment (`todo-validate.py`, "the fallback never displaces a real panel") contradicting last-wins, the skill sentence ("when both headings exist the Opus section governs") contradicting "LAST panel section as the record", the README cell (same pair), and the §32 fixture covering Opus-governs only with a clean GPT panel plus no GPT-terminator fixture. Both live drives exercised the no-Opus-heading path only.
- `consistency` approve
- `integration` advisory: the plan-review step names no record heading while the validator reserves `GPT panel`-starting headings; the plan review is itself a GPT round in that file, so a natural heading choice collides. Name the plan-review heading explicitly.
- `record` approve

## Opus panel (round 2)

- `adversarial` advisory: no fixture locks the literal round-1 shape (clean earlier Opus panel, defective GPT panel last); the branch makes it compositional, so coverage not a live hole. Fixing with §36.
- `consistency` approve
- `integration` approve
- `record` approve

## Opus panel (round 3)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Raw round-1 output (fenced, not part of the verdict scan):

```
**adversarial: needs-attention**: `scripts/todo-validate.py:587` gates the entire GPT-panel check behind `if not heads:`, so any findings file that contains at least one `Opus panel` heading has its GPT fallback record validated by nothing at all. This is the ordinary fix-loop shape, not an exotic one: rounds 1-2 run on Opus, Opus goes down at round 3, the GPT round that actually authorizes the stamp is appended last. The validator then reads the superseded round-2 Opus section (`scripts/todo-validate.py:615`, whose verdicts may all be `needs-attention` and still pass, since the rule checks presence not value) and never checks the round-3 GPT section for four lenses or for the `Opus outage` note. A file whose last section is `## GPT panel (round 3)` carrying two lenses and no outage note passes silently, which is exactly the forgetfulness the rule exists to defeat. Same family, other sites: `scripts/todo-validate.py:421` ("the fallback never displaces a real panel") and the documented last-wins rule it contradicts at `scripts/todo-validate.py:613-614`; `.claude/skills/review-todo-section/SKILL.md:107` ("when both headings exist the Opus section governs", stated one sentence after "reading the LAST panel section as the record of verdict"); `todo/README.md:380` (same pair of rules in the same cell). Test coverage has the matching hole: `scripts/todo-graph.py:4465-4503` §§29-33 cover Opus-governs only with a *clean* GPT panel (`90-panel-gptopus.md`, `scripts/todo-graph.py:4241-4249`), so no case fails if the GPT section is defective, and the GPT path's heading terminator (`scripts/todo-validate.py:590-592`) has no fixture at all. The reported live drives ("scratch GPT record silent, note removed fires 1", `todo/00-workspace/TODO-01-repo-and-toolchain.md:357`) both exercise the no-Opus-heading path, which is why this survived.

**consistency: approve**

**integration: advisory**: `.claude/skills/review-todo-section/SKILL.md:127-139` tells the session to record the plan review and its filings in the same findings file but names no heading for it, while `scripts/todo-validate.py:447` now reserves any level-2+ heading starting with the words `GPT panel` as the lens-panel fallback record. The plan review is itself a GPT round written into that file, so a natural heading choice collides with a validator-significant shape. Naming the plan-review heading explicitly (and stating it is not a panel record) would close it.

**record: approve**
```
