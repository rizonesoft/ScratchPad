# D00 T02 §9 findings

Candidate range: `2e40b8d 7ec7495 ce19202 2fd0ea4 31923a6 1a6b781 5a006c9 2dd6a07 cfb18e2` (ship plus R1-R5 fix-loops; stamp names the whole range). R1 through `ce19202`; R2 through `31923a6`; R3 through `1a6b781`; R4 through `5a006c9`; R5 final through `2dd6a07` (2 advisories filed, 1 arithmetic fixed in `cfb18e2`). Panel closed; plan review next.

Tier: Full panel (new unattended runner with teeth -- process reaps, scheduled task, guard cron -- plus procedure, triage filings, and quarantine rows).

Panel scope note: the full diff is small (13 files, +378/-73); rounds review the whole range. Ignored scratch (leg logs, trx, morning report) is quoted below, not in the range.

## Self-review

Author pass over the ship (`2e40b8d`) before panels. Verdicts: fix, defend, or record.

- S1 `tools/nightly.ps1` launched the gate with Start-Process, whose ExitCode reads back empty in Windows PowerShell 5.1 (measured with and without redirection): both legs recorded null gate codes. Fixed in `7ec7495` (job-based capture; 5-second micro-run captured 0 matching the verdict line; no leaked jobs).
- S2 Item 6 Done claims the cron "fired 02:30:01" but the parent died mid-loop, so no cron run completed end to end; the re-run was manual. Fix in flight: evidence archived to `build/nightly-rerun-2026-09-20/`, task demand-started 04:13:43 for a literal end-to-end cron run (proves the S1 fix in situ too); item-6 Done updates after its outcome.
- S3 Item 7 skip observation is indirect (03:30/03:40 boundaries produced no fire during live work; fire_count 1 at the idle ~03:50 fire). Defended: the harness exposes no skip log, and zero injections plus the counter are the available evidence. A boundary-hold direct observation stands by if the panel demands it.
- S4 Tonight's auto-report carries UI-only Run A counts (old generator already loaded when the trx-overwrite gap was found). The triage append carries merged counts; the transcript-parsing generator rides the ship, proven separately against the live leg (545/1/3 plus assemblies). Defended as designed: script generates the shape, triage completes it.
- S5 Forward refs to this file (7 quarantine rows, §34 SOURCE) resolve at stamp; this file ships in the stamp commit.
- S6 Fix commit message says "backslash-dollar-LASTEXITCODE" (over-escaped `$LASTEXITCODE`). Cosmetic; never amend. Recorded.
- S7 Filings ride §9's commit (item 3 owns them: "files every failure as a finding in the owning file"); the add-todo standalone-commit rule yields to the owning section's flow (§32 batched-commit precedent). Recorded decision.
- S8 §34 birth verified complete (Why/Job/Treatment/Chrome/Needs, XREF to §13 plus §9, SOURCE, 3 items, checkpoint, order row 33 plus plan row, §13 back-link; validate 0/0, plan current). Pass.
- S9 Skip-to-row consistency mechanically verified: all 7 slugs appear exactly once in `tests/UI` and once in the quarantine list; UI suite builds 0 warnings 0 errors. Pass.
- S10 Run A gate primary=1 attribution softened to likely (same leg, dirty-tab title, not pid-proven); the quarantine stands on the COM timeout. Fixed in the fix-loop (this file's range note plus the row).
- S11 Working-tree incident during triage: a same-path read-inside-write one-liner emptied 30 `tests/UI` files (0 bytes); restored byte-identical from HEAD (`git checkout HEAD -- tests/`, no user content at risk), Skips re-applied byte-exact (final diff 7 lines, CRLF pins preserved). Lesson recorded in the run file: never `open(w).write(open(r).read())` on one path. No range impact (final bytes are the 7 Skip lines).
- S12 Self-found in FL2: leg transcripts plus the report read HEAD per-leg/at-end, so the task run's legs read 7ec7495 then 2fd0ea4 while running one build. Fixed in FL2 (build-time HEAD captured once after the up-front build). The trigger label also misreads demand fires ("manual (parent svchost.exe)"); cosmetic, left for R2 to judge.

## Evidence (quoted)

02:30 Nightly UI run (scheduled trigger, parent died after ui-soak-1, 02:54:42, no OS trace): fenced `ui-interactive.trx` 28 total, 27 executed (24 passed, 3 failed), 1 quarantine skip, zero quiet-hours skips; `soak-solution.trx` UI 163/1/3; `ui-soak-1.trx` 159/5/3.

Governed re-run (manual `-SkipSoak`, in-window backup trigger, launched 03:20:13): Run A 545 passed, 1 failed, 3 skipped (Smoke 1/0/0, Unit 346/0/0, Protocol 35/0/0, UI 163/1/3), gate `flagged=0; census=743 primary=1 expect=secondary`; Run B 2/2, gate `flagged=0; census=11 primary=1 expect=primary`; Interactive 24/3/1 plus 1 quarantine skip. N1: 27 of 28 collected. Logs `build/nightly/2026-09-20-{default,primary,full}.log`, report `build/nightly/morning-2026-09-20.md` (archived under `build/nightly-rerun-2026-09-20/` before the task run).

Triage (11 failures): red-red (both runs) `SingleCloseStillClosesPinned`, `CloseRightSkipsPinned` -> D01 T01 §34; red-green `FileOpenMissingNameOffersCreate`, `LiveItemsStayEnabledAcrossStates` -> D01 T02 §16 items; red-green `MultiWindowSessionRestoresBothWindows`, `DirtyReloadDiscardsEdits`, `LockedFileReportsLocked`, `DirtyLockedBufferStaysOutOfSessionAndRestoresAsGhost`, `DirtyKeepPreservesEdits` -> quarantine rows; green-red `WindowCloseWithDirtyTabsIsSilentAndRestores` (COM 0x80131505), `QuitAndRelaunchRestoresTabsContentsAndCarets` (11 vs 5) -> quarantine rows. Proof of test-identical trees: `git log fa2fc56..a8127c7 -- tests/ src/` empty; uncommitted files docs plus scripts only.

Tasks: `\ScratchPad\Nightly UI` daily 02:30 local (trigger boundary 2026-09-19), action `tools\nightly.ps1`, 4h limit, wake, interactive; fired 02:30:01 and (demand) 04:13:43. Guard cron `7e89433d` (`*/10 2-6 * * *`, skip-if-active).

Gates: validate 0 fatal 0 warnings; plan current (241 rows after §34 sequencing); self-test 1176/1176; UI build 0/0.

## GPT panel (round 1)

Sol rung, candidate `2e40b8d 7ec7495 ce19202` (Full tier: rounds 1-2 Sol, round 3 Opus sign-off). Delivery needed a resume nudge: the first invocation died after its investigation with no verdicts, and the resume prompt (no tool calls, verdicts only) delivered them. R1 prompt was custom, not `review_prompt.py` fence/check; R2+ use the skill mechanics. The panel ran `--approve-for-me` with workspace-write (deviation from `-s read-only`); it wrote nothing (the tree held only author files after).

`adversarial` needs-attention
`consistency` needs-attention
`integration` needs-attention
`record` needs-attention

Findings (IDs round-scoped):

- R1-F1 (adversarial) Gate duration is not an enforced timeout: the suite runs synchronously, overrun is evaluated only after return, and a hung leg blocks later legs until the task 4h limit. Evidence: `tools/nightly.ps1:93`, `docs/testing.md:51`.
- R1-F2 (adversarial) Pre-flight reaps every `ScratchPad.exe`/`testhost.exe` under `Bin` with no run lock and no ownership check, so a concurrent run can kill the live one. Evidence: `tools/nightly.ps1:192`, `tools/nightly.ps1:203`.
- R1-F3 (consistency) The bar says an outside-window or locked-workstation Interactive skip fails the leg, but both script branches only log, leaving a green run that never collected Interactive. Evidence: `docs/testing.md:45`, `tools/nightly.ps1:283`.
- R1-F4 (consistency) The §16 extension claims five flakes while its only SOURCE describes three and the checkpoint still requires three Skips plus three rows. Evidence: `TODO-02-menus-settings-status.md:475,482,493`.
- R1-F5 (integration) Day-scoped outputs merge across runs: the report says HEAD `a8127c7` while its Run A log says `7ec7495`. Evidence: `tools/nightly.ps1:192,323`, `morning-2026-09-20.md:3`, both leg logs `:19`.
- R1-F6 (integration) Counts come from every transcript assembly but skip reasons come only from the single overwritten solution trx, so off-trx skips count without reasons. Evidence: `tools/nightly.ps1:127,152,168`.
- R1-F7 (integration) The task contract records a relative `-File` action with no StartIn, principal, or multiple-instance policy. Evidence: `docs/testing.md:37`, `TODO-02-test-backbone.md:280`. (Live reads later showed the action is absolute with StartIn set; the doc omission was real.)
- R1-F8 (record) Three ticks overclaim Done: the governed run was manual, not on cron; the cron fire died mid-loop with no quoted dry run; the guard skip is inferred, not observed. Evidence: `TODO-02-test-backbone.md:279-281`.
- R1-F9 (record) The §9 record quotes Run B "gate exit 0" while both full-rerun gate codes were null; the micro-run proves the mechanism, not the candidate evidence. Evidence: `TODO-02-test-backbone.md:279`, `2026-09-20-primary.log:25` (archive).
- R1-F10 (record) The ship commit is not section-atomic: it also carries the D00 T06 audit touch plus Windows-only plan prose. Evidence: `TODO-06-operator-docs.md:43`, `implementation-plan.md:73,75`.

Dispositions (fix-loop 1, `2fd0ea4`):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 2fd0ea4 (suite in job, killed at bell, tree reaped) |
| R1-F2 | fixed | 2fd0ea4 (Global mutex plus stand-down exit 0 plus age-scoped reap) |
| R1-F3 | fixed | 2fd0ea4 (both skip branches set red) |
| R1-F4 | fixed | 2fd0ea4 (night-flakes SOURCE plus five-checkpoint) |
| R1-F5 | fixed | 2fd0ea4 (stamp-scoped evidence, day-scoped report kept) |
| R1-F6 | fixed | 2fd0ea4 (trx plus transcript skip merge) |
| R1-F7 | fixed | 2fd0ea4 (task contract from live reads: absolute action, StartIn, IgnoreNew, PT4H, WakeToRun, InteractiveToken) |
| R1-F8 | fixed | 2fd0ea4 (honest Done lines; task outcome quoted in FL2) |
| R1-F9 | fixed | 2fd0ea4 (blank-code quotes; task outcome quoted in FL2) |
| R1-F10 | fixed | 2fd0ea4 (record acknowledgement; history immutable) |

Author error inside FL1 (caught before R2, restored in FL2): the Run A rewording over-corrected 545/1/3 to 163/1/3. The archive sums to 545 passed (Smoke 1, Unit 346, Protocol 35, UI 163); 163 is the UI-only auto-report row (the S4 generator defect). The panel never disputed 545.

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 710s; outcome needs-attention; tokens 338721

Duration is approximate (04:18:39-04:28:29 plus resume ~04:29-04:31); tokens are the codex `tokens used` tail and include the resume transcript replay; the verdict delivery resumed without `-c`, so delivery ran at high effort against the pinned medium (noted, not hidden). R1 verdicts transcribed above; raw output below.

```markdown
adversarial: needs-attention

1. Gate duration is not an enforced timeout. The test runs synchronously and overrun is evaluated only after it returns, so a hung leg prevents later legs from running and leaves the scheduled task's four-hour limit as the real abort mechanism. [tools/nightly.ps1:93](R:/conclave/projects/ScratchPad/tools/nightly.ps1:93), [docs/testing.md:51](R:/conclave/projects/ScratchPad/docs/testing.md:51)

2. Pre-flight kills every `ScratchPad.exe` or `testhost.exe` under the checkout's `Bin` without acquiring a run lock or checking process ownership/age. A concurrent manual, guard, or scheduled run can therefore terminate the live run it overlaps. [tools/nightly.ps1:192](R:/conclave/projects/ScratchPad/tools/nightly.ps1:192), [tools/nightly.ps1:203](R:/conclave/projects/ScratchPad/tools/nightly.ps1:203)

consistency: needs-attention

1. The documented bar says an outside-window or locked-workstation Interactive skip fails the leg, but both script branches merely log the skip and leave `$failed` unchanged, allowing a green overall result without running Interactive. [docs/testing.md:45](R:/conclave/projects/ScratchPad/docs/testing.md:45), [tools/nightly.ps1:283](R:/conclave/projects/ScratchPad/tools/nightly.ps1:283)

2. The §16 extension says the section now owns five flakes, but the only `SOURCE` still describes the original three, while the checkpoint still requires "All three Skips plus all three rows." This breaks the evidence-key and complete-checkpoint birth rules for the two nightly filings. [TODO-02-menus-settings-status.md:475](R:/conclave/projects/ScratchPad/todo/01-notepad-core/TODO-02-menus-settings-status.md:475), [TODO-02-menus-settings-status.md:482](R:/conclave/projects/ScratchPad/todo/01-notepad-core/TODO-02-menus-settings-status.md:482), [TODO-02-menus-settings-status.md:493](R:/conclave/projects/ScratchPad/todo/01-notepad-core/TODO-02-menus-settings-status.md:493), [add-todo/SKILL.md:42](R:/conclave/projects/ScratchPad/.claude/skills/add-todo/SKILL.md:42)

integration: needs-attention

1. All output is date-scoped rather than invocation-scoped, and report generation unconditionally reloads whatever same-day artifacts exist. The observed report identifies `a8127c7`, while its Run A log identifies `7ec7495` and Run B/Interactive logs still identify `a8127c7`, proving cross-run report merging. [tools/nightly.ps1:192](R:/conclave/projects/ScratchPad/tools/nightly.ps1:192), [tools/nightly.ps1:323](R:/conclave/projects/ScratchPad/tools/nightly.ps1:323), [morning-2026-09-20.md:3](R:/conclave/projects/ScratchPad/build/nightly/morning-2026-09-20.md:3), [2026-09-20-default.log:19](R:/conclave/projects/ScratchPad/build/nightly/2026-09-20-default.log:19), [2026-09-20-primary.log:19](R:/conclave/projects/ScratchPad/build/nightly/2026-09-20-primary.log:19)

2. Solution-wide counts come from every transcript assembly, but skip reasons come only from the single overwritten solution TRX. Any skip outside the final TRX assembly will increase the displayed skip count without appearing in the required reason list. [tools/nightly.ps1:127](R:/conclave/projects/ScratchPad/tools/nightly.ps1:127), [tools/nightly.ps1:152](R:/conclave/projects/ScratchPad/tools/nightly.ps1:152), [tools/nightly.ps1:168](R:/conclave/projects/ScratchPad/tools/nightly.ps1:168)

3. The committed scheduled-task contract uses a relative `-File tools\nightly.ps1` action but records neither `StartIn`, the principal identity, nor the multiple-instance policy. Those omissions determine whether the action resolves, UI tests have the correct desktop, and overlapping fires are rejected. Live principal/settings verification was attempted but denied by Task Scheduler access control. [docs/testing.md:37](R:/conclave/projects/ScratchPad/docs/testing.md:37), [TODO-02-test-backbone.md:280](R:/conclave/projects/ScratchPad/todo/00-workspace/TODO-02-test-backbone.md:280)

record: needs-attention

1. Three ticks overclaim their Done conditions: the successful governed run was manual rather than end-to-end on cron; the cron fire died mid-loop rather than completing or supplying a quoted dry run; and the guard "skip observation" is inferred from no fire rather than an observed live-turn fire. The self-review explicitly acknowledges the latter two gaps. [TODO-02-test-backbone.md:279](R:/conclave/projects/ScratchPad/todo/00-workspace/TODO-02-test-backbone.md:279), [TODO-02-test-backbone.md:280](R:/conclave/projects/ScratchPad/todo/00-workspace/TODO-02-test-backbone.md:280), [TODO-02-test-backbone.md:281](R:/conclave/projects/ScratchPad/todo/00-workspace/TODO-02-test-backbone.md:281), [D00-T02-s9.md:14](R:/conclave/projects/ScratchPad/docs/reviews/00-workspace/D00-T02-s9.md:14)

2. The §9 record quotes Run B "gate exit 0," but its own self-review says both full rerun gate codes were null; the archived transcript confirms a blank exit field. The later five-second micro-run validates the fix mechanism, not the quoted 300/1800-second candidate evidence. [TODO-02-test-backbone.md:279](R:/conclave/projects/ScratchPad/todo/00-workspace/TODO-02-test-backbone.md:279), [D00-T02-s9.md:13](R:/conclave/projects/ScratchPad/docs/reviews/00-workspace/D00-T02-s9.md:13), [2026-09-20-primary.log:25](R:/conclave/projects/ScratchPad/build/nightly-rerun-2026-09-20/2026-09-20-primary.log:25)

3. The two follow-up commits read as legitimate self-found fix loops, but the ship commit is not section-atomic: it also changes the unrelated D00 T06 prerequisite audit and Windows-only implementation-plan prose. Those are not §9-owned filings or derived projection changes. [TODO-06-operator-docs.md:43](R:/conclave/projects/ScratchPad/todo/00-workspace/TODO-06-operator-docs.md:43), [implementation-plan.md:73](R:/conclave/projects/ScratchPad/todo/implementation-plan.md:73), [implementation-plan.md:75](R:/conclave/projects/ScratchPad/todo/implementation-plan.md:75)

overall-outcome: needs-attention

TELEMETRY: model=gpt-5; effort=high; duration_seconds=1200; overall_outcome=needs-attention; approximate_tokens=32000
```

(The runner echoed the verdict block a second time with the token tail `tokens used 338,721`; echoed once above.)

## GPT panel (round 2)

Sol rung, candidate `2e40b8d 7ec7495 ce19202 2fd0ea4 31923a6`, skill mechanics (`review_prompt.py` fence, read-only runner, `check-panel` PASS). No R1 finding re-reported: all ten fixes held. Launch notes: `-s read-only` conflicts with `--approve-for-me` on this CLI (read-only alone works non-interactively); prompt bytes must stay UTF-8 end to end (a cp1252 `§` from a PowerShell redirect failed the runner once).

`adversarial` needs-attention
`consistency` approve
`integration` needs-attention
`record` needs-attention

Findings (IDs round-scoped):

- R2-F1 (adversarial) The Interactive leg reads green from the exit code alone, and xUnit exits 0 under skips, so non-quarantine skips could satisfy the quarantine-only bar. Evidence: `tools/nightly.ps1:378-379`.
- R2-F2 (integration) A red up-front build throws before report generation, leaving a scheduled red run with no fixed-path failure record. Evidence: `tools/nightly.ps1:319-322`.
- R2-F3 (record) Skipped tests default to the placeholder `triage annotates`, which is not skipped-with-reason evidence. Evidence: `tools/nightly.ps1:170-174`, `docs/testing.md:49`. DEFENDED (see below).
- R2-F4 (record) Items 5-6 cite archive directories, not the individual log paths the Done-when requires. Evidence: `TODO-02-test-backbone.md:279,282`.

Dispositions (fix-loop 3, `1a6b781`):

| ID | Disposition | Evidence |
| R2-F1 | fixed | 1a6b781 (Get-NonQuarantineSkips enforced in both Interactive branches) |
| R2-F2 | fixed | 1a6b781 (build try/catch plus skip-all plus report Build line) |
| R2-F3 | fixed | 1a6b781 (defense recorded here; no code change) |
| R2-F4 | fixed | 1a6b781 (individual archived log paths quoted) |

R2-F3 defense: the placeholder is the documented triage-annotation hook (the report section is titled "Skips (triage annotates reasons)"), and the mechanism demonstrably works: all 9 task-run skips carried real reasons through ErrorInfo.Message. The panel's "most" premise is refuted by that evidence.

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 165s; outcome needs-attention; tokens 33564

```markdown
**adversarial: needs-attention (1)**
1. `tools/nightly.ps1:378-379` marks the Interactive leg green solely from the `dotnet test` exit code; xUnit returns zero when tests are skipped, so quiet-hours or other non-quarantine skips can falsely satisfy the contract's quarantine-only skip bar.

**consistency: approve**

**integration: needs-attention (1)**
1. `tools/nightly.ps1:319-322` throws immediately when either up-front build fails, before morning-report generation, leaving a scheduled red run without the fixed-path failure record required by the nightly procedure.

**record: needs-attention (2)**
1. `tools/nightly.ps1:170-174` records most skipped tests with the placeholder `triage annotates` because it reads only `ErrorInfo.Message`; this does not produce the skipped-with-reason evidence required by `docs/testing.md:49`.
2. `todo/00-workspace/TODO-02-test-backbone.md:279` and `todo/00-workspace/TODO-02-test-backbone.md:282` mark the governed-run evidence complete while citing only archive directories, not the individual default, primary, and full log paths required by the section's done condition.
```

## Opus panel (round 3)

Opus sign-off rung, candidate `2e40b8d 7ec7495 ce19202 2fd0ea4 31923a6 1a6b781`, skill mechanics (`review_prompt.py` fence, `claude -p --model opus --effort medium --allowedTools Read`, `check-panel` PASS). No R1/R2 finding re-reported. All four lenses needs-attention; F1 judged blocking (stamp-invalidating silent-death path in an unattended runner), F2-F4 folded/filed below.

`adversarial` needs-attention
`consistency` needs-attention
`integration` needs-attention
`record` needs-attention

Findings (IDs round-scoped):

- R3-F1 (adversarial, BLOCKING) The Interactive leg runs sync with no timeout; a hung drive strands the soak, the report, and the window until the PT4H kill leaves no record. Evidence: `tools/nightly.ps1:386,402`, `docs/testing.md:51` (no bar for untimed legs).
- R3-F2 (consistency) `-SkipDefault/-SkipPrimary/-SkipFenced` are undocumented in help and procedure; the abort path needs `-SkipFenced` unnamed. Evidence: `tools/nightly.ps1:22-24`, `docs/testing.md:51`.
- R3-F3 (integration) The trigger label tests `taskeng.exe`, wrong since Win8+; the task run's parent is `svchost.exe`, so every cron run mislabels as manual. Evidence: `tools/nightly.ps1:437`, `build/nightly-taskrun-2026-09-20/morning-2026-09-20.md:4`.
- R3-F4 (record) The 7 quarantine rows name stamped owners with no tree owner for the fix-or-remove windows. Evidence: `docs/soak-and-quarantine.md:37-43`, the 7 Skip sites, `TODO-02-test-backbone.md:280`.

Dispositions (fix-loop 4, `5a006c9`):

| ID | Disposition | Evidence |
| R3-F1 | fixed | 5a006c9 (Invoke-TimedStep plus 1800s cap on Interactive and soak, abort bar stated) |
| R3-F2 | fixed | 5a006c9 (help plus abort-path switch docs; folded into the blocking-driven R4 as a 5-line doc fix) |
| R3-F3 | fixed | 5a006c9 (svchost-parent trigger branch; folded, 3 lines) |
| R3-F4 | fixed | 5a006c9 (D01 T01 §35 filed owning the 7 windows, due 09-27) |

F2-F4 process note: the sign-off file-instead rule binds rounds whose findings ALL sit below the bar; with F1 blocking, R4 runs regardless, and these three ride it (two trivial same-candidate fixes R4 verifies, one filing). If R4 disagrees with the process, it governs.

Telemetry: round 3; model opus; effort medium; duration 81s; outcome needs-attention; tokens unknown

Model is the requested `opus` alias (runner reported no resolved id); tokens unknown (plain `-p` prints no usage).

```markdown
**adversarial: needs-attention (1)**

1. `tools/nightly.ps1:386` and `tools/nightly.ps1:402` — the Interactive leg runs through `Invoke-Step` with no job, no gate, and no enforced timeout, unlike Run A/B (`tools/nightly.ps1:133-139`). A hung foreground UI drive (the same class the 02:30 run died in) blocks forever: the soak never runs, the morning report block at `tools/nightly.ps1:428-489` is never reached, and the run occupies the window until the scheduled task's PT4H kill leaves no record at the fixed path. `docs/testing.md:51` abort rules only cover "a leg that overruns its gate window", so the untimed leg has no stated bar either.

**consistency: needs-attention (1)**

1. `tools/nightly.ps1:22-24` — `-SkipDefault`, `-SkipPrimary`, and `-SkipFenced` are added as parameters but named nowhere in the comment-based help (`tools/nightly.ps1:6-20` documents only `-SkipSoak` and `-Force`) nor in `docs/testing.md:35-51`; `docs/testing.md:51` states the morning-triage abort path as "re-drives what the window allows (Run A plus Run B)" without naming the `-SkipFenced` switch that path requires, so a second operator cannot execute the documented abort path from the procedure.

**integration: needs-attention (1)**

1. `tools/nightly.ps1:437` — the trigger label tests the parent process for `taskeng.exe`, which no longer hosts scheduled tasks on Windows 8+; on this Windows 11 host a task-fired run's parent is `svchost.exe`. Proven by the archived cron-path evidence this section cites: `build/nightly-taskrun-2026-09-20/morning-2026-09-20.md:4` reads `Trigger: manual (parent svchost.exe)` for the 04:13:43 task run. Every cron run is therefore recorded as manual, and the report field cannot distinguish the cron path from the manual backup, which is the exact distinction items 5 and 6 turn on.

**record: needs-attention (1)**

1. `docs/soak-and-quarantine.md:37-43` plus `tests/UI/DirtyPromptTests.cs:178`, `tests/UI/LaunchTests.cs:294`, `tests/UI/LockedResidueTests.cs:72`, `tests/UI/ReloadTests.cs:142`, `tests/UI/ReloadTests.cs:179`, `tests/UI/SessionRestoreTests.cs:19`, `tests/UI/SessionRestoreTests.cs:356` — seven failures are quarantined with rows only; nothing is filed in their owning TODO files, contrary to `todo/00-workspace/TODO-02-test-backbone.md:280` ("files every failure as a finding in the owning file"), whose Done line substitutes "7 quarantine rows" for that filing. The named owners (`D01 T01 §4`, `§6`, `§7`, `§21`, `§30`) are all stamped `[x]` sections that cannot absorb the work, and no new section was filed for them (unlike the same-triage `D01 T01 §34` and `D01 T02 §16` filings), so the 2026-09-27 fix-or-remove window at `docs/soak-and-quarantine.md:47` has no owner the todo tree can surface, and seven UI behaviors run nowhere with no tracked reinstatement.
```

## Opus panel (round 4)

Opus blocking round, candidate through `5a006c9`, skill mechanics (`review_prompt.py` fence, `claude -p --model opus --effort medium --allowedTools Read`, `check-panel` PASS). No earlier finding re-reported. F1 disproven by experiment (defended), F2 filed (§14; catastrophe-only new design), F3-F4 fixed.

`adversarial` needs-attention
`consistency` needs-attention
`integration` approve
`record` needs-attention

Findings (IDs round-scoped):

- R4-F1 (adversarial) DEFENDED: Stop-Job allegedly leaves the native dotnet driver alive past the kill, uncaptured by the name/path reap. Evidence: `tools/nightly.ps1:118,127,169,178`, `tools/nightly.ps1:71-75`.
- R4-F2 (adversarial) FILED: no global deadline; worst-case caps sum to ~6.1h (21900 s; the panel's "~5.6h" was wrong, corrected in §14 per R5-F3), past PT4H and the window end, stranding the report. Evidence: `tools/nightly.ps1:133-460`, `docs/testing.md:51`.
- R4-F3 (consistency) The soak doc still describes the pre-ship shape (solution run plus `TestResults/` paths). Evidence: `docs/soak-and-quarantine.md:7-11`.
- R4-F4 (record) Item 2 requires "section range and HEAD" but headers carry suite scope, and the cited smoke log has no header at all. Evidence: `TODO-02-test-backbone.md:276`, `tools/nightly.ps1:69`, `2026-09-20-054411-smoke.log:1-10`.

Dispositions (fix-loop 5, `2dd6a07`):

| ID | Disposition | Evidence |
| R4-F1 | fixed | 2dd6a07 (disproven, defense recorded; drafted reap reverted, nothing kept) |
| R4-F2 | fixed | 2dd6a07 (D00 T02 §14 filed: run-level deadline) |
| R4-F3 | fixed | 2dd6a07 (soak doc shape plus paths synced; §5 rules untouched) |
| R4-F4 | fixed | 2dd6a07 (item reworded to suite scope; smoke headered, re-proved green) |

R4-F1 defense: the scratch replica (job spawns a token-marked native child, Stop-Job, CIM before/after) shows pre-stop match 1, post-stop 0: Stop-Job kills the child tree on this host. dotnet/testhost children die by the same job-object mechanics (name-blind, depth-blind). The 02:30 orphans came from a disorderly parent death, which the pre-flight reap covers — consistent, not contradictory.

Telemetry: round 4; model opus; effort medium; duration 90s; outcome needs-attention; tokens unknown

```markdown
**adversarial: needs-attention (2)**

1. `tools/nightly.ps1:118` and `tools/nightly.ps1:169` — after Stop-Job kills the runspace, neither kill path terminates the native `dotnet.exe` child it spawned: `Stop-Job` tears down the job host, not the test driver process, and the follow-up `Invoke-OrphanReap` at `tools/nightly.ps1:127` and `tools/nightly.ps1:178` filters `Name='ScratchPad.exe' OR Name='testhost.exe'` with `ExecutablePath` under `Bin`, which cannot match `dotnet.exe` living under `.tools`. A killed Run A/B or Interactive leg therefore leaves its driver alive holding `Bin` locks and, worse, an Interactive driver keeps driving the foreground while the report and P3-gated follow-ons assume the leg is dead: the abort corrupts exactly the proof it was added to protect.

2. `tools/nightly.ps1:133-460` — there is no global deadline: worst case 1800 (Run A) + 300 (Run B) + 1800 (Interactive) + 10×1800 (soak) is ~5.6h, past the task `PT4H` limit in `docs/testing.md:37` and past the 06:50 window end in `docs/testing.md:35`, while the report generates only after the soak loops. The scheduler kills the run before the record lands, which is the failure the always-written report was added to prevent.

**consistency: needs-attention (1)**

1. `docs/soak-and-quarantine.md:7` and `docs/soak-and-quarantine.md:11` — the procedure still describes the pre-ship shape: the local soak as "the full Windows suite plus five extra repetitions" with results "under `TestResults/`". Since this candidate, the full-solution run belongs to governed Run A and every trx lands under `build/nightly/<stamp>/`, so following the procedure as written reproduces neither the command shape nor the evidence paths.

**integration: approve**

**record: needs-attention (1)**

1. `todo/00-workspace/TODO-02-test-backbone.md:276` — item 2 requires "the run's section range and HEAD" in the log header, but the implementation at `tools/nightly.ps1:69` writes only the suite scope (`scope=...`), never the section range, and the cited first-stamped proof `build/nightly/2026-09-20-054411-smoke.log:1-10` is a bare `Start-Transcript` with no header line at all: the smoke branch at `tools/nightly.ps1:341` bypasses `Start-LegLog`, so the quoted evidence cannot satisfy the item as written.
```

## Opus panel (round 5)

Opus final re-sign-off, candidate through `2dd6a07`, skill mechanics (`review_prompt.py` fence, `claude -p --model opus --effort medium --allowedTools Read`, `check-panel` PASS). No earlier finding re-reported. Two advisories (filed, no re-round past 5), one arithmetic needs-attention (fixed in `cfb18e2`, covered by plan review). Panel closed.

`adversarial` advisory
`consistency` advisory
`integration` approve
`record` needs-attention

Findings (IDs round-scoped):

- R5-F1 (adversarial, ADVISORY) FILED: HookFact capability skips would trip the quarantine-only enforcement with a misleading leak message on hook-less hosts. Evidence: `tools/nightly.ps1:423,441`, `tests/UI/TabBarTests.cs:435`, `tests/UI/HookFactAttribute.cs:18`.
- R5-F2 (consistency, ADVISORY) FILED: reported skip count comes from name lines while passed/failed come from assembly sums; `$s` computed but unused. Evidence: `tools/nightly.ps1:266,283,484,487`.
- R5-F3 (record) The §14 rationale arithmetic is wrong (21900 s is 6.1h, not 5.6h); conclusion unaffected. Evidence: `TODO-02-test-backbone.md:373`.

Dispositions (fix-loop 6, `cfb18e2`):

| ID | Disposition | Evidence |
| R5-F1 | fixed | cfb18e2 (D00 T02 §15 filed: allowlist plus unification) |
| R5-F2 | fixed | cfb18e2 (same §15: co-located micro-work, combined filing noted) |
| R5-F3 | fixed | cfb18e2 (§14 arithmetic corrected to 21900 s / 6.1h) |

Telemetry: round 5; model opus; effort medium; duration 92s; outcome needs-attention; tokens unknown

```markdown
**adversarial: advisory (1)**
1. `tools/nightly.ps1:423` and `tools/nightly.ps1:441` — the Interactive enforcement reds the leg for any trx skip whose message lacks `QUARANTINED`, but `tests/UI/TabBarTests.cs:435` (`MiddleClickClosesTheTabUnderTheCursor`, `[HookFact]` + `Category=Interactive`) skips with the capability message set at `tests/UI/HookFactAttribute.cs:18` ("Low-level mouse hooks are unavailable on this host"); on a host where `UiHooks.AreAvailable` is false the run goes red and logs it as a quiet-hours-class leak, which `docs/testing.md` (Interactive leg bar) attributes only to quiet-hours skips.

**consistency: advisory (1)**
1. `tools/nightly.ps1:266` computes `$s` (summed per-assembly Skipped from the authoritative transcript rows) and never returns it; `tools/nightly.ps1:283` carries only the name-line list, and `tools/nightly.ps1:484` derives the reported skip count from `$Sum.Skipped.Count` while passed/failed at `tools/nightly.ps1:264-265` come from the assembly sums — the same report cell then appends the per-assembly `P/F/S` breakdown (`tools/nightly.ps1:487`), so any name-line drop or cross-assembly name collision makes the cell contradict its own breakdown.

**integration: approve**

**record: needs-attention (1)**
1. `todo/00-workspace/TODO-02-test-backbone.md:373` — the §14 rationale states "1800 (Run A) + 300 (Run B) + 1800 (Interactive) + 10 x 1800 (soak) is about 5.6 hours"; that sum is 21900 s = about 6.1 hours, so the filed section's own arithmetic is wrong (conclusion of exceeding PT4H is unaffected).
```

Arch trigger: none - procedure plus local runner plus scheduled task plus guard cron; no editor, storage, extensibility, agent, protocol, or consent surface changes.

## Fix-loop 2 evidence (`31923a6`)

Task run (demand-fired 04:13:43, old code, full legs plus soak, Last Result 1, report 05:38:07, archived `build/nightly-taskrun-2026-09-20/`): Run A 540/0/9, gate exit 1 "flagged=0; census=696 primary=1 expect=secondary", test-seconds 578; Run B 2/0/0, gate exit 0 "flagged=0; census=10 primary=1 expect=primary"; Interactive 24/2/2 (both fails already in D01 T01 §34); soak 10/10 green (UI 5x 158/0/9, Protocol 5x 35/0/0). Both gate codes non-blank: the 7ec7495 job capture proven in situ twice. Run A gate primary=1 recurs from the re-run (743-census, primary=1): filed as D00 T02 §13 (no tests//src/ commit between fa2fc56 and a8127c7, so nondeterministic, not a code regression).

Live proofs on the FL1 candidate: P4 mutex stand-down exit 0 under a held lock; P2 Interactive window-skip RED exit 1 (`SCRATCHPAD_INTERACTIVE_WINDOW=00:00-00:01`); P1 smoke GREEN exit 0 with stamped log `2026-09-20-054411-smoke.log` plus stamped dirs `2026-09-20-054325` (P2) and `2026-09-20-054411` (P1); P3 Run-B-only GREEN exit 0 on the new gated-leg job path (2/2 in 8s, gate exit 0, census=9, trx in `2026-09-20-054451/`). Proof runs overwrote the day report three times; the task report was restored from the archive after (bytes identical, mtime new).

## Plan review

Round: Sol-tier plan review over §9 plus 9 corpus sections, 2026-09-20 (model string unrecoverable post-compaction; tier mirrors the §8 plan review). Neighbors: D00 T02 §8 (Depends), §5/§10/§13/§14/§15/§16/§17 plus D01 T01 §34/§35 plus D01 T02 §16 (XREFs and filings); dependents: D00 T02 §14/§15/§16/§17, D01 T01 §35. Output check-plan PASS (32 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T02 §8, D00 T02 §9, D00 T02 §13, D00 T02 §14, D00 T02 §15, D00 T02 §16, D00 T02 §17, D01 T01 §34, D01 T01 §35, D01 T02 §16]; dependents [D00 T02 §14, D00 T02 §15, D00 T02 §16, D00 T02 §17, D01 T01 §35]; bytes 318331; run 20260920-D00-T02-S9-plan

Unrecovered slots PR24/PR25: the review output for these two targets did not survive context compaction (2026-09-20 ~06:40). No in-tree change cites them. Surviving fragments, unassigned to either slot:

```text
- §16/§17 Done/Cancelled digests plus widened order-row Depends
- a `quiet-hours-smoke` row (ledger.json Run A stand-in via `--fixture run-a`; previously-done rows still parse; `$q` non-null and `$p` null; exactly the 2 ledger rows quitable, not 4, not the §14/§15 rows)
- a `02:30-night` row (Run A `$p` null, oddity window `01:00-02:30`; checkpoint `$p -or ($a -and ($a -lt $o))`; SingleClose/CloseRight checkboxes; Run B `$q` via `--assert-quiet`)
- a D01 T02 §18 filing with quitable ledger rows proving Run A was ungoverned
```

The next plan review over any §9-neighbor section re-derives or drops these slots (ledger trigger `next-plan-review-re-derives`, due with the §35 window close 2026-09-27).

Ledger (namespaced IDs per the §16 shape; 28 filed, 2 corrected in place before stamp, 2 deferred with trigger):

Ledger:

- [D00-T02-S9-PR1] [minor] §8 XREF still two tiers/halves -> rejected corrected in place before stamp (open section): rewritten to three legs with marker
- [D00-T02-S9-PR2] [minor] §8 N1 lacks collection outcome -> rejected corrected in place before stamp (open section): 27-of-28 plus §10 closure noted with marker
- [D00-T02-S9-PR3] [minor] timer-fired completion unproven -> filed D00 T02 §16
- [D00-T02-S9-PR4] [minor] post-fix green unproven -> filed D00 T02 §16
- [D00-T02-S9-PR5] [minor] same-day runs ambiguous -> filed D00 T02 §15
- [D00-T02-S9-PR6] [minor] run evidence lacks retention plus checksums -> filed D00 T02 §14
- [D00-T02-S9-PR7] [minor] soak verdict hidden behind legs -> filed D00 T02 §15
- [D00-T02-S9-PR8] [minor] verdict cells mix test, gate, enforcement, infra -> filed D00 T02 §15
- [D00-T02-S9-PR9] [minor] legs lack snapshot identity -> filed D00 T02 §15
- [D00-T02-S9-PR10] [minor] launch mutual exclusion untested -> filed D00 T02 §15
- [D00-T02-S9-PR11] [minor] task power-state behavior undefined -> filed D00 T02 §14
- [D00-T02-S9-PR12] [minor] recurrence unproven, single completion is luck -> filed D00 T02 §16
- [D00-T02-S9-PR13] [minor] leg failures lack auto-capture -> filed D00 T02 §15
- [D00-T02-S9-PR14] [minor] §13 overclaims not-a-regression -> filed D00 T02 §13
- [D00-T02-S9-PR15] [minor] §13 attribution rests on timestamps -> filed D00 T02 §13
- [D00-T02-S9-PR16] [minor] §13 checkpoint is a single green -> filed D00 T02 §13
- [D00-T02-S9-PR17] [minor] §13 vs §11 landing order undefined -> filed D00 T02 §13
- [D00-T02-S9-PR18] [minor] deadline bound vague -> filed D00 T02 §14
- [D00-T02-S9-PR19] [minor] watchdog simulation unverified -> filed D00 T02 §14
- [D00-T02-S9-PR20] [minor] soak can starve the core verdict -> filed D00 T02 §14
- [D00-T02-S9-PR21] [minor] allowlist matches free text -> filed D00 T02 §15
- [D00-T02-S9-PR22] [minor] parser lacks fixtures -> filed D00 T02 §15
- [D00-T02-S9-PR23] [minor] counts lack conservation check -> filed D00 T02 §15
- [D00-T02-S9-PR24] [minor] correction text lost to compaction, fragments in prose above -> deferred owner operator due 2026-09-27 trigger next-plan-review-re-derives
- [D00-T02-S9-PR25] [minor] correction text lost to compaction, fragments in prose above -> deferred owner operator due 2026-09-27 trigger next-plan-review-re-derives
- [D00-T02-S9-PR26] [minor] §35 removal bar vague -> filed D01 T01 §35
- [D00-T02-S9-PR27] [minor] §35 lacks Depends on §34 plus menus §16 -> filed D01 T01 §35
- [D00-T02-S9-PR28] [minor] §34 lacks visible-outcome assertions -> filed D01 T01 §34
- [D00-T02-S9-PR29] [minor] §34 owes no Interactive leg -> filed D01 T01 §34
- [D00-T02-S9-PR30] [minor] overdue windows never fail -> filed D00 T02 §15
- [D00-T02-S9-PR31] [minor] repeat failures lack incident IDs -> filed D00 T02 §15
- [D00-T02-S9-PR32] [minor] no notify plus trend surface -> filed D00 T02 §17
End of ledger

## Live proof

Digests below are sha256 over the quoted outputs. Quotes ran on the stamp working tree whose code (`src/`, `tests/`, `tools/`, `scripts/`) is byte-identical to candidate tip `cfb18e2` (the stamp commit adds plan prose plus this file only); quoted trees resolve there. Box: Venom-PC. Leg evidence (re-run, task-run, fix-loop scratch proofs) is quoted in Evidence plus Fix-loop 2 above, not re-run here.

`py scripts/todo-graph.py plan --check`: `implementation plan is current -- 90/247 sections complete (36%)`.

Provenance: candidate cfb18e2955e3cb823b59255fa468cd2a4b38fdc1; command py scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.3; digest 0d05af9eade340502843223941f1786e851a8a5aca6e34b161ae3a1780c92d5a; path scripts/todo-graph.py; run 20260920-D00-T02-S9-plan

`py scripts/todo-graph.py validate`: `28 todos, 252 sections -- 0 fatal, 0 warning(s)`.

Provenance: candidate cfb18e2955e3cb823b59255fa468cd2a4b38fdc1; command py scripts/todo-graph.py validate; exit 0; tool CPython 3.14.3; digest 70ad83f5d9c2932d840d9d2f7baee79c98e1861a8bdf36b724b24d99fa4ea70a; path scripts/todo-graph.py; run 20260920-D00-T02-S9-plan
