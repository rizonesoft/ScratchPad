# Review: D00 T01 §32 -- Clearance Governance and Diagnostics

Candidate: `ad8b071` + `10d677c` + `82edff4` + `beb7920` + `1db0dff` (SHIP plus self-review plus two panel fixes plus the filing commit; output `check-panel PASS` on all four headless rounds). Panel tier: Full (the diff touches `scripts/todo-graph.py`, `scripts/todo-validate.py`, the review skill, and the plan-health contract surface), Sol rounds 1-2, Opus rounds 3-4 (sign-off plus confirmation).

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. One self-review fix commit (`10d677c`): the rule-26 malformed message read confusingly on post-cutoff records, reworded before the panel.

Does it make sense: five items take the rule-26 cross-check plus malformed blocking, the stated same-day path, the 16-code failure taxonomy with the /6 bump, and exact seconds beside floored minutes. Exactly the contract, including the recorded shapes (day-granularity anchors, fail-soft parser with fail-closed rule, first-failure codes, progress-only seconds with the plan-health half of the correction amended as drifted).

Is it logical: the parser raises `duration_malformed` without changing fail-soft values; rule 26 skips minute forms, Duration-less sections, absent Started, and pre-cutoff stamps; every clearance break names its leg with furthest-progress proof-touch reporting; the sort key stays total and `failure_code` stays singly typed; seconds scale minute forms and compute ranges. The panel later tightened two spots self-review missed (unshaped Started anchors, the /6 pin sweep).

Did it break anything else: suite moves 902 to 933 by the new fixtures (935 after the panel fix); live validate stays 0 fatal with all ranged records tying on both anchors; plan-health --check stays green. Blast radius is the Duration parser, rule 26, the clearance query, and their fixtures, all covered.

## GPT panel (round 1)

- `adversarial` needs-attention: (1) `Started:` stored unchecked with a prefix-only tie lets `2026-09-20-invalid` anchor a span.
- `consistency` approve
- `integration` approve
- `record` approve

Fix record: Started anchors fail closed (full-instant shape gate plus calendar reality, fall-through to the end check per the §10 precedent); README severity row extended; fixtures pin the unshaped and impossible-date anchors. Fixed in `82edff4`; gates re-ran green before round 2 (935 cases, validate 0 fatal, plan current).

Raw round-1 output (fenced, not part of the verdict scan; digest `5f9fa3a2047445cb9f945dc6e978f26ca2431aac56e0c209b69cbf3df29dedab`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:544`, `scripts/todo-validate.py:1664`, `todo/README.md:168`: `Started:` is stored as unchecked text and the validator compares only its first ten characters, so a malformed value such as `2026-09-20-invalid` passes as the range anchor despite the documented UTC-instant shape, leaving the start anchor uncheckable.

**consistency: approve**

**integration: approve**

**record: approve**
```

## GPT panel (round 2)

Round 2 ran against the R1-fix range with the round-1 verdicts appended as fixed context.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

No fix record: clean round, no commit.

Raw round-2 output (fenced, not part of the verdict scan; digest `fc52c157b6f4a3154c61d8c9fddcca514347b90416974ddb51fe4de3dee22ecb`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Opus panel (round 3)

Round 3 signed off over the SHIP-to-R1 range with rounds 1-2 appended.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` needs-attention: (1) the §35 open pin still reads `plan-health/5` after the /6 bump, against the §29 sweep precedent.

Fix record: the §35 open pin swept `/5` to `/6` with its correction note, plus the §17 supersession note per the same precedent (same family, swept together); the §32 item-4 Done line corrected to name the complete sweep. Fixed in `beb7920`; gates re-ran green before round 4 (935 cases, validate 0 fatal, plan current).

Raw round-3 output (fenced, not part of the verdict scan; digest `c64569741ce8fbde9dd0e068ac58d9549ac076663ae64ca436c4e4c7ea5e74b4`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:970`: the live forward pin still reads `plan-health/5` ("holding the `plan-health/5` shape stable", D00-T01-S35-PR12) after this candidate bumps the contract to `/6`, breaking the established sweep convention (commit `9d571aa` updated it `/3`->`/4` with §27, commit `2c9af72` updated it `/4`->`/5` with §29, each in the bumping candidate), and the §32 item-4 Done claim at `todo/00-workspace/TODO-01-repo-and-toolchain.md:888` asserts "schema /6 with the full sweep" while that pin is unswept.
```

## Opus panel (round 4)

Round 4 confirmed over the R3-fix delta with rounds 1-3 appended.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

No fix record: clean round, no commit.

Raw round-4 output (fenced, not part of the verdict scan; digest `fc52c157b6f4a3154c61d8c9fddcca514347b90416974ddb51fe4de3dee22ecb`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Review notes

Governance section with shared-surface touch: one parser flag, one validator rule, one severity row, seventeen coded clearance sites, a schema bump with a six-surface sweep, and thirty-three fixture checks. The panel's two corrections (unshaped Started anchors, the /6 pin sweep) both landed before close; the R1 fix added its own fixtures and the R3 fix swept the finding's family. The implementation caught one hollow probe by construction (PR70 stayed listed one leg early until the codes proved it; b000001 now touches its proof file and the probe reaches strict ancestry as named). No panel leftovers. Architecture gate skipped silently (no editor state, storage, extensibility, protocol, or consent surface in the diff).

## Plan review

Round: `gpt-5.6-sol` high over §32 plus §31 plus §22 plus §30, 2026-09-19. Neighbors: §31 (sibling review half), §22 (filed-from), §30 (scope). Dependents: none by the mechanical rule (no reverse Depends rows, no XREF consumers yet). The run minted through the `run-id` genesis path (no prior claims, bare base). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (16 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §32, D00 T01 §31, D00 T01 §22, D00 T01 §30]; dependents [none]; bytes 25983; run 20260919-D00-T01-S32-gpt

Ledger (namespaced IDs per the §16 shape; 2 of 16 filed at §55, 5 duplicates of the §31 triage, 9 rejected with reasons on their rows):

Ledger:

- [D00-T01-S32-PR1] [minor] Start anchors forgeable -> rejected ordering reads ends, not starts; day-granularity is the recorded design, and full-instant match would break resume-pinned Started (README: do not overwrite on resume)
- [D00-T01-S32-PR2] [minor] Missing anchors underspecified -> rejected defined behavior: absent Started anchors nothing and unstamped records skip; stamps validate at rest, so no well-shaped range escapes
- [D00-T01-S32-PR3] [minor] Cutoff mutable via Started -> rejected mechanism wrong: the cutoff keys on stamp dates, never Started; backdated-stamp residual is the shared grandfathering design across date-scoped rules
- [D00-T01-S32-PR4] [minor] Duplicates conceal audit data -> rejected last-governs is the tree-wide recorded rule (§22 review R3); duplicate rejection would contradict it
- [D00-T01-S32-PR5] [minor] Seconds undisplayed -> rejected machine half ships in progress JSON; no committed human surface exists, and the dashboard reads the JSON when it lands
- [D00-T01-S32-PR6] [minor] Diagnostics lack context -> filed D00 T01 §55
- [D00-T01-S32-PR7] [minor] Taxonomy unauditable -> filed D00 T01 §55
- [D00-T01-S32-PR8] [minor] First-failure too lossy -> rejected fail-fast is the recorded semantic matching the provable boolean; multi-report is a contract revision, not a missing behavior
- [D00-T01-S32-PR9] [minor] Absence semantics ambiguous -> rejected contract comment plus fixture pin empty string with the singly-typed rationale; no ambiguity ships
- [D00-T01-S32-PR10] [minor] Diagnostic coverage incomplete -> rejected dashboard renders counts only, while run and register never evaluate clearance; plan-health plus summary are the code-carrying surfaces
- [D00-T01-S32-PR11] [minor] Section attribution unproved -> duplicate D00-T01-S31-PR7
- [D00-T01-S32-PR12] [minor] Proof-touch attribution unproved -> duplicate D00-T01-S31-PR9
- [D00-T01-S32-PR13] [minor] Range bases loosely bound -> duplicate D00-T01-S31-PR6
- [D00-T01-S32-PR14] [minor] Merge policy inconsistent -> duplicate D00-T01-S31-PR10
- [D00-T01-S32-PR15] [minor] Execution prose-attested -> duplicate D00-T01-S31-PR8
- [D00-T01-S32-PR16] [minor] Completion state premature -> rejected candidates are immutable recorded commits; ticks record implementation while the stamp gates the row
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `c44823b4fab1d68c8ae9735e0cc2b772feb2634e5c588c69f642297e61f1025f`):

```
- Duration anchors remain forgeable in §32: require the range start to match the full `Started:` instant and record a full review completion instant, because date-only equality still lets arbitrary same-day times manufacture clearance ordering.
- Missing anchors are under-specified in §32: define blocking behavior when `Started:` or the target `Verified:` stamp is absent, because “anchors nothing” lets an otherwise well-shaped range escape the claimed cross-check.
- The cutoff is mutable in §32: define `PLAN_REVIEW_CUTOFF`, its exact boundary semantics, and an immutable record-introduction test, because a backdated `Started:` line could obtain legacy silent-fallback treatment.
- Duplicate Duration records have no owner: add duplicate rejection to §32 rather than relying on last-wins parsing, because a later valid line can conceal malformed or contradictory audit data.
- The duration acceptance claim contradicts the projection in §32: update human and machine-facing progress output to display seconds or a nonzero sub-minute value, because retaining `duration_minutes: 0` still makes real work read as zero.
- Duration diagnostics lack actionable context in §32: include the section reference, offending value, expected anchor, and actual anchor, because a failure code alone does not tell an operator how to repair the record.
- The failure taxonomy is not fully auditable in §32: map all 17 failure sites to a documented stable enum, because the listed categories total only 16 and consumers cannot verify coverage or compatibility.
- First-failure reporting is too lossy in §32: expose all failed clearance legs in deterministic order while retaining a primary code, because fixing one error at a time creates avoidable review loops.
- JSON absence semantics are ambiguous in §32: specify whether accepted and deferred rows omit `failure_code` or emit `null`, because “carry none” permits incompatible plan-health/6 implementations.
- Diagnostic coverage is incomplete in §32: propagate clearance codes to `query run`, dashboard, and risk-register views or explicitly designate one canonical diagnostic surface, because operators otherwise receive different explanations for the same failed clearance.
- Section-level attribution is still unproved in §31: require a changed hunk within the resolved target section, because an unrelated edit elsewhere in the same TODO file can satisfy file-level touch while old evidence clears the finding.
- Proof-touch attribution is still unproved in §31: bind the range diff to the named test definition or its digest, because an unrelated edit elsewhere in the proof file can satisfy the current proof-touch leg.
- Range bases are insufficiently bound in §§22 and 31: require the base to equal or strictly descend from the reviewed candidate and represent the actual pre-fix tip, because an old first-parent ancestor can widen clearance across unrelated history.
- Merge policy is inconsistent in §31: reject merge commits anywhere inside a fix range or redefine “linear loop,” because checking only the tip permits internal merge resolutions to qualify despite the stated exclusion.
- Execution evidence remains prose-attested in §31: add a machine-produced receipt containing command, exit result, candidate, proof digest, and timestamp, because definition presence and file touch do not prove the acceptance test ran against the cleared candidate.
- Completion state is premature in §32: reserve “ships” and final Done claims until the commit, review, verification stamp, and plan sync exist, because every implementation item is checked while the commit remains open and no immutable candidate is identified.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `1db0dff`: `todo-graph self-test: 935 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `1db0dff`: `24 todos, 229 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `1db0dff`: `implementation plan is current -- 72/224 sections complete (32%)`, exit 0.

Provenance: candidate 1db0dff; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 00b7ecf45fcd2c80872647455bc278837169b69d14331d89044fac6ba17bc674; path docs/reviews/00-workspace/D00-T01-s32.md; run 20260919-D00-T01-S32-gpt
