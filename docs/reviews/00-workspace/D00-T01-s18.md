# Review: D00 T01 §18 -- Checker Count Residual

Candidate: `9c5254b` (ship) + `R1FIX` (review R1 fix). Tier: full panel (diff touches shared contract surface: `scripts/`). Lenses owed: adversarial, consistency, integration, record (source-defect/design not owed: no Notepad behavior or surface at stake).

## Self-review

Worked the three questions across the diff and its blast radius; no fixes.

- Sense: both work items land as specified plus the commit. Item 1 implements the decided alternation with the full PR23 matrix; item 2 records the parser comparison with the alternation winning on identical shape at one reviewed line (no relocated decisions, no new capability forgone).
- Logic: the single alternation's three branches are mutually exclusive by construction (count-then-closer, closer-then-count, neither), so double counts cannot match; malformed counts fail the `\d+`; quoted headers with counts stay details via end-anchoring; the 11/11 battery plus the matrix fixtures lock every branch.
- Blast radius: one regex plus comment in `review_prompt.py`, three fixtures, count, ticks; 538 cases green (3 new); live validate silent; the real R5 output still passes; the prompt sentence already reads singular and needs no change.

## Live proof

- `python3 scripts/todo-graph.py self-test` → `todo-graph self-test: 538 cases, 0 failed` at ship, `539 cases, 0 failed` after the R1 fix (outer-closer quote case)
- `python3 scripts/todo-graph.py validate` → `22 todos, 185 sections -- 0 fatal, 0 warning(s)`
- Alternation battery → 11/11; `/tmp/panel-s17-r5.txt` → `check-panel PASS` under the new checker

## Opus panel (round 1)

- `adversarial` approve
- `consistency` approve
- `integration` needs-attention: the stamped §17 item still asserts the double-count defect live although this commit removes it, without the dated closure note the item's convention requires: a present-tense claim contradicted by the shipped checker.
- `record` needs-attention: the Done claims quoted headers with counts in both positions stay details, but only the inner-closer quote is fixtured; the outer-closer quote (the alternation branch whose regression would silently promote a quote) has no case.

Fix: `R1FIX` (dated closure note on §17 item 15; outer-closer quote fixture; record counts reconciled).

Raw round-1 output (fenced, not part of the verdict scan):

```
**adversarial: approve**

**consistency: approve**

**integration: needs-attention (1)**

1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:460` — the stamped §17 item still asserts the defect this commit removes as live behavior: "**Known exception (§17 plan review PR20, filed here):** a header carrying two finding counts (`**v (2)** (3)`) passes although the prompt reads exclusive; tracked in §18, which closes it." Verified false after `scripts/review_prompt.py:32-33`: `check_panel_output("**adversarial: needs-attention (2)** (3)\n…")` returns `(False, 'line 1 precedes the first verdict: …')`. §18 shipped without appending the dated closure note that the same item uses for every other superseded claim (`**Corrected 2026-09-18 (review R1/R2/R3):**`, same line), so the tree now carries a present-tense claim contradicted by the shipped checker.

**record: needs-attention (1)**

1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:485` — the Done text claims "full matrix locked … quoted headers with and without counts stay details", but the contract's PR23 matrix names "quoted headers with counts in **both** positions" and only one position is fixtured: `scripts/todo-graph.py:5988` covers the inner-closer quote (`**record: approve (2)** noted above`); the outer-closer quote (`**record: approve** (2) noted above`) has no case. Probed live: that shape does stay a detail (`(True, 'four lenses, one verdict each')`), so the claim is true of the code and false of the fixtures — the uncovered cell is exactly the alternation branch (`\*{0,2}\s*\(\d+\)`, `scripts/review_prompt.py:33`) whose regression would silently promote a quote to a verdict.
```
