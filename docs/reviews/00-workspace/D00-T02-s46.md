# D00 T02 S46 review - Acknowledgement Third Residuals

Candidate: 499f8cc (Started plus implementation), d12efb0 (round-1 fixes), 3a57d30 (round-2 fixes), 3937689 (sign-off fixes), 3940849 (round-4 fixes).
Tier: Full panel. The diff changes the shared acknowledgement gate (`tools/NightlyParse.ps1` `Test-Acknowledgements`, `Test-AckV2`, `Get-AckDemands`, the classification ledger), which the nightly, the ack helper, and notify all read, plus `tools/NightlyAck.ps1` and the §31 record.

## Evidence map

- Item 1 (duplicate linkage): deferred duplicate resolution in `Test-Acknowledgements`; fixture s46-duplicate-stays-open-naming-the-surviving-action.
- Item 2 (withdrawal keeps remediation): valid non-governing acks stay action sources; fixture s46-withdrawn-ack-keeps-its-open-action.
- Item 3 (deadline inheritance): `Get-DemandDue` over every copy plus corruption inheritance; fixture s46-revised-result-keeps-its-original-due.
- Item 4 (receipt time): committer date in `Get-AckHistory`, `Get-RunFirstNamed`, `Get-AckHistoricalTargets`; fixture s46-backdated-author-date-still-reads-late.
- Item 5 (§31 note, resolution): multi-valued `replaces:`, the §31 Corrected note; fixture s46-merge-resolution-decides-deterministically.
- Item 6 (invalid revision): `InvalidCopies` and `REVISION conflict`; fixture s46-invalid-revision-never-supersedes.
- Item 7 (repair collision): corruption aliases in `Test-AckV2`, `CORRUPTION merge`; fixture s46-repair-collision-keeps-both-histories.
- Item 8 (deleted damage): persisted demands from the record (with the damaged checksum); fixture s46-deleted-damage-stays-demanded.
- Item 9 (verification and remediation items): `Test-FixVerified`, checklist-only `Test-SectionMentions`; fixture s46-fix-needs-a-later-pass-and-filed-needs-an-item.
- Item 10 (classification integrity): the checksum chain in `Add-ResultClassification` and `Read-ResultClassifications`; fixture s46-edited-classification-reads-tampered.
- Item 11 (one disposition per incident): per-incident coverage faults and `Rejected`; fixture s46-overlapping-covers-refuse-and-reject-only-their-run.
- Item 12 (filing crash points): the lock wait and the lost-receipt path; fixtures s46-a-waiting-filing-lands-after-the-lock and s46-lost-receipt-commits-once.
- Item 13 (status): the `-Status` view; fixture s46-status-explains-a-run.

## Self-review

- Does it make sense: every item has a fixture over the shipped gate or helper; the older s39 fixtures that assumed the looser rules (duplicate closing at signing, a commit closing a fix) were updated to the new contract and say so.
- Is it logical: each change fails closed (an invalid revision never wins, a tampered ledger line drops trust from there on, a deleted damaged file stays demanded, a fix waits for verification).
- Did it break anything: NightlyAck, NightlyParse, NightlyNotify, and NightDebt suites green; the nightly calls the same gate signature (new parameters are optional).

Arch trigger: none - nightly acknowledgement tooling and its test fixtures; no editor state, app storage format, extension API, AI, protocol, or consent/undo surface changes.

## GPT panel Round 1

Candidate 499f8cc, slot bulk, run 20260925-D00-T02-S46-codex-c06751119.

**adversarial: needs-attention** (2)
1. `tools/NightlyParse.ps1:5326-5335`: Cover validation runs only when multiple incidents are listed and `covers-all` is absent. Single-incident acknowledgements and acknowledgements declaring `covers-all: yes` can therefore contain overlapping covers without rejection, violating the exactly-one-disposition requirement.
2. `tools/NightlyParse.ps1:5954-5955,5997-6000`: Duplicate targets bypass insertion into `targetsByFile`, and resolving them never registers their state. In an acyclic chain A duplicates B duplicates C, A can close because B appears to have no actions while B still waits on C's open action.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:5929-5930,5518-5519`: Historical top-level actions inherit incidents from the current acknowledgement instead of their original version. Withdrawing a `fixed` acknowledgement in place and omitting `incidents` gives its historical action an empty incident list; `Test-FixVerified` then accepts it without a passing run, closing outstanding remediation during withdrawal.

**integration: needs-attention** (2)
1. `tools/NightlyAck.ps1:127`: Status recomputes the deadline without the corruption deadline inherited inside `Test-Acknowledgements`. A repaired run can display a later deadline than the gate enforces.
2. `tools/NightlyAck.ps1:136-144`: Incident states and corrective blockers consider only the governing acknowledgement. Open actions retained from superseded acknowledgements are omitted, so status can report an incident closed while its earlier remediation remains open.

**record: needs-attention** (1)
1. `tools/NightlyAck.Tests.ps1:388-394`: The concurrency fixture runs one filing against a sleeping lock holder and checks only for an October 1 row. It never runs two filings for different runs or verifies both records survive, leaving the contract's concurrent-filing checkpoint unproven.

Telemetry: round 1; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | d12efb0 (covers validated in every shape; fixture s46-single-incident-overlap-refuses) |
| R1-F2 | fixed | d12efb0 (duplicate fixpoint; fixture s46-duplicate-chain-waits-on-the-end) |
| R1-F3 | fixed | d12efb0 (historical incidents per version; fixture s46-in-place-withdrawal-keeps-the-verification-debt) |
| R1-F4 | fixed | d12efb0 (status reads the enforced deadline; fixture s46-status-reads-the-inherited-deadline) |
| R1-F5 | fixed | d12efb0 (status counts every claiming ack; fixture s46-status-counts-superseded-actions) |
| R1-F6 | fixed | d12efb0 (two concurrent filings; fixture s46-concurrent-filings-both-record) |

## GPT panel Round 2

Candidate d12efb0 (range 499f8cc~1..d12efb0, section files only), slot bulk, run 20260925-D00-T02-S46-codex-c06751119-r2. Round-1 findings did not re-report.

**adversarial: needs-attention** (2)
1. `tools/NightlyParse.ps1:5997-6002,6030-6032`: Duplicate resolution examines only the repeated run's governing acknowledgement. If that run has an open action retained from a superseded acknowledgement and its new governing action is closed, the duplicate closes prematurely despite outstanding remediation for the matching incident.
2. `tools/NightlyParse.ps1:5108-5122`: Legacy classification entries enter the trusted map before their anchor is verified. Editing a legacy operational entry to proof breaks the first chained entry, but the returned map retains the edited proof classification. The tampered publication therefore escapes the operational queue instead of being ignored.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:5686-5689,5917-5932`: Historical actions are deduplicated solely by finding label, and history is skipped whenever that label remains current. Revising a fixed acknowledgement from incidents A+B to A while retaining its fix SHA discards B's verification obligation; a later pass for A alone closes the action. The prior historical-incident fix protects removed labels but still loses incidents removed from a retained label.

**integration: needs-attention** (1)
1. `tools/NightlyAck.ps1:136-148`: Status obtains historical ownership exclusively from current `Claims`. When an acknowledgement is edited to name another run, its retained historical actions no longer associate with the original run in this view. Status consequently omits those corrective blockers and reports no acknowledgement ownership despite outstanding historical remediation.

**record: approve**

Telemetry: round 2; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R2-F1 | fixed | 3a57d30 (duplicates wait on every claiming ack; fixture s46-duplicate-waits-on-a-superseded-open-action) |
| R2-F2 | fixed | 3a57d30 (legacy entries trusted only once anchored; fixture s46-edited-legacy-entry-never-survives) |
| R2-F3 | fixed | 3a57d30 (a current label keeps every version's incidents; fixture s46-narrowed-fix-keeps-the-dropped-incident-owed) |
| R2-F4 | fixed | 3a57d30 (status reads history; fixture s46-status-finds-historical-acks) |

## GPT panel Round 3

Candidate 3a57d30 (range 499f8cc~1..3a57d30, section files only), slot signoff, run 20260925-D00-T02-S46-codex-c06751119-r3. Round-1 and round-2 findings did not re-report.

**adversarial: needs-attention** (2)
1. `tools/NightlyParse.ps1:5194-5198,5627-5644`: Deadline inheritance retains only copies present during the current scan. Overwriting a result with a later revision removes the original deadline inputs, allowing a changed day or severity to grant a fresh response window.
2. `tools/NightlyParse.ps1:5967-5993`: `$allClosed` bypasses `Test-FixVerified`. A `fixed` acknowledgement with an accepted `closed:` reference therefore closes without a passing run after its fix commit, violating the verification requirement.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:5944-5947`: A retained finding label merges historical incidents but discards the historical disposition and evidence. Changing a `duplicate` action to `expected` under the same section reference can close it on that section’s stamp while the repeated run’s action remains open, losing outstanding remediation during a disposition change.

**integration: needs-attention** (1)
1. `tools/NightlyParse.ps1:6023-6026`: Duplicate linkage filters acknowledgements by their current `Runs`, excluding historical run associations. If an acknowledgement is reassigned from run A to run B, its retained open action for A disappears from this lookup. A duplicate of A can consequently close once A’s new governing action closes, despite the historical action remaining open.

**record: approve**

Telemetry: round 3; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

Sign-off disposition: R3-F1 (an in-place revision resets its deadline, so item 3's Done-when fails in the common case) and R3-F2 (a `closed:` reference skips item 9's verification) are stamp-invalidating; R3-F3 and R3-F4 lose tracked remediation (data integrity). All four are fixed in 3937689 and round 4 runs on the depth slot over §46's own commits (the unrelated §24 redesign commit 6c12f22 sits inside the range and is excluded from the diff).

| ID | Disposition | Evidence |
| --- | --- | --- |
| R3-F1 | fixed | 3937689 (the due record; fixture s46-in-place-revision-keeps-its-deadline) |
| R3-F2 | fixed | 3937689 (closed: still verifies; fixture s46-closed-never-skips-fix-verification) |
| R3-F3 | fixed | 3937689 (a label under another disposition stays its own action; fixture s46-disposition-change-keeps-the-earlier-action) |
| R3-F4 | fixed | 3937689 (historical runs in duplicate linkage; fixture s46-reassigned-ack-still-links-its-first-run) |

## GPT panel Round 4

Candidate 3937689 (§46's own commits 499f8cc, d12efb0, 3a57d30, 3937689; the unrelated §24 redesign commit 6c12f22 excluded), slot depth, run 20260926-D00-T02-S46-codex-c06751119-r2. R3-F1..F4 did not re-report. An earlier issue of this round (20260925-D00-T02-S46-codex-c06751119-r4) failed on the provider's 401 and its fallback (20260925-D00-T02-S46-grok-c06751119) timed out; neither counts, and the round was re-issued once the credential returned.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:6069-6070`: An accepted `closed:` reference still closes a `duplicate` before duplicate linkage runs. Supplying that reference closes the duplicate while the repeated run’s matching corrective actions remain open, violating item 1.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:6027-6042`: Historical reconstruction retains only the first disposition and evidence for each finding label. The new comparison preserves that first action but loses intermediate transitions: `expected → duplicate → expected` under the same label drops the duplicate obligation. Changing a duplicate’s evidence under the same label also discards its earlier dependency because matching dispositions merge only incidents.

**integration: needs-attention** (1)
1. `tools/NightlyParse.ps1:5696-5706`, `tools/NightlyAck.ps1:238-240`, `tools/nightly.ps1:1656-1658`: Deadline persistence performs an unlocked read-modify-write from both filing and nightly processes. The filing lock does not protect the nightly writer. Concurrent updates can overwrite an earlier deadline with a later one computed from a rewritten result; atomic replacement prevents partial files but does not preserve the minimum across concurrent writers.

**record: approve**

Telemetry: round 4; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

All three findings lose tracked remediation or a deadline (data integrity), so they are fixed in 3940849 and round 5 runs on the depth slot.

| ID | Disposition | Evidence |
| --- | --- | --- |
| R4-F1 | fixed | 3940849 (linkage before closed:; fixture s46-closed-never-short-circuits-a-duplicate) |
| R4-F2 | fixed | 3940849 (actions keyed by label, disposition, and evidence; fixture s46-intermediate-duplicate-survives-a-return) |
| R4-F3 | fixed | 3940849 (the due record's update runs under an exclusive lock; fixture s46-due-record-lock-keeps-the-minimum) |

## GPT panel Round 5

Candidate 3940849 (§46's own commits through the round-4 fixes), slot depth, run 20260926-D00-T02-S46-codex-c06751119-r3. R4-F1..F3 did not re-report.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:5388-5393,5405`: Removing `h` from every classification entry makes the ledger appear entirely legacy. An operational entry changed to proof is then trusted without a tampering warning, bypassing the publication-integrity requirement.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:6142-6150,6263-6265`: Historical remediation is reconstructed only for currently valid acknowledgement files. Committing a reassignment to a nonexistent finding invalidates the file and removes its previously opened actions from tracking without validated closure.

**integration: needs-attention** (2)
1. `tools/NightlyParse.ps1:5605-5606,5628,5866-5870,6142-6145`: Coverage rejection remains file-wide for missing listed incidents and invalid cover evidence. In a batch covering runs A and B, omitting A’s incident or referencing a nonexistent finding in A’s cover also rejects B’s valid acknowledgement, violating the affected-runs-only requirement.
2. `tools/NightlyParse.ps1:6094-6099,6150,6403-6404`: Corruption aliases transfer deadlines but never transfer action ownership to the restored identity. An open action acknowledged under the unreadable filename is excluded when resolving duplicates of the restored run, allowing those duplicates to close while the inherited remediation remains open.

**record: approve**

Telemetry: round 5; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R5-F1 | live | still reporting at the hard cap (fix-forward 40f7731, unreviewed) |
| R5-F2 | live | still reporting at the hard cap (fix-forward 40f7731, unreviewed) |
| R5-F3 | live | still reporting at the hard cap (fix-forward 40f7731, unreviewed) |
| R5-F4 | live | still reporting at the hard cap (fix-forward 40f7731, unreviewed) |

## Escalation

Round 5 is the hard cap, and R5-F2 (an ack reassigned to a nonexistent finding drops its opened actions) and R5-F4 (a repaired damaged file's ack is not linked to its restored run, so a duplicate can close early) lose tracked remediation (data integrity), so per the review rule the run stops with no stamp and the operator owns the decision. The four findings are fixed forward in 40f7731 (the anchored chain head, invalid acks keeping their history, per-run coverage faults, and alias-aware duplicate linkage), each with a fixture, and NightlyAck, NightlyParse, and NightlyNotify read all green on it, but no panel round has reviewed it. Options for the operator: (a) authorize a confirming sixth round on 40f7731, then stamp; (b) review 40f7731 directly and record the acceptance; (c) direct a redesign (for example, one consolidated acknowledgement state-transition model, which the plan review also asks for). Owner: operator. The row stays `[ ]`; the campaign continues with other ready rows meanwhile. The plan review ran (20260926-D00-T02-S46-codex-c06751119, 15 findings); its ledger is filed with the stamp.

## Live proof

Provenance: candidate 40f7731139c0f5c62efe56eeb75f3a049cc78e9a; command powershell -File tools/NightlyAck.Tests.ps1 plus the NightlyParse and NightlyNotify suites; exit 0; tool Windows PowerShell 5.1; digest 8df4de88ea96b30c647c83929d39e2412aa36362cdf3cfc94b8a01db282725ec; path tools/NightlyParse.ps1; run 20260926-D00-T02-S46-codex-c06751119
