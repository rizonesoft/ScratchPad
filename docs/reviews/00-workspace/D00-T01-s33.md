# Review: D00 T01 §33 -- Provenance Residuals

Candidate: `3961f43` + `6ed61e1` + `97d6587` + `1b97c10` + `4b35a32` (SHIP plus self-review plus two panel fixes plus the filing commit; output `check-panel PASS` on all five headless rounds). Panel tier: Full (the diff touches `scripts/todo-graph.py`, `scripts/todo-validate.py`, the review skill, README, AGENTS.md, and the TODO contract), Sol rounds 1-2, Opus rounds 3-5 (sign-off plus confirmation plus filings delta).

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. One self-review fix commit (`6ed61e1`): the short-candidate WARN matcher used `[0-9a-f]`, which would fire on a recorded uppercase full SHA; widened to `[0-9a-fA-F]` before the panel.

Does it make sense: seven items take the unprovable-candidate WARN, the commit peel in both helpers, the full-ID ratchet with its run-date scope, the candidate-tree mode leg with the same scope, the adversarial path fixtures plus the NUL fix, last-marker-run binding, and the doc-only §21 cites. Exactly the contract, including the recorded shapes (WARN-plus-baseline as the visible degraded state, peel-through shorts under the ratchet, mode-judged tree entries, last-governs runs).

Is it logical: the WARN fires before the baseline check so unaccepted skips stay visible; both git helpers peel `^{commit}` since every caller wants commits; the ratchet keys on the run-date string prefix so history keeps its shorts; the tree leg reads the entry mode from the candidate and degrades unresolving candidates through item 1; run equality retires the any-marker set. The panel later tightened three spots self-review missed (tag-object full IDs, checkout-bound containment, unproduced directory modes).

Did it break anything else: suite moves 935 to 968 by the new fixtures; live validate stays 0 fatal with all 46 grandfathered records silent under both ratchets; plan-health --check stays green. Blast radius is the provenance rule, the two git helpers, the tree-mode reader, and their fixtures, all covered.

## GPT panel (round 1)

- `adversarial` needs-attention: (1) tag-object IDs pass as canonical candidates.
- `consistency` needs-attention: (1) the review skill canonicalizes without enforcing commit type.
- `integration` needs-attention: (1) containment resolves through the checkout, not the candidate tree.
- `record` needs-attention: (1) the symlink fixture never touches a candidate tree.

Fix record: rule 23 compares the peeled commit to the recorded full ID (tag-object IDs rejected, shorts keep peel-through under the item-3 ratchet); the tree leg binds to the candidate-tree entry mode instead of checkout-resolved containment; the skill plus README plus AGENTS.md resolve `<sha>^{commit}` and record the peeled ID; fixture §16 pins the tag-ID fire and the adversarial fixtures ride the candidate leg. Fixed in `97d6587`; gates re-ran green before round 2 (validate 0 fatal, plan current).

Raw round-1 output (fenced, not part of the verdict scan; digest `9912c5ee25e83a576778bc49761795bcb8abf0165306a605546a61fbbcc51b5a`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1788` and `scripts/todo-validate.py:1369,1377` accept a 40-character annotated-tag object ID: `git_resolves()` peels it to a commit but discards the peeled ID, while the validator mistakes the original tag ID for a canonical commit ID.

**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:175` instructs authors to run `git rev-parse <sha>`, which can canonicalize a blob, tree, or annotated-tag object ID without enforcing the required commit type; it must resolve `<sha>^{commit}` and record that peeled commit ID.

**integration: needs-attention** (1)
1. `scripts/todo-validate.py:1389,1408` determines containment by resolving the path through the current checkout, then checks only whether `git_file_at()` returns bytes from the candidate. A candidate-tree symlink can pass when the checkout has a regular file at that path, while a valid candidate blob can fail when the checkout later replaces it with an escaping symlink, so validation is not bound to the candidate tree.

**record: needs-attention** (1)
1. `scripts/todo-graph.py:11200` exercises symlinks only in the fixture checkout and never commits or cans a candidate-tree symlink, while `todo/00-workspace/TODO-01-repo-and-toolchain.md:911` records candidate-tree adversarial coverage as done; the claimed escaping-symlink fixture therefore does not test the implemented candidate-tree leg.
```

## GPT panel (round 2)

Round 2 ran against the R1-fix range with the round-1 verdicts appended as fixed context.

- `adversarial` needs-attention: (1) ls-tree pathspec glob, verified false positive, no commit.
- `consistency` approve
- `integration` approve
- `record` approve

Fix record: no commit. The pathspec finding was verified against git 2.43.0 as a false positive (bare `*` matches nothing so the leg fails closed, `:(glob)` fatals with "pathspec magic not supported by this command", and `--literal-pathspecs` is not an ls-tree option); sibling git call sites were swept (membership tests and resolve-first ordering fail closed). The triage note rode all later round prompts ("Do not re-report it") and no round re-reported it.

Raw round-2 output (fenced, not part of the verdict scan; digest `7439839257ff5fa8b2d50697c76b2e95e646ad6d96dbe460a4d9e99841d1e23b`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1822` and `scripts/todo-validate.py:1429` pass the recorded artifact path to `git ls-tree` as a pathspec rather than a literal path, so values such as `docs/*.md` can match another regular file and falsely satisfy the candidate-tree check; use `--literal-pathspecs` or an explicit literal pathspec and reject multiple results.
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

Round 3 signed off over the SHIP-to-R1 range with rounds 1-2 appended plus the round-2 triage note.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` needs-attention: (2) the Done text names `git_file_at` while the shipped leg reads `git_tree_mode`, and no fixture produces directory or gitlink modes.

Fix record: the item-4 Done line names the shipped `git_tree_mode` leg with the `..`-canonicality and tag-ID rejections, and the fixtures produce `040000` plus `160000` entries so the claimed rejections execute. Fixed in `1b97c10`; gates re-ran green before round 4 (validate 0 fatal, plan current).

Raw round-3 output (fenced, not part of the verdict scan; digest `e857ba94b28785bd02ce3405be77859c62379998b292f1420e6f10bdde97d464`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: needs-attention** (2)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:909` records the shipped shape as "must exist in the candidate tree via `git_file_at`", but the shipped leg is `scripts/todo-validate.py:1433` reading `graph.git_tree_mode` (`scripts/todo-graph.py:1805`); `git_file_at` is no longer called by rule 23 at all. Same family, same line range: no `[x]` item records the two other shipped legs with fixtures, the `..`-canonicality rejection (`scripts/todo-validate.py:1426-1431`, fixtures §10/§18 at `scripts/todo-graph.py:11385,11424`) and the tag-object full-ID rejection (`scripts/todo-validate.py:1397-1403`, fixture §16 at `scripts/todo-graph.py:11419`), so the section's Done text is not the shipped shape.
2. `scripts/todo-graph.py:11410` names its probe "a directory fails loudly at the tree leg" while `scripts/todo-graph.py:11307` leaves §13 uncanned, so `git_tree_mode` returns `None` and the probe takes the identical missing-path branch as §6 (`scripts/todo-graph.py:11364`); the directory mode `040000` and the gitlink mode `160000` that `todo/README.md:412` and `scripts/todo-validate.py:1434` claim to reject are never produced by any fixture, and the real-git pins at `scripts/todo-graph.py:13441-13447` cover only `100644`, `120000`, missing, and bad-ref. `todo/00-workspace/TODO-01-repo-and-toolchain.md:910` nonetheless records "directory" as a shipped fixture "failing exactly once on its leg".
```

## Opus panel (round 4)

Round 4 confirmed over the R3-fix delta with rounds 1-3 appended plus the round-2 triage note.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

No fix record: clean round, no commit.

Raw round-4 output (fenced, not part of the verdict scan; digest `fc52c157b6f4a3154c61d8c9fddcca514347b90416974ddb51fe4de3dee22ecb`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Opus panel (round 5)

Round 5 covered only the filings delta (plan-review joiners plus the checkpoint qualification) with rounds 1-4 appended; the code candidate had already passed four rounds.

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` needs-attention: (2) the checkpoint parenthetical inverts the ratchet scope, and three §55 rows stay unreconciled after §33 shipped their content.

Fix record: fixed in the stamp commit (no sixth round: the skill caps the fix loop at round 5, and both findings are record text with zero behavior change, per the §30 "R3 record count fixed at stamp" and §31 "R4 count only fixed at stamp" precedents). Finding 1 replaces "(legacy shorts warn)" with "(new-record shorts warn; old runs keep their shorts)". Finding 2 appends one reconciliation note per row: PR11 notes the candidate-tree half delivered by §33 item 4 with the digest-recompute half staying open, PR12 notes the commit-peel half delivered by §33 item 2 (named tags peel to their commit, only tag object IDs rejected) with the dangling-commit half staying open, and PR14 notes full delivery by §33 item 1 for the §55 owner to disposition. Exact diffs quoted under Review notes.

Raw round-5 output (fenced, not part of the verdict scan; digest `02b43cb48d4ec5e0e9c3a7a0a08b8e18d35c1359002fdfc001273f608fc84311`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: needs-attention** (2)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:915` states "(legacy shorts warn)", inverting the shipped ratchet scope: `scripts/todo-validate.py:1383` fires `provenance-short-candidate` only when the run date prefix is `> "20260919"`, so legacy (pre-cutoff) short candidates stay silent and it is new records carrying shorts that warn, exactly as item 3 at line 906 records ("a WARN ... fires on short candidates in records whose run date postdates 2026-09-19 ... old-run grandfathering").
2. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1219`, `:1218`, `:1220` stay unreconciled after the §33 candidate shipped their content, in the same commit that files the §33 joiners into §55: line 1219 ("Provenance peels commits ... blobs, trees, tags ... rejected") is delivered by §33 item 2 (`scripts/todo-validate.py:1397-1403`, `scripts/todo-graph.py` peel to `^{commit}`), line 1218's candidate-tree half ("the path reads from the recorded candidate tree") by §33 item 4 (`scripts/todo-validate.py:1414-1440`), and line 1220 ("silent skip" → explicit unverified state) by §33 item 1's `provenance-candidate-unprovable` WARN (`scripts/todo-validate.py:1373-1378`); none of the three rows notes the delivered half or the residual (dangling commits, digest recompute), unlike the SOURCE line at `:1210` which does record "PR13 fixed in the §33 candidate instead".
```

## Review notes

Governance section with shared-surface touch: two peeled git helpers, one WARN-class ratchet with its README mirror, one mode-judged tree leg with run-date scope, last-marker-run binding, four doc-only cites, and thirty-three fixture checks. The panel's corrections all landed before close (R1 tag-object IDs plus checkout-bound containment plus skill wording plus the symlink fixture leg, R3 the true Done text plus produced directory and gitlink modes, R5 the checkpoint wording plus §55 reconciliation); the R1 fix added its own fixtures and the R3 fix produced the modes its finding named. The R2 adversarial was a verified false positive with the git-version proof quoted in Live proof, carried as a triage note rather than a commit. No panel leftovers. Architecture gate skipped silently (no editor state, storage, extensibility, protocol, or consent surface in the diff).

Round-5 cap handling: the skill ends the fix loop after round 5, so the two record-text findings above ride the stamp commit instead of a sixth round. The stamp diff for finding 1 is one parenthetical: `-every new-record candidate is a commit with a canonical ID (legacy shorts warn)` becomes `-every new-record candidate is a commit with a canonical ID (new-record shorts warn; old runs keep their shorts)`. The stamp diff for finding 2 is three appended notes: `**Reconciled 2026-09-19 (§33):** the candidate-tree half ships in §33 item 4 (mode-judged, run-date scoped); the digest-recompute half stays open.` on PR11, `**Reconciled 2026-09-19 (§33):** the commit-peel half ships in §33 item 2 (`^{commit}` in both helpers plus the recorded-full-ID check; named tags peel to their commit, only tag object IDs rejected); the dangling-commit half stays open.` on PR12, and `**Reconciled 2026-09-19 (§33):** delivered by §33 item 1 (`provenance-candidate-unprovable` WARN plus baseline block); no residual, §55 owner dispositions.` on PR14. Zero behavior change; gates re-ran green on the stamp.

## Plan review

Round: `gpt-5.6-sol` high over §33 plus §23 plus §20, 2026-09-19. Neighbors: §23 (filed-from), §20 (run identity). Dependents: none by the mechanical rule (no reverse Depends rows, no D00 T01 §33 consumers in todo/). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (16 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §33, D00 T01 §23, D00 T01 §20]; dependents [none]; bytes 22879; run 20260919-D00-T01-S33-gpt

Ledger (namespaced IDs per the §16 shape; 5 of 16 filed at §55, 0 duplicates, 11 rejected with reasons on their rows):

Ledger:

- [D00-T01-S33-PR1] [minor] Tag-name peel vs cannot-pass -> rejected the shipped item-2 correction defines it (names peel to their commit, tag object IDs rejected, the recorded full ID must be the commit itself); no ambiguity ships
- [D00-T01-S33-PR2] [minor] Warn never rewrites shorts -> rejected shorts fail closed never corrupt, so WARN-plus-baseline is the ratchet by design (CI blocks until accepted); FATAL would break all 46 live records plus the canned fixtures
- [D00-T01-S33-PR3] [minor] SHA-1 40-hex hard-coded -> rejected the whole validator plus fixtures assume 40-hex and the repo reports object-format sha1 with no SHA-256 repo in scope; format migration is tree-wide future work, mis-scoped to a residuals section
- [D00-T01-S33-PR4] [minor] Cutoff boundary imprecise plus backdated bypass -> rejected the boundary is the string prefix > "20260919" in shipped code; the backdated-run residual is the shared grandfathering design across date-scoped rules (same as §32-PR3)
- [D00-T01-S33-PR5] [minor] Migration inventory owner deadline -> filed D00 T01 §55
- [D00-T01-S33-PR6] [minor] Digest never recomputed from tree bytes -> rejected the tree half is delivered by §33 item 4 and the digest half is already owned by the S31-PR11 residual at §55; recompute-from-bytes would also contradict the recorded digest-coverage decision
- [D00-T01-S33-PR7] [minor] One path per artifact or manifest -> filed D00 T01 §55
- [D00-T01-S33-PR8] [minor] Canonical Git-path rules -> filed D00 T01 §55
- [D00-T01-S33-PR9] [minor] Markerless skip silent -> rejected markerless sections are legitimate (no marker means nothing to bind); fatal would break every unmarked section, and the skip ships as a pinned fixture per the item-6 Done
- [D00-T01-S33-PR10] [minor] Last-marker by position retargetable -> rejected last-governs is the tree-wide recorded rule (§22 review R3, same as §32-PR4); explicit marker keys are a contract revision
- [D00-T01-S33-PR11] [minor] Governed warning acceptance -> rejected the baseline entry is the explicit per-record acceptance the section designs (CI blocks until it exists); reason/owner/expiry scoping is process beyond the section Done
- [D00-T01-S33-PR12] [minor] Inventory drift plus overdue gates -> filed D00 T01 §55
- [D00-T01-S33-PR13] [minor] Checkpoint overclaims every -> fixed in the §33 candidate (checkpoint qualified by record generation: new-record candidates and paths plus the named legacy behavior)
- [D00-T01-S33-PR14] [minor] §23 plus §20 missing from dependents -> rejected run-date scoping preserves old-record semantics, so old stamps still describe exactly what those records experience; no revalidation owed
- [D00-T01-S33-PR15] [minor] Hostile-filename fixtures plus NUL-delimited lookup -> rejected tree lookups pass argv with `--` (todo-graph.py:1819) and ls-tree takes no pathspec magic (the R2 false-positive proof), so the named cases cannot misparse; fixtures pin the legs that can fail
- [D00-T01-S33-PR16] [minor] Stable failure diagnostics -> filed D00 T01 §55
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `31b97f248a24841e04fd1a913f31ceb15b4e5e1153904598c72f306facf4d913`):

```
- §33 item 2 is internally inconsistent about tags: it says tags cannot pass, then explicitly allows tag names to peel to commits; define whether post-cutoff candidates must be raw commit OIDs, because mutable tag refs do not pin evidence.
- §33 item 3 promises canonicalization but only warns on short IDs and never rewrites or rejects them; require the stored candidate to equal the resolved canonical commit OID for new records, because warning acceptance leaves ambiguous identities live.
- §33 item 3 hard-codes 40-hex SHA-1 IDs; use Git's repository object format and canonical OID length, because SHA-256 repositories would make valid provenance fail or remain noncanonical.
- §33 items 3 and 4 use "postdates 2026-09-19" without an inclusive timestamp or immutable schema version; define a precise boundary that cannot be bypassed by same-day records or backdated run metadata.
- §33 items 3 and 4 need a migration inventory, owner, and deadline for the 46 short-ID and findings-self-path records; indefinite grandfathering leaves most live evidence outside the guarantees claimed by the section.
- §23 provenance shape plus §33 item 4 verifies only that a path exists in the candidate tree while the digest is never recomputed from those bytes; bind the digest algorithm and value to the exact candidate-tree blob, because existence alone does not prove artifact integrity.
- §33 item 4 changes the path meaning from a findings-file self-path to the "primary reviewed artifact" without defining that artifact for multi-file candidates; specify one path per attested artifact or a manifest, because reviewers otherwise cannot reproduce what the digest covers.
- §33 item 4 needs canonical Git-path rules for separators, repeated separators, dot segments, case, and Unicode normalization; the tooling runs on Windows and Unix, so platform-dependent normalization can validate different tree entries.
- §33 item 6 silently skips run equality when a section has no shaped marker; make markerless post-cutoff provenance fatal and grandfather only inventoried legacy records, because a foreign run can otherwise remain attributed without verification.
- §33 item 6 binds provenance to the section's last marker by document position rather than an explicit marker identity; add a direct marker/manifest key to each line, because reordering or appending a marker can silently retarget existing evidence.
- §33 item 1 needs governed warning acceptance with reason, owner, expiry, and candidate scope; a baselined `provenance-candidate-unprovable` warning can otherwise become a permanent substitute for verification.
- §33 item 7 is documentation-only and does not enforce the §21 owner or 2026-12-31 deadline; add inventory drift and overdue gates, because exemptions can persist after their promised retirement date.
- §33's test checkpoint overclaims "every skip," "every candidate," and "every path" despite markerless skips, warnings, and grandfathered checkout-only paths; qualify it by record generation or close those exceptions so the acceptance statement is testable.
- §23 and §20 are missing from REVIEW DEPENDENTS even though §33 changes their verified provenance and run-binding semantics; require revalidation or dated correction annotations, because their existing stamps now describe superseded behavior.
- §33 item 5 covers several hostile paths but does not state fixtures for tabs, newlines, leading dashes, or Git pathspec metacharacters; add literal NUL-delimited tree lookup cases, because premium-grade provenance must not misparse legal adversarial filenames.
- §33 should give each provenance failure a stable diagnostic containing run, candidate, normalized path, and failed leg; this belongs beside items 1, 4, and 6 because actionable diagnostics materially reduce recovery time without weakening validation.
```

## Live proof

Self-test 968/968 green (33 new §33 cases over the §32 935); live validate 0 fatal 0 warnings with all 46 grandfathered records silent under both ratchets; plan --check current at 74/224 after the stamp. Round-2 false-positive re-proof on git 2.43.0, this session: `git ls-tree HEAD 'docs/*.md'` prints nothing (exit 0, fail-closed empty), and `git ls-tree --literal-pathspecs HEAD docs` errors `unknown option 'literal-pathspecs'`; the call site passes argv with `--` (`scripts/todo-graph.py:1819`), so no shell or pathspec parsing stands between the recorded path and the tree read.
