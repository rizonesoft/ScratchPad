# Review: D00 T01 §4 -- Warning and Analysis Gates

Candidate: `f5482ca` (gates) plus checkpoint evidence commits `f554b05` (red probe) and `2e8a38f` (revert). No fixes; one round.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Warnings as errors | Root `Directory.Build.props` with `TreatWarningsAsErrors` + `EnforceCodeStyleInBuild`; probe fails locally and in CI with `CS0169` |
| 2. Analysis in CI | `AnalysisLevel latest` + `AnalysisMode All` with committed root `.editorconfig`; CI log shows `CA1822`/`CA1823`, proving analysis ran in CI |
| 3. Path exclusions | `.editorconfig` marks `obj/` and `bin/` generated and excludes `CA1515` for `src/IntelligentNotepad/*.xaml.cs` (XAML generates public partials, `CS0262` proven); no blanket suppression |
| 4. Findings block | Same `dotnet build` locally and in CI; probe finding failed CI and reproduced locally; `docs/build.md` records the local command |
| 5. Commit | `f5482ca`, subject matches the item plus the skill-mandated ref |

Runs: `34787039263` green both jobs (gates), `34787733488` red both jobs (probe, `CS0169`+`CA1822`+`CA1823` named), `34788413405` green both jobs (revert). `main` is green at HEAD with zero warnings on both scopes.

## Self-review

1. Does it make sense? Yes. Every item has its change; `AnalysisMode All` is the recorded default with the probe as its proof (CA rules stay silent otherwise); the `CA1515` exclusion is the narrowest path scope with a proven cause.
2. Is it logical? Yes. `All` resolves inside the pinned SDK so the rule set does not float; editorconfig eol rules match `.gitattributes` exactly; the tree builds warning-free on both OSes under the gates.
3. Did it break anything else? No. The `internal` round-trip left the stub files byte-identical to §2; `validate` 0 fatal; `self-test` 391/391; future `tests/` projects inherit the gates by root placement.

## Lenses

- `adversarial`: approve. Red-by-design probe commits reverted immediately with adjacent reverts; no secrets, no permission surface, no workflow change.
- `consistency`: approve. Props and editorconfig follow SDK conventions; T01 marks dated; Markdown one line per item; no em dashes.
- `integration`: approve. §2 outputs and §3 CI consumed unchanged (gates flow through the existing build commands); §5/T02 inherit automatically.
- `record`: approve. Three run IDs plus local outputs quoted; ticks carry evidence; zero deferrals.
- `source-defect`: not owed (no Notepad behavior or protocol detail).
- `design`: not owed (no UI surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; local builds green under the gates on both OSes; CI evidence stands on the exact pushed commits; `plan --check` green after the stamp-flow sync.
