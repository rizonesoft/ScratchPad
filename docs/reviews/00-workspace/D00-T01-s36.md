# Review: D00 T01 §36 -- README Mixed-Panel Touch-Up

Candidate: `4800663` plus `56b17c3` (SHIP plus the R1 fix). Panel tier: Light (one-line README touch-up plus TODO ticks, one consumer, no shared contract surface): Sol round 1, Opus round-2 sign-off, escalated to Full round 3 on the sign-off needs-attention.

## Self-review

Three questions over the SHIP diff, answered with files and behavior. No self-review fix was needed.

Does it make sense: one README line moves exactly as the item prescribes (`headless Opus panel` to `mixed headless panel` on the Deferred bullet), and the TODO carries Started, the exclusion-completing validation correction, and both ticks. Exactly the contract.

Is it logical: both corrected exclusions stand to reason (stamped §9 Why prose is immutable history; §36's Why names the defect it fixes, explicitly marking the title retired), and the re-sweep verdict follows mechanically from the completed enumeration.

Did it break anything else: README prose plus TODO ticks and one correction sentence; validate stays 0 fatal; no code, no fixtures, no behavior. The panel later caught the unquoted checkpoint evidence (R1, fixed on the item) and the stale ignored projections (R2, fixed by sync).

## GPT panel (round 1)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` needs-attention: (1) checkpoint evidence unquoted in the candidate.

Fix record: the re-sweep verdict plus the validate output now ride quoted on the item itself (the SHIP commit carried no evidence body, process-todo-section step 7). Fixed in `56b17c3`; gates re-ran green before round 2 (validate 0 fatal).

Raw round-1 output (fenced, not part of the verdict scan; digest `b944e4c775c401ec0dab824348141e755fa5261b0a5b32b4dfb15b783cf57136`; run `20260919-D00-T01-S36-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:981` and `todo/00-workspace/TODO-01-repo-and-toolchain.md:984` mark the sweep complete and require a quoted clean sweep plus live validation, but candidate `48006637ad94d442aaf1e87494a809a5ce59e717` records neither command nor output for either required checkpoint.
```

## Opus panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` needs-attention: (1) derived plan projections stale.
- `record` approve

Sign-off disposition: a Light sign-off needs-attention escalates to Full continuing at round 3 rather than filing follow-ups. Fix record: `plan --sync` run (ignored `build/` projections only, nothing committable); `plan --check` fully clean with no `::error::` lines. Verified genuine before fixing: the full `plan --check` output carried both stale-JSON errors, hidden until now by a `tail -1` habit (lesson recorded in the run file).

Raw round-2 output (fenced, not part of the verdict scan; digest `df72f951178f09092bdc5364e5ca031adda68f80736217d69875fb83a648bd9a`; run `20260919-D00-T01-S36-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**

**consistency: approve**

**integration: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:976,981-982` (Started marker plus the two flipped items) leave the derived plan projections stale: `python3 scripts/todo-graph.py plan --check` emits `::error::build/todo-progress.json is stale` and `::error::build/todo-operator.json is stale`; a re-sync diff attributable solely to this candidate shows `in_progress 0 -> 1`, `open 155 -> 154`, and the §36 item `"status": "open" -> "done"` with the appended evidence text, so `plan --sync` is owed before this section closes (working tree restored to its pre-check state after verification).

**record: approve**
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Panel closes: all approve on the escalated round; no filings, no leftovers. Architecture gate: not triggered (README prose plus TODO ticks; none of the six surfaces).

Raw round-3 output (fenced, not part of the verdict scan; digest `fc52c157b6f4a3154c61d8c9fddcca514347b90416974ddb51fe4de3dee22ecb`; run `20260919-D00-T01-S36-opus-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**

**consistency: approve**

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §36 plus §35, 2026-09-19. Neighbors: §35 (Depends plus XREF). Dependents: none by the mechanical rule (no reverse Depends rows, no D00 T01 §36 consumers in todo/). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (16 findings, one per line). Run note: the plan run shares its base with the round-1 panel run (`20260919-D00-T01-S36-gpt`); kind distinguishes them and the minter is authoritative. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §36, D00 T01 §35]; dependents [none]; bytes 10989; run 20260919-D00-T01-S36-gpt

Ledger (namespaced IDs per the §16 shape; 5 of 16 filed at §37 plus §38 plus §55, 3 duplicates, 8 rejected with reasons on their rows):

Ledger:

- [D00-T01-S36-PR1] [minor] §35 Commit unticked despite stamp -> filed D00 T01 §37
- [D00-T01-S36-PR2] [minor] §36 lacks stamp artifacts -> rejected the lifecycle orders plan review before the stamp; artifacts land with the stamp commit
- [D00-T01-S36-PR3] [minor] Re-sweep summary is not a quote -> rejected Live proof quotes every gate at stamp; items point, the record quotes
- [D00-T01-S36-PR4] [minor] Directory exclusions too broad -> rejected records are append-only history with no live-guidance vector
- [D00-T01-S36-PR5] [minor] Sweep pattern unpinned -> rejected a case-insensitive sweep returns only excluded records; durable enforcement rides the PR7 lint item
- [D00-T01-S36-PR6] [minor] Two panel terms survive -> filed D00 T01 §37
- [D00-T01-S36-PR7] [minor] No retired-term regression rule -> filed D00 T01 §55
- [D00-T01-S36-PR8] [minor] Non-last rounds skip structural validation -> rejected disputes the recorded last-wins rule; early rounds are superseded drafts by design
- [D00-T01-S36-PR9] [minor] Advisory-file vs escalate contradiction -> rejected verdict-dependent rules are compatible (advisory files, Light needs-attention escalates)
- [D00-T01-S36-PR10] [minor] Round-5 file-and-stamp over blocking -> duplicate D00-T01-S35-PR2
- [D00-T01-S36-PR11] [major] Arch blocking leftover has no terminal -> filed D00 T01 §38
- [D00-T01-S36-PR12] [minor] Arch gate outage unrunged -> duplicate D00-T01-S35-PR5
- [D00-T01-S36-PR13] [minor] Mid-sequence failover plus fallback pin missing -> rejected mid-sequence failover is stated; the Sol rung pins model effort sandbox by reference
- [D00-T01-S36-PR14] [minor] Skipped gate needs no recorded rationale -> duplicate D00-T01-S35-PR4
- [D00-T01-S36-PR15] [minor] Arch gate never proven live -> filed D00 T01 §38
- [D00-T01-S36-PR16] [minor] §36 row lacks §35 dependency -> rejected the row already Depends On §35 (resolve exits 0)
End of ledger

Raw plan-review output (fenced, not part of any scan):

```
- The §35 Commit item remains unchecked despite a Verified stamp; §35 should reconcile the checklist with the reviewed candidate range because contradictory completion state undermines plan automation and audit trust.
- §36 has every implementation item checked but no Verified stamp, Review record, candidate identity, duration, or implementer; §36 should add the normal review lifecycle evidence because completion claims are not yet independently proven.
- §36 requires the re-sweep to be “quoted clean” but records only a summary of its result; §36 should preserve the exact bounded command, pattern, exit status, and output because the stated acceptance gate cannot be reproduced.
- The §36 sweep excludes entire `docs/reviews/` and `docs/phase-runs/` trees; §36 should allowlist exact historical records or clearly historical contexts because broad directory exclusions can conceal newly introduced live guidance using retired terminology.
- The §36 acceptance criterion does not define the exact retired-title pattern or case and punctuation handling; §36 should pin the literal or regex search contract because semantic variants and capitalization changes can evade an ambiguous sweep.
- The README wording uses “mixed headless panel” while §35 establishes “mixed Sol/Opus panel” as the regime name; §36 should use one canonical term or explicitly define the alias because another terminology split defeats the consistency touch-up.
- The retired-title check is a one-time manual sweep; §36 or the validator should add a regression rule for live documentation because the same stale wording can return immediately after this section closes.
- §35 deliberately keeps a malformed GPT-early record silent when a clean Opus record is last; §35 should validate every round’s structural integrity while letting only the last round govern the verdict because last-wins must not erase corrupt or unauditable review history.
- §35 says advisories after sign-off are filed without rerounding, but also says any Light sign-off with `needs-attention` escalates to Full; §35 should distinguish advisory-only from blocking `needs-attention` because the same outcome currently requires two incompatible actions.
- §35 permits a round-5 `needs-attention` result to use a file-and-stamp path while rounds 4–5 are restricted to blocking findings; §35 should prohibit stamping with unresolved blocking findings or define an explicit exception authority because the hard cap must not convert risk into approval.
- The architecture gate allows one rerun and then files leftovers without stating what happens when a blocking architecture finding remains; §35 should define a no-stamp terminal state and remediation path because expensive-to-reverse defects cannot safely become deferred advisories.
- The outage matrix covers Opus failure at panel sign-off but not Opus-high failure at the architecture gate; §35 should specify an equivalent reviewer, postponement, or no-stamp rule because triggered high-risk sections otherwise have no executable completion path.
- “Sol unreachable runs all rounds on Opus” does not cover Sol failing after an earlier Sol round, and “GPT sign-off” does not pin a model and effort; §35 should define mid-sequence failover, numbering, retry/no-flap behavior, and the exact fallback reviewer because transient outages must produce deterministic records.
- Architecture-gate applicability has trigger categories but no required recorded rationale when skipped; §35 should require a positive or negative applicability decision in every review record because silent misclassification can bypass the premium-risk control.
- §35 defers the first live architecture-gate proof to an unspecified future section; §35 should include a disposable end-to-end fixture or name a concrete owner section because an unexercised blocking workflow can fail only when a costly product change reaches review.
- §36 cites §35 only as an XREF rather than showing an explicit dependency contract; §36’s Implementation Order row should depend on §35 if it does not already because the repair must not run or close independently of the regime that created and defines it.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `8c75c46`: `todo-graph self-test: 1038 cases, 0 failed`, exit 0.

`grep -rn "headless Opus panel" README.md todo/ docs/ scripts/ .claude/ resources/ src/ tests/ AGENTS.md` on `8c75c46` (lines cut at 120 chars), exit 0:

```
todo/00-workspace/TODO-01-repo-and-toolchain.md:306:Why this section exists: `review-todo-section` requires lens verdict
todo/00-workspace/TODO-01-repo-and-toolchain.md:979:Why this section exists: the §35 Opus sign-off (round 3) reported o
docs/reviews/00-workspace/D00-T01-s35.md:115:- `consistency` advisory (1): root `README.md:79` still names the retired `
docs/reviews/00-workspace/D00-T01-s35.md:124:1. `README.md:79` still reads "lens verdicts come from the headless Opus pa
docs/reviews/00-workspace/D00-T01-s36.md:9:Does it make sense: one README line moves exactly as the item prescribes (`he
```

`python3 scripts/todo-graph.py validate` on `8c75c46`: `24 todos, 235 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `8c75c46`: `implementation plan is current -- 75/230 sections complete (33%)`, exit 0.

Provenance: candidate 8c75c46fc8959697eb8ce9f7f84024c54c6321db; command `python3 scripts/todo-graph.py self-test && grep -rn "headless Opus panel" README.md todo/ docs/ scripts/ .claude/ resources/ src/ tests/ AGENTS.md | cut -c1-120 && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 1609328f6c6bb88be564448ada0f0ca46e36ba672a18b52b72338d5cb5cdcac7; path README.md; run 20260919-D00-T01-S36-gpt

(Digest over the self-test line, the five fenced sweep lines, the validate line, and the plan line, joined by newlines with a trailing newline.)
