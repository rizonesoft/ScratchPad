# Review: D00 T01 §37 -- Panel Rule Residuals

Candidate: `3bb28dc` plus `3310659` plus `a242025c` (SHIP plus the R1 and R2 fixes). Full panel (skill plus validator plus help): Sol rounds 1-2, Opus round-3 sign-off. This review dogfoods the new item-3 rule: per-round finding IDs plus disposition tables throughout.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. No self-review fix was needed.

Does it make sense: the skill names the note shape (replacing the no-note-owed sentence), the round-5 bar plus stop rule, the finding IDs, the all-Opus round positions, and the term alias; the validator fires the new class with birthday scope; the help defines the fallback; §35 carries the annotation; fixtures pin accept, reject, boundary, and bare-note shapes. Exactly the contract.

Is it logical: the birthday boundary exempts s25, verified the sole post-cutoff Opus-only record tree-wide; colon-plus-failure is required; undated stamps fail closed with none live; fixture repairs were audited per family (injection where inert, pre-rule restamp where an append heals fences); the definition rides the query description because argparse has no per-choice help; the probe normalizes whitespace because argparse reflows to terminal width.

Did it break anything else: suite moves 1038 to 1043 by the 4 pins plus the help probe, then 1045 by the R2 pins; live validate stays 0 fatal; plan stays current. The new FATAL fires only on future Opus-only records. The panel later caught colon-denials (R1, fixed), the empty-GPT shield plus the missing table shape (R2, fixed), and two below-bar residuals (R3, filed at §55).

## GPT panel (round 1)

- `adversarial` needs-attention: (R1-F1) shaped note still admits colon-denials.
- `consistency` approve
- `integration` approve
- `record` approve

Disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 3310659 |

Fix record: the regex gains a `No`-prefix lookbehind plus a nothing-valued lookahead (none, n/a, nothing, never, not applicable); free-form failures stay admissible; the accept fixture pins the no-CLI vocabulary and the reject fixture pins all four denial shapes. Fixed in `3310659` (denial matrix 15/15, suite 1043/1043, validate 0 fatal); gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `5c7502f9223b541461a1b7151ba9c8f835bbe11d03dbe32c41765639557a191a`; run `20260919-D00-T01-S37-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-validate.py:459` accepts any substring matching `sol outage:\s*\S`, including `No Sol outage: occurred` or `Sol outage: none`; this lets a denial or non-failure satisfy the mandatory failure-accountability note required by `.claude/skills/review-todo-section/SKILL.md:124` and documented in `todo/README.md:403`.
**consistency: approve**
**integration: approve**
**record: approve**
```

## GPT panel (round 2)

- `adversarial` needs-attention: (R2-F1) empty GPT heading shields the rule.
- `consistency` needs-attention: (R2-F2) table shape missing from the skill.
- `integration` approve
- `record` needs-attention: (R2-F3) item ticked while the table shape is missing.

Disposition table (R1-F1 stays fixed):

| ID | Disposition | Evidence |
| R2-F1 | fixed | a242025c |
| R2-F2 | fixed | a242025c |
| R2-F3 | fixed | a242025c |

Fix record: Sol evidence is now a GPT section carrying at least one lens verdict (sliced like the GPT-last branch), so empty GPT plus Opus fires and single-verdict stays silent; the skill gains the table row shape (`| ID | Disposition | Evidence |`), which completes the ticked item. Fixed in `a242025c` (fixtures §§44-45, suite 1045/1045, validate 0 fatal); gates re-ran green before round 3.

Raw round-2 output (fenced, not part of the verdict scan; digest `d25e602b21eb522df127c2e7cddfecd98c0ef957eb91512459f3392d91f57079`; run `20260919-D00-T01-S37-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`; receipt carries the short candidate `3310659`, full form `33106590bae0459576dfa37a12861dbaca5bfb30`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-validate.py:596` treats any `GPT panel` heading as proof that Sol ran. An empty or malformed early `GPT panel` heading followed by a valid final `Opus panel` bypasses `panel-sol-outage-missing`, allowing skipped Sol rounds to pass as a mixed record.

**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:124` defines finding IDs and disposition values but provides no disposition-table shape, despite the section contract requiring both ID and table shapes.

**integration: approve**

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1001` marks the disposition-table item complete even though `.claude/skills/review-todo-section/SKILL.md:124` does not define the required table shape.
```

## Opus panel (round 3)

- `adversarial` advisory: (R3-F1) nothing-check false-fires on honest openers.
- `consistency` needs-attention: (R3-F2) verdict-evidence condition missing from the docs.
- `integration` approve
- `record` approve

Sign-off disposition: both findings sit below the blocking bar (validator edge wording, docs follow-up), so per the Full sign-off rule each files through add-todo and the panel closes without re-rounding: R3-F1 joins D00 T01 §55 (re-anchor the nothing-check to the whole value), R3-F2 joins D00 T01 §55 (state the verdict-evidence condition in the README row plus the skill). Verified genuine against source before filing: both honest openers trip the nothing-valued lookahead by inspection, and neither docs site states the evidence condition.

Final disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 3310659 |
| R2-F1 | fixed | a242025c |
| R2-F2 | fixed | a242025c |
| R2-F3 | fixed | a242025c |
| R3-F1 | filed | D00 T01 §55 |
| R3-F2 | filed | D00 T01 §55 |

Architecture gate: not triggered (review tooling only: runner-adjacent skill prose, validator rule, help text; none of the six surfaces).

Raw round-3 output (fenced, not part of the verdict scan; digest `31f91bec20d136082cbf946aa6b092c03f7294a62b077c266257f17c611f1aae`; run `20260919-D00-T01-S37-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: advisory (1)**
1. `scripts/todo-validate.py:465` — the nothing-valued lookahead is anchored only at the start of the note value, so honest failure prose that opens with one of the listed words is rejected: `Sol outage: never responded before the 600s timeout` and `Sol outage: none of the Sol CLIs are installed on this box` both fail the lookahead and raise `panel-sol-outage-missing`, contradicting the comment at `scripts/todo-validate.py:459-461` that honest prose must never false-fire, and the emitted message ("lacks the Sol outage line") misdescribes a line that is present.

**consistency: needs-attention (1)**
1. `todo/README.md:403` — the class description defines the firing set as "An Opus-only findings file (Opus panel sections, zero GPT panel sections)", which no longer matches the shipped rule: after the R2 fix at `scripts/todo-validate.py:594-604`, a record containing a `GPT panel` section still fires whenever that section carries no lens verdict, which is exactly what fixture §44 (`scripts/todo-graph.py:7093`, `90-panel-gptempty.md` at `scripts/todo-graph.py:7898-7905`) pins as firing. The verdict-evidence condition is absent from both documentation sites of this family: `todo/README.md:403` and `.claude/skills/review-todo-section/SKILL.md:124`, whose outage matrix owes the note only when "Sol is unreachable before any round runs" and never states that a verdict-less `GPT panel` section fails to count as a Sol round.

**integration: approve**

**record: approve**
```
