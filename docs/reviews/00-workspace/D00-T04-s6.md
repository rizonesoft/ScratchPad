# Review: D00 T04 §6 -- Acceptance-Record Sign-Off Residuals

Tier: Full (the diff touches shared contract surface: `scripts/todo-graph.py`, `scripts/todo-validate.py`, the review skill, and `todo/README.md`). Candidates: `2ccde87` (ship), `0f10935` (self-review fix), `4ec826c` (fix-loop 1).

## Self-review

Q1 (exact outcome, no widening): item 1 maps to the accepted/deferred critical append order (`failure_code` before `accepted_outcome`) plus the §23 renewed-chain pin; item 2 maps to the peel-identity re-rejection recorded in the `evidence_lineage` docstring, the validator foreign-evidence leg comment, the skill evidence sentence, and the README foreign-evidence row, plus the tag-proceeds pin; item 3 maps to `stamp_generations_by_section`, the validator `stamp-generation-regressed` leg, the SEVERITY_MAP entry plus README row, and the §§112-113 pins; item 4 maps to `OUTCOME_MATRIX`, the `terminal-outcome` leg with its `acceptance_hold` outcome parameter at all three call sites, the skill matrix sentence plus cause-list entry, the README matrix, and the row-for-row plus void pins; item 5 maps to `CriticalFinding`/`MajorFinding`, the three keyword-constructed producers, the fourteen attribute-routed consumers, and the seven contract pins. Item 6 matches the commit subject verbatim. No widening: the README severity-table row and the skill cause-list entry are each forced by the shipped rules.

Q2 (logic): the health-result field order preserves the old positional layout exactly, so the default tuple sort reproduces the removed explicit index keys element for element; every producer constructs with keywords and every consumer reads attributes (verified by search: no positional append, unpack, or index remains); the terminal-outcome leg reads the record outcome at all three hold call sites; the generation map reads coverage off the stamp cover field through COVER_ITEM_RE exactly like the parser. One finding before the lenses: the generation map first scanned the whole stamp body for `§N` mentions (missing sigil-less and spaced ranges, reading evidence prose as coverage); fixed with the ranged-cover pin in `0f10935`.

Q3 (blast radius): the tuple-to-named-type refactor is behavior-preserving (sort order identical, JSON keys unchanged, index reads replaced one for one); the terminal-outcome leg changes coverage only for matched rejected/closed records (no live record carries those outcomes; the §23 rejected chain is fixture-only); the generation leg fires only on uncommitted regressions against HEAD (validate green on the live tree); the remediated matrix cell follows established §22 covering behavior. Live gates green at ship (self-test 1263/1263, validate 0 fatal 0 warnings, plan-health gate ok, plan current).

## GPT panel (round 1)

- `adversarial` needs-attention (1)
- `consistency` needs-attention (1)
- `integration` needs-attention (1)
- `record` needs-attention (1)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Fix record: R1-F1 (pre-match terminal check lets an unrelated rejected/closed record report `terminal-outcome` for another target) and R1-F3 (same root at the covering loops, poisoning the reported cause and fix) fixed by moving the outcome check after the match in `acceptance_hold` with the docstring order corrected: unmatched terminal records read `no-match`, matched terminal records void before the remaining legs. The two `voids before match` unit pins became four matched/unmatched pins. R1-F2 (the matrix says `remediated` terminates while the code lets it cover) and R1-F4 (same contradiction plus the S22 check mislabeled `renewed`) fixed by correcting the matrix cell to `continues` in code, README, and skill (established §22 covering behavior stands; the debt being gone obligates filing, not renewing) and renaming the check to `the remediated marker waiver covers and reports its outcome`. No live record carries a `remediated` outcome, so established behavior is fixture-pinned only. Fixed in the fix-loop 1 commit; gates re-ran green before round 2 (self-test 1266/1266, validate 0 fatal 0 warnings).

Raw round-1 output (run 20260920-D00-T04-S6-gpt, artifact `94f66c1317cfef44c060eb1d832a8b91cd4abe23bd0fd8a16f5f01051b8a7d15`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1729-1732` evaluates `rejected`/`closed` before verifying `match`, so an unrelated terminal record returns `terminal-outcome` instead of `no-match` and can poison another target’s noncoverage result.

**consistency: needs-attention** (1)
1. `todo/README.md:441`, `.claude/skills/review-todo-section/SKILL.md:190`, and `scripts/todo-graph.py:1774-1783` declare `remediated` coverage terminated and the waiver inert, but `scripts/todo-graph.py:1729-1749` lets `remediated` return `cover`.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:4281-4334` and `scripts/todo-graph.py:4546-4581` consult every file-scoped acceptance, so the pre-match terminal check at `scripts/todo-graph.py:1729-1732` makes an unrelated rejected or closed acceptance supply another run, outage, or finding’s reported cause and remediation.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:139` claims fixtures pin every outcome cell, but `scripts/todo-graph.py:16507-16513` labels a `remediated` case “renewed” and asserts that it still covers, directly contradicting the recorded inert-waiver semantics.
```

## GPT panel (round 2)

- `adversarial` needs-attention (2)
- `consistency` needs-attention (1)
- `integration` needs-attention (1)
- `record` needs-attention (1)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Fix record: R1-F1 through R1-F4 confirmed fixed (not re-reported). R2-F2 (unbounded range expansion in `stamp_generations_by_section`: a crafted `§1-999999999` cover stalls validation) fixed by mirroring the parser's `MAX_STAMP_COVERAGE` cap: over-wide stamps skip whole, with a no-stall pin. R2-F1 (terminal voids before the remaining legs) rejected: voiding is fail-closed (a void record never un-covers another record's cover), and the cause names the dispositive fact (a stale rejected record reporting `stale-generation` would misdirect remediation toward renewal, which the review already foreclosed). R2-F3 (section note vs order pins) rejected as a contradiction: the §23 pin proves voiding with perfect legs and the matched unit pins prove voiding precedes the remaining legs, so both hold at once; the section note now says so explicitly. R2-F4 and R2-F5 (the risk register reads rejected/closed heads `live` and omits their outcome) rejected: register state is contractually wall-clock by design (the register header plus schema risk-register/1: instruments with residual severity and wall-clock state, never coverage), no consumer gates on register state, and coverage reads live in plan-health; outcome visibility in the register is new scope, not a defect. Fixed in the fix-loop 2 commit; gates re-ran green before round 3 (self-test 1267/1267, validate 0 fatal 0 warnings).

Raw round-2 output (run 20260920-D00-T04-S6-gpt-r2, artifact `1c2e61a58c6751c1c7fc0c0e728cc175686b0ca89c88a091cc4a22ea4b8b1d3e`):

```
**adversarial: needs-attention** (2)
1. `scripts/todo-graph.py:1733-1753` returns `terminal-outcome` before checking generation, dates, ancestry, resolvability, or freshness, so a stale, future-dated, foreign, or unresolvable rejected/closed record can terminate coverage without valid evidence.
2. `scripts/todo-graph.py:2174-2183` expands ranges without enforcing `MAX_STAMP_COVERAGE`; a malformed range up to 999999999 sections can exhaust memory or stall validation.

**consistency: needs-attention** (1)
1. `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:139` says terminal outcomes void with perfect legs, but `scripts/todo-graph.py:17252-17259` deliberately supplies a missing target day and asserts that rejected/closed bypass the remaining legs.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:5467-5501` discards `_outcome`, labels an unexpired rejected/closed head `live`, and omits its terminal outcome from the risk-register entry, contradicting the new coverage semantics for downstream consumers.

**record: needs-attention** (1)
1. `scripts/todo-graph.py:5488-5501` records rejected/closed instruments as `live` without their outcome, leaving the generated acceptance register unable to distinguish terminating decisions from covering waivers.
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` advisory (1)
- `integration` approve
- `record` approve

Telemetry: round 3; model opus; effort medium; duration 81s; outcome advisory; tokens unknown

Fix record: sign-off round, no re-round (the one advisory files through add-todo per the sign-off rule). R3-F1 (the generation map claims parser-refused stamps: bad cover elements skip per element while the parser refuses whole, and date plus post-prefix-evidence checks are missing): filed as D00 T04 §7. Below the blocking bar: a refused stamp co-fires `malformed-stamp`, so the map's generation cause is redundant diagnostics, never a lone false gate. R1 fixes and the R2 fix confirmed (not re-reported); the R2 rejections unchallenged. Panel closed.

Raw round-3 output (run 20260920-D00-T04-S6-opus, artifact `7367e37ff2ccef3547586d59a676f092c08cac09441c53101f12f006b4a87a78`):

```
**adversarial: approve**

**consistency: advisory (1)**
1. `scripts/todo-graph.py:2181-2186` — `stamp_generations_by_section` skips a malformed (`item is None`) or empty (`lo < 1 or hi < lo`) coverage element and still claims the remaining elements, while the parser at `scripts/todo-graph.py:516-529` refuses the whole stamp on the first such element (and at `scripts/todo-graph.py:497-511` on an invalid date or an evidence field empty after the generation prefix, neither of which the map checks at `scripts/todo-graph.py:2172-2176`). So a live stamp the parser refuses (section reads unstamped) can still be claimed by the map at generation 0 and fire `stamp-generation-regressed` on it, naming a generation cause for a stamp that claims nothing. The docstring at `scripts/todo-graph.py:2156-2158` asserts the opposite parity ("exactly like the parser"), as does the over-wide note at `scripts/todo-graph.py:2164-2166` which does skip whole, and the section record repeats it at `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:137` ("coverage reads off the cover field per parser").

**integration: approve**

**record: approve**
```

## Disposition

| ID | Disposition | Evidence |
| R1-F1 | fixed | fix-loop 1, confirmed R2 R3 |
| R1-F2 | fixed | fix-loop 1, confirmed R2 R3 |
| R1-F3 | fixed | fix-loop 1, confirmed R2 R3 |
| R1-F4 | fixed | fix-loop 1, confirmed R2 R3 |
| R2-F1 | rejected | fail-closed void names the dispositive fact; unchallenged R3 |
| R2-F2 | fixed | fix-loop 2, confirmed R3 |
| R2-F3 | rejected | both hold at once; note clarified; unchallenged R3 |
| R2-F4 | rejected | register state is wall-clock by contract; unchallenged R3 |
| R2-F5 | rejected | same root as R2-F4; unchallenged R3 |
| R3-F1 | filed | D00 T04 §7 (refusal parity; redundant-with-malformed-stamp advisory) |

Arch trigger: none - scripts plus skill plus README prose only; no editor state, storage, extensibility, AI, protocol, or consent surface in the diff.

## Plan review

Round: `gpt-5.6-sol` high over §6 plus §51 plus §7 with dependents D00 T04 §7 plus D00 T02 §11 plus D00 T02 §8 plus D00 T02 §13, 2026-09-20. Neighbors: §51 (Depends: §6 files its sign-off residuals); §7 (XREF plus dependent: the sign-off advisory filed there); dependents: D00 T04 §7 (reverse dependent plus XREF), D00 T02 §11 (reverse dependent: the tuple-order edge), D00 T02 §8 plus D00 T02 §13 (the transitive hop: XREF consumers of §11). Output `check-plan PASS` (18 findings, one per line). Five filed (PR1 record fix in §6; PR5 extension at §7; PR6 plus PR7 merged at §8; PR10 at §9), one duplicate of D00-T01-S51-PR5, twelve rejected with reasons below. Two majors (PR6, PR10, both filed), no criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T04 §6, D00 T01 §51, D00 T04 §7]; dependents [D00 T04 §7, D00 T02 §11, D00 T02 §8, D00 T02 §13]; bytes 19244; run 20260920-D00-T04-S6-gpt-r3

Ledger:
- [D00-T04-S6-PR1] [minor] visibility quote reversed -> filed D00 T04 §6
- [D00-T04-S6-PR2] [minor] tag metadata unattested -> rejected trust model answers: no signature consults anywhere, single-operator default with stated registry cost
- [D00-T04-S6-PR3] [minor] history-leg case coverage -> rejected scope is the uncommitted edit by design; hooks gate the commit, --no-verify forbidden
- [D00-T04-S6-PR4] [minor] §7 should block or §6 declare -> rejected filed-follow-up plus stamp Review-line naming is the declaration mechanism; stamp names §7
- [D00-T04-S6-PR5] [minor] shared stamp parser for §7 -> filed D00 T04 §7
- [D00-T04-S6-PR6] [major] NamedTuple still positional -> filed D00 T04 §8
- [D00-T04-S6-PR7] [minor] boundary contract tests -> filed D00 T04 §8
- [D00-T04-S6-PR8] [minor] chain enforcement missing -> rejected factually wrong; risk-acceptance-chain-broken ships self, cycle, double, id, cross-target, and fork legs
- [D00-T04-S6-PR9] [minor] serial collision on branches -> rejected single writer on trunk; no parallel allocation exists
- [D00-T04-S6-PR10] [major] semantic version binding missing -> filed D00 T04 §9
- [D00-T04-S6-PR11] [minor] mandatory action plus owner -> duplicate D00-T01-S51-PR5
- [D00-T04-S6-PR12] [minor] remediated end-event plus recurrence -> rejected filing names the event; line-touching regressions void via stale-evidence; line-silent world state is outside byte-attestation by design
- [D00-T04-S6-PR13] [minor] fail-closed evidence faults -> rejected only `cover` covers; every other code is void by predicate contract, git-unresolvable loudly
- [D00-T04-S6-PR14] [minor] monotonic and non-future dates -> rejected post-dated grants nothing and escalates loud; successor date order is cosmetic metadata
- [D00-T04-S6-PR15] [minor] reviewer identity plus signatures -> rejected decided by the trust model with stated registry cost
- [D00-T04-S6-PR16] [minor] §8 and §13 XREFs to §6 -> rejected no direct consumption; the §11 edge carries the transitive need
- [D00-T04-S6-PR17] [minor] per-path order fixtures -> rejected repaired append serves accepted plus deferred jointly; item 5 subsumes consumers
- [D00-T04-S6-PR18] [minor] checkpoint must name residuals -> rejected residuals ride filings plus the stamp Review line; §51 precedent
End of ledger

Raw plan-review output (run 20260920-D00-T04-S6-gpt-r3, artifact `11a1adf7c8e23c7d79a89bd1af9c0b65ecaec20bf7e396fbeef7d5f4cbdd15a5`):

```
- D00 T04 §6 item 2 and D00 T01 §51 item 2 must distinguish the reviewed-state commit from the later evidence-record commit; a descendant is not visible from its ancestor, so the quoted Git reachability proof is factually reversed and does not establish candidate visibility.
- D00 T04 §6 item 2 should define whether evidence authenticates only the peeled commit or also an annotated tag’s metadata and signature; peeling discards attestation information, so the current construction does not settle tag-object identity.
- D00 T04 §6 item 3 should specify how stamp history is checked for working-tree, staged, committed, merge, and rewritten cases; comparison merely “against HEAD” may detect an uncommitted strip but miss one already committed.
- D00 T04 §7 should block §6 sign-off or §6 should explicitly declare the residual; §6 claims generation handling is closed while §7 documents parser-refused stamps that the generation map still partially accepts, weakening diagnostic trust.
- D00 T04 §7 needs one shared stamp parser rather than duplicated “word-for-word” refusal behavior; separately maintained parsers will drift again and make future correctness depend on synchronized implementations.
- D00 T04 §6 item 5 should use a non-tuple, keyword-only immutable result type instead of `NamedTuple`; `NamedTuple` still permits positional construction, unpacking, and indexing, so a source scan does not eliminate the class of swap defect being closed.
- D00 T04 §6 item 5 needs contract tests at every serialization and public-consumer boundary, not only the 14 currently found consumers; reflection, indexing, aliases, and future consumers can bypass the no-positional source scan.
- D00 T01 §51 item 6 should enforce an acyclic, single-successor supersession chain with exactly one live leaf per target; same-target validation alone permits forks, cycles, or competing terminal decisions.
- D00 T01 §51 item 6 should replace file-scoped serial IDs with collision-resistant stable IDs or define merge allocation rules; parallel branches can independently create the same `A<n>` and corrupt later supersession.
- D00 T01 §51 items 3 and 4 need a policy/schema-version binding; target-only digests leave an old acceptance valid after parser, outcome-matrix, coverage, or normalization semantics materially change.
- D00 T04 §6 item 4 and D00 T01 §51 item 5 should make follow-up action, owner, and due/trigger mandatory where an outcome creates further work; an optional free-text action cannot prove that `renewed` or `remediated` obligations are owned.
- D00 T04 §6 item 4 should define the exact event that ends `remediated` coverage and how recurrence is detected; “continues until the row files” is ambiguous and can let a waiver conceal a regression indefinitely.
- D00 T01 §51 items 1 and 2 should state fail-closed behavior for missing, shallow, pruned, or corrupt Git evidence; preserving a distinct `git-unresolvable` fault is insufficient unless it is explicit that unresolved evidence grants no coverage.
- D00 T01 §51 should validate monotonic and non-future record dates in addition to ancestry; ancestry resolves same-day order but does not prevent misleading future dates or backward-dated successor records.
- D00 T01 §51 and D00 T04 §6 need reviewer identity and authorization provenance, ideally verified commit or tag signatures; immutable evidence proves content history but not that an authorized reviewer approved the waiver.
- D00 T02 §8 and §13 need explicit cross-references naming which §6 contract they consume; appearing only in the dependent list leaves their integration obligation and required regression coverage undiscoverable.
- D00 T04 §6 item 1 needs fixtures for every outcome and severity path, including accepted, deferred, superseded, missing-code, and nonempty-code cases; one accepted-critical example does not prove the repaired field order across all named consumers.
- D00 T04 §6’s checkpoint should include the known refusal-parity residual and supersession invariants, or link them as exclusions; its current completion claim can pass while audit output remains contradictory or the acceptance chain is structurally ambiguous.
```

## Live proof

`py scripts/todo-graph.py self-test` (exit 0, 1267 cases):

todo-graph self-test: 1267 cases, 0 failed

Provenance: candidate 62e23418c087f287f150acad864d8948e929aec7; command `scripts/todo-graph.py self-test`; exit 0; tool CPython 3.14.3; digest cdb2154b23b8aec47fcb6083de606610b74313491ec5defca73b79e7e4f8308a; path todo/00-workspace/TODO-04-panel-rule-follow-ups.md; run 20260920-D00-T04-S6-gpt-r3

`py scripts/todo-graph.py validate` (exit 0):

28 todos, 257 sections -- 0 fatal, 0 warning(s)

Provenance: candidate 62e23418c087f287f150acad864d8948e929aec7; command `scripts/todo-graph.py validate`; exit 0; tool CPython 3.14.3; digest fef15e23303805458c38cd318027e9a71fbd0f797f202043fc968096033e56f1; path todo/00-workspace/TODO-04-panel-rule-follow-ups.md; run 20260920-D00-T04-S6-gpt-r3

`py scripts/todo-graph.py query risk-register --check` (exit 0):

risk register is current -- 1 entries

Provenance: candidate 62e23418c087f287f150acad864d8948e929aec7; command `scripts/todo-graph.py query risk-register --check`; exit 0; tool CPython 3.14.3; digest b6102c2466a3c3406d9d2bf0eca86c64e0d8487c230bf78aaed6684dcf0ae1cc; path todo/00-workspace/TODO-04-panel-rule-follow-ups.md; run 20260920-D00-T04-S6-gpt-r3

`py scripts/todo-graph.py plan --check` (exit 0):

implementation plan is current -- 93/252 sections complete (37%)

Provenance: candidate 62e23418c087f287f150acad864d8948e929aec7; command `scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.14.3; digest cb27417909d63685bdd34520e22919fd6e6bc6cec5eab838a600d5a5694e4aa1; path todo/00-workspace/TODO-04-panel-rule-follow-ups.md; run 20260920-D00-T04-S6-gpt-r3
