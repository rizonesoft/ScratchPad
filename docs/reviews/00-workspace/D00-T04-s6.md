# Review: D00 T04 §6 -- Acceptance-Record Sign-Off Residuals

Tier: Full (the diff touches shared contract surface: `scripts/todo-graph.py`, `scripts/todo-validate.py`, the review skill, and `todo/README.md`). Candidates: `2ccde87` (ship), `0f10935` (self-review fix), `4ec826c` (fix-loop 1).

## Self-review

Q1 (exact outcome, no widening): item 1 maps to the accepted/deferred critical append order (`failure_code` before `accepted_outcome`) plus the §23 renewed-chain pin; item 2 maps to the peel-identity re-rejection recorded in the `evidence_lineage` docstring, the validator foreign-evidence leg comment, the skill evidence sentence, and the README foreign-evidence row, plus the tag-proceeds pin; item 3 maps to `stamp_generations_by_section`, the validator `stamp-generation-regressed` leg, the SEVERITY_MAP entry plus README row, and the §§112-113 pins; item 4 maps to `OUTCOME_MATRIX`, the `terminal-outcome` leg with its `acceptance_hold` outcome parameter at all three call sites, the skill matrix sentence plus cause-list entry, the README matrix, and the row-for-row plus void pins; item 5 maps to `CriticalFinding`/`MajorFinding`, the three keyword-constructed producers, the fourteen attribute-routed consumers, and the seven contract pins. Item 6 matches the commit subject verbatim. No widening: the README severity-table row and the skill cause-list entry are each forced by the shipped rules.

Q2 (logic): the health-result field order preserves the old positional layout exactly, so the default tuple sort reproduces the removed explicit index keys element for element; every producer constructs with keywords and every consumer reads attributes (verified by search: no positional append, unpack, or index remains); the terminal-outcome leg reads the record outcome at all three hold call sites; the generation map reads coverage off the stamp cover field through COVER_ITEM_RE exactly like the parser. One finding before the lenses: the generation map first scanned the whole stamp body for `§N` mentions (missing sigil-less and spaced ranges, reading evidence prose as coverage); fixed with the ranged-cover pin in `0f10935`.

Q3 (blast radius): the tuple-to-named-type refactor is behavior-preserving (sort order identical, JSON keys unchanged, index reads replaced one for one); the terminal-outcome leg changes coverage only for matched rejected/closed records (no live record carries those outcomes; the §23 rejected chain is fixture-only); the generation leg fires only on uncommitted regressions against HEAD (validate green on the live tree); the remediated matrix cell follows established §22 covering behavior. Live gates green at ship (self-test 1263/1263, validate 0 fatal 0 warnings, plan-health gate ok, plan current).

## GPT panel (round 1)

- `adversarial` needs-attention (1)
- `consistency` needs-attention (1)
- `integration` needs-attention (1)
- `record` needs-attention (1)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Fix record: R1-F1 (pre-match terminal check lets an unrelated rejected/closed record report `terminal-outcome` for another target) and R1-F3 (same root at the covering loops, poisoning the reported cause and fix) fixed by moving the outcome check after the match in `acceptance_hold` with the docstring order corrected: unmatched terminal records read `no-match`, matched terminal records void before the remaining legs. The two `voids before match` unit pins became four matched/unmatched pins. R1-F2 (the matrix says `remediated` terminates while the code lets it cover) and R1-F4 (same contradiction plus the S22 check mislabeled `renewed`) fixed by correcting the matrix cell to `continues` in code, README, and skill (established §22 covering behavior stands; the debt being gone obligates filing, not renewing) and renaming the check to `the remediated marker waiver covers and reports its outcome`. No live record carries a `remediated` outcome, so established behavior is fixture-pinned only. Fixed in the fix-loop 1 commit; gates re-ran green before round 2 (self-test 1266/1266, validate 0 fatal 0 warnings).

Raw round-1 output (run 20260920-D00-T04-S6-gpt, artifact `94f66c1317cfef44c060eb1d832a8b91cd4abe23bd0fd8a16f5f01051b8a7d15`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1729-1732` evaluates `rejected`/`closed` before verifying `match`, so an unrelated terminal record returns `terminal-outcome` instead of `no-match` and can poison another target’s noncoverage result.

**consistency: needs-attention** (1)
1. `todo/README.md:441`, `.claude/skills/review-todo-section/SKILL.md:190`, and `scripts/todo-graph.py:1774-1783` declare `remediated` coverage terminated and the waiver inert, but `scripts/todo-graph.py:1729-1749` lets `remediated` return `cover`.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:4281-4334` and `scripts/todo-graph.py:4546-4581` consult every file-scoped acceptance, so the pre-match terminal check at `scripts/todo-graph.py:1729-1732` makes an unrelated rejected or closed acceptance supply another run, outage, or finding’s reported cause and remediation.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:139` claims fixtures pin every outcome cell, but `scripts/todo-graph.py:16507-16513` labels a `remediated` case “renewed” and asserts that it still covers, directly contradicting the recorded inert-waiver semantics.
```

## GPT panel (round 2)

- `adversarial` needs-attention (2)
- `consistency` needs-attention (1)
- `integration` needs-attention (1)
- `record` needs-attention (1)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Fix record: R1-F1 through R1-F4 confirmed fixed (not re-reported). R2-F2 (unbounded range expansion in `stamp_generations_by_section`: a crafted `§1-999999999` cover stalls validation) fixed by mirroring the parser's `MAX_STAMP_COVERAGE` cap: over-wide stamps skip whole, with a no-stall pin. R2-F1 (terminal voids before the remaining legs) rejected: voiding is fail-closed (a void record never un-covers another record's cover), and the cause names the dispositive fact (a stale rejected record reporting `stale-generation` would misdirect remediation toward renewal, which the review already foreclosed). R2-F3 (section note vs order pins) rejected as a contradiction: the §23 pin proves voiding with perfect legs and the matched unit pins prove voiding precedes the remaining legs, so both hold at once; the section note now says so explicitly. R2-F4 and R2-F5 (the risk register reads rejected/closed heads `live` and omits their outcome) rejected: register state is contractually wall-clock by design (the register header plus schema risk-register/1: instruments with residual severity and wall-clock state, never coverage), no consumer gates on register state, and coverage reads live in plan-health; outcome visibility in the register is new scope, not a defect. Fixed in the fix-loop 2 commit; gates re-ran green before round 3 (self-test 1267/1267, validate 0 fatal 0 warnings).

Raw round-2 output (run 20260920-D00-T04-S6-gpt-r2, artifact `1c2e61a58c6751c1c7fc0c0e728cc175686b0ca89c88a091cc4a22ea4b8b1d3e`):

```
**adversarial: needs-attention** (2)
1. `scripts/todo-graph.py:1733-1753` returns `terminal-outcome` before checking generation, dates, ancestry, resolvability, or freshness, so a stale, future-dated, foreign, or unresolvable rejected/closed record can terminate coverage without valid evidence.
2. `scripts/todo-graph.py:2174-2183` expands ranges without enforcing `MAX_STAMP_COVERAGE`; a malformed range up to 999999999 sections can exhaust memory or stall validation.

**consistency: needs-attention** (1)
1. `todo/00-workspace/TODO-04-panel-rule-follow-ups.md:139` says terminal outcomes void with perfect legs, but `scripts/todo-graph.py:17252-17259` deliberately supplies a missing target day and asserts that rejected/closed bypass the remaining legs.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:5467-5501` discards `_outcome`, labels an unexpired rejected/closed head `live`, and omits its terminal outcome from the risk-register entry, contradicting the new coverage semantics for downstream consumers.

**record: needs-attention** (1)
1. `scripts/todo-graph.py:5488-5501` records rejected/closed instruments as `live` without their outcome, leaving the generated acceptance register unable to distinguish terminating decisions from covering waivers.
```
