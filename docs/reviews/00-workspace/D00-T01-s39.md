# Review: D00 T01 §39 -- Panel Telemetry

Candidate: `b51982a` plus `dc6e819` (SHIP plus the self-review fix). Full panel (shared tooling plus skill): Sol rounds 1-2, Opus round-3 sign-off. First live user of the telemetry shape (item 1).

Arch trigger: none - tooling plus skill prose; no editor, storage, extensibility, AI, protocol, or consent surface changes.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. One fix, committed in `dc6e819`, re-asked clean.

Does it make sense: each item maps to its code (telemetry shape plus captures in the skill, totals plus outages plus summary in `query telemetry`, fixtures pinning all three), and the review itself records the first live lines. Exactly the contract.

Is it logical: the first pass found one gap, fixed in `dc6e819`: the aggregate loop counted round/telemetry mismatches but neither the tree text nor the tree JSON printed them. Considered and kept: unanchored outage matching (validator parity beats a second definition), `unknown` tokens beside `run` (see the capture note below). Corrected at R1: outcomes must equal the round's worst transcribed verdict or the line counts malformed (the R1-F1 fix; an earlier draft of this note claimed verbatim buckets, which the fix reversed). Re-asked clean.

Did it break anything else: the change is additive (new query branch, new schema const, widened gates); full self-test green at 1071 then 1072, `validate` clean, `run` and all other queries untouched.

Capture note: both families record `tokens unknown` beside `run`: the wrapper swallows producer stderr (verified 0-byte capture on R1), and the pinned `-p` text rung yields no usage readout (verbose stream-json would break the verdict checker). The codex tail and claude usage shapes in the skill are measurable on unwrapped runs only, until the T04 receipts-bind-usage item lands. Durations measure the run-command wall clock, which overstates the producer by the wrapper's seconds.

## GPT panel (round 1)

- `adversarial` needs-attention: (R1-F1) telemetry outcome unvalidated against verdicts.
- `consistency` needs-attention: (R1-F2) panel context never closes.
- `integration` needs-attention: (R1-F3) per-section totals missing; (R1-F4) affected runs over-attributed.
- `record` needs-attention: (R1-F5) live-review item unticked.

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 89s; outcome needs-attention; tokens unknown

Disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 5d3e659 |
| R1-F2 | fixed | 5d3e659 |
| R1-F3 | fixed | 5d3e659 |
| R1-F4 | fixed | 5d3e659 |
| R1-F5 | fixed | R1 telemetry transcribed here; item 1 ticked |

Fix record: telemetry outcome must equal the round's worst transcribed verdict or the line counts malformed; panel context closes at same-or-higher headings (deeper notes stay); the per-section view gains totals in text plus JSON; outages attribute their own round's runs with file runs only when file-level. R1-F5's remedy is this transcription: the review carries live lines, so item 1 ticks. Fixed in `5d3e659` (self-test 1080/1080, validate 0 fatal); gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `0ec75f293d38400221fccbc80efd965ab60d71f3ebd9e96abe0e77d486ba13e0`; run `20260919-D00-T01-S39-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:2411` accepts any declared outcome without validating it against the round’s verdicts, allowing an `approve` outcome to conceal `needs-attention` and corrupt telemetry totals.
**consistency: needs-attention** (1)
1. `scripts/todo-graph.py:2383` opens a panel context but never closes it at a subsequent same-or-higher-level heading, so telemetry, verdicts, and dispositions outside the panel section can be attributed to that round.
**integration: needs-attention** (2)
1. `scripts/todo-graph.py:2857` provides detailed per-section rounds but no per-section round count, token sum, or family totals, leaving the contract’s per-section aggregation unimplemented.
2. `scripts/todo-graph.py:2439` extracts every run ID in the findings file and reports them all as affected by each outage, so reviews containing pre-outage or otherwise unrelated runs produce an incorrect affected-run list.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1047` leaves the required live-review telemetry deliverable unchecked while later implementation and commit items are marked complete, so the section contract is not yet satisfied.
```

## GPT panel (round 2)

- `adversarial` needs-attention: (R2-F1) outcome unvalidated with zero verdicts.
- `consistency` needs-attention: (R2-F2) level-1 panel headings accepted.
- `integration` approve
- `record` needs-attention: (R2-F3) live-review item still unticked in the candidate.

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 88s; outcome needs-attention; tokens unknown

Disposition table (R1-F1 through R1-F5 stay fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 5d3e659 |
| R1-F2 | fixed | 5d3e659 |
| R1-F3 | fixed | 5d3e659 |
| R1-F4 | fixed | 5d3e659 |
| R1-F5 | fixed | R1 telemetry transcribed; item 1 ticked |
| R2-F1 | fixed | 3084869 |
| R2-F2 | fixed | 3084869 |
| R2-F3 | fixed | record commit below |

Fix record: telemetry without recognized verdicts counts malformed (no checkable outcome); panel headings match the validator exactly (level-2-plus, case-insensitive). R2-F3's remedy is this record commit: the R1 transcription plus the item-1 tick ride it, so the R3 candidate carries both. Fixed in `3084869` (self-test 1083/1083, validate 0 fatal); gates re-ran green before round 3.

Raw round-2 output (fenced, not part of the verdict scan; digest `14a248f3150d538405bf6f6bfefc22a24aa0b2f60d468ae3f27391e66200e9fd`; run `20260919-D00-T01-S39-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:2462` validates the declared outcome only when at least one verdict was recognized, so a panel with zero recognizable verdicts can still contribute an arbitrary outcome to telemetry totals.
**consistency: needs-attention** (1)
1. `scripts/todo-graph.py:1615` accepts level-1 panel headings even though the skill requires level-2 or deeper headings, allowing the query to recognize telemetry from a panel section rejected by the validator.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1049` still leaves the required live-review telemetry deliverable unchecked, so the section contract remains incomplete.
```

## Opus panel (round 3)

- `adversarial` needs-attention: (R3-F1) repeated runs unattributed; (R3-F2) near-miss lines silent.
- `consistency` needs-attention: (R3-F3) fallback round collision; (R3-F4) stale annotations.
- `integration` needs-attention: (R3-F5) AGENTS query list missing telemetry.
- `record` needs-attention: (R3-F6) self-review contradicts shipped outcome rule.

Telemetry: round 3; model opus; effort medium; duration 86s; outcome needs-attention; tokens unknown

Disposition table (R1-F1 through R2-F3 stay fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 5d3e659 |
| R1-F2 | fixed | 5d3e659 |
| R1-F3 | fixed | 5d3e659 |
| R1-F4 | fixed | 5d3e659 |
| R1-F5 | fixed | R1 telemetry transcribed; item 1 ticked |
| R2-F1 | fixed | 3084869 |
| R2-F2 | fixed | 3084869 |
| R2-F3 | fixed | 6156205 |
| R3-F1 | fixed | 8e2263b |
| R3-F2 | fixed | 8e2263b |
| R3-F3 | fixed | 8e2263b |
| R3-F4 | fixed | 8e2263b |
| R3-F5 | fixed | 8e2263b |
| R3-F6 | fixed | record commit below |

Fix record: repeated run IDs still attribute to their round (dedup gates the file list only); near-miss Telemetry lines count malformed without parsing; fallback round numbers skip taken numbers; the outage-list annotations match the built shape; the AGENTS query list gains the telemetry row; the self-review now states the shipped outcome rule. Fixed in `8e2263b` plus this record (self-test 1086/1086, validate 0 fatal); gates re-ran green before round 4.

Raw round-3 output (fenced, not part of the verdict scan; digest `24cb4a838b14ec714efdc24b214df35415a62db8a59aedce75762d9e01fe9623`; run `20260919-D00-T01-S39-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (2)
1. `scripts/todo-graph.py:2394` gates round attribution behind the file-wide dedup (`_rid not in file_runs`), so a run ID mentioned anywhere earlier in the file is never added to `round_runs` when it reappears under its panel section; probed: a record naming `20260920-D90-T09-S1-gpt` in prose and again under `## GPT panel (round 2)` yields `round_runs {}`, so the outage's affected-run list prints `none recorded` for a round that names its run.
2. `scripts/todo-graph.py:2418` recognizes `Telemetry:` only at column 0 with no Markdown marker, and a line that fails that prefix is neither parsed nor appended to `malformed`; probed: `  Telemetry: round 1; ...` and `- Telemetry: round 1; ...` under a panel round give `telemetry None, malformed []`, so the round reads as "no telemetry line" with a zero malformed count, contradicting the docstring at `scripts/todo-graph.py:2378` ("a malformed line counts, never parses and never fails") and the marker rule the verdict lines use at `scripts/todo-graph.py:1622`.

**consistency: needs-attention** (2)
1. `scripts/todo-graph.py:2406` falls back to the panel-section ordinal when a heading carries no round number, and that ordinal can collide with an explicitly numbered round; probed: `## GPT panel (round 2)` followed by `## Opus panel` produces two rounds both keyed `n=2`, merging their runs into `round_runs {2: [aaa, bbb]}` so an outage under either round reports both runs (the R1-F4 over-attribution defect in a second shape).
2. `scripts/todo-graph.py:2842` and `scripts/todo-graph.py:2843` annotate `sol_files`/`opus_files` as `list[tuple[str, list[str], list[str]]]` while the appends at `scripts/todo-graph.py:2866` and `scripts/todo-graph.py:2868` build 2-tuples of `(path, list[tuple[str, list[str]]])`; the annotation describes a shape the code never constructs.

**integration: needs-attention** (1)
1. `AGENTS.md:57-65` lists every operator query (`ready`, `blocked`, `stats`, `plan-health`, `summary`, `run`, `risk-register`, `dashboard`, `notify`) but the diff adds `query telemetry` only to `todo/README.md:347`, leaving the shipped command absent from the Validation block that AGENTS.md presents as the query surface; the AGENTS.md hunk touches that same block (self-test count 1045 -> 1083) without adding the row.

**record: needs-attention** (1)
1. `docs/reviews/00-workspace/D00-T01-s39.md:13` still records "Considered and kept: verbatim outcome buckets (a typo'd outcome reports itself in the totals rather than dropping the line)", which the R1-F1 fix reversed: `scripts/todo-graph.py:2477` now maps an unrecognized outcome to `-1`, mismatches the worst verdict, and drops the line to `malformed`; the self-review states the opposite of the shipped behavior.
```

## Opus panel (round 4)

- `adversarial` needs-attention: (R4-F1) fallback collision in the forward direction.
- `consistency` needs-attention: (R4-F2) bare-line rule unstated in the skill.
- `integration` needs-attention: (R4-F3) file count reported as sections.
- `record` approve

Telemetry: round 4; model opus; effort medium; duration 110s; outcome needs-attention; tokens unknown

Disposition table (R1-F1 through R3-F6 stay fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 5d3e659 |
| R1-F2 | fixed | 5d3e659 |
| R1-F3 | fixed | 5d3e659 |
| R1-F4 | fixed | 5d3e659 |
| R1-F5 | fixed | R1 telemetry transcribed; item 1 ticked |
| R2-F1 | fixed | 3084869 |
| R2-F2 | fixed | 3084869 |
| R2-F3 | fixed | 6156205 |
| R3-F1 | fixed | 8e2263b |
| R3-F2 | fixed | 8e2263b |
| R3-F3 | fixed | 8e2263b |
| R3-F4 | fixed | 8e2263b |
| R3-F5 | fixed | 8e2263b |
| R3-F6 | fixed | 695e2d4 |
| R4-F1 | fixed | 8c07c19 |
| R4-F2 | fixed | 8c07c19 |
| R4-F3 | fixed | 8c07c19 |

Fix record: fallback round numbers pre-scan explicit numbers and dodge them in both directions; the skill pins the telemetry line bare at column 0 with the malformed consequence stated; the no-telemetry count reports reviews in text plus JSON. Fixed in `8c07c19` (self-test 1088/1088, validate 0 fatal); gates re-ran green before round 5.

Raw round-4 output (fenced, not part of the verdict scan; digest `10bae3875785630eb8a6cb96a77738a6b063c423eae6463d72ad239b57d41eae`; run `20260919-D00-T01-S39-opus-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:2410` still collides fallback round numbers in the forward direction: `used` holds only the rounds seen so far, so a later explicitly numbered heading can reuse an earlier fallback ordinal; probed on the shipped file: `## GPT panel` (fallback n=1) followed by `## Opus panel (round 1)` yields `[1, 1]` and `round_runs {1: ['20260920-D90-T09-S1-gpt', '20260920-D90-T09-S1-opus']}`, so `Sol outage:` under either round reports both runs. This is R3-F3's over-attribution family, half fixed (the backward direction only).

**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:124` pins the telemetry line as `` `Telemetry: round <n>; ...` `` without stating that it must be bare at column 0, while the same paragraph requires every verdict line to open with a Markdown marker (`*`, backtick, `>`, `-`); `scripts/todo-graph.py:1619` (`TELEMETRY_NEAR_RE`) counts a marked or indented `- Telemetry: ...` as malformed and drops the round's telemetry. A record author following the marker rule the paragraph does state produces a silently unparsed round.

**integration: needs-attention** (1)
1. `scripts/todo-graph.py:2866` increments `no_tel` once per findings file (the `files` dict dedups by path at `scripts/todo-graph.py:2765`), but it is reported as a section count at `scripts/todo-graph.py:2907` ("sections without telemetry") and `scripts/todo-graph.py:3056` (`"sections_without_telemetry"`), in the same output block that correctly labels the outage counts "reviews"; a findings file cited by two stamps (`docs/reviews/01-notepad-core/D01-T01-s3.md`, cited twice in `todo/`) counts once while representing two sections.

**record: approve**
```

## Opus panel (round 5)

- `adversarial` needs-attention: (R5-F1) near-miss shapes beyond the marker class drop silently.
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 5; model opus; effort medium; duration 36s; outcome needs-attention; tokens unknown

Round-5 bar disposition: R5-F1 is below bar (diagnostic-counter gap on exotic author-error shapes; primary totals never fabricate, and the round detail shows "no telemetry line" for diagnosis), so it files at D00 T04 §1 and the stamp proceeds. No blocking leftovers; the run continues.

Final disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 5d3e659 |
| R1-F2 | fixed | 5d3e659 |
| R1-F3 | fixed | 5d3e659 |
| R1-F4 | fixed | 5d3e659 |
| R1-F5 | fixed | R1 telemetry transcribed; item 1 ticked |
| R2-F1 | fixed | 3084869 |
| R2-F2 | fixed | 3084869 |
| R2-F3 | fixed | 6156205 |
| R3-F1 | fixed | 8e2263b |
| R3-F2 | fixed | 8e2263b |
| R3-F3 | fixed | 8e2263b |
| R3-F4 | fixed | 8e2263b |
| R3-F5 | fixed | 8e2263b |
| R3-F6 | fixed | 695e2d4 |
| R4-F1 | fixed | 8c07c19 |
| R4-F2 | fixed | 8c07c19 |
| R4-F3 | fixed | 8c07c19 |
| R5-F1 | filed | D00 T04 §1 |

Architecture gate: not triggered (`Arch trigger: none`, see header).

Raw round-5 output (fenced, not part of the verdict scan; digest `1f95f90656825683544b5841fe2fddb630dbf802062e26b8b54a591e3eb7646e`; run `20260919-D00-T01-S39-opus-r3` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1619` (`TELEMETRY_NEAR_RE = r"^\s{0,3}[*>\-]?\s*Telemetry\s*:"`) omits the backtick from its marker class, so a backtick-wrapped telemetry line is neither matched by the `ln.startswith("Telemetry:")` branch nor by the near-miss branch at `scripts/todo-graph.py:2460`: it is silently dropped with `telemetry None, malformed 0`, the exact failure R3-F2/R4-F2 were meant to close, in a third shape. Probed shape: `` `Telemetry: round 1; model m; effort e; duration 1s; outcome approve; tokens 1` `` under `## GPT panel (round 1)` parses as "no telemetry line" with a zero malformed count. This is the shape the skill itself displays: `.claude/skills/review-todo-section/SKILL.md:124` pins the line inside a code span (`` `Telemetry: round <n>; model <name>; ...` ``) while the verdict-marker set the same paragraph teaches includes the backtick, so an author copying the pinned shape verbatim produces an unparsed, uncounted round. Same family, same regex: `+ Telemetry: ...`, `1. Telemetry: ...`, and nested `> - Telemetry: ...` also fall through both branches silently. The docstring at `scripts/todo-graph.py:2380` ("a malformed line counts, never parses and never fails") remains false for all four shapes.

**consistency: approve**

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §39 plus §35 plus §37 plus D00 T04 §1, 2026-09-19. Neighbors: §35 (filed-from), §37 (XREF). Dependents: D00 T04 §1 by the mechanical rule (recorded in the run's dependents file; carries this review's joiners, mirroring §55 in the §37 run). Runner note: positional run-kind `run` beside `panel` behaves exactly like §37 (Sol prose capture unreachable beside `run`); no new CLI drift. Output `check-plan PASS` (18 findings, one per line). Run note: the plan run shares its base with the panel runs (`20260919-D00-T01-S39-gpt`); kind distinguishes them and the minter is authoritative. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: entry section D00 T01 §39; neighbors D00 T01 §35, D00 T01 §37; dependents D00 T04 §1; sections [D00 T01 §39, D00 T01 §35, D00 T01 §37, D00 T04 §1]; dependents [D00 T04 §1]; prompt includes all section bodies: True; bytes 25033; prompt run ID `20260919-D00-T01-S39-gpt`.

Ledger:

| ID | Severity | Disposition | Evidence |
| D00-T01-S39-PR1 | minor | rejected | receipt integration is review-run tooling, homed at D00 T04 §1 |
| D00-T01-S39-PR2 | minor | rejected | overtaken: R1 through R5 all carry live lines (Done note touched) |
| D00-T01-S39-PR3 | minor | duplicate | D00-T01-S36-PR2 |
| D00-T01-S39-PR4 | minor | rejected | one review holds one panel sequence; run-ID keying solves nothing real |
| D00-T01-S39-PR5 | minor | rejected | premature: distributions over one review are noise |
| D00-T01-S39-PR6 | minor | rejected | outcome is the worst lens verdict; transport and process are separate legs |
| D00-T01-S39-PR7 | minor | filed | D00 T04 §1 (receipts item) |
| D00-T01-S39-PR8 | minor | rejected | unimplementable today: the pinned claude rung reports no resolved version |
| D00-T01-S39-PR9 | minor | rejected | durations exist only for completed rounds; nothing undefined occurs |
| D00-T01-S39-PR10 | minor | filed | D00 T04 §1 |
| D00-T01-S39-PR11 | minor | filed | D00 T04 §1 (merged with PR10) |
| D00-T01-S39-PR12 | minor | rejected | bindings exist except review kind, which has no consumer |
| D00-T01-S39-PR13 | minor | rejected | the query reports measured/unknown/denominator; a hardcoded expected would rot |
| D00-T01-S39-PR14 | minor | rejected | premature: countable shapes land with their filed items |
| D00-T01-S39-PR15 | minor | rejected | already satisfied: 43 telemetry fixtures pin the aggregation |
| D00-T01-S39-PR16 | minor | duplicate | D00-T01-S37-PR21 |
| D00-T01-S39-PR17 | minor | filed | D00 T04 §1 |
| D00-T01-S39-PR18 | minor | rejected | premature and externally-dependent: usage reads unknown, no rate card owned |

## Per-finding triage (D00 T01 §39)

- `D00-T01-S39-PR1` minor rejected: the section claims measurement produces no receipts and hands integration to future work, so the dependency only delays value rather than defining it.
- `D00-T01-S39-PR2` minor rejected: the live-proof Done covers R1 while later rounds are future work, so the automation claim is unproven and the section should stay open or soften the text.
- `D00-T01-S39-PR3` minor duplicate D00-T01-S36-PR2: no stamp line is present, so the prior duplicate answer stands.
- `D00-T01-S39-PR4` minor rejected: summaries print per-review totals without a run-ID key, which hides resumed runs sharing a review identity.
- `D00-T01-S39-PR5` minor rejected: effort and duration are single values with no distributions for tuning.
- `D00-T01-S39-PR6` minor rejected: the outcome taxonomy conflates round result, transport state, degraded process, and review disposition.
- `D00-T01-S39-PR7` minor filed D00 T04 §1: token dimensions are unnormalized across providers, so sums may blend unlike quantities.
- `D00-T01-S39-PR8` minor rejected: model identity is an unpinned label, letting two generations aggregate under one name.
- `D00-T01-S39-PR9` minor rejected: duration boundaries are undefined and retries, queueing, and resumed runs are unhandled.
- `D00-T01-S39-PR10` minor filed D00 T04 §1: the telemetry schema has no validator enforcement.
- `D00-T01-S39-PR11` minor filed D00 T04 §1 (merged with PR10): schema validation is incomplete, so future view claims rest on unchecked history.
- `D00-T01-S39-PR12` minor rejected: receipt bindings are unowned with no named owners for candidate, round, disposition, or tier.
- `D00-T01-S39-PR13` minor rejected: coverage lacks a denominator, so measured-only totals hide how much telemetry is missing.
- `D00-T01-S39-PR14` minor rejected: the outage taxonomy is incomplete across Sol, GPT-fallback, both-family, and partial-model stops.
- `D00-T01-S39-PR15` minor rejected: the aggregation Done only requires run-and-quote, so successful execution does not prove correct accounting.
- `D00-T01-S39-PR16` minor duplicate D00-T01-S37-PR21: the T04 test checkpoint covers only the §37 items, so later joiners have no falsifiable acceptance.
- `D00-T01-S39-PR17` minor filed D00 T04 §1: the T04 commit title still names the §37 review after the §38 and §39 joins.
- `D00-T01-S39-PR18` minor rejected: no cost-control view exists for duration and tokens per generation.

Raw plan-review output (fenced, not part of the verdict scan; run `20260919-D00-T01-S39-gpt` via self-hosted `review-run`, kind `run`-adjacent capture; plan runs ledger no receipt, mirroring §37):

```
- S39’s completed runner-capture claim conflicts with D00 T04 §1’s finding that producer stderr is swallowed and Sol usage is unreachable beside `run`; the receipt integration belongs in S39’s acceptance boundary because telemetry cannot be declared automated while token legs remain unknown.
- S39 says one live review proves the telemetry shape, but its Done note records only a manually transcribed R1 and promises that later rounds “follow”; S39 should require a complete multi-round live review populated automatically because partial prospective evidence does not satisfy the stated outcome.
- S39 has every checklist item ticked but no `Verified:` stamp or review record; these belong after its checkpoint because the repository lifecycle forbids treating an unstamped section as verified completion.
- The “per-run panel summary” is corrected into a per-section view without changing its requirement; S39 should key summaries by stable run ID and optionally aggregate them by section because a section can have implementation review, plan review, reruns, and resumed runs that must not be conflated.
- Telemetry records effort, duration, and outcome, but the query acceptance criteria aggregate only rounds, tokens, and model families; S39’s query should expose distributions for every collected field because otherwise most of the new evidence cannot inform cap tuning.
- `outcome` is undefined between process status, transport failure, reviewer verdict, and final run disposition; S39’s fixed shape should carry separate enumerated fields because outages and `needs-attention` reviews otherwise become analytically indistinguishable.
- Codex’s single `tokens used` value and Claude’s structured input/output/cache usage are not directly comparable; S39 should define normalized token dimensions and unknown handling before tree-wide sums because a blended total can misstate relative cost.
- The telemetry shape lacks a stable model snapshot/provider identity and records only a family-like model name; S39 should retain the exact runner and resolved model identifier because aliases can drift and destroy longitudinal comparability.
- Duration has no specified clock boundary or treatment of queueing, retries, timeouts, resumed runs, and outages; S39 should define monotonic start/end semantics because inconsistent timing cannot support reliable cap or performance decisions.
- The query parses free-form `Telemetry:` prose but S39 provides no validation for missing, duplicate, malformed, negative, or out-of-range fields; enforcement belongs with the schema in S39 or D00 T04 §1 because silent omission makes apparently precise totals incomplete.
- D00 T04 §1 widens only a few malformed-line prefixes, leaving malformed canonical lines and duplicate round telemetry unowned; its telemetry-validation item should cover complete schema validation because formatting-specific patches will continue missing bad records.
- S39 does not define how telemetry binds to candidate hashes, round IDs, panel tier, review kind, and disposition rows; the receipt shape belongs in S39 because evidence must remain attributable when rounds are retried, resumed, or appended.
- Tree-wide totals do not report coverage across the 19-file/79-round legacy baseline or distinguish zero from unavailable usage; S39 should emit expected, measured, and unknown round counts because totals without a denominator invite false cost conclusions.
- The outage metric covers degraded all-Opus reviews but not partial Sol failures, recovered rounds, GPT sign-off fallback, architecture-gate fallback, or both-family stops; S39 and D00 T04 §1 should share an explicit outage taxonomy because the current count will understate degraded execution.
- S39’s aggregation Done condition only requires that the command run and its output be quoted; deterministic fixtures for family splits, token sums, ordering, duplicate rejection, unknown legs, and legacy records belong in S39 because successful execution does not prove correct accounting.
- D00 T04 §1’s checkpoint omits its architecture-outage, failure-injection, gate-shape validation, receipt-token, and telemetry-near-miss items; the checkpoint belongs to the whole section because several safety and telemetry deliverables could otherwise ship without named proof.
- D00 T04 §1’s commit title says it follows up only the §37 review while the section also owns §38 architecture-gate and §39 telemetry work; retitle it to describe all joined scope because commit provenance should make the mixed change discoverable.
- A premium cost-control view is unowned: S39 should add per-model cost estimates with a versioned rate card, trend deltas, and configurable budget warnings because raw token totals alone do not tell operators whether panel policy is becoming cheaper or more expensive.
```
