# D00 T02 S41 review - Sibling Sweep Second Residuals

Candidate: 7cda042 (Started plus implementation), f70aa49 (round-1 fixes), a13bb32 (round-2 fix), e7ad95a (sign-off R3-F1..F3 fixes, reviewed tip).
Tier: Full panel. The diff changes the app's background-birth sweep (`src/ScratchPad/MainWindow.xaml.cs`, `src/Notepad.Core/SiblingSelection.cs`), adds a startup seam (`src/Notepad.Core/TestHold.cs`, `src/ScratchPad/App.xaml.cs`), and changes shared UI-harness surface: the launch path and its diagnostics schema (`tests/UI/UiLaunch.cs`, `tests/UI/UiLaunchDiagnostics.cs`), the launch guard (`tests/UI/LaunchGuard.cs`), and the sweep proofs and recorder (`tests/UI/LaunchTests.cs`); consumers include every UI launch, the §18 guard contract, and the population fingerprint.

## Evidence map

- Item 1 (same-thread exclusivity): construction-specific provenance replaces the assumption. `SiblingSnapshot.Generation`, `SiblingSnapshotSlot.Claim`/`Claims`, the `other-construction` reason in `SiblingSelection.Decide`; fixture `InterleavedConstructionsWindowReadsAsTheOtherConstructions` (a planted interleaving reads the other construction's helper as `other-construction`, with and without a snapshot).
- Item 2 (lifecycle): the `ScratchPad.SiblingMark` property set at the snapshot (`SiblingSelection.Begin` marking form), `SiblingTopLevel.Mark`, reuse read as new, `SiblingSnapshotSlot.Release` for destroyed windows; fixture `ReusedHandleNeverReadsPreexistingAndClaimsReleaseWithTheWindow` (reuse, release, cancelled construction); overlapping completion stays pinned by `OverlappingConstructionsNeverTradeSnapshots`.
- Item 3 (ambiguous provenance): the `ambiguous` reason for unmarked snapshotted windows and unreadable windows; fixture `AmbiguousProvenanceIsSkippedAndReported`.
- Item 4 (late helpers): `SiblingSelection.DecideLate`, `SiblingPin.Late` enqueued at low priority after the pinned show, the `sweep-late` log line, the late-helper seam; fixtures `DelayedPassPinsLateHelpersOnlyWithoutAnOverlap` (unit) and `HelperBornAfterTheSweepIsParkedByTheDelayedPass` (UI: the planted popup at (100,100) reads back at the target in the `sweep-late` line of the same generation). Bound: helpers born after the delayed pass are runtime popups, owned separately.
- Item 5 (revalidation): `SiblingSelection.Revalidate` and `SiblingPin.PinDecided` (each selected window read again before its move); fixture `RevalidationSkipsReownedReusedAndGoneHandles`.
- Item 6 (observation barrier): `LocationRecorder.DrainToBarrier` (a synchronous WM_NULL to each app window, then a barrier message behind the pending events); both §34 proofs print `recorder drained N event(s) at the barrier` (the flyout proof ran focus-free: 0 events; the owned-dialog proof is Interactive, Night-owed under D00-T02-S34-N1 as before).
- Item 7 (threat model): the threat-model block in `tests/UI/LaunchGuard.cs`; P/Invoke detection widened to DllImport or LibraryImport in every spelling (four new planted bypasses); `ShapesOutsideTheStatedBoundaryAreNotClaimed` pins runtime-resolved exports, delegate pointers, and runtime-compiled code as outside; §18 carries the matching XREF line.
- Item 8 (diagnostics): every UI launch arms a sweep log (`UiLaunch.LaunchRecorded`), and records (schema `launch-diagnostics/2`) quote the birth's sweep lines in `sweep`; `SuccessRecordQuotesFields` asserts the quoted line names the main with its generation and reasons.
- Item 9 (outside observation): `TestHold` seam and `FirstBirthPreexistingSetMatchesAnOutsideObservation`; the snapshot moved to a field initializer (before the base `Window` constructor). Falsified before landing: with the constructor-body snapshot the proof failed, naming a base-constructor helper read as preexisting.

## Self-review

- Does it make sense: nine items from the §34 plan review and sign-off, each with a unit or UI fixture; item 9's proof found and fixed a real placement gap rather than only asserting.
- Is it logical: every new uncertainty fails safe (ambiguous and revalidation skips never move a window; the delayed pass does nothing under an overlap), and provenance (generation claims, marks) decides instead of timing assumptions.
- Did it break anything: the §34 slot semantics are unchanged (an overlapped construction still gets no snapshot); the sweep-log line gained `gen=` and a phase, and its one parser moved with it; every UI launch now sets the run marker for its launch only, and every seam behind the marker needs its own variable too; the fingerprint was regenerated (run-a 251/312) and the population gate passes.

Arch trigger: none - test-harness placement of background test windows and test-only seams behind the run marker; no editor state, app storage format, extension API, AI, protocol, or consent/undo surface changes.

## GPT panel Round 1

Candidate 7cda042, slot bulk, run 20260925-D00-T02-S41-codex-c06751119.

**adversarial: needs-attention** (2)
1. `src/Notepad.Core/SiblingSelection.cs:172`: `Revalidate` treats an unchanged thread ID as window identity. Destroying the selected HWND and reusing its value for another unowned window on the same thread passes validation and moves the replacement, violating item 5.
2. `src/Notepad.Core/SiblingSelection.cs:300` and `src/ScratchPad/MainWindow.xaml.cs:438`: Claims expire only when `IsWindow` returns false. If a claimed HWND is destroyed and reused before the next sweep, its stale claim survives and the replacement is skipped as `other-construction`; the window-generation mark never participates in claim validation.

**consistency: needs-attention** (1)
1. `tests/UI/LaunchGuard.cs:354`: Attribute alias resolution misses C# attribute-suffix elision: `using DIAttribute = System.Runtime.InteropServices.DllImportAttribute;` followed by `[DI("kernel32.dll")] static extern bool CreateProcessW();` is not recognized. This contradicts the stated coverage of aliased P/Invoke declarations in every spelling.

**integration: needs-attention** (2)
1. `src/ScratchPad/MainWindow.xaml.cs:448` and `src/ScratchPad/MainWindow.xaml.cs:493`: A second construction overwrites the single `lastSweep` before the first construction's queued delayed pass runs. The first callback then returns without placement or reporting, leaving its late helpers uncovered despite item 4.
2. `tests/UI/LaunchTests.cs:1593`: The observation barrier assumes synchronous `WM_NULL` waits for previously queued work. Sent messages can execute before posted messages, so this does not establish completion of queued placement work before the recorder barrier and shutdown. The dialog and flyout proofs can miss subsequent transient moves.

**record: approve**

Telemetry: round 1; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | f70aa49 (identity is thread, class, and both marks; `RevalidationSkipsReownedReusedAndGoneHandles` adds class and mark reuse) |
| R1-F2 | fixed | f70aa49 (`ScratchPad.SiblingClaim` mark; stale claim ignored in `InterleavedConstructionsWindowReadsAsTheOtherConstructions`) |
| R1-F3 | fixed | f70aa49 (suffix-elided attribute alias; new planted bypass in `PlantedBypassFailsTheGuard`) |
| R1-F4 | fixed | f70aa49 (`PendingLate` keyed by main) |
| R1-F5 | fixed | f70aa49 (barrier waits for each born main's `sweep-late` line) |

## GPT panel Round 2

Candidate 0940799..f70aa49, slot bulk, run 20260925-D00-T02-S41-codex-c06751119-r2. Round-1 findings did not re-report.

**adversarial: needs-attention** (1)
1. `src/Notepad.Core/SiblingSelection.cs:292`: `BegunSince` checks only `pending`, which `Take` clears. If A sweeps, B begins and completes its sweep, then A's delayed pass runs, it incorrectly sees no newer construction and can pin and claim B's unowned late helpers as A's. Retain the latest begun generation independently of the consumable snapshot and test this completed-overlap sequence.

**consistency: approve**

**integration: approve**

**record: approve**

Telemetry: round 2; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R2-F1 | fixed | a13bb32 (the slot keeps the latest begun generation; completed-overlap sequence in `DelayedPassPinsLateHelpersOnlyWithoutAnOverlap`) |

## GPT panel Round 3

Candidate 0940799..a13bb32, slot signoff, run 20260925-D00-T02-S41-codex-c06751119-r4. Round-1 and round-2 findings did not re-report. All three sign-off findings sit below the blocking bar (placement completeness in a test harness, a guard spelling, diagnostics completeness; no safety, data-integrity, or stamp-invalidating defect), so they were fixed in e7ad95a and re-gated at the stamp (the §18/§21 precedent).

**adversarial: needs-attention** (1)
1. `src/Notepad.Core/SiblingSelection.cs:199`: `DecideLate` excludes previously decided numeric handles without checking window identity. If a swept window is destroyed and its HWND reused before the delayed pass, the new helper is silently omitted, neither parked nor reported, violating item 4.

**consistency: needs-attention** (1)
1. `tests/UI/LaunchGuard.cs:354` and `tests/UI/LaunchGuard.cs:361`: Attribute normalization misses namespace-alias qualification such as `using I = System.Runtime.InteropServices; [I::DllImport("kernel32.dll")] static extern bool CreateProcessW();`. Splitting only on `.` leaves `I::DllImport` unrecognized, contradicting the promised coverage of qualified and aliased P/Invoke declarations.

**integration: needs-attention** (1)
1. `tests/UI/UiLaunchDiagnostics.cs:179` and `tests/UI/UiLaunchDiagnostics.cs:191`: Diagnostics stop reading after the first sweep line and delete the owned log before the delayed pass necessarily runs. A subsequent `sweep-late` placement failure is absent from the launch record, and its append can recreate an orphaned log, defeating item 8’s placement diagnostics.

**record: approve**

Telemetry: round 3; model gpt-6-astra; effort high; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | f70aa49 |
| R1-F2 | fixed | f70aa49 |
| R1-F3 | fixed | f70aa49 |
| R1-F4 | fixed | f70aa49 |
| R1-F5 | fixed | f70aa49 |
| R2-F1 | fixed | a13bb32 |
| R3-F1 | fixed | e7ad95a (decided identity in `DecideLate`; reuse case in `DelayedPassPinsLateHelpersOnlyWithoutAnOverlap`) |
| R3-F2 | fixed | e7ad95a (`I::DllImport` and `global::` planted bypasses) |
| R3-F3 | fixed | e7ad95a (record waits for `sweep-late` or names it missing; `SuccessRecordQuotesFields` asserts the late line) |

## Plan review

Round: `gpt-6-astra` medium (plan-primary slot) over §41 plus its Depends neighbor §34 and the sweep and launch contracts §26 and §18, 2026-09-25. Output check-plan PASS (12 findings, one per line). Triage: 9 filed as the new D00 T02 §48, 3 rejected.

Manifest: sections [D00 T02 §41, D00 T02 §34, D00 T02 §26, D00 T02 §18]; dependents [none]; bytes 32990; run 20260925-D00-T02-S41-codex-c06751119-r3

Ledger:

- [D00-T02-S41-PR1] [minor] overlap placement policy beyond pinning nothing -> filed D00 T02 §48
- [D00-T02-S41-PR2] [minor] containment and failure policy for ambiguous and late helpers -> filed D00 T02 §48
- [D00-T02-S41-PR3] [minor] claim attribution on shared framework threads -> filed D00 T02 §48
- [D00-T02-S41-PR4] [minor] the revalidation-to-move race -> rejected: owned in this candidate as a documented residual (Revalidate's comment and docs/testing.md: Win32 exposes nothing between the read and the move beyond thread, class, and marks; HWND values carry a reuse counter), with the reuse fixtures in RevalidationSkipsReownedReusedAndGoneHandles
- [D00-T02-S41-PR5] [minor] recorder delivery guarantees and a snap-back control -> filed D00 T02 §48
- [D00-T02-S41-PR6] [minor] every observed base-constructor helper reaches the target -> filed D00 T02 §48
- [D00-T02-S41-PR7] [minor] cleanup after a base-constructor failure -> filed D00 T02 §48
- [D00-T02-S41-PR8] [minor] diagnostics correlation by construction generation -> filed D00 T02 §48
- [D00-T02-S41-PR9] [minor] reconcile §18's promise with the guard's threat model -> rejected: §18 carries the §41 XREF line stating the threat model as its operative boundary (stamped checklists are not rewritten), and the guard's fixtures pin both sides
- [D00-T02-S41-PR10] [minor] carry the Night-owed dialog proof explicitly -> rejected: the evidence map and the candidate commit name it (the owned-dialog proof stays Interactive, Night-owed D00-T02-S34-N1)
- [D00-T02-S41-PR11] [minor] topology revalidation before a delayed move -> filed D00 T02 §48
- [D00-T02-S41-PR12] [minor] bounded cost for enumeration and passes -> filed D00 T02 §48

End of ledger

Raw plan output (fenced; fences strip from every scan):

```
- §41’s overlap policy permits pinning nothing while §34 requires every new helper off-screen; §41 should define deferred placement or launch failure for overlapping constructions so safety does not silently disappear.
- §41 allows ambiguous and late helpers to be merely reported without defining whether launch succeeds; add explicit containment and failure policy there so visible helpers cannot coexist with a passing background-placement verdict.
- §41’s generation claims lack an acceptance test for attribution of unowned helpers on shared framework threads; extend its interleaving fixture to prove both that foreign helpers stay untouched and that each construction’s own helpers still pin.
- §41’s immediate pre-move revalidation leaves destruction, HWND reuse, or ownership transfer between validation and the native move unspecified; own that race explicitly there with a synchronization boundary or a documented residual limitation and adversarial fixture.
- §41 equates a drained event count with proof that transient moves cannot escape observation, although §18 explicitly acknowledges asynchronous observation losses; define recorder delivery guarantees, timeout/overflow failures, and a planted move-and-snap-back control in §41.
- §41’s first-birth acceptance checks the preexisting set but does not explicitly require independently observed base-constructor helpers to reach the target; extend that fixture to catch helpers omitted from both the snapshot and the sweep log.
- §41 covers cancellation and destruction without specifying cleanup after failure before the constructor body runs; add a base-constructor-failure fixture proving generation claims and snapshot tokens cannot contaminate the next birth.
- §41 requires one diagnostic record to quote a sweep line but leaves concurrent launch correlation and missing or truncated logs undefined; extend §18’s versioned diagnostics contract with construction IDs, complete sweep outcomes, and explicit missing-evidence status.
- §18 still promises an analyzer or enforced abstraction that prevents every wrapper bypass, while §41 describes a bounded syntax-plus-alias guard; reconcile the operative criterion with that threat model so completion is assessed against a testable promise.
- §34’s owned-dialog proof remains Night-owed while §41 marks its dialog observation work complete without naming the corresponding live proof or debt; carry that evidence obligation explicitly in §41 so recorder improvements do not imply the dialog scenario passed.
- §18 proves the initial off-screen point but none of these sections explicitly owns topology changes before a delayed helper move; add move-time monitor revalidation and a hot-plug fixture to §41 so an obsolete target cannot become visible.
- §41 lacks a bounded-cost criterion for enumeration, attribution, and delayed passes; add repeated multi-window birth and teardown checks for latency and retained state to preserve responsive launches as the helper population grows.
```

## Live proof

Provenance: candidate e7ad95abb59555fa229fd543ab02c098cb66c5ea; command dotnet test src/ScratchPad.slnx --no-build --filter "Category!=Interactive&Category!=Primary" -e SCRATCHPAD_BACKGROUND=1 beside Bin/ForegroundLog/Debug/ForegroundLog.exe 1500; exit 0; tool .NET 10.0.400 and ForegroundLog; digest 18eda73371830a14f9ba8e836ea7359e255b925aa2ba7bd4af6bbda89638bbe8; path src/Notepad.Core/SiblingSelection.cs; run 20260925-D00-T02-S41-codex-c06751119-r3

```
Smoke 1/0, Unit 379/0, Protocol 35/0
UI Passed 297, Skipped 16, Failed 0 (11 m 2 s)
test exit 0
gate exit 0
changes logged; flagged=0; census=246 primary=0 events=252 event-primary=0 uncovered=4 uncovered-primary=0 mismatch=0 expect=secondary
population OK run-a=251/315 run-b=4/4 interactive=43/74
outside-observation proof, falsified before landing: with the constructor-body snapshot, "the sweep read 0x1771362 as preexisting, but the outside observation before the construction never saw it"
```

An earlier rerun at f70aa49 went red on `event-primary=1` from a foreign "Granola Setup" installer window attributed to ScratchPad through ForegroundLog's pid cache after pid reuse (filed D00 T02 §49); it is not used as proof.
