# D00 T02 S45 review - Nightly Evidence Third Residuals

Candidate: e742a88 (Started plus implementation), eb42141 (round-1 fixes), b7298b5 (round-1 docs), 884203f (round-2 fixes), 7835f1a (sign-off fixes).
Tier: Full panel. The diff changes shared nightly tooling that several consumers read: the governed run (`tools/nightly.ps1`), the parser library (`tools/NightlyParse.ps1`), retention (`tools/NightlyRetention.ps1`), the ledger rebuild (`tools/NightlyLedger.ps1`), the incident policy, and a new triage step (`tools/NightlyTriage.ps1`).

## Evidence map

- Item 1 (sweep safety): `Clear-StaleCaptureStaging` with `Get-LiveRunStamps`, `Test-ReparsePoint`, `Remove-TreeNoFollow`; fixture s45-sweep-spares-live-and-junctions.
- Item 2 (capture-time authorization): `binaryCaptures` in `Read-IncidentPolicy`, `$script:BinaryCapturesAllowed` (fail closed), `Get-DumpArgs`, the refusal in `Invoke-FailureCapture`; fixture s45-captures-off-writes-no-binary.
- Item 3 (retention priorities): `Get-ProtectedSnapshot`, the prune keep and the quota accounting in `tools/NightlyRetention.ps1`; fixtures s45-quota-names-and-counts-the-snapshot and s45-prune-keeps-the-snapshot-stamp.
- Item 4 (crash interleavings): the per-run checkpoint, `previousStamp`, `Get-LedgerCheckpoints`, `Get-ResultChainGaps`, the rebuild's checkpoint base and gap refusal, `-AcceptGaps`; fixtures s45-interrupted-write-rebuilds-to-checkpoint and s45-gap-is-named-and-refuses.
- Item 5 (alias migration): `Move-AliasedIncidents` wired before the nightly update; fixture s45-joined-alias-carries-link-and-streak.
- Item 6 (qualifying runs): the `NotQualifying` parameter of `Update-IncidentLedger` and the nightly's disqualifying list; fixture s45-non-qualifying-run-holds-the-streak.
- Item 7 (no-commit promise): `Register-TrackedWrite`, `Compare-TreeWithTrackedWrites`, `Write-TrackedWriteManifest`, `tools/NightlyTriage.ps1`; fixture s45-tree-check-expects-collector-lines and `tools/NightlyTriage.Tests.ps1`.
- Item 8 (one summary): `Format-EvidenceSummary` and the report's Evidence completeness block; fixture s45-one-summary-names-degraded-evidence.

## Self-review

- Does it make sense: each item has a fixture over the shipped function, and three plan decisions are recorded in the section with their cost of changing.
- Is it logical: every new path fails closed (an unreadable policy takes no binary capture, a gap refuses the rebuild, a reparse point is never followed, anything beyond the recorded lines reads MUTATED).
- Did it break anything: NightlyParse, NightlyRetention, NightlyTriage, NightDebt, NightlyNotify, NightlyAck, and BuildInputsDigest suites green; every changed script parses; validate 0 fatal.

Arch trigger: none - nightly evidence tooling only; no editor state, app storage format, extension API, AI, protocol, or consent/undo surface changes.

## GPT panel Round 1

Candidate e742a88, slot bulk, run 20260925-D00-T02-S45-codex-c06751119.

**adversarial: needs-attention** (1)
1. `tools/NightlyTriage.ps1:57-59` commits without verifying the Claude writer session, allowing other harnesses to commit despite the repository’s exclusive-writer rule.

**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:2819-2824` serializes only `file` and `lines` into `tracked-writes.json`, omitting the required pre-write text. The recorded decision promises that baseline in the manifest, but it survives only in memory.

**integration: needs-attention** (2)
1. `tools/NightlyParse.ps1:4398-4400` merges recovery streaks by taking the larger value, even when the other alias has a newer failure. An old alias with two passes and a newly failed v2 alias with zero becomes a two-pass incident, allowing premature closure after one additional pass.
2. `tools/nightly.ps1:1213-1220` disqualifies only an explicitly killed Interactive leg, omitting killed non-Interactive legs and other evidence-validation failures. Qualifying tests from those aborted or evidence-invalid runs can still advance or reset incident streaks.

**record: needs-attention** (1)
1. `tools/nightly.ps1:1223-1230` writes the authoritative ledger before its recovery checkpoint. A crash between those writes leaves neither a checkpoint nor a result containing the newer state, so rebuilding loses that update. The fixture in `tools/NightlyParse.Tests.ps1:1803-1817` supplies an already-written checkpoint and does not cover this crash interval.

Telemetry: round 1; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | eb42141 (triage -Commit refuses outside a Claude Code session; fixture triage-commit-needs-the-writer-session) |
| R1-F2 | fixed | eb42141 (the manifest keeps the pre-write text and its SHA-256; fixture s45-manifest-keeps-the-pre-write-text) |
| R1-F3 | fixed | eb42141 (a streak survives an alias merge only from the side whose own last failure is newer; fixture s45-alias-merge-never-carries-a-streak-past-a-newer-failure) |
| R1-F4 | fixed | eb42141 (killed or overrun legs, a red gate, a killed soak, and broken conservation disqualify) |
| R1-F5 | fixed | eb42141 (checkpoint before ledger plus roll-forward; fixture s45-crash-before-the-ledger-rolls-forward) |

## GPT panel Round 2

Candidate b7298b5 (range e742a88~1..b7298b5, section files only), slot bulk, run 20260925-D00-T02-S45-codex-c06751119-r2. Round-1 findings did not re-report.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:2813-2829` validates collector files against their pre-write text, then excludes them from the start/end comparison. An unrelated edit made after the initial fingerprint but before the collector reads that file becomes part of the accepted baseline, allowing a mutated tree to report clean.

**consistency: approve**

**integration: needs-attention** (1)
1. `tools/NightlyParse.ps1:4425-4430` copies the selected alias’s `passStreak` and `lastPassStamp` without its `streakPopulation`. When the old alias supplies the surviving streak, the destination retains unrelated or empty population provenance, causing the subsequent update to reset a valid streak or accept it against the wrong population.

**record: needs-attention** (1)
1. `tools/nightly.ps1:1237-1240` continues writing the authoritative ledger after a checkpoint write fails. A crash before result publication then leaves the newer state without either recovery artifact, preserving the data-loss interval reported in R1-F5. Refuse the ledger update when its checkpoint cannot be persisted.

Telemetry: round 2; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R2-F1 | fixed | 884203f (the tree check pins each written file's start text; fixture s45-tree-check-pins-the-start-text) |
| R2-F2 | fixed | 884203f (the surviving streak's population moves with it; fixture s45-alias-merge-never-carries-a-streak-past-a-newer-failure) |
| R2-F3 | fixed | 884203f (no checkpoint, no ledger write; the run reds) |

## GPT panel Round 3

Candidate 884203f (range e742a88~1..884203f, section files only), slot signoff, run 20260925-D00-T02-S45-codex-c06751119-r3. Round-1 and round-2 findings did not re-report.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:2796,2815-2816` compares baseline text using PowerShell’s case-insensitive `-eq`/`-ne`. An unrelated case-only edit therefore passes the collector-only check and is excluded from fingerprint comparison. `tools/NightlyTriage.ps1:51` uses the same helper, allowing that edit into the collector commit. Use case-sensitive comparisons for these content checks.

**consistency: approve**

**integration: needs-attention** (1)
1. `tools/NightlyTriage.ps1:31-35,49-51` processes only one run’s manifest against HEAD. If two uncommitted nightly runs insert recorded lines into the same TODO file, either manifest rejects the other run’s legitimate lines as unrelated changes. A newer empty manifest also hides pending earlier writes. Reconcile outstanding manifests together so delayed triage can commit the accumulated collector changes.

**record: needs-attention** (1)
1. `tools/NightlyLedger.ps1:45` writes the rebuilt ledger without its recovery stamp. `tools/NightlyParse.ps1:2555-2556` subsequently treats any stamped checkpoint as newer than that unstamped ledger and replaces its state. When rebuild restores a snapshot or replays results newer than the surviving checkpoint, the next nightly run silently rolls recovery backward. Persist the rebuilt state’s latest applied stamp.

Telemetry: round 3; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

Sign-off disposition: R3-F3 (a rebuild without its stamp lets an older checkpoint roll recovery backward) is data integrity, so it is fixed and round 4 runs on the depth slot; R3-F1 and R3-F2 sit below the bar but have contained fixes, so they are fixed in the same commit and re-gated at the stamp (the §18/§21 precedent).

| ID | Disposition | Evidence |
| --- | --- | --- |
| R3-F1 | fixed | 7835f1a (case-sensitive content checks; fixture triage-refuses-a-case-only-edit) |
| R3-F2 | fixed | 7835f1a (every pending manifest together; fixture triage-commits-every-pending-run-together) |
| R3-F3 | fixed | 7835f1a (the rebuilt ledger carries its latest applied stamp; fixture s45-rebuild-stamp-blocks-a-backward-roll) |

## GPT panel Round 4

Candidate 7835f1a (range e742a88~1..7835f1a, section files only), slot depth, blocking findings only, run 20260925-D00-T02-S45-codex-c06751119-r5. R3-F1..F3 did not re-report.

**adversarial: approve**

**consistency: approve**

**integration: needs-attention** (1)
1. `tools/NightlyParse.ps1:4425-4426` adopts the old alias’s specific owner but clears `due` instead of carrying that alias’s due date. When both IDs exist, migration therefore loses a scheduled deadline and suppresses its overdue escalation, violating the requirement that ownership and due date follow the v2 ID.

**record: approve**

Telemetry: round 4; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

R4-F1 sits below the blocking bar: an incident whose owner comes from a quarantine row carries no triage due by construction (`Update-IncidentLedger` clears it when a specific owner is set), so clearing the due while adopting the old id's specific owner matches the ledger's own rule and loses a deadline only for a hand-edited entry. It files to D00 T02 §53 with the alias-collision item, and no blocking finding remains.

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | eb42141 |
| R1-F2 | fixed | eb42141 |
| R1-F3 | fixed | eb42141, 884203f |
| R1-F4 | fixed | eb42141 |
| R1-F5 | fixed | eb42141, 884203f |
| R2-F1 | fixed | 884203f, 7835f1a |
| R2-F2 | fixed | 884203f |
| R2-F3 | fixed | 884203f |
| R3-F1 | fixed | 7835f1a |
| R3-F2 | fixed | 7835f1a |
| R3-F3 | fixed | 7835f1a |
| R4-F1 | filed | D00 T02 §53 |

## Plan review

Round: `gpt-6-astra` medium (plan-primary slot) over §45 plus its Depends neighbor §38 and the contracts it extends, §30 and §22, 2026-09-25. Output check-plan PASS (15 findings, one per line). Triage: 13 filed as the new D00 T02 §53, 1 applied (§45's own finding count, corrected in place), 1 rejected.

Manifest: sections [D00 T02 §45, D00 T02 §38, D00 T02 §30, D00 T02 §22]; dependents [none]; bytes 29705; run 20260925-D00-T02-S45-codex-c06751119-r4

Ledger:

- [D00-T02-S45-PR1] [minor] §45's source count and disposition mapping -> accepted and applied in the stamp commit: §45's Why line carries a Corrected note (15 ledger rows: the §38 review's first line repeated as PR2)
- [D00-T02-S45-PR2] [minor] escape-proof sweep (ancestors, replacement mid-delete) -> filed D00 T02 §53
- [D00-T02-S45-PR3] [minor] run ownership and sweep coordination -> filed D00 T02 §53
- [D00-T02-S45-PR4] [minor] absent versus explicit capture consent -> filed D00 T02 §53
- [D00-T02-S45-PR5] [minor] bounded lifetime for every binary copy -> filed D00 T02 §53
- [D00-T02-S45-PR6] [minor] snapshot replacement space reservation -> filed D00 T02 §53
- [D00-T02-S45-PR7] [minor] durable replay record for a crash after the ledger write -> rejected shipped in R1-F5: the checkpoint lands before the ledger in the run's own stamp directory, so a crash after the ledger write already left the checkpoint, and a rebuild after losing the ledger restores it (fixtures s45-interrupted-write-rebuilds-to-checkpoint and s45-crash-before-the-ledger-rolls-forward).
- [D00-T02-S45-PR8] [minor] replay ordering invariants -> filed D00 T02 §53
- [D00-T02-S45-PR9] [minor] alias collision reconciliation -> filed D00 T02 §53
- [D00-T02-S45-PR10] [minor] per-incident qualification -> filed D00 T02 §53
- [D00-T02-S45-PR11] [minor] collector write ordering and edit conflicts -> filed D00 T02 §53
- [D00-T02-S45-PR12] [minor] clean versus pending-writes tree state -> filed D00 T02 §53
- [D00-T02-S45-PR13] [minor] the collection-to-triage chain end to end -> filed D00 T02 §53
- [D00-T02-S45-PR14] [minor] one structured completeness record -> filed D00 T02 §53
- [D00-T02-S45-PR15] [minor] a compound acceptance scenario -> filed D00 T02 §53

End of ledger

Raw plan output (fenced; fences strip from every scan):

```
- §45’s provenance totals 15 dispositions against 14 findings, and cites PR15; reconcile the source count and disposition mapping in §45 so every accepted, rejected, and duplicate finding is traceable.
- Sweep safety checks `.staging` but does not explicitly protect against reparse points in ancestor directories or path replacement during deletion; extend §45’s sweep contract and fixtures to prove deletion cannot escape the evidence root.
- A journal naming one live run does not define stale-journal recovery or protection for overlapping invocations; add run ownership, liveness checks, and exclusive sweep coordination to §45 to prevent deleting active evidence.
- Capture authorization defaults to enabled when `binaryCaptures` is absent, but policy absence and explicit consent are not distinguished; clarify first-run and upgrade behavior in §45 so existing compatibility defaults cannot silently authorize sensitive captures.
- Binary-copy expiry does not explicitly cover refused retention, interrupted captures, abandoned staging, or diagnostic temporary files; assign all capture locations and cleanup failure reporting to §45 so sensitive evidence has a bounded lifetime.
- Snapshot replacement is atomic, but quota accounting does not reserve space for both the old snapshot and its replacement; extend §45’s retention contract to preserve the recoverable snapshot during low-disk replacement failures.
- A crash after ledger write but before result write leaves the later state unavailable if that ledger is subsequently lost; §45 needs a durable replay record or equivalent transaction boundary, tested with subsequent ledger loss, to substantiate its recovery promise.
- Ordering replay by stamp does not define clock rollback, equal timestamps, duplicate results, or conflicting snapshots; add ordering and deduplication invariants to §45’s recovery contract so rebuilding cannot silently change incident history.
- Alias migration leaves conflicts undefined when both source and destination incidents already have links, owners, occurrences, or recovery streaks; extend §45 with deterministic reconciliation and collision fixtures to prevent lost ownership or premature closure.
- §45 holds recovery streaks even when a nonqualifying run records a relevant failure, weakening §22’s failure-reset rule; define qualification per incident and distinguish trustworthy failures from invalid evidence so unrelated run defects cannot conceal a regression.
- `tracked-writes.json` records mutations but does not define crash recovery between TODO writes and journal writes, or protection against subsequent user edits; extend §45’s collector and triage contract with recoverable write ordering and conflict checks so triage commits only attributable changes.
- §45’s “tree stays clean” acceptance conflicts with its decision to modify tracked TODO files before triage commits; distinguish an actually clean tree from a tree containing expected pending writes in §45 and the §22 consumer documentation.
- §45 references §42’s post-run debt check and a triage commit step without declaring their connected contracts; add reciprocal references and an end-to-end acceptance case covering collection, debt visibility, triage failure, retry, and commit.
- The operator summary alternates between “one line” and “one summary block” and lacks severity or exit-status semantics; define one structured completeness record with a concise rendered summary in §45 so automation and operators agree on degraded evidence and required action.
- Existing tests mainly prove individual mechanisms; add a combined §45 acceptance scenario with capture refusal, quota pressure, interrupted publication, rebuild, and triage retry to prove the complete recovery workflow remains usable under compounded failures.
```

## Live proof

Provenance: candidate 7835f1a5054da7d8ad3cc675f72d12c2dda94ada; command powershell -File tools/NightlyParse.Tests.ps1 plus the NightlyRetention, NightlyTriage, NightDebt, NightlyNotify, NightlyAck, and BuildInputsDigest suites; exit 0; tool Windows PowerShell 5.1; digest 617dcd552d346c5c8f2f55ae6485ee0f1da0a854ef51fb25f45a98d9a91e2028; path tools/NightlyParse.ps1; run 20260925-D00-T02-S45-codex-c06751119-r4
