# Review: D00 T01 §38 -- Architecture Gate Residuals

Candidate: `510a94f` plus `8bc5d63` (SHIP plus the self-review fix). Full panel (skill is shared contract surface): Sol rounds 1-2, Opus round-3 sign-off.

Arch trigger: none - skill prose plus TODO ticks; no editor, storage, extensibility, AI, protocol, or consent surface changes.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. Four fixes, committed in `8bc5d63`, re-asked clean.

Does it make sense: each item maps to its skill hunk (trigger line plus checklist, outage rung plus record plus stop, Arch-N numbering plus budget plus governance, post-panel order plus blocking powers, terminal plus redesign path), the live proof ran Arch-1 plus Arch-2 over a staged storage flaw with the terminal path exercised, and the forward scope on the trigger line respects append-only history. Exactly the contract.

Is it logical: the first pass found four gaps, all fixed in `8bc5d63`: redesigned candidates re-entered only the gate while the panel's approves rested on the superseded candidate; multi-surface triggers had no line shape; the fallback's round coverage was unstated; `post-§38 review` misread as a section-number bound instead of the stamp-date scope. Re-asked: re-entry runs panel plus gate, one line per surface, fallback covers only the round whose primary failed, defect scoped to reviews stamped after §38.

Did it break anything else: no mechanical consumers of the old gate prose (grep over scripts, tests, todo, .claude, docs clean) and no script references the skill path, so the change is prose-only; `validate` stays 0 fatal and the self-test is untouched (no code changed).

## Gate live proof (item 6 record)

Arch trigger: storage - staged settings-format diff exercised the gate; this review itself is `none` (see header).

Dry-run record, transcribed per the Arch-N shapes (full reviewer outputs were ephemeral by design and torn down with the fixture; verdicts plus finding titles below are as observed, and the durable artifact is the SHIP commit body `510a94f`):

- `Architecture review (Arch-1)`: `**architecture: needs-attention**`, 6 findings (contract break on deleting the MUST-keep v1 reader; Load deserializes unconditionally with no version dispatch; write side moves to v2 irreversibly; no migration seam anywhere; in-place write with no atomic replace; no test or fixture for the format change).
- Fix: version probe plus migrate-on-read, v1 reader kept, temp-file write with readback.
- `Architecture review (Arch-2)`: `**architecture: needs-attention**`, 6 new findings (unconditional v2 write with no v1 hatch; temp write unflushed before rename; readback result discarded; open-ended version dispatch; unguarded version peek; comment over-asserts the routing).
- Terminal: budget (one round plus one rerun) spent with blocking findings still open, so per the new rule the run stops with no stamp and the redesign direction above is the remediation path. Teardown confirmed (`ls /tmp/arch-*` clean). Sol-high rung echo probe exit 0 (`RUNG-OK`).

## GPT panel (round 1)

- `adversarial` approve
- `consistency` needs-attention: (R1-F1) trigger-line count contradiction.
- `integration` approve
- `record` needs-attention: (R1-F2) live proof lacks a findings record.

Disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 92196b5 |
| R1-F2 | fixed | 92196b5 |

Fix record: the trigger mandate now reads one line per triggered surface with a single `none` line when nothing triggers, matching the multi-surface rule; the dry-run record rides the findings file under Gate live proof with the SHIP body as the durable artifact (R1-F2's no-artifact premise corrected: verdicts plus titles were quoted in `510a94f`). Fixed in `92196b5` (validate 0 fatal); gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `089467f3427830864571dd53187cf3c46383bf912247928de050cbac76ce11a5`; run `20260919-D00-T01-S38-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:129` requires exactly one trigger line per review but later requires one trigger line per surface when multiple surfaces trigger, leaving the canonical record shape contradictory.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1028` marks the live-proof item complete, and `todo/00-workspace/TODO-01-repo-and-toolchain.md:1031` claims dry-run evidence, but the candidate contains neither the required disposable live Opus-high round record nor another artifact proving that round ran and was torn down.
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Disposition table (R1-F1, R1-F2 stay fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 92196b5 |
| R1-F2 | fixed | 92196b5 |

No fix record: round 2 approved on all lenses with no new findings; the candidate stands at `92196b5`.

Raw round-2 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`; run `20260919-D00-T01-S38-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Sign-off disposition: all four lenses approve with no new findings; R1-F1 and R1-F2 stay fixed. The panel closes with no advisories and no filed follow-ups.

Final disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 92196b5 |
| R1-F2 | fixed | 92196b5 |

Architecture gate: not triggered (`Arch trigger: none`, see header).

Raw round-3 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711` (same bytes as round 2: all-approve); run `20260919-D00-T01-S38-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §38 plus §35 plus §36, 2026-09-19. Neighbors: §35 (filed-from), §36 (XREF). Dependents: D00 T01 §36 by the mechanical rule (recorded in the run's dependents file). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (18 findings, one per line). Run note: the plan run shares its base with the panel runs (`20260919-D00-T01-S38-gpt`); kind distinguishes them and the minter is authoritative. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §38, D00 T01 §35, D00 T01 §36]; dependents [D00 T01 §36]; bytes 16049; run 20260919-D00-T01-S38-gpt

Ledger (namespaced IDs per the §16 shape; 3 of 18 filed at D00 T04 §1, 2 duplicates, 13 rejected with reasons on their rows):

Ledger:
- [D00-T01-S38-PR1] [minor] §35 terminal contradicts §38 -> duplicate D00-T01-S37-PR2
- [D00-T01-S38-PR2] [minor] Trigger lacks significance thresholds -> rejected premature calibration: the parentheticals scope the first runs, and the trigger-line audit trail produces the data to tune on; invented examples now would be guesses
- [D00-T01-S38-PR3] [minor] Cross-cutting surfaces lack a home -> rejected the six surfaces are the shipped §35 design (consent/undo plus storage cover the privacy/recovery-adjacent cases); a vague cross-cutting criterion trades under- for over-triggering without data; widen on the first missed trigger
- [D00-T01-S38-PR4] [minor] Trigger record lacks audit fields -> rejected the candidate binds positionally (the line lives in the candidate-bound findings file with its recorded runs); reviewer/model/timestamp over-schema an in-session triage decision
- [D00-T01-S38-PR5] [minor] No verdict/finding schema -> rejected the schema exists distributed (verdict line, numbered file-line findings, contract surface, terminal direction plus owner); severity is moot under no-below-bar; a consolidated block restates the record shapes
- [D00-T01-S38-PR6] [minor] Outage classification underspecified -> rejected qualifying failures are named (exit/auth/model/timeout) with retry bounded at exactly one, and verdict content can never qualify since only transport signals do; quota surfaces through the nonzero-exit leg; the malformed-output half disputes the recorded no-checker design
- [D00-T01-S38-PR7] [minor] Fallback dispatch fidelity unproven -> rejected the fallback command is pinned; dispatch fidelity composes from the proven plan-review rung plus the proven stdin dash; hash assertions need a runner harness the operator-run judgment gate deliberately lacks
- [D00-T01-S38-PR8] [minor] Outage records lack round numbers -> filed D00 T04 §1
- [D00-T01-S38-PR9] [minor] Candidate invalidation unstated -> rejected already satisfied: item 5's Done-when delegates the path to the skill, which names panel re-entry plus gate restart (self-review fix 8bc5d63, in the reviewed range)
- [D00-T01-S38-PR10] [minor] Plan-review loop-back unstated -> rejected plan review cannot change the candidate (advisory filings route, never applied blind) and criticals already reopen through audit stance; nothing loops back, so no loop-back rule is owed
- [D00-T01-S38-PR11] [minor] Checkpoint asserts no behavior -> rejected flaw-detected, stamp-blocked (terminal stop), rerun, and teardown are all quoted in the record; final-approval is not this proof's terminal (the blocking workflow ends in approve or stop; the dry run demonstrated stop, the case the gate exists for)
- [D00-T01-S38-PR12] [minor] No clean control -> rejected one clean control cannot measure a judgment round's specificity, and Arch-2 already demonstrates discrimination (new flaws drew new findings, not a blanket re-block); specificity evidence accrues from live trigger lines
- [D00-T01-S38-PR13] [minor] Outage rung lacks injection proof -> filed D00 T04 §1
- [D00-T01-S38-PR14] [minor] Torn-down proof keeps no artifacts -> rejected the item mandates a disposable fixture (transcribe, tear down); raw bytes and staged hashes cannot survive teardown by design; the transcribed verdicts, flaw description, teardown confirmation, and SHIP-body artifact are the retained record
- [D00-T01-S38-PR15] [minor] §35 live-proof state contradicts §38 -> rejected no contradiction: §35's live proof explicitly means proof on a triggered review (none has run; still true), while §38's staged run proves the gate executes; a pointer would conflate the two claims
- [D00-T01-S38-PR16] [minor] Unticked Commit needs machine state -> duplicate D00-T01-S36-PR1
- [D00-T01-S38-PR17] [minor] No architecture decision record -> rejected findings already carry the rationale (defect plus files plus terminal direction); a mandated five-field record pre-first-triggered-review is speculative schema; file from the first triggered retro if findings prove insufficient
- [D00-T01-S38-PR18] [minor] No regression on gate rules -> filed D00 T04 §1
End of ledger

Raw plan-review output (fenced, not part of any scan):

```
- The §35 rule permits unresolved architecture findings to be filed and stamped, while §38 forbids stamping; record §38 as the explicit superseding rule in the skill and a dated §35 note because conflicting terminal behavior can produce an invalid stamp.
- The architecture trigger lists six surfaces but gives no threshold for what change is architecture-significant; add objective examples and exclusions to §38 because reviewers may inconsistently skip or over-trigger the costly gate.
- Security, privacy, accessibility, performance, update, and recovery architecture are absent from the trigger checklist; extend §38 with a cross-cutting or expensive-to-reverse criterion because premium-product risks do not fit neatly into the six named surfaces.
- A trigger record names only the surface or non-trigger reason; require candidate identity, reviewer, model, timestamp, and prompt or rubric version in §38 because the decision must remain auditable when the candidate or process changes.
- The gate has no defined verdict and finding schema; add approve/needs-attention rules, severity, evidence, affected surface, and remediation fields to §38 because “blocking round” alone cannot produce consistent machine-checkable governance.
- The outage rung does not define timeout, authentication, quota, malformed-output, or partial-response classification; specify qualifying failures and bounded retry behavior in §38 because ordinary model disagreement must never be mistaken for an outage.
- “Same prompt” is not executable proof that the Sol fallback receives the same candidate and rubric; add a pinned fallback command plus prompt and candidate hash assertions to §38 because an echo probe does not test real dispatch fidelity.
- Arch numbering is outside the panel cap, but fallback attempts, reruns, and failed calls are not mapped to `Arch-N`; define numbering for every outcome in §38 because records otherwise become ambiguous during outages and remediation.
- The plan does not state what happens when an architecture fix changes the candidate after panel approval; add candidate invalidation and panel re-entry rules to §38 because architecture remediation can introduce defects outside the architecture lens.
- The post-panel sequence places plan review after all gates without defining what happens if plan review changes the candidate or raises a blocking finding; add a loop-back or no-stamp terminal to §38 because the stated order can otherwise stamp unreviewed fixes.
- The live-proof item requires detection of a planted flaw, but the checkpoint checks only record shape, command echo, numbering, and prose order; add explicit flaw-detected, stamp-blocked, remediation-rerun, final-approval, and teardown assertions to §38 because its most important acceptance behavior is currently untested.
- The disposable fixture has no clean control; add an equivalent sound candidate that must pass in §38 because proving sensitivity without proving resistance to false positives can normalize a gate that blocks valid work.
- The outage test exercises only a command echo rather than a real failed-primary/successful-fallback flow; add deterministic failure injection and a double-failure no-stamp case to §38 because the safety terminal is otherwise documentary rather than operational.
- The disposable proof is torn down without naming the durable raw judgment, staged candidate hash, planted flaw, or teardown evidence location; require retained sanitized artifacts in the §38 review record because future audits must distinguish a real gate run from reconstructed prose.
- §35 still says the first live architecture proof waits for a future triggered review, while §38 says the staged live proof now carries it; add a dated immutable-note pointer to §35 because connected sections currently present contradictory live-proof state.
- §35 remains verified while its Commit checkbox is unchecked and relies on a prose note to explain shipped candidates; add a validator-recognized shipped-state annotation or reconciliation section because machine and human readers otherwise disagree about completion.
- The gate records decisions but does not preserve the architecture rationale; require a lightweight decision record covering alternatives, reversibility, migration, rollback, and user impact in §38 because premium-product architecture should remain understandable after the review transcript ages.
- No durable regression protects the new trigger, numbering, outage, and terminal rules after the disposable run; add focused validator fixtures owned by §38 or a named follow-up section because later skill edits can silently remove the governance this section claims to complete.
```
