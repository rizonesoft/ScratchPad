# Review: D00 T01 §38 -- Architecture Gate Residuals

Candidate: `510a94f` plus `8bc5d63` (SHIP plus the self-review fix). Full panel (skill is shared contract surface): Sol rounds 1-2, Opus round-3 sign-off.

Arch trigger: none - skill prose plus TODO ticks; no editor, storage, extensibility, AI, protocol, or consent surface changes.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. Four fixes, committed in `8bc5d63`, re-asked clean.

Does it make sense: each item maps to its skill hunk (trigger line plus checklist, outage rung plus record plus stop, Arch-N numbering plus budget plus governance, post-panel order plus blocking powers, terminal plus redesign path), the live proof ran Arch-1 plus Arch-2 over a staged storage flaw with the terminal path exercised, and the forward scope on the trigger line respects append-only history. Exactly the contract.

Is it logical: the first pass found four gaps, all fixed in `8bc5d63`: redesigned candidates re-entered only the gate while the panel's approves rested on the superseded candidate; multi-surface triggers had no line shape; the fallback's round coverage was unstated; `post-§38 review` misread as a section-number bound instead of the stamp-date scope. Re-asked: re-entry runs panel plus gate, one line per surface, fallback covers only the round whose primary failed, defect scoped to reviews stamped after §38.

Did it break anything else: no mechanical consumers of the old gate prose (grep over scripts, tests, todo, .claude, docs clean) and no script references the skill path, so the change is prose-only; `validate` stays 0 fatal and the self-test is untouched (no code changed).

## Gate live proof (item 6 record)

Arch trigger: storage - staged settings-format diff exercised the gate; this review itself is `none` (see header).

Dry-run record, transcribed per the Arch-N shapes (full reviewer outputs were ephemeral by design and torn down with the fixture; verdicts plus finding titles below are as observed, and the durable artifact is the SHIP commit body `510a94f`):

- `Architecture review (Arch-1)`: `**architecture: needs-attention**`, 6 findings (contract break on deleting the MUST-keep v1 reader; Load deserializes unconditionally with no version dispatch; write side moves to v2 irreversibly; no migration seam anywhere; in-place write with no atomic replace; no test or fixture for the format change).
- Fix: version probe plus migrate-on-read, v1 reader kept, temp-file write with readback.
- `Architecture review (Arch-2)`: `**architecture: needs-attention**`, 6 new findings (unconditional v2 write with no v1 hatch; temp write unflushed before rename; readback result discarded; open-ended version dispatch; unguarded version peek; comment over-asserts the routing).
- Terminal: budget (one round plus one rerun) spent with blocking findings still open, so per the new rule the run stops with no stamp and the redesign direction above is the remediation path. Teardown confirmed (`ls /tmp/arch-*` clean). Sol-high rung echo probe exit 0 (`RUNG-OK`).

## GPT panel (round 1)

- `adversarial` approve
- `consistency` needs-attention: (R1-F1) trigger-line count contradiction.
- `integration` approve
- `record` needs-attention: (R1-F2) live proof lacks a findings record.

Disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 92196b5 |
| R1-F2 | fixed | 92196b5 |

Fix record: the trigger mandate now reads one line per triggered surface with a single `none` line when nothing triggers, matching the multi-surface rule; the dry-run record rides the findings file under Gate live proof with the SHIP body as the durable artifact (R1-F2's no-artifact premise corrected: verdicts plus titles were quoted in `510a94f`). Fixed in `92196b5` (validate 0 fatal); gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `089467f3427830864571dd53187cf3c46383bf912247928de050cbac76ce11a5`; run `20260919-D00-T01-S38-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: needs-attention** (1)
1. `.claude/skills/review-todo-section/SKILL.md:129` requires exactly one trigger line per review but later requires one trigger line per surface when multiple surfaces trigger, leaving the canonical record shape contradictory.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1028` marks the live-proof item complete, and `todo/00-workspace/TODO-01-repo-and-toolchain.md:1031` claims dry-run evidence, but the candidate contains neither the required disposable live Opus-high round record nor another artifact proving that round ran and was torn down.
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Disposition table (R1-F1, R1-F2 stay fixed):

| ID | Disposition | Evidence |
| R1-F1 | fixed | 92196b5 |
| R1-F2 | fixed | 92196b5 |

No fix record: round 2 approved on all lenses with no new findings; the candidate stands at `92196b5`.

Raw round-2 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`; run `20260919-D00-T01-S38-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Sign-off disposition: all four lenses approve with no new findings; R1-F1 and R1-F2 stay fixed. The panel closes with no advisories and no filed follow-ups.

Final disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | 92196b5 |
| R1-F2 | fixed | 92196b5 |

Architecture gate: not triggered (`Arch trigger: none`, see header).

Raw round-3 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711` (same bytes as round 2: all-approve); run `20260919-D00-T01-S38-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```
