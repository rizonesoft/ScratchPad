# Review findings: D00 T01 §13 -- Environment-Gated Ready Queries

Candidates: ship `21c53b2`, self-review fix `ceae321`, panel-response `5c29b83`, panel-response `3b0653b`, panel-response `2a697a4`, panel-response `3f922ce`, post-panel tidy `9a20476`. Scope: `**Requires:**` marker (parse, validate, detect, split, snapshot), §15 proving mark, skill rules in process-plan/process-phase/process-todo-section/add-todo/create-todo.

## Self-review

Does it make sense: item 1 is the `todo/README.md` marker spec (line shape, vocabulary table, semantics, grandfathering) plus the Tooling line; item 2 is the `query ready` now/elsewhere split with `--context` and the `resolve` verdict; item 3 is the two FATAL rules in `todo-validate.py` plus the 24 self-test cases; item 4 is the §15 mark with its run-5 citation; item 5 is the runnable-now rule in both skills. No widening beyond the contract plus the review loop.

Is it logical: the parser splits values from reason on the first ` -- `, last line wins, unknowns kept aside for the validator; the detector grants display-session only to a Windows interactive session; the split, `resolve`, and the snapshot agree that unknown or empty marks never clear; every skill that selects, starts, or authors a row holds elsewhere rows back. Failure paths fail loud (FATAL) or park named (elsewhere), never silent.

Did it break anything else: no consumer parses the old `query ready` summary line (repo-wide grep empty); the snapshot payload gains no keys, only a narrower `first_ready`; `process-todo-section` gains a stop rule for direct invocation; `add-todo`/`create-todo` gain one prompt word each. `validate`, self-test, and `plan --check` stay green.

Fix applied before the lenses (`ceae321`): the `requires-unknown` README row omitted the empty-values case the rule also refuses.

## Opus panel (round 1, candidates `21c53b2` `ceae321`)

**adversarial: needs-attention**: the detector accepts any non-empty SESSIONNAME, so session 0 (`Services`) and headless CI runners read as display-session and §15 offers where no pixel renders; no detector case covers a non-interactive Windows session.

**consistency: needs-attention**: the `resolve` `requires` label pads one column right of `needs`; process-plan still concludes no-ready-row means every leftover is blocked (lines 48, 70) under a split where elsewhere rows are not blocked; the new rule sits numbered `8d` above the pre-existing `8b`.

**integration: needs-attention**: `_campaign_snapshot` computes `first_ready` from exit 0 alone, handing out §15 in the operator JSON; the canonical run-guard prompt resumes on any printed ready row (elsewhere rows print) and its completion condition is unreachable for an elsewhere leftover.

**record: needs-attention**: item-3 evidence claims five messages where the fixture emits four FATAL lines across three texts from two rules; item 4's tick deleted its `Done when` instead of appending `Done:`.

## Round-1 responses

R1-A1 (session 0): detector excludes `Services` case-insensitively with a docstring note; self-test gains the session-0 case (5 detector). R1-C1: both `requires` prints pad to column 11 (proven aligned by byte readout). R1-C2: both process-plan conclusions admit runnable-elsewhere leftovers. R1-C3: rule 8d moved after the 8b block with a lands-last note. R1-I1: new `requires_missing_for_ref` helper (mirrors `needs_for_ref`, unknown sentinel included) filters the snapshot: Phase 1 `first_ready` moves from §15 to §5 (quoted before/after). R1-I2: guard prompt resumes on runnable-now rows only, parks with blocked-or-elsewhere leftovers, and the re-evaluation sentence says dependency-ready. The live guard cron keeps its old text until closeout (harmless: Phase 0 has no elsewhere rows). R1-R1: evidence reads two rules, three texts, four lines, plus a both-FATALs case (7 validator). R1-R2: item-4 `Done when` restored verbatim from the pre-tick text. All in `5c29b83`; suite 444/444.

## Opus panel (round 2, candidate `5c29b83`)

**adversarial: needs-attention**: the split reads `requires` only, so a misspelt value lists the row runnable-now while `resolve` prints INVALID and the snapshot holds it out; verified live with a `display-sesion` typo (reverted); no split case covers an unknown value.

**consistency: needs-attention**: the README detector prose and table omit the session-0 exclusion the code enforces; the snapshot helper docstring claims a symmetry with `query ready` the unknown path breaks.

**integration: advisory**: the three-endings lines in process-plan and process-phase still state the pre-split park condition.

**record: approve**: no live findings.

## Round-2 responses

R2-A1: unknowns join `missing` as `unknown:<v>` and the `requires` display, so they park in every context (no declared context can name them); new split case proves §1 parks in a display context. R2-C1: README prose and table name the interactive-session rule with the `Services` exclusion. R2-C2: moot after R2-A1, the symmetry holds, docstring stands. Advisories fixed too (three-endings lines in both skills). All in `3b0653b`; suite 445/445.

## Opus panel (round 3, candidate `3b0653b`)

**adversarial: approve**: no live findings.

**consistency: needs-attention**: process-phase line 8 still states the pre-split park condition against lines 3 and 10; line 46's "ship every exit-0 row" contradicts line 48's whatever-the-exit-code leftover rule on §15, which resolves exit 0.

**integration: needs-attention**: the step-3 skip rule is exit-code-only, so the ship loop hands the exit-0 elsewhere row to `process-todo-section`; the verdict check lives only in the step-1 bullet, not the step that picks the row.

**record: approve**: no live findings.

## Round-3 responses

R3-C1: line 8 admits runnable-elsewhere parks. R3-C2: line 46 ships exit-0 runnable-now rows only. R3-I1: the skip rule skips non-exit-0 or runnable-elsewhere verdicts. Repo-wide `blocked` sweep over both skills confirms no further pre-split condition. All in `2a697a4`; gates re-run green.

## Opus panel (round 4, candidate `2a697a4`)

**adversarial: advisory**: a no-values mark is FATAL to validate but reads marked-and-met to `resolve` and the split (both test unknowns only); verified live (reverted); the §3 fixture is exercised only through `cmd_validate`.

**consistency: advisory**: `AGENTS.md` still pins 422 self-test cases against the live 445.

**integration: advisory**: `process-todo-section` gates `Started:` on `needs` but has no `requires` clause, so a direct invocation starts §15 on a display-less host.

**record: advisory**: item-3 evidence reads `445/422`, which in this file's notation asserts more passing than exist.

## Round-4 responses

All four fixed in `3f922ce`: no-values marks read INVALID/unmet in all three consumers with a split case proving §3 parks in a display context (6 split, suite 446/446); `AGENTS.md` pins 446; `process-todo-section` gains the `requires` stop rule; evidence reads `446 green (422 before; +24 ...)`.

## Opus panel (round 5, candidate `3f922ce`)

**adversarial: approve**: no live findings.

**consistency: advisory**: `FIDELITY_EXEMPT_RE` sits on the line after `detect_context`'s `return` with no blank-line separation.

**integration: advisory**: `add-todo` and `create-todo` enumerate birth markers without `Requires:`, so a new gated section files unmarked.

**record: approve**: no live findings.

## Round-5 responses

Both advisories fixed as post-panel tidy in `9a20476` (blank-line separation; one prompt word in each authoring skill). Cosmetic and doc-only; gates re-run green (446/446, validate silent). No sixth round: advisory is noted, not blocking, and the cap is five.
