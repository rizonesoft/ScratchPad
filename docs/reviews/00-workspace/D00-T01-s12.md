# Review findings: D00 T01 §12 -- Repo-Managed Git Hooks Gating TODO Edits

Candidates: ship `01fb330`, self-review fix-forward `d4e1f9c`, lens fixes `2fa3f30`, lens fixes `5a8a2a2`. Scope: pre-commit hook gating TODO edits, provisioner auto-wiring, bootstrap one-time step, CI artifact assert.

## Self-review

Does it make sense: item 1 is `tools/githooks/pre-commit` validating the staged tree plus the provisioner wiring and the `plan.yml` artifact assert; item 2 is the bootstrap Git-hooks section; item 3 is the ship commit with the prescribed message. The provisioner auto-wire implements item 1's parenthetical under the §6 script-first decision; the CI assert answers a lens finding inside this review. No widening beyond the contract plus the review loop.

Is it logical: the hook resolves the work tree, probes python3/python/py with a per-candidate version check, extracts the hook-visible index to a temp dir under the git dir, refuses when validate is red, and warns (never blocks) when unstaged changes carry findings the staged tree does not. Every failure path exits loud with its own message; the last line always exits 0 by construction. The drift warning fires exactly when unstaged modifications carry findings (proven by elimination: staged green excludes HEAD and staged content, the validator is deterministic).

Did it break anything else: provisioners gain a guarded block that can only warn (sh proven on three paths, ps1 lines executed on real Windows PowerShell); bootstrap gains one section plus one prereq row; plan.yml gains trigger paths plus one assert step; `.gitattributes` gains one pin. No caller reads `core.hooksPath` or `tools/` besides the new code. `validate`, self-test, and `plan --check` stay green.

Fix applied before the lenses (`d4e1f9c`): the sh wiring block warned instead of dying under `set -e` on git-config failure, symmetric with the ps1 branch. Final-bytes re-read fixed the header comment (FATAL wording now says validation) and gave the `cd` failure its own message.

## Opus panel (round 1, candidates `01fb330` `d4e1f9c`)

**adversarial: needs-attention**: the extensionless hook is not `.gitattributes`-pinned, so an autocrlf=true Windows checkout is CRLF and the hook dies before validate runs; the gate reads the worktree, never the commit, so a staged FATAL commits (false pass) and an unstaged FATAL blocks a clean commit (false block); validate exits 1 on a new WARN too but the hook message says FATALs; `python` is preferred over `py`, so a Python-2 python misreports as a red graph.

**consistency: needs-attention**: the pin miss against the `*.sh`/`*.ps1` precedent; bootstrap "carries a FATAL" understates the WARN exit; "provisioned before 2026-09-17" is off by one (ship commit `01fb330` is 2026-09-18 +0200).

**integration: needs-attention**: wiring runs after the fallible SDK download, so a failed provision leaves no gate; nothing asserts the hook stays present, executable, and LF.

**record: needs-attention**: the item-2 Done names no OS (cold proof was Linux-only) while citing the Windows prereq row; the worktree-read scope was recorded only in the commit body, not the section; `d4e1f9c` stands unrecorded against one-section-one-commit.

## Round-1 responses

R1-A1 (CRLF): reproduced (autocrlf=true clone checks out `#!/bin/sh\r\n`, direct run exits 127). Fixed with `tools/githooks/** text eol=lf`; re-proven by autocrlf=true checkout (od shows `h \n`) and refusal on real Windows git (W1).

R1-A2 (staged scope): reproduced both directions pre-fix (staged FATAL landed as `fa3a4e3`; unstaged FATAL printed the refusal). Fixed with a temp `checkout-index` of the hook-visible index under the git dir (native path form on every platform, no residue by trap). T1 staged FATAL refused exit 1; T2 clean staged commit allowed exit 0 with the warning; T3 clean commit 1s with no warning; T4 path-limited refused, which disproved the assumed residual: `--include` presents the listed worktree files in a temporary index (`HOOK-IDX=.../.git/next-index-*.lock`, measured) that the hook reads like any index, and a refusal discards it (real index shows unstaged-only after).

R1-A3 (WARN wording): hook message, header comment, and bootstrap now say findings/red instead of FATALs; gate semantics unchanged (mirrors the §7 CI exit).

R1-A4 (broken python): probe is now a loop with a per-candidate version check, falling through broken entries; only no-working-interpreter blocks, with the doc pointer. Windows measured the need: the `python3` stub fails the check, the loop uses `python` 3.14.4, W1 refuses with FATAL text.

R1-C1: same pin as R1-A1.

R1-C2: bootstrap line reworded to staged tree plus FATAL-or-uncovered-warning.

R1-C3: timeless sentence (the provisioner's own `git hooks wired to tools/githooks` output is the test, no dates).

R1-I1: wiring moved before the download in both provisioners (sh after the prereq check, ps1 after `$Root`); sh block re-run green after the move, ps1 moved block re-run green on real PowerShell; full e2e rides CI since both cache keys hash the provisioners.

R1-I2: `plan.yml` asserts index mode 100755 plus blob LF and triggers on the hook paths plus `.gitattributes`; proven positive locally and negative (index removal fails the step); full CI proof rides the stamp push.

R1-R1: Windows proven end to end via git.exe (W1 fixture refused exit 1 with FATAL text, W2 clean commit passes exit 0); item-2 Done names Linux plus Windows.

R1-R2: §12 carries a Decided line (staged scope, temp-index mechanism with the T4 measurement, CI backstop, cost of changing).

R1-R3: all candidates are listed in the stamp Review line per the review skill (precedent: the five-candidate §9/§10/§11 stamps); checklist item 3 names the ship commit per file convention.

## Opus panel (round 2, candidate `2fa3f30`)

**adversarial: approve**: no live findings.

**consistency: advisory**: `tools/githooks/pre-commit:3` header claims "never the worktree" while line 23 validates the worktree for the drift probe.

**integration: needs-attention**: the assert cannot detect pin removal (blob stays LF under `text=auto`, mode stays 100755; `git check-attr eol` is the discriminating check); sh wiring still sits behind the prereq hard exit while ps1 wires first, asymmetric failure surfaces.

**record: needs-attention**: the warn-and-continue wiring failure appears in no checklist line, Decided line, or checkpoint; the Commit line still names one commit while three landed, with `d4e1f9c` only in the untracked findings file.

## Round-2 responses

R2-C1 (comment scope): header reworded to advisory-probe-only (never a refusal).

R2-I1 (pin assert): added the `git check-attr eol` line (lf versus unspecified measured); proven positive plus pin-removal negative and index-removal negative; mode and blob checks kept as defense in depth (bit drops, a future CRLF mis-pin).

R2-I2 (wiring first): sh block moved above the prereq check, symmetric with ps1; `bash -n` plus snippet happy-path re-run green.

R2-R1 (warn path): kept warn-and-continue (SDK-first, tarball and no-git cannot wire) and recorded it in a second Decided line plus the item-1 Done tail, with the bootstrap step as repair and the fail-closed cost noted.

R2-R2 (candidate list): item 3 now carries the running list (ship plus fix-forwards with roles); the stamp Review line finalizes all candidates.

## Opus panel (round 3, candidate `5a8a2a2`)

**adversarial: approve**: no live findings.

**consistency: approve**: header matches the drift probe; bootstrap repair matches the Decided line; Decided date frame correct.

**integration: approve**: check-attr line discriminates (lf versus unspecified); trigger paths cover the pin; graph gates green.

**record: advisory**: item 3 lists three candidates while four landed; `5a8a2a2` must gain its place in the stamp commit.

## Round-3 response

R3-R1 (candidate list): `5a8a2a2` added to item 3 in the stamp commit per the advisory; the stamp Review line finalizes all four candidates.
