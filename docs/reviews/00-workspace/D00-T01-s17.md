# Review: D00 T01 §17 -- Third Plan-Review Residuals

Candidate: `ffef531` (ship) + `44b90ff` (review R1 fix) + `d7101a3` (review R2 fix) + `9db9176` (review R3 fix). Tier: full panel (diff touches shared contract surface: `scripts/`, skills). Lenses owed: adversarial, consistency, integration, record (source-defect/design not owed: no Notepad behavior or surface at stake).

## Self-review

Worked the three questions across the diff and its blast radius; no fixes.

- Sense: all 20 items land as specified plus the commit. Item 1 takes the rerun-renumber option (documented default, no shape change); item 10 states the transition-order boundary (git backstop, §9 precedent) instead of claiming unprovable enforcement; item 13's §15/§16 notes are Why-appendices with stamped checklists untouched; the AGENTS self-test count rides along (484 to 532: 527 at ship, 3 checker-shape cases in the R1 fix, 2 quote-shape cases in the R2 fix; required hygiene, disclosed in the commit).
- Logic: grammar checks run before the outage skip (grammar binds every marker); last-governs falls out of parser overwrite; clearance fail-closes on undated reviewers (max sentinel), same-day stamps, and missing back-links; the LIKE three-way is probed 30/30 with both residuals stated; dup-ID versus multi-target resolves coherently (single-row-multi-ref legal with ALL-clearance, split rows illegal); rule 19 is file-scoped unlike 17b because the missing back-link is identical for every reporter; reopen voids via post-process with rule 7 cascading honestly.
- Blast radius: parser changes additive (Reopened kind, field, verified-discard touching only sections with Reopened lines: none live); plan-health additive except the documented retry_owed-to-degraded replacement; STAMP_RE and SEVERITY_MAP extended append-only; 532 cases green (48 new: 43 at ship, 3 checker-shape cases in the R1 fix, 2 quote-shape cases in the R2 fix); live validate silent; live plan-health quoted (3 stale with 0 removed, legacy empty, 53 grandfathered, 0 criticals/majors).

## Live proof

Build gates quoted from the candidate run (first record under the item-17 rule):

- `python3 scripts/todo-graph.py self-test` → `todo-graph self-test: 527 cases, 0 failed` at ship, `530 cases, 0 failed` after the R1 fix (3 checker-shape cases), `532 cases, 0 failed` after the R2 fix (quote plus backtick cases), `535 cases, 0 failed` after the R3 fix (count positions plus repeat-lens)
- `python3 scripts/todo-graph.py validate` → `22 todos, 183 sections -- 0 fatal, 0 warning(s)`
- `python3 scripts/todo-graph.py query plan-health` → 2 marked, 0 unmarked; 3 stale (s14 +§15+§16, s15 +§16+§17, s16 +§17), 0 removed; legacy empty; 53 grandfathered; 0 criticals, 0 majors
- `python3 scripts/todo-graph.py query plan-health --json` → `schema: plan-health/1`, 12 dimensions
- LIKE three-way battery → 30/30; prompt-helper battery → 12/12; R1 panel output → `check-panel PASS` (raw output fenced below, so the claim has an artifact)

## Opus panel (round 1)

- `adversarial` needs-attention: (1) shipped tags use `$RANDOM` (bash-only, empty under sh as this round's own prompt proved, 15 predictable bits) while the proven 64-bit `unique_tag` is called by neither template: evidence and artifact disjoint. (2) `check_plan_output` gates on a `- ` prefix the plan prompt never asks for, so a compliant bare/numbered round FAILs and burns the rung. (3) `_PANEL_LINE_RE` false-FAILs legal rounds both ways: it rejects `**adversarial** approve` (a shape rule 16 accepts) and it reads a detail line quoting another lens as a repeated verdict.
- `consistency` needs-attention: the README stamp example carries `no findings`, `filed`, and `outage:` on one line, FATAL three times over under the grammar documented eight lines below; split the alternatives.
- `integration` approve
- `record` needs-attention: (1) item 17's Done claims the record demonstrates Live proof, but s17.md is untracked with no `Live proof` section: a forward promise, not an artifact. (2) `unshaped` is a `dict[str, bool]` only ever set True with a dead guard; a set carries the same information.

Fix: `44b90ff` (templates take tags from the `tag` subcommand; plan prompt mandates `- `; panel prompt mandates header verdicts with the checker accepting bold-no-colon, keeping quoted mentions as details, failing dash-opened lines; README example split; s17.md lands with Live proof; unshaped dict becomes a set).

Raw round-1 output (fenced, not part of the verdict scan):

```
**adversarial: needs-attention**

1. `.claude/skills/review-todo-section/SKILL.md:87` and `:138` — the shipped delimiter tags are `PANEL-$(date +%s)-$RANDOM` / `PLAN-$(date +%s)-$RANDOM`. `$RANDOM` is a bash-ism: under `sh`/dash it expands empty, so the tag degrades to a bare Unix timestamp (`sh -c 'echo PANEL-$(date +%s)-$RANDOM'` → `PANEL-1789732975-`), and the panel prompt this round was actually invoked with carries exactly that empty-suffix shape (`PANEL-1789732911-`). Even in bash, `$RANDOM` is 15 bits over a predictable second. Item 14's Done claim ("both prompt templates generate per-prompt tags") therefore does not hold for the templates that run, while the 64-bit `unique_tag` the fixture proves (`scripts/todo-graph.py:5888`, `scripts/review_prompt.py:22`) is called by neither template. The evidence and the artifact are disjoint.

2. `scripts/review_prompt.py:88` — `check_plan_output` rejects any line not starting with `- `, but the plan-review prompt it gates (`SKILL.md:139`) asks only for "one line: the gap, where it belongs, and why it matters. No other text." It never asks for a `- ` prefix. A fully compliant round emitting bare or numbered one-line findings (`Gap: X belongs in §2 because Y`, `1. Gap: ...`) returns FAIL, which `SKILL.md:140` and the fallback paragraph define as runner failure, discarding a good review and burning the rung. Verified: both forms return `(False, 'line 1 is not a `- ` finding: ...')`.

3. `scripts/review_prompt.py:16-19` — `_PANEL_LINE_RE` is both too narrow and too loose against real panel output, so `check-panel` false-FAILs legal rounds. (a) It rejects `**adversarial** approve` (bold marker, no colon) — precisely a shape `stamp-no-opus-panel` accepts (`todo/README.md:384`: marker `*`/backtick/`>`/`-`, mid-line mentions never count), returning `line 1 precedes the first verdict`; the two acceptance shapes must agree because the record is transcribed from this output. (b) It matches lens-plus-verdict text inside a *detail* line, so a finding that quotes another lens (`- \`consistency: approve\` is wrong in doc`) is read as a repeated verdict and the round FAILs (`line 3 repeats the consistency verdict`). Both verified by direct invocation.

**consistency: needs-attention**

4. `todo/README.md:144` — the canonical `Plan review:` stamp example now reads `GPT high, no findings (or: filed D00 T01 §16; degraded states carry owner and due: outage: both rungs (owner ann, due 2026-09-25))`, which carries `no findings`, `filed`, and `outage:` on one line. Under the grammar this same diff documents eight lines below (`:152`) and in the severity table (`:387`), and as `scripts/todo-validate.py:626-647` implements it, that marker is FATAL three times over (no-findings-beside-filings, outage-beside-filings, outage-beside-no-findings). The dictionary entry's own example is an illegal marker; split the alternatives into separate example lines.

**integration: approve**

**record: needs-attention**

5. `todo/00-workspace/TODO-01-repo-and-toolchain.md:459` (item 17) — Done reads "this section's own record demonstrates it", but `docs/reviews/00-workspace/D00-T01-s17.md` is untracked (absent from candidate `ffef531`) and its headings are `# Review: D00 T01 §17` plus `## Self-review` only: no `Live proof` section exists yet. The item's evidence is a forward promise, not a candidate-bound artifact, which is exactly the failure the item was filed to close.

6. `scripts/todo-graph.py:1496`, `:1564`, `:1629` — `unshaped` is a `dict[str, bool]` whose values are only ever set to `True`, so the `if not flag: continue` guard at `:1629` is dead code and the mapping carries no information a `set` would not. It reads as if unshaped-ness were a tri-state the query tracks.
```

## Opus panel (round 2)

- `adversarial` needs-attention: `_PANEL_LINE_RE` still reads a detail line as a verdict whenever the quoted lens is not dash-opened: a finding quoting a header verbatim (`**record: approve** claim is stale`) matches, so a legal round FAILs and burns the rung, the exact failure R1 claimed to close; the R1 fixture passes only because its detail is dash-opened.
- `consistency` needs-attention: (1) the comment claims dash- or quote-opened lines are details, but backtick-opened lenses pass: the quote half is false. (2) The split README examples stack three markers on one stamp, so the governing example marker is an unresolved outage: a degraded review documented as the normal case; label the alternatives instead.
- `integration` approve
- `record` needs-attention: (1) the Live proof PASS claim points at an ephemeral `/tmp` file with no artifact (item 22's own defect): the transcription in-file would FAIL the shipped checker. (2) No `Fix:` line or candidate attribution for the R1 fix, breaking the sibling record convention. (3) Self-review counts left at ship values while AGENTS and the live run read 530.

Fix: `d7101a3` (verdict lines end-anchored and `**`-required with bare headers mandated; backtick opener off-shape; README alternatives unstacked; raw R1/R2 outputs fenced with a skill mandate; counts reconciled).

Raw round-2 output (fenced, not part of the verdict scan):

```
**adversarial: needs-attention**

- `scripts/review_prompt.py:21-24` — `_PANEL_LINE_RE` still reads a detail line as a verdict whenever the quoted lens is not dash-opened. The panel prompt (`.claude/skills/review-todo-section/SKILL.md:89`) mandates `**<lens>: <verdict>**` for verdicts but imposes no shape on detail lines, so a finding quoting a header verbatim (`**record: approve** is wrong in the stamp`) matches the verdict pattern. Probed live: `**adversarial: needs-attention**` / `**record: approve** claim is stale` / three approves → `FAIL line 5 repeats the record verdict (first at line 2)`. A legal round FAILs and burns the rung, which is the exact failure mode the round-1 finding (3) claimed to close; the new fixture at `scripts/todo-graph.py:5929-5936` passes only because its detail line happens to be dash-opened.

**consistency: needs-attention**

- `scripts/review_prompt.py:19-20` — the comment states "dash- or quote-opened lines are details, never verdicts", but the pattern's `` `? `` accepts a backtick-opened lens: probed, `` `adversarial` approve `` plus three bold approves returns `PASS four lenses, one verdict each`. Only dash-opened lines are excluded; the quote half of the comment is false.
- `todo/README.md:144-146` — the split fixes the FATAL, but the three markers now stack on one stamp with nothing marking them as alternatives, and under the grammar documented at `todo/README.md:154` ("the last marker line governs") the example's governing marker is an unresolved `outage: both rungs (owner ann, due 2026-09-25)`. The canonical stamp example therefore documents a degraded review as the normal case. Label the alternatives (separate one-line examples, or an "or:" gloss per line) instead of stacking them in one stamp block.

**integration: approve**

**record: needs-attention**

- `docs/reviews/00-workspace/D00-T01-s17.md:15` — "R1 panel output → `check-panel PASS`" is contradicted by the artifact in the same file. The R1 verdicts transcribed at lines 23-26 are dash-opened (`- \`adversarial\` needs-attention: ...`), and fed to the shipped checker they return `FAIL line 1 precedes the first verdict` (the shape the new fixture at `scripts/todo-graph.py:5937-5945` deliberately rejects). Either the raw output differed from the transcription (in which case it is not stored, so the claim has no artifact, which is item 22's own defect) or the PASS claim is false.
- `docs/reviews/00-workspace/D00-T01-s17.md:3` and end of file — no `Fix:` line naming the R1 fix candidate `44b90ff`, and the `Candidate:` line names only `ffef531`, breaking the sibling record convention (`docs/reviews/00-workspace/D00-T01-s16.md:20` `Fix: \`c20f8dc\` (...)`; `docs/reviews/00-workspace/D00-T01-s15.md:3` `Candidate: \`a643242\` (ship) + \`6d0957c\` (review self-fix)`). The round-1 fixes are unattributed in the record they answer.
- `docs/reviews/00-workspace/D00-T01-s17.md:9` and `:28` — self-test counts left at ship values (`484 to 527`, `527 cases green (43 new)`) while `AGENTS.md:54` and the live run now read 530; line 15 states the new count, so the same file carries both without the two prose sites being reconciled.
```

## Opus panel (round 3)

- `adversarial` advisory: the count position is accepted asymmetrically (`(2)` inside the closer passes, outside fails), yet the stamp's `Review:` line requires per-lens counts and the prompt forbids counts entirely while the regex admits one form: prompt and checker disagree on whether a count is legal. Per stop-and-re-think (unit patched R1+R2), the resolution is one decision about counts with a fixture per side, not a third blind patch.
- `consistency` approve
- `integration` approve
- `record` needs-attention: item 15's Done claims a repeat-lens fixture that does not exist (nine panel fixtures, none repeating a lens), and the empty/trailing-prose halves are plan-side only, which the claim does not distinguish.

Fix: `9db9176` (finding counts accepted in either header position, prompt and code together, one fixture per side; repeat-lens fixture added; item-15 claim split by side).

Raw round-3 output (fenced, not part of the verdict scan):

```
**adversarial: advisory**

`scripts/review_prompt.py:26-27` — the count position is accepted asymmetrically, and the risky side is the one the record convention produces. Probed live: `**adversarial: needs-attention (2)**` plus three approves → `PASS`; `**adversarial: needs-attention** (2)` plus three approves → `FAIL line 1 precedes the first verdict`. The stamp's `Review:` line requires a per-lens finding count (`todo/README.md:143`, `` `integration` needs-attention (1) ``), so a panel that reports counts in the ordinary Markdown position burns the rung on a legal round. The prompt (`.claude/skills/review-todo-section/SKILL.md:89`, "with nothing else on the line") forbids counts entirely, yet the regex deliberately admits one form, so prompt and checker disagree on whether a count is legal at all, and the R2 residual note at `scripts/review_prompt.py:21-24` names only the exact-header-quote case. Advisory rather than blocking because this unit (`_PANEL_LINE_RE`/`check_panel_output`) has now been patched in R1 and R2: the stop-and-re-think clause at `.claude/skills/review-todo-section/SKILL.md:79` and `:111` applies, so the resolution is one decision about counts (accept both positions or forbid both, prompt and code together, with a fixture per side), not a third consecutive patch.

**consistency: approve**

**integration: approve**

**record: needs-attention**

`todo/00-workspace/TODO-01-repo-and-toolchain.md:458` — item 19's Done claims "fixtures lock trailing-garbage, missing-lens, repeat-lens, empty, and trailing-prose failures", but no fixture exercises the repeat-lens branch. The nine `check_panel_output` fixtures are at `scripts/todo-graph.py:5909` (four approves), `:5917` (details under needs-attention), `:5925` (bold-no-colon), `:5933` (quoted lens detail), `:5941` (header quote with trailing prose), `:5949` (backtick opener), `:5957` (dash opener), `:5965` (trailing garbage), `:5973` (missing lens); none repeats a lens, so the `line N repeats the <lens> verdict` path at `scripts/review_prompt.py:69` is unfixtured. The `empty` and `trailing-prose` halves of the same claim are covered only on the plan side (`scripts/todo-graph.py:5983`, `:5988`), which the claim does not distinguish. The claim survived two fix rounds unchanged and rides into the stamp as proven coverage.
```
