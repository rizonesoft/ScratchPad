# Review: D00 T01 §43 -- Findings Count Touch-Up

Candidate: `d1a9460` (SHIP) plus `32fbca8` (self-review fix). Full panel (shared contract surface: findings files are validator-consumed records): Sol rounds 1-2, Opus round-3 sign-off.

Arch trigger: none - prose-only tally reconciliation plus dated annotations; no editor, storage, extensibility, AI, protocol, or consent surface changes.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. One fix; asked clean after.

Does it make sense: each shipped item maps to its edit (s20.md header 6/9/3, adjacent dated Note on the §20 stamp, two inline annotations on the §24 Why line, §37/§38 parenthetical completions to 2+3+3+8=16). Exactly the corrected contract; the item-3/checkpoint scope correction (§20 Why carries no tally) rides in the candidate.

Is it logical: the header matches the re-derived row parse (6 filed, 9 rejected, 3 duplicate); the stamp Note names the old counts plus the PR11 parenthetical reason; the §36 siblings check out both ways (§37's three are §38 PR11 PR15 plus §55 PR7; §38's three are §37 PR1 PR6 plus §55 PR7); shipped prose keeps its bytes with dated notes beside it. One fix: Started read 17:42:00Z (after the SHIP commit, in the future); the session log pins first resolve at 16:29:41Z, recorded in `32fbca8`.

Did it break anything else: no code touched; ledger rows untouched (header sits outside the block); validate 0 fatal; plan current 81/234 with no box changes; the Note line parses as inert prose beside the stamp.

## GPT panel (round 1)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 15s; outcome approve; tokens unknown

No findings; nothing to disposition.

Raw round-1 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`; run `20260919-D00-T01-S43-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 15s; outcome approve; tokens unknown

No findings; nothing to disposition.

Raw round-2 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`; run `20260919-D00-T01-S43-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` needs-attention: (R3-F1) third §36 citer keeps the incomplete accounting.
- `integration` approve
- `record` approve

Telemetry: round 3; model opus; effort medium; duration 128s; outcome needs-attention; tokens unknown

Round-3 sign-off disposition: R3-F1 is below the blocking bar (prose accounting sum in a shipped Why, no safety/data/stamp risk), so it files at D00 T04 §1 instead of re-rounding. No live findings remain; this round is FINAL.

Final disposition table:

| ID | Disposition | Evidence |
| R3-F1 | filed | D00 T04 §1 |

Architecture gate: not triggered (`Arch trigger: none`, see header).

Raw round-3 output (fenced, not part of the verdict scan; digest `b0349074300c04a5305a7f8638318e33042c732a9de4a4e5400c14b212cdb5d3`; run `20260919-D00-T01-S43-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**

**consistency: needs-attention (1)**

1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1295` -- the third citer of the same §36 round keeps the incomplete accounting the candidate just corrected at its two siblings: `plan-review-D00-T01-s36-2026-09-19-s55 ... (16 findings, 1 filed here, 3 duplicates, 8 rejected ...)` sums to 12 of 16, while the §37 Why (line 1003) and §38 Why (line 1027) now read "2 filed here plus 3 filed at siblings, 3 duplicates, 8 rejected" and both name §55 PR7 as one of those siblings. Same family, one site left: line 1295 needs "1 filed here plus 4 filed at siblings" to sum to 16.

**integration: approve**

**record: approve**
```

## Plan review

Round: `gpt-5.6-sol` high over §43 plus §20 plus §24 plus §37, 2026-09-19. Neighbors: §20 (Depends, filed-from), §24 (XREF), §37 (XREF). Dependents: none by the mechanical rule (no Depends row names §43; citers are upstream filers). Runner note: run via the skill template; output `check-plan PASS` (9 findings, one per line; re-verified at transcription against the ledgered artifact). Run note: plan run `20260919-D00-T01-S43-gpt-r3`, digest `sha256:796cb72bd3fc056c3daf28a42ac83ed3f7b61fd1d4f003c359bbfada5960daa1`, receipt ledgered under ignored `build/review-runs/`; bytes 23256 counted live over the prompt via `canonical_prompt_bytes`. No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §43, D00 T01 §20, D00 T01 §24, D00 T01 §37]; dependents [none]; bytes 23256; run 20260919-D00-T01-S43-gpt-r3

Ledger (namespaced IDs per the §16 shape; 1 of 9 filed at D00 T04 §1, 2 accepted in the §43 stamp run, 1 duplicate, 5 rejected with reasons on their rows):

Ledger:
- [D00-T01-S43-PR1] [minor] Tallies need a validator rule -> filed D00 T04 §1
- [D00-T01-S43-PR2] [minor] §43's §37 accounting sums short -> accepted in the §43 stamp run: Why now reads 1 filed here plus 8 filed at siblings (2 at §39, 6 at D00 T04 §1), summing 1 plus 8 plus 4 plus 13 to 26
- [D00-T01-S43-PR3] [minor] §43 lacks a stamp -> rejected review in progress: the stamp lands at step 8 of this run; the row stays [ ] and the plan unflipped until then
- [D00-T01-S43-PR4] [minor] Checkpoint lacks exact commands -> duplicate D00-T01-S37-PR16
- [D00-T01-S43-PR5] [minor] Stale-copy search too narrow -> accepted in the §43 stamp run: repo-wide `10 rejected` sweep over todo/ docs/ .claude/ (11 hits) shows 3 annotated §20 sites, the intentional past-tense §43 description, and 7 own-review counts (5 §41, 2 §33); no surviving copy
- [D00-T01-S43-PR6] [minor] §36 repair splits scope identity -> rejected the joiner pattern is the designed mechanism (item marked joiner, SOURCE names S37-PR15, SHIP body names the work); a split strands a two-line change
- [D00-T01-S43-PR7] [minor] T04 XREF dangles -> rejected already satisfied: the XREF pair runs both ways and the T04 item names R3-F1; T04 §1 sat outside the review manifest
- [D00-T01-S43-PR8] [minor] query run should emit canonical totals -> rejected disproportionate: the stamp-plus-Note pattern is the designed mechanism and query run's contract is runs/markers, not finding tallies; no operator incident
- [D00-T01-S43-PR9] [minor] Totals should be generated projections -> rejected PR1's filed rule covers the drift mechanically; projection machinery is unscoped with no consumer named
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `sha256:796cb72bd3fc056c3daf28a42ac83ed3f7b61fd1d4f003c359bbfada5960daa1`; run `20260919-D00-T01-S43-gpt-r3`):

```
- Recurrence prevention is unowned; add a validator rule in D00 T01 §43 that derives disposition totals from ledger rows and rejects mismatching headers, stamps, and summaries, because another parenthetical can silently recreate the same counting error.
- §43’s §37 source accounting is incomplete; amend §43’s Why to state 9 filed total, including 1 here and 8 at §39/D00 T04 §1, because its current 1 filed + 4 duplicate + 13 rejected accounts for only 18 of the claimed 26 findings.
- Completion evidence is missing; D00 T01 §43 needs its own Verified, Review, CRUD, Duration, candidate range, and validation results before its all-checked state can legitimately synchronize into the implementation plan.
- The checkpoint is not executable enough; D00 T01 §43 should name exact bounded commands or fixtures for ledger parsing, repository-wide stale-tally search, validation, and plan checking, because “falsifiable by any tally” does not prove those checks ran.
- The stale-copy search is too narrow; D00 T01 §43 should require a repository-wide scan for the old 10/2 tally and the affected source/run identifiers rather than relying on two known sites and a brittle line-number claim, because copied summaries may survive elsewhere.
- Section scope and commit identity disagree; either split the §36 accounting repair into its own section or broaden §43’s title and commit message beyond “reconcile §20 findings counts,” because the candidate also changes §37 and §38 records.
- The D00 T04 §1 cross-reference in §43 is dangling context; tie it to a specific finding/accounting item or remove it, because an unexplained review-filing link is not auditable or clearly bidirectional.
- Corrected counts remain awkward for consumers; extend D00 T01 §24’s `query run` contract to emit canonical row-derived disposition totals plus historical corrections, because operators should not manually reconcile a stale Verified line with a later note.
- Count authority is duplicated across prose, stamps, and source summaries; define one structured ledger-derived projection in D00 T01 §43 and treat human-readable totals as generated output, because this reduces drift and gives the review system a premium, trustworthy audit trail.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `95552de`: `todo-graph self-test: 1100 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `95552de`: `28 todos, 239 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --sync` on `95552de`: `synced 234 row(s) -- 81 complete (35%), 0 box(es) changed`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `95552de`: `implementation plan is current -- 81/234 sections complete (35%)`, exit 0.

Provenance: candidate 95552de29e86973ebef9807536d9c1166b37073b; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --sync && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 0fca84cba05e2af97fb4c2e6e5ac566576c9ab6637768ace437699b4f76b3246; path todo/00-workspace/TODO-01-repo-and-toolchain.md; run 20260919-D00-T01-S43-gpt-r3

(Digest over the self-test line, the validate line, the sync line, and the plan line, joined by newlines with a trailing newline. Observed in a clean worktree at the candidate; the sync reported 0 boxes changed.)
