# Review: D00 T02 §4 -- ACP Loopback Fixture

Candidate: `6fdfda3` (fixture plus Protocol suite) plus in-review fix `b505081` (rejection hardening). One fix cycle from self-review evidence; lenses clean.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Fixture app | `tests/Fixtures/AcpLoopback/` (`net10.0` console, spawned via the .NET host) answers `initialize`, `session/new`, `session/prompt` from `Scripts/prompt-turn.json`; `LoopbackTurnTests` drives a full turn (init, session/new, 2 streamed chunks, `end_turn`) green on Linux and Windows in CI |
| 2. Faults | `--emit-garbage`, `--drop <method>`, `--chunk-delay-ms` plus 3 `LoopbackFaultTests`: garbage skipped (count 1) with turn completing, dropped prompt surfacing `TimeoutException` with the process alive, 300 ms chunk delays still completing |
| 3. Validation | Envelope checks plus per-method `ValidateParams` coded from the v1 schema; violations exit 2 with stderr (malformed JSON, non-object, envelope), bad params get `-32602`, unknown methods `-32601`; 4 `LoopbackValidationTests`; a mutation probe (fixture accepting a sessionId-less prompt) turned the validation test red, then green on revert |
| 4. D03 consumption | Ownership line in §4 (this section owns the fixture; D03 T01 and D03 T02 consume and extend it); back-references in D03 T01 (Current state, Inputs, §2 checkpoint, pre-existing) and D03 T02 (new Inputs line) |
| 5. Commit | `6fdfda3`, subject matches the item exactly |

Runs: `34807053331` green both jobs with Protocol 7/7, `34807778662` green both jobs with Protocol 8/8 (plus Smoke 1/1, Unit 2/2, UI 3/3 throughout). Local: full neutral solution green (Protocol 8/8). `main` is green at HEAD.

## Self-review

1. Does it make sense? Yes. Every item has its change; the `tests/Protocol/` driver is the scaffolding item 1's done-when requires, and it stays in the suite per the `tests/Common` ownership rule. Self-review found one real defect (valid-JSON non-object input crashed instead of exiting 2, breaking the README contract); fixed in `b505081` with script-field hardening, a `Clone` consistency cleanup, and a new test.
2. Is it logical? Yes. Exit codes 0/2/3 match the README contract; error codes `-32601`/`-32602` match JSON-RPC; the stdin BOM failure found during implementation was fixed on the client side (real ACP clients emit no BOM); timeouts are CI-generous (5 s drop probe, 30 s slow stream); `ExchangeAsync` stashes mid-exchange notifications instead of misattributing them.
3. Did it break anything else? No. Both solutions build with the two new projects; UI 3/3 still green in CI; the only existing-file edits are solution lists plus one doc line; `validate` 0 fatal; `self-test` 391/391.

## Lenses

- `adversarial`: approve. Every hostile input path exits 2/3 or answers a JSON-RPC error; no hangs (client reads all carry timeouts; dispose kills after 5 s); stderr echoes never touch stdout; script paths and flags are test constants, so the quoted spawn line has no injection surface.
- `consistency`: approve. Test naming is PascalCase per the suites; the CA2007 `NoWarn` carries a justification comment matching the CaptureBaseline pattern; pins match Unit (`xunit` 2.9.3, Test SDK 17.14.1); lockfiles committed; Markdown one line per item; no em dashes in new files.
- `integration`: approve. Both solution consumers green on both OSes; D03 refs recorded both directions; `TryTakeNotification` is currently defensive-only (no test drains it yet) but `ExchangeAsync` needs the queue for correctness, so it stays.
- `record`: approve. Two CI run IDs plus local counts quoted; the mutation probe recorded; zero deferrals.
- `source-defect`: approve. Method names verified against the schema page headers (`session/new` wins over a prose `new_session`); newline-delimited stdio from the transports page; `stopReason` enum, `-32601`/`-32602`, and the AgentCapabilities default verbatim from the schema docs; `session/update` nesting matches the prompt-turn example; sessionId-only `session/new` response confirmed conformant (modes and configOptions optional).
- `design`: not owed (test infrastructure only, no app surface).

Advisory (not blocking): non-ASCII chunk text is wire-safe today only because `System.Text.Json` escapes non-ASCII by default; if the fixture ever emits raw text, set `Console.OutputEncoding` to UTF-8 first.

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; full neutral solution green locally (Smoke 1/1, Unit 2/2, Protocol 8/8); CI green both jobs on the candidate HEAD; `plan --check` green after the stamp-flow sync.
