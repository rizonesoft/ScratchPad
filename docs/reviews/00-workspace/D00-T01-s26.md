# Review: D00 T01 §26 -- Grandfathered Migration Execution

Candidate: `5ba4970` + `6d7f11f` + `12e5579` + `b04c585` + `4ec094a` + `45e2e17` (SHIP plus three session fixes plus two panel fixes; output `check-panel PASS` on all four headless rounds). Panel tier: Full (the diff touches `scripts/todo-graph.py`, shared contract surface), Sol rounds 1-2, Opus rounds 3-4.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. Three session fixes landed before the headless panel ran.

Does it make sense: the migration retires all 53 grandfathered stamps across the four batches (13/7/27/6), the retirement scan drains only well-formed notes, and the Current-state block gains its addressable heading. Exactly the contract: no grammar change (prose path, item 2), no behavior outside the scan plus notes.

Is it logical: retirement recognition fails closed (real date, ref naming the section, non-empty reason); malformed notes read as absent so the stamp stays listed. The full-XREF ref check closes prefix collisions (§66 vs §6) and cross-file copies. The plan-health grandfathered leg drains to zero on the live tree.

Did it break anything else: suite moves 786 to 795 by exactly the 9 new probes (drain, fixtures §§66-69, direct ref checks, garbage rejection); live validate stays 0 fatal; plan-health grandfathered reads 0. Blast radius is the query plus validator retirement paths, both covered.

Session fixes: `6d7f11f` (fail-closed recognition plus §§68-69 fixtures), `12e5579` (full-XREF ref validation plus 3 direct probes), `b04c585` (README Retired-note contract plus stale-claim sweep across AGENTS.md and README.md).

## GPT panel (round 1)

- `adversarial` needs-attention: (1) retirement scan used `XREF_RE.search` over the ref field, so surrounding garbage still drained.
- `consistency` approve
- `integration` approve
- `record` approve

Fix record: ref field fullmatches after stripping; regression probe pins the garbage case as staying listed. Fixed in `4ec094a`; gates re-ran green before round 2 (self-test 795/795, validate 0 fatal, plan current). Old-vs-new demonstrated live (search True, fullmatch False on the garbage field).

Raw round-1 output (fenced, not part of the verdict scan; digest `31ab3fb68ea4059322ba36273174d94ca63ca42a3b863c5d49e0a70fdff037c6`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1245` uses `XREF_RE.search()` instead of validating the entire retirement reference field, so malformed values such as `garbage D90 T07 §66 garbage` still drain a grandfathered stamp, contradicting the fail-closed, well-formed-note contract documented at `todo/README.md:163`.
**consistency: approve**
**integration: approve**
**record: approve**
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Fix record: none (all approve).

Raw round-2 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` needs-attention: (1) the `## Current state` heading had no standing in the README file anatomy.
- `integration` approve
- `record` needs-attention: (1) AGENTS.md quoted 794 self-test cases against 795 actual.

Fix record: anatomy template carries the optional heading with its addressability rule; quoted count bumped to 795. Fixed in `45e2e17`; gates re-ran green before round 4 (self-test 795/795, validate 0 fatal, plan current). No validator coupling to the anatomy prose (grep for `Current state` over `scripts/` empty).

Raw round-3 output (fenced, not part of the verdict scan; digest `c5ff5055a7094241214287dd0234d97434f00afeeba73dd4346d734e90110348`):

```
**adversarial: approve**

**consistency: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:13` adds a `## Current state` heading that the canonical File anatomy in `todo/README.md:57-59` does not carry (Goal blockquote, then `> [!IMPORTANT] > **Current state:**`, then `## Inputs`), and the format spec was not amended; the other 23 TODO files keep the unheaded shape (`todo/00-workspace/TODO-02-test-backbone.md:15`, `todo/01-notepad-core/TODO-01-winui-app-spine.md:16`, `todo/01-notepad-core/TODO-02-menus-settings-status.md:16`, `todo/02-editor/TODO-01-editing-surface.md:16`, and the rest), so the file and the spec now disagree about the anatomy.

**integration: approve**

**record: needs-attention (1)**
1. `AGENTS.md:55` states `self-test      # 794 cases, must stay green`; the tree at `4ec094a` prints `todo-graph self-test: 795 cases, 0 failed` (the round-1 fix commit added the `retirement ref rejects surrounding garbage` probe at `scripts/todo-graph.py:9483` without bumping the quoted count), so the only documented case count in the repo is off by one.
```

## Opus panel (round 4)

- `adversarial` approve
- `consistency` advisory: (1) `section_retired` duplicates the lazy load plus span computation from `section_markers`.
- `integration` approve
- `record` approve

Fix record: post-sign-off advisory, filed through `add-todo` as D00 T01 §47 (section-span scan helper) instead of re-rounding, per the skill. No home existed (§33 provenance plus §45 run-inspection scopes checked, neither covers a scan refactor). Panel closed.

Raw round-4 output (fenced, not part of the verdict scan; digest `9d263bc26ad304fa8c5cca7c04aee68f82bffbbc1842e91d571b2ac85676f32c`):

```
**adversarial: approve**

**consistency: advisory (1)**
1. scripts/todo-graph.py:1230-1238 duplicates the lazy `todo_lines` load plus span computation verbatim from `section_markers` (scripts/todo-graph.py:1192-1202); the two copies can drift independently (a span fix applied to one leaves the other scanning the wrong range). No behavioral defect today: both compute identical bounds.

**integration: approve**

**record: approve**
```

## Review notes

Migration section with script plus note-plus-doc touch: the session fixes hardened fail-closed recognition before the panel, the panel's three corrections (garbage drain, anatomy standing, quoted count) all landed before close, and the round-4 advisory filed as tracked work. No panel leftovers. Architecture gate skipped silently (no editor state, storage, extensibility, protocol, or consent surface in the diff).

## Plan review

Round: `gpt-5.6-sol` high over §26 plus §21, 2026-09-19. Neighbors: §21 (XREF both directions: §26 filed from its plan review, §21 consumes the migration); dependents: §21 (XREF consumer; no Depends hop, §21 carries no Depends line). The run minted through the `run-id` genesis path (no prior claims, bare base). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (14 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §26, D00 T01 §21]; dependents [D00 T01 §21]; bytes 11830; run 20260919-D00-T01-S26-gpt

Ledger (namespaced IDs per the §16 shape; 6 of 14 filed at §48 plus one §29 item, 1 duplicate with canonical ID, 6 rejected with reasons on their rows):

Ledger:

- [D00-T01-S26-PR1] [minor] No Verified stamp or review record -> rejected satisfied by the stamp step: stamp, findings file, and Live proof land with the stamp commit; re-verify post-stamp
- [D00-T01-S26-PR2] [minor] Retirement erases review debt without assurance -> rejected grandfathering is the blessed design (§21 risk acceptance), not erased debt: retirement honestly asserts no review ran, evidenced by the dated note plus the cutoff exemption plus the drain to zero
- [D00-T01-S26-PR3] [minor] Same-bar items need conversation context -> rejected in-section reference, not conversation context: the batch A item states owner, evidence, and completion in its own Done line
- [D00-T01-S26-PR4] [minor] Retirement shape omits audit fields -> rejected shape carries date, ref, and reason, sits under the stamp it retires (original stamp is context), actor is owner the operator; no replacement evidence exists because no review ran, which is the claim
- [D00-T01-S26-PR5] [minor] No canonical ledger of the 53 decisions -> rejected a ledger would be a second record of the notes the plan forbids duplicating; the notes live grep-ably under their stamps and the drain-to-zero is the completeness proof (omissions stay listed, fullmatch ref check bars wrong-stamp retirement)
- [D00-T01-S26-PR6] [minor] Prose acceptance names no required fields -> filed D00 T01 §48
- [D00-T01-S26-PR7] [minor] Waiver state past discharge unstated -> filed D00 T01 §48
- [D00-T01-S26-PR8] [minor] Stamp/file/run mapping lives in totals only -> filed D00 T01 §48
- [D00-T01-S26-PR9] [minor] Boundary rules unstated -> filed D00 T01 §48
- [D00-T01-S26-PR10] [minor] No immediate zero-leftover gate -> filed D00 T01 §29
- [D00-T01-S26-PR11] [minor] Owner recorded as generic operator -> duplicate D00-T01-S21-PR13
- [D00-T01-S26-PR12] [minor] Batch notes link no evidence -> filed D00 T01 §48
- [D00-T01-S26-PR13] [minor] Completion depends on unshipped §47 helper -> rejected §47 is behavior-preserving by contract (item 2: probes pass unedited); current scan is the shipped code verified by the 795-case suite, no retroactive dependence
- [D00-T01-S26-PR14] [minor] No post-migration regression rule -> filed D00 T01 §48
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `59ee792b18820df25040aaffa8e3d75f75eb8eb59fce50785569e17bbc731fab`):

```
- The completed §26 has no `Verified:` stamp or review record; add it to D00 T01 §26 because checked items and a commit label do not prove the candidate passed its stated gates.
- The batch items report “per-stamp verification” while allowing every stamp to be retired; clarify the evidence required for retirement in D00 T01 §26 because retirement can otherwise erase review debt without establishing equivalent assurance.
- “Same bar as batch A” leaves batches B–D dependent on surrounding prose; repeat the owner, evidence, retirement, and completion requirements in each D00 T01 §26 item because every section item must remain executable without conversation context.
- The retirement shape is only “dated, piped, validator-silent” and omits reason, actor, original stamp, and replacement evidence; strengthen D00 T01 §26 and the canonical grammar owner because auditability requires reconstructing why each obligation ceased.
- The four batch completion claims provide totals but no canonical ledger or artifact containing the 53 stamp decisions; add a machine-addressable migration ledger to D00 T01 §26 because aggregate counts cannot detect omissions, duplicates, or retirement against the wrong stamp.
- The blessed prose acceptance has no required fields for batch, candidate, verifier, gate results, or acceptance date; define that record in D00 T01 §26 because an audit path that only finds grandfathered and OVERDUE status cannot prove batch acceptance.
- D00 T01 §21 assigns the grandfathered set a risk acceptance through 2026-12-31, while §26 says all 53 stamps are retired without stating whether that acceptance is closed, superseded, or still live; reconcile this in §26 and §21 because stale waivers distort plan-health and operator decisions.
- D00 T01 §21 freezes four original populations totaling 53 stamps, while §26 also discusses 57 run-less files and nine lineage-era files without a durable mapping among stamps, files, and runs; own the mapping in §26 because future recounts cannot reproduce the reconciliation from totals alone.
- The migration completion rule says a file completes when all stamps are marked or retired, but it does not define mixed files, newly discovered pre-cutoff stamps, or stamps added after the frozen inventory; add these boundary rules to D00 T01 §26 because the completed population can silently expand or regress.
- The deadline gate in D00 T01 §21 remains specified even after §26 claims full migration, but neither section requires a zero-leftover assertion now; add an immediate empty-backlog gate to §26 because waiting until 2026-12-31 can conceal a faulty completion claim.
- Batch ownership is recorded only as generic “operator”; require a stable accountable identity or role record in D00 T01 §26 because premium audit trails must survive personnel and session changes.
- The dated completion notes give only a day and no candidate range, review run, or immutable evidence reference; enhance each D00 T01 §26 batch record with those links because precise provenance makes regression investigation and external audit materially faster.
- The section references a helper in future §47 but does not state whether its absence limits current verification; add the current scan method and expected result to D00 T01 §26 because completion should not depend retroactively on unshipped tooling.
- The plan lacks a post-migration regression rule preventing new unmarked pre-cutoff stamps or run-less findings from reappearing; add a permanent validator fixture and gate to D00 T01 §21 or the canonical validation owner because a premium product should preserve the cleanup continuously.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `b7a2f4e`: `todo-graph self-test: 795 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `b7a2f4e`: `24 todos, 222 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `b7a2f4e`: `implementation plan is current -- 66/217 sections complete (30%)`, exit 0.

`python3 scripts/todo-graph.py query plan-health` on `b7a2f4e`: `grandfathered stamps 0 (pre-cutoff, excused, unmarked; migrate by 2026-12-31)`, exit 0.

Provenance: candidate b7a2f4e; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --check && python3 scripts/todo-graph.py query plan-health`; exit 0; tool CPython 3.12.3; digest b9268eefd91e595ff0c6bad21bd86072e7f34114bdd40000fe241ab1cfc5ca64; path docs/reviews/00-workspace/D00-T01-s26.md; run 20260919-D00-T01-S26-gpt
