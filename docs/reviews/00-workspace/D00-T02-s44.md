# D00 T02 S44 review - Population Gate Second Residuals

Candidate: a7297b1 (Started plus implementation), 987b4d9 (round-1 fixes).
Tier: Full panel. The diff changes shared test-harness surface: the population gate and fingerprint (`tools/NightlyParse.ps1`, `tests/UI/TestPopulation.fingerprint`), the nightly collector (`tools/nightly.ps1`), the UI project build (`tests/UI/UI.csproj`), and the capability attributes every gated UI test uses.

## Evidence map

- Item 1 (content freshness): `Get-BuildInputsDigest`, `Test-BuildInputsDigest`, `tools/Write-BuildInputsDigest.ps1`, the `WriteBuildInputsDigest` target, `Get-UiBuildFreshness`; `tools/BuildInputsDigest.Tests.ps1` (preserved timestamps, deletion, SDK change).
- Item 2 (case identity): `Get-CaseIdentityRows` (sorted, duplicates counted), `Get-TruncatedCaseSourceRows` for cut arguments; fingerprint `schema: population/2` with per-leg case rows.
- Item 3 (capability skips): `HookFact(owner)`, `PrinterFact(owner)`, `Test-CapabilitySkip` grammar with owner and host.
- Items 4 and 5 (per-case debt): `Get-UnexecutedCaseRows`, `Merge-AttemptNames`, `Close-OwedCases`, `Resolve-CarriedCaseDebt`, nightly `owedCases` carry-forward.
- Item 6 (proof binds population): `Get-PopulationIdentity` and the `Update-IncidentLedger` population parameter; streaks reset on a changed or unrecorded population.
- Items 7 and 8 (schema and drift): the version refusal naming the regen command; drift naming removed and added rows per leg.

## Self-review

- Does it make sense: each item has a fixture in `tools/NightlyParse.Tests.ps1` or `tools/BuildInputsDigest.Tests.ps1`; no app code changes beyond test attributes.
- Is it logical: identity, freshness, and debt all move from counts and timestamps to content and names, and every refusal names its recovery command.
- Did it break anything: `tools/Test-TestPopulation.ps1` reads population OK (run-a=258/322 run-b=4/4 interactive=43/74); NightlyParse, BuildInputsDigest, NightDebt, and NightlyNotify suites green.

Arch trigger: none - test tooling and harness evidence only; no editor state, app storage format, extension API, AI, protocol, or consent/undo surface changes.

## GPT panel Round 1

Candidate a7297b1, slot bulk, run 20260925-D00-T02-S44-codex-c06751119.

**adversarial: needs-attention** (2)
1. `tools/NightlyParse.ps1:1063-1081` derives identity from display names, whose truncated arguments are visible in `tests/UI/TestPopulation.fingerprint:375-380`. Replacing an argument beyond the displayed prefix preserves the identity and hash; duplicate suffixes count collisions without distinguishing the underlying cases. Use complete, deterministic argument identities.
2. `tools/NightlyParse.ps1:1523-1535,1554-1560` treats repeated results as distinct cases. With two identically named cases, executing or passing one case twice through retries discharges both, including the unexecuted case. Reconcile stable case identities and deduplicate attempts before calculating or closing debt.

**consistency: needs-attention** (1)
1. `tests/UI/UI.csproj:49-50` and `tools/Write-BuildInputsDigest.ps1:20-23` overwrite provenance after every build, including an incremental build that skips compilation. Editing a source while restoring its timestamp, then rebuilding, can attach the changed source digest to the unchanged binary and make freshness pass. Record provenance only for inputs actually compiled, or force rebuilding when their content changes.

**integration: needs-attention** (1)
1. `tools/NightlyParse.ps1:1548-1563` adds `Close-OwedCases`, but the candidate never connects it to the debt collector. Its only new caller is the unit fixture in `tools/NightlyParse.Tests.ps1:594`. The collector therefore does not gain the contracted behavior of retaining the remaining owed case after two of three cases pass.

**record: needs-attention** (1)
1. `tools/NightlyParse.ps1:2044-2049` initializes missing `streakPopulation` to empty, then requires a nonempty previous population before resetting a streak. Existing unbound evidence therefore retains its accumulated passes and acquires the current population identity; two legacy passes plus one current pass can close an incident without three passes proven against that population. Missing population provenance must invalidate the prior streak.

Telemetry: round 1; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | fixed | 987b4d9 (args-source digest rows for truncated names; UTF-8 capture) |
| R1-F2 | fixed | 987b4d9 (`Merge-AttemptNames`; fixture s44-retry-never-discharges-a-twin) |
| R1-F3 | fixed | 987b4d9 (digest rewritten only on compile or changed copy; restored-timestamp edit reads stale) |
| R1-F4 | fixed | 987b4d9 (`Resolve-CarriedCaseDebt` wired into nightly; fixture s44-carried-debt-closes-per-case) |
| R1-F5 | fixed | 987b4d9 (unrecorded population resets; fixture s44-unrecorded-population-streak-resets) |

## GPT panel Round 2

Candidate 987b4d9 (range a7297b1~1..987b4d9, section files only), slot bulk, run 20260925-D00-T02-S44-codex-c06751119-r2.

**adversarial: needs-attention** (2)
1. `tools/NightlyParse.ps1:1108-1114` still hashes incomplete argument sources: multiline attributes are omitted, and MemberData capture stops after 41 lines without following dependencies. Changing a truncated argument outside those captured lines preserves the population hash. R1-F1 remains unresolved.
2. `tools/NightlyParse.ps1:1626-1640` closes debt by display-name count without identifying the owed duplicate. If one of two identically named cases remains owed, rerunning the already completed case green closes that unexecuted twin. The retry helper does not resolve this ambiguity.

**consistency: needs-attention** (1)
1. `tests/UI/UI.csproj:58` permits rewriting provenance when the digest is absent or any reference was copied, even when UI compilation was skipped. A timestamp-restored source edit followed by either condition receives a fresh digest for an unchanged UI binary, preserving the R1-F3 bypass.

**integration: needs-attention** (2)
1. `tools/nightly.ps1:1358-1364` concatenates tonight’s owed cases with surviving carried cases. Two consecutive budget-cut nights therefore record the same case twice; one later successful execution leaves phantom debt. Reconcile overlapping obligations instead of summing them.
2. `tools/nightly.ps1:1362` converts an unreadable previous result into empty debt and publishes that empty carry-forward state. Corrupting the latest result silently erases outstanding obligations instead of preserving them or refusing reconciliation.

**record: needs-attention** (1)
1. `tools/NightlyParse.ps1:1970,2066,2348` omit `streakPopulation` when reading the ledger, exporting lifecycle evidence, and rebuilding it. Consequently, the reset at `tools/NightlyParse.ps1:2147-2155` treats each subsequent nightly pass as unrecorded and resets the streak again, preventing recovery across persisted runs even when the population is unchanged.

Telemetry: round 2; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | live | still reporting as R2-F1 |
| R2-F1 | fixed | 4b0d845 (whole attribute block, full member text, transitive static helpers, typeof source files; fixture s44-args-source-covers-multiline-and-helpers) |
| R2-F2 | fixed | 4b0d845 (twins close only when every listed copy ran green; fixture s44-twin-closes-only-when-every-copy-ran) |
| R2-F3 | fixed | 4b0d845 (digest rewritten only when the UI assembly compiled; missing-digest refusal names --no-incremental) |
| R2-F4 | fixed | 4b0d845 (`Merge-OwedCases` merges per case; fixture s44-carried-debt-merges-and-survives-a-corrupt-result) |
| R2-F5 | fixed | 4b0d845 (`Read-PreviousOwedCases` names an unreadable result, reds, and carries the newest readable debt) |
| R2-F6 | fixed | 4b0d845 (`streakPopulation` persists through read, lifecycle, and rebuild; fixture s44-streak-population-persists) |

## GPT panel Round 3

Candidate 4b0d845 (range a7297b1~1..4b0d845, section files only), slot signoff, run 20260925-D00-T02-S44-codex-c06751119-r3.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:1144-1160` still omits argument dependencies: `[InlineData(Value)]` never queues `Value`, and `const` declarations are excluded from the static-member map. Changing a long constant beyond its displayed prefix produces the same source digest and population hash, confirmed with an in-memory fixture. R1-F1/R2-F1 remains live.

**consistency: needs-attention** (1)
1. `tests/UI/UI.csproj:60-61` and `tools/Write-BuildInputsDigest.ps1:20-23` bless all transitive inputs whenever UI recompiles, without establishing that referenced projects recompiled. Restore a referenced source’s timestamp, then edit a UI source normally: UI recompiles against the unchanged reference binary, while the new digest records the edited reference source as fresh. Provenance must cover what each project actually compiled.

**integration: needs-attention** (2)
1. `tools/NightlyParse.ps1:1180-1195`, `tools/nightly.ps1:954-955,1366` leave retry normalization disconnected from execution reconciliation. `Merge-AttemptNames` is used for merging obligations, while runtime debt calculation and closure consume raw TRX names. Repeated results for one case can therefore satisfy the count of two identically named cases; the helper-only fixture does not establish the runtime fix.
2. `tools/nightly.ps1:1362-1366` reconciles earlier debt using display names and the current fingerprint, without checking the earlier population identity. Replacing a truncated argument with another sharing its displayed prefix lets the replacement’s passing result close the original case’s debt, even when the population hash correctly changes. Carry the owed case’s original identity through reconciliation.

**record: needs-attention** (1)
1. `tools/NightlyParse.ps1:1744-1746` accepts any non-null JSON as a previous result without validating its structure or debt field. A latest result containing `{}` returns empty debt and stops the search, silently discarding obligations in the preceding valid result. Validate the result before accepting its carry-forward state; malformed records must trigger the existing refusal and fallback path.

Telemetry: round 3; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

Sign-off disposition against the blocking bar: R3-F3 (the retry rule is not wired into the runtime trx readers, so the item 4 claim is false at run time) and R3-F5 (a `{}` result silently erases carried obligations) are data-integrity and stamp-invalidating, so they are fixed and round 4 runs on the depth slot for them. R3-F1, R3-F2, and R3-F4 sit below the bar (each needs a timestamp-restored or constant-held edit crafted past xunit's cut or a population swap that keeps a truncated prefix) and file to D00 T02 §52. The args-source unit (R1-F1, R2-F1, R3-F1) was patched in three consecutive rounds, so it stops being patched and is re-thought in §52 rather than patched again.

| ID | Disposition | Evidence |
| --- | --- | --- |
| R3-F1 | filed | D00 T02 §52 |
| R3-F2 | filed | D00 T02 §52 |
| R3-F3 | fixed | 60e4869 (trx readers count one entry per testId; fixture s44-runtime-retry-counts-once-per-case) |
| R3-F4 | filed | D00 T02 §52 |
| R3-F5 | fixed | 60e4869 (a previous result must pass Test-ResultFile and match its file name; fixture s44-empty-object-result-never-erases-debt) |

## GPT panel Round 4

Candidate 60e4869 (range a7297b1~1..60e4869, section files only), slot depth, blocking findings only, run 20260925-D00-T02-S44-codex-c06751119-r4.

**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:1144-1160` still excludes `const` declarations and never follows identifiers in `[InlineData(Value)]`. Changing a long constant beyond its displayed prefix leaves both the argument-source digest and population hash unchanged. Filing R3-F1 to §52 does not resolve this contract violation.

**consistency: needs-attention** (1)
1. `tests/UI/UI.csproj:60-61` and `tools/Write-BuildInputsDigest.ps1:20-23` record all transitive source content whenever UI recompiles, without proving referenced projects recompiled. A timestamp-restored reference-source edit followed by an ordinary UI-source edit records fresh provenance while UI uses the unchanged reference binary. R3-F2 remains live.

**integration: needs-attention** (1)
1. `tools/nightly.ps1:1362-1367` reconciles carried debt against display names and the current fingerprint without checking the original population identity. Replacing a truncated argument with another sharing its displayed prefix allows the replacement’s passing result to close the original case’s debt. R3-F4 remains live despite its §52 filing.

**record: approve**

Telemetry: round 4; model gpt-6-astra; effort medium; duration unknown; outcome needs-attention; tokens unknown

R3-F3 and R3-F5 did not re-report: both stay fixed in 60e4869. The three live verdicts re-report R3-F1, R3-F2, and R3-F4, which the sign-off already dispositioned below the blocking bar and filed: each Done-when of items 1, 2, 4, and 6 is met by its fixture (a timestamp-restored input refuses; two rows with one display name hash apart and a reordered listing hashes the same; a retried row and a duplicate-named row each keep the right owed count; a proof reads stale after a row-swap regen), and each leftover needs a crafted edit beyond that contract. No blocking finding remains, so round 5 does not run and the stamp proceeds with the leftovers tracked in D00 T02 §52.

| ID | Disposition | Evidence |
| --- | --- | --- |
| R1-F1 | filed | D00 T02 §52 (as R3-F1, the args-source unit re-thought) |
| R1-F2 | fixed | 987b4d9, 60e4869 |
| R1-F3 | filed | D00 T02 §52 (as R3-F2) |
| R1-F4 | fixed | 987b4d9 |
| R1-F5 | fixed | 987b4d9 |
| R2-F1 | filed | D00 T02 §52 (as R3-F1) |
| R2-F2 | fixed | 4b0d845 |
| R2-F3 | filed | D00 T02 §52 (as R3-F2) |
| R2-F4 | fixed | 4b0d845 |
| R2-F5 | fixed | 4b0d845, 60e4869 |
| R2-F6 | fixed | 4b0d845 |
| R3-F1 | filed | D00 T02 §52 |
| R3-F2 | filed | D00 T02 §52 |
| R3-F3 | fixed | 60e4869 |
| R3-F4 | filed | D00 T02 §52 |
| R3-F5 | fixed | 60e4869 |

## Plan review

Round: `gpt-6-astra` medium (plan-primary slot) over §44 plus its Depends neighbor §37 and the input contracts §29 and §15, 2026-09-25. Output check-plan PASS (12 findings, one per line). Triage: 11 filed as the new D00 T02 §52 (three of them absorb the sign-off leftovers R3-F1, R3-F2, and R3-F4), 1 rejected.

Manifest: sections [D00 T02 §44, D00 T02 §37, D00 T02 §29, D00 T02 §15]; dependents [none]; bytes 40655; run 20260925-D00-T02-S44-codex-c06751119-r5

Ledger:

- [D00-T02-S44-PR1] [minor] proofs bound to the build-input digest, filters, and candidate -> filed D00 T02 §52
- [D00-T02-S44-PR2] [minor] digest bound to the actual binaries and a consistent snapshot -> filed D00 T02 §52
- [D00-T02-S44-PR3] [minor] canonical case-identity encoding -> filed D00 T02 §52
- [D00-T02-S44-PR4] [minor] exclusion ledger -> filed D00 T02 §52
- [D00-T02-S44-PR5] [minor] fail-closed trx reconciliation -> filed D00 T02 §52
- [D00-T02-S44-PR6] [minor] execution debt apart from failure resolution -> filed D00 T02 §52
- [D00-T02-S44-PR7] [minor] capability debt due dates and escalation -> filed D00 T02 §52
- [D00-T02-S44-PR8] [minor] debt migration across population changes -> filed D00 T02 §52
- [D00-T02-S44-PR9] [minor] unverified-CI override scope -> filed D00 T02 §52
- [D00-T02-S44-PR10] [minor] schema compatibility across readers -> filed D00 T02 §52
- [D00-T02-S44-PR11] [minor] durable triage handoff receipt -> filed D00 T02 §52
- [D00-T02-S44-PR12] [minor] redacted drift diagnostics -> rejected: the drift lines name UI test display names built from fixture literals in tracked test source, never user data or secrets, and each drift prints only the changed rows per leg (bounded by the change, not the population); redaction would hide the very case the operator must fix.

End of ledger

Raw plan output (fenced; fences strip from every scan):

```
- Proof invalidation in §44 covers case-population changes but not changed test or application code with identical cases; bind proofs to the build-input digest, filters, configuration, and candidate so unchanged listings cannot preserve obsolete evidence.
- Content provenance in §44 does not specify how the recorded digest is bound to the actual binaries or protected against inputs changing during build/discovery; require artifact hashes and a consistent snapshot so stale or mixed outputs cannot pass.
- Case identity in §44 lacks a canonical encoding contract for nulls, types, culture-sensitive values, unsupported arguments, and identical duplicate rows; define these rules and refusal fixtures so identities remain reproducible across hosts and discovery orders.
- Exclusion accountability is missing between §37’s “excluded by rule” option and §44’s capability-skip accounting; require an exclusion ledger with case identity, reason, owner, and review expiry so exclusion cannot silently erase coverage.
- TRX reconciliation in §44 specifies scenarios without defining outcome precedence or handling unmatched, ambiguous, and foreign results; require exact run/build association and fail-closed reconciliation so retries or leftover artifacts cannot manufacture coverage.
- Debt closure in §44 allows a later green execution without specifying what happens to an earlier failure; separate execution-debt closure from failure and flakiness resolution so a passing retry cannot erase a release-relevant defect.
- Capability debt in §44 requires an owner and host but no due date, capability verification, or escalation when that host is unavailable; extend the collector handoff so permanently skipped functionality cannot remain indefinitely unproven.
- Population changes have no debt-migration contract in §44; preserve unresolved obligations through renamed, removed, or reparameterized cases with explicit retirement evidence so regeneration neither loses debt nor leaves impossible obligations.
- §37’s verified `-AllowUnverifiedCi` exception conflicts with its unconditional green-CI admission requirement; define override scope, audit evidence, and proof eligibility so an unverified run cannot be treated as governed proof.
- Schema refusal in §44 does not define compatibility across the checker, nightly runner, collector, and historical proof readers; add coordinated migration and mixed-version fixtures so an upgrade cannot strand debt or misinterpret retained evidence.
- §15 stages debt for manual triage while §44 assumes collector-managed closure; specify a durable, idempotent handoff and reconciliation receipt so a crash or missed triage cannot drop obligations between reporting and collection.
- Drift diagnostics in §44 may expose Theory arguments and become unwieldy for large populations; add redacted, bounded summaries with a complete structured artifact so failures remain actionable without leaking sensitive fixture data.
```

## Stamp gate

Run A over the reviewed code: `dotnet test src/ScratchPad.slnx --no-build --filter "Category!=Interactive&Category!=Primary" -e SCRATCHPAD_BACKGROUND=1` at 6831c40, whose src, tests, and tools are identical to 60e4869 (`git diff --stat 60e4869 6831c40 -- src tests tools` is empty; 6831c40 adds only the §43 stamp's TODO and docs files), with the suite display attached: UI 304 passed, 16 skipped, 0 failed; Unit 387; Protocol 35; Smoke 1; the foreground gate exited 0 (census 262, primary 0, events 271, event-primary 0, uncovered 3, mismatch 0). `tools/Test-TestPopulation.ps1` read population OK run-a=258/322 run-b=4/4 interactive=43/74 on the fresh build.

## Live proof

Provenance: candidate 60e4869f7f62c37699cdc15426e98d2ec672cfb7; command powershell -File tools/NightlyParse.Tests.ps1 plus the BuildInputsDigest, NightDebt, and NightlyNotify suites, and dotnet test src/ScratchPad.slnx --no-build --filter "Category!=Interactive&Category!=Primary" -e SCRATCHPAD_BACKGROUND=1 beside Bin/ForegroundLog/Debug/ForegroundLog.exe 1500; exit 0; tool Windows PowerShell 5.1 and .NET SDK 10.0.400; digest e58f951b57de344c4880b3daf8b0b3a07f238b62527cd089a13de59f783802f1; path tools/NightlyParse.ps1; run 20260925-D00-T02-S44-codex-c06751119-r5
