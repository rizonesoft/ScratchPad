# Review: D00 T01 §45 -- Run Inspection Residuals

Candidate: `0361722` (SHIP) plus `85d7f0d` (self-review fix). Full panel (shared contract surface: query plus graph): Sol rounds 1-2, Opus rounds 3-5 (sign-off plus fix loops).

Arch trigger: none - query legs plus JSON schema plus fixtures; no editor, storage, extensibility, AI, protocol, or consent surface changes.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. One fix; asked clean after.

Does it make sense: each shipped item maps to its code (verdict/corrections/confidence legs, `run/1` JSON with gatekeeper plus README, fixtures §98 plus 22 checks). Exactly the corrected contract; the design defaults (semantics, schema shape) are recorded in the items with costs.

Is it logical: verdict reads worst last-marker state with unavailable explicit; trails follow supersedes links cycle-safe within the run's rows; confidence binds provenance to candidate resolution; JSON mirrors prose from shared lists with sort_keys determinism. One fix: `corrected_by` rendered lowercase link keys against uppercase live IDs; map back to declared case, locked by a JSON assertion (`85d7f0d`, suite 1126).

Did it break anything else: self-test 1126/1126 with all pre-existing cases green; live validate silent; prose legs byte-identical above the appended trust legs (existing query-run assertions green); `--json` gate extended additively; plan current with no box changes.

## GPT panel (round 1)

- `adversarial` needs-attention: (R1-F1) confidence mapping overstates mixed and bogus evidence.
- `consistency` advisory: (R1-F2) AGENTS.md run-view line stale.
- `integration` needs-attention: (R1-F3) prose lacks corrected-by lines.
- `record` needs-attention: (R1-F4) schema key count wrong, membership-only check.

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration 149s; outcome needs-attention; tokens unknown

Disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | e5b9b60 |
| R1-F2 | fixed | e5b9b60 |
| R1-F3 | fixed | e5b9b60 |
| R1-F4 | fixed | e5b9b60 |

Fix record: confidence extracted to `confidence_for` (all-must-resolve high, mixed medium, all-bogus low); AGENTS.md line synced; prose emits corrected-by lines; key count corrected to 12 with an exact-set assertion. Fixed in `e5b9b60` (mapping tables plus end-to-end fixtures, self-test 1131 green); gates re-ran green before round 2.

Raw round-1 output (fenced, not part of the verdict scan; digest `80d6167201ffabbdb94d185e5064124f792c7ee635c1e307f45923f026e47336`; run `20260919-D00-T01-S45-gpt` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:2834-2843` reports high confidence when any provenance candidate resolves, even if another is missing or unverifiable, and reports medium when every candidate resolves to nothing, although `scripts/todo-graph.py:1940-1945` states that such a candidate attests nothing; the trust view can therefore overstate confidence.

**consistency: advisory** (1)
1. `AGENTS.md:62` still documents the old run view without verdict, corrections, confidence, or `--json`, while `todo/README.md:344` documents the expanded `run/1` interface.

**integration: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1179` requires per-row corrected-by lists in both prose and JSON, but `scripts/todo-graph.py:2930-2936` renders only forward `corrects` trails for amended rows and never emits `corrected_by` in prose.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:1180` records an 11-key schema, but `scripts/todo-graph.py:2868-2886` emits 12 top-level keys; `scripts/todo-graph.py:12553-12570` checks only required-key membership, so it cannot enforce the stated key count or the checkpoint’s prohibition on output outside the schema.
```

## GPT panel (round 2)

- `adversarial` approve
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 78s; outcome approve; tokens unknown

No new findings; R1 fixes hold.

Raw round-2 output (fenced, not part of the verdict scan; digest `d5a767ec020327ec8af8b8e8b61f044a39273266ee52a107425642a0466e0711`; run `20260919-D00-T01-S45-gpt-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**
**consistency: approve**
**integration: approve**
**record: approve**
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` needs-attention: (R3-F1) trail-side case skew plus cross-block split.
- `integration` approve
- `record` approve

Telemetry: round 3; model opus; effort medium; duration 99s; outcome needs-attention; tokens unknown

Disposition table:

| ID | Disposition | Evidence |
| R3-F1 | fixed | ff3abdd |

Fix record: trails extracted to `correction_trails` with declared-case normalization and lower-dedupe; table checks lock the mixed-case trail and the dedupe. Fixed in `ff3abdd` (suite 1133 green); gates re-ran green before round 4. Above the filing bar (contract data integrity), so fix-loop instead of file-and-stamp.

Raw round-3 output (fenced, not part of the verdict scan; digest `96efe19d005e5a450f326e6be769270e384e2dd5da98c5cc77e701b5f5e44db7`; run `20260919-D00-T01-S45-opus` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: approve**

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:2831,2835,2837,2844` emit correction-trail identifiers in the citing text's case while `scripts/todo-graph.py:2847-2850` normalizes `corrected_by` to the row-declared case, so the same identifier prints two ways. Both `LEDGER_ROW_RE` (`scripts/todo-graph.py:1432-1435`) and `FINDING_ID_RE` (`scripts/todo-graph.py:1725`) are `re.IGNORECASE`, so a ledger row `- [D90-T07-S92-PR12] [major] ... -> filed §2 supersedes d90-t07-s92-pr11` yields `corrects ['d90-t07-s92-pr11']` and `"original": "d90-t07-s92-pr11"` (verified by replaying the block through `ledger_supersedes` plus the §45 trail loop) while `D90-T07-S92-PR11`'s `corrected_by` is `['D90-T07-S92-PR12']`; in `run/1` JSON a consumer cannot join `corrections[].corrects`/`original` against `corrections[].row` or `corrected_by` without lowercasing, and the same skew splits one identifier into two `corrections` entries at `scripts/todo-graph.py:2824` when two blocks spell the row differently. The R1-F4 case fix covered only the `corrected_by` direction; the §98 fixture at `scripts/todo-graph.py:12535-12543` pins only that direction, so no probe holds the trail side.

**integration: approve**

**record: approve**
```

## Opus panel (round 4)

- `adversarial` needs-attention: (R4-F1) JSON truncates text lossily.
- `consistency` approve
- `integration` needs-attention: (R4-F2) run field uses raw target; (R4-F3) findings lack structured fields.
- `record` approve

Telemetry: round 4; model opus; effort medium; duration 122s; outcome needs-attention; tokens unknown

Disposition table:

| ID | Disposition | Evidence |
| R4-F1 | fixed | 3fb997c |
| R4-F2 | fixed | 3fb997c |
| R4-F3 | fixed | 3fb997c |

Fix record: JSON legs carry full untruncated text (prose keeps bounds); run field emits the normalized ID (synonym-deterministic); findings gain id/severity/disposition. Fixed in `3fb997c` (synonym pair, structured fields, truncation both-directions fixtures; suite 1139 green); gates re-ran green before round 5. Above the filing bar (contract integrity), so fix-loop continues.

Raw round-4 output (fenced, not part of the verdict scan; digest `d7fb70710ba26f436a92787a14bf2d310b53d5a6bbde1a61dee2d4af0f9ddb30`; run `20260919-D00-T01-S45-opus-r2` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention (1)**
1. `scripts/todo-graph.py:2818,2821,2885,2910` silently truncate text into the `run/1` JSON (findings row text at 80, row suffix at 140, marker text at 120, outage text at 160) with a bare slice (`_one_line`, `scripts/todo-graph.py:2758-2759`, no ellipsis or flag), so the machine contract emits lossy values a consumer cannot detect or recover: `python3 scripts/todo-graph.py query run 20260919-D00-T01-S44-gpt-r3 --json` returns `findings[0].row` ending `"...the skip only stops false fires on partial memb"`, cutting the disposition rationale mid-word while the record file holds the full text.

**consistency: approve**

**integration: needs-attention (2)**
1. `scripts/todo-graph.py:2898` sets `"run": target` from the raw argument instead of the normalized `want` (`normalize_run_id`, `scripts/todo-graph.py:1458-1468`), so the same run yields two different JSON documents under the `-r1` synonym and the emitted ID does not match the recorded run: `query run 20260918-D00-T01-S40-gpt-r1 --json` prints `"run": "20260918-D00-T01-S40-gpt-r1"` while `markers[].run` is `20260918-D00-T01-S40-gpt`.
2. `scripts/todo-graph.py:2905` (built at `2819-2823`) emits each finding as one pre-formatted prose string (`- [ID] [sev] -> disp ... :: text`) with no `id`, `severity`, or `disposition` fields, while `corrections[].row` at `2904` is keyed by bare row ID, so joining a finding to its correction trail requires regex-scraping the prose the section exists to eliminate ("tooling must read it without scraping").

**record: approve**
```

## Opus panel (round 5)

- `adversarial` needs-attention: (R5-F1) mixed-evidence confidence ordering.
- `consistency` approve
- `integration` approve
- `record` approve

Telemetry: round 5; model opus; effort medium; duration 154s; outcome needs-attention; tokens unknown

Round-5 hard-cap disposition: R5-F1 is below the blocking bar (mixed False/None orderings arise only where a bogus candidate already FATALs validation or git is unprovable; valid trees never present the state), so it files at D00 T04 §1 instead of stopping the run. No live findings remain; this round is FINAL.

Final disposition table:

| ID | Disposition | Evidence |
| R1-F1 | fixed | e5b9b60 |
| R1-F2 | fixed | e5b9b60 |
| R1-F3 | fixed | e5b9b60 |
| R1-F4 | fixed | e5b9b60 |
| R3-F1 | fixed | ff3abdd |
| R4-F1 | fixed | 3fb997c |
| R4-F2 | fixed | 3fb997c |
| R4-F3 | fixed | 3fb997c |
| R5-F1 | filed | D00 T04 §1 |

Architecture gate: not triggered (`Arch trigger: none`, see header).

Raw round-5 output (fenced, not part of the verdict scan; digest `8d7022210176a1a1faaff37de0f2423448f9fdb5b3ba92c25a0e691303923c01`; run `20260919-D00-T01-S45-opus-r3` via self-hosted `review-run`, receipt ledgered under ignored `build/review-runs/`):

```
**adversarial: needs-attention (1)**

1. `scripts/todo-graph.py:1530-1536` (`confidence_for`): the `any(_r is None)` branch is tested before the no-resolution `low`, so a run whose candidates are one definitively-absent sha (`git_resolves` -> `False`, rev-parse exit 1) plus one unprovable sha (`None`, e.g. an ambiguous short exiting 128) reports `medium` with the single reason `provenance bound; candidate unverifiable here`. Adding an unverifiable candidate therefore *raises* confidence (`[False]` -> `low`, `[False, None]` -> `medium`, probed live) and the emitted reason never names the candidate that resolves to nothing, contradicting the R1 principle recorded at `scripts/todo-graph.py:1523-1526` ("a resolving-to-nothing candidate attests nothing"). No fixture covers the mixed `False`/`None` case: the mapping table at `scripts/todo-graph.py:12563-12574` only probes single-state lists plus `[True, False]`.

**consistency: approve**

**integration: approve**

**record: approve**
```
