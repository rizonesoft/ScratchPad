# D00 T02 §14 review: Run-Level Deadline for the Governed Run

Candidate: `96f749da109317fc61f2ea239a3dd4223ec2224a`.
Tier: Full panel (the diff changes the governed-run script: the scheduled task, triage, and Run A/B evidence all consume it).

## Self-review

Three questions over `git diff eef775b..96f749d` (3 files, 183 insertions, 30 deletions):

1. Makes sense: yes. Item 1's deadline math lands in the abort rules; item 2's watchdog gates every leg start with injectable shorts and unproven marks; item 3 publishes core verdicts before soak with a soak ledger; items 4-5 write retention plus the session matrix and migrate the archives.
2. Logical: yes. Budget checks admit a leg only when `remaining >= cap + 180s` kill slack; killed legs mark unproven via overrun/killed notes; the `$sum` move is safe (leg trx files are static after the legs); debt attribution runs once (final report only), so no double-append.
3. Breakage: none found. Normal runs behave identically (budget admits everything; only additive report lines); parse 0 errors; validate 0 fatal; CheckOnly green. No self-finds.

## GPT panel Round 1

Round 1 over candidate `96f749d` (Sol medium, run 20260920-D00-T02-S14-gpt, output check PASS).

- `adversarial` needs-attention (R1-F1: builds plus gate-wait unbounded; R1-F2: truncated trx throws under Stop, killing the report)
- `consistency` needs-attention (R1-F3: Set-Content is not atomic; R1-F4: killed soak iteration without trx omitted from the ledger)
- `integration` needs-attention (R1-F5: retained/ under ignored build/ contradicts the outside-scratch clause)
- `record` needs-attention (R1-F6: items marked done against the contradictions; R1-F7: leg-invocation cites stale under the diff)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Six real, one filed with a threat-model boundary. Disposition:

| ID | Disposition | Evidence |
| R1-F1 | filed | build plus gate-wait bounding filed at §15 (`D00-T02-S14-R1-F1`); the §14 threat model covers hung legs (the analyzed 21900 s arithmetic), and hung toolchains are fail-fast plus unobserved |
| R1-F2 | fixed | fix commit wraps both trx parses in try/catch (malformed reads as absent; scratch proof: truncated trx reads null) |
| R1-F3 | fixed | fix commit routes both report writes through `Write-AtomicReport` (temp plus same-volume rename; scratch proof: 2 lines land) |
| R1-F4 | fixed | fix commit emits killed-without-trx soak rows (re-read: the branch mirrors the trx row) |
| R1-F5 | fixed | fix commit strikes the outside-ignored clause with the bloat-math reason (~60 MB per 30 d in git); governed-ignored retention stands |
| R1-F6 | fixed | falls out of F2-F5: claims now match the implementation |
| R1-F7 | fixed | fix commit refreshes the cites to the shipped lines |

Raw round-1 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (2)
1. `tools/nightly.ps1:404-406`, `tools/nightly.ps1:490-494`, and `tools/nightly.ps1:196` do not implement a run-level watchdog: the deadline only gates leg admission, while builds and the gate-job `Receive-Job -Wait` remain unbounded and can prevent the report from landing before PT4H.
2. `tools/nightly.ps1:204-206`, `tools/nightly.ps1:607-621`, and `tools/nightly.ps1:725-730` parse any existing TRX as XML without handling truncated files; a killed all-hang leg can leave partial XML that throws under `$ErrorActionPreference = 'Stop'` and prevents report publication.
**consistency: needs-attention** (2)
1. `docs/testing.md:76` promises atomic report publication, but `tools/nightly.ps1:621` and `tools/nightly.ps1:835` overwrite the fixed report directly with `Set-Content`; interruption can leave a missing or truncated report.
2. `docs/testing.md:76` says the final soak ledger marks killed iterations, but `tools/nightly.ps1:725-730` emits a killed iteration only when a parseable TRX exists; a killed iteration producing no TRX is omitted entirely.
**integration: needs-attention** (1)
1. `docs/testing.md:80` and `todo/00-workspace/TODO-02-test-backbone.md:443` place retained evidence under ignored `build/nightly/retained/`, directly contradicting the contract requirement that retention live outside ignored `build/` scratch and leaving the evidence unavailable to repository-integrated review.
**record: needs-attention** (2)
1. `todo/00-workspace/TODO-02-test-backbone.md:441-443` marks atomic publication, complete killed-soak reporting, and outside-scratch retention done despite the implementation and documented location contradicting all three claims.
2. `todo/00-workspace/TODO-02-test-backbone.md:433` cites `nightly.ps1:442,457,472,511,516` as the thirteen-leg evidence, but those current lines are debt-filter, smoke, and budget-cut code rather than the leg invocations and loop bounds, so the recorded source anchors are stale.
```

Fix loop 1 (`da67f6f`): truncated-trx guards, atomic report writes, killed-without-trx soak rows, outside-ignored clause struck with the bloat reason, cites refreshed, R1-F1 filed at §15 (superseded below).

## GPT panel Round 2

Round 2 over candidate `eef775b..da67f6f` scoped to `tools/nightly.ps1`, `docs/testing.md`, `todo/00-workspace/TODO-02-test-backbone.md` (Sol medium, run 20260920-D00-T02-S14-gpt-r2, output check PASS), with the round-1 verdicts appended.

- `adversarial` needs-attention (R2-F1: build plus gate-wait still unbounded; R2-F2: teardown cmdlets unbounded past the slack)
- `consistency` approve
- `integration` needs-attention (R2-F3: sim runs can exit 0 and read as governed proof)
- `record` needs-attention (R2-F4: "all-hang" overclaims the sim; R2-F5: watchdog complete while build/gate deferred)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

All five real. R1-F1's filing is superseded: the bounds are cheap, so they land in §14 and the §15 item is removed. Disposition:

| ID | Disposition | Evidence |
| R2-F1 | fixed | fix commit bounds both builds (600 s timed steps, killed builds red like red builds) plus the gate wait (bell-plus-120 s grace, hung gate marks unproven); R1-F1's §15 item plus SOURCE key removed as superseded |
| R2-F2 | fixed | fix commit reaps before teardown and bounds the wait (Wait-60, no-throw receive, forced remove); the reap lands however teardown ends |
| R2-F3 | fixed | fix commit marks every sim report `(SIMULATION: not a governed proof)` from either knob and forces sim runs RED |
| R2-F4 | fixed | fix commit rewords item 2 plus the checkpoint to budget-exhaustion with the infeasibility note (5 s legs cannot hang, daytime Interactive skips) |
| R2-F5 | fixed | falls out of the R2-F1 fix: nothing deferred, the guarantee is complete (smoke stays exempt as a non-governed diagnostic) |

Raw round-2 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (2)
1. `tools/nightly.ps1:387-390`, `tools/nightly.ps1:492-494`, and `tools/nightly.ps1:196` do not implement a run-level watchdog: the deadline only gates leg admission, while builds and the gate-job `Receive-Job -Wait` remain unbounded and can prevent the required report from landing before PT4H.
2. `tools/nightly.ps1:143-151` and `tools/nightly.ps1:191-199` rely on an estimated 180-second teardown allowance while `Stop-Job`, `Receive-Job`, and `Remove-Job` themselves remain unbounded; catching a transport exception does not stop those operations from hanging past the run deadline.
**consistency: approve**
**integration: needs-attention** (1)
1. `tools/nightly.ps1:404-418`, `tools/nightly.ps1:688-696`, and `tools/nightly.ps1:851` permit simulation overrides to finish with exit 0, and setting only `SCRATCHPAD_LEG_CAP_SECONDS` does not mark the final report as simulated; such a run can be consumed as governed green proof despite `docs/testing.md:77` declaring simulation runs non-governed.
**record: needs-attention** (2)
1. `todo/00-workspace/TODO-02-test-backbone.md:440` calls the evidence an all-hang simulation, but its own record says only Run A and two soak iterations were killed while the other executable legs completed or were skipped; this does not establish the contract's simulated all-hang checkpoint.
2. `todo/00-workspace/TODO-02-test-backbone.md:439-440` marks the run-level watchdog complete while `todo/00-workspace/TODO-02-test-backbone.md:476` defers the still-unbounded build and gate-binary paths to §15, leaving the §14 deadline guarantee knowingly incomplete.
```

Fix loop 2 (`cbccd54`): bounded builds plus gate wait, reap-first bounded teardown, sim marking plus forced RED, budget-exhaustion rewording, §15 filing removed as superseded.

## Opus panel Round 3

Round 3 over candidate `eef775b..cbccd54` scoped to `tools/nightly.ps1`, `docs/testing.md`, `todo/00-workspace/TODO-02-test-backbone.md` (Opus medium, run 20260920-D00-T02-S14-opus, output check PASS), with the round-1 and round-2 verdicts appended. Sign-off round: R3-F1 is blocking-grade (the core guarantee), so it fixes with an R4 confirmation round; R3-F2 files as below-bar residual.

- `adversarial` needs-attention (R3-F1: gate grace double-charges the leg; R3-F2: Stop/Remove-Job unbounded on a wedged runspace)
- `consistency` needs-attention (R3-F3: docs math does not bound the coded path)
- `integration` approve
- `record` needs-attention (R3-F4: claim overstates the guarantee; sim never hung a gate)

Telemetry: round 3; model opus; effort medium; duration unknown; outcome needs-attention; tokens unknown

Disposition:

| ID | Disposition | Evidence |
| R3-F1 | fixed | fix commit measures the gate wait from gate start (bell-plus-grace, never a fresh cap); scratch proof with a synthetic hung job marks hung at bell-plus-grace (13 s on 5+10) |
| R3-F2 | filed | wedged-runspace teardown filed at §15 (`D00-T02-S14-R3-F2`); unobserved, and reap-first plus core-verdicts-first bound the damage |
| R3-F3 | fixed | fix commit documents the concurrent-bell rationale; code now matches `remaining >= cap + 180s` |
| R3-F4 | fixed | fix commit records the hung-gate proof in item 2; the claim matches the implementation |

Raw round-3 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (2)
1. `tools/nightly.ps1:205` starts the bounded gate wait (`Wait-Job -Job $job -Timeout ($GateSeconds + 120)`) only after the test-job wait at `tools/nightly.ps1:186` and the 60 s teardown wait at `tools/nightly.ps1:195`, so a hung gate binary makes one leg cost up to `2 x cap + 180 s` (3780 s for Run A), while the admission test at `tools/nightly.ps1:436-437` with `$killSlack = 180` (`tools/nightly.ps1:414`) reserves only `cap + 180 s` (1980 s); one hung gate therefore overruns the run deadline by ~1800 s and can still die by scheduler kill before the report lands.
2. `tools/nightly.ps1:129` and `tools/nightly.ps1:188` call `Stop-Job` synchronously, and `tools/nightly.ps1:143` and `tools/nightly.ps1:197` call `Remove-Job -Force`, with no timeout on either; the added `Wait-Job -Timeout 60` at `tools/nightly.ps1:141` and `tools/nightly.ps1:195` bounds only the intervening wait, so a wedged runspace still blocks teardown past the deadline the 180 s slack is sized against.
**consistency: needs-attention** (1)
1. `docs/testing.md:76` states that "the 180 s slack covers job-teardown latency on a kill" and that "the gate wait carries bell-plus-120 s grace, so hung toolchains strand nothing either", but the gate grace at `tools/nightly.ps1:205` is charged after the full cap already elapsed and is not included in the per-leg reservation at `tools/nightly.ps1:436-437`; the documented deadline math `remaining >= cap + 180s` does not bound the code path it claims to bound.
**integration: approve**
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:441` marks the watchdog item `[x]` on the claim that legs are bounded by `remaining >= cap + 180s`, but the shipped gate wait at `tools/nightly.ps1:205` can consume an additional `cap + 120 s` beyond that reservation, so the stamped claim overstates what the implementation guarantees; the cited simulation (`build/nightly/2026-09-20-202108`) exercised only killed and budget-cut legs and never a hung gate, so it does not cover the case.
```

Fix loop 3 (`cdd955a`): gate wait measured from gate start with a hung-gate scratch proof, R3-F2 filed at §15, docs rationale plus residual.

## GPT panel Round 4

Round 4 over candidate `eef775b..cdd955a` scoped to `tools/nightly.ps1`, `docs/testing.md`, `todo/00-workspace/TODO-02-test-backbone.md` (Sol medium, run 20260920-D00-T02-S14-gpt-r3, output check PASS), with the round-1 through round-3 verdicts appended. Past-sign-off confirmation round: R4-F2 is blocking-grade (deadline guarantee), so it fixes with an R5 confirmation at the hard-5 cap; R4-F1 restates the filed R3-F2 residual.

- `adversarial` needs-attention (R4-F1: Stop/Remove unbounded restated; R4-F2: zero-budget start still runs builds)
- `consistency` needs-attention (R4-F3: docs claims vs teardown plus late builds)
- `integration` needs-attention (R4-F4: late StartWhenAvailable recovery defeats the boundary)
- `record` needs-attention (R4-F5: complete with filed residual plus late builds)

Telemetry: round 4; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Disposition:

| ID | Disposition | Evidence |
| R4-F1 | filed | restates the filed R3-F2 residual (`D00-T02-S14-R3-F2`); stands as filed: unobserved, reap-first plus core-verdicts-first bound the damage, out-of-process legs are §15 work |
| R4-F2 | fixed | fix commit fails fast at zero budget (red-build path reused: nothing runs, report lands immediately, whole run unproven); zero-budget sim proves it |
| R4-F3 | fixed | falls out of the R4-F2 fix plus the already-recorded R3-F2 residual |
| R4-F4 | fixed | same fail-fast gate covers late recovery launches; the matrix already routes still-unfired runs to the manual backup |
| R4-F5 | fixed | falls out of the R4-F2 fix; completion with the filed below-bar R3-F2 residual follows the §9 FL6 precedent |

Raw round-4 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (2)
1. `tools/nightly.ps1:128-144` and `tools/nightly.ps1:188-218` still invoke `Stop-Job` and `Remove-Job -Force` synchronously without a timeout; a wedged runspace can therefore block beyond the run deadline and prevent report publication.
2. `tools/nightly.ps1:417-421` can produce a deadline at or before run start for an in-window late start after 06:45, but `tools/nightly.ps1:524-535` still runs two independently capped 600-second builds before checking any remaining budget, so the report can land after the 06:50 boundary.
**consistency: needs-attention** (1)
1. `docs/testing.md:76` claims the deadline mechanism ensures the report lands and that hung toolchains strand nothing, while `tools/nightly.ps1:128-144`, `tools/nightly.ps1:188-218`, and `tools/nightly.ps1:524-535` retain unbounded job teardown and permit builds to run past an already exhausted window deadline.
**integration: needs-attention** (1)
1. `docs/testing.md:78` says `StartWhenAvailable` may launch a missed task later inside the window, but `tools/nightly.ps1:417-421` and `tools/nightly.ps1:524-535` do not reject or shorten builds when that launch occurs after the 06:45 report-reserve boundary, defeating the documented Task Scheduler recovery path.
**record: needs-attention** (1)
1. `todo/00-workspace/TODO-02-test-backbone.md:439-443` marks the deadline watchdog complete even though `tools/nightly.ps1:128-144` and `tools/nightly.ps1:188-218` retain the acknowledged unbounded teardown defect deferred at `todo/00-workspace/TODO-02-test-backbone.md:476`, and late-window builds at `tools/nightly.ps1:524-535` can still overrun the recorded deadline.
```

Fix loop 4 (`cc0f3e5`): fail-fast gate at zero budget with a zero-budget sim proof, R3-F2 restated as filed-standing.

## Opus panel Round 5

Round 5 over candidate `eef775b..cc0f3e5` scoped to `tools/nightly.ps1`, `docs/testing.md`, `todo/00-workspace/TODO-02-test-backbone.md` (Opus medium, run 20260920-D00-T02-S14-opus-r2, output check PASS), with the round-1 through round-4 verdicts appended. Hard-5 cap: no round 6 exists, so every finding fixes or files here with execution proof, and the panel closes.

- `adversarial` needs-attention (R5-F1: hardcoded 06:50 breaks moved windows; R5-F2: slow teardown mis-marks healthy gates hung)
- `consistency` needs-attention (R5-F3: docs math vs moved windows)
- `integration` needs-attention (R5-F4: core presence vs guard completion; no machine marker)
- `record` needs-attention (R5-F5: stale anchors, including §15 cites drifted under this diff)

Telemetry: round 5; model opus; effort medium; duration unknown; outcome needs-attention; tokens unknown

Disposition (the first fix attempt read a nonexistent `$windowEnd` as midnight and would have failed every in-window run fast; the moved-window proof run caught it before commit, and the shipped fix parses the spec properly):

| ID | Disposition | Evidence |
| R5-F1 | fixed | fix commit derives the end from the resolved spec (`Get-InteractiveWindowEnd`, crossing-midnight rollover, null-safe fallback); moved-window Run B proof (deadline 23:54, admitted plus proved) |
| R5-F2 | fixed | fix commit verdicts hung only when the job still runs at grace expiry; scratch proof (completed job, exhausted timeout) receives normally with the verdict kept |
| R5-F3 | fixed | fix commit rewrites the docs math around the resolved window end |
| R5-F4 | fixed | fix commit adds `Status:` lines to both publications plus the docs marker clause (the guard reads the path for triage; the marker resolves the ambiguity either way) |
| R5-F5 | fixed | fix commit refreshes every cited anchor to shipped lines, verified by grep (not the round's numbers, which predated this loop) |

Raw round-5 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (2)
1. `tools/nightly.ps1:420` hardcodes `$windowEnd = Get-Date -Hour 6 -Minute 50` while the window itself is configurable at `tools/nightly.ps1:50-51` (`SCRATCHPAD_INTERACTIVE_WINDOW`, documented at `docs/testing.md:29`); under any moved window (e.g. `14:00-18:00` at 14:30) `$inWindow` is true at `tools/nightly.ps1:422`, today's 06:50 is already past, so `$deadline` lands in the past and `tools/nightly.ps1:533-539` fails the entire run fast ("deadline passed at start"), skipping builds and every leg on a run that has hours of real budget.
2. `tools/nightly.ps1:207-209` computes `$gateTimeout = ($GateSeconds + 120) - elapsed-since-gate-start`, but that elapsed time is measured after the kill path at `tools/nightly.ps1:188-197` has already run the orphan reap plus a 60 s `Wait-Job` teardown; when reap plus teardown exceeds the 120 s grace (the same ~120 s teardown latency asserted at `tools/nightly.ps1:414-417`), `$gateTimeout <= 0`, the wait is skipped, and a gate job that already completed and wrote its verdict file is force-stopped at `tools/nightly.ps1:210-214` and reported as `gate hung past its bell plus grace (unproven)` at `tools/nightly.ps1:220`, overwriting the real verdict read at `tools/nightly.ps1:219`.
**consistency: needs-attention** (1)
1. `docs/testing.md:76` states the deadline is `min(runStart + 4h, 06:50)` with "the 06:50 window end binds in-window runs only", and `docs/testing.md:29` plus the report line at `tools/nightly.ps1:735` document `SCRATCHPAD_INTERACTIVE_WINDOW` as moving that window; the implementation at `tools/nightly.ps1:420-422` binds a fixed 06:50 to whatever window `tools/nightly.ps1:50-51` resolved, so the documented deadline math does not describe the shipped behavior under a moved window.
**integration: needs-attention** (1)
1. `tools/nightly.ps1:679` publishes the pre-soak core report to the fixed path `morning-$day.md`, but `tools/nightly.ps1:453` and `tools/nightly.ps1:524-525` record that the guard treats the presence of that fixed-path record as the run having completed, and `docs/testing.md:74` makes guard resurrection the recovery for a dead run; a run that dies during soak (or is scheduler-killed there) now leaves a fixed-path record, so the guard reads it as completed and never resurrects, and neither `docs/testing.md:76` nor the core report body at `tools/nightly.ps1:666-672` defines a machine-checkable marker distinguishing the pre-soak publication from the final one.
**record: needs-attention** (1)
1. Line anchors cited as evidence in `todo/00-workspace/TODO-02-test-backbone.md` no longer point at the cited code after this diff's renumbering: `todo/00-workspace/TODO-02-test-backbone.md:433` cites `nightly.ps1:536,557,579,602` invocations plus `637,649` soak bounds, while the shipped invocations are `tools/nightly.ps1:579,600,622,645` and the soak loops `tools/nightly.ps1:682,694` (536 and 557 are the fail-fast block and a skip assignment); `todo/00-workspace/TODO-02-test-backbone.md:451` cites `nightly.ps1:423,441` for the quarantine check (actually `tools/nightly.ps1:626,649`) and `nightly.ps1:266` for the unused `$s` (actually `tools/nightly.ps1:309`); `todo/00-workspace/TODO-02-test-backbone.md:471` cites `nightly.ps1:439` for the shared `run-a.trx` logger name (actually `tools/nightly.ps1:578`).
```

Panel closed at the hard-5 cap: R5-F1 and R5-F2 both carry execution proof (moved-window leg; completed-after-timeout scratch receives normally with the verdict kept). R5-F3..F5 are record plus docs, verified by re-read and grep. No round 6 exists; the plan-review round below provides the final independent lens.

Fix loop 5 (`8d4e140`): window end from the resolved spec, completed-gate receive branch, `Status:` markers, anchor refresh.

Arch trigger: none - scheduler script plus docs prose plus plan record only; no editor, storage, extensibility, AI, protocol, or consent surface.

## Plan review

Round: `gpt-5.6-sol` high over §14 plus 2 corpus sections, 2026-09-20. Neighbors: D00 T02 §9 plus §5 (both XREF; §5 gains a Depends edge via PR6); dependents: D00 T02 §5, D00 T02 §9 (XREF consumers; no direct Depends; the transitive hop past them lands on §1, §2, §8, all out of blast radius). Output check-plan PASS (25 findings, one per line). No criticals; 11 corrected in place at §14, 6 filed at §15, 3 filed at §16, 2 filed at §17, 2 rejected with reasons, 1 duplicate. A first plan run (20260920-D00-T02-S14-gpt-r4, 33 findings) is superseded: it ran with a wrong candidate hash (`git cat-file` failed on it), so the review re-ran with the true hash.

Manifest: sections [D00 T02 §14, D00 T02 §9, D00 T02 §5]; dependents [D00 T02 §5, D00 T02 §9]; bytes 20723; run 20260920-D00-T02-S14-gpt-r5

Ledger:

- [D00-T02-S14-PR1] [minor] no supervisor for startup, cleanup, checksum, or publication hangs -> filed D00 T02 §15 (outer-supervisor item with tombstone proof)
- [D00-T02-S14-PR2] [minor] PT4H origin at task start vs script runStart -> rejected startup latency (tens of seconds worst case, `Get-AppxPackage` included) sits inside the 300 s reserve; schtasks-start parsing adds fragility for no gain; the window-end boundary is unaffected
- [D00-T02-S14-PR3] [minor] 21900 s omits thirteen kill-grace periods, "simultaneous" for sequential -> filed D00 T02 §14 (24240 s worst case plus cleanup; "thirteen hung legs in sequence")
- [D00-T02-S14-PR4] [major] all-hang proof weakened to two kills plus cuts -> filed D00 T02 §14 (`SCRATCHPAD_STUB_LEGS` sleeper harness hangs every leg type; the stub sim kills all 13 legs at the bell and lands the RED report)
- [D00-T02-S14-PR5] [minor] "infeasible" is a harness limit, want injectable executors plus clocks -> duplicate PR4 plus PR18 (stub legs are the injectable executors; short-deadline knobs bound time without waiting on real tests)
- [D00-T02-S14-PR6] [minor] no direct Depends on the §9 runner or §5 soak it changes -> filed D00 T02 §14 (Implementation Order row now reads §9, §5)
- [D00-T02-S14-PR7] [minor] "full fourth-phase reporting" assigned to §15 with no contract -> filed D00 T02 §14 (bidirectional XREF pair plus the mark-vocabulary contract)
- [D00-T02-S14-PR8] [minor] provisional plus final share a path with no marker -> filed D00 T02 §14 (`Status:` lines mark pre-soak vs final; landed in fix loop 5, confirmed by this review; the immutable-records mechanism is stronger than the confusion it names needs)
- [D00-T02-S14-PR9] [minor] core-green plus soak-killed has no combined verdict -> filed D00 T02 §14 (verdict-semantics sentence: RED on any red, killed, or cut leg; core rows vs soak ledger)
- [D00-T02-S14-PR10] [minor] cut Interactive never becomes Night-owed debt -> filed D00 T02 §15 (debt-handoff item under §10)
- [D00-T02-S14-PR11] [minor] killed or cut soak routing undefined (finding vs infra vs debt) -> filed D00 T02 §14 (infrastructure verdicts for morning triage, never quarantine inputs)
- [D00-T02-S14-PR12] [minor] §5 duration contract vs remaining-budget soak -> filed D00 T02 §15 (truncation-rules item agreed with §5)
- [D00-T02-S14-PR13] [minor] struck clause preserves contradictory acceptance text -> rejected struck-with-reason is the tree convention (matches the §13 PR14 handling: history preserved, the replacement explicit in the Done line)
- [D00-T02-S14-PR14] [minor] checksums beside mutable ignored bytes, fresh clone unverifiable -> filed D00 T02 §14 (tracked hash manifests under `docs/nightly-evidence/`; sample hash verified)
- [D00-T02-S14-PR15] [minor] retention without pruning, verification, failure, or owner gates -> filed D00 T02 §15 (executable-gates item with a named owner)
- [D00-T02-S14-PR16] [major] reboot, kill, or power loss orphans the run with no report -> filed D00 T02 §16 (next-start recovery item with a RED record; extended to operator and scheduler stops after the trap proof)
- [D00-T02-S14-PR17] [minor] session matrix is documentation-only -> filed D00 T02 §16 (executable-proofs-or-accepted-gaps item)
- [D00-T02-S14-PR18] [minor] wall clock drives elapsed budgets -> filed D00 T02 §14 (monotonic stopwatch clock; civil time serves the boundary only)
- [D00-T02-S14-PR19] [minor] manual-backup deadline rule imprecise -> filed D00 T02 §14 (task-limit-alone rule; Interactive still skips out of window)
- [D00-T02-S14-PR20] [major] `IgnoreNew` misses manual and second-launcher collisions -> filed D00 T02 §15 (run-scoped mutex plus loser-report item)
- [D00-T02-S14-PR21] [minor] name-based reaping risks unrelated processes -> filed D00 T02 §15 (PID-tree or Job-Object containment item)
- [D00-T02-S14-PR22] [minor] no acceptance run proving healthy nights uncut -> filed D00 T02 §16 (end-to-end timing-ledger item)
- [D00-T02-S14-PR23] [major] cancel, shutdown, and termination lack report semantics -> filed D00 T02 §14 (script trap lands atomic RED `Status: cancelled`; scratch-proven for throw plus PipelineStoppedException plus the no-dir guard; Stop-Job provably cannot trap and joins PR16)
- [D00-T02-S14-PR24] [minor] unattended REDs enter no filing workflow -> filed D00 T02 §17 (morning-acknowledgement item)
- [D00-T02-S14-PR25] [minor] premium budget telemetry missing -> filed D00 T02 §17 (telemetry-series item on the trend surface)
End of ledger

## Live proof

`py scripts/todo-graph.py self-test`: `todo-graph self-test: 1267 cases, 0 failed`.

Provenance: candidate 8356fe60035ab6e23925a466c8a8f3d8beac5b5f; command py scripts/todo-graph.py self-test; exit 0; tool CPython 3.14.3; digest 05eb1b4cb856253d48a2cb4d8d5fed6c4050e4bafc5b597e241b950a50ad21e6; path scripts/todo-graph.py; run 20260920-D00-T02-S14-gpt-r5

`py scripts/todo-graph.py validate`: `28 todos, 261 sections -- 0 fatal, 0 warning(s)`.

Provenance: candidate 8356fe60035ab6e23925a466c8a8f3d8beac5b5f; command py scripts/todo-graph.py validate; exit 0; tool CPython 3.14.3; digest 05eb1b4cb856253d48a2cb4d8d5fed6c4050e4bafc5b597e241b950a50ad21e6; path scripts/todo-graph.py; run 20260920-D00-T02-S14-gpt-r5

`tools/nightly.ps1` parse: `nightly.ps1 parse errors: 0`.

Provenance: candidate 8356fe60035ab6e23925a466c8a8f3d8beac5b5f; command System.Management.Automation.Language.Parser.ParseFile; exit 0; tool PowerShell 5.1; digest 8a15e36bf5e2f17f7112b7e3efd7b1105fdfbe9cfa7b3f265d0f166f5604860d; path tools/nightly.ps1; run 20260920-D00-T02-S14-gpt-r5

`tools/nightly.ps1 -CheckOnly`: `nightly: environment OK`.

Provenance: candidate 8356fe60035ab6e23925a466c8a8f3d8beac5b5f; command tools/nightly.ps1 -CheckOnly; exit 0; tool PowerShell 5.1 plus repo-local SDK; digest 8a15e36bf5e2f17f7112b7e3efd7b1105fdfbe9cfa7b3f265d0f166f5604860d; path tools/nightly.ps1; run 20260920-D00-T02-S14-gpt-r5

Stub-leg all-hang sim on the range (2700 s budget, 10 s caps, every leg stubbed):

```
--- run-a suite killed at the 10 s bell; reaping its tree ---
--- run-b suite killed at the 10 s bell; reaping its tree ---
--- interactive killed at the 10 s cap; reaping its tree ---
nightly: core verdicts published before soak
--- soak-ui-1 killed at the 10 s cap; reaping its tree ---
--- soak-protocol-5 killed at the 10 s cap; reaping its tree ---
nightly: report at R:\conclave\projects\ScratchPad\build\nightly\morning-2026-09-20.md
nightly: RED (see above)
```

Report head: `# Morning report: 2026-09-20 (SIMULATION: not a governed proof)`, `Status: final`, `Budget-cut: none`, exit 1 (forced RED: sims never exit 0). All 13 legs stubbed and killed at the bell with reap-first teardown per leg; the zero-leftover census rides the budget-exhaustion sim (unchanged teardown path).

Provenance: candidate 8356fe60035ab6e23925a466c8a8f3d8beac5b5f; command tools/nightly.ps1 with SCRATCHPAD_STUB_LEGS plus short knobs; exit 1; tool PowerShell 5.1 plus dotnet 10.0.400; digest 8a15e36bf5e2f17f7112b7e3efd7b1105fdfbe9cfa7b3f265d0f166f5604860d; path tools/nightly.ps1; run 20260920-D00-T02-S14-gpt-r5

Cancel-trap scratch proof (mechanical copy of the shipped trap plus `Write-AtomicReport`): throw lands `Status: cancelled` with exit 1; PipelineStoppedException (the Ctrl+C exception) lands it with exit 1; the no-evidence-dir guard lands nothing with exit 1. Stop-Job on a live run (in-process and child-process) lands nothing: process and scheduler stops cannot trap and join the §16 recovery item.

Provenance: candidate 8356fe60035ab6e23925a466c8a8f3d8beac5b5f; command R:\tmp\trap-proof.ps1 modes throw, stop, nodir plus live Stop-Job runs; exit 1; tool PowerShell 5.1; digest 8a15e36bf5e2f17f7112b7e3efd7b1105fdfbe9cfa7b3f265d0f166f5604860d; path tools/nightly.ps1; run 20260920-D00-T02-S14-gpt-r5

PR14 manifest sample: `certutil -hashfile build/nightly/retained/nightly-rerun-2026-09-20/morning-2026-09-20.md SHA256` returns `f7e958a7...b0b`, matching the tracked manifest at `docs/nightly-evidence/nightly-rerun-2026-09-20.md`.

Provenance: candidate 8356fe60035ab6e23925a466c8a8f3d8beac5b5f; command certutil -hashfile; exit 0; tool certutil; digest f7e958a76e94f526b11d4d13ad2224d4b2e43e48e71873dde2bde233fbc1bb0b; path docs/nightly-evidence/nightly-rerun-2026-09-20.md; run 20260920-D00-T02-S14-gpt-r5
