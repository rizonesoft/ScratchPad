# Review: D00 T01 §35 -- Mixed Sol/Opus Panel with Soft and Hard Caps

Candidate: `324eefc` (implement commit; filing `dd2dff3` holds the section skeleton per convention, unreviewed). Tier: Full panel (the diff touches shared contract surface: `scripts/`, skills). Regime: mixed panel dogfood (Sol rounds 1-2, Opus sign-off round 3, soft-3/hard-5 caps), the first review run under the new rules.

## Live proof

Gates on `cfba540`. Digests are sha256 over the quoted bytes, each line LF-terminated.

`python3 scripts/todo-graph.py self-test`:

```
todo-graph self-test: SKIP sibling stamp-gate clamp probe (no section_commit_gate.py)
todo-graph self-test: 746 cases, 0 failed
```

Provenance: candidate cfba540; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest 391a36b78c1b7cf3eb3c06716267052d0fd03f29325fe06306e3d4ee6f12fbd2; path docs/reviews/00-workspace/D00-T01-s35.md; run 20260918-D00-T01-S35-gpt

`python3 scripts/todo-graph.py validate`: `22 todos, 205 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate cfba540; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest 4c0d243f6882a80cf49af38bdc833aa5adcd673dd136e14d42464dcffc08db79; path docs/reviews/00-workspace/D00-T01-s35.md; run 20260918-D00-T01-S35-gpt

`python3 scripts/todo-graph.py plan --check`: `implementation plan is current -- 62/200 sections complete (31%)`, exit 0.

Provenance: candidate cfba540; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest ab96c1ffa43e7760f33f25a07714d620745485fd427245aa87d747c8e2cee71c; path docs/reviews/00-workspace/D00-T01-s35.md; run 20260918-D00-T01-S35-gpt

`python3 scripts/todo-graph.py query plan-health --check`: `gate: ok`, exit 0.

Provenance: candidate cfba540; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 83e9a060459d5921fedae881c1e079a0169941dd7fcff74fabc57c3188d0943b; path docs/reviews/00-workspace/D00-T01-s35.md; run 20260918-D00-T01-S35-gpt

## Self-review

Three questions, answered against the candidate diff (`git show 324eefc`), not memory. (1) Sense: the diff delivers the ten contract items (mixed sequence, caps, Sol command, outage matrix, architecture gate, mixed-record self-tests, GPT-last fallback leg, README row, AGENTS.md count, live drives) with no widening; the only adjacent touch is the one-word rule-16 comment the section explicitly rides. (2) Logic: the skill's per-tier sequences, caps, and outage matrix agree with the contract; the validator needs no behavior change because rule 16 reads last-wins already; the fallback leg mirrors that semantic with the same regex shape; schema stays `plan-health/3` (membership refined, keys unchanged). (3) Blast radius: no external consumer parses the fallback text label or the panel headings (swept `scripts/`, `.github/`, `docs/` outside reviews/phase-runs: no hits); plan-health JSON keeps its keys; existing fixtures keep passing (90-health.md GPT-last still counts; opuslast newly excluded by design, locked by a presence check pair).

Failure-mode sweep (the codebase list): no trusted UI value, no file write outside atomic temp fixtures (unlinked after), no consent path, no encoding path, no protocol message, no allow-by-default, no frozen behavior, no mock-accepts-anything test (silence checks fail closed on fixture mis-parse: a structurally broken §§37-39 fixture FATALs under its own section number instead of passing vacuously), no UI surface. Integration greps quoted: fallback-label consumers none; `Opus panel` docs hits none outside reviews/phase-runs.

Verdict: clean, no self-review fixes. Fixes below all come from the panel.

## Architecture gate

Not triggered. The candidate changes review process tooling (skill prose, validator comment, query counting, self-tests), none of the trigger surfaces (editor state, storage, extensibility, AI integration, protocol, consent/undo). Absence of an `Architecture review` record is per the gate's own skip rule, not a gap.

## GPT panel (round 1)

Candidate `324eefc`, Sol medium, exit 0, `check-panel PASS`.

- `adversarial` needs-attention (1): SKILL.md:122 never required advisories past sign-off to file. Accepted: contract item 2 demands filing; fixed in the R1-fix commit (advisories at/after sign-off file; early-round advisories stay noted).
- `consistency` needs-attention (2): (1) the Sol-R2 skip contradicted the filed Full sequence (Sol R1-2 plus Opus R3). Accepted: skip removed, early sequence always runs whole per the contract. (2) Light escalation contradicted the below-bar filing shortcut. Accepted: filing shortcut scoped to the Full round-3 sign-off; Light round 2 always escalates.
- `integration` approve
- `record` needs-attention (1): item-3 Done claimed the probe output was quoted in the implement-commit body, which paraphrased it. Accepted: verbatim probe output rides the R1-fix commit body plus the fenced probe record below; Done note corrected to point there.

```text
Raw GPT panel round 1 output (check-panel PASS four lenses, one verdict each):
**adversarial: needs-attention (1)**
1. `.claude/skills/review-todo-section/SKILL.md:122` does not require advisory findings from post-sign-off rounds to be filed through `add-todo`, allowing those findings to be dropped contrary to the contract.

**consistency: needs-attention (2)**
1. `.claude/skills/review-todo-section/SKILL.md:79` and `.claude/skills/review-todo-section/SKILL.md:122` allow Sol round 2 to be skipped and make Opus sign-off round 2, contradicting the Full-panel sequence at `.claude/skills/review-todo-section/SKILL.md:117` and the contract requiring Sol rounds 1-2 followed by Opus sign-off round 3.
2. `.claude/skills/review-todo-section/SKILL.md:118` requires every Light sign-off `needs-attention` verdict to escalate to Full at round 3, while `.claude/skills/review-todo-section/SKILL.md:122` instead permits nonblocking sign-off `needs-attention` findings to be filed and stamped without another round.

**integration: approve**

**record: needs-attention (1)**
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:800` claims the probe output is quoted in the commit body, but the commit body only says "echo returned" and does not quote the returned probe output required by the contract.
```

## GPT panel (round 2)

Candidates `324eefc` plus `4caf5e3`, Sol medium, exit 0, `check-panel PASS`, R1 output appended.

- `adversarial` approve
- `consistency` needs-attention (1): the Sol-rung failover note and the outage matrix disagreed on whether a mid-sequence Sol failure reruns completed Sol rounds on Opus. Accepted: matrix scoped (pre-start failure runs all rounds on Opus; mid-sequence failure fails over that round and after with completed rounds standing); fixed in the R2-fix commit.
- `integration` approve
- `record` approve

```text
Raw GPT panel round 2 output (check-panel PASS four lenses, one verdict each):
**adversarial: approve**

**consistency: needs-attention (1)**
1. `.claude/skills/review-todo-section/SKILL.md:108` says a failed Sol round fails over to Opus only for that round and later rounds, while `.claude/skills/review-todo-section/SKILL.md:122` requires all rounds to run on Opus when Sol is unreachable; a round-2 Sol failure therefore has contradictory instructions about whether the successful Sol round 1 must be rerun on Opus.

**integration: approve**

**record: approve**
```

```text
Raw Sol-medium stdin probe (/tmp/sol-probe.out, exit 0, 2026-09-18):
OpenAI Codex v0.155.0
--------
workdir: /mnt/r/conclave/projects/ScratchPad
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 01a0b655-beb4-79b3-ad04-cbd7f2e5f23f
--------
user
Reply with exactly this line and nothing else: runner-ok

codex
runner-ok
tokens used
9,573
runner-ok
```

## Opus panel (round 3)

Candidates `324eefc` plus `4caf5e3` plus `2eb0343`, Opus medium, exit 0, `check-panel PASS`, R1-R2 outputs appended. Sign-off round.

- `adversarial` approve
- `consistency` advisory (1): root `README.md:79` still names the retired `headless Opus panel` title. Verified genuine; below the blocking bar, so it files through `add-todo` as D00 T01 §36 instead of re-rounding, per the soft-cap rule (this review's first live exercise of it).
- `integration` approve
- `record` approve

```text
Raw Opus panel round 3 output (check-panel PASS four lenses, one verdict each):
**adversarial: approve**

**consistency: advisory (1)**
1. `README.md:79` still reads "lens verdicts come from the headless Opus panel per `review-todo-section`", stale against this candidate's rename and regime change (`.claude/skills/review-todo-section/SKILL.md:78` `### The mixed panel`, `:10` mixed headless panel with Sol early rounds and the Opus sign-off); sweep of the rest of the tree finds no other site (`todo/README.md:395` and the plan/Implementation Order rows at `todo/00-workspace/TODO-01-repo-and-toolchain.md:57`, `:58`, `:83` are rule-class and historical-section names, correctly untouched).

**integration: approve**

**record: approve**
```

## Plan review

Manifest: sections [D00 T01 §35, D00 T01 §9, D00 T01 §14]; dependents [D00 T01 §36]; bytes 19753; run 20260918-D00-T01-S35-gpt

Reviewer `gpt-5.6-sol` high, exit 0, `check-plan PASS 16 findings`. Scope rode inline as five fence chunks (section §35, neighbors §9 and §14, dependent §36 body, dependents list). Triage: 12 accepted (11 filed at §§36-39, 1 evidenced in this record), 4 rejected with reasons; no criticals. Counts in accepted findings versus implementation items: 12 accepted findings, 11 new checklist items (§37: 5, §38: 4, §39: 3 with PR16 spanning all three, §36 refinement amending in place), plus the PR13 baseline below standing as its own evidence.

Ledger:
- [D00-T01-S35-PR1] [minor] Sol sequence unenforced; skipped Sol rounds pass as planned-mixed -> filed D00 T01 §37
- [D00-T01-S35-PR2] [major] Round 5 stamps over blocking findings -> filed D00 T01 §37
- [D00-T01-S35-PR3] [minor] No finding-ID disposition across fix-loop rounds -> filed D00 T01 §37
- [D00-T01-S35-PR4] [minor] Arch trigger verdict unrecorded; skip leaves no evidence -> filed D00 T01 §38
- [D00-T01-S35-PR5] [major] Arch gate outage rung undefined -> filed D00 T01 §38
- [D00-T01-S35-PR6] [minor] Gate lacks scheduled live proof -> rejected §15-item-9 deferred-proof convention governs; synthetic proof of a judgment gate is theater
- [D00-T01-S35-PR7] [minor] Arch round numbering, cap interaction, governance unstated -> filed D00 T01 §38
- [D00-T01-S35-PR8] [minor] Post-panel order (arch vs plan review) unstated -> filed D00 T01 §38
- [D00-T01-S35-PR9] [minor] All-Opus round count and sign-off positions unstated -> filed D00 T01 §37
- [D00-T01-S35-PR10] [minor] Review scope misses reverse dependents -> rejected wrong premise; reverse dependents in scope since §15 and §36 rode this run inline
- [D00-T01-S35-PR11] [minor] Rule name no longer describes mixed acceptance -> rejected stable-address name; semantics documented in the README row; rename is churn
- [D00-T01-S35-PR12] [minor] Fallback redefinition lacks compat guarantee -> filed D00 T01 §37
- [D00-T01-S35-PR13] [minor] Cost baseline unmeasured, no savings target -> accepted measured baseline recorded in this file; no TODO work owed
- [D00-T01-S35-PR14] [minor] Probes underprove model, effort, sandbox, delivery -> rejected evidence exists: probe headers name model/effort/sandbox, live 55KB rounds prove delivery; tied in this record
- [D00-T01-S35-PR15] [minor] §36 line anchor drifts, exclusions vague -> filed D00 T01 §36
- [D00-T01-S35-PR16] [minor] No per-round telemetry; caps tuned on anecdote -> filed D00 T01 §39
End of ledger

```text
Raw plan review output (check-plan PASS 16 findings, one per line):
- The mixed family sequence, tier, sign-off round, and caps are not validator-enforced; add enforcement and mutation tests to §35, because prose-only controls can be bypassed while still producing a valid stamp.
- Round 5 permits filing and stamping despite a blocking `needs-attention`; resolve this in §35 by forbidding stamps for unresolved safety, data-integrity, or stamp-invalidating findings, because a hard budget must not overrule correctness.
- Last-panel-wins does not prove that earlier blocking findings were fixed; add finding identifiers and explicit resolution checks to §35, because a clean later verdict could silently discard unresolved defects.
- Architecture-gate triggering is subjective and leaves no evidence when skipped; add a decision table plus a recorded triggered/not-triggered result to §35, because broad labels such as "editor state" are not auditable.
- The outage matrix does not cover an unavailable Opus-high architecture gate; define its fallback and no-stamp behavior in §35, because a mandatory pre-stamp gate currently has an undefined failure path.
- The architecture gate has no scheduled live proof; add a synthetic triggered fixture or dedicated proof section after §35, because the most consequential new path otherwise remains untested until an incidental future review.
- The relationship between panel rounds and architecture rounds is undefined; specify numbering, hard-cap interaction, headings, and which verdict governs in §35, because records and validators may interpret the extra rounds differently.
- §14 says plan review is post-stamp while its correction points to an unseen §15 after-panel-close rule, and §35 inserts architecture review before stamping; consolidate the exact order in §35 and §14, because reviewers cannot determine which findings may block a stamp.
- Sol-unreachable behavior says "all rounds on Opus" without defining whether the normal mandatory round count and sign-off positions remain; clarify this in §35, because an outage could cause redundant Opus rounds or inconsistent records.
- Plan review covers Depends and XREF neighbors but not reverse dependents such as §36; extend §14's scope to direct dependents, because downstream contracts are where many compatibility regressions appear.
- `stamp-no-opus-panel` and §9's Opus-only rationale no longer describe a validator that accepts mixed and GPT fallback panels; rename or deprecate the rule in §35 with compatibility handling, because misleading governance names invite incorrect maintenance.
- `plan-health/3` changes the semantic meaning of `fallback` without a schema-version change; either version the schema or add explicit compatibility guarantees in §35, because downstream automation can silently reinterpret existing data.
- The claimed 16-section, median-five-round cost baseline has no reproducible evidence or expected-savings target; add a measured report to §35, because the central cost-versus-quality decision cannot otherwise be reassessed.
- The echo probes prove runner reachability but not selected model, effort, read-only mode, or prompt delivery beyond echoing; strengthen §35's probe evidence, because the controls being claimed are more specific than the test.
- §36 anchors acceptance to README line 79 and permits vaguely defined "script history comments"; use content anchors and explicit included/excluded paths there, because line numbers drift and exclusions can hide active stale terminology.
- Add per-round model, effort, duration, outcome, token usage, and estimated cost telemetry to §35 and `plan-health`; this is a premium operational win because future cap tuning can be evidence-driven instead of anecdotal.
```

## Measured baseline (PR13)

Reproducible commands, run 2026-09-18 on the live tree (in-progress `D00-T01-s35.md` excluded):

Per-file (00-workspace): `for f in docs/reviews/00-workspace/D00-T*.md; do n=$(grep -c "^## .*panel" "$f"); echo "$n $(basename $f)"; done` gives 5 rounds ×11 files (s9, s10, s11, s13, s15, s16, s17, s19, s20, s21, s22), 4 ×2 (s23, D00-T02-s7), 3 ×2 (s12, s14), 2 ×1 (s18): 16 files, 71 rounds, median 5, mean 4.44.

Tree-wide: `for f in $(find docs/reviews -name "*.md" ! -name "README.md" ! -name "D00-T01-s35.md"); do grep -c "^## .*panel" "$f"; done` adds `01-notepad-core/D01-T02-s14` (5), `D01-T01-s32` (2), `D01-T02-s13` (1): 19 files, 79 rounds, median 5, mean 4.16.

Projection (the savings target §39 telemetry will check): a typical Full section drops from 5 Opus-medium rounds to 2 Sol-medium plus 1 Opus-medium; the worst case is unchanged (2 Sol plus 3 Opus at the hard cap). This review itself ran Sol R1 (4 findings), Sol R2 (1 finding), Opus R3 sign-off (1 advisory filed): 2 Sol plus 1 Opus, as projected.

## Probe evidence tie (PR14)

Model, effort, and sandbox ride the probe's own headers (stderr): `model: gpt-5.6-sol`, `reasoning effort: medium`, `sandbox: read-only`, exit 0, echo `runner-ok` returned (fenced above). Prompt delivery beyond echoing is proven by the live rounds: R1 prompt 54,914 bytes and R2 prompt 55,930 bytes both delivered over stdin and returned `check-panel PASS` verdicts. No element of the claimed control lacks its evidence.
