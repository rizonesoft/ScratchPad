# Review: D00 T01 §2 -- Solution Scaffold with One-Command Build

Candidate: `46319af` (single commit). No fixes; one round.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Scaffold | `src/IntelligentNotepad.slnx` (app + 3 `net10.0` libs) plus `src/Notepad.Neutral.slnf` (neutral filter); slnf green on Linux 0w/0e, slnx green on Windows 0w/0e, stub launched with versioned title observed |
| 2. Build doc | `docs/build.md` with the per-OS commands, provision and runtime prerequisites, run path, and provenance note |
| 3. Version report | Title carries `0.0.0+<sha>` plus runtime; `SourceRevisionId` stamps the binary (ProductVersion observed from the exe) |
| 4. Output layout | Per-project `bin/`/`obj` ignored and proven by clean status after both builds; `dist/` rule in place, publish unexercised (D07 publishes) |
| 5. Commit | `46319af`, subject matches the item plus the skill-mandated ref, corrections and the §3/§6 flag in the body |

## Self-review

1. Does it make sense? Yes. Every item has its change; the extras (RID-suffixed provisioners, slnf eol rule) are the recorded §2 decisions, and the §3/§6 premise flags are recorded, not acted on.
2. Is it logical? Yes. TFM, min version, unpackaged properties, RID inference, lockfile, and SourceLink agree and the Windows build proves the combination; the slnx carries no platform mapping so the RID inference is the single arch selector.
3. Did it break anything else? No. Both provisioners re-ran green into the new dirs (§1's per-OS checkpoint re-proven; §1's stamp line stays true); `validate` 0 fatal; `self-test` 391/391; no consumers exist yet.

## Lenses

- `adversarial`: approve. Tampered lockfile fails restore on contentHash; provisioners quote paths and verify before clobbering; the stub is unelevated with no network or consent surface.
- `consistency`: approve. Naming follows the plan, C# matches template conventions, Markdown keeps one line per paragraph and item, no em dashes. Advisory A1: the lockfile carries a `win-x64`-qualified section, so an ARM64 Windows restore must regenerate it (`--force-evaluate`); no section demands ARM64 today.
- `integration`: approve. §1's contracts extended compatibly; the slnf is a strict subset of the slnx; §3/§5/§6/T02 touchpoints flagged in the commit body. Advisory A2: the `?? "0.0.0"` title fallback would mask a future stamping break; D01 replaces the window wholesale, so no fix here.
- `record`: approve. Every claim quotes a real run; corrections are dated with spike evidence; zero deferrals. Note: `docs/build.md` points at the Windows App SDK release for a missing runtime without a URL (no guessed URLs); §6 sharpens it.
- `source-defect`: not owed (no Notepad behavior or protocol detail; the TFM floor was settled by build evidence).
- `design`: not owed (stub window with no Fidelity counterpart; D01 owns surfaces).

## Gates re-run

`slnf` rebuilt on Linux 0w/0e; `validate` 0 fatal; `self-test` 391/391; Windows build plus launch evidence stands (bytes unchanged since); fresh clones clean on Linux and Windows with the CRLF rules observed in effect; `plan --check` green after the stamp-flow sync.
