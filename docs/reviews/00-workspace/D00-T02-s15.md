# D00 T02 §15 review: Nightly Enforcement and Count Hardening

Candidate: `1211928..2a0da28` (base 6083c1d plus 5 fix loops; range opens at 6083c1d's parent per R5-F3; signed off 2026-09-21).
Tier: Full panel (the diff changes the governed-run scripts plus the test harness and scheduled-task surface: the night run, triage, provision, and every UI suite consume them).

## Self-review

Three questions over `git diff 1211928..b3ead49` (19 files, 2509 insertions, 284 deletions), read as a whole plus the four live proofs re-run at review time:

1. Makes sense: yes. Every item's Done names its mechanism plus its proof: enforcement allowlist plus unified counts plus smoke (items 1-3), report pointer plus soak phase plus split verdicts plus snapshot plus mutex matrix (4-8), captures plus fixtures plus conservation plus quarantine windows plus incidents (9-13), count pin plus per-project trx plus suite guard plus partition plus fingerprint plus filter fixture (14-19), teardown plus supervisor plus soak minimums plus retention plus mutex scope plus containment plus debt handoff (20-26). No scope beyond the contract.
2. Logical: yes. Capture fires exactly when the leg fails (R1-F1 aligned the condition with the fail verdict); log cells name transcript plus out.logs (R1-F2); per-project trx merge over the shared `$runAProjects` set; conservation aggregates only when every listed trx parses; placement plus population checks red before any leg runs; quarantine scoping keys on the exact list header; tombstone freshness keys on mtime plus Status.
3. Breakage: none found. Single-file legs pass one path each through the array signatures (fixtures unchanged and green); the fingerprint bootstrap matches the in-suite partition quote (150/2/35); provision re-verifies 9/9 with the supervisor action live; the trigger grandparent walk keeps direct readings identical. Self-finds fixed pre-commit: JobControl flag parse stops at `--` (child args cannot inject flags; probe refuses), timeout clamped to 24h (no uint wrap), supervisor mutex mirrors the run-mutex abandonment shape, retention prune regex captures the date (loose logs now plan), filter evaluator rejects `|`-in-value (probed red first).

## GPT panel Round 1

Round 1 over candidate `1211928..b3ead49` scoped to `tools/nightly.ps1` (Sol medium, run 20260920-D00-T02-S15-gpt, output check PASS; the intended three-file scope collapsed to the first pathspec, caught before round 2).

- `adversarial` needs-attention (R1-F1: gate-only or non-kill overrun skips capture)
- `consistency` approve
- `integration` approve
- `record` needs-attention (R1-F2: Log cells name the wrapper transcript, not the out.logs)

Telemetry: round 1; model gpt-5.6-sol; effort medium; duration unknown; outcome needs-attention; tokens unknown

Both real, both fixed. Disposition:

| ID | Disposition | Evidence |
| R1-F1 | fixed | `e586532` aligns the capture condition with the fail verdict (test, gate, or overrun) on both gated legs |
| R1-F2 | fixed | `e586532` builds log cells naming transcript plus every out.log (Run A lists all five) in core and final reports; fresh report quotes the five-name Run A cell |

Raw round-1 output (fenced, stripped from scans):

```
**adversarial: needs-attention** (1)
1. `tools/nightly.ps1:666-667` and `tools/nightly.ps1:688-689` mark Run A and Run B failed when the gate fails or the leg overruns, but invoke `Invoke-FailureCapture` only for a nonzero test code or killed suite. A gate-only failure or non-kill overrun therefore produces no required screenshots, window metadata, event slice, or capture record.

**consistency: approve**

**integration: approve**

**record: needs-attention** (1)
1. `tools/nightly.ps1:824-826` and `tools/nightly.ps1:918-920` advertise the wrapper transcript as each leg's `Log`, although `Invoke-ContainedSuite` redirects actual test output to separate `.out.log` files at `tools/nightly.ps1:661-663`, `tools/nightly.ps1:685`, `tools/nightly.ps1:722`, and `tools/nightly.ps1:746`. All three report rows therefore point triage away from the logs containing the test-run detail, and Run A additionally omits its four project-specific output logs.
```

## GPT panel Round 2

Round 2 over candidate `1211928..e586532` full 19-file scope (Sol medium, run 20260921-D00-T02-S15-gpt, output check PASS, duration 226 s).

- `adversarial` needs-attention (R2-F1: fail-open enforcement on missing trx; R2-F2: loose skip matching; R2-F3: multiline trait bypass)
- `consistency` needs-attention (R2-F4: Run A counts-only fingerprint)
- `integration` needs-attention (R2-F5: supervisor timeout past task kill; R2-F6: failed-no-trx soak loss)
- `record` advisory (R2-F7: fingerprint header mojibake)

Telemetry: round 2; model gpt-5.6-sol; effort medium; duration 226s; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R2-F1 | fixed | `78c336e`: `Get-NonQuarantineSkips` returns Ok/Names; missing/malformed trx is unproven and reds the run; fixtures `truncated-enforcement-unproven`, `missing-enforcement-unproven`, `enforce-unproven` |
| R2-F2 | fixed | `78c336e`: stamp-shape `QUARANTINED` (case-sensitive), case-sensitive `CAPABILITY:`, prefix-anchored legacy; fixture `skip-strict` |
| R2-F3 | fixed | `78c336e`: whole-file trait scan with computed start line; fixture `placement-multiline-stray` |
| R2-F4 | fixed | `78c336e`: Run A FQNs pinned sorted-unique with swap-red compare; fixtures `fingerprint-run-a-swap`, `fingerprint-run-a-required` |
| R2-F5 | fixed | `78c336e`: supervisor default 16200 to 14280 with provision pin `Test-SupervisorUnderLimit`; fixtures `supervisor-live-under`, `supervisor-over-red`, `supervisor-limit-unparseable-red`, `supervisor-equal-red` |
| R2-F6 | fixed | `78c336e`: failed and exit-0-without-trx rows plus explicit Ran gate; fixtures `soak-failed-no-trx`, `soak-exit0-no-trx`, `soak-failed-never-empty` |
| R2-F7 | fixed | `78c336e` (advisory folded into the open loop): header section sign by code point; fixture `fingerprint-header-encoding`; live bytes C2-A7, no C3-82 |

Raw output:

```text
**adversarial: needs-attention** (3)
1. `tools/NightlyParse.ps1:67-70` returns an empty skip list when the Interactive trx is missing or malformed, and both callers (`tools/nightly.ps1:728,752`) plus the verdict formatter (`tools/NightlyParse.ps1:728-734`) treat that as zero leaked skips, allowing enforcement to report GREEN without classifying any skip reasons. `tools/NightlyParse.Tests.ps1:60-63` locks in this fail-open behavior by asserting an empty result instead of an unclassifiable or failing state.
2. `tools/NightlyParse.ps1:79-80` excuses any skip whose message merely contains `QUARANTINED` or starts with `CAPABILITY:`, without validating the quarantine stamp shape or restricting capability codes to the enumerated attributes, so a bare or mislabeled skip can self-allowlist through free text.
3. `tools/NightlyParse.ps1:446-456` scans each source file line by line for the Primary trait, so an attribute split across lines never matches and a Primary-tagged test outside `tests/UI` bypasses the pre-build placement guard.
**consistency: needs-attention** (1)
1. `tools/NightlyParse.ps1:529-537,561-569` persists and compares only Run A counts while persisting and comparing Run B plus Interactive FQNs, so a Run A method swap that preserves counts keeps the fingerprint green even though the stated requirement is that every material membership change stale prior proofs.
**integration: needs-attention** (2)
1. `tools/tasks/nightly-ui.xml:27` caps the scheduled task at PT4H while `tools/NightlySupervisor.ps1:23` defaults the supervisor timeout to 16200 seconds, so the scheduler kills the production supervisor before its timeout path can publish the required RED tombstone. The header comment (`tools/NightlySupervisor.ps1:14-16`) documents this inversion instead of resolving it.
2. `tools/nightly.ps1:849-865` records only timed-out iterations in the soak-killed set, and `tools/NightlyParse.ps1:254-260,279` drops any non-timeout iteration without trx before emitting rows, so a failed iteration that produces no trx vanishes from the fourth-phase ledger and a fully failed soak prints `(no soak iterations ran: -SkipSoak)` instead of its RED verdict.
**record: advisory** (1)
1. `tests/UI/TestPopulation.fingerprint:1` contains the double-encoded section marker `Â§15` instead of `§15`, corrupting the fingerprint's provenance record.
```

## Opus panel Round 3

Round 3 over candidate `1211928..78c336e` full scope (Opus medium sign-off). Attempt 1 (run 20260921-D00-T02-S15-opus): producer failed fast (`Error: Reached max turns (1)`); the `--max-turns 1` cap is dropped for attempt 2. Producer: `cmd /c claude -p --model opus --effort medium --allowedTools Read -` (`--effort medium --allowedTools Read` per proven run-6 argv). Attempt 2 (run 20260921-D00-T02-S15-opus-r2): output check PASS, duration 93 s.

- `adversarial` needs-attention (R3-F1: Failed set ignored on trx-present soak legs)
- `consistency` needs-attention (R3-F2: stale reap-precedes-every-leg doc line)
- `integration` needs-attention (R3-F3: unbounded discovery invocations)
- `record` approve

Telemetry: round 3; model opus; effort medium; duration 93s; outcome needs-attention; tokens unknown

| ID | Disposition | Evidence |
| --- | --- | --- |
| R3-F1 | fixed | `ac281f8`: Failed set consulted on trx-present legs; fixtures `soak-failed-aborted-trx`, `soak-failed-keeps-names` |
| R3-F2 | fixed | `ac281f8`: testing.md reap line corrected to pre-flight (single call site verified) |
| R3-F3 | fixed | `ac281f8`: `Invoke-BoundedCapture` (job plus 180 s cap plus exit relay); fixtures `bounded-fast-relays`, `bounded-hang-kills`, `bounded-exit-relays`, `bounded-discovery-parses`, `bounded-discovery-hang-throws`; live discovery 150/167 2/2 35/35 |

Raw output:

```text
**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:301-311`: `Format-SoakLedger` consults `$Failed` only inside the `$null -eq $st` branch (line 297). A soak iteration that exits nonzero without being killed but still leaves a trx carrying no `Failed` outcomes (aborted/crashed host writing `outcome="Aborted"` rows, or an empty `<Results>`) falls through to the `else` at line 309, is printed `(proved)`, and increments `$proved`, so the phase verdict can read `- Verdict: GREEN (10/10 iterations proved)` for a night whose `ui-soak-N` was in `$soakFailed` (`tools/nightly.ps1:858`, `tools/nightly.ps1:872`). This is the exact hiding PR7 forbids, and `docs/testing.md:79` states soak status reads from the `## Soak` ledger.
**consistency: needs-attention** (1)
1. `docs/testing.md:47` still states "the reap precedes every leg", but the candidate removed both per-leg reap calls (the old `Invoke-TimedStep`/`Invoke-GatedLeg` kill paths) and `Invoke-OrphanReap` now has exactly one call site, the pre-flight at `tools/nightly.ps1:485` (`tools/nightly.ps1:116-130` re-describes it as transitional pre-flight only).
**integration: needs-attention** (1)
1. `tools/nightly.ps1:625` (`Get-UiTestDiscovery`, three `dotnet test --list-tests` invocations) and `tools/nightly.ps1:709` (`Get-ListTestsCases` on the budget-cut path) run `& $Dotnet` synchronously in-process via `tools/NightlyParse.ps1:631`, with no job bound, no cap, no containment, and no `Test-LegBudget` gate. Every other toolchain invocation in the run is bounded (`Invoke-BootstrapStep` 600 s at `tools/nightly.ps1:591-597`, `Invoke-ContainedSuite` at the leg caps), and `docs/testing.md:79` claims "hung toolchains strand nothing". A hung discovery therefore blocks the run past its deadline with no report; the 709 site is worst, since it only executes once the budget is already exhausted.
**record: approve**
```

Fix loop 1 (`e586532`): capture on any leg failure, log cells name every evidence file.

Fix loop 2 (`78c336e`): fail-closed enforcement, strict skip matching, multiline trait scan, Run A FQN pin, supervisor under the task kill, failed-no-trx soak rows, fingerprint header encoding.

Fix loop 3 (`ac281f8`): Failed set consulted on trx-present soak legs, reap doc line corrected, bounded discovery invocations.

## Opus panel Round 4

Round 4 over candidate `1211928..ac281f8` full scope (Opus medium sign-off). Four material findings; the delivered output numbered them F1, F2, F3, F6 (F4/F5 carried no content). Lens and telemetry detail compacted with the session; dispositions plus evidence below are exact.

| ID | Disposition | Evidence |
| --- | --- | --- |
| R4-F1 | fixed | `31bd1e9`: JobControl kill opens TERMINATE plus QUERY; killsim quotes pre-fix killed=0, fixed killed=1, outsider alive |
| R4-F2 | fixed | `31bd1e9`: `Test-InteractiveCaptureNeeded` trigger matrix (code, kill, unproven, leaks); fixtures `capture-matrix-code/killed/unproven/leaks/green-quiet`; both Interactive call sites |
| R4-F3 | fixed | `31bd1e9`: Run A expected set derives from `$runAProjects` at the call site; grand-sum skew quotes `trx 2/1/1 vs transcript 10/1/1`; killed-step trx skips the aggregate |
| R4-F6 | fixed | `31bd1e9`: forced soak skips thread `$SkipReason` (placement, deadline, build, drift); operator keeps the `-SkipSoak` row; fixtures `soak-forced-reason/placement/deadline/drift` plus `soak-operator-shape` |

The round's follow-up challenges (pre-fix Bin quote, capture test names, single-source derivation, skew quote shape, forced-skip names) were answered with executed proofs, not prose: killsim rerun, the five capture assert names, the line-802 expression evaluated against the line-54 list, the skew break string, and the five soak assert names.

Independent re-review of `31bd1e9` re-ran all four proofs against the tree: verdict SHIP (F1/F2/F3/F6 all CONFIRMED, suite 109 PASS, zero FAIL).

Fix loop 4 (`31bd1e9`): kill rights, capture trigger matrix, derived expected set, soak skip reasons.

Fix loop 5 (`2a0da28`): supervisor job sets its own cwd (self-found FL5-F1: Start-Job lands in Documents, every contained step MSB1009'd the 023003 night; probe quotes child-sees-csproj False pre-fix shape vs True fix shape; gate job all-absolute, untouched). Independent re-review re-ran the diff, the mechanism, the probe, the scope check, and the suite (109 PASS): verdict SHIP.

## Opus panel Round 5

Round 5 over candidate `1211928..2a0da28` full 21-file scope (Opus medium re-round; run 20260921-D00-T02-S15-opus-r4, output check PASS). At the Full hard cap: needs-attention leftovers disposition against the bar (safety, data-integrity, stamp-invalidating); below-bar findings file forward.

`adversarial` needs-attention
`consistency` needs-attention
`integration` approve
`record` needs-attention

| ID | Disposition | Evidence |
| --- | --- | --- |
| R5-F1 | filed | D00 T02 §22 (all-skipped banner drops its transcript row, reding healthy runs; below bar: unhandled shape, not a broken mechanism) |
| R5-F2 | filed | D00 T02 §22 (testing.md procedure omits the .out.log siblings; doc-only, below bar) |
| R5-F3 | fixed | the stamp commit (record Candidate/Sign-off plus TODO-02 §15 Commit line corrected to `1211928..2a0da28`; 6083c1d's parent is 1211928, matching every round's reviewed coverage; hash unknowable at write time) |

Telemetry: round 5; model opus; effort medium; duration unknown; outcome needs-attention; tokens unknown

Raw output:

```text
**adversarial: needs-attention** (1)
1. `tools/NightlyParse.ps1:41` matches only the `Passed!`/`Failed!` VSTest banners, so an assembly whose run is entirely skipped (`Skipped!  - Failed: 0, Passed: 0, Skipped: N, Total: N - UI.dll (net10.0)`, a shape this repo has already observed and quoted at `docs/reviews/00-workspace/D00-T02-s12.md:142`) contributes no transcript row. Under this candidate that silent drop is now load-bearing twice: `tools/NightlyParse.ps1:243` reports `expected-set: <asm>.dll missing from assembly rows` for a completed leg, and `tools/NightlyParse.ps1:220-227` reports `cross-level-aggregate: trx a/b/c vs transcript x/y/z` because the merged Run A trx still carry that project's skips. An all-skipped Smoke, Unit, Protocol, or Interactive assembly therefore reds a healthy run with a conservation break, and `Get-LegSummary` (`tools/NightlyParse.ps1:139-146`) under-reports that project's skip count in the leg cell at the same time.

**consistency: needs-attention** (1)
1. `docs/testing.md:55` still enumerates the run's evidence as the three leg transcripts `YYYY-MM-DD-HHmmss-{default,primary,full}.log` plus trx and gate logs, but this candidate moved all suite stdout into `.out.log` siblings (`tools/nightly.ps1:663` per-project `$stamp-default-<proj>.out.log`, `tools/nightly.ps1:695` `$stamp-primary.out.log`, `tools/nightly.ps1:733`/`:760` `$stamp-full.out.log`, `tools/nightly.ps1:865`/`:879` `soak-*.out.log`) and the report's Log cells now name them (`tools/nightly.ps1:840-842`). The procedure paragraph that triage reads to find logs omits the files that actually carry the test output, which is the same defect R1-F2 raised against the report cells.

**integration: approve**

**record: needs-attention** (1)
1. The declared candidate range excludes one of the section's own commits. `todo/00-workspace/TODO-02-test-backbone.md:491` records `Done: b3ead49 (range so far 6083c1d..b3ead49)` and `docs/reviews/00-workspace/D00-T02-s15.md:3` records `Candidate: 6083c1d..b3ead49`, but `6083c1d` is itself §15 implementation work (`workspace: harden nightly parsing plus reports (D00 T02 §15)`, touching `tools/NightlyParse.ps1`, `tools/nightly.ps1`, `tools/NightlyParse.Tests.ps1`, `docs/testing.md`, `tests/UI/HookFactAttribute.cs`, `tests/UI/PrinterFactAttribute.cs`, and this section's checklist), so in git range notation it is excluded from the range the stamp will name. Every review actually run covered it: `docs/reviews/00-workspace/D00-T02-s15.md:9` (self-review `git diff 1211928..b3ead49`, 19 files), `:21` (GPT R1 `1211928..b3ead49`), `:62` (GPT R2 `1211928..e586532`), `:88` (Opus R3 `1211928..78c336e`). The recorded candidate identity and the reviewed candidate identity disagree by one commit of shipped code.
```

## Plan review

Round: `gpt-5.6-sol` high over §15 plus 4 corpus sections, 2026-09-21. Neighbors: D00 T02 §9 (Depends), §13 plus §14 (filed here, ledger contract), §16 (successor boundary); dependents: none. Output check-plan PASS (30 findings, one per line). A first attempt went FAIL on producer start (WinError 2: bare codex unresolvable without a shell; r2 minted, no findings); the cmd /c retry (r3) delivered the 30 below. No criticals; 7 majors filed, 20 minors filed, 3 minors rejected.

Manifest: sections [D00 T02 §9, D00 T02 §13, D00 T02 §14, D00 T02 §15, D00 T02 §16]; dependents [none]; bytes 61620; run 20260921-D00-T02-S15-gpt-r3

Ledger:

- [D00-T02-S15-PR1] [minor] timer section edges -> filed D00 T02 §16
- [D00-T02-S15-PR2] [major] timer proof correlation -> filed D00 T02 §16
- [D00-T02-S15-PR3] [major] recurrence definition -> filed D00 T02 §16
- [D00-T02-S15-PR4] [major] green definition -> filed D00 T02 §16
- [D00-T02-S15-PR5] [major] no manufactured green -> filed D00 T02 §16
- [D00-T02-S15-PR6] [minor] parent death diagnosis -> filed D00 T02 §16
- [D00-T02-S15-PR7] [minor] durable recovery journal -> filed D00 T02 §16
- [D00-T02-S15-PR8] [minor] recovery tombstone dedup -> filed D00 T02 §16
- [D00-T02-S15-PR9] [minor] session gap acceptance shape -> filed D00 T02 §16
- [D00-T02-S15-PR10] [major] never launched case -> filed D00 T02 §16
- [D00-T02-S15-PR11] [major] durable evidence both runs -> filed D00 T02 §16
- [D00-T02-S15-PR12] [minor] duration baseline -> filed D00 T02 §16
- [D00-T02-S15-PR13] [minor] env trend dimensions -> filed D00 T02 §17
- [D00-T02-S15-PR14] [minor] dirty content fingerprint -> filed D00 T02 §16
- [D00-T02-S15-PR15] [minor] post run hash reverify -> filed D00 T02 §16
- [D00-T02-S15-PR16] [major] project omission gate -> filed D00 T02 §16
- [D00-T02-S15-PR17] [minor] discovery authority -> rejected C# only xUnit suite: attribute names match in source regardless of subclassing; no generators; no non-C# tests; counts already cross-checked by the fingerprint
- [D00-T02-S15-PR18] [minor] trend ownership wording -> filed D00 T02 §15
- [D00-T02-S15-PR19] [minor] quarantine notice vs delivery -> filed D00 T02 §15, D00 T02 §17
- [D00-T02-S15-PR20] [minor] machine readable result -> filed D00 T02 §17
- [D00-T02-S15-PR21] [minor] capture policy -> filed D00 T02 §22
- [D00-T02-S15-PR22] [minor] KEEP quota -> filed D00 T02 §22
- [D00-T02-S15-PR23] [minor] checksum signing -> rejected tamper evidence beyond the local operator threat model; accidental corruption scope declared
- [D00-T02-S15-PR24] [minor] incident key refinement -> filed D00 T02 §22
- [D00-T02-S15-PR25] [minor] latest verification -> filed D00 T02 §16
- [D00-T02-S15-PR26] [minor] layout contract conflict -> rejected living contract docs/testing.md:49/55/80 already names per-project trx, stamp dirs, and retention citing §15; §9 text is dated stamped history
- [D00-T02-S15-PR27] [minor] separate follow-up sections -> filed D00 T02 §16, D00 T02 §17, D00 T02 §22
- [D00-T02-S15-PR28] [minor] stand-down surfacing -> filed D00 T02 §17
- [D00-T02-S15-PR29] [minor] run ID consistency -> filed D00 T02 §16
- [D00-T02-S15-PR30] [minor] alert routing classes -> filed D00 T02 §17
End of ledger

## Live proof

Digests below are sha256 over the quoted outputs (UTF-8, no trailing newline). Quotes ran on the stamp working tree whose code is byte-identical to candidate tip `2a0da28` (the stamp commit adds this file's review sections plus the §15 stamp lines, §16/§17/§22 filings, and row flips only). Killsim, cwd probe, derived-set, and skew quotes ran as quoted scratch proofs in the R4/R5 dispositions above, not re-run here.

`py scripts/todo-graph.py validate`: `28 todos, 262 sections -- 0 fatal, 0 warning(s)`.

Provenance: candidate 2a0da286f28d293dea119e7efe1f9002f926a09d; command py scripts/todo-graph.py validate; exit 0; tool CPython 3.14.3; digest d5f29546c16fd025770839721fee6c4a7e807e097d039da6fe5bcb38fff3d59b; path scripts/todo-graph.py; run 20260921-D00-T02-S15-gpt-r3

`py scripts/todo-graph.py plan --check`: `implementation plan is current -- 99/257 sections complete (39%)`.

Provenance: candidate 2a0da286f28d293dea119e7efe1f9002f926a09d; command py scripts/todo-graph.py plan --check; exit 0; tool CPython 3.14.3; digest 886fc61df3583bca94fe910e2e44bacc9ab2322c1a1cc67719e984df10929acd; path scripts/todo-graph.py; run 20260921-D00-T02-S15-gpt-r3

`powershell -NoProfile -ExecutionPolicy Bypass -File tools/NightlyParse.Tests.ps1`: `NightlyParse.Tests: all green` (109 PASS lines, 0 FAIL).

Provenance: candidate 2a0da286f28d293dea119e7efe1f9002f926a09d; command powershell -NoProfile -ExecutionPolicy Bypass -File tools/NightlyParse.Tests.ps1; exit 0; tool Windows PowerShell 5.1; digest 305aa922f9b07dcb0445d62fe62550994c13d937c6247edba265eaeb23c206ab; path tools/NightlyParse.Tests.ps1; run 20260921-D00-T02-S15-gpt-r3

`powershell -NoProfile -ExecutionPolicy Bypass -File R:\tmp\parsecheck.ps1`: `tools/nightly.ps1 errors=0; tools/NightlySupervisor.ps1 errors=0; tools/provision.ps1 errors=0` (three output lines joined).

Provenance: candidate 2a0da286f28d293dea119e7efe1f9002f926a09d; command powershell -NoProfile -ExecutionPolicy Bypass -File R:\tmp\parsecheck.ps1; exit 0; tool Windows PowerShell 5.1; digest 52111bcd0c0ef0084e219bf60cca20b7dc21626d18c55452cb91c916d1b77754; path tools/nightly.ps1; run 20260921-D00-T02-S15-gpt-r3

## Arch gate

Arch trigger: none - tooling plus scripts plus docs; no editor, storage, extensibility, AI, protocol, or consent surface.

## Sign-off

Candidate range `1211928..2a0da28` (base 6083c1d plus 5 fix loops). All rounds closed: GPT R1, GPT R2, Opus R3, Opus R4, FL4 re-review SHIP, FL5 re-review SHIP, Opus R5 (F1/F2 filed §22, F3 range identity corrected here). Stamped 2026-09-21.
