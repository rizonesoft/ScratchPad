# Review: D00 T01 §19

Candidate: `879f007` (ship) + `be14f5b` (self-review) + `638a608` (R1 fix) + `2a1ae8d` (R2 fix) + `eb27912` (record) + `182be67` (R3 fix) + `5272b6e` (R4 fix) + `585bc33` (record) + `b0c499e` (post-R5 touch-up). Full panel (scripts plus skill: shared contract surface).

## Self-review

Sense: all 22 work items land as specified plus the commit. Each Done-when maps to code: run shape in the skill plus manifest/query (item 1), extracted `review_dependents` with the bound justification (item 2), rule 22 over git history (item 3), marker lineage in rule 17 (item 4), `partial:` vocab (item 5), escalation in degraded entries (item 6), owner/due legality plus surfacing (item 7), fix-commit clearance (item 8), recursive rule 21 (item 9), ledger blocks with the LIKE retirement (item 10), checked fencing (item 11), provenance mandate (item 12), total keys plus types plus schema /2 (item 13), gate mode (item 14), migration note (item 15), AGENTS lifecycle policy (item 16), §17 checkpoint reconciliation (item 17), battery citations plus trigger retirement (item 18), count cross-check (item 19), count grammar (item 20), dispatch coverage (item 21), scored record (item 22).

Logic: the validator, query, skill, and README agree on every new shape (run IDs, blocks, partial, escalation, fix tokens, cascade); fixture counts preserved where behavior kept (§4 4, §6 4, §8 7, §9 0, §11 4, §14 1, §15 1, back-link 1, dup 1, legacy 2).

Blast radius: one behavior change outside the review loop (none: all edits are review-loop code, its fixtures, its docs, and dated annotations); live tree silent (validate 0 fatal, plan-health dimensions preserved, `--check` exits 0).

Fix: `_FINDING_ITEM_RE` used `\d` (Unicode digits would tally toward ASCII counts), fixed to `[0-9]` with a locking case in `be14f5b`, the self-review commit. AGENTS count carried at each step (609 at ship+self-review, 613 after R1, 614 after R2, 615 after R3, 617 after R4, 619 after the post-R5 touch-up).

## Live proof

`python3 scripts/todo-graph.py self-test` on `b0c499e`: `todo-graph self-test: 619 cases, 0 failed`, exit 0.

Provenance: candidate b0c499e; command `python3 scripts/todo-graph.py self-test`; exit 0; tool CPython 3.12.3; digest c90cb5111ef80f89a658d8b0523e066bc6aea1b4d3337ca8170e289b4a9b3af1; path docs/reviews/00-workspace/D00-T01-s19.md

`python3 scripts/todo-graph.py validate` on `b0c499e`: `22 todos, 185 sections -- 0 fatal, 0 warning(s)`, exit 0.

Provenance: candidate b0c499e; command `python3 scripts/todo-graph.py validate`; exit 0; tool CPython 3.12.3; digest e8ffcc23a00a66964f2911364c87620b54a7d21155a981c501d39dd351bc116b; path docs/reviews/00-workspace/D00-T01-s19.md

`python3 scripts/todo-graph.py plan --check` on `b0c499e`: `implementation plan is current -- 57/180 sections complete (32%)`, exit 0.

Provenance: candidate b0c499e; command `python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 069c966fdaed20a3fc3be64b9713eb7d6f5337757028b094852ca9c04b05089d; path docs/reviews/00-workspace/D00-T01-s19.md

`python3 scripts/todo-graph.py query plan-health --check` on `b0c499e`: `gate: ok`, exit 0.

Provenance: candidate b0c499e; command `python3 scripts/todo-graph.py query plan-health --check`; exit 0; tool CPython 3.12.3; digest 0eead5f6297fe3e36e6331a167d08b280e827c5ecc203d961ca4c50587d382e0; path docs/reviews/00-workspace/D00-T01-s19.md

## Opus panel (round 1)

Candidate: `879f007` + `be14f5b`. Shape note: the round-1 output carried bare counts (`**v** 1`, no parens), so `check-panel` FAILed it; the prompt never showed the `(N)` shape, which the R1 fix corrects in the skill template. The verdicts below are transcribed from the fenced raw output (digest `04a34de7322ead150f790ed4fb1828e4f7d07c352220afbda964482b391f6e68`); every finding was verified against the code before fixing, and round 2 re-ran on the checked pipeline.

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings (all verified true, all fixed in `638a608`): adversarial `--check`/`--fail-on` elif swallowing (now union, stale-only union case); consistency-1 checked fencer with no production caller (`fence` subcommand added, both templates rewired; round 2 runs the checked pipeline live); consistency-2 stale `plan-review-malformed` severity comment (restated to blocks). Fix: `638a608`.

Raw round-1 output (fenced, not part of the verdict scan):

```
**adversarial: needs-attention** 1

1. `scripts/todo-graph.py:1803-1806` — `--check` and `--fail-on` are dispatched as `if/elif`, so `--check --fail-on stale` silently discards the explicit dimension and exits 0. Verified live: `query plan-health --fail-on stale` exits 1, `query plan-health --check --fail-on stale` prints `gate: ok` and exits 0 on the same tree. The skill (`.claude/skills/review-todo-section/SKILL.md:144`) and §19 item 14's Done text both promise stale stays "gateable explicitly", so a CI gate written as `--check --fail-on stale` reads green while stale scope is unchecked.

**consistency: needs-attention** 2

1. `.claude/skills/review-todo-section/SKILL.md:101` claims "Tags carry 64 bits of entropy, are collision-checked against every payload chunk with bounded retries before the prompt ships, and generation refuses rather than degrading on exhaustion", but the documented pipeline at `SKILL.md:87` and `SKILL.md:137` builds the prompt from `python3 scripts/review_prompt.py tag PANEL` plus shell `echo`/`cat`, which never checks the payload. `scripts/review_prompt.py:79` `fence_chunks_checked` has no production caller (`grep` finds only `scripts/todo-graph.py:6845,6855,6864` self-test uses) and no `__main__` dispatch (`scripts/review_prompt.py:146-153` handles only `tag`, `check-panel`, `check-plan`), so no live panel or plan-review prompt is collision-checked.
2. `scripts/todo-graph.py:873-875` — the `plan-review-malformed` severity comment still reads "a Plan review section without its Manifest line, or a ledger-looking line outside the row shape", describing the heuristic this candidate deletes and omitting the `Ledger:`/`End of ledger` block requirement. Every sibling site was updated (`scripts/todo-validate.py:847-853`, `todo/README.md:396`, `SKILL.md:128`); this is the one stale copy of the rule's description.

**integration: approve**

**record: approve**
```

## Opus panel (round 2)

Candidate: `879f007` + `be14f5b` + `638a608` (checked `fence` pipeline, R1 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` approve
- `integration` approve
- `record` advisory

Findings: adversarial deferred-vocabulary rows evade owner/due (validator-legal, query-UNACCOUNTABLE), fixed in `2a1ae8d` (query reports the review date as the due date; skill plus README say so; PR18 case locks it). Record advisory: item 12's demonstration file missing at review time, closed by this file (Provenance lines above). Fix: `2a1ae8d`.

Raw round-2 output (fenced, not part of the verdict scan; digest `7d71de08c5f7116a02399af4b89dcd47edb635d7b959d16098fb02eff4be5186`):

```
**adversarial: needs-attention (1)**

1. `scripts/todo-validate.py:895` — rule 18 requires `owner` plus `due` only on `accepted` critical/major rows, so an open high-severity finding evades §19 item 7 entirely by being triaged `deferred`: `- [D00-T01-S19-PR1] [critical] Big gap -> deferred owner ann date 2026-10-01 trigger review-lands` passes the deferred leg (owner + any date + trigger, `todo-validate.py:872-877`) with no `due` at all. Verified live: `OWNER_RE` matches that rest, `DUE_RE` does not. The same row is then appended to `criticals` by `scripts/todo-graph.py:1678` and rendered with `due ?  UNACCOUNTABLE` (`todo-graph.py:1745-1749`, JSON `due: ""`), so the validator calls it legal while the report calls it unaccountable, and nothing can clear it. Doc sites in the same family: `.claude/skills/review-todo-section/SKILL.md:153` states "Every open (accepted or deferred) critical or major carries `(owner <name>, due <YYYY-MM-DD>)` on its row" (unenforced for deferred, and in conflict with the deferred row's own `date` vocabulary on the same line), and `todo/README.md:397` names only "an `accepted` critical or major without owner and due" in the `plan-review-malformed` class.

**consistency: approve**

**integration: approve**

**record: advisory (1)**

1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:518` — item 12's Done text claims "skill mandates the `Provenance:` line on every live quote; this section's own findings file demonstrates it", but `docs/reviews/00-workspace/D00-T01-s19.md` does not exist in the tree or in the candidate diff, and no `Provenance:` line exists anywhere under `docs/reviews/00-workspace/` (the only hit repo-wide is `docs/reviews/00-workspace/D00-T02-s7.md:53`, a different shape). The demonstration half of the claim is unbacked until the stamp commit writes the record, exactly as §17 item PR22 was corrected for previously.
```

## Opus panel (round 3)

Candidate: `879f007` + `be14f5b` + `638a608` + `2a1ae8d` + `eb27912` (checked `fence` pipeline, R1+R2 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings (all verified true, all fixed in `182be67`): adversarial deferred majors dropped from plan-health (majors now collects accepted plus deferred, header "open majors", PR19 case); consistency-1 authored em dashes at five sites (replaced; fenced reviewer quotes stay byte-exact); consistency-2 §17 item 16 Done contradicted by /2 (dated supersession note). Fix: `182be67`.

Raw round-3 output (fenced, not part of the verdict scan; digest `87190597f2ac4508379f79e1ea319c6f6f6c6a2003fbfe0f09f553c543d1abbb`):

```
**adversarial: needs-attention (1)**

1. `scripts/todo-graph.py:1670` and `scripts/todo-graph.py:1689` — an open `major` triaged `deferred` is dropped from plan-health entirely: `majors` collects only `sev == "major" and disp == "accepted"`, and the next guard `if sev != "critical": continue` discards every non-accepted major, so §19 item 7's "plan-health surfaces the missing ones" and the aging/overdue visibility §17 item 11 exists for are both escapable one triage level down (deferred criticals are collected at `todo-graph.py:1694`, deferred majors are not). Verified live on a scratch tree carrying `- [D90-T01-S1-PR1] [major] Deferred open major -> deferred owner ann date 2026-01-01 trigger later` (a past date, validator-legal by rule 18's deferred leg): text report prints `accepted majors 0 (0 overdue)`, `--json` returns `"majors": []`, and `query plan-health --fail-on majors` exits 0 while the sibling deferred critical is listed with `owner ann due 2026-01-01`; the row is invisible to the `majors` dimension of `--check` (`todo-graph.py:1819`) forever.

**consistency: needs-attention (2)**

1. Em dashes in authored prose, which `AGENTS.md:44` forbids, introduced at five sites by this candidate: `todo/00-workspace/TODO-01-repo-and-toolchain.md:513` ("panel adversarial — deferred rows"), `:517` ("panel consistency-1 — the checked fencer"), `:520` ("panel adversarial — `--check` plus `--fail-on`"), and `docs/reviews/00-workspace/D00-T01-s19.md:13` ("Unicode digits would tally toward ASCII counts) — fixed to `[0-9]`") and `:70` ("query-UNACCOUNTABLE) — fixed in `2a1ae8d`"); every pre-existing `**Corrected …:**` line in the same file (`:370`, `:421`, `:461`, `:462`, `:464`, `:487`) uses a colon or comma instead, and the quoted reviewer blocks at `D00-T01-s19.md:49`, `:54`, `:77`, `:85` are exempt as byte-exact capture.
2. `todo/00-workspace/TODO-01-repo-and-toolchain.md:463` — §17's stamped Done text still reads "`schema: plan-health/1`, every list sorted by its first field, exit 0 always (usage errors exit 2)", and this candidate contradicts all three (schema `plan-health/2` at `scripts/todo-graph.py:918`, total tuple sort keys at `todo-graph.py:1737-1751`, exit 1 under `--check`/`--fail-on` at `todo-graph.py:1845` and `:1856`), with no dated annotation; every sibling stamped claim this candidate superseded got one (`:467` "**Retired 2026-09-18 (§19 item 10)**", `:470` "**Reconciled 2026-09-18 (§19)**", `:487` "**Battery-cited 2026-09-18 (§19)**"), and §19 item 17's Done-when is exactly "no stamped canonical status contradicts shipped code".

**integration: approve**

**record: approve**
```

## Opus panel (round 4)

Candidate: `879f007` + `be14f5b` + `638a608` + `2a1ae8d` + `eb27912` + `182be67` (checked `fence` pipeline, R1-R3 output appended; output `check-panel PASS`).

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` advisory

Findings (all verified true, all fixed in `5272b6e`): adversarial finding rows never fire overdue on blown due dates (majors overdue by age or due, criticals gain `overdue` plus `escalation`, PR22/PR23 cases); consistency stale overdue-days comment (restated to open majors); record findings file stale at R3 (this refresh: candidate line, round sections, Live proof on `5272b6e`). Fix: `5272b6e`.

Raw round-4 output (fenced, not part of the verdict scan; digest `527553957f233c0723e07c01e3d043f3b314221fa9d5aedde8ce5ad019bed180`):

```
**adversarial: needs-attention (1)**

1. `scripts/todo-graph.py:1687` — an open critical or major's `due` date is never compared to today: `overdue` for majors is computed solely from the review stamp age (`bool(not since or since < old_line)`, `todo-graph.py:1686`) and criticals carry no overdue flag at all (`todo-graph.py:1694`, `:1751`), so §19 item 6's stated trigger ("the passed due date") and item 7's accountability never fire on finding rows even though degraded markers on the same report do compare `due < today` (`todo-graph.py:1552`); verified live by inserting `- [D00-T01-S18-PR90] [major] Blown due date -> accepted owner ann due 2020-01-01` into the `D00-T01-s18.md` ledger (validator-legal, `validate` 0 fatal), after which the text report prints `open majors 1 (0 overdue)` and `owner ann  due 2020-01-01` with no `OVERDUE` and no `escalate operator`, and `--json` returns `{"id": "D00-T01-S18-PR90", ..., "overdue": false, "due": "2020-01-01"}`, so automation keyed on the typed `overdue` field reads a row six years past its due date as on time.

**consistency: needs-attention (1)**

1. `scripts/todo-graph.py:904` — the `PLAN_REVIEW_OVERDUE_DAYS` comment still reads "An accepted major older than this many days past its review's stamp is overdue (D00 T01 §17 item 11)", describing the accepted-only dimension the R3 fix widened; every sibling site was updated in `182be67` (the collector comment at `todo-graph.py:1670-1678` now reads "Open majors age visibly ... deferred majors count too", the text header at `:1888` is "open majors", and §19 item 7's Done text at `todo/00-workspace/TODO-01-repo-and-toolchain.md:513` names the rename), and the remaining "accepted major" hits (`:5710`, `:5792`, `:6315`, `:6320`) correctly describe accepted fixture rows, leaving `:904` as the one stale copy of the rule.

**integration: approve**

**record: advisory (1)**

1. `docs/reviews/00-workspace/D00-T01-s19.md:3` — the record does not cover the candidate range it is reviewing: the Candidate line names only `879f007` + `be14f5b` + `638a608` + `2a1ae8d` while the range now includes `eb27912` and `182be67`, there is no `## Opus panel (round 3)` section although round 3 ran and its fixes shipped in `182be67` (rounds 1 and 2 each have one, with transcribed verdicts and fenced raw output), and the Live proof at `:17` plus the count ledger at `:13` quote `614 cases` on `2a1ae8d` while the tree at `182be67` runs 615 (`todo-graph self-test: 615 cases, 0 failed`, matching `AGENTS.md:55`), so the provenance digests at `:19`, `:23`, `:27`, `:31` bind evidence to a superseded commit.
```

## Opus panel (round 5)

Candidate: `879f007` + `be14f5b` + `638a608` + `2a1ae8d` + `eb27912` + `182be67` + `5272b6e` + `585bc33` (checked `fence` pipeline, R1-R4 output appended; output `check-panel PASS`). Final round of five; no re-run.

- `adversarial` needs-attention
- `consistency` needs-attention
- `integration` approve
- `record` approve

Findings (both verified true, both closed by post-round touch-up `b0c499e`, no carry-forward): adversarial clearance git leg vacuous (any sha with the back-link cleared, including the filing commit itself) — touch leg added (the sha must have touched the target file) and item 8 restated to attribution bound to the target's post-finding stamp with the remediation boundary explicit, PR24 case; consistency lineage class row missing the pre-cutoff boundary (dated stamps skip lineage, the whole live tree) — boundary sentence added. Fix: `b0c499e`.

Raw round-5 output (fenced, not part of the verdict scan; digest `acb83103b755add49dabad19b6ff20b5153b0455638ec9e7767be222d46e6747`):

```
**adversarial: needs-attention (1)**

1. `scripts/todo-graph.py:1777-1783` — the clearance "git leg" proves nothing a fix must have done: it reads `fix <sha>` out of the target section and passes as soon as that commit's copy of the **target file** matches the finding ID (`git_file_at(fm.group(1), tpath)` plus `\bID\b`), and the ID is present in the target file solely because of the `-> SOURCE: ... <ID>` back-link that the preceding leg already checks (`todo-graph.py:1756-1767`). So `fix <sha>` naming the filing commit itself clears a filed critical with zero remediation in it. The shipped positive fixture is exactly that shape: `scripts/todo-graph.py:5934` cans `("aaa1111", marker_todo)` to `marker_todo.read_text()`, i.e. the tree's own todo bytes, whose only occurrence of `D90-T07-S4-PR1` is the back-link at `:5491` (`-> SOURCE: fixture-health D90-T07-S4-PR1 ... fix aaa1111`); the negative (`bbb2222`, `:5935`) fails only because its bytes are `"nothing fixed here"`. §19 item 8's Done-when at `todo/00-workspace/TODO-01-repo-and-toolchain.md:514` is "the query proves the reviewed candidate contains the fix", and `.claude/skills/review-todo-section/SKILL.md:153` promises "a filed critical clears only when the target also names `fix <sha>` and the commit's own tree contains the ID" — the implemented predicate is satisfied by the commit that created the back-link, so the only thing rejected is a sha whose target file lacks the ID (a typo or an unrelated commit), never an unfixed finding.

**consistency: needs-attention (1)**

1. `todo/README.md:401` — the `plan-review-no-lineage` class row states the exemptions ("Outage markers and markers over record-less findings are exempt") but omits the pre-cutoff grandfathering the code applies: the lineage checks sit after `scripts/todo-validate.py:622` (`if s.stamped_on is not None and s.stamped_on <= graph.PLAN_REVIEW_CUTOFF: continue`), so every stamp dated on or before 2026-09-18 skips lineage entirely, which is the whole live tree (all §§13-19 stamps are 2026-09-18; `validate` reports 0 fatal with no live marker carrying a run). Every sibling row in the same table names its boundary — `:397` "Pre-cutoff records predate the shapes and are grandfathered", `:399` "Reviews stamped on or before 2026-09-18 predate the back-link requirement", `:400` (`plan-review-duplicate-id`) "stamped on or before 2026-09-18 predate the uniqueness rule", `:402` "Uncommitted findings skip" — and the `Plan review:` bullet at `todo/README.md:156` carries "Required on stamps dated after 2026-09-18" for the marker itself, leaving `:401` the one class description that reads as enforced everywhere.

**integration: approve**

**record: approve**
```
