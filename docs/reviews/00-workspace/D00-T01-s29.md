# Review: D00 T01 §29 -- Unattended Checks and Risk Visibility

Candidate: `2c9af72` + `c8b3a9d` + `6a4757f` + `dadc2dd` + `164b34e` + `93dd4e3` (SHIP plus R1 fixes plus the subject-recovery commit plus R2 fixes plus two filing commits; output `check-panel PASS` on all three headless rounds, voided run excluded). Panel tier: Full (the diff touches `scripts/todo-graph.py`, the review skill, the plan workflow, plus README and AGENTS contract surface), Sol rounds 1-2, Opus round-3 sign-off.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. No separate self-review fix commit: the validation pass had already recorded its corrections on items 1-5, 7, and 8 before the build, and the diff matched the corrected contract.

Does it make sense: nine items take scheduled enforcement plus notify, the risk register plus dashboard, review-date firing, the inert dimension, the migration-zero wiring, the digest tally, cause-coded noncoverage, and the evidence tri-state. Exactly the contract, including the recorded shapes (flat notify payloads with a `gh` poster, contents-gated register sync, warn-only inert, cause channel without a new dimension).

Is it logical: the gates the job runs are the gates the queries define; residuals read owed states off the debt maps with `none`/`unmatched` at the edges; superseded skips stay silent under the head-wins rule; the schema bump to /5 carries the inert dimension plus the cause keys. The record advisory later confirmed the one gap self-review missed (the AGENTS.md count).

Did it break anything else: suite moves 829 to 856 by exactly the 27 new probes; live validate stays 0 fatal; plan-health --check stays green. Blast radius is the query surface plus the plan workflow, both covered.

## GPT panel (round 1)

- `adversarial` needs-attention: (1) a superseded predecessor sets the first noncoverage cause, hiding the head's post-dated, stale-evidence, or git-unresolvable cause.
- `consistency` needs-attention: (1) run and outage acceptances read flat `unreviewed` instead of owed-state residuals.
- `integration` needs-attention: (1) the notify step runs after fail-fast gates, so a gate failure skips owner notification.
- `record` needs-attention: (1) the SHIP subject drops the contracted `per eighth live round` suffix.

Fix record: head-wins causes with the §8 lock; marker residuals read owed states (`none`/`unmatched` at the edges); notify plus summary steps run `always()`; the contracted subject rides the empty recovery commit `6a4757f` (amend and force-push forbidden, and the panel had already fingerprinted `2c9af72`). Fixed in `c8b3a9d` plus `6a4757f`; gates re-ran green before round 2 (validate 0 fatal, plan current).

Raw round-1 output (fenced, not part of the verdict scan; digest `e69751c1ffb89fe15d854690a066acadc12889e5d5d86537a3a85e397e8615c9`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:2520`, `scripts/todo-graph.py:2547`, `scripts/todo-graph.py:2758`, `scripts/todo-graph.py:2785`: a superseded predecessor sets the first noncoverage cause, preventing a current chain head’s post-dated, stale-evidence, or git-unresolvable cause from replacing it; the reported cause and remediation are therefore incorrect.

**consistency: needs-attention** (1)
1. `scripts/todo-graph.py:3544`, `todo/00-workspace/TODO-01-repo-and-toolchain.md:816`: run and outage acceptances receive residual value `unreviewed`, although the contract requires residual severity derived from the marker’s owed-state weight.

**integration: needs-attention** (1)
1. `.github/workflows/plan.yml:48`, `.github/workflows/plan.yml:54`: the notification step runs after three fail-fast gate steps, so any plan-health, register, or summary failure skips owner notification and allows the obligations causing the failure to expire silently.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:823`: the required commit item remains unchecked and requires subject `workspace: unattended checks and risk visibility per eighth live round`, while candidate `2c9af727c50a3dc73268ca514d29bf1a677b4294` uses the incomplete subject `workspace: unattended checks and risk visibility`.
```

## GPT panel (round 2)

Round 2 ran twice. The first run reviewed the wrong contract (inherited UI library text, D00 T02 §5 scope) against the §29 candidate, so every lens chased the mismatch; it is voided in full and re-ran as round 2 with a contract-only prompt (section contract plus candidate diff, no neighbor sections). The void output is fenced below for the record, not part of any scan.

- `adversarial` needs-attention: (1) residual logic fixture-pinned only for a retry-owed run plus an unmatched outage; matched outage debt, healthy-marker `none`, and combined owed states untested.
- `consistency` approve
- `integration` approve
- `record` approve

Fix record: residual fixture coverage for matched outage debt, healthy-marker `none`, and combined owed states. Fixed in `dadc2dd`; gates re-ran green before round 3 (validate 0 fatal, plan current).

Raw round-2 output (re-run, fenced, not part of the verdict scan; digest `eed476c31b1c1ef5ba9e508d6078a8bd64588bc60a621409e9eaf74c8b56b704`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:3566`, `scripts/todo-graph.py:3578`, `scripts/todo-graph.py:3582`, `scripts/todo-graph.py:11507`: the new residual logic is fixture-pinned only for a retry-owed run and an unmatched outage; matched outage debt, healthy-marker `none`, and combined owed states remain untested despite the section record claiming owed-state residual coverage.
**consistency: approve**
**integration: approve**
**record: approve**
```

Raw voided round-2 output (wrong contract, fenced, not part of any scan; digest `2cb596601b89b3344bc11624839b621825214c38cca08df3283b0c842c8917d9`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:2497`, `scripts/todo-graph.py:3566`, `.github/workflows/plan.yml:54`: the executable changes implement risk acceptance reporting and notifications, while the contract requires UI theme, DPI, animation, icon, and control tests; none of the required test coverage is present.

**consistency: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:816`, `todo/00-workspace/TODO-01-repo-and-toolchain.md:824`: the candidate records completion of unattended plan governance work from a different section instead of the supplied inherited UI library section.

**integration: needs-attention** (1)
1. `.github/workflows/plan.yml:54`, `scripts/todo-graph.py:11416`: the candidate wires plan notifications and self-tests but does not add or wire the required `[ui]` cases or the `ctest --preset debug -L ui` checkpoint.

**record: needs-attention** (1)
1. `todo/00-workspace/TODO-01-repo-and-toolchain.md:824`: the checked commit record names `workspace: unattended checks and risk visibility per eighth live round`, while the section contract requires `workspace: cover the inherited ui library`.
```

## Opus panel (round 3)

- `adversarial` advisory: (1) the multi-state residual join stays unpinned: every fixture resolves a single-element debt set, so composite `partial: <rung>` plus `retry-owed` and `partial` itself never reach the sort-plus-join.
- `consistency` advisory: (1) waivers on runs carried by healthy pre-cutoff markers read `unmatched` rather than `none`, against the residual vocabulary.
- `integration` advisory: (1) the committed register's clock-derived `State` column fails the daily unattended job on date crossings with no repo change.
- `record` advisory: (1) AGENTS.md self-test count stale (812 vs 856).

Filing record: sign-off round, all four below the blocking bar, so filed plus fixed-at-stamp instead of re-rounding. The adversarial filed as a D00 T01 §52 item (composite residual pin); consistency plus integration filed as D00 T01 §34 items (pre-cutoff `none` wording, daily-gate State crossings); the record fixed in the stamp commit (AGENTS.md count 812 to 856, convention pre-tracked at D00 T01 §42). No fix commit.

Raw round-3 output (fenced, not part of the verdict scan; digest `e5236cef2c08d870244b78614c619252a92541058467d080225277dfd5967d78`):

```
**adversarial: advisory** (1)
1. `scripts/todo-graph.py:3575`, `scripts/todo-graph.py:3583`, `scripts/todo-graph.py:11538-11541`: the multi-state residual join is still unpinned. Every fixture target resolves to a single-element debt set (`retry-owed`, `outage`, `none`, `unmatched`), so the reachable composite marker state `partial: <rung>` plus `retry-owed` (grammar-legal per `todo/README.md:163`) and the `partial` state itself never reach `"+".join(sorted(...))`; dropping the sort or the join keeps the suite green.

**consistency: advisory** (1)
1. `scripts/todo-graph.py:2445-2451` against the register's own wording at `scripts/todo-graph.py:3626`: markers on stamps at or before `PLAN_REVIEW_CUTOFF` (`scripts/todo-graph.py:952`) are recorded into `marked` but never into `run_debt`/`outage_debt`, so a waiver targeting a run carried by a healthy pre-cutoff marker reads `unmatched` ("past any marker") rather than `none`, contradicting the documented residual vocabulary for a case the same branch acknowledges exists.

**integration: advisory** (1)
1. `.github/workflows/plan.yml:52` with `scripts/todo-graph.py:3587` and `scripts/todo-graph.py:3633`: the committed register carries the clock-derived `State` column (`live`/`post-dated`/`expired`), so `query risk-register --check` fails the daily unattended job on the day any instrument crosses its record or expiry date, with no repository change and no remediation other than a re-sync commit.

**record: advisory** (1)
1. `AGENTS.md:55`: the range edits the adjacent command block (`plan-health/5`, three new query rows) and adds self-test cases, but the self-test count still reads `812 cases, must stay green` while the shipped tree reports `856 cases, 0 failed`.
```

## Review notes

Governance section with shared-surface touch: nine items across the query surface, validator, fixtures, skill, README, workflow, and the committed register. The panel's five corrections (head-wins causes, owed-state residuals, always-on notify, commit subject, residual fixtures) all landed before close; the sign-off advisories filed as tracked work (§52 composite pin, §34 wording plus gate items) and the sign-off record fixed at stamp (§42 pre-tracks the convention going forward). The voided round-2 run stays on record per the void note above; no panel leftovers. Architecture gate skipped silently (no editor state, storage, extensibility, protocol, or consent surface in the diff).

## Plan review

Round: `gpt-5.6-sol` high over §29 plus §21, 2026-09-19. Neighbors: §21 (Depends plus XREF neighbor: §29 filed from its plan review). Dependents: ten refs by the mechanical rule (no reverse Depends rows at review time; first-clause XREF consumers §25, §26, §27; one downstream reverse-Depends hop past them adds §46, §47, §48, §49, §50, §51; §21 rides as the filer per the §27 precedent). Post-run correction, disclosed: the as-run prompt named §19, §20, §23, §28, §34, §52 instead of §46-51 (an upstream hop plus parser-invisible later-clause XREFs); the manifest carries the mechanical set while bytes still attest the as-run prompt, sections and run unchanged. The run minted through the `run-id` genesis path (no prior claims, bare base). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (21 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §29, D00 T01 §21]; dependents [D00 T01 §21, D00 T01 §25, D00 T01 §26, D00 T01 §27, D00 T01 §46, D00 T01 §47, D00 T01 §48, D00 T01 §49, D00 T01 §50, D00 T01 §51]; bytes 18002; run 20260919-D00-T01-S29-gpt

Ledger (namespaced IDs per the §16 shape; 16 of 21 filed at §53, 1 duplicate of D00-T01-S28-PR7, 4 rejected with reasons on their rows):

Ledger:

- [D00-T01-S29-PR1] [minor] Full-history checkout unrequired -> rejected both plan-gates jobs already carry `fetch-depth: 0` with the git-legs rationale in the workflow comment; the reviewer saw plan text, not the shipped job
- [D00-T01-S29-PR2] [minor] Poster path unproven -> filed D00 T01 §53
- [D00-T01-S29-PR3] [minor] Per-run issue identity -> filed D00 T01 §53 (per-run dedup already holds via the stable rolling title, rejected half; the wave-spanning reopen is the filed remainder)
- [D00-T01-S29-PR4] [minor] Owner delivery unmapped -> filed D00 T01 §53
- [D00-T01-S29-PR5] [minor] Production horizon unstated -> filed D00 T01 §53
- [D00-T01-S29-PR6] [minor] Post-due silence -> filed D00 T01 §53
- [D00-T01-S29-PR7] [minor] Poster failure handling -> filed D00 T01 §53 (`issues: write` already set on the unattended job, rejected half; bounded retry plus the failure gate is the filed remainder)
- [D00-T01-S29-PR8] [minor] Schedule heartbeat missing -> filed D00 T01 §53
- [D00-T01-S29-PR9] [minor] Clock boundary unstated -> filed D00 T01 §53
- [D00-T01-S29-PR10] [minor] Gates fail fast -> filed D00 T01 §53
- [D00-T01-S29-PR11] [minor] Future-dated auto-activation -> filed D00 T01 §53
- [D00-T01-S29-PR12] [minor] Approver authorization missing -> duplicate D00-T01-S28-PR7
- [D00-T01-S29-PR13] [minor] Review disposition undefined -> filed D00 T01 §53
- [D00-T01-S29-PR14] [minor] Review-expiry lead time missing -> filed D00 T01 §53
- [D00-T01-S29-PR15] [minor] Residual combination untabulated -> filed D00 T01 §53
- [D00-T01-S29-PR16] [minor] Lapsed acceptance retention -> filed D00 T01 §53 (lapsed instruments already list in the register, rejected half; superseded linkage is the filed remainder)
- [D00-T01-S29-PR17] [minor] Dashboard section count mismatch -> filed D00 T01 §53
- [D00-T01-S29-PR18] [minor] Review age outside oldest-due -> filed D00 T01 §53
- [D00-T01-S29-PR19] [minor] Infrastructure gate missing -> rejected item 8's recorded shape is no separate dimension (the distinct cause plus recovery path is the surfacing); the ask is the decided-against alternative
- [D00-T01-S29-PR20] [minor] Schema consumer compatibility -> rejected no live plan-health/4 consumers exist (in-process readers ride the bump; the remaining /4 refs are Superseded history plus findings quotes); the §29 sweep covered skill, AGENTS.md, README, and the contract comment
- [D00-T01-S29-PR21] [minor] Candidate identity outside commit item -> rejected candidate identity rides the findings file plus the stamp Review line at stamp time by design; item 9's contract is the subject, matched via the recovery commit
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `5faaf45d4cab1be3cf95d97d740a8494021e1082e846f14c1f9c98047f6bbac8`):

```
- The scheduled job does not require a full-history checkout; add `fetch-depth: 0` to D00 T01 §29 item 1 because shallow history can manufacture the `git-unresolvable` faults item 8 is meant to diagnose.
- The notification acceptance test covers query payloads but does not prove GitHub issue creation, deduplication, or failure handling; add an isolated poster fixture to D00 T01 §29 item 1 because the stated “notification path fire” criterion remains unproven.
- “One deduplicated GitHub issue per failing run” gives every scheduled execution a new identity; define a stable obligation key and update, reopen, and close behavior in D00 T01 §29 item 1 so recurring checks do not create duplicate or stale issues.
- Posting an issue does not necessarily notify the named owner; add a validated owner-to-GitHub-login mapping with assignment or mention behavior to D00 T01 §29 item 1 because free-form owner names provide no delivery guarantee.
- The operational value of `--within-days N` is unspecified; choose and document the production horizon in D00 T01 §29 item 1 because fixtures for arbitrary values do not define when owners receive notice.
- Notification behavior after a due date is undefined; add reminder cadence and escalation ownership to D00 T01 §29 item 1 so an ignored issue cannot leave an overdue obligation silent indefinitely.
- Workflow token permissions, unavailable `gh` authentication, API errors, and rate limits have no required outcome; add explicit `issues: write`, bounded retry, and a visible notification-failure gate to D00 T01 §29 item 1 because the enforcement job can otherwise fail to alert anyone.
- The cron has no heartbeat or stale-run detector; add last-success monitoring to D00 T01 §29 item 1 because delayed, disabled, or repeatedly failing schedules violate the claim that dates cannot expire silently.
- Date evaluation has no declared timezone or boundary rule; define the schedule and `today` semantics in D00 T01 §29 items 1 and 3 because review and expiry status can differ around midnight.
- The plan does not require all independent gates to run and aggregate when an earlier command fails; add separate always-running steps with a final combined verdict to D00 T01 §29 item 1 so one failure does not hide register drift or later diagnostics.
- Future-dated acceptances remain valid and automatically become effective later; change D00 T01 §21 item 2 and D00 T01 §29 item 4 to reject them or require activation review because an inert warning permits pre-authorized future gate suppression.
- Acceptance records can clear gates without proving that the named approver authorized the record; add an approver authorization mechanism to D00 T01 §21 item 2 because any contributor able to edit Markdown can otherwise manufacture a valid-looking waiver.
- Review-due and review-overdue states lack a defined disposition workflow; add renew, narrow, revoke, and evidence requirements to D00 T01 §29 item 3 because surfacing a date alone does not complete the risk review.
- Review dates may equal expiry under the inclusive bounds rule; require useful lead time in D00 T01 §21 item 2 because a review performed only when acceptance expires cannot support a timely renewal or mitigation decision.
- Residual severity is derived from target severity and owed-state weight without a specified combination table; define the algorithm and ordering in D00 T01 §29 item 2 because the register can assign inconsistent or misleading risk weights.
- The register lists only live acceptances, with no rule for expired or superseded acceptances whose underlying risk remains unresolved; add retained status or linkage to the reopened finding in D00 T01 §29 item 2 so risk does not disappear during lifecycle transitions.
- The dashboard shape names four areas while its completion claim says all five sections are tested; enumerate the fifth section in D00 T01 §29 item 2 because the acceptance contract and delivered claim cannot currently be compared.
- Review-overdue owners are deliberately excluded from `oldest due`; include their review date in D00 T01 §29 item 6 because the digest cannot prioritize the oldest neglected acceptance accurately.
- `git-unresolvable` is described as an infrastructure fault but has no dedicated gate or infrastructure status; add an explicit actionable dimension to D00 T01 §29 item 8 because folding it into ordinary noncoverage obscures whether the risk evidence is stale or the runner is defective.
- The `plan-health/5` schema change lists documentation updates but no downstream consumer compatibility check; add consumer discovery and contract fixtures to D00 T01 §29 item 7 because dashboards or automation pinned to schema `/4` may break silently.
- The commit item names the work commit but omits the recovery commit hash and exact candidate range; record both endpoints in D00 T01 §29 item 9 before review because the repository requires an unambiguous candidate identity.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `93dd4e3`: `todo-graph self-test: 856 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `93dd4e3`: `24 todos, 227 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `93dd4e3`: `implementation plan is current -- 69/222 sections complete (31%)`, exit 0.

Provenance: candidate 93dd4e3; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 3e05f2e280beb419281984677b750d5d1325b24a6b797e639ea024411d578c94; path docs/reviews/00-workspace/D00-T01-s29.md; run 20260919-D00-T01-S29-gpt
