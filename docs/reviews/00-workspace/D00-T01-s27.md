# Review: D00 T01 §27 -- Acceptance Integrity

Candidate: `ba50847` + `0f2bde0` + `388cef0` + `da34cc5` + `9d571aa` + `e18a16a` + `f4dbe48` + `cd9d2d8` (SHIP plus self-review fix plus five panel fixes plus two filing commits; output `check-panel PASS` on all six headless rounds). Panel tier: Full (the diff touches `scripts/todo-graph.py` plus `scripts/todo-validate.py`, shared contract surface), Sol rounds 1-2, Opus rounds 3-6.

## Self-review

Three questions over the SHIP diff plus its blast radius, answered with files and behavior. One self-review fix landed before the headless panel ran.

Does it make sense: six items harden the §21 acceptance machinery end to end (instance-bound outage targets, target-before-record, evidence binding, append-only amendment, action owners, review scheduling) with the skill, README, and AGENTS updates the shape change owes. Exactly the contract, including the four validation corrections recorded on the items before building.

Is it logical: the hold predicate orders match, target-before-record, liveness, and evidence freshness; expiry alone redirects escalation to the owner; superseded records are history everywhere; reopen voids by construction through the §17 verified-sections discard. The reviews dimension gates overdue only.

Did it break anything else: suite moves 795 to 808 by exactly the 13 new probes; live validate stays 0 fatal; plan-health schema bumps /3 to /4 with the new keys documented in the machine-contract comment. Blast radius is the acceptance query plus validator paths, both covered.

Session fix: `0f2bde0` (summary `next:` names the overdue review, mutation-proven: fails exactly without the pool filter).

## GPT panel (round 1)

- `adversarial` needs-attention: (1) supersedes chains allowed self-links, cycles, and double successors.
- `consistency` needs-attention: (1) post-dated acceptances wrongly redirected escalation to the owner.
- `integration` needs-attention: (1) evidence byte-compare was circular for finding acceptances (the record cannot cite a commit containing it).
- `record` approve

Fix record: chain-shape validation under new FATAL `risk-acceptance-chain-broken`; owner escalation scoped to true expiry plus a post-dated probe; evidence comparison excludes acceptance lines both sides plus an ancestor-bytes probe. Fixed in `388cef0`; gates re-ran green before round 2 (self-test 811/811, validate 0 fatal, plan current).

Raw round-1 output (fenced, not part of the verdict scan; digest `46d8f7badad1b4a4ca1d154fade659b9a236bf7717af49d1b9343844305a3238`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-graph.py:1159-1162` (`superseded_acceptances`) and `scripts/todo-validate.py:1407-1432` (rule 24 history legs) accept any `supersedes <date>` value that parses, with no link-validity rules: a record can supersede itself (`supersedes <its own record date>`), two records can supersede each other in a cycle, and two successors can supersede the same predecessor date. A self-superseding record voids its own cover silently (its key lands in `supd`), cycles void every member the same way, and multiple successors leave the "chain head governs" read ambiguous. The ledger `supersedes` machinery this item mirrors enforces acyclicity plus single-successor (`ledger-supersession-broken`); the acceptance mirror enforces neither.
**consistency: needs-attention** (1)
1. `scripts/todo-graph.py:2404-2409` and `:2635-2640` set `esc_owner` for every non-live match, but `acceptance_live` fails two ways (expired `exp < today`, post-dated `rec > today`) while item 5 promises only "expiry escalates to them": a post-dated acceptance (record date in the future, e.g. the S44 `typo'd year` shape aimed at a real row) redirects the escalation from `operator:` to its owner although nothing expired. The §21 R3 contract the code cites treats post-dated as dangling-to-operator; the expired branch breaks it.
**integration: needs-attention** (1)
1. `scripts/todo-graph.py:1167-1180` (`evidence_fresh`) compares whole-file bytes at the cited commit against current bytes, but a finding acceptance lives inside the findings file it cites, so no newly committed finding acceptance can satisfy byte-equality: the evidence commit predates the line (the sha must exist before it is written into the line) and therefore never contains the line, while the live file always does. Every finding acceptance voids on its first day by construction; only run/outage targets (owned by the TODO file, which the acceptance never touches) can pass. The covering fixtures never catch it because `canned_git` returns the live bytes verbatim (e.g. `ddd4545` at `scripts/todo-graph.py:8327-8329`), never mocked commit contents that predate the acceptance line.
**record: approve**
```

## GPT panel (round 2)

- `adversarial` needs-attention: (1) duplicate target-date keys collapse silently; unlinked multi-head forks pass.
- `consistency` approve
- `integration` needs-attention: (1) reviews leg lists void acceptances (no target-before-record or evidence check).
- `record` needs-attention: (1) chained predecessors exempt from silent-edit detection, contradicting append-only.

Fix record: record identity plus one-head-per-target under the chain-broken class; reviews leg runs the shared hold predicate (all three sites refactored onto `acceptance_hold`); chained exemptions dropped (append-only absolute). Fixed in `da34cc5`; gates re-ran green before round 3 (self-test 811/811, validate 0 fatal, plan current).

Raw round-2 output (fenced, not part of the verdict scan; digest `dc4a888c21253dc83484aa223cb82a77449415f6d2e7de488a543a3576e727a1`):

```
**adversarial: needs-attention** (1)
1. `scripts/todo-validate.py:1396-1406` (`_acceptance_records`) keys records by `(target, record date)` in a plain dict, so two live lines sharing one key overwrite silently and the validator compares a collapsed view: twin records with different approvers/owners/rationales validate as one, and a `supersedes` link naming the shared date chains ambiguously. Worse, two DIFFERENT-date unlinked records for one target (two heads, neither superseded) pass every leg: the R1 chain rules fire only on link shapes (self/cycle/double-successor), never on head count, while `superseded_acceptances` (`scripts/todo-graph.py:1159-1162`) skips only named predecessors, so both heads cover (first-match wins) and both list in `reviews`, violating the single-current-head model the R1 fix claims.
**consistency: approve**
**integration: needs-attention** (1)
1. `scripts/todo-graph.py:2906-2950` (the new `reviews` leg) lists every live, un-superseded, window-valid acceptance with a near/past review date without running the target-before-record or evidence-freshness legs that gate covering: a prewritten waiver (record predates its run), a stale-evidence waiver (owning record changed), and a dangling-target waiver (typo'd ID, uncovered run) each list as `review-due`/`review-overdue` and fail `--check` via `dim_failing`, although none covers anything. The leg consults `acceptance_live` only; items 2 and 3 void coverage for exactly these records, and the leg never asks.
**record: needs-attention** (1)
1. `scripts/todo-validate.py:1420-1431` exempts a vanished or edited record from the silent-edit FATAL whenever its key appears in `chained` (a successor names it), which contradicts the append-only semantics item 4 promises ("validated against history like ledger rows"): the ledger precedent (rule 22) requires the superseded row to REMAIN byte-identical beside its successor, and a chain never licenses a rewrite. Under the shipped legs a successor can silently narrow its predecessor's scope (edit the rationale, shorten the expiry) or delete it outright and validate clean, so the audit trail the item exists to protect is mutable by the exact mechanism meant to extend it.
```

## Opus panel (round 3)

- `adversarial` approve
- `consistency` approve
- `integration` advisory: (1) reviews leg evaluates multi-citer files under the first citer only.
- `record` needs-attention: (1) stamped canonical claims contradict the shipped /4 shape.

Fix record: three stamped Done lines gain dated `Superseded` notes (the file's own precedent); the open §35 direction now names /4. Advisory filed as D00 T01 §49 instead of re-rounding. Fixed in `9d571aa`; gates re-ran green before round 4 (validate 0 fatal, plan current).

Raw round-3 output (fenced, not part of the verdict scan; digest `790c09678edf1a0d33726c371010feb7a222d1f784d4a31150cfafb09b46f37f`):

```
**adversarial: approve**

**consistency: approve**

**integration: advisory (1)**

1. `scripts/todo-graph.py:2957` — the new `reviews` leg reads `owners[path][0]` only, so a findings file cited by more than one stamped section is evaluated against the first citer's marker run and stamp day alone. Two sections `§10` and `§11` both stamped `Raw findings: docs/reviews/x.md`, with `Risk accepted: <§11's run id>; ...; review <yesterday>; ...`: the degraded loop at `scripts/todo-graph.py:2427-2460` matches `§11`'s marker and covers (clearing OVERDUE and the gate vote), while the reviews leg computes `_pmrun` from `§10`'s body (`scripts/todo-graph.py:2968-2970`), fails `_pmatch`, and lists no `review-overdue` entry, so the overdue review never surfaces or gates. `owners` is a list precisely because multiple citers are expected (`scripts/todo-graph.py:2540`, consumed in full at `scripts/todo-graph.py:2937`); no such file exists in the tree today.

**record: needs-attention (1)**

1. Stamped canonical claims this candidate contradicts carry no dated supersession note, against the file's own precedent (`todo/00-workspace/TODO-01-repo-and-toolchain.md:515` and `:571` each carry `**Superseded 2026-09-18 (§19 ...)**` / `(§21 item 2)` notes for exactly this kind of change) and §19 item 17's "no stamped canonical status contradicts shipped code": `todo/00-workspace/TODO-01-repo-and-toolchain.md:515` still states "schema is now `plan-health/3` (degraded, criticals, and majors carry `accepted_by`/`accepted_expires`/`accepted_review`/`accepted_rationale`...)" while `scripts/todo-graph.py:983` now reads `plan-health/4` with `accepted_owner` and a `reviews` dimension; `:571` still states "cases now lock the /3 schema with the acceptance (by, expires, review, rationale) and overdue keys" while the cases now assert `/4` (`scripts/todo-graph.py:10042`, `:10060`); `:630` still states the record shape is "target (finding ID, run ID, or `outage <rung>`), approver, record date, expiry, review date, and rationale tail ... (schema plan-health/3)" while the shipped shape at `scripts/todo-graph.py:995-999` requires `owner`, `evidence <sha>`, optional `supersedes`, and `outage <rung> <date>`; and the open row `:910` still directs a future implementer to make the query help name "the `plan-health/3` shape stable".
```

## Opus panel (round 4)

- `adversarial` approve
- `consistency` needs-attention: (1) reviews listed waivers for minor rows and healthy markers.
- `integration` approve
- `record` approve

Fix record: the reviews leg requires a live escalation on the target's dimension (critical/major entry, or an outage/retry-owed degraded state), sharing the computed dimensions; fixture rows promote to major; a minor-row probe locks the exclusion; the three "covering only" claim sites reworded. Fixed in `e18a16a`; gates re-ran green before round 5 (self-test 812/812, validate 0 fatal, plan current).

Raw round-4 output (fenced, not part of the verdict scan; digest `75953635e236028e68d08b38638f2a9e8fe93cc807733f47e5fb406a971fd4e6`):

```
**adversarial: approve**

**consistency: needs-attention (1)**
1. `scripts/todo-graph.py:2942-2999` — the `reviews` leg matches on target *existence* (`_pmatch = tgt.lower() in _prow_ids` at :2995 for findings, `normalize_run_id(tgt) == _pmrun` at :2999 for runs), not on an escalation the acceptance actually terminates, so it lists and gates records the covering loops never consult: the degraded loop only consults acceptances when `"outage" in state or "retry-owed" in state` (`:2417`), and the finding loops only reach `acceptance_hold` for critical/major rows (`:2809`, `:2905`). A `[minor]` ledger row or a healthy (non-degraded) marker carries no escalation, yet its acceptance lists as `review-overdue` and fails `--check` via `dim_failing` (`:1235-1238`). The shipped fixture demonstrates it: `scripts/todo-graph.py:10611-10617` writes `D90-T07-S6-PR1..PR4` as `[minor] watched row -> accepted`, and `:10696-10705` asserts PR1 lists `review-overdue` although no minor row ever enters `criticals`/`majors`. Three claim sites state the narrower behavior and are wrong as written: `scripts/todo-graph.py:2942` ("Covering only (panel R2): a void acceptance lists no obligation it cannot back"), `todo/00-workspace/TODO-01-repo-and-toolchain.md:769` ("for covering acceptances only (panel R2 ...)"), `.claude/skills/review-todo-section/SKILL.md:172` ("covering acceptances near or past their review date surface in the plan-health `reviews` dimension").

**integration: approve**

**record: approve**
```

## Opus panel (round 5)

- `adversarial` approve
- `consistency` approve
- `integration` advisory: (1) summary digest tally omits review-overdue owners.
- `record` needs-attention: (1) README coverage sentence omits the new cover conditions.

Fix record: README coverage sentence completed (target-before-record, evidence freshness, no superseder, live-escalation requirement). Advisory joined D00 T01 §29 instead of re-rounding. Fixed in `f4dbe48`; gates re-ran green before round 6 (validate 0 fatal, plan current).

Raw round-5 output (fenced, not part of the verdict scan; digest `0697e6320bbd547e364704eb350468c36a4963bfeb89b26ae5850638904bd14e`):

```
**adversarial: approve**

**consistency: approve**

**integration: advisory (1)**

1. `scripts/todo-graph.py:3193-3201` (`query summary`): the `overdue owners` tally is built from `degraded_sorted`, `criticals_sorted`, and `majors_sorted` only; review-overdue entries carry an owner and an owed action and vote in the gate (`scripts/todo-graph.py:3135`, `1235-1238`), but feed no digest line. With an uncovered critical plus an overdue acceptance review owned by `bob`, summary prints `overdue owners 1` (the critical's owner only), `next:` names the critical because `reviews` sorts last in `gate_dims`, and `bob`'s overdue review appears nowhere but the `gate: FAIL (criticals, reviews)` dimension name.

**record: needs-attention (1)**

1. `todo/README.md:411`: the `risk-acceptance-malformed` cell was updated for the new record shape but its coverage sentence still states the plan-health/3 conditions ("Coverage needs the record date on or before today and today on or before expiry"), omitting the three conditions this candidate ships and that the skill states at `.claude/skills/review-todo-section/SKILL.md:172` (target-before-record, evidence-commit freshness, no superseder). A reader taking README as the format spec concludes a live, in-window waiver covers, while `acceptance_hold` (`scripts/todo-graph.py:1147-1180`) voids it on any of the three.
```

## Opus panel (round 6)

- `adversarial` advisory: (1) outage instance key binds the stamp day, not the event.
- `consistency` approve
- `integration` approve
- `record` approve

Fix record: advisory-only round, no re-round. Filed as D00 T01 §50 (outage instance dating); the §27 item-1 residual note corrected to the honest scoping (outages sharing a stamp share a key). Filed in `cd9d2d8`. Panel closed.

Raw round-6 output (fenced, not part of the verdict scan; digest `2ec1ce34bb5ccf43a1ca575ad60a089c56ff354b2864b64a1ccd3e05021808fb`):

```
**adversarial: advisory (1)**

1. `scripts/todo-graph.py:2422,2438` with `todo/00-workspace/TODO-01-repo-and-toolchain.md:764` — the outage instance key is `(rung, s.stamped_on)`, the section's `Verified:` stamp day, not any date tied to the outage event. A section carries one stamp for its whole candidate range while `Plan review:` markers append per round, so every outage marker recorded under one stamp yields one key regardless of how many calendar days the fix loop spans: a waiver written after round 1's `outage: gpt rung` covers a round-3 rerun outage of the same rung recorded two days later, which is the exact scenario PR6 names. The item's recorded residual ("two outages of one rung in one file on one day share a key; pathological (same-day same-rung rerun outage)") scopes the gap to one day and so understates it; the honest statement is "outages sharing a stamp share a key". Advisory, not blocking: the residual is a recorded default and the marker grammar carries no outage-instance date to bind instead.

**consistency: approve**

**integration: approve**

**record: approve**
```

## Review notes

Hardening section with deep script touch: six items across the acceptance shape, both covering loops, rule 24, and a new plan-health dimension. The panel's nine corrections (chain shape, expiry scope, evidence circle, record identity, covering reviews, absolute append-only, supersession notes, live-escalation reviews, coverage sentence) all landed before close, and the three advisories filed as tracked work (§49 multi-citer, §29 digest joiner, §50 outage dating). No panel leftovers. Architecture gate skipped silently (no editor state, storage, extensibility, protocol, or consent surface in the diff).

## Plan review

Round: `gpt-5.6-sol` high over §27 plus §21, 2026-09-19. Neighbors: §21 (Depends plus XREF both directions: §27 filed from its plan review and hardens its acceptance machinery); dependents: §21, §29, §49, §50 (XREF consumers; transitive Depends hop adds nothing: §21 carries no Depends line, §29 depends §21, §49 and §50 depend §27). The run minted through the `run-id` genesis path (no prior claims, bare base). Runner note: the skill's `codex exec` template ran with the prompt as a positional argument over the fenced sections; no new CLI drift. Output `check-plan PASS` (16 findings, one per line). No criticals, so the safety exception does not trigger and the stamp proceeds.

Manifest: sections [D00 T01 §27, D00 T01 §21]; dependents [D00 T01 §21, D00 T01 §29, D00 T01 §49, D00 T01 §50]; bytes 15409; run 20260919-D00-T01-S27-gpt

Ledger (namespaced IDs per the §16 shape; 6 of 16 filed at §51 plus two §29 items, 2 duplicates with canonical IDs, 6 rejected with reasons on their rows):

Ledger:

- [D00-T01-S27-PR1] [minor] Outage identity incomplete, checkpoint overclaims -> rejected already filed as D00 T01 §50 from the round-6 panel advisory (same scope: event-bound outage identity); the residual note carries the honest scoping
- [D00-T01-S27-PR2] [minor] Same-day chronology unresolved -> filed D00 T01 §51
- [D00-T01-S27-PR3] [minor] Approval authority unowned -> duplicate D00-T01-S21-PR13
- [D00-T01-S27-PR4] [minor] Owner identity too weak -> duplicate D00-T01-S21-PR13
- [D00-T01-S27-PR5] [minor] Schema /3 to /4 migration missing -> rejected zero live acceptance records (verified by grep) and no out-of-repo JSON consumers (CI never touches plan-health); the version string is the compat gate, so no migration burden exists
- [D00-T01-S27-PR6] [minor] Evidence lineage incomplete -> filed D00 T01 §51
- [D00-T01-S27-PR7] [minor] Evidence freshness overbroad -> filed D00 T01 §51
- [D00-T01-S27-PR8] [minor] Reopen revival unspecified -> filed D00 T01 §51
- [D00-T01-S27-PR9] [minor] Review outcomes unstructured -> filed D00 T01 §51
- [D00-T01-S27-PR10] [minor] Review-date boundaries ambiguous -> rejected fixed in the item-6 note pre-stamp (on-date reads due, UTC calendar); the code already defined both, the sentence pins the spec
- [D00-T01-S27-PR11] [minor] Review obligations vanish quietly -> rejected the append-only file is the audit surface (predecessors stay byte-identical with dates, links, and successor rationales); the reviews list is for owed actions only, and a dead waiver listing there would double-count
- [D00-T01-S27-PR12] [minor] Coverage failures lack diagnostics -> filed D00 T01 §29
- [D00-T01-S27-PR13] [minor] §29 handoff incomplete -> rejected the round-5 advisory joiner owns the overdue-owner tally, due stays warn-only by design, and summary `next:` already names overdue reviews (shipped §27 with a probe)
- [D00-T01-S27-PR14] [minor] Date-only supersession fragile -> filed D00 T01 §51
- [D00-T01-S27-PR15] [minor] Missing git objects silent -> filed D00 T01 §29
- [D00-T01-S27-PR16] [minor] Review alerts pull-only -> rejected no notification channel exists in the system (pull-only is the architecture; `query summary` is the digest); push needs its own domain with operator channel decisions, disproportionate to file here
End of ledger

Raw plan-review output (fenced, not part of any scan; digest `3c168b2ad871769671599bfe98bd0738e5cfb8a6479ef2224822f6c6fbb4ae44`):

```
- Outage identity remains incomplete in D00 T01 §27 item 1 and §50: same-rung outages sharing a stamp date share one key, so the checkpoint claim that every waiver binds one instance is false until §50 supplies a unique immutable event identity.
- Same-day chronology is unresolved in D00 T01 §27 item 2: date-only comparison lets a waiver written before its target on the same day cover it, so ordering needs a timestamp, ledger sequence, or commit ancestry.
- Approval authority is unowned in D00 T01 §21 item 2 and §27: validating an approver name does not prove that person may accept the target risk, so add scoped authority rules and reject unauthorized approvers.
- Owner identity is too weak in D00 T01 §27 item 5: free-form `owner <name>` values can be ambiguous, stale, or unreachable, so bind owners to a canonical directory identity or maintained team alias.
- The schema /3 to /4 migration is missing from D00 T01 §27: existing valid acceptance records and downstream JSON consumers need an explicit migration and compatibility gate to avoid silently breaking live records or automation.
- Evidence lineage is incomplete in D00 T01 §27 item 3: any resolvable Git commit can apparently qualify, including an unrelated branch or repository history, so require an unambiguous full commit that is an ancestor of the reviewed candidate.
- Evidence freshness is overbroad in D00 T01 §27 item 3: comparing the entire owning file invalidates a waiver after unrelated edits, so target-level canonical digests would reduce needless renewals without weakening material-change detection.
- Reopen invalidation is under-specified in D00 T01 §27 item 3: removing a section from `verified_sections` suppresses consultation temporarily but does not explicitly prevent an old finding acceptance from reviving after re-verification, so persist a reopen generation and bind acceptances to it.
- Review outcomes are unstructured in D00 T01 §27 item 6: hiding the outcome in free-form rationale prevents reliable reporting of renewed, remediated, rejected, or closed decisions, so add an outcome field plus evidence and follow-up action.
- Review-date boundaries are ambiguous in D00 T01 §27 item 6: “within 7 days before” and “past” do not state the state on the review date or the governing timezone, so define inclusive boundaries and one injected calendar source for deterministic operation.
- Review obligations disappear too quietly in D00 T01 §27 item 6: superseded, lapsed, void, and no-longer-covering records vanish from the review dimension, so retain an audit surface explaining their final state and whether a scheduled review was completed.
- Coverage failures lack operator diagnostics in D00 T01 §27 and §21 item 4: stale evidence, predated targets, missing targets, supersession, and expiry all produce noncoverage for different reasons, so `query summary` should name the exact cause and remediation.
- The §29 handoff is incomplete in D00 T01 §27 items 5 and 6: the cross-reference does not state that due and overdue reviews must join the owner digest with an exact next action, so the two sections can independently pass while the operator receives no actionable task.
- Date-only supersession is fragile in D00 T01 §27 item 4: `supersedes <date>` cannot naturally represent two legitimate same-target decisions on one day and depends on duplicate rejection, so records need stable IDs and supersession should reference those IDs.
- Missing Git objects are treated only as failed coverage in D00 T01 §27 item 3: shallow clones, garbage collection, or rewritten local history can make valid evidence unresolvable, so surface a distinct infrastructure fault with a documented recovery path.
- Review alerts remain pull-only across D00 T01 §§21, §27, and §29: a premium product should emit deduplicated owner notifications before due dates and on escalation, with acknowledgement and delivery status, so commitments are acted on without manual polling.
```

## Live proof

`python3 scripts/todo-graph.py self-test` on `62f0b8e`: `todo-graph self-test: 812 cases, 0 failed`, exit 0.

`python3 scripts/todo-graph.py validate` on `62f0b8e`: `24 todos, 225 sections -- 0 fatal, 0 warning(s)`, exit 0.

`python3 scripts/todo-graph.py plan --check` on `62f0b8e`: `implementation plan is current -- 67/220 sections complete (30%)`, exit 0.

Provenance: candidate 62f0b8e; command `python3 scripts/todo-graph.py self-test && python3 scripts/todo-graph.py validate && python3 scripts/todo-graph.py plan --check`; exit 0; tool CPython 3.12.3; digest 89dbaa53943f880fa704e38b331fa6055d0ce14feac14f4cee3b3f8eec8df341; path docs/reviews/00-workspace/D00-T01-s27.md; run 20260919-D00-T01-S27-gpt
