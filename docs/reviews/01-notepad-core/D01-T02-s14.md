# Review: D01 T02 §14 -- Title-Bar Icon Beside the Tabs

## Evidence map

Round-1 candidate `1d60089` carried the implementation (16px raster content, icon row, two UIA drives) with `45eb86a` carrying the record (Started, items, item-4 deferral to D00 T02 §7, XREFs, plan row). Round-2 candidate `47245a3` answered round 1 (drag-rect window-space mapping, decode state on HelpText, 32px raster with scale selection, real-click test, parsed Deferred marker). Round-3 candidate `35c9a8a` answered round 2 (ItemStatus channel, `XamlRoot.Changed` re-selection, COMException guard). Round-4 candidate `31a10b5` answered round 3 (unchanged-asset guard, once and `closed` guards, raw-view attempt with measured revert, `or` idiom). Round-5 candidate `4a0e2d2` answered round 4 (AT-claim correction, round-label fix). Evidence for each fix is quoted under its panel record.

## Self-review

Sense: the icon renders from asset-native rasters at stock placement with no gesture, drag, or layout regressions. Logic: TabBar-local content edge mapped to window space at the single drag call site; decode proven by `ImageOpened` on ItemStatus; per-scale asset with change guard and teardown parking. Breakage: the icon takes no input and adds no drag rect; the only behavior delta is the intended 32 DIP strip shift. Process note: a window-station-less session made pixel capture impossible (measured black frames), so item 4 deferred to the CI evidence pipeline with a parsed marker. No self-review fixes; the panel found the real ones below.

## Opus panel (round 1, candidates `45eb86a` + `1d60089`)

**adversarial: needs-attention**: drag rects mixed TabBar-local x with window space, covering the add button; neither test could tell a rendered glyph from an unloaded source (`ms-appx` unproven, no failure handler); single 16px raster upscales off-100%. Fixed: TabRegion mapping with teardown guard; HelpText decode signal awaited by the drive; 32px raster with scale selection; real-click regression test.

**consistency: advisory**: bare `§11` dangled in §7 and meant D01 T01 §11 in §14; §7 lacked Needs and its Verification looked stale. Fixed: explicit refs; Needs and Verification rebuttals accepted in round 2.

**integration: approve**: XREFs bidirectional, §7 resolves with the expected dep, capture tooling exists, no other TabRegion.Content readers.

**record: needs-attention**: deferral was prose, invisible to `query deferred`; item-3 evidence falsified by the drag bug. Fixed: parsed Deferred marker; evidence rewritten around the fix and the click test.

## Opus panel (round 2, candidate `47245a3`)

**adversarial: advisory**: prior findings fixed and independently verified (rasters byte-exact, mapping provably window-space).

**consistency: advisory**: prior findings fixed; both rebuttals accepted (single-value Needs list; TODO-01 Verification precedent with qualification).

**integration: approve**: no new issues.

**record: needs-attention**: "display-gated" named a nonexistent gate and item 3 rested on inspection. Fixed: honest plain-Fact wording with the headless result recorded; CI green required before the stamp.

## Opus panel (round 3, candidate `35c9a8a`)

**adversarial: needs-attention**: per-resize decode loop (no asset-change guard); Changed hook without teardown guard or once protection. Fixed: asset-name guard; `closed` parking plus once flag.

**consistency: advisory**: ItemStatus claim overstated (announcement possible); `||` against the file's `or` idiom. Fixed: accurate observable-clients wording; idiom matched.

**integration: approve**: no new issues.

**record: advisory**: test comment kept the corrected phrasing; `help` variable stale. Fixed: comment and name updated.

## Opus panel (round 4, candidate `31a10b5`)

**adversarial: approve**: guards and once protection verified; flag ordering proven (closed precedes teardown).

**consistency: advisory**: AT-claim wording still asserted an undriven negative; raw-view measurement lacked an artifact. Fixed: observable-clients wording; measurement stated as the observation taken.

**integration: approve**: no new issues.

**record: advisory**: item-2 Done carried stale round-1 claims; round labels misattributed round 2; wording survived a rename. Fixed: claims corrected to measured sets; labels in commit order.

## Opus panel (round 5, candidate `4a0e2d2`, FINAL)

**adversarial: approve**: all gates and discriminators re-measured on the final candidate; working tree byte-clean; no new attack shape.

**consistency: advisory**: HelpText-never-authored wording (fixed in the stamp commit); otherwise docs, comment, and code agree.

**integration: approve**: fixtures, rows, and gates complete and green.

**record: advisory**: round-4 rewrite lacked a `Corrected` marker against the file's own convention (added in the stamp commit); review file noted as the remaining stamp obligation.

## Gates re-run

Windows build 0 warnings; host TitleBarIconTests filter 2 passed plus the expected headless red on the real-click test; host TabBar/Chrome/MainWindow filter delta 0 against baseline (9 pre-existing headless reds both ways); `validate` 0 fatal; `plan --check` current. Ship CI run 35228650168 on `4a0e2d2`: success on build-windows and build-linux; Windows UI.dll 169 passed, 1 skipped, 0 failed on the display-bearing runner, quoting the green gate item 3 required (per-test names are not in the CI log, so the gate is the whole-suite 0-failed line). After the round-5 advisory fixes (comment/prose-only): `validate` 0 fatal 0 warnings (re-run, quoted at stamp). The stamp push re-runs CI on the final tree. Panel rounds used: 5 of 5 (adversarial/integration approve, consistency/record advisory at close, zero needs-attention). Leftover: none (item 4 deferred to D00 T02 §7 with a parsed marker, carried in the stamp).
