# D01 T02 §3 review: Settings Page

Candidate: `224998a` (20 files) plus 2 round-1 review fixes in `SettingsPage.xaml.cs` (folded into the Ship commit: the candidate is pushed and the repo forbids amending pushed candidates). Full page in stock order (5 cards bound live, 5 disabled with owner, About panel), gear entry, Edit > Font enabled with jump, theme live via Changed, 11 UI drives, new page golden, main golden refreshed after confinement proof, capture settle-wait fix, menu pending/live list rotation.

## Self-review

- Makes sense: the 8 live items map to the diff exactly (page plus bindings, theme live, font picker plus jump, live-apply scope, page-half extension record, gear entry, About placement) and the strike carries its parity negative. The Changed subscription, ApplyTheme extract, capture settle-wait, menu list rotation, golden refresh, and the D02 rendering-owner line are required contract changes, not scope. The §12 filing rode its own `todo:` commit.
- Logical: every handler writes through `Update` only (grep-verified, no direct save); the suspend guard blocks init and refresh feedback; refresh re-reads the replaced `Current`; unknown combo values append and display (uniform rule, self-healing on reselect); radios leave unknowns unchecked (no radio to append); the Changed handlers only read; Unloaded and Dispose unsubscribe. Round 1 caught two gaps (below), both fixed.
- Breakage: full gate green with exactly the expected deltas (+11 UI, golden refresh confined to the menu band); MenuBarTests 27/27 with Font rotated pending-to-live; Changed fires stay idempotent (theme re-apply, guarded refresh); new package is Windows-only API in the Windows-only app with both lock files updated.

## Lenses round 1 (candidate `224998a`)

- `adversarial` fix: a stored Opening value outside the list (hand-edited file) left the combo blank, breaking the uniform append-and-display rule the other combos follow. Fixed: unknown values append as items; reselecting one writes the default (self-healing).
- `consistency` fix: a hand-emptied file (keys omitted, store backfills nothing) crashed the settings open in the preview builder (`FontFamily(null)`). Fixed: preview coalesces to Consolas/Regular. Advisory (not blocking): the store permits null keys; the page is now defensive, but store-side backfill belongs to whoever hardens the store next.
- `integration` approve: six app files rewire through the designed seams (registry, Changed, overlay); coexisting consumers stay idempotent; owners named in card comments; capture hook shared by both golden paths.
- `source-defect` approve: four balanced stock probes (full UIA dump, option labels, Font-jump confirmation, toggle repair) plus the full-page shot; labels verbatim; reset struck with evidence; gear and About placement from captures; stock 11.2607.14.0 current.
- `design` approve: item-by-item against the stock captures (back, heading, 6 groups, 13 cards in order, control kinds, toggle/dropdown text, About panel, no reset) with two advisories: card icons are approximate Symbol-enum glyphs (stock Glyphs unrecorded), and expanded-card interiors are test-driven but uneyeballed. About carries name plus assembly version per the recorded decision (support rows omitted, copyright with packaging identity); the golden's bottom sliver is background bleed inside the cropped margin, same class as the committed main golden.
- `record` approve: every tick maps to drives named in the commit body; family choices sampled (Arial plus Consolas round-trip; all styles, all sizes driven) since driving every installed family proves nothing beyond the shared path; strike follows precedent with its negative; §12 XREF bidirectional; no deferrals; gate ran on the exact candidate content.

## Lenses round 2 (candidate `224998a` plus fixes, final)

- `adversarial` approve · `consistency` approve · `integration` approve · `record` approve · `source-defect` approve · `design` approve (2 advisories above, dispositioned). No further defects.

## Gate quotes

Full gate on the final tree (`build/gate-last.log`, exit 0, no strays):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 285, Skipped: 0, Total: 285 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 156, Skipped: 1, Total: 157 - UI.dll` (the skip is the pre-existing quarantine)

Section coverage on the final tree: `SettingsPageTests` 11/11, `MenuBarTests` 27/27, golden pair green.

`validate` 0 fatal, `self-test` 393/393, build 0 warnings 0 analyzer errors.
