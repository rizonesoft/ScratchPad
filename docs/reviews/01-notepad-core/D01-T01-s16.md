# Review: D01 T01 §16 -- File Snapshots

Candidate: `44404da` (SnapshotStore, SnapshotsDialog, Ctrl+Shift+H opener, SnapshotTests) plus `089922a` (review round 1: read-only save-failure drive, containment drive) plus `a628db3` (review round 2: fresh-dialog reshow). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (named take, byte-identical): `SnapshotStore.Take` writes through `FileSave.SaveFile` under the tab's spec into `<file>.snapshots/snap-{id:0000}.bin` with a manifest entry. Driven: `TakeStoresSaveIdenticalBytes` (take bytes equal a `FileSave` reference) plus `TakeStoresBytesAndListsVersion` (take lists, bytes decode to the buffer).
- Item 2 (versions list, one-click restore): restore buttons per entry (`Restore {name}`), click replaces the buffer via `FileOpen` detection. Driven: `RestoreDontSaveReplacesBuffer`, `RestoreSaveWritesThenRestores`, `RestoreOnCleanBufferSkipsPrompt`.
- Item 3 (dirty prompt through §7): dirty restores hide the versions dialog and show the real `SavePromptDialog` (Save saves then restores, Don't-save restores, Cancel reshows with nothing applied). Driven: the full matrix across the three restore tests.
- Item 4 (retention cap): `MaxSnapshots` 10 with oldest-first eviction at take (bytes deleted, manifest pruned), documented in-dialog. Driven: `RetentionEvictsOldestPastTen` in unit (11 takes hold 10, oldest bytes gone) and in UI (11 takes in the room, s01 gone, s11 kept, bin deleted).
- Item 5 (commit): message matches `44404da` verbatim; the round-1 fix rides its own commit per the fix-forward rule.

## Self-review (three questions)

- Does it make sense: yes. Named versions beside the file with take/list/restore/retention and a deferred menu trigger, nothing more; no delete UI (the section asks for none), no chrome added.
- Is it logical: yes. Validation lives in both dialog (disable) and store (throw); corrupt manifests heal with sequence resumption; missing bytes surface inline; save failure aborts the restore with work preserved (driven round 1); takes serialize on the single UI thread with fresh manifest reads, so two windows cannot fork the manifest.
- Did it break anything else: no. One-word visibility change in TabBar (no behavior), additive MainWindow wiring on a free accelerator, additive files and tests only.

## Round 2 lenses (reshow flake, found by the final gate)

The final gate failed `RestoreDontSaveReplacesBuffer`: after answering the prompt, the reshown versions dialog never reappeared (1 in 4 reshows that run; the focused suite passed). Re-showing the hidden instance races its `Hide` completion; the fix opens a fresh dialog through a `reshow` callback instead, which is always legal. Proven 8/8 focused twice before the re-run.

- `adversarial` approve: the race surface is gone (no same-instance re-`ShowAsync` anywhere); prompt and dialog stay strictly sequential.
- `consistency` approve: the callback matches the dialog's injected-seam shape (reader, saver, applier, reshow).
- `integration` approve: the owner flag was already clear when `Hide` completed, so the fresh open cannot stack; modal focus still swallows shortcut repeats.
- `source-defect` approve: no Notepad surface at stake.
- `design` approve: no surface changed (same dialog, fresh instance).
- `record` approve: the flake and its gate evidence recorded here; item ticks unchanged.

## Round 1 lenses

- `adversarial` approve: names never become paths (sequence files kill traversal); corrupt manifest heals; missing bytes show inline; read-only save-failure aborts with work preserved (driven); externally deleted files orphan sidecars that reattach if the file returns (history survives; accepted, not litter-chased).
- `consistency` approve: store mirrors SessionStore/ShellSettings (atomic temp-plus-move, Debug swallows, DTO suppressions); dialog mirrors StatsDialog/SavePromptDialog; tests mirror the suite.
- `integration` approve: §5 (`FileSave` plus `ApplySave`), §7 (`SavePromptDialog` plus answer semantics), §4 (`Detect`) all reused, none forked; trigger contract recorded both sides with the menu owner; buffer arrives through the D02-swappable seam.
- `source-defect` approve: new build, no baseline; no Notepad surface claimed.
- `design` approve: the rendered dialog eyeballed on capture (title, suggestion-filled name box, take button, retention note verified; the fold cut below the list, so containment is driven instead); name box plus take plus restore buttons plus retention note all driven; untitled save-first state driven. No unaccounted controls. User guide: none exists in the tree, N/A.
- `record` approve: item ticks name their drives; deferrals: none.

## Gate quotes (final full gate on the candidate, exit 0)

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 190, Skipped: 0, Total: 190 - Unit.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 74, Skipped: 1, Total: 75 - UI.dll (net10.0)` (the skip is the pre-existing `TabBarTests` ctxmenu-name-race quarantine, not §16)

## Round 3 lenses (final stamp pass)

Finding: the section Chrome line said "ListView for the versions" but the dialog renders one-click restore buttons in a StackPanel. Fixed as a Correction on the section (buttons satisfy "restores on click" directly; a ListView would need selection plus a separate restore control), no code change; text-only, so no gate re-run owed beyond `validate` plus `plan --sync`.

- `adversarial` approve: nothing new; round-1 hostile analysis stands on the unchanged code.
- `consistency` approve: Chrome line now matches the built surface; store and dialog still mirror the tree's atomic-write and code-built-dialog patterns.
- `integration` approve: no caller changes since round 2; §4/§5/§7 reuse intact.
- `source-defect` approve: new build, no baseline.
- `design` approve: rendered surface unchanged since round 1; every control driven except the error TextBlock (advisory below). No user guide exists in the tree, N/A.
- `record` approve: stamp quotes the gate above; Correction recorded on the section, not smuggled into the stamp.

Advisories (noted, not blocking): (a) when the Save branch of a dirty restore fails, the dialog reshows the list with no error surfaced; buffer and disk are preserved (driven), so this is UX silence, not data loss. (b) The dialog's error TextBlock is undriven in the room (store throws are unit-driven; the room cannot reach Take's catch paths without forcing IO failure). (c) Orphaned `.snapshots` sidecars reattach if a deleted file returns (accepted in round 1).
