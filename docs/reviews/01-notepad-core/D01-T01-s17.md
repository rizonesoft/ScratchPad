# Review: D01 T01 §17 -- New-File Templates

Candidate: `2ff23cb` (template picker dialog, Ctrl+Shift+E opener, TemplateTests UI plus neutral adopt) plus `d7cc0d1` (review round 1: Daily-journal open drive, containment drive). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (picker lists built-ins on new, every built-in opens expanded): `TemplatesDialog` lists `Templates.BuiltIn` as `Use {name}` buttons; `MainWindow.UseTemplate` opens an untitled tab with the expanded body. Driven: `PickerListsBuiltInsAndUsesMeetingNotes` (all three listed, Meeting notes opens with title plus locale date) plus `BlankNoteOpensCleanEmptyTab`.
- Item 2 (date and title variables expand): `Templates.Expand` (`{date}` locale short, `{title}` from the picker prompt with empty meaning Untitled, unknown braces literal). Driven: neutral `TemplateTests` 5/5 plus `UnknownBracesStayLiteralInRoom`.
- Item 3 (customs persist across restarts): `TemplateStore` (`%LocalAppData%/IntelligentNotepad/templates/*.txt`, atomic temp-plus-move saves); the dialog's Save button stores the active buffer under the title-box name. Driven: `SaveCurrentPersistsAcrossRelaunch` (save in the room, relaunch, still listed and usable) plus `EmptyTitleDisablesSave`.
- Item 4 (commit): message matches `2ff23cb` verbatim.

## Self-review (three questions)

- Does it make sense: yes. A Ctrl+Shift+E picker that opens templated tabs, with user-made customs that survive restarts; Ctrl+N stays blank per the recorded parity reading, nothing more.
- Is it logical: yes. Expansion is single-homed in `Templates.Expand` (the dialog never formats); new tabs get natural dirty tracking (empty stays clean, content opens dirty); save validates in both dialog (disable) and store (throw); customs save atomically and expand at use time, never at save time.
- Did it break anything else: no. Additive dialog plus one accelerator plus one guard flag; `TabModel.NewTab` untouched; Ctrl+T/Ctrl+N paths unchanged.

## Round 1 lenses (Daily-journal gap, found by this review)

Item 1's Done-when demands every built-in opens expanded, but only Meeting notes and Blank note opened in the room (Daily journal was listed, and all three expand in unit). Fixed with `DailyJournalOpensExpanded` (third built-in opens with the locale date, panel containment asserted), no product change.

- `adversarial` approve: template names never become paths except through the store's sanitizer (unit-pinned); title text is inert post-expansion (no re-expansion pass); a hostile custom body renders as text, never executes.
- `consistency` approve: dialog mirrors SnapshotsDialog (injected seams, guard flag, error TextBlock); store mirrors the atomic temp-plus-move shape; tests mirror the suite's per-file helpers.
- `integration` approve: §2 (`NewTab` plus `NotifyEdited`) reused, none forked; Ctrl+N/Ctrl+T paths untouched; trigger contract recorded both sides with the menu owner; settings-seam root reused for the customs dir.
- `source-defect` approve: new build, no baseline; no Notepad surface claimed.
- `design` approve: title box, all use buttons, save button, and error text accounted for; every built-in now opens in the room; panel containment driven. No user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives (item 1 cites the round-1 drive); deferrals: none.

Advisories (noted, not blocking): (a) the dialog's error TextBlock is undriven in the room (store throws are unit-driven; same standing advisory as §16). (b) Customs are listed unsorted in directory order; fine at human scale.

## Gate quotes

Final full gate on the candidate (`2ff23cb` plus `d7cc0d1`), exit 0:

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 190, Skipped: 0, Total: 190 - Unit.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 80, Skipped: 1, Total: 81 - UI.dll (net10.0)` (the skip is the pre-existing `TabBarTests` ctxmenu-name-race quarantine, not §17)
