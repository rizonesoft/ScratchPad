# Review: D01 T01 §13 -- Pinned Tabs

Candidate: `a14f5d8` (pin model, session round-trip, tab-bar toggle plus glyph, bulk-close skips, `PinnedTabsTests` plus unit tests) plus `6a5d36d` (UI-harness cascade repair, tests only; the `run-gate.ps1` stray sweeps ride as gitignored working-tree tooling, `build/` is untracked by repo rule) plus `7799a73` (review round 1: feed release on close). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (pin plus unpin): `Tab.IsPinned` (`TabModel.cs`, `INotifyPropertyChanged`, default false); `TabBar.TogglePin` on `TabViewItem.DoubleTapped` with the live-tab guard (a double-clicked X closes first, the second click must not pin a ghost); pin `FontIcon` (`\uE718`, UIA name `Pinned`) leading the header, `Collapsed` (zero width) when unpinned so unpinned tabs render exactly as stock; pathed pins feed `ShellSettings.PinnedFiles` via `PinnedFiles.NotePinned`/`NoteUnpinned` (oldest-first, uncapped, case-insensitive; untitled pins stay session-only); `PinToggled` re-commits the jump list. The context menu is untouched (4 stock items, §3 parity). Driven: `DoubleClickTogglesPinGlyph` (glyph appears/vanishes in the live UIA tree), `PinsSurviveRelaunch` (settings write read back).
- Item 2 (restart persist): `SessionTab.IsPinned` (additive, pre-pin sessions deserialize unchanged); `SessionCapture` copies it; `MainWindow` restore applies it (file, untitled, and ghost paths); a pinned tab makes a session non-trivial (`SessionData.IsTrivial`); §6 missing-file rules unchanged. Driven: `PinsSurviveRelaunch` end-to-end plus `CaptureCopiesPinState` and `TrivialPinnedTabPersists`.
- Item 3 (bulk skip): `CloseOthersAsync` and `CloseRightSkipsPinned` filter `!tab.IsPinned`; window close preserves everything silently per §7; explicit single closes (X, menu, Ctrl+W, middle-click) close pinned tabs normally, browser-style. There is no close-all command (menus are D01 T02 §1's surface), so the contract covers the two bulk closes. Driven: `CloseOthersSkipsPinned`, `CloseRightSkipsPinned`, `SingleCloseStillClosesPinned` (extended round 1: feed presence before close, absence after).
- Item 4 (commit): message matches `a14f5d8` verbatim; review fixes ride their own commits per the fix-forward rule.

## Self-review (three questions)

- Does it make sense: yes. The exact outcome (toggle gesture, glyph, persistence, feed, bulk skips) with no extra scope; no menu surface added, no second pin treatment (shared tab styles consumed).
- Is it logical: yes after round 1. Before the fix, closing a pinned tab orphaned its `PinnedFiles` entry (feed claimed a pin with no removal path, since unpin needs the tab); `ReleasePinFeed` on both removal sites closes it, with the same-path second-tab guard. Failure paths agree: corrupt sessions reset whole (§6), jump commits never throw out of the gesture, settings writes stay atomic.
- Did it break anything else: no. `IsPinned` is additive everywhere (model default, session default, trivial-rule narrowing is the intended behavior, driven); bulk-close filters only skip pinned; the harness repair touches tests plus the gate runner only and re-proves the §11 icon suite 2/2.

## Round 0 lenses (gate cascade, found while gating)

The full gate failed 58/63 UI in 27s and left ~60 windows in one process: the first test failed its icon read and its `finally` only closed the window (FlaUI `Dispose` does not reap), so the lingering single-instance primary absorbed every later launch as a redirected window. Fix: `AppIconTests` gets the wait-then-kill `CloseApp` guarantee; the icon read polls past `Loaded`; all 60 attach sites route through `UiApp.Attach` (reaps on attach failure, rethrows); the gate pre-sweeps strays and fails loudly if any survive the UI phase. The trigger was a test race, not a product regression (the pin commit never touched the `SetIcon` path).

- `adversarial` approve: a test that cannot reap its app now fails at the stray count instead of poisoning the suite; sweeps name `IntelligentNotepad` only, stock `notepad.exe` never matched.
- `consistency` approve: `UiApp` follows the `Ui*` shared-helper shape; `CloseApp` mirrors the other files verbatim.
- `integration` approve: attach-failure reap preserves the single-instance behavior under test (multi-window and launch-routing tests unaffected, they attach through the same funnel).
- `source-defect` approve: no Notepad behavior at stake; the stock double-click no-op stands as recorded on the section.
- `design` approve: no surface changed.
- `record` approve: the cascade is filed as a run critical event with mechanism plus rule; the §11 stamp stands (its suite re-proven 2/2 on the repaired harness).

## Round 1 lenses (orphan feed entry, found in review)

- `adversarial` approve: mistyped `IsPinned` in `session.json` takes the whole-session reset path (§6, `JsonException`); `TogglePin`/`ReleasePinFeed` settings writes are atomic; the jump commit cannot throw out of the gesture (`RefreshIfChanged` catches `COMException`/`InvalidOperationException`/`UnauthorizedAccessException`); the live-tab guard kills the double-clicked-X ghost.
- `consistency` approve: `ReleasePinFeed` sits beside `TogglePin`, same load-mutate-save shape; `PinToggled` reuse for the jump refresh; test helper mirrors `WaitForPinGone`.
- `integration` approve: all removals funnel through the two hooked sites (`CloseClean`, DontSave branch; spare drop routes through `RequestCloseAsync`); reopen pops unpinned, matching the release; window close preserves (no removal, feed kept); second pinned tab holding the path keeps the entry.
- `source-defect` approve: new build, no baseline; the 4-item menu is byte-untouched (no menu code in the candidate).
- `design` approve: the glyph is the whole surface, driven in the live UIA tree per tab (appear/vanish, correct scoping); collapsed pins occupy zero width (unpinned renders as stock). No unaccounted controls. User guide: none exists in the tree, N/A.
- `record` approve: item ticks name their drives; item 3 ticked at stamp on its recorded drives; deferrals: none.

## Gate quotes (final full gate on the candidate, exit 0, no strays)

- `Smoke.dll`: `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll (net10.0)`
- `Unit.dll`: `Passed! - Failed: 0, Passed: 182, Skipped: 0, Total: 182 - Unit.dll (net10.0)`
- `Protocol.dll`: `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll (net10.0)`
- `UI.dll`: `Passed! - Failed: 0, Passed: 62, Skipped: 1, Total: 63, Duration: 6 m 27 s - UI.dll (net10.0)` (the skip is the pre-existing quarantine)
- Focused proofs on the candidate: `PinnedTabsTests` 5/5 (with the round-1 feed assertions), `AppIconTests` 2/2. `TreatWarningsAsErrors` plus `AnalysisMode All` make green mean 0 warnings and analysis clean. `validate`: 0 fatal, 0 warnings. `self-test`: 393/393.
