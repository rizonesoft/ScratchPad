# D01 T02 §1 review: Menu Bar with All Items and Enablement

Candidate: `156839b` (single commit). Full WinUI menu bar (File/Edit/View plus the marked Tools additions), COM open/save dialogs with encoding combos, `IMenuHost` seam with id-keyed `MenuCommands` enablement, Bing URL builder, 27 UI drives plus Bing unit pins, §7 contract-change update, four stock menu captures.

## Self-review

- Makes sense: the eleven items map to the diff exactly (XAML structure, live routing plus pending table, enablement matrix, Tools placement, File behaviors, Time/Date pending, Bing, View pendings, access keys, Clear Formatting pending, commit). The TabBar outlets, accelerator migration off `AddAccel`, and §7 test update are required contract changes, not scope. No Redo item, matching the probe.
- Logical: live items enabled in every state per the stock probe; pending items disabled with owner-recorded enablement lines on all ten owner sections; Exit and Close window share `Close()`; Auto-Detect maps to null forced encoding; Save on untitled opens Save As and Cancel preserves the buffer; Recent empty state reads `No recent files` plus Clear list; missing recents report the §4 NotFound text.
- Breakage: accelerators moved from `AddAccel` to menu-owned bindings (double-fire removed, Ctrl+W ownership transferred with a comment); TabBar gains nullable outlets (null keeps the old fallback, no bare-TabBar crash); `OpenOptions` untouched; no engine, protocol, or settings behavior moved. Full gate green with no new skips.

## Lenses round 1 (8 findings, all fixed pre-commit)

- `record` needs-attention: `PendingOwnerItemsStayDisabled` and `AssertStates` skipped absent items silently (`if not null`), so a missing item passed without proof. Fixed: per-menu id lists with presence asserts; the `live` array is cross-checked by `Assert.Contains`.
- `consistency` needs-attention: toggle unchecked-state (item 8) was unproven. Fixed: `ToggleState Off` asserts for Status bar and Word wrap (the Markdown pair are plain items, check-state vacuous).
- `integration` needs-attention: typed-missing-name offer (item 5) had no drive. Fixed: `FileOpenMissingNameOffersCreate` types a missing name, asserts the `CreateFileDialog` names it, declines, and proves no tab and no file.
- `integration` needs-attention: tab-encoding prefill and tab EOL (item 5) were unproven. Fixed: `FileSaveAsPrefillsEncodingAndEol` opens a BOM-less UTF-16 LE CRLF file, asserts the filename and encoding prefills, saves, and proves CRLF UTF-16 bytes.
- `source-defect` needs-attention: Open filter value, Auto-Detect default, and single-select (item 5) were unproven. Fixed: `FileOpenShowsPickerWithEncodingList` asserts the type filter and the default; `FileOpenOpensExactlyOneFile` shift-extends a two-file selection and proves exactly one tab.
- `consistency` needs-attention: live keyboard shortcuts Ctrl+N/O/Shift+S/Shift+W had no drive while the file header claimed behavioral pins. Fixed: `FileMenuLiveAcceleratorsWork` drives all four (close-window via relaunch-restore); header reworded to live-only.
- `record` needs-attention: `FileRecentMissingReportsNotFound` used a mouse click (stale-capture class) and asserted dialog presence without the message. Fixed: `InvokeOrClick` plus the NotFound text assert.
- `design` advisory: rendered menus screenshotted and compared item by item against the six stock captures. File/Edit/View/Zoom/Markdown/Recent/Tools match in labels, order, separators, and shortcuts except two framework-owned cosmetics on disabled D02-owned items: accelerator text renders `Delete`/`Ctrl++`/`Ctrl+-` where stock reads `Del`/`Ctrl+Plus`/`Ctrl+Minus` (WinUI renders the correct keys; no XAML override exists), and the Markdown pair lacks stock's glyph icons. Both route to the §6 audit note recorded on §6.

## Lenses round 2 (candidate `156839b`, final)

- `adversarial` approve: unknown menu ids throw in `SetEnabled`; forced encoding names are index-mapped (Auto-Detect to null, no unknown-name path); missing/failed opens render dialogs, never tabs; no input reaches a handler without its owner; Bing never launches in tests.
- `consistency` approve: naming follows the `Menu{Top}{Item}` convention; dialogs reuse the §4/§5/§8 specs and messages; close helpers reap on every path including COM timeouts; no second menu treatment.
- `integration` approve: all five deferred T01 triggers invoke their owner dialogs from Tools; open/save/close/exit share the engine paths; §7 matrix updated for inline Save As; ten owner sections carry enablement lines.
- `source-defect` approve: labels, order, separators, and shortcuts read off the six stock captures plus UIA dumps; Bing pair presence re-proven (no-Bing rule dead); Bing URLs, typed-missing offer, missing-recent NotFound, and Exit-equals-close recorded as defaults with costs.
- `design` advisory: surfaces match per the round-1 comparison; the two advisories above stand, both invisible until D02 owners land and both dispositioned to §6.
- `record` approve: every tick names its drives; the §6 audit note carries the advisories; deferrals name owners; the cascade incidents (COM-timeout leak, pipe hang, eaten clicks) are run-logged with fixes, not hidden.

## Gate quotes

Full gate on `156839b` (`build/gate-last.log`, exit 0, no strays):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 275, Skipped: 0, Total: 275 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 144, Skipped: 1, Total: 145 - UI.dll` (the skip is the pre-existing quarantine)

Section checkpoint on `156839b`: `Passed! - Failed: 0, Passed: 27, Skipped: 0, Total: 27 - UI.dll` (`FullyQualifiedName~MenuBarTests`), exit 0, no strays.

`validate` 0 fatal, `self-test` 393/393, build 0 warnings 0 analyzer errors.
