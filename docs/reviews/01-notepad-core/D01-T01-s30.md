# D01 T01 §30 review: Locked-Tab Residue Hardening

Candidate: `b04f7f0` (single commit). Closes the §19 review advisory (a): no locked-tab plaintext on disk outside the locked file.

## Self-review

- Makes sense: dialog refusal plus capture rule plus doc residues note map to the three items exactly; no widening.
- Logical: refusal is dialog-deep (Take disabled, note onscreen, `Take()` early-returns) with the versions list kept for pre-§30 restores; the capture rule nulls Content for locked tabs so session and checkpoint share one path-only shape that dispatches to the §19 ghost rule; the nested ternary is right-associative-correct and analyzer-clean; locked-plus-untitled cannot occur (lock needs a path) but the rule covers it; the dirty-drop consequence is documented, not silent.
- Breakage: `TabSnapshot` gains an optional-last param (existing constructions compile; app passes the 9th), `SessionTab` shape unchanged (old sessions deserialize), dialog ctor has one caller (updated), `SnapshotStore.Take` needs no guard (dialog is its sole caller). Neighbors green.

## Lenses (single round, no findings)

- `adversarial` approve: refusal is three-deep (disabled button, `Take()` guard, sole-caller); locked Content is unconditionally null (no flag combination persists a buffer); dirty-drop is documented data loss, not silent loss.
- `consistency` approve: note mirrors the save-first treatment (inline TextBlock, AutomationId, onscreen drive); capture rule extends the §6 pure function; doc paragraph follows the file's one-line-per-paragraph style.
- `integration` approve: ghost dispatch needs no change (path-only hits the §19 rule); old sessions deserialize (shape unchanged); checkpoint and session share the rule (same file, one change).
- `source-defect` approve: new build, no baseline (stock encrypts nothing); advisory (a) quoted verbatim as the source; no parity claim.
- `design` approve: note text pinned verbatim in the room; versions list preserved by recorded decision; no user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives; the dirty-drop consequence is in the doc, not buried; pre-§30 residues named with the no-wipe cost.

## Gate quotes

Full gate on `b04f7f0` (`build/gate-last.log`, exit 0, no strays, first try):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 271, Skipped: 0, Total: 271 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 117, Skipped: 1, Total: 118 - UI.dll` (the skip is the pre-existing quarantine)

`validate` 0 fatal, `self-test` 393/393, build 0 warnings.
