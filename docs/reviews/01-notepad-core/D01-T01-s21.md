# Review: D01 T01 §21 -- Reload Prompt on External Change

Candidate: `e71fa04` (WroteFile event, ReloadDialog, reload engine partial with window hooks, unlock spec-apply plus success report, ReloadTests plus WroteFile unit tests) plus `5c2c1e6` (round 1: path-filtered static-event tests, dirty-dot Keep postcondition, Cancel drive). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (prompt on clean): per-tab `FileWatcher` pends on external bytes; `CheckPendingReloadsAsync` asks when window and tab are active. Driven: `CleanChangePromptsAndReloadRefreshes`.
- Item 2 (reload refreshes, keep holds): reload fills with the detected spec applied; keep holds the buffer and dirties. Driven: reload half of the clean test plus `KeepHoldsBufferAndDirties` (close prompts after keep) plus `CancelKeepsLikeKeep` (Cancel keeps too).
- Item 3 (dirty resolves explicitly): dirty prompt names the discarded edits; reload takes disk, keep takes edits. Driven: `DirtyReloadDiscardsEdits` plus `DirtyKeepPreservesEdits`.
- Item 4 (unsaved never prompts): untitled tabs carry no watcher. Driven: `UntitledAndIdenticalWritesNeverPrompt` plus `DeletedFileReloadFailsLoud` and `LockedReloadRoutesThroughUnlock`.
- Item 5 (commit): message matches `e71fa04` verbatim.

## Self-review (three questions)

- Does it make sense: yes. External writes prompt reload-or-keep with both answers explicit; own saves never prompt via commit baselines; convergent bytes auto-resolve; deleted and locked files degrade to the missing-file notice and the unlock detour.
- Is it logical: yes. Detection is hash-based (no timing windows); the asked-bytes guard dedups watcher stragglers while genuine rewrites re-pend; baselines refresh on every resolution; the prompt gate rechecks only when work remains.
- Did it break anything else: two §19 interplay changes, both disclosed in the commit (unlock applies the detected spec; the detour reports success). Own-save prompts verified absent by every existing save test staying green.

## Lenses (round 1, one finding, fixed)

- `adversarial` approve: rapid external rewrites converge (reload always re-reads fresh bytes, keep always holds the buffer, so no TOCTOU loses data either way); watcher callbacks marshal to the UI thread; deleted and locked branches degrade loud; Cancel keeps (driven this round). Watcher overflow drops stay declared in the Treatment with the hook-plus-rescan cost.
- `consistency` approve: dialog mirrors the tree's code-built pattern (AutomationId, primary/secondary/close, default button); per-tab watcher lifecycle follows add/remove plus path-change; tests mirror the suite's per-file helpers. Advisory: `OnWroteFile` skips unresolvable paths while `FullPathOrEmpty` maps them to a junk `""` baseline key (one key max, never matched); unify when the map is next touched.
- `integration` approve: §4 `Detect` plus §5 `SaveFile`/`SaveBytes`/`ApplySave` reused, none forked; the unlock detour's new bool return is ignored on the §19 open path (behavior unchanged, proven by the §19 suite staying green); `Start/StopReloadWatching` hook window open/close; the static event's test subscribers detach in `finally`.
- `source-defect` approve: new build, no baseline; no Notepad surface claimed.
- `design` approve: Reload, Keep, and Cancel buttons plus clean and dirty wordings all driven in the room (Cancel added this round). No user guide exists in the tree, N/A.
- `record` needs-attention then approve: finding (a) the Cancel button had no drive (Treatment: "Cancel keeps"); fixed by `CancelKeepsLikeKeep` in `5c2c1e6`, green focused and in the final gate. Item ticks name their drives; deferrals: none (reactive, nothing to defer; overflow hook cost recorded).

## Gate quotes

Final full gate on `e71fa04` plus `5c2c1e6` (`build/gate-last.log`, exit 0, no strays):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 226, Skipped: 0, Total: 226 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 101, Skipped: 1, Total: 102 - UI.dll` (the skip is the pre-existing quarantine)

Focused proofs along the way: Unit 226/226 for the path-filtered static-event tests; `KeepHoldsBufferAndDirties` 1/1 with the dirty-dot postcondition plus its no-dot-while-clean guard; `CancelKeepsLikeKeep` 1/1. `validate` 0 fatal, `self-test` 393/393, build 0 warnings.
