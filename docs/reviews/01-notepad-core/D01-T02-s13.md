# Review: D01 T02 §13 -- ScratchPad Rename Completion

## Evidence map

Candidate `f9e38b2` (ship) covers all 6 items: item 1, tracked case-insensitive census of 33 hits in 13 files with keeps classified (NoteCrypto magic + tests, AppDataDir migration + test, historical plans, §6 stamped history, plan before-state prose) and 18 fixed (17 scheme + 1 skill prompt); item 2, `ProtocolAssociation.Scheme` to `scratchpad` plus comment and 15 test literals, unclaimed in HKCU/HKCR, Linux link units 38/38, Windows ProtocolHandlerTests 5/5; item 3, both-OS builds 0 warnings 0 errors quoted; item 4, Windows neutral Smoke 1/1 Unit 336/336 Protocol 35/35 plus UI golden subset 5/5, Linux neutral green; item 5, AppDataDirTests fixtures plus live migration proof (marker copied byte-identical, legacy intact, user data restored byte-verified); item 6, the commit itself. §26 decision lines carry dated corrections; the §26 stamp block is untouched.

## Self-review

Sense: the rename completes exactly (one functional straggler fixed, keeps documented, nothing behavioral beyond the name literal) with one mechanical skill-prompt fix flagged in the commit body. Logic: registry keys derive from the renamed const so no key text was hand-edited; constant-driven tests follow automatically and literal-driven tests assert the new links; migration semantics untouched. Breakage: no caller branches on the old name (LaunchArgs link branch and registrar are const-driven); affected suites green on both OSes; the full-suite proof is the ship CI run quoted below. No fixes in self-review.

## Lenses (round 1, candidate `f9e38b2`)

- `adversarial`: approve. Scheme verified unclaimed in both hives before use; parsing logic untouched (same hand parser, same rooted check); no compat shim needed or added (unreleased app, zero deployed links); migration best-effort and legacy-preserving as before; registry clean after the drives (both scheme keys absent). Advisory: a pre-rename `intelligent-notepad` key lingers only if a pre-rename test run died mid-cycle on a dev box; dead key, never invoked, no cleanup code warranted.
- `consistency`: approve. Identity now uniform (`scratchpad` scheme beside `ScratchPad.Document`, `Rizonesoft.ScratchPad`, `URL:ScratchPad Protocol`); test literals match the const; dated corrections follow the file's convention without touching the §26 stamp; skill prompt matches the live guards already emitted.
- `integration`: approve. D07 T01 §1 consumes the final identity per the XREF (rename-first sequencing held: §13 ships in Phase 1, packaging later); §26 behavior re-verified under the new name (5/5 drives); golden subset green (chrome untouched by the rename); untouched Path/registry call sites unaffected.
- `record`: approve. Every item's Done-when is evidenced with quoted runs; the Why/item/checkpoint corrections are marked with before-state; no deferrals owned or written; row flip earned.
- `source-defect`, `design`: not owed (mechanical rename plus verification; no Notepad behavior or UI surface at stake).

## Opus panel (round 1, candidate `f9e38b2`, verbatim)

**adversarial: advisory**
- `src/Notepad.Core/ProtocolAssociation.cs:10,14,16` — after the constant flips to `scratchpad`, `SchemeKey` and `BackupKey` derive only the new key names, so `/unregister-protocol` (`src/ScratchPad/FileAssociationRegistrar.cs:102-123`) can no longer see `HKCU\Software\Classes\intelligent-notepad` or the backup `...\protocol-intelligent-notepad`. Any machine where the old build registered the scheme and did not complete its unregister keeps an orphaned tree with a `shell\open\command` pointing at the old exe path, cleanable only by hand. This is not hypothetical on the rename machine: `docs/phase-runs/2026-09-15-phase-1.md:63,77` records UI runs terminated mid-flight, which is exactly the path that skips `ProtocolVerbsCycleCleanly`'s `finally` unregister (`tests/UI/ProtocolHandlerTests.cs:38-41`). The section's evidence verifies only that `scratchpad` is unclaimed (`todo/01-notepad-core/TODO-02-menus-settings-status.md:381`); the old key's live state was never checked, yet item 1 claims "Zero functional stragglers". Fix: query and delete the two old keys on the rename machine and quote the result, or record the residue as a known dev-machine leftover.

**consistency: advisory**
- `src/Notepad.Core/ProtocolAssociation.cs:6-7` and `tests/UI/ProtocolHandlerTests.cs:13-14` — the substituted shorter name left the comment wraps ragged ("...`scratchpad://<url-encoded absolute path>`; the launch" and "// D01 T01 §26: the scratchpad:// protocol. Registration cycles") breaking well short of the fill column used by every neighbouring comment line. Rewrap both comment blocks.

**integration: advisory**
- `todo/07-release/TODO-01-packaging-and-update.md:53` and `todo/01-notepad-core/TODO-02-menus-settings-status.md:378` — the bidirectional XREF pins "exe, AppId, ProgId" as the identity packaging inherits, but this section just made the URL scheme a fourth identity surface (its own Why at line 366 audits exactly those four). Nothing carries the scheme forward: `D07 T01 §1` item 2 (line 57, package identity pinned) and `D07 T01 §2` item 3 (line 71, "uninstall removes the app, associations, and settings with nothing orphaned") will not know a protocol declaration exists. Add the scheme to both XREF texts.

**record: advisory**
- `docs/phase-runs/2026-09-17-phase-1.md:1-30` in `f9e38b2` — the checkpoint at `todo/01-notepad-core/TODO-02-menus-settings-status.md:387` states "this section's commit contains only rename-completion", but the commit also adds a 30-line phase-run record (phase repairs, resolve table, gap audit, a run-4 staleness correction), none of which is rename work. Repo precedent allows run files to ride section commits (`04c766c`, `7e0283d`), so this is a contract-wording mismatch rather than mixed behavior, but the checkpoint as written is not literally met.
- `todo/01-notepad-core/TODO-02-menus-settings-status.md:380` — "census 33 hits in 13 files" is arithmetically self-consistent (17 scheme + 1 skill line + 15 keeps; 13 files) only because `docs/reviews/` and `docs/phase-runs/` were excluded from the count, which the item does not state. Re-running the item's own stated case-insensitive tracked grep returns roughly 50 hits across 25+ files (the review and phase-run class is named as a keep but never counted). State the exclusion in the census so the number reproduces.

Fix record (all four applied, no `needs-attention` so no fix-loop round owed): adversarial, old-scheme key queried on the rename machine (absent, `Test-Path` false) so no orphan exists and nothing was deleted; consistency, both comment blocks rewrapped; integration, scheme added to both XREF texts; record, checkpoint corrected with the run-file precedent and census states its exclusion. All fixes comment/prose-only; gates below re-run after them.

## Gates re-run

Ship CI run 35212112337 on `f9e38b2`: success on build-linux and build-windows. After the advisory fixes (comment/prose-only): `validate` 0 fatal 0 warnings (re-run, quoted at stamp). The stamp push re-runs CI on the final tree.
