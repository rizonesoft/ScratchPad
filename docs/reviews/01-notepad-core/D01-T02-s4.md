# D01 T02 §4 review: Status Bar

Candidate: `fbf0088` (21 files: strip control plus neutral segments, shell/menu/tab wiring, 8 fill sites through `SetBoxText`, 8 UI drives plus 46 unit fixtures, 6 stock captures, refreshed main golden, bidirectional D02 T04 back-reference). Pre-findings folds into the unpushed candidate: Dispose unhook, main-golden refresh with confinement proof, TODO-04 enablement line.

## Self-review

- Makes sense: the 8 live items map to the diff exactly (strip binds all six segments live; static-segment parity negative driven; count math plus selection counts in fixtures; View toggle persisted and driven; doc-total versus selected-plus-total fixtures; disabled Formatted switch with named owner; CR segment fixture) and the strike carries its parity negative. `SetBoxText`/`SetSelectionStart`, the golden refresh, the MenuBarTests rotation, and the D02 T04 enablement line are required contract changes, not scope.
- Logical: every settings write goes through `Update` only (the toggle handler writes just `ShowStatusBar`); fills announce via `TabsEdited` while clean-vs-dirty stays with the callers that already own it; caret/selection offsets clamp in neutral code; `Show` writes text only and fires nothing back, so refresh is reentrancy-free; Dispose unhooks the box.
- Breakage: full cheap gate green plus section UI 8/8 and golden pair 2/2; no remaining direct tab-box fills (grep-verified); `IMenuHost` has one implementer; checkpoint re-arms on fills only rewrite identical-or-newer buffers (debounced, idempotent).

## Lenses round 1 (candidate `fbf0088`)

- `adversarial` approve · `consistency` approve · `integration` approve · `record` needs-attention (1) · `source-defect` approve · `design` approve (2 advisories, dispositioned above). No code defects.

## Lenses round 2 (stamp fixes, final)

- `record` fix: the stamp as written failed `validate` with 3 fatals (deferral without `-> XREF:` owner syntax; file-level XREF to TODO-04 one-sided twice via the stamp bodies). Fixed: deferral names `-> XREF: D02 T04 §3 (item: "...")`, `- -> XREF:` bullets added both ways (§4 forward, TODO-04 §3 back). Re-ran `validate`: 0 fatal, 0 warnings.
- `adversarial` approve · `consistency` approve · `integration` approve · `record` approve · `source-defect` approve · `design` approve. No further defects.

- `adversarial` approve: null text throws, offsets clamp, zoom default flows from the store; no new trust boundary (the strip reads the active tab and store, writes nothing). Advisory (not blocking): per-keystroke recompute is O(n) over the buffer; the 1 MiB/500 ms bar test bounds it and §9 owns typing perf.
- `consistency` approve: helper naming matches `ContentFor`; section-cited comments; store writes through `Update`; test layout mirrors §§1/3 (UI drives plus neutral fixtures).
- `integration` approve: all 8 programmatic fill sites converted (launch ×2, restore ×2, reload, undo-redo, template, peek-rehydrate); View toggle enabled through the §1 registry path with the check synced both ways; MenuBarTests rotates StatusBar from the disabled list to an enabled-and-checked assert; main golden refreshed.
- `source-defect` approve: six stock captures filed (`tabs` plain strip, `md-formatted`/`md-syntax` switch pair, `cr` Macintosh segment, `eol-click` full-frame no-popup proof, `toggle-off` bar-absent negative); click parity (single probed twice plus double-click once across five segments, no popup, no state change) and the toggle On/Off pair recorded in the struck item; labels and order verbatim from the captures; stock 11.2607.14.0 current.
- `design` approve: fresh capture eyeballed (one clean Untitled tab, settled menus, strip reads `Ln 1, Col 1 | 0 characters | Plain text` left and `100% | Windows (CRLF) | UTF-8` right, matching stock order and labels); pre-refresh mismatch 0.187% confined to the status band (933 px, diff image viewed, tab/menu/editor pixel-identical). Advisory (not blocking): during an active selection Ln/Col shows the selection start because WinUI TextBox exposes no anchor direction; exact whenever no selection.
- `record` approve: every tick maps to named drives (`StatusBarTests` 8/8, `StatusSegmentsTests` 46/46 incl. the §9 latency bar, MenuBarTests delta, golden pair); one deferral (Formatted switch enablement) owned by D02 T04 §3 with the enablement line bidirectional; gates ran on the exact candidate content.

## Gate quotes

Cheap gate exit 0 plus section drives on the final tree:

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 331, Skipped: 0, Total: 331 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 8, Skipped: 0, Total: 8 - UI.dll (StatusBarTests)`
- `Passed! - Failed: 0, Passed: 2, Skipped: 0, Total: 2 - UI.dll (GoldenComparisonTests)`

`validate` 0 fatal, `self-test` 393/393, build 0 warnings.
