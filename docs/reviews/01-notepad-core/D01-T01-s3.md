# Review: D01 T01 §3 -- Tab Bar UI

Candidate: `fea3412` (single commit). Stance: ship review. Rounds: 1.

## Evidence map

| Item | Satisfied by |
| ---- | ------------ |
| TabBar renders model list, new-tab, switch, close, no-reorder | `TabBar.xaml` + `TabBar.xaml.cs` (manual TabViewItems, CanReorderTabs off); drives `InitialTabRendersFromModel`, `ThreeTabsSwitchAndClose`, `DragAttemptLeavesOrderUnchanged` |
| Dirty close routes to prompt | `SavePromptDialog.cs`, `RequestCloseAsync`; drives `DirtyClosePromptsAndCancelKeepsTheTab`, `DontSaveClosesAndNeverReopens` |
| Shortcuts match Notepad | `AddTabAccelerators` in `MainWindow.xaml.cs`; drives `ThreeTabsSwitchAndClose`, `NumberShortcutsAndReopenMatchNotepad` |
| Middle-click and context menu | `MiddleClickHook.cs` + `Tabs_RightTapped`; drives `MiddleClickClosesTheTabUnderTheCursor`, `ContextMenuMatchesNotepad`; resting pixels in refreshed `main-window.png` golden |
| Reopen shortcut | `ReopenLast` over §2 stack; drives in `NumberShortcutsAndReopenMatchNotepad` (empty-untitled no-op) and `DontSaveClosesAndNeverReopens` (discard no-op); saved-tab reopen stays model-level per the section's Corrected note until §4 |
| Overflow shrink | `ShrinkTabsToFit`; drive `OverflowShrinksTabsWithoutScrollChrome` (nonzero widths, fits strip, selected widest, no scroll chrome) |

## Self-review

1. Does it make sense? Yes. Every checklist item has its drive; the checkpoint (three tabs, reorder attempt unchanged, switch, close each, dirty prompt) is `ThreeTabsSwitchAndClose` plus the prompt drives. No scope beyond the section: the hook, the prompt dialog, and the test helpers all serve named items.
2. Is it logical? Yes. Closes funnel through `RequestCloseAsync` (glyph, Ctrl+W, menu, middle-click); the model stays the sole decider (dirty lives in `TabModel`, the bar reflects); failure paths prompt or stay silent exactly as probed (dirty prompts, clean closes silent, Save-keeps-open interim recorded for §4/§5).
3. Did it break anything else? No. MainWindowTests still 9/9, Unit 31/31, Protocol 8/8, Smoke 1/1; `MainWindow.xaml` diff is stretch alignment only; editor/status regions untouched; neutral suite unaffected (no `Notepad.Core` change).

## Lenses

- `adversarial`: advisory. (a) The hook swallows the paired middle-up wherever it lands, so a down on our tab held across a window switch eats one middle-up in another app; middle-up is meaningless nearly everywhere, no action. (b) `RequestCloseAsync` is fire-and-forget from its callers, so a dialog failure would go unobserved rather than crash; §7 owns the full answer matrix including failure behavior. (c) The hook callback does one comparison plus `CallNextHookEx` for non-middle traffic; middle traffic costs a few p/invokes on the UI thread, no XAML reentry.
- `consistency`: approve. File-scoped namespaces, `internal sealed`, p/invoke style mirrors the tree (`DefaultDllImportSearchPaths`, nested `NativeMethods`); `RelayCommand` carries a reasoned CS0067 pragma; comments cite live probes with dates; test helpers (`TabItemAt`, `PinTopmost` use) extend existing patterns.
- `integration`: approve. MainWindow owns the hook lifetime (install on first Loaded/Activated, `Dispose` on close); the bar-to-model contract is unchanged from §2; `SavePromptDialog` declares §7 reuse; editor auto-scroll passes through for D02; §9 multi-window routing is noted at the hook; `ContentFor` scaffolding declares D02 replacement.
- `source-defect`: approve. Every Notepad claim is probed and recorded: no-reorder (three negative probes), Ctrl+9-last, Ctrl+Shift+T no-restore after empty/discard closes, prompt title/wording/buttons, dirty-dot-replaces-X, shrink overflow (3..128 widths, selected widest), four context-menu items, middle-close. Declared soft spots: `{first-line}.txt` prompt naming is a single observation (§7 re-verifies); batch-cancel-aborts-all, NumPad, and drag-out-to-window are recorded defaults with costs.
- `design`: approve. Rendered strip against `stock/notepad-tabs`: tab+X+add arrangement matches, dirty dot replaces the X until hover, overflow shrinks with no scroll UI; the refreshed `app/main-window.png` golden holds `FreshCaptureMatchesGolden` in tolerance. Deferred chrome (menu row, formatting toolbar, status segments, avatar, gear) has named owners per the §1 surface accounting. No user guide exists in `docs/` yet, so no guide update is owed.
- `record`: approve. Evidence below matches executed runs; no open deferrals; both transient flakes are disclosed with their handling (one unknown 8/9 mid-run, one empty `TabItems` query now covered by the polling `TabItemAt` helper, which still fails genuine emptiness after 5s).

## Gates (on the candidate)

- `dotnet build src/IntelligentNotepad.slnx` (Windows): Build succeeded, 0 warnings, 0 errors.
- `dotnet test src/IntelligentNotepad.slnx` (Windows): Smoke 1/1, Unit 31/31, Protocol 8/8, UI 18/18 (9 MainWindow + 9 TabBar); TabBar 9/9 repeated across three runs.
- `dotnet test src/Notepad.Neutral.slnf` (Linux): Smoke 1/1, Unit 31/31, Protocol 8/8.
- `todo-graph.py self-test`: 391 cases, 0 failed. `validate`: 0 fatal, 0 warnings, 19 pre-existing adjacency advisories.
