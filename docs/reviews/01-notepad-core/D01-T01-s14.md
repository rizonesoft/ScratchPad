# Review: D01 T01 §14 -- Text Statistics Panel

Candidate: `b247cff` (stats engine, `StatsDialog`, Ctrl+Shift+G opener, `StatsPanelTests`) plus `ab51d4f` (review round 1: dialog reentrancy guard, single-dialog drive, repetition-truncation drive). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (top words): `StatsDialog` top grid (label/value pairs on a shared right edge) over `StatsController.Current.TopWords`; `TextStats` top-10 by count with the Ordinal tie-break. Driven: `PanelListsFixtureExactStats` (fixture-exact rows, containment in window, single dialog).
- Item 2 (sentence distribution): sentences grid (count, mean, shortest, longest, three buckets) over `DocumentStats`. Driven: `PanelListsFixtureExactStats` (7 fixture-exact rows) plus `EmptyTabsShowZeros` (all-zero rows).
- Item 3 (repetition flags): repeated-words list over `DocumentStats.RepeatedWords` (3-plus non-stopwords), display-capped at 50 with a `+N more` trailer. Driven: `PanelListsFixtureExactStats` (seeded `cat` flagged, stopword `The` excluded) plus `LongRepetitionListTruncatesWithTrailer` (51 rows, trailer exact).
- Item 4 (on-demand only): `StatsController` computes in the constructor and on `Refresh`, never on provider change; `StatsDialog` constructs fresh per open over the active tab's buffer (`ActiveTabTextProvider`), Refresh re-reads. Driven: `ControllerComputesOnOpenAndOnlyOnRefresh` (compute-count) plus `RefreshAndReopenRecompute` (reopen-recompute plus Refresh control in the room).
- Item 5 (commit): message matches `b247cff` verbatim; the round-1 fix rides its own commit per the fix-forward rule.

## Self-review (three questions)

- Does it make sense: yes. The exact outcome (panel with three exact sections, compute on open, refresh on demand, deferred menu trigger with a recorded contract) with no extra scope; no chrome added (goldens untouched), no menu surface touched.
- Is it logical: yes after round 1. The modal Refresh cannot show changed data (nothing types while modal), but it is the section's on-demand control and its re-read plus re-render path is driven; the reentrancy guard closes the double-open throw; the truncation cap covers the unbounded list. Failure paths agree: null provider rejected, null text coerced to empty, zero tabs show zeros, corrupt sessions reset whole (§6).
- Did it break anything else: no. Ctrl+Shift+G is free in-tree with no stock meaning; all product additions are additive (new dialog, one accelerator, one opener); the test class joins the serial UI collection through `UiApp.Attach`.

## Round 1 lenses

- `adversarial` approve: reentrancy guarded (`statsDialogOpen` flag; a second `ShowAsync` throws, so stacking is refused); null inputs rejected or coerced with drives; corrupt session takes the §6 whole-reset path. Advisory, not blocking: very large buffers compute on the UI thread on open with no cap (a 100 MB buffer would hang the open); the section demands no perf bound, so no fix here.
- `consistency` approve: dialog mirrors `SavePromptDialog` (AutomationId, title, close button, code-built); `AddAccel` reuse; test file mirrors `PinnedTabsTests` (seed, attach, close discipline); invariant number formatting throughout.
- `integration` approve: trigger contract recorded both sides (§14 Treatment, T02 §1 item 5; placement per T02 item 4 as a marked addition); `ITextProvider` seam swaps for the D02 T01 §1 buffer with no dialog change; no caller besides the accelerator.
- `source-defect` approve: new build, no baseline; no Notepad surface claimed.
- `design` approve: the rendered dialog eyeballed on capture (right-aligned values verified visually; a Star-column blowout caught on the first capture and fixed to Auto/Auto); headings plus Refresh plus Close all driven; containment in the window driven. No unaccounted controls. User guide: none exists in the tree, N/A.
- `record` approve: item ticks name their drives; the UIA-settling flake (two fused-word reads) disclosed with its fix (box-write readback, stable-double-read polling); deferrals: none.

## Gate quotes (final full gate on the candidate, exit 0, no strays)

- `Smoke.dll`: `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll (net10.0)`
- `Unit.dll`: `Passed! - Failed: 0, Passed: 182, Skipped: 0, Total: 182 - Unit.dll (net10.0)` (includes the 9 `TextStatsTests`)
- `Protocol.dll`: `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll (net10.0)`
- `UI.dll`: `Passed! - Failed: 0, Passed: 66, Skipped: 1, Total: 67, Duration: 6 m 51 s - UI.dll (net10.0)` (the skip is the pre-existing quarantine; includes the 4 `StatsPanelTests`)
- Focused proofs on the candidate: `StatsPanelTests` 4/4, `TextStatsTests` 9/9. `TreatWarningsAsErrors` plus `AnalysisMode All` make green mean 0 warnings and analysis clean. `validate`: 0 fatal, 0 warnings. `self-test`: 393/393.
