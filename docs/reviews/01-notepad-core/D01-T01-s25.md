# Review: D01 T01 §25 -- Jump List Tasks

Candidate: `fcdd3b4` (new-note flag through args, drops, and both launch paths; EnsureFreshNote; static task commit; JumpListTaskTests plus unit coverage; §25 plan decisions and ticks). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (all three appear): service commits the static task ahead of the feed. Driven: `TaskbarCarriesNewNotePinnedAndRecent`.
- Item 2 (new note opens a tab): flag flows fresh and redirected into `EnsureFreshNote`. Driven: `NewNoteRedirectOpensFreshTab` plus `NewNoteFreshWithFileSelectsSpare`.
- Item 3 (pins launch): verbatim feed arguments launch to the file. Driven: `PinTaskArgsOpenTheFile` plus `RecentTaskArgsOpenTheFile`.
- Item 4 (commit): message matches `fcdd3b4` verbatim.

## Self-review (three questions)

- Does it make sense: yes. The taskbar carries the task plus the feed, the flag opens or selects a fresh tab on both paths, and feed arguments launch verbatim.
- Is it logical: yes. Combined launches order files before the note; redirected notes target the first window like routed files; pre-flag drops still parse; the fingerprint still covers exactly the user-driven feed.
- Did it break anything else: launch record, drops, and startup signatures extended, all defaulted or single-called; the §8 feed tests and launch drives hold (proven by the gate). One blemish: the SHIP commit body contains a stray non-English word (message only, immutable, no code impact).

## Lenses (single round, no findings)

- `adversarial` approve: poison drops delete unread with the flag lost fail-closed; the note lands on the UI thread after routed files; dirty untitled tabs never satisfy the gesture; the flag opens a tab at most, never a window or a process.
- `consistency` approve: flag grammar, drop record default, and service commit mirror the §8 shapes they extend; tests mirror the per-file helpers.
- `integration` approve: record and drops extensions are defaulted (single construction site, positional callers unaffected); `Drain` keeps exact semantics via `DrainAll`; bare launches still open windows; the §8 feed tests and launch drives hold unmodified.
- `source-defect` approve: no stock counterpart per Fidelity; our contract only.
- `design` approve: no new surface (the taskbar renders the commit). No user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives; the bare-flag case rides the driven fresh path (empty file list provably no-ops into the spare select); the SHIP message blemish is noted above, code unaffected.

## Gate quotes

Full gate on `fcdd3b4` (`build/gate-last.log`, exit 0, no strays, first try):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 236, Skipped: 0, Total: 236 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 108, Skipped: 1, Total: 109 - UI.dll` (the skip is the pre-existing quarantine)

`validate` 0 fatal, `self-test` 393/393, build 0 warnings.
