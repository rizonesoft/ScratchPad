# Review: D01 T01 §2 -- Tab Model with Dirty Tracking

Candidate: `f16aeae` (model plus tests) plus `a0dbe54` (idempotent-close guard). One fix cycle from self-review evidence; lenses clean.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Model | `src/Notepad.Core/TabModel.cs`: `TabModel` (tabs, active tab, closed stack), `Tab` (id, path, dirty, encoding, line endings, display name), `ClosedTab` record, `SaveRequestOutcome`; compiles UI-free in the neutral suite |
| 2. Dirty | `NotifyEdited` flips (untitled: content presence; saved: latch), `MarkSaved` and `MarkClean` (undo-to-save-point seam for D02 T01 §4) clear; tests cover edit, save, and no-op edits |
| 3. Observable | `ObservableCollection<Tab>` plus `INotifyPropertyChanged` on model and tabs, the only notification path; two-observer test asserts identical sequences including `IsDirty` |
| 4. Untitled | No path until save; `SaveRequest` returns `SaveAsRequired` for untitled, `SaveToPath` for pathed (§5 wires the dialog) |
| 5. Auto-naming | `TabDisplayName.FromContent`: first line, trimmed, 35 chars, no ellipsis, else "Untitled" (whitespace-only inferred); boundary table 34/35/36/50 plus trim and first-line-only cases |
| 6. Closed stack | Pushes path, contents, caret, encoding, endings; skips empty untitled closes and discarded closes per live probing (11 and 11 closes, 0 reopens each); unbounded by recorded decision; LIFO, neighbor selection, and kept-fields tests |
| 7. Commit | `f16aeae`, subject matches the item exactly |

Runs: `34816231797` green both jobs (Unit 31/31, UI 9/9, Protocol 8/8, Smoke 1/1). The `34815770936` run on the first candidate had Unit 30/30 green with two UI launch-timeout flakes (`FreshCaptureMatchesGolden`, `FirstRunShowsWhatsNewAndMegaphoneReopensIt`) on unchanged UI code; signature recorded in the phase log for the §3 run to harden or quarantine. Local: TabModel 24/24, full suites green both OSes.

## Self-review

1. Does it make sense? Yes. Every item has its change; the plan correction (singular test name, twice) was recorded before building. Self-review found one real edge (non-member close with ActiveTab pointed at it indexed `Tabs[-1]`); fixed with an idempotent-close guard plus a regression test that reproduces the crash shape.
2. Is it logical? Yes. Dirty semantics split honestly (untitled content-presence per the cleared-content probe, saved latch per save/undo); the stack-push condition encodes exactly the probed reopen behavior; reopen restores path-clean or content-dirty correctly; `SaveRequest` keeps §5's dialog out of the model.
3. Did it break anything else? No. Core additive (one new file), Unit additive (one new file); no existing file touched except the TODO contract. `validate` 0 fatal; `self-test` 391/391.

## Lenses

- `adversarial`: approve. Null tab/content arguments throw; double close is a no-op; empty pop returns null; unbounded stack cannot be fed by UI races (callers pass snapshots); no parsing, no IO, no hangs.
- `consistency`: approve. Naming and INPC patterns match the codebase; no new NoWarns; public API on a library (no CA1515); Markdown one line per item; no em dashes in new files.
- `integration`: approve. Only new downstream edges: TabView (§3) observes, buffer (D02 T01 §2) supplies content and caret, undo (§4) calls MarkClean, Save As (§5) answers SaveRequest; all seams documented at the call site.
- `record`: approve. Probe counts and rule derivations quoted; the unbounded-stack decision recorded with its cost of changing; one mutation probe (discard guard) red then green on revert; zero deferrals.
- `source-defect`: approve. Auto-naming from boundary probes (34/35/36/50, trim, first-line); empty-clean from the cleared-content probe; stack skips from two 11-close reopen probes against a confirmed Ctrl+Shift+T chord; tab-close prompt text recorded for §3; whitespace-only and saved-discard-symmetry marked as inferences.
- `design`: not owed (headless model, no rendered surface).

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; TabModel 24/24 plus full suites green locally both OSes; CI green both jobs on the candidate HEAD; `plan --check` green after the stamp-flow sync.
