# Review: D01 T01 §22 -- First-Line Titles for Untitled Tabs

Candidate: `47b571d` (UntitledTitleTests surface drives, two stock first-line crops plus README provenance, §22 plan corrections and ticks) plus `28e355f` (round 1: retried snapshot prompt-button lookup). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (live default at the surface): typing renames through `NotifyEdited` into `RefreshHeader`'s `DisplayName` binding; no product change needed. Driven: `TypingFirstLineRenamesTab` plus `FirstLineTrimsAndTruncates`.
- Item 2 (stock confirmation): one-shot stock drive, tab `PROBE22` from first line `PROBE22 ` and 40-char line titled `PROBE22-abcdefghijklmnopqrstuvwxyz0` (exactly 35); crops filed, no difference to record.
- Item 3 (commit): message matches `47b571d` verbatim.

## Self-review (three questions)

- Does it make sense: yes. The section asked for a surface re-proof plus a stock confirmation of §2's shipped rule; both landed, and the rule needed no product change.
- Is it logical: yes. The drives assert the rendered tab names (not the model), and the stock observations (trim, 35-cap) match `TabDisplayName` exactly.
- Did it break anything else: no. Additive test file plus two baseline crops plus plan text; no product code touched.

## Lenses (round 1, one finding, fixed)

- `adversarial` approve: no product change; the drives assert rendered names only.
- `consistency` approve: test file mirrors the suite's per-file helpers; capture names follow the stock convention; README provenance appended in the established human-judgment class.
- `integration` approve: titles flow through the existing `RefreshHeader` binding untouched; the §28 recon punctuation is corroborated, not consumed.
- `source-defect` approve: stock rule read from live UIA names (trim plus exact 35-cap), crops eyeballed, no difference to record.
- `design` approve: rename, back-to-Untitled, trim, and truncation all driven on the rendered tab. No user guide exists in the tree, N/A.
- `record` needs-attention then approve: finding (a) the gate failed `RestoreSaveFailureAbortsWithWorkPreserved` 1/104 on the lone unretried button lookup in `SnapshotTests`; fixed by polling in `28e355f`, green focused and in the final gate. Item ticks name their drives; deferrals: none.

## Gate quotes

Final full gate on `47b571d` plus `28e355f` (`build/gate-last.log`, exit 0, no strays):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 226, Skipped: 0, Total: 226 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 103, Skipped: 1, Total: 104 - UI.dll` (the skip is the pre-existing quarantine)

First gate failed `RestoreSaveFailureAbortsWithWorkPreserved` 1/104 (unretried button lookup, fixed in the round); the fixed test went green focused before the re-gate. `validate` 0 fatal, `self-test` 393/393, build 0 warnings.
