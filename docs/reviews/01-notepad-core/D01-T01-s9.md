# Review: D01 T01 §9 -- Multi-Window with Open-In Mode

Candidate: `d4416bb` plus review fix `172a857` (two commits). Stance: ship review. Rounds: 2.

## Evidence map

| Item | Satisfied by |
| ---- | ------------ |
| New-window command | `App.NewWindow` + Ctrl+Shift+N accelerator in `MainWindow`; drive `CtrlShiftNOpensSecondWindowAtCascade` (count 1 to 2 to 1, origins differ, untitled tab, no first-run in second). Menu invocation defers to D01 T02 §1 per the item's Corrected note |
| Opening-files modes | `ShellSettings.OpenIn` + `OpenInRouting.Route`; drives `OpenInDefaultsToNewTab`, `OpenInRoutesBothModesAndDefaultsUnknownToActiveWindow`. End-to-end honor stays at §8/T02 §1 per the item's Corrected note |
| Window independence | Per-window `TabModel` (structural) + merge-on-close; drives `WindowsKeepSeparateTabsDirtyAndClosedStacks` (model), `WindowsKeepIndependentTabs` (live: counts, content, clean title, close), `SecondWindowClosePreservesNewerExternalState` (merge, mutation-checked) |
| Tear-out/dock | Struck with probe evidence (two clean drag-out negatives; parity is no detach). Parity negative driven: `TabDragOutsideStripDetachesNothing` |

## Self-review

1. Does it make sense? Yes. The three live items have their drives; the checkpoint (new window, open-in modes per the Corrected scope, isolation) is the MultiWindow suites plus the probe record; the strike carries its evidence and a no-replacement reason. No scope beyond the section: the probe, captures, and routing all serve named items.
2. Is it logical? Yes, after the round-1 fix. First-window behavior (geometry restore, first-run) is preserved by the `firstWindow` flag; later windows open at the OS cascade; close merges geometry onto fresh state so stale snapshots cannot clobber newer flags; unknown open-in values stay in the active window. The open first-run dialog swallows Ctrl+Shift+N (standard ContentDialog modality, once per install, no action).
3. Did it break anything else? No. Full suites green on both OSes (Windows UI 22/22 including all §1/§3 tests, Unit 90/90, Protocol 35/35, Smoke 1/1; Linux neutral green); `MainWindow`'s new ctor parameter has one caller; single-window close/save behavior is unchanged (merge equals whole-save with no sibling writes).

Round-1 finding (fixed in `172a857`): every window saved its whole constructor-loaded settings snapshot on close, so a window opened before a sibling's newer save clobbered it (real victim: whatsnew.seen). Close and the first-run dismiss now merge onto freshly loaded state, driven mutation-checked (red unfixed, green fixed).

## Lenses

- `adversarial`: approve. (a) The stale-snapshot clobber is fixed and mutation-driven; residual interleaved closes can still lose one's geometry (last-wins, documented default for §6). (b) Dialog-up accelerators swallow (found in testing, no action). (c) The window registry removes on Closed (no leak). (d) Null/unknown open-in stays put. (e) Per-window hooks degrade independently as in §3.
- `consistency`: approve. Accelerator wiring mirrors `AddTabAccelerators`; test helpers mirror `TabBarTests` (Press with shift, PinTopmost drag, CloseAll over all windows); comments cite probes with dates; `openin.mode` documented in the seam the T02 §2 store adopts.
- `integration`: approve. Sole `MainWindow` caller updated; `OpenIn` default unchanged in value; geometry and first-run paths re-proven by the existing MainWindow drives; no-dialog in second window asserted; `Route` ready for §8 and the T02 §1 menu; last-wins geometry noted for §6.
- `source-defect`: approve. Ctrl+Shift+N, same-size cascade (three offsets), and untitled tab probed with captures; Opening-files strings and selection read from the combo plus capture; the tear-off strike cites two clean negatives against the 2023 announcement's silence. Fresh-install open-in default stays honestly unconfirmed.
- `design`: approve. The second window is the same `MainWindow` class (no new visuals, no golden change); cascade placement observed live (origins differ); the `windows/` Fidelity dir is filed with versioned captures. No user guide exists in `docs/` yet, so no guide update is owed.
- `record`: approve. Evidence below matches executed runs; `query deferred` shows none open; the struck item stays `[ ]` with its reason per convention; the Unit totals include untracked other-section test files present in the tree (additive; the committed subset is what the candidate covers).

## Gates (on the candidate)

- `dotnet test src/IntelligentNotepad.slnx` (Windows): Smoke 1/1, Unit 90/90, Protocol 35/35, UI 22/22.
- `dotnet test src/Notepad.Neutral.slnf` (Linux): Smoke 1/1, Unit 90/90, Protocol 35/35.
- `todo-graph.py self-test`: 393 cases, 0 failed. `validate`: 0 fatal, 0 warnings.
