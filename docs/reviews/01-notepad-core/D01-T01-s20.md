# Review: D01 T01 §20 -- Backup on Save

Candidate: `f32b447` (backup engine in FileSave, BackupTests plus FileSaveBackupTests, §5 leftover-test contract update). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (every save writes a timestamped sibling first): `WriteBackup` inside `SaveFile`/`SaveBytes` (pre-save destination bytes, atomic temp-move, `{file}.{UTC ms}.bak` with collision suffix). Driven: `SaveWritesSiblingWithPreSaveBytes` plus `SecondSaveKeepsPreSaveBytesInSibling` and `SaveBytesBacksUpToo`.
- Item 2 (retention caps the count): `RotateBackups` (cap 5, creation-time order, own-pattern only, best-effort deletes after the commit). Driven: `RotationEvictsOldestSeededSibling` plus `RetentionRotatesPastFive` and `ForeignBakFilesAreLeftAlone`.
- Item 3 (crashed save leaves the newest sibling intact): backup precedes the temp commit. Driven: `FailedSaveStillWritesSibling` (read-only redirect in the room) plus `FaultBeforeCommitLeavesSiblingAndOriginalIntact` (injected fault).
- Item 4 (commit): message matches `f32b447` verbatim.

## Self-review (three questions)

- Does it make sense: yes. Every file write keeps pre-save bytes in a capped, rotating sibling set; locked files back up ciphertext; first saves and Save-As-to-new-path skip (nothing to preserve).
- Is it logical: yes. Order is encode, backup, commit, rotate (encode failures write nothing; rotation never deletes before the replacement lands); backup failures map through the §5 redirect map so locked destinations still redirect; rotation failures never fail the save.
- Did it break anything else: the §5 leftover test now expects the sibling (contract change this section mandates, disclosed in the commit; no-temp-debris intent preserved). Locked-save redirect and Save-All inline-Save-As shapes verified unchanged (34/34 focused). Snapshot takes unaffected (new files, no destination to preserve).

## Lenses (single round, no findings)

- `adversarial` approve: sibling names derive from the destination's own directory (no user-directed traversal); foreign `.bak` files are never managed or deleted; backup failures abort loud with the original untouched; rotation is best-effort after the commit.
- `consistency` approve: engine lives in `FileSave` beside the writer it guards (atomic temp-move, shared redirect map, `Debug` + quiet-delete idioms); tests mirror the suite's unit and room conventions.
- `integration` approve: §5 behavior extended exactly as the section mandates (sibling first, redirect shapes preserved: locked-save redirect and Save-All inline-Save-As both green); `SaveBytes` (locks) covered; snapshot takes unaffected (new destinations); the one test update keeps its no-temp-debris intent and now pins the sibling.
- `source-defect` approve: new build, no baseline; no Notepad surface claimed.
- `design` approve: no new surface per the section (the file list is the surface); sibling naming, cap, rotation, and failure behavior all driven in the room. No user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives; the §5 test update and its rationale are disclosed in the SHIP commit body; deferrals: none.

Advisories (noted, not blocking): (a) no size guard: very large files duplicate whole bytes per save up to the cap (the section caps count only). (b) Same-millisecond siblings rotate in name order among themselves (all newest; only the count is contractual).

## Gate quotes

Final full gate on the candidate (`f32b447`), exit 0:

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 224, Skipped: 0, Total: 224 - Unit.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 93, Skipped: 1, Total: 94 - UI.dll (net10.0)` (the skip is the pre-existing `TabBarTests` ctxmenu-name-race quarantine, not §20)
