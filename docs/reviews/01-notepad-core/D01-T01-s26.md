# Review: D01 T01 §26 -- Protocol Handler

Candidate: `11816b6` (ProtocolAssociation plans plus parser, link mapping and verbs in LaunchArgs, registrar methods, headless branches, ProtocolHandlerTests plus unit coverage, §26 plan decisions and ticks). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (scheme registered): HKCU plans plus verbs mirror §8. Driven: `ProtocolVerbsCycleCleanly`.
- Item 2 (activation opens the path): links map to files in the parser, open through §4. Driven: `LinkOpensTheCarriedFile` plus `ShellLinkOpensTheFile`.
- Item 3 (malformed declines): unparsable links ignored into the bare path. Driven: `MalformedLinksOpenBareWindow`.
- Item 4 (commit): message matches `11816b6` verbatim.

## Self-review (three questions)

- Does it make sense: yes. Links carry paths into tabs, malformed links open nothing, registration cycles cleanly with foreign claims restored.
- Is it logical: yes. Only our scheme is special; well-formed-but-missing reuses the missing path; the shell's trailing slash is trimmed; combined verbs keep the §8 first-match behavior.
- Did it break anything else: LaunchArgs and the registrar extended additively; the §8 association tests and drives hold (proven by the gate).

## Lenses (single round, no findings)

- `adversarial` approve: traversal stays inside absolute paths; directories skip; garbage overflows nowhere (argv-capped, rooted-checked); unregister is idempotent with partial backups failing toward tree removal; registry failures exit 1, never crash.
- `consistency` approve: plans, backup shape, and verbs mirror the §8 registrar they extend; combined verbs keep the §8 first-match order.
- `integration` approve: record extension is single-sited; link mapping preserves §8 file-args behavior for everything else; secondaries file parsed paths so redirect needs no new machinery; the §8 association tests and drives hold unmodified.
- `source-defect` approve: no stock counterpart per Fidelity; our contract only.
- `design` approve: no new surface (the opened file is the shipped tab UI). No user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives; the shell's trailing slash is pinned in code and test.

## Gate quotes

Full gate on `11816b6` (`build/gate-last.log`, exit 0, no strays, first try):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 259, Skipped: 0, Total: 259 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 113, Skipped: 1, Total: 114 - UI.dll` (the skip is the pre-existing quarantine)

`validate` 0 fatal, `self-test` 393/393, build 0 warnings.
