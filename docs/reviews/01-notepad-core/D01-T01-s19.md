# Review: D01 T01 §19 -- Encrypted Notes

Candidate: `f0937c8` (NoteCrypto plus SaveBytes plus IsLocked, Lock/Unlock dialogs, Ctrl+Shift+L opener, unlock-on-open detour, re-lock on close-save, restore ghosting, EncryptedNotesTests plus NoteCryptoTests, encrypted-notes doc). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (algorithm/KDF/params stated in header and docs): magic plus JSON header line per lock; `docs/encrypted-notes.md` (parameters, layout, normative decrypt steps, test vector). Driven: `DocumentedVectorPinsCiphertext` (cross-checked against an independent Python implementation).
- Item 2 (lock writes encrypted form through §5): `NoteCrypto.Relock` (encode under the tab spec, lock, `FileSave.SaveBytes` atomic commit, tab marked locked and clean). Driven: `LockUnlockRoundTripsExactBytes` (ciphertext holds no plaintext) plus `RelockMarksTabLockedAndClean`.
- Item 3 (unlock restores exact bytes): `NoteCrypto.Unlock` plus `FileOpen.Detect` on the plaintext. Driven: `LockUnlockRestoresExactBytes` theory over five encodings plus the room unlock half.
- Item 4 (wrong password fails loud, nothing leaks): `WrongPasswordException` (tag mismatch, tampered ciphertext) and `InvalidDataException` (corrupt headers) before any render. Driven: `WrongPasswordFailsLoudWithNothingRendered` plus tamper/truncation fixtures.
- Item 5 (password never persists, memory-only key): key bytes zeroed after use; password lives only in the dialog; restore ghosts locked files. Driven: `PasswordNeverReachesDisk` (app-data scan) plus `RelockOnCloseSaveKeepsCiphertext` and `RestoreGhostsLockedFile`.
- Item 6 (commit): message matches `f0937c8` verbatim.

## Self-review (three questions)

- Does it make sense: yes. Lock on Ctrl+Shift+L, unlock on open with in-dialog retry, re-lock on every file write so saves never silently decrypt, restore ghosts locked files; copy-as and the menu trigger stay out per the recorded split.
- Is it logical: yes. Crypto is single-homed in `NoteCrypto` (the dialogs never derive); the header is AAD-bound so tampering fails like a wrong password with no oracle detail; `RelockOrThrow` single-homes the failure map for both dialog callbacks; `IsLocked` re-derives from bytes and is never persisted.
- Did it break anything else: no. Additive Core APIs plus one model flag; the open path detours only locked files (magic-gated); close-save plaintext flow untouched for unlocked tabs; the restore ghost is the established missing-file shape.

## Lenses (single round, no findings)

- `adversarial` approve: AES-256-GCM with fresh random salt/nonce per lock, 600k PBKDF2, AAD-bound header, one loud failure for wrong passwords and tampering alike (no oracle detail); keys zeroed after use, password dialog-scoped; lock writes only to the tab's own path (no user-directed traversal); restore ghosts locked files with no render and no prompt.
- `consistency` approve: dialogs mirror the tree's code-built pattern (injected seams, guard flag, status text, save-first state); `RelockOrThrow` single-homes the failure map for both callbacks; tests mirror the suite's per-file helpers.
- `integration` approve: §4 (`Detect` on the plaintext) and §5 (`SaveBytes` twin, `ApplySave`) reused, none forked; close-save keeps the plaintext path for unlocked tabs; the snapshot-restore Save branch fails safe per §16; Ctrl+Shift+L verified free in-tree; LockFile trigger recorded both sides with the menu owner.
- `source-defect` approve: new build, no baseline; no Notepad surface claimed.
- `design` approve: password, confirm, Lock button, status, save-first note, unlock prompt, and wrong-password error all driven in the room. No user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives; deferrals: none.

Advisories (noted, not blocking): (a) decrypted-buffer residues: snapshots, session buffers, and the crash checkpoint can hold plaintext of a locked tab; the section covers password/key only, so this ships as a filed hardening follow-up. (b) Lock confirm-mismatch disable undriven (matching path driven). (c) The unlock dialog's corrupt-file display undriven in the room (unit-covered). (d) Second occurrence of the §16 reshow wait flake (see below); watch item.

## Gate runs

First full gate on the candidate failed 1/91: `SnapshotTests.RestoreSaveWritesThenRestores` timed out on the post-Save reshow wait (line 205), the §16 round-2 flake site. No strays, no pollution signal (the §19 deltas on that path are a magic-prefix pre-read at launch and a false `IsLocked` check). Focused re-run green (7s); full re-gate on the unchanged candidate green. Verdict: transient load flake, second occurrence, watch item.

Final full gate on the candidate (`f0937c8`), exit 0:

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 219, Skipped: 0, Total: 219 - Unit.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 90, Skipped: 1, Total: 91 - UI.dll (net10.0)` (the skip is the pre-existing `TabBarTests` ctxmenu-name-race quarantine, not §19)
