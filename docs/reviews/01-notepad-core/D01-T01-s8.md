# Review: D01 T01 §8 -- File Association and Command-Line Open

Candidate: `511c99a` (ship) plus `973d50e` (review round 1 fix). Record under review: the §8 evidence notes plus `resources/baseline/stock/notepad-missing-offer-n11.2607.14.0-win25h2.png`, committed with the stamp. Tree record read at review time; the reviewed diff is the two commits, not the working tree.

## Evidence map (item to code)

- Item 1 (launch routing): `src/IntelligentNotepad/App.Launch.cs` (`RedirectAndExitAsync`, `OnAppRedirected`, `OpenFilesRoutedAsync`, `OpenIntoFirstWindowAsync`), `src/Notepad.Core/LaunchDrops.cs` (secondary-to-primary ferry), `src/IntelligentNotepad/App.xaml.cs` (single-instance gate in `OnLaunched`). Driven: `SingleFileLaunchOpensTab`, `MultiFileLaunchOpensTabsInOneWindow`, `SecondLaunchRedirectsFileIntoFirst`, `BareSecondLaunchOpensNewWindow`, `OpenInNewWindowModeOpensSecondWindow`, `FreshLaunchHoldsFilesInOneWindowInNewWindowMode`, `DoubleClickCommandOpensTab`.
- Item 2 (command-line opens): `src/IntelligentNotepad/MainWindow.Launch.cs` (`OpenFilesAsync`, `OfferCreateAsync`), `src/IntelligentNotepad/CreateFileDialog.cs`, `src/IntelligentNotepad/OpenFailureDialog.cs` (first render of the §4 strings). Driven: `MissingFileOfferNoCreatesNothing` (verbatim text asserted live), `MissingFileOfferYesBindsTabAndSaveCreates`, `MissingFileOfferEnterAcceptsAsYes` (round 1), `LockedFileReportsLocked`, `OverLimitFileRefusesTooLarge`, `LargeFileOpensResponsively`. Stock wording: three identical UIA observations plus the committed dialog crop; Yes-default observed (accent) and driven.
- Item 3 (assoc cycle): `src/Notepad.Core/FileAssociation.cs` (plans) plus `src/IntelligentNotepad/FileAssociationRegistrar.cs` (HKCU executor). Driven: `AssociationVerbsCycleCleanly` (dev-machine snapshot-diff), `DoubleClickCommandOpensTab`. Sandbox re-drive owed, recorded.
- Item 4 (nine-ext claim): `FileAssociation.ClaimedExtensions` (`.txt .log .ini .inf .ps1 .psd1 .psm1 .scp .wtx`, matches the item's AppxManifest list). Driven: `FileAssociationTests` plus the verbs cycle.
- Item 5 (jump list): `src/Notepad.Core/JumpListFeed.cs` (pins plus recents, fingerprint) plus `src/IntelligentNotepad/JumpListService.cs` (WinRT commit, unpackaged-spike-proven). Driven: `JumpListCommitsRecentsAndPinsFromStore` (taskbar read-back), `JumpListFeedTests`, `JumpListApiWorksUnpackaged`.
- Item 6 (drop entry): `WireFileDrops` (`MainWindow.Launch.cs:136-144`, called at startup `MainWindow.xaml.cs:46`), `OnFilesDropped` through the §4 `OpenFilesAsync` path. Wire record quoted in the item (`s8drop.log`: `wire contentnull=False`). Single/multi end-to-end owed on a capable host, recorded, with the multi-drop cap probe attached (round 1).
- Item 7 (/p, /pt): `src/Notepad.Core/LaunchArgs.cs` (parser, verbs, unknown-flag default) plus `src/IntelligentNotepad/PrintSeam.cs` (silent interim, `D01 T02 §5` binds). Driven: `PrintFlagsExitWithoutWindows` (/p and /pt) plus `LaunchArgsTests`.
- Item 8 (commit): message matches `511c99a` verbatim.

## Self-review (three questions)

- Does it make sense: yes. Every item resolves to shipped behavior or a declared, owned interim (print seam, drop end-to-end, Sandbox re-drive). No scope beyond the contract: the spare-tab consume, the drop-vs-typed missing split, and the directory skip are all inside the launch path the section owns.
- Is it logical: yes. Typed entries offer on missing while drops notice (§4 path in both); Yes binds without bytes until save; secondaries ferry absolute paths and the primary drains on Activated; stale drops self-heal at next launch (unopened files reopen, which is the crash-safe reading); fingerprint gates jump commits; unregister restores only own claims. TOCTOU between `File.Exists` and open degrades into the rendered failure result.
- Did it break anything else: no. `NewWindow` now returns the window (additive; existing callers ignore it). §6 restore runs before startup files route; §9 isolation untouched (spare consume is guarded by still-clean-Untitled plus more-tabs). Full gate green with the candidate: Smoke 1/1, Unit 174/174, Protocol 35/35, UI 54 passed plus 1 pre-existing quarantine of 55.

## Round 1 lenses

- `adversarial` approve (advisories): stale drops self-heal (noted above, no fix); concurrent register/unregister is last-wins on a dev-machine verb whose installer path D07 owns (no fix); multi-file drops have no cap (stock limit unprobed) -- recorded in item 6 as part of the owed capable-host drive rather than fixed blind. Illegal-char argv cannot crash: Core `GetFullPath` passes them through and the save-fails-loud path answers.
- `consistency` approve: ContentDialog plus AutomationId pattern matches `SavePromptDialog`; plans-as-data matches §6; fingerprint matches the golden pattern; `ShellSettings` keys extend the seam comment the T02 §2 store adopts. No second treatments.
- `integration` approve: `D01 T02 §1` trigger home and `D01 T02 §5` item 5 print binding are bidirectional with this section's notes; `OpenFailureDialog` reuse is named for the Open trigger; §6/§9 composition proven green in the same gate.
- `source-defect` needs-attention (fixed): the `CreateFileDialog` comment still said two observations with an unobservable default after the third observation plus crop proved three with an observed default; the directory-skip default lived in code but not in the plan. Both corrected (`973d50e` plus item 2 clause).
- `design` needs-attention (fixed): the Yes-default parity claim was set but undriven. `MissingFileOfferEnterAcceptsAsYes` added: click-foreground plus bounded Enter retry (bare global Enter is focus-fragile; three red attempts diagnosed to foreground acquisition, green on the fourth). Rendered dialogs are text-asserted live (offer verbatim, locked/over-limit via `OpenMessages` constants); pixel parity holds the §7 bar (text-level for dialogs, goldens for chrome). Controls accounted: offer Yes/No, failure OK (all driven); jump, assoc, drop, print have no in-app surface. User guide: none exists in the tree, N/A.
- `record` approve: every tick names its drives; owed work names owners (Sandbox host, capable host, T02 §5); the `s8drop.log` wire line is quoted into the item so the evidence survives its gitignored log; the filter-run citations were true at writing (full gate had proven each cited test) and re-proven after.

## Round 2 lenses (re-verification on `973d50e`)

- All six lenses re-run over the final candidate: `adversarial` approve, `consistency` approve, `integration` approve, `source-defect` approve, `design` approve, `record` approve. No new findings.

## Gate quotes (post-fix, §8 filter)

- `Unit.dll`: `Passed! - Failed: 0, Passed: 26, Skipped: 0, Total: 26, Duration: 80 ms - Unit.dll (net10.0)`
- `UI.dll`: `Passed! - Failed: 0, Passed: 20, Skipped: 0, Total: 20, Duration: 2 m 23 s - UI.dll (net10.0)`
- Filter exit 0 through the fixed runner (exit-code pipeline bug documented in the phase run). `TreatWarningsAsErrors` plus `AnalysisMode All` make green mean 0 warnings and analysis clean. `validate`: 0 fatal, 0 warnings.
