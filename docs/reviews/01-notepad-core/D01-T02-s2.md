# D01 T02 §2 review: Settings Store with One Writer

Candidate: `abae736` (single commit). Single-writer store (`SettingsStore` over the adopted shell keys plus font/wrap/statusbar/zoom), every production mutation through `Update` and every persist through atomic `WriteSnapshot`, v0-to-v1 lazy migration, corrupt/null/future reset with a once-at-startup notice dialog, `Changed` plus reentrancy guard, schema doc with consumers, six app files rewired off `Load`/`Save`, 10 unit tests plus 1 UI notice drive.

## Self-review

- Makes sense: the five items map to the diff exactly (store ownership plus the six-file rewire, atomic persist plus migration plus notice, live `Changed` plus cached-copy removal, schema doc, probed defaults). The `JumpListService` signature change (`ShellSettings` to `SettingsStore`) and the `MainWindow.settings` field removal are required contract changes, not scope. No second writer, no extra surface.
- Logical: `Update` reloads then mutates then stamps v1 then persists then notifies, rolling `Current` back to the file when the persist throws; reentrant `Update` from inside `Changed` throws rather than nests; readers re-read `Current` because each `Update` replaces it; corrupt-at-startup sets the notice flag while corrupt-at-`Update` self-heals silently per the recorded decision; the notice runs inside the first-window gate before the whatsnew dialog. The persist catch covers `IOException`/`UnauthorizedAccessException`, which is every failure the fixed production path can produce (all file APIs throw IO-family; argument failures are unreachable from the fixed path; the plain-DTO serialize cannot throw).
- Breakage: production holds zero `Save`/`Load` calls (all tests-only seeding and readback); the one `RefreshIfChanged` caller moved to the new signature; tab pin, recents, whatsnew, geometry, and jumplist behaviors are unchanged calls inside `Update`. Full gate green with exactly the two expected count deltas (+10 Unit, +1 UI) and no new skips.

## Lenses round 1 (candidate `abae736`, final)

- `adversarial` approve: malformed, null-document, and future-version files all reset to defaults with the notice flag (unit-pinned each way); missing and unreadable files fall back silently per the inherited contract; null mutates and null snapshots throw `ArgumentNullException`; reentrancy throws; persist failure rolls back and rethrows; unknown keys round-trip instead of dropping. Advisory (not blocking): the persist catch scope is complete only while the path stays fixed; re-review it if the path ever becomes user-supplied.
- `consistency` approve: naming follows store conventions (`Current`, `Changed`, `Update`, `WriteSnapshot`, `WasResetFromCorrupt`); every code comment cites the section; the schema doc matches the code key for key including default sources; seeding tests keep working through the delegating `Save`.
- `integration` approve: all six rewired files compile into the green gate; the jumplist caller, pin toggles, recents, whatsnew latch, geometry save, and launch routing hold; later consumers (settings page, status bar, editor surface) are named in the doc and subscribe on landing.
- `source-defect` approve: fresh-install defaults read off the recorded stock 11.2607.14.0 probes (font dropdowns, wrap switch, view-menu check, system-theme radio, opening/starts selections) with the operator-profile freshness caveat and the zoom-100 recorded default both stated with costs; the notice dialog is declared honest-non-parity (stock has none).
- `design` approve: the one new surface renders and drives end to end (found by AutomationId, wording asserted, OK-dismissed, whatsnew following, defaults read back); no stock baseline exists for it by declared decision; no user guide exists in the tree yet (same precedent as the menu section).
- `record` approve: every tick maps to drives named in the commit body; no deferrals open or resolved; the gate evidence ran on the candidate's exact code content (the commit adds only the six tick marks); the mid-run operator stop and resume are run-logged with the re-armed timer id.

## Gate quotes

Full gate on `abae736` (`build/gate-full-s2.log`, exit 0, no strays):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 285, Skipped: 0, Total: 285 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 145, Skipped: 1, Total: 146 - UI.dll` (the skip is the pre-existing quarantine)

Section checkpoint on `abae736`: `Passed! - Failed: 0, Passed: 10, Skipped: 0, Total: 10 - Unit.dll` plus `Passed UI.SettingsStoreNoticeTests.CorruptSettingsShowsNoticeAndRestoresDefaults` (`--filter SettingsStore`, exit 0, no strays).

`validate` 0 fatal, `self-test` 393/393, build 0 warnings 0 analyzer errors.
