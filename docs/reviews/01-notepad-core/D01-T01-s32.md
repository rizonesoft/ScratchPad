# Review: D01 T01 §32 -- Quarantine the AppIcon and Launch CI Flakes

## Evidence map

Candidate `869c49a` carries all 4 items: item 1, QUARANTINED Skip on `WindowChromeIconMatchesAsset` (`tests/UI/AppIconTests.cs:35`, id `chrome-icon-uia-timeout`) plus its quarantine-list row (UIA-timeout signature, both runs quoted, owner D01 T01 §11, due 2026-09-24); item 2, QUARANTINED Skip on `MissingFileOfferYesBindsTabAndSaveCreates` (`tests/UI/LaunchTests.cs:193`, first id `missing-offer-dialog-null`) plus its row (both runs quoted, due 2026-09-24); item 3, run 35247431391 success both jobs with UI.dll 167 passed, 3 skipped, 0 failed; item 4, the commit itself. The commit also carries the Phase-1 run-file repair notes, per the run-file-rides precedent (`04c766c`, `7e0283d`, D01-T02-s13 record). **Corrected in review (round 1):** the candidate named the offer dialog (id, row signature, row owner §8) and stamped §32 in both owner slots; the stack proves the Ctrl+W save prompt, owned by §7. The fix commit carries the corrected stamps (`S11`, `S7`), id (`save-prompt-dialog-null`), row, SOURCE call site, Depends On (§7, §11), and item Dones.

## Self-review

Sense: exactly the §5 quarantine procedure (prove, Skip with stamp, list rows), no test logic touched, no scope beyond the two flakes. Logic: Skip strings match the existing `ctxmenu-name-race` stamp shape with owning-section slots; signature ids are unique in the doc; rows carry the stable failure shape without timestamps; owners are the failing surfaces' owning sections (§11 icon attach, §7 dirty prompt); dues are +7d per the window rule. Breakage: the two tests stay compiled and run nowhere; the suite is green without them (167/3/0); no other file touched. Self-review missed the wrong dialog and the owner-slot rule; the panel caught both below.

## Failure excerpts (durable proof text for the quarantine rows)

Run 35234746568 attempt 1, commit `c07b2c6` (the `--failed` rerun of the same commit went green):

`UI.AppIconTests.WindowChromeIconMatchesAsset` (10 s): `System.TimeoutException : UIA Timeout` with inner `System.Runtime.InteropServices.COMException : Operation timed out. (0x80131505)`, at `FlaUI.UIA3.UIA3Automation.FromHandle` via `UI.AppIconTests.WindowChromeIconMatchesAsset() in tests/UI/AppIconTests.cs:line 40`.

`UI.LaunchTests.MissingFileOfferYesBindsTabAndSaveCreates` (13 s): `Assert.NotNull() Failure: Value is null`, at `UI.LaunchTests.WaitForDialog(Window window, String automationId) in tests/UI/LaunchTests.cs:line 727` via `MissingFileOfferYesBindsTabAndSaveCreates() in tests/UI/LaunchTests.cs:line 216`, which is `var prompt = WaitForDialog(window, "SavePromptDialog");` (the `CreateFileDialog` offer at line 207 passed).

## Opus panel (round 1, candidate `869c49a`)

**adversarial: needs-attention**
- The Launch row's signature named the missing-file offer dialog; the stack proves the Ctrl+W save prompt (`SavePromptDialog`, line 216). Fixed: row, signature id (`save-prompt-dialog-null`), owner (§7), SOURCE call site, Depends On, and item-2 Done all name the save prompt.
- The signature id encoded the wrong surface. Fixed with the row.
- Both rows pointed proof at `D00-T02-s7.md`, which carries no failure text. Fixed: rows point at this review's excerpts above.

**consistency: needs-attention**
- Both stamps put the quarantining section (§32) in the procedure's `<owner>` slot while the rows named §11/§8. Fixed: slots now name the owning sections (`D01-T01-S11`, `D01-T01-S7`), matching the `S3` precedent shape; the Launch owner is §7 per the save-prompt finding.

**integration: advisory**
- `build/todo-progress.json` was stale at review time (gitignored derived artifact). Fixed: `plan --sync` re-ran before the fix commit. Items 3-4 verified exactly (run 35247431391, both jobs success, 167/3/0).

**record: needs-attention**
- Item-2 Done, SOURCE, evidence map, and self-review certified or repeated the wrong dialog. Fixed: all name the save prompt with the call site; SOURCE carries a `Corrected` marker.

Fix record: all round-1 findings fixed in the round-2 candidate (`cd0e338`). Panel round 2 ran against that candidate.

## Opus panel (round 2, candidate `cd0e338`, FINAL)

**adversarial: advisory**
- Round-1 fixes confirmed (save-prompt signature/id/owner match the stack; UIA timeout anchored at attach).
- Advisory, fixed in the stamp commit: both rows cite this review file, which was untracked at review time. It ships with the stamp, so the proof path resolves from the stamped tree.

**consistency: advisory**
- Round-1 fix confirmed (owner slots `S11`/`S7` match rows and the `S3` precedent).
- Advisory, fixed in the stamp commit: nothing recorded that §8's offer-Yes-through-save path is uncovered for the window. The Launch row now carries the coverage note with the surviving siblings named.

**integration: approve**

**record: advisory**
- Advisory, fixed in the stamp commit: item 3 quoted run 35247431391, which predates the corrected Skip strings. It now quotes run 35249487881 attempt 3 (167/3/0, both jobs success).
- Advisory, fixed in the stamp commit: the evidence map contradicted itself on the round-1 owner slots. It now states the candidate truthfully (S32 stamped, S8 in the row).

Fix record: all round-2 advisories fixed in the stamp commit (findings file ships, coverage note, HEAD run re-quote, evidence-map wording). Panel rounds used: 2 of 5 (round 1 needs-attention all fixed and re-proven; round 2 advisory-only; zero needs-attention at close).

## Gates re-run

Ship CI run 35249487881 attempt 3 on `cd0e338`: success both jobs, UI.dll 167 passed, 3 skipped, 0 failed. `validate` 0 fatal 0 warnings (re-run, quoted at stamp). The stamp push re-runs CI on the final tree. Leftover: none (the §16 trio ships in its own section, unblocked by this stamp).
