# Review: D01 T01 §7 -- Dirty Prompts and Crash Recovery

Candidate: `0a40085` plus review fix `284e49a` (two commits). Stance: ship review. Rounds: 1.

## Evidence map

| Item | Satisfied by |
| ---- | ------------ |
| Tab-close prompt matrix | `TabBar.TrySaveAndClose` over the §3 `SavePromptDialog` (Save writes pathed tabs in place and closes, keeps untitled tabs dirty, keeps on redirect/failure; Don't-save discards; Cancel aborts); `PromptName` full-path/untitled naming; drives `SaveOnPathedDirtyTabWritesBytesAndCloses` (non-ASCII bytes plus closed, full-path message), `DontSaveDiscardsAndCloses`, `CancelKeepsTabExactly`, `UntitledSaveKeepsTabDirtyWithNothingWritten` |
| Silent window close | Already silent; stale OnClosed comment corrected; drive `WindowCloseWithDirtyTabsIsSilentAndRestores` (zero modals across 3 s of close, both buffers restored dirty, file bytes intact) |
| Cancel aborts one close | Pre-existing `return false`; drive `CancelKeepsTabExactly` (text, name, count, bytes unchanged; re-prompt proves still dirty) |
| Continuous checkpoint, silent restore | `CrashCheckpoint` policy, `TabBar.TabsEdited`, App 2 s debounce, `CheckpointAll` into session.json; drive `KillRecoversBuffersSilentlyWithFilesUntouched` (checkpoint gated on JSON markers, kill, relaunch to both buffers with zero dialogs) plus `FreshTypingDeletesStaleSession` and 5 policy unit tests |
| Recovery never writes files | By construction (restore loads dirty buffers; bytes move only via §5 saves); drive asserts file bytes identical before the kill and after the relaunch |

## Self-review

1. Does it make sense? Yes. Every item has its drives; the checkpoint (tab answers incl. non-ASCII Save bytes, silent window close with restore, killed-process silent recovery, bytes identical across the kill) is the DirtyPrompt suite at 7/7 plus the CrashCheckpoint suite at 5/5; Save-on-untitled and failure reporting defer honestly to the named D01 T02 §1 with keep-open interim defaults that lose nothing. No scope beyond the section: the two prompt captures, the policy type, and the debounce all serve named items.
2. Is it logical? Yes, after one review round. SaveSuccess applies the save before closing (reopen reloads bytes); redirect/failure/untitled keep the tab open and return true so batch closes continue past non-vetoes (recorded); recents record through the shared model-remove path; checkpoint writes are atomic temp-plus-move (a mid-write kill leaves the previous checkpoint or nothing); fresh mode deletes stale instead of writing; trivial live state deletes like quits; the timer is a UI-thread one-shot restarted per edit (no reentrancy, no cross-thread timer).
3. Did it break anything else? No. Full suites green on the candidate (Smoke 1/1, Unit 148/148, Protocol 35/35, UI 37/38 (37 passed plus the 1 pre-existing quarantine)); §3/§5/§6/§9 tests unmodified and green; `TabBar.TextChanged` behavior unchanged (event added alongside); `ShellSettings` untouched; prompt wording order and default button unchanged from §3.

Round-1 findings (fixed in `284e49a`): (a) the fresh-mode stale-delete branch executed with no drive; added `FreshTypingDeletesStaleSession`. (b) The Save drive used ASCII-only bytes, which would hide a swapped encoding; the drive now saves accented content. The `SaveSpec` argument order was verified against the record definition (no bug).

Non-finding (recorded, not quarantined): one full-suite run on `284e49a` failed two §9 MultiWindowTests (`CtrlShiftNOpensSecondWindowAtCascade`, `SecondWindowClosePreservesNewerExternalState`) with signatures lost to quiet logging; both pass isolated (4/4) and the full suite went green on re-run with no code change. Regression is mechanically implausible (neither test types, so the checkpoint timer never arms; all §7 tests self-seed and delete). Treated as live-box interference on a 4-minute run with the operator present; no quarantine filed without a characterizable signature. Recurrence with a signature should quarantine per procedure.

## Lenses

- `adversarial`: approve. (a) Kill mid-checkpoint-write leaves previous-or-nothing (atomic move), both restore clean. (b) Hostile session content rides the §6-hardened loader. (c) Save on a tab whose file vanished routes through SaveFile and keeps open on failure (no throw path: redirect/failed/default all return true). (d) Prompt on a torn-down tree returns Cancel via the §3 XamlRoot guard. (e) Empty untitled tabs never prompt (clean by construction), matching stock. (f) Zero-tab Ctrl+W keeps the §3 null-guard behavior.
- `consistency`: approve. `CrashCheckpoint` mirrors `WhenStartsRouting` (pure static, named constants); drives mirror SessionRestoreTests (Retry waits, pattern driving, filtered cleanup catches, per-test seeding with session delete); comments cite probes with capture ids; the 2 s debounce and fresh-delete rule are recorded in the section, not just the code.
- `integration`: approve. `RequestCloseAsync` callers (single, CloseOthers, CloseRight) keep their contracts (false still aborts batches; keep-open true continues, recorded); recents record through model removal for Save-closes too; App owns the timer and the multi-window merge (no per-window checkpoint skew); §8 launch rules and T02 §1 dialogs consume the recorded seams untouched; T02 §1 item 5 already names the Save As hookup this section defers.
- `source-defect`: approve. Prompt title, wording, order, default, and straight apostrophe read verbatim from UIA dumps plus two filed captures; full-path naming for saved tabs and {tab}.txt for untitled each double-sourced (capture plus independent probe run); window-close silence proven for one and two dirty tabs (zero dialogs each, singleton re-verified); kill recovery proven silent with buffers and bytes (zero dialogs, both buffers, file identical); the untitled-Save interim is declared non-parity pending T02 §1, not silent reinterpretation.
- `design`: approve (no new surface). §7 renders nothing new: the §3 dialog is reused (its rendered message text is asserted through UIA in four drives, including the full path), silence and restore are verified by absence plus content, and the checkpoint is invisible. No user guide exists in `docs/` yet, so no guide update is owed.
- `record`: approve. Evidence below matches executed runs; all 6 items ticked with named drives; the Unit total includes untracked other-section test files present in the tree (additive); the one red full-suite run is quoted above with its re-run verdict and no-quarantine rationale; Duration is wall clock including the probe campaign.

## Gates (on the candidate)

- `dotnet test src/IntelligentNotepad.slnx` (Windows): Smoke 1/1, Unit 148/148, Protocol 35/35, UI 37 passed plus 1 pre-existing quarantine (`ctxmenu-name-race`) of 38.
- `dotnet test tests/UI --filter DirtyPrompt`: 7/7. `dotnet test tests/Unit --filter CrashCheckpoint`: 5/5.
- `todo-graph.py self-test`: 393 cases, 0 failed. `validate`: 0 fatal, 0 warning

## Re-verification 2026-09-15 (stamp run)

The stamp landed a session after the review, so the gates were re-run on the current tree (candidate plus D01 T01 §27 and comment-only review fixes; §7 code paths unchanged): `DirtyPrompt` 7/7 in 53 s, `CrashCheckpoint` 5/5, full slnx Smoke 1/1, Unit 148/148, Protocol 35/35, UI 37 plus the `ctxmenu-name-race` quarantine of 38, 0 warnings. UI totals above corrected from 35/36 to 37/38 for the two §27 ChromeTests; all other review substance re-verified unchanged. Added reachability proof: the `TrySaveAndClose` empty-text fallback cannot fire on a dirty tab (dirtiness requires box edits, restore pre-fills every box, and ghost tabs are clean until activated), and the `PromptName` null-forgiving operator is safe (`IsUntitled` is defined as `FilePath is null`).
