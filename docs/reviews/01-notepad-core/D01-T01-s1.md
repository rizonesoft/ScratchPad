# Review: D01 T01 §1 -- Main Window Shell with Menu Bar Host

Candidate: `8d7c266` (shell) plus `93bf74f` (untracked test follow-up) plus `79f6786` (capture determinism fix). Two fix cycles, both evidence-driven; lenses clean.

## Evidence map

| Item | Change that satisfies it |
| ---- | ------------------------ |
| 1. Four regions | `MainWindow.xaml` hosts MenuRegion (MenuBar), TabRegion, EditorRegion, StatusRegion as UIA-exposed hosts in Notepad layout; `ShellRegionsExistAndTitleFollowsConvention` finds all four plus the megaphone by AutomationId |
| 2. Title convention | `Notepad.Core/WindowTitle.cs` composes `{[*]name} - {app}` from 4 live Notepad states; 5 theory cases green on Linux; live window title asserted in every UI drive |
| 3. Geometry restore | Seam-persisted, atomically written; `GeometryRestoresAcrossLaunches` seeds 120,130 800x600, relaunches into it within ±4px physical, and proves the save-back |
| 4. UI drive | `tests/UI/MainWindowTests.cs`, 7 tests; `--filter MainWindow` green locally and in CI |
| 5. Mica plus themes | MicaBackdrop (stub-carried) with rounded-corner Win11 frame; `ThemesRenderWithMica` drives light/dark/system with polled brightness proof; dark body sampled RGB(27,34,34) vs stock (39,39,39); light stock set captured for eyeball |
| 6. What's New | Megaphone entry plus `WhatsNewDialog` with the 4 recorded blurbs verbatim; first-run shown once via Loaded (CI-safe, no foreground needed), revisitable; `FirstRunShowsWhatsNewAndMegaphoneReopensIt` drives show, dismiss, persist, reopen |
| 7. Commit | `8d7c266`, subject matches the item exactly |

Runs: `34812982991` red on UI 8/9 (first-run dialog in the golden capture on the fresh runner, 3.537% diff, artifact-proven), fixed by `79f6786` (UiCapture seeds the seam); `34813967613` green both jobs (Smoke 1/1, Unit 7/7, Protocol 8/8, UI 9/9). Local: UI 9/9 repeatedly, full slnx green twice, slnf green on Linux.

## Self-review

1. Does it make sense? Yes. Every item has its change; the plan corrections (app/test/capture paths, seam persistence) were recorded in the file before building. Empty Grid regions were UIA-invisible (dump-proven), so hosts became ContentControls with Name plus Transparent background: zero pixel change (golden still green), full UIA exposure.
2. Is it logical? Yes. First-run keys off Loaded so background launches still show it; geometry round-trips physical pixels with DPI-aware reads; theme resolves through the seam with system default; the dialog dismiss persists seen asynchronously with the test polling for the effect; captures pin dark Mica plus seen for determinism on any machine.
3. Did it break anything else? No, with accounted blast radius: UISmoke removed (stub title gone) with a soak-log removal row; golden repointed to the shell with refresh-procedure eyeball; the 10px shift probe moved to the stock capture (comparer property, independent of golden sparseness); UiCapture's DPI/pin helpers extracted to UiDpi with golden tests re-proving behavior. `validate` 0 fatal; `self-test` 391/391.

## Lenses

- `adversarial`: approve. Corrupt seam resets to defaults; negative geometry clamps; garbage theme falls back to system; unknown methods n/a; no hangs (every UI wait bounded 5-20s, kills after 10s).
- `consistency`: approve. Test naming PascalCase; no new NoWarns; pins untouched; lockfiles record only the Core project edge; Markdown one line per item; no em dashes in new files.
- `integration`: approve. App references Core (one direction); UI tests arrange through the public seam; D01 T02 §2 adopts the documented keys; D02 T01 §1 later fills EditorRegion behind its contract; the megaphone id and dialog contract are stable for future drivers.
- `record`: approve. Run IDs, pixel samples, and the six-run probe sequence quoted; the staging-miss follow-up and the red-run fix recorded as candidates; one unidentified local transient (1 in 8 UI runs, green on rerun, name not captured) recorded in the phase log for the soak to watch.
- `source-defect`: approve. Title rule from 4 live states (clean-saved symmetric inference declared); dialog text from a live UIA dump; Mica colors sampled both sides; light stock set captured from Notepad 11.2607.14.0; every other stock-main control routed to its owning section with none unaccounted.
- `design`: approve. Rendered shell eyeballed dark (regions, caption, megaphone) and light (Mica correct) against the stock captures; dialog card eyeballed with all 4 blurbs verbatim, accent button, and Close.

Advisories (not blocking): stale geometry on a disconnected monitor opens off-screen with no clamp; D01 T01 §9 (multi-window restore) owns placement edges and takes this as a watch item. The `Window.AppWindow` member access keeps the `Microsoft.UI.Windowing` using the compiler is satisfied with.

## Gates re-run

`validate` 0 fatal; `self-test` 391/391; full slnx green locally (Smoke 1/1, Unit 7/7, Protocol 8/8, UI 9/9) and slnf green on Linux; CI green both jobs on the candidate HEAD; `plan --check` green after the stamp-flow sync.
