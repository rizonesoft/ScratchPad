# Review: D01 T01 §27 -- Tab-Strip Chrome Parity Repair

Candidate: `1b19937` plus review fix `69d082f` (two commits). Stance: ship review. Rounds: 1.

## Evidence map

| Item | Satisfied by |
| ---- | ------------ |
| Shell rows are 43/32 DIP with the editor step at 75 | `MainWindow.xaml` rows plus the measured-geometry comment; captured step at y114 (150%) equals 76 DIP, 1px rounding over stock's 75 |
| A full 12-tab strip parks the add button left of minimize | `SetCaptionInset` narrows the TabView by the RightInset margin so `ShrinkTabsToFit` and `TabStripContentRight` read narrowed widths with no other math changes; drive `FullStripParksAddButtonLeftOfCaption` (add right at most minimize left; 94px clearance, pre-fix tucked under maximize) |
| The zero-tab strip centers the add button | `ApplyAddButtonPlacement` zero path (Center alignment plus measured re-margin, idempotent sets); drive `ZeroTabsCentersAddButton` (within 8px; add center 33 versus strip center 32.25) |
| The dirty dot is a 6-DIP ellipse at stock gray keeping the "•" accessible name | 6x6 `Ellipse`, primary brush at 55% opacity, `SetName` bullet; measured 9px peak 156 versus stock 10-11px/154; §3 drives `DirtyClosePromptsAndCancelKeepsTheTab` plus `InitialTabRendersFromModel` green on the kept name |
| Last-tab close after move/resize never crashes 0xC000027B | Template touches deferred one dispatch with `InvalidOperationException`/`COMException` catches on the walk and the transform; 3/3 native crashes pre-fix, 5/5 alive post-fix |
| The shell golden is refreshed per procedure and the full gate is green | `main-window.png` recaptured (chrome shift plus sub-threshold Mica tint, inspected in this review: tab row with the add left of the caption, editor, status bar); Smoke 1/1, Unit 148/148, Protocol 35/35, UI 37 passed plus the 1 pre-existing quarantine of 38, 0 warnings |
| Commit | `1b19937` (`notepad-core: repair tab-strip chrome parity (D01 T01 §27)`) |

## Self-review

1. Does it make sense? Yes. Every item has its drives; the checkpoint (add-vs-caption, zero-tab centering, dot size/color, crash survival, all measured in the room, §3 dot contract plus the full suite green) is the ChromeTests suite at 2/2 plus the named §3 drives plus the full gate. No scope beyond the section: the inset, the placement sync, the dot, the band rect, and the golden all serve named items.
2. Is it logical? Yes. The inset narrows the TabView itself so shrink and content-right need no new math; the zero path records the original margin once and restores it when tabs return; the band rect is disjoint from content by min-top construction with a 0.5-DIP change gate on reissue; the main rect math is unchanged in shape; failure paths (teardown, recycle, mid-transition) return early and the next layout pass recomputes.
3. Did it break anything else? No. Full suites green on the candidate plus fix (Smoke 1/1, Unit 148/148, Protocol 35/35, UI 37 plus 1 pre-existing quarantine of 38, 0 warnings); §3 strip tests unmodified and green; the drag rects are additive (old rect untouched, band rect appended); the `headers` tuple widening is internal; the golden holds `FreshCaptureMatchesGolden` in tolerance.

Round-1 findings (fixed in `69d082f`): (a) the two "Right edge" sentences described `TabStripContentRight` but sat above `SetCaptionInset`; moved to their owner. (b) The XAML comment claimed 43/32 "lands it exactly" while the section measures 76 DIP against stock's 75; softened to the measured numbers.

Non-findings (verified, not actioned): (a) the reviewer's drag-band fear (band rect over tab tops deadening clicks) is disproven twice over: structurally, `TabStripContentTop` takes the minimum so the band cannot cover content, and live, a probe clicked tab2 at top+3px and the selection changed (control click at bottom-6px also selected), proving XAML hit-testing takes precedence over drag rects (probe `build/probe-tabtop.ps1`, ignored scratch; tabs observed bottom-hugged with the add below the tab tops). (b) The `Tabs.FindName("AddButton")` call sites versus the "FindName cannot see the add button" comment tension is pre-existing §3 code, unchanged by §27 and empirically green; noted only. (c) `TabStripContentRight` measures UserControl-relative while `TabStripContentTop` measures TabView-relative, but the frames coincide (a bare TabView fills the UserControl with zero padding), so no correction is owed.

## Lenses

- `adversarial`: approve. (a) Mid-transition native 0xC000027B is stowed as `COMException` and caught on both the walk and the transform, with retry on the next pass. (b) Teardown and container recycle surface `InvalidOperationException`, caught the same way. (c) `TabItems` mutation mid-enumeration throws the same caught type, so the pass degrades to recompute. (d) The inset clamps at zero with a 0.5-DIP idempotence threshold. (e) Both drag rects are content-disjoint by min/max construction, and the live click probe confirms tab tops still select. (f) The zero path guards on zero heights and a missing button; the walk is null-safe.
- `consistency`: approve. Drives mirror the suite's Retry/seed/filtered-cleanup shape; the 0.5-DIP thresholds match the pre-existing shrink style; comments cite probes with measured numbers; the band-rect change gate mirrors the pre-existing drag-rect change detection.
- `integration`: approve. §1 and §3 back-pointers are in place for the re-measured rows and geometry; T02 §1's 32-DIP MenuBar constraint is recorded in the XAML comment; the rect array extends the pre-existing single rect without changing its math; new API is internal; no caller outside `MainWindow`/`TabBar` is touched.
- `source-defect`: approve. Stock numbers are double-sourced (canonical capture plus live probes); the 76-versus-75 rounding is recorded in the box rather than hidden; the zero-tab centered target is declared by-construction (stock has no zero-tab state); the 2.5px tab text stays recorded non-parity; the golden diff (chrome shift plus sub-threshold Mica tint) was re-inspected in this review.
- `design`: approve (no new surface). §27 renders nothing new: rows, add placement, dot, and band are all re-measured existing chrome, verified by capture and UIA geometry. No user guide exists in `docs/` yet, so no guide update is owed.
- `record`: approve. Evidence below matches executed runs; all 7 items ticked with named drives; the golden was visually re-verified in this review; the full gate was re-run independently on the candidate plus fix (numbers below); the tab-top click evidence is a live probe run, quoted with its control.

## Gates (on the candidate plus fix)

- `dotnet test src/IntelligentNotepad.slnx` (Windows): Smoke 1/1, Unit 148/148, Protocol 35/35, UI 37 passed plus 1 pre-existing quarantine (`ctxmenu-name-race`) of 38, 0 warnings, exit 0 (UI leg re-run logged to `build/scratch-s27-review-ui.log`, 4 m 5 s).
- `dotnet build src/IntelligentNotepad/IntelligentNotepad.csproj` after the comment fixes: 0 warnings, 0 errors.
- `todo-graph.py self-test`: 393 cases, 0 failed. `validate`: 0 fatal, 0 warnings.
- `build/probe-tabtop.ps1` (ignored scratch): two tabs bottom-hugged (tops 12px below strip top, bottoms at strip height), add below the tab tops; top+3px click on tab2 selected it, bottom-6px control click kept it selected.
