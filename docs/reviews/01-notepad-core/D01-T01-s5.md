# Review: D01 T01 §5 -- File Save and Save As

Candidate: `911ff19` plus review fix `4615807` (two commits). Stance: ship review. Rounds: 2.

## Evidence map

| Item | Satisfied by |
| ---- | ------------ |
| Atomic save | `FileSave.SaveFile` (same-dir temp plus move); drives `SaveWritesAtomicallyViaTempAndRename`, `FaultBeforeCommitKeepsOldBytesIntact` (injected crash throws, old bytes intact) |
| Save As honors choice | `FileSave.Encode` + `SaveDialogDefaults`; drives `SaveAsMatrixHonorsEveryOfferedCombination` (5 encodings times 3 EOLs through detect), `BomFlagRoundTripsIndependentOfName` |
| Preserve round-trips | Encode over detected params plus declared BOM divergence; drive `OpenSaveRoundTripsAcrossDetectionMatrix` (all 12 §4 fixtures byte-identical) plus `UnencodableCharactersFailLoudlyInsteadOfCorrupting` |
| Failure without silent loss | `RedirectsToSaveAs` plus `SaveFailed`; drives `LockedSaveRedirectsToSaveAsAndKeepsBytesIntact` and `ReadOnlyAttributeSaveRedirects` (Windows-only) plus `RedirectMapCoversDestinationsAndPassesThroughUnknown` plus `UnmappedFailureReportsOsMessage` |
| Exact offer list | `SaveDialogDefaults.OfferedEncodings` (verbatim) plus prefill; drive `SaveDialogPrefillAppendsTxtToDisplayName` plus the item-2 matrix |
| New-file defaults | `Tab` defaults plus `SaveDialogDefaults.DefaultEncoding`; drive `NewTabDefaultsToUtf8WithoutBomAndCrlf` |
| Save All order/cancel | `FileSave.SaveAll` (strict tab order, cancel continues); drives `SaveAllWalksTabOrderAndContinuesPastCancel` (call-order log), `SaveAllContinuesPastFailure`, `SaveAllRedirectPromptsInlineSaveAs` (Windows-only) |

## Self-review

1. Does it make sense? Yes. Every item has its drives; the checkpoint (byte-identical matrix, fault-injection old-or-new, honored combinations) is the FileSave suite at 29/29; the rendered Save As dialog and triggers defer to the named D01 T02 §1 with the spec shipped here. No scope beyond the section.
2. Is it logical? Yes, after the round-1 fix. EOL normalization cannot double-convert (CRLF, CR, then LF collapsing to LF first); BOM bytes come from an explicit table; the two save catch filters share `RedirectsToSaveAs` as their single source; `ApplySave` runs only on success so failures and redirects never touch the model; Save All cannot regress (redirects re-prompt once per tab, then record).
3. Did it break anything else? No. Full suites green on both OSes (Windows Unit 119/119, UI 22/22, Protocol 35/35, Smoke 1/1; Linux Unit 116 plus 3 OS-skipped, rest green); `TabModel` changes are additive; `FileOpen` untouched; §4 tests unmodified and green.

Round-1 finding (fixed in `4615807`): the 1252 encoder silently wrote `?` for unencodable characters. All encoders are strict now; `EncoderFallbackException` surfaces as `SaveFailed` with the detail, bytes intact, dirty kept. Driven single and Save All (ANSI emoji plus lone surrogate).

## Lenses

- `adversarial`: approve. (a) Silent-`?` corruption fixed and driven. (b) The `DirectoryNotFound`-is-`IOException` misroute was caught in testing; the opt-out list plus mapping test pins it. (c) Crash points yield old bytes with a documented orphan policy. (d) GUID temp names in the target directory. (e) Saving over a directory redirects to Save As (functionally right). Advisory: `SaveFile` is synchronous (whole-buffer write, typically milliseconds); no item demands async.
- `consistency`: approve. Result records, message holder, and dialog spec mirror the §4 shapes; the bool redirect map (single outcome, no reason) differs from `MapFailure`'s enum deliberately, with the Windows-conflation rationale recorded; tests mirror `FileOpenTests` (temp dirs, fixture helper, OS gating via the `HookFact`-pattern attribute).
- `integration`: approve. `HasBom` defaults false (existing tabs unaffected); `OpenTab` carries it; `ApplySave` is called only by `FileSave`; new types have no prior consumers; the `SaveAsChoice` seam (path plus full spec) is what the T02 §1 dialog and the T02 §4 EOL menu fill; §7 prompts and §8 share the engine untouched.
- `source-defect`: approve. Encoding list verbatim (dropdown dump plus expanded screenshot); type list and UTF-8 default from the dialog capture; CRLF default from the fresh-tab status dump; no-EOL-dropdown from the capture plus the 3-valued-combo enumeration; read-only and locked redirects from dialog-title dumps; BOM-add divergence from a byte read; Save All order plus continue-on-cancel reconciled across two runs; prefill from the edits dump plus capture. Fresh-install state and stock's unencodable warning stay honestly declared.
- `design`: approve (not applicable). Neutral engine section with no rendered surface; the Save As dialog defers to the named D01 T02 §1 trigger and the EOL choice UI to the named D01 T02 §4 menu. No user guide exists in `docs/` yet, so no guide update is owed.
- `record`: approve. Evidence below matches executed runs; `query deferred` shows none open; all 8 items ticked with named drives; the prefill sanitization gap is noted in code; the File-menu shortcut dump stays in the probe record for D01 T02 §1's audit.

## Gates (on the candidate)

- `dotnet test src/IntelligentNotepad.slnx` (Windows): Smoke 1/1, Unit 119/119, Protocol 35/35, UI 22/22.
- `dotnet test src/Notepad.Neutral.slnf` (Linux): Smoke 1/1, Unit 116 plus 3 OS-skipped, Protocol 35/35.
- `todo-graph.py self-test`: 393 cases, 0 failed. `validate`: 0 fatal, 0 warnings.
