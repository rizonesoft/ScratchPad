# Review: D01 T01 §11 -- App Icon Wiring

Candidate: `22f6b4c` (csproj refs, operator asset, `AppIconTests`) plus the `SetIcon` lines cross-shipped in `9f43493` (§6 commit, `// D01 T01 §11:` comment). Record under review: the §11 evidence notes plus `resources/baseline/app/icon-taskbar-evidence.png`, `resources/baseline/app/icon-window-chrome-evidence.png`, and the `resources/baseline/README.md` line, committed with the stamp.

## Evidence map (item to code)

- Item 1 (exe icon): `ApplicationIcon` plus `Content` copy in `src/IntelligentNotepad/IntelligentNotepad.csproj` (`22f6b4c`); asset `resources/notepad.ico` (131902 bytes, 8 images, valid header, parsed). Driven: `ExeIconMatchesAsset` (extracted exe icon 0-diff vs shipped asset).
- Item 2 (window icon): `AppWindow.SetIcon` at `OnFirstLoaded` (`MainWindow.xaml.cs:507`, shipped in `9f43493`, Loaded-not-ctor with the HWND reason recorded). Chrome is content-extended so no caption glyph surface exists; the handle is the surface. Driven: `WindowChromeIconMatchesAsset` (`WM_GETICON` small 0-diff vs asset 16x16).
- Item 3 (taskbar plus chrome): exe plus window 0-diff pins plus the platform contract (taskbar and Alt+Tab render from those two sources). Driven: committed eyeballed taskbar crop (asset mark, running underline, no default glyph) plus the chrome crop documenting the glyph-free extended chrome.
- Item 4 (commit): message matches `22f6b4c` verbatim.

## Self-review (three questions)

- Does it make sense: yes. The exact outcome (asset in exe, window, taskbar; no default glyph anywhere) with no extra scope; MSIX assets stay D07's per the checkpoint.
- Is it logical: yes. The `Content` copy guarantees the file `SetIcon` reads (the test asserts its presence, so a stale copy fails loud in the suite, not silently at startup); a missing asset would crash `OnFirstLoaded` instead of shipping a default glyph, which is the correct loud failure for a dev-only packaging break. Failure paths agree: no silent fallbacks anywhere in the chain.
- Did it break anything else: no. csproj additions are additive; the `SetIcon` lines already rode the §6 gate green; the two new tests pass and nothing else moved.

## Round 1 lenses

- `adversarial` approve: missing-asset startup crash considered (loud, dev-only, suite-pinned; no swallowing fix, which would hide packaging breaks); icon handles correctly unowned (`FromHandle` wrapper disposed, window keeps the icon); no elevation, no registry, no network.
- `consistency` approve: test file mirrors `LaunchTests` patterns (`AppExePath`, collection, close discipline); P/Invoke style matches the tree (`ExactSpelling` plus `DefaultDllImportSearchPaths`); evidence captures follow the README's human-judgment class with a Sources line.
- `integration` approve: `Content` copy flows to the output the test reads; nothing here claims the D07 MSIX surface; `Alt+Tab follows the exe by platform contract` stands as stated.
- `source-defect` approve: operator-supplied asset, no Notepad baseline to answer to (the Fidelity line says so); byte and image counts verified by parsing the file header, not by trusting the note.
- `design` approve: both rendered surfaces eyeballed (taskbar mark plus underline, chrome without glyph); text-level pins back both. No unaccounted controls: the icon is the whole surface. User guide: none exists in the tree, N/A.
- `record` approve: the stale 2026-09-14 proof is superseded visibly, not deleted; the cross-shipped `SetIcon` cites its hash; ticks name their drives; captures filed plus README'd.

## Gate quotes (§11 filter)

- `UI.dll`: `Passed! - Failed: 0, Passed: 2, Skipped: 0, Total: 2, Duration: 891 ms - UI.dll (net10.0)`
- Filter exit 0 through the fixed runner. `TreatWarningsAsErrors` plus `AnalysisMode All` make green mean 0 warnings and analysis clean. `validate`: 0 fatal, 0 warnings.
