# Review: D01 T01 §28 -- UIA Tab Accessibility Names

Candidate: `1475bbb` (SetName rewire through the run-2 formatter, TabAccessibilityTests drives, 9-file assert migration, §28 plan decisions and ticks). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (stock suffixes): both `SetName` sites format through `TabAccessibilityName.For`. Driven: `TabNamesCarryStockSuffixes` with literal punctuation.
- Item 2 (live tracking): `IsDirty` to `RefreshHeader` refreshes the name. Driven: `SuffixFlipsLiveWithDirty` across edit, save, and reopen.
- Item 3 (commit): message matches `1475bbb` verbatim.

## Self-review (three questions)

- Does it make sense: yes. Tab names carry the stock suffixes and track dirty live; the suite asserts the settled suffix everywhere.
- Is it logical: yes. Punctuation single-sourced in Core; the migration asserts each tab's reasoned settled state, confirmed file by file.
- Did it break anything else: the rewire touched every tab-name assert by design; all migrated files went green focused, and the full gate proves the rest.

## Lenses (single round, no findings)

- `adversarial` approve: pure string function, null display names throw loud; no new surface or input path.
- `consistency` approve: one formatter, two call sites, suite-wide asserts through it; punctuation pinned literally in the neutral tests and the new drives.
- `integration` approve: reuses the §2 dirty flag and the §3 refresh path with no new hookup; no product code reads UIA names, so only tests migrated.
- `source-defect` approve: both punctuation halves live-proven against stock (`PROBE22. Modified.`, `Untitled. Unmodified.`); no difference to record.
- `design` approve: accessible names only, no visual change. No user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives; the stale-tab correction (discarded restore reads clean) is reasoned from the discard, confirmed focused.

## Gate quotes

Full gate on `1475bbb` (`build/gate-last.log`, exit 0, no strays, first try):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 259, Skipped: 0, Total: 259 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 115, Skipped: 1, Total: 116 - UI.dll` (the skip is the pre-existing quarantine)

`validate` 0 fatal, `self-test` 393/393, build 0 warnings.
