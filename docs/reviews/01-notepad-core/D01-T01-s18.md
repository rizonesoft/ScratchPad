# Review: D01 T01 §18 -- Export as Markdown, HTML, Plain Text

Candidate: `29b9f2f` (shared FormatConverter, ExportDialog, Ctrl+Shift+X opener, ExportTests plus FormatConverterTests). The gate quotes below ran on the full candidate.

## Evidence map (item to code)

- Item 1 (export writes all three formats): `ExportDialog` converts the live buffer through `FormatConverter` and writes beside the source via `FileSave` (UTF-8 no-BOM CRLF); default name `{basename}-export` never overwrites the source. Driven: `ExportMarkdownWritesConvertedFile` (exact bytes, source untouched), `ExportHtmlRendersStructure` (exact document plus structural tags), `ExportPlainStripsMarkers` (exact text, link kept as `text (url)`), `UntitledShowsSaveFirstNote`.
- Item 2 (fidelity fixtures): `FormatConverterTests` 13/13 pin headings, emphasis/strong, code spans, links, both list kinds, paragraph breaks, literal fallbacks, the document shell, and escaping.
- Item 3 (commit): message matches `29b9f2f` verbatim.

## Self-review (three questions)

- Does it make sense: yes. A Ctrl+Shift+X dialog exporting the buffer to Markdown, HTML (document shell), or plain text beside the source, over a shared UI-free converter copy-as will reuse; copy-as itself stays out per the split.
- Is it logical: yes. Conversion is single-homed in `FormatConverter` (the dialog never formats); the subset and every literal fallback are pinned; writes go through the §5 atomic writer; untitled is a save-first state mirroring §16.
- Did it break anything else: no. Additive dialog plus one accelerator plus one guard flag; `FileSave` and tab paths untouched.

## Lenses (single round, no findings)

- `adversarial` approve: buffer text is inert input (escaping pinned, no script execution surface); the name box directs the write like a Save As filename (a `..` segment navigates, platform-conventional; no privilege boundary is crossed since the write runs as the user through the atomic §5 writer); invalid names fail gracefully inline with nothing half-written.
- `consistency` approve: dialog mirrors TemplatesDialog (injected seam, guard flag, status text, save-first state); converter is UI-free in Notepad.Core like its siblings; tests mirror the suite's per-file helpers.
- `integration` approve: §5 (`FileSave` plus spec) reused, none forked; trigger contract recorded both sides with the menu owner; the shared converter exposes the fragment/document split copy-as needs; Ctrl+N/Ctrl+T paths untouched.
- `source-defect` approve: new build, no baseline; no Notepad surface claimed.
- `design` approve: name box, all three export buttons, status text, and save-first note accounted for; default name asserted; panel renders default WinUI chrome. No user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives; deferrals: none.

Advisories (noted, not blocking): (a) typing a custom export name is undriven in the room (the default value flows through the same `Text` read on every export, so the control is proven, only the keystroke path is not). (b) The empty-name button disable is undriven (defense in depth; the export seam also rejects empties). (c) `ExportBuffer` reads the live buffer including unsaved edits, so an export can differ from the file on disk; that is the section's "buffer text" letter.

## Gate quotes

Final full gate on the candidate (`29b9f2f`), exit 0:

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 203, Skipped: 0, Total: 203 - Unit.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll (net10.0)`
- `Passed! - Failed: 0, Passed: 84, Skipped: 1, Total: 85 - UI.dll (net10.0)` (the skip is the pre-existing `TabBarTests` ctxmenu-name-race quarantine, not §18)
