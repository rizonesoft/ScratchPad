# D01 T01 §29 review: Open with Explicit Encoding

Candidate: `5a7bf3a` (single commit). Neutral engine section: option-list spec plus `FileOpen` decode override; rendered picker defers to the D01 T02 §1 Open trigger.

## Self-review

- Makes sense: the four items map to the diff exactly (list spec, override, null-equivalence, open-tab-save flow); the T02 §1 trigger note and the README index line are required contract/index updates, not scope.
- Logical: forced names decode with replacement fallback and match-only BOM strip; null delegates to `Detect(bytes)`; unknown names throw; `.LOG` stamping and failure paths sit outside the override and behave identically. Name and BOM flag stay independent per the §5 model (forcing "UTF-8 with BOM" on BOM-less bytes records the name with `HasBom` false, and save follows the flag), consistent, not a finding.
- Breakage: `OpenOptions` gains an optional init (positional construction unchanged), `Detect` gains an overload (existing callers untouched), `OpenDialogDefaults` is additive; `FileSave` and `TabModel` untouched. Blast radius nil.

## Lenses (single round, no findings)

- `adversarial` approve: unknown names throw loud; forced decode never throws (replacement fallback), so no hostile bytes fail an open; empty name throws; pure functions, no races.
- `consistency` approve: names reuse the `FileOpen` constants, tail tied to `SaveDialogDefaults.OfferedEncodings`; name/BOM independence follows the §5 model; trigger note follows the §§14-19 `Recorded` pattern on the menus trigger.
- `integration` approve: `OpenOptions` gains an optional init (existing constructions unchanged), `Detect` an overload (existing callers untouched), defaults additive; `.LOG` and failure paths sit outside the override; app launch path passes null today and is unaffected.
- `source-defect` approve: option list pixel- plus UIA-proven against stock (`Auto-Detect, ANSI, UTF-16 LE, UTF-16 BE, UTF-8, UTF-8 with BOM`); forced-decode semantics (replacement fallback, match-only BOM strip) recorded as defaults with costs, no parity claim beyond the list.
- `design` approve: neutral section, no surface; no user guide exists in the tree, N/A.
- `record` approve: item ticks name their drives; the probe incidents (build-hang kills, stock-window tidy) are run-logged, not hidden.

## Gate quotes

Full gate on `5a7bf3a` (`build/gate-last.log`, exit 0, no strays, first try):

- `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1 - Smoke.dll`
- `Passed! - Failed: 0, Passed: 268, Skipped: 0, Total: 268 - Unit.dll`
- `Passed! - Failed: 0, Passed: 35, Skipped: 0, Total: 35 - Protocol.dll`
- `Passed! - Failed: 0, Passed: 115, Skipped: 1, Total: 116 - UI.dll` (the skip is the pre-existing quarantine)

`validate` 0 fatal, `self-test` 393/393, build 0 warnings.
